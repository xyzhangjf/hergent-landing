# H4 完成报告 — 订单明细导入 + gl_dimensions 完整 CRUD + 重复条码冲突检测告警

> 审计任务来源：`hergent-audit-task-tracking.md` 第三节 🔴 H4「订单明细导入（解领域引擎）」
> 完成日期：2026-07-24
> 提交：`0ccf972`（主：订单明细导入 + gl_dimensions CRUD + 条码冲突）、`6c77d76`（H4.3 修复：冲突改专用表）
> 状态：✅ 已部署生产（47.113.224.140:8700）并端到端验证

---

## 一、目标拆解

| 子任务 | 目标 | 解锁价值 |
|---|---|---|
| **H4.1** 订单明细导入 | 向已有销售订单写入 `sale_order_items` 明细行 | 解开 M1/M3/M4 领域引擎（盈亏快报 / 专属价匹配 / 需求预测卡）的真值数据 blocker |
| **H4.2** gl_dimensions 完整 CRUD | create / read / update / delete + 输入校验 + 错误处理 | 补完此前 500 的 `/api/gl/*` 半成品功能（db 层函数此前全部缺失） |
| **H4.3** 重复条码冲突检测与告警 | 批次内重复 + 已存在库冲突检测；记录冲突详情、明确错误；支持导出 / 标记处理 | 把 H3 残留的「业务数据重复条码」从静默忽略升级为可审计告警 |

---

## 二、改动文件清单（后端 `server/`）

| 文件 | 动作 | 关键内容 |
|---|---|---|
| `server/db/queries/gl_dimensions.py` | 新建 | 维度 `gl_dimension_*` + 维度值 `gl_dimension_value_*` 完整 CRUD；`gl_trial_balance_dimensional()` 维度试算预览；`coa_expand()` 扩展科目表；内置 `_ensure_tables(db)` 幂等建表 |
| `server/routers/gl_dimensions.py` | 重写 | Pydantic 模型（`GlDimCreate`/`GlDimUpdate`/`GlDimValueCreate`/`GlDimValueUpdate`）；**补回缺失的 `PUT /api/gl/dimensions/{code}`**；清晰错误：`ValueError→400` / `IntegrityError→409(_dup)` / 找不到 `→404` |
| `server/db/queries/sales.py` | 编辑 | 新增 `sale_order_item_create(order_id, product_id, quantity, unit_price, db_conn=None)`：复用导入事务连接写、自动重算 `total_amount`（保留原 `discount`） |
| `server/routers/import_router.py` | 扩展 | `COLUMN_PATTERNS` 新增 `order_items` 类别；products 导入前**预扫描**条码冲突；新增 `elif category=="order_items"` 分支；返回体新增 `barcode_conflicts`；模板新增 order_items 列；新增端点 `detect-barcode-conflicts` / `barcode-duplicates`(扫库) / `barcode-conflicts`(列表) / `barcode-conflicts/{cid}/resolve` / `barcode-conflicts/export`(CSV) |
| `server/db/queries/products.py` | 编辑 | `barcode_conflicts_in_records()`（批次内+库内重复）、`find_duplicate_barcodes()`（扫库）、`log_barcode_conflict()`（改写入专用 `barcode_conflicts` 表 + 轻量镜像 `alert_history`）、`list_barcode_conflicts()`、`resolve_barcode_conflict()`；`_ensure_barcode_conflicts_table(db)` 幂等建表 |
| `server/erp_db.py` | facade 接线 | import 块追加 `gl_dimension_*` / `sale_order_item_create` / `barcode_conflict*` 并 re-export |

---

## 三、关键设计决策

- **连接池 re-entrant 安全**：导入事务内调用 `sale_order_item_create(db_conn=conn)` 时，函数若自调用 `get_db()` 会返回一次性（throwaway）连接，不污染外层事务缓存，**不会提前 commit 外层导入事务**（依赖 H3.2 连接池机制）。
- **租户解析一致性**：`/api/import` **不在** `_TENANT_REQUIRED_PREFIXES` 内 → 解析到 **master `erp.db`**（所有 import 类别共享的旧行为，H4.1 与之对齐）。订单导入默认写入 master 库。
- **条码冲突专用表**：新建 `barcode_conflicts`（字段：`cid, category, ref_value, barcode, conflict_type, detail, resolved, created_at`）存完整冲突明细 + `resolved` 标记；`log_barcode_conflict` **同时按真实 `alert_history` schema 镜像轻量告警**（`metric='barcode_conflict'` / `severity` / `message`，无 `rule_name` / `ref_type` 列——修复了首版误写 `alert_history(rule_name,...)` 导致的 500）。
- **部分唯一索引**：`idx_products_barcode WHERE barcode!=''` → 空条码永不冲突，仅非空重复触发报错，符合业务实际。
- **幂等建表**：所有新增表走 `_ensure_*` / `_safe_migrate`，重启不刷屏。

---

## 四、验证证据（生产实测 47.113.224.140:8700）

| 项目 | 结果 |
|---|---|
| 健康 `GET /api/health` | **200**（复测 2026-07-24） |
| gl_dimensions CRUD | list 200；create 重复 code → **409**；update 缺失 code → **404**；delete 级联删子值 200 |
| 订单明细导入 H4.1 | 导入 2 行到测试订单 → `success=1`；订单 `total_amount` **自动重算 = 17.5**（保留 discount 逻辑）；验证后清理测试数据 |
| 条码冲突 预扫描 | products 导入预扫描：冲突行跳过并写 `barcode_conflicts` + `alert_history` 镜像 |
| 条码冲突 扫库 | `GET /api/import/barcode-duplicates` → 发现 **9 组**真实重复条码（含 v89 椰乳冲突） |
| 条码冲突 管理 | `list`(200) / `export`(CSV 200) / `resolve`(标记已处理 200) 全通 |
| 路由挂载复检 | 无鉴权访问 `/api/gl/dimensions` 与 `/api/import/barcode-duplicates` 均返 **401 envelope（非 500）**，证 db 函数已接线、路由已挂载 |
| 回归 | 正常商品导入 `success=1`，无回归 |
| 生产数据残留 | 临时 token / 测试订单 / 测试冲突行 **全部清理，零残留** |

---

## 五、已知限制 / 后续

1. **订单导入解析 master 库**：当前 `/api/import` 非租户必需，订单明细写入 master `erp.db`。若需按租户隔离导入，须把 `/api/import` 纳入 `_TENANT_REQUIRED_PREFIXES`（影响所有 import 类别，范围外，待用户拍板）。
2. **生产库仍 0 行明细**：导入通道已通，但**真实 `sale_order_items` 仍为 0 行**——领域引擎（盈亏/专属价/预测）需用户侧导入真实订单明细才能跑出真值（数据问题，非代码）。
3. **gl 维度试算为预览**：`gl_trial_balance_dimensional` 注明 `journal_entries` 尚未带维度标签，返回维度结构 + 值列表，非完整会计维度核算。
4. **GitHub 推送 pending**：沙箱出网阻断，`upgrade/v84-international` 本地领先 2 提交未推；待网络可用环境执行 `--force` 推送。

---

## 六、部署铁律遵守

commit 进 git（`0ccf972` / `6c77d76`）→ 精确 scp（禁通配符）→ `chown hergent:hergent` → `find -name '*.pyc' -delete` → `IMPORT_OK` 预检（`python3 -c "import routers.gl_dimensions; import routers.import_router"`）→ `systemctl restart hergent-erp` → health 200 + 路由挂载 401 验证。

> 与既有 H4（commit `ed7b1bd` 修复 prepay-refunds/bpm/salary-send 残留 500 端点）**不是同一任务**——本报告的 H4 指审计任务「订单明细导入」，请勿混淆。
