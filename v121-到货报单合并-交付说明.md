# v121 到货/报单节奏合并 —— 交付说明（2026-09-08）

## 结论
「到货模式」与「智能创建/关闭报单表」**合并为一个卡片**，本质是**统一节奏为单一数据源**（报单语义为锚点，到货日派生），不是简单拼卡片。

**判断依据（回代码核实，非推测）**
- 后端 `domain/arrival_schedule.py:165-168` 已定分工：`arrival_*`=到货语义→服务均单；`order_*`=报单语义→服务报单表。二者由 `order_lead_days` 桥接（到货日 = 报单日 + lead）。
- **两处硬冲突**：① 锚点不同 —— `arrival_first_dom` 是**月内日**（每月重置、跨月畸变）vs `order_first_date` 是**绝对日期**（跨月连续）；② 同一「到货」两个数据源 —— 均单走 `arrival_*`、预览表走 `order_*` 派生，可算成两个结果。
- 实例危害：`arrival_cadence=2`（均单按 15 次）却配 `order_cadence=3`（预览到货 10 次）→ 均单 6 万/次 vs 实际 9 万/次，返利冲刺看板算错。

## 合并后卡片结构（Rebate.vue）
标题：**到货与报单节奏**（单卡片，三分区）

| 分区 | 字段 | 层级 |
|---|---|---|
| **① 节奏** | 报单模式(间隔/星期) → 首次报单日 + 报单周期 **或** 报单星期 → 提前天数(天到货) → 最多可提前 | 唯一输入 |
| **② 到货产出** | 本月到货次数(可覆盖) + 均单(只读派生) + 未迁移时的一致性告警与「从旧排程反推」按钮 | 只读 |
| **③ 报单自动化** | 手动/自动建表 → 三时点 → 未来 6 期预览表 | 开关控制 |

已删除的重复输入项：`到货模式` / `首次到货日(月内日)` / `到货周期(天)` / `到货星期`。
新增派生提示：「到货日 = 报单日 + N 天（自动派生，不用再填到货排程）」。

## 改动清单

**后端（hergent-erp，工作区未提交）**
- `domain/arrival_schedule.py`
  - 新增 `compute_arrival_dates_from_order()` —— 由报单日序列派生某自然月到货日（跨月连续）
  - `arrival_summary()` 改**双轨**：已配 `order_first_date` 走报单派生，否则回退 `arrival_*`；返回体新增 `source` 字段（`'order'` / `'arrival'`）
- `routers/rebate_rules.py`
  - `/arrival-preview` 新增 `order_*` 5 个透传参数；用 `None` 作「未传」哨兵、空串表示显式清空（退回 arrival_* 口径）

**前端（hergent-cn-v2，工作区未提交）**
- `src/pages/Rebate.vue`
  - 两块 `.arrival-block` 合并为一块，三分区（`.ap-sec` / `.ap-sec-note` / `.ap-derive` 新样式）
  - `loadArrivalPreview()` 改为透传 `order_*`；watch 增加 `order_*` 依赖（改节奏即时重算均单）
  - 新增迁移弹窗：`openMigrate()` 由旧 `arrival_*` 反推 `order_*`（interval：当月首到日 − lead；weekday：到货星期 − lead 天）、`applyMigrate()` 写入表单，需用户确认
  - 保存校验由「到货星期」改为「报单星期」（未迁移规则默认 interval，不误伤）
  - 删除死代码 `isWk` / `toggleWk`

## 验证结果
| 项 | 结果 |
|---|---|
| 后端语法 | ✅ `py_compile` 通过 |
| 纯函数 | ✅ 蒙牛首报 9/1 cadence=2 lead=4 → 9 月到货 **13 次**（9/5 起每 2 天）；简爱周二五 +lead4 → **8 次** |
| 端点双轨 | ✅ `order_first_date=2026-09-01` → `source=order count=13 均单 6.9231 万/次`；空 → `source=arrival count=15 均单 6.0 万/次` |
| 前端构建 | ✅ `Rebate-D0iVD2g0.js` 126.55 kB，2.07s |
| 生产落地 | ✅ 后端 2 文件 md5=工作区、服务 `active`、health 200；前端 `index.html` 200、新资源已上线 |
| bundle 校验 | ✅ 新标记（只填一次/到货产出/报单自动化/迁移弹窗）在；旧输入项（到货模式/到货周期）已移除 |

## 兼容性与影响
- `arrival_*` 列**一律保留不删**（兼容老数据与报表），仅降为「未迁移时的回退口径」，不做静默改写。
- **均单口径会变**：迁移后到货次数由「每月 N 号起」变为「首次报单日 + lead 起」，例：9 月由 15 次(6.0万/次) 变 13 次(6.92万/次)。这是**正确的新语义**（首次报单→首次到货隔着提前期），迁移弹窗已明确提示，且需用户点「确认换算」。
- 未点迁移的规则行为完全不变（回退 `arrival_*`），零破坏性。

## 遗留
- 两仓改动**未 commit / 未 push**（与之前的 3789 行历史未提交项一起，待统一整理）。
- `deploy.sh` 的 `--delete` 噪音（`.hermes` / `backups/` 等非源码目录）仍未处理，不影响运行。
- v121 后续（P3 调度执行、P4b 管理员重开、P4c 时刻判定、P6 期次表+10 列、P7 触达）仍待排期。
