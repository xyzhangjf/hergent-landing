# 副驾「一次提问两条矛盾回复」修复 — A/B/C/D 四项全做 · 交付说明

日期：2026-09-13
触发：用户实测提问「算一下这个月货损」，副驾回了 **2 条结论相反**的消息，错误结论（「库存健康」）已推送至企微。

---

## 一、交付

| 项 | 改动 | 效果 |
|---|---|---|
| **A** 前端出卡守卫 | `CopilotDrawer.vue` 判据由「只看 ```cards 复数围栏」扩充为「AI 已出卡（任意形态）」 | **不再出现两条 assistant 消息**；AI 越守协议，兜底越不会补卡 |
| **B** 判据区分「真零/没数据」 | `batch_tracker.scan_expiry` 暴露 `unscanned_batches`；`server.py` loss-card 改三态判据 | 「效期全空」不再渲染成全绿「库存健康」，缺数据显式暴露 |
| **C** 口径对齐 | 卡标题/指标「本月货损」→「近 30 天货损」 | 标签与实现（`wastage_summary(30)` 滚动窗口）一致 |
| **D** 兜底卡不推 IM | `useCardTrigger.pushCard` 移除推送调用 | 只有带口径披露的 **AI 主回复**进老板企微 |

---

## 二、变更明细

### A — 前端出卡守卫（`src/components/CopilotDrawer.vue`）

```js
// 原：cardIntent 只认复数 ```cards；协议却要求 AI「出单个 ```card 就别再出 ```cards」
if (cardIntent) { ... } else if (!DENY_RE.test(q)) { triggerCards(q) }

// 现：已出卡（任意形态）即不再兜底
const aiCarded = !!(cardIntent || (replyMsg && replyMsg.card))
if (cardIntent) {
  if (cardIntent.show && cardIntent.show.length) fireCards(cardIntent.show, q)
} else if (!aiCarded && !DENY_RE.test(q)) {
  triggerCards(q)
}
```

**根因**：`extractCard`（渲染）认单数 ```card，`extractCardIntent`（判据）只认复数 ```cards —— 两个正则不同名，形成契约漏洞：**AI 越守协议，前端越判定「AI 未出卡」而再补一张**。

### B — 判据不再把「没数据」读成「零风险」

**`server/domain/batch_tracker.py::scan_expiry`** 新增两个返回字段：

```python
"total_batches": total_batches,                              # 在库批次总数
"unscanned_batches": max(0, total_batches - len(items)),     # 未纳扫（没录到期日 / 日期不可解析）
```

（原 SQL 带 `WHERE i.expiry_date != ''`，未录效期的批次被整体排除，且**总数也无从得知**。）

**`server/server.py::ai_loss_card`** 判据改为三态：

| 指标 | 旧 | 新 |
|---|---|---|
| 临期风险值 | `at_risk>0 ? warn : **good**` | `at_risk>0 ? warn : (效期不全 ? neutral : good)` |
| 临期 SKU → **临期批次** | 填 `total_items`（含正常批次） | 填四档风险 count 之和（**口径修正**） |
| 未录效期批次 | 无 | **新增**，`tone=warn`，`hint=临期预警扫不到` |
| summary（无报损） | 「近 30 天无已审批报损，**库存健康**。」 | 「…但 N 个在库批次没录到期日，临期风险还查不出来 —— **这不等于库存健康**。」 |
| points（无报损） | 「货损可控。」`tone=good` | `tone=warn`「别把它当成「没有临期风险」」 |

### C — 口径对齐

- `"本月货损核算（真实数据）"` → `"近 30 天货损核算（真实数据）"`
- metric `"本月货损"` → `"近 30 天货损"`
- （`source` 字段原本已写「（近 30 天）」，此前标题与之自相矛盾）

### D — 兜底卡不参与 IM 推送（`src/composables/useCardTrigger.js`）

`pushCard()` 内的 `pushRoleReply(..., 'card')` 整段移除。

理由：确定性经营卡只做数值聚合、**没有 LLM 主回复那套口径披露**；一旦判据出错，错误结论会直接进老板企微（本次实际发生）。推送保留给 AI 主回复。

### 顺带修正（同源 / 直接后果）

| 文件 | 内容 |
|---|---|
| `server/scheduler.py` | 定时日报有**完全同源**的「临期 N 个SKU，库存健康」，且也推 IM —— 一并改为三态（效期不全时输出「查不出来」，不输出「健康」），临期数口径同修 |
| `server/server.py:1580` | 经营画像消费 `loss_risk_items`，标签「当前临期 SKU」→「当前临期批次」（随口径修正同步） |

---

## 三、构建与部署

| 端 | 产物 | 核对 |
|---|---|---|
| 前端 | `useCardTrigger-BJImtYTd.js`（`5d852cf9…`）、`index-DhbMQ2tA.js`（`57bc6ede…`）、`Shell-DZnJTaFh.js`（`1b6e1352…`） | rsync + chown，**双侧 md5 MATCH_OK** |
| 后端 | `server.py`（`13ceac45…`）、`scheduler.py`（`2d11cf6a…`）、`domain/batch_tracker.py`（`6f8443a6…`） | scp 具名文件 + chown + 清 `__pycache__` + restart，health **200**、active |

回滚备份：`/root/backup_v156_20260913/`（三份后端文件）。

---

## 四、验证（全部实测，非推断）

### B / C — 直打生产接口 `/api/ai/loss-card`（真实数据）

**tenant_1（问题现场：54 个在库批次效期全空）**

```
title  : 近 30 天货损核算（真实数据）
summary: 近 30 天没有已审批报损单，但 54 个在库批次没录到期日，临期风险还查不出来 —— 这不等于库存健康。
  - 近 30 天货损: ¥0   tone=neutral
  - 报损单数:     0     tone=neutral
  - 临期风险值:   ¥0    tone=neutral     ← 旧版是 good（绿）
  - 临期批次:     0     tone=neutral     ← 旧版是 good 且口径错
  - 未录效期批次: 54    tone=warn  hint=临期预警扫不到   ← 新增
points: [warn] 54 个在库批次没录到期日，临期预警扫不到 —— 别把它当成「没有临期风险」。
```

**tenant_10（效期完整：正常路径未被误伤）**

```
summary: 近 30 天没有已审批报损单，在库批次效期已录全。
  - 临期风险值: ¥0  tone=good        ← 效期完整时才允许绿
  - 临期批次:   7   tone=warn        ← 旧版会显示 total_items（全部纳扫批次）
  - 未录效期批次：不出现（expiry_complete=True）
```

数据层另验：tenant_1 `total_batches=54 / unscanned_batches=54 / total_items=0 / total_at_risk=0.0` —— 旧版只能看到后两个 0。

### A / D — 前端真机（puppeteer，请求拦截伪造 AI 回复以精确控制「AI 是否出卡」）

| 场景 | 断言 | 结果 |
|---|---|---|
| **A**：AI 出单数 ```card | assistant 消息 = 1；`/api/ai/loss-card` 请求 = 0 | **PASS**（1 / 0） |
| **D**：AI 完全不出卡 | assistant = 2（兜底卡仍在）；`/api/ai/loss-card` ≥ 1；`kind='card'` 推送 = **0**；`kind='reply'` 推送 = 1 | **PASS**（2 / 1 / 0 / 1） |

两次均 **0 pageerror**。A 场景的卡片被正确渲染（标题「本月货损核算 / 数据缺失，先补录效期 / 采纳 / 驳回」），D 场景兜底卡文案已是新的「…在库批次效期已录全。」

---

## 五、需确认事项

1. **已推送的错误卡片仍在企微**：09-13 09:55:32 那条「库存健康」已进你手机，本次修复不会撤回它。若需要，可补发一条更正，或下轮日报自然覆盖。
2. **`push_scope='card'` 语义变化**：后端仍支持 `'card'`，但前端已不再发起 `kind='card'` 推送 —— 若有角色配了「只推卡不推回复」，这些角色今后将**收不到**任何推送。需要保留该场景的话告诉我，我改成「仅 AI 出卡时推、兜底卡不推」的折中。
3. **`loss_risk_items` 指标口径变更**：自本次起由「全部纳扫批次」修正为「临期批次数」，`daily_logs` 历史序列存在口径跳变（9-13 之前为旧口径）。
4. **同源风险未清**：`payroll-card` / `rebate-card` / `forecast-card` 三个聚合接口**仍可能是「零值即健康」**的写法（本次只按授权范围改了 loss-card）。建议后续做一次同类审查。
5. **真实数据缺口**：tenant_1 的 54 个在库批次到期日全空、报损单 0 行 —— 这是 AI 第一条回复就点出的真问题，也是「临期预警」功能当前完全失效的原因。是否要做一个批量补录入口？
6. **本地改动未 commit**：前端 2 文件（`hergent-cn-v2`）、后端 3 文件（`hergent-erp`）均在途。要单独提交吗？
