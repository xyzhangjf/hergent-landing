# 本期预报：默认期次兜底 + 报单侧停售拦截（2026-09-12）

## 一、你的四个决定 → 落地情况

| # | 问题 | 你的选择 | 状态 |
|---|---|---|---|
| 1 | 期次默认选中谁 | **a**：后端兜底「最近 open，无则最近创建」 | ✅ 已上线并验证 |
| 2 | 停用商品要不要显示 | **b**：报单侧禁止选停用 | ✅ 已上线并验证（含一个隐藏缺口，见第五节） |
| 3 | 是否改按 period_id 归属 | 你没看懂 → 见第四节解释 | ⏸ 待你点头 |
| 4 | 是否重开期次 9 | 不需要（测试阶段） | ⏸ 未动 |

---

## 二、改动 1a：默认期次兜底

**问题**：所有期次都关闭时，接口返回「没有当前期次」→ 前端把查询区间兜底成「今天~今天」→ 打开页面就是空表，看起来像"小程序报的数据没同步"。

**改法**（`server/erp_db.py` 新增 `forecast_period_default()`；`server/routers/forecast.py` 的 `/api/forecast/periods` 改用它）：

优先级：
1. 今天在报单窗口内的 open 期次（原口径不变）
2. 最新创建的 open 期次（原口径不变）
3. **最近有报单数据的期次**（新增）
4. **最新创建的期次**（新增，不限状态）

⚠️ **关键设计**：没有改动原来的 `forecast_period_current()`，而是新开一个函数。因为原函数被两处**写路径**依赖（Web 补录自动挂期次 · `forecast_audit.py:449`；AI 助手工具 · `hermes_core.py:711`），放宽它会静默改变数据落库归属。

**验证（生产活体）**
- 改前：`current = None`
- 改后：`current = 9「9月提审期-开放填报」`（窗口 08-30 ~ 09-15）
- 默认视图实测：`GET /api/forecast-submissions/summary?start=2026-08-30&end=2026-09-15` → **rows=4**，不再空表

---

## 三、改动 2b：报单侧停售拦截

**现状核查（先查证再动手）**
- 小程序选品接口 `/api/products/fill-search` — **本来就过滤停用** ✅
- Web 补录选品接口 `/api/forecast-audit/products` — **本来就过滤停用** ✅
- 但 **提交接口不校验**，且「一键带入上次报单」会把后来被停用的商品带进来 → 落库后 Web 端必然看不到（这正是 1160/1196 消失的成因之一）

**改法**（`server/routers/forecast_submissions.py`）
- 落库前查一次主档 `is_active`，**停售明细直接剔除**，回传 `skipped_inactive: [商品名]`
- 全部明细都被剔除 → `400 所选商品已停售：XXX，请移除后再提交`
- 主档查询失败（老库缺列 / 连接异常）→ **不剔除**（宁可放行也不误杀整单）

**小程序配套**（`pages/fill/fill.js`）：成功后弹窗提示「已自动跳过 N 项已停售商品」，并把它们从「上次报单」快照里剔除，避免下次一键带入又被拦一次。

**验证（生产端到端，临时建一期）**

| 用例 | 输入 | 结果 |
|---|---|---|
| A | 在售 1161×2 + 停售 1160×3 | ✅ `total_qty=2`、`skipped_inactive=[友芝友…]`、DB 明细只有 1161 |
| B | 仅停售 1160×3 | ✅ `HTTP 400 所选商品已停售：*友芝友…，请移除后再提交` |

**验证痕迹已清理**：临时期次已关闭+删除、测试提交已删、孤儿明细 0、期次恢复 7/8/9/10 全 closed。

---

## 四、问题 3 的大白话解释（你说没看懂的那条）

**现在系统判断"这笔报单属于哪一期"，看的是日期，不是编号。**

举例：
- 期次 9 的窗口 = 8月30日 ~ 9月15日
- 期次 10 的窗口 = 9月3日（单日）

9月3日**同时落在两个窗口里**。所以那天报的单子，在「期次 9 的视图」和「期次 10 的视图」里**都会出现一次** → 同一笔被算两遍。**只要两个期次的日期窗口重叠，就会重复统计。**

**改法**：每笔报单提交时其实**已经记了自己的期次号**，汇总表直接按这个号筛，就不重复了。

**代价**：老数据（Excel 导入的 60 条）期次号是 0（当时没记），改成按号筛后这 60 条会消失 → 需兜底（期次号为 0 的仍按日期算）。

**建议：值得做。** 理由：
- 你说"测试阶段、还没真用起来" → 那 60 条测试数据不重要，改动很干净
- 这个错**很难自己发现**（数字看着是对的，只是被放大了）
- 属定时炸弹：只要运营建了重叠窗口的期次（很常见，如"9月整月 + 9月3日单日"）就会踩

**回「3 做」或「3 不做」即可。**

---

## 五、⚠️ 已知残留（选 b 带来的，必须知道）

b 只堵住"**报单时已经停售**"，堵不住这两类：

1. **已经报上来的停售商品** — 如 1160、1196，历史已入库，交叉表里**依然不显示**（选 b 就不做"追加标记行"）。当前是测试数据，可不管。
2. **报完之后才被停用的商品** — 报单时在售 → 能入库 → 之后商品被停用 → 那笔数据在交叉表里**消失**。

第 2 类是 b 方案的固有缺口。将来真实数据遇到，处理办法：临时启用该商品 → 导完数再停用。

---

## 六、改动清单与部署

| 文件 | 改动 |
|---|---|
| `server/erp_db.py` | +`forecast_period_default()`（展示用途兜底，**不动**原函数） |
| `server/routers/forecast.py` | `/periods` 的 `current` 改用新函数 |
| `server/routers/forecast_submissions.py` | 落库前剔除停售明细 + `skipped_inactive` + 明确 400 文案 |
| 小程序 `pages/fill/fill.js` | 弹窗提示跳过项 + 快照剔除跳过项 |

**部署方式**：scp 具名文件到 `/opt/hergent-erp/` 根目录（生产扁平布局）。

⚠️ **未使用仓库根 `deploy.sh`** —— 它的 `--delete` 会删掉生产机上 `hermes-engine/`、`.hermes/`、`.cache/`、`incident_*/` 等运行时目录（dry-run 已确认）。该风险之前记过一次，本次又差点踩到，已强化到记忆。
⚠️ 另：`rsync -aR --delete` 的 `/./` 截断在本机不生效，会把文件推到 `/opt/hergent-erp/server/` 子目录（本次已发生并清理），改用 **scp 显式目标路径**逐个部署。

服务已重启，`/api/health` = **200**。

---

## 七、验证状态（诚实标注）

- ✅ 后端两项：生产**活体接口**验证通过（真实租户 1、真实商品数据、真实报单写入）
- ✅ 小程序改动：`node --check` 语法通过
- ⏸ 小程序改动**尚未在开发者工具/真机验证** —— 需你点一次「编译」，下次报单即可看到新提示
