# 订单模块重构方案: showOrderForm(config) 配置驱动

> 日期：2026-08-08 | 目标：8 个订单模块复用访销订单核心交互，按参数化配置驱动业务差异
> 基准：Hergent ERP v107.117 | 截图：舟谱 8 个订单创建页

---

## 一、现状评估

### 1.1 后端接口覆盖情况

| 模块 | 接口 | 状态 |
|------|------|------|
| 销售（访销/车销/自提复用） | `POST /api/sale-orders` | ✅ 已有，v107.117 |
| 销售退货 | `POST /api/return-orders` | ✅ 已有，含 `sale_order_id` 关联 |
| 采购 | `POST /api/purchase-orders` | ✅ 已有，含 confirm/approve/partial-receive/batch-create |
| 采购退货 | `POST /api/purchase-return-orders`（或 `purchase-orders/return`） | ⚠️ 需确认（grep 显示有 `purchase-return-settlements`，但 create 接口需核实） |
| 调拨 | `POST /api/transfer-orders` | ✅ 已有，含 complete |
| 车销装载/卸货 | `POST /api/vehicle-load`、`/vehicle-return` | ✅ 已有（车销订单的实操接口） |

**结论**：**8 个模块后端接口基本齐全**，无重大开发工作。差异集中在：
- 采购退货的 create 接口（如不存在则需补一个）
- 各接口的 payload schema 微调（已有字段足以覆盖差异）

### 1.2 前端代码现状

- `static/app.js` ~7000 行（铁律：只读不写，但已写入多次）
- `_vsfSaveAndClose`（v107.117）— 访销订单提交函数，~150 行
- `showVisitSaleForm`（v107.117）— 访销订单表单构建，~250 行
- `_vsfValidate`、`_vsfCheckStock`、`_vsfApiRetry`、`showVsfReceipt` — 校验/库存/重试/回执
- 字段 UI 助手：`initSearchableSelect`、`_vsfOpenCustomerPicker`、`_vsfOpenOperatorPicker` 等

**没有现成的"通用订单表单"组件**。当前是"按访销订单定制"，未来扩展只能复制粘贴。

---

## 二、设计原则

### 2.1 单一原则

> **业务差异靠配置（config）解决，**不靠代码分支（if/else）。

8 个订单模块的差异点本质是：
- **接口地址**（哪条 URL）
- **字段组合**（客户/供应商/调出仓/调入仓）
- **商品列配置**（退货价/采购价/数量/小单位库存/大单位库存）
- **业务约束**（必须选原单、退货原因、预计生产日期）

把差异点提取成 config 对象，**底层复用一套组件 + 交互逻辑**。

### 2.2 向后兼容

- 现有 `showVisitSaleForm` / `_vsfSaveAndClose` 内部调用新版 `showOrderForm({type:'visit_sale', ...})`
- 现有调用方（菜单入口/营业员/详情页跳创建）零改动
- 截图所见的"创建访销订单"页面行为完全不变

### 2.3 渐进式实施

不一次性铺开 8 个模块。分阶段：
1. **第 1 周**：抽出 `showOrderForm(config)` 通用框架 + 验证访销订单照旧
2. **第 2 周**：补 4 个销售变体（访销退/车销/车销退/自提/自提退）
3. **第 3 周**：补采购订单、采购退货
4. **第 4 周**：补调拨订单 + 全量测试

---

## 三、技术方案：showOrderForm(config)

### 3.1 核心架构

```
┌─────────────────────────────────────────┐
│         showOrderForm(config)            │  ← 通用入口
├─────────────────────────────────────────┤
│   渲染层 (Build Order Form Overlay)     │  ← 按 config 渲染字段 + 表格
├─────────────────────────────────────────┤
│   交互层 (Inline Behaviors)              │  ← showPicker/searchable/stock
├─────────────────────────────────────────┤
│   校验层 (validate(config, items))       │  ← 行级 + 全局校验
├─────────────────────────────────────────┤
│   提交层 (saveOrderForm(config, body))   │  ← 幂等 + 库存 + 重试 + 回执
└─────────────────────────────────────────┘
```

### 3.2 config schema 设计

```javascript
{
  // ----- 元信息 -----
  type: 'visit_sale',                           // 模块唯一标识
  title: '创建访销订单',                          // 标题
  endpoint: '/api/sale-orders',                  // POST 接口
  statusFlow: ['draft', 'pending', 'signed'],    // 状态机（决定按钮文案）

  // ----- 字段区（顶部） -----
  fields: [
    { key: 'customer',  label: '客户',    required: true, type: 'customer_picker' },
    { key: 'employee',  label: '业务员',  required: true, type: 'employee_picker' },
    { key: 'department',label: '部门',    required: false, type: 'department_picker' },
    { key: 'warehouse', label: '销售仓库', required: true, type: 'warehouse_picker' },
    { key: 'warehouseOut', label: '调出仓', required: true, type: 'warehouse_picker', when: { type: 'transfer' } },
    { key: 'warehouseIn',  label: '调入仓', required: true, type: 'warehouse_picker', when: { type: 'transfer' } },
    { key: 'orderDate', label: '单据日期', required: true, type: 'date', default: 'now' },
    { key: 'deliveryDate', label: '预提货日期', type: 'date' },
    { key: 'deliveryTime', label: '预配送时间', type: 'time' },
    { key: 'driver',    label: '送货司机', type: 'employee_picker' },
    { key: 'vehicle',   label: '车辆',     type: 'vehicle_picker' },
    { key: 'note',      label: '备注',     type: 'textarea', maxLength: 500 },
    { key: 'urgent',    label: '加急',     type: 'select', options: ['普通','加急'] },
    { key: 'payment',   label: '付款方式', type: 'select' },
    { key: 'reason',    label: '退货原因', type: 'select', when: { type: /_return$/ } },
    { key: 'relatedOrder', label: '原订单', type: 'order_picker', when: { type: /_return$/ } },
  ],

  // ----- 商品明细表（中部） -----
  itemsTable: {
    columns: [
      'product_name', 'barcode', 'unit',
      { key: 'price', label: '销售价(折扣)', showIn: /^sale/ },
      { key: 'cost_price', label: '参考成本价', showIn: /^purchase/ },
      { key: 'actual_price', label: '实际价', showIn: 'sale', default: true },
      { key: 'return_price', label: '退货价(折扣)', showIn: /_return$/ },
      { key: 'quantity', label: '订单数量', showIn: 'sale' },
      { key: 'return_quantity', label: '订单数量(退货)', showIn: /_return$/ },
      { key: 'quantity_combined', label: '订单数量(综合)', showIn: 'transfer' },
      { key: 'amount', label: '订单金额' },
      { key: 'warehouse', label: '销售/退货仓库', showIn: /sale|_return/ },
      { key: 'warehouse_in', label: '调入仓', showIn: 'transfer' },
      { key: 'available_stock', label: '可用库存' },
      { key: 'actual_stock', label: '实际库存' },
      { key: 'remark', label: '备注' },
    ],
    priceField: 'actual_price',         // 主价格字段
    qtyField: 'quantity',                // 主数量字段
    rowCount: 16,                        // 默认 16 行预置
    // 业务特殊配置
    transferMode: false,                 // 调拨模式：双仓库联动
    dualStockDisplay: false,             // 双库存显示（采购退货：可用/实际都分大小单位）
  },

  // ----- 业务约束 -----
  validators: [
    { kind: 'positive_qty', message: '数量必须为正数' },
    { kind: 'price_format', message: '价格最多2位小数' },
    { kind: 'stock_available', blocking: true, message: '库存不足' },
    { kind: 'related_order_required', when: { type: /_return$/ } },  // 退单必选原单
  ],

  // ----- 提交配置 -----
  submit: {
    primaryLabel: '提交并审核',
    draftLabel: '保存草稿',
    endpoint: '/api/sale-orders',
    idempotency: true,
    stockCheck: true,
    receipt: true,
  },
}
```

### 3.3 通用入口函数

```javascript
window.showOrderForm = function(config) {
  // 1. 复用 showVisitSaleForm 的 overlay 创建逻辑，但字段按 config 渲染
  // 2. 复用 _vsfAddRow / _vsfFillProductPrice / _vsfSyncDeliveryTime 等
  // 3. 复用 _vsfValidate (按 config.validators 动态配置)
  // 4. 提交时调通用 saveOrderForm
};

window.saveOrderForm = function(config, body) {
  // 1. 幂等 key 生成
  // 2. 校验（按 config.validators）
  // 3. 库存检查（按 config.itemsTable）
  // 4. POST 到 config.endpoint
  // 5. 重试 + 回执 + 关闭
};
```

---

## 四、8 个模块的差异矩阵

| 模块 | type 标识 | 字段差异 | 列差异 | 接口 |
|------|-----------|---------|--------|------|
| **创建访销订单** | `visit_sale` | 客户/业务员/部门/销售仓库/单据日期/预提货日期/预配送时间/送货司机/车辆/备注/加急/付款方式 | 退货价/订单数量/订单金额/销售仓库/可用库存/实际库存 | `POST /sale-orders` |
| **创建访销退单** | `visit_sale_return` | + 退货原因、+ 原订单关联、- 送货司机/车辆、- 预提货日期/预配送时间 | 退货价(折扣)/订单数量(退货)/退货仓库/可用库存/实际库存 | `POST /return-orders` (含 `sale_order_id`) |
| **创建车销订单** | `car_sale` | - 预提货日期/预配送时间/送货司机/车辆、+ 备注更多 | + 订单数量(盒份)、+ 销售仓库 | `POST /sale-orders` + 后续 `/vehicle-load` |
| **创建车销退单** | `car_sale_return` | + 退货原因、+ 原订单 | + 订单数量(盒份)、- 调入仓 | `POST /return-orders` |
| **创建自提订单** | `self_sale` | - 送货司机/车辆、+ 备注 | - 订单数量(盒份) | `POST /sale-orders` |
| **创建自提退单** | `self_sale_return` | + 退货原因、+ 原订单 | - 订单数量(盒份) | `POST /return-orders` |
| **创建采购订单** | `purchase` | 客户→供应商、参考成本价/采购价 | - 销售仓库、+ 采购价、+ 应付款金额（负数/正数） | `POST /purchase-orders` |
| **创建采购退货订单** | `purchase_return` | + 退货原因、+ 原采购订单 | + 预计生产日期、+ 双单位库存（小/大单位都显示） | `POST /purchase-orders` (type='return') 或 `/purchase-return-orders` |
| **创建调拨订单** | `transfer` | 客户/供应商都无、+ 调出仓/调入仓/经办人 | + 订单数量(综合)、+ 可用/实际库存（调出仓）、+ 实际库存（调入仓） | `POST /transfer-orders` |

**共性提炼**（所有 8 个模块都有的）：
- 顶部蓝色 tab 栏 + 标题 + 关闭按钮
- 字段区第一行（客户/供应商/调出仓 必选）
- 商品明细表（行数预置、列设置齿轮）
- 底部合计 + 保存/提交并审核按钮
- 右上角齿轮（列设置）、按单价锁变价、按金额反算实际价

---

## 五、实施路线图

### 阶段 1：通用框架（第 1 周，约 4-5 个工作日）

**目标**：抽出 `showOrderForm(config)` + `saveOrderForm(config, body)`，访销订单照旧工作

| 任务 | 工作量 | 风险 |
|------|--------|------|
| 设计 config schema + 类型注释 | 0.5d | 低 |
| 抽出 `_orderFormHelpers.js`（公用方法：overlay/校验/库存/重试/回执） | 1.5d | 中（必须保证 100% 行为一致） |
| 实现 `showOrderForm(config)` 通用入口 | 1.5d | 中 |
| 现有 `showVisitSaleForm` 改为薄包装 → 调用通用函数 | 0.5d | 低 |
| 全量回归测试（访销订单所有交互） | 1d | — |

**交付**：访销订单 0 行为变化 + 框架就绪

### 阶段 2：销售变体（第 2 周，约 4 个工作日）

**目标**：访销退单、车销、车销退、自提、自提退全部跑通

| 任务 | 工作量 |
|------|--------|
| 退单 variant（加退货原因、原订单关联字段） | 1.5d |
| 车销 variant（去掉司机/车辆，+盒份列） | 0.5d |
| 自提 variant | 0.5d |
| 注册到侧边栏菜单（侧边栏加 4-5 个入口） | 0.5d |
| 端到端测试 | 1d |

**交付**：5 个销售类创建页全部可用

### 阶段 3：采购 + 调拨（第 3 周，约 3-4 个工作日）

| 任务 | 工作量 |
|------|--------|
| 采购订单 variant（供应商/参考成本价/采购价/应付款） | 1d |
| 采购退货 variant（+预计生产日期/双单位库存） | 1d |
| 调拨订单 variant（双仓库联动/双仓库库存） | 1d |
| 注册菜单 + 测试 | 0.5d |

**交付**：8 个模块全部上线

### 总工作量

| 角色 | 工时 |
|------|------|
| 前端（重构 + 8 模块） | 11-12 个工作日 |
| 后端（采购退货接口补全，如需） | 0.5-1 个工作日 |
| 测试与回归 | 持续并行 |

---

## 六、风险与缓解

| 风险 | 等级 | 缓解 |
|------|------|------|
| 重构破坏现有访销订单功能 | 🔴 高 | 阶段 1 必须全量回归；用 7000 行 app.js 的 submodule 化（铁律）或独立模块文件 |
| 8 模块同时上线风险 | 🟠 中高 | 分阶段上线（每阶段仅新增 1-3 模块） |
| 字段标签/列名差异繁多，遗漏 | 🟠 中 | 截图差异表（第四节）逐字段核对 |
| `app.js` 继续膨胀（铁律警示） | 🟠 中 | 把通用代码放 `static/js/modules/order-form/` 目录，遵循铁律 |
| 调拨订单的"双仓库联动"业务逻辑复杂 | 🟠 中 | 阶段 3 单独规划，先调研再开发 |
| 现有菜单未挂载 8 个入口 | 🟡 低 | 阶段 2/3 同步注册到 sidebar（已有 `_tabs` 机制） |

---

## 七、关键设计决策

### 7.1 config 放在哪？

**方案 A：单文件 config 字典**（推荐）
- `static/js/modules/order-forms/configs.js`：8 个模块 config + 全局注册表
- 优点：集中、易查、改一处一目了然
- 缺点：单文件可能 800-1200 行

**方案 B：每个模块独立 config 文件**
- `static/js/modules/order-forms/visit-sale.js`、`car-sale.js` 等
- 优点：单一职责、git diff 干净
- 缺点：8 个文件，跨文件改动需同步

**推荐 A**，因为 8 个模块的 config 80% 字段相同，集中在同一文件便于复用基础配置。

### 7.2 通用代码放哪？

按 v90 铁律"app.js 只读不写"，新代码必须放 `static/js/modules/`：
```
static/js/modules/order-form/
├── engine.js           # showOrderForm + saveOrderForm 入口
├── render-fields.js    # 字段区按 config 渲染
├── render-items.js     # 商品明细表按 config 渲染
├── helpers.js          # _vsfValidate/_vsfCheckStock/_vsfApiRetry 等
├── configs.js          # 8 个模块 config + registry
└── receipt.js          # 提交回执卡片
```

### 7.3 不在本次范围（明确边界）

- ❌ 重构现有 `app.js` 中与订单无关的代码（财务/人事/CRM 等）
- ❌ 改造后端接口 schema（仅在必要时小幅扩展）
- ❌ 离线提交队列（独立排期）
- ❌ 打印模板重构（沿用现有 `printDeliveryNote` + 新增 transfer/return 打印模板）

---

## 八、立即可执行的下一步

如果方案认可，下一步建议：

1. **用户拍板**：确认分阶段方案是否接受
2. **建任务**：阶段 1 的 5 个子任务（config 设计/通用框架/访销回归等）
3. **开工**：先把 `_orderFormHelpers` 抽出来 + `showOrderForm(config)` 空壳函数跑通
4. **第 1 天内**：访销订单继续 100% 工作（验证重构零回归）

---

## 九、总结

> **重构的核心价值不是"少写代码"，而是"让新增业务模块从 1 周降到 1 天"。**

按本方案落地后：
- 第 9 个订单模块（如调拨到店、领料出库）只需新增一份 50-100 行的 config，无需触碰通用代码
- 通用代码一处修复，所有模块受益
- 业务字段差异完全透明可查（configs.js 即文档）