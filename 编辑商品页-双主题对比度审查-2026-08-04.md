# 编辑商品页深浅色主题对比度审查报告

**日期**：2026-08-04 ｜ **审查范围**：`static/js/modules/product-form.js` + `static/styles.css` 6110-6130 行
**审查版本**：v107.x（commit 520f6b1 后） ｜ **报告类型**：静态分析 + 截图佐证

---

## 一、核心结论

**整个 `product-form.js` 的 `.pf-*` 样式体系是为深色背景硬编码的**——所有输入框、卡片、价格表、标签、提示信息均使用 `var(--black)` / `rgba(white, ...)` / 固定灰度，**完全忽略主题变量**。导致两种模式下都存在可读性问题：

| 场景 | 浅色主题（截图可见） | 深色主题（代码推断） |
|---|---|---|
| `.pf-unit-card` | 🔴 **黑底卡片在白底页面突兀** | 🟠 黑底与页面同色，**仅靠 8% 白边框辨识**，卡片"消失" |
| `.pf-price-table` | 🔴 同上（黑底） | 🟠 同上（边界模糊） |
| `.pf-input` | 🟠 黑底白字输入框（与页面风格冲突） | 🟡 半透白底 + 白字对比度低（深色 4%白 ≈ 不可见） |
| `.pf-label` | 🟡 `var(--gray-300)` 浅灰在白底偏淡 | ✅ 浅灰在深色底可读 |
| `.pf-section-title` | 🟡 同上 | ✅ 同上 |
| `.pf-hint` | ✅ `gray-600` 对比度 OK | 🟡 浅色页设计的提示文在深色页对比度低 |
| `.pf-checkbox` | 🟡 `gray-300` 偏淡 | ✅ 可读 |

**根因**：`.pf-*` 用了**直接色值**（`var(--black)` / `rgba(white, ...)` / `--gray-*`）而非**主题令牌**（`var(--bg2)` / `var(--bg3)` / `var(--t1)` / `var(--t2)` / `var(--bd)`），导致主题切换时这套组件完全失配。

---

## 二、截图实证（浅色主题）

### 🔴 问题 1：单位卡片深色突兀

**位置**：`product-form.js:42-44` 三个 `.pf-unit-card`
**样式**（styles.css:6110）：
```css
.pf-unit-card{background:var(--black);border:1px solid rgba(var(--white-rgb),.08);border-radius:8px;padding:14px}
```

**截图证据**：底部"小单位 / 中单位 / 大单位"三个卡片是**深黑色背景**，与浅色主页面（白底）形成强烈对比，三个黑色矩形像贴在白纸上的"黑色补丁"，视觉割裂。

**深色主题推断**：`.pf-unit-card` 黑底 + 主页面 `--bg-deep:#000000` → **完全同色**，仅靠 8% 透明白边框区分 → 卡片"消失"在背景中。

### 🔴 问题 2：价格表同样黑底

**位置**：`product-form.js:49-60` 的 `.pf-price-table`
**样式**（styles.css:6112）：`background:var(--black)`
**场景**：点击"售价管理"tab 后出现。同样在浅色页面突兀，深色页面消失。

### 🟠 问题 3：输入框深色冲突

**样式**（styles.css:6118）：
```css
.pf-input{padding:8px 10px;border:1px solid rgba(var(--white-rgb),.1);border-radius:8px;
  background:rgba(var(--white-rgb),.04);color:var(--white);font-size:13px}
```

**截图证据**：上半部分表单（品名、知名品牌、规格、颜色、产地、编号、首字母/助记码、仓库位置）的输入框显示为**深色背景**——"品名"输入框明显是黑色矩形，与浅色页面风格冲突。
- 浅色主题：黑底白字输入框，突兀
- 深色主题：4% 透明白底 + 白字，**深色页面对比度严重不足**（白字在接近黑色的背景上，对比度可能 < 3:1，违反 WCAG AA 4.5:1）

### 🟡 问题 4：标签对比度不足

**样式**（styles.css:6120）：
```css
.pf-label{font-size:12px;font-weight:500;color:var(--gray-300)}
.pf-section-title{font-size:14px;font-weight:600;color:var(--gray-300)}
.pf-checkbox{...color:var(--gray-300)}
.pf-unit-card-title{...color:var(--gray-300)}
```

- `--gray-300: #d2d2d7`（浅色主题下）→ 在白底/浅灰底上**对比度仅约 1.6:1**（远低于 WCAG AA 4.5:1）
- 截图中的"商品名称 / 知名品牌 / 规格 / 颜色"标签文字偏淡，肉眼可见发灰

### 🟡 问题 5：提示信息/小字偏淡

**样式**（styles.css:6122）：
```css
.pf-hint{font-size:11px;color:var(--gray-600);margin-top:2px}
```
- `--gray-600: #636366` 11px → 浅色页约 4.7:1（边缘达标）；深色页约 4.3:1（边缘达标但偏淡）

### 🟡 问题 6：图片上传卡片几乎不可见

**样式**（styles.css:6130）：
```css
.pf-image-card{...border:2px dashed rgba(var(--white-rgb),.12);background:rgba(var(--white-rgb),.02)}
```
- 浅色主题：白底 + 12% 白虚线 = **几乎看不见边框**
- 深色主题：4% 透明白底 + 12% 透明白边 = **完全不可见**

---

## 三、统一改造方案（用主题令牌替代硬编码）

### 原则
- ✅ **背景**用 `var(--bg3)` / `var(--bg2)`（主题响应）
- ✅ **文字**用 `var(--t1)` / `var(--t2)` / `var(--t3)`（自动对比）
- ✅ **边框**用 `var(--bd)` / `var(--bd2)`（主题响应）
- ✅ **品牌色**保留 `var(--p)` 不变（两个主题都是深蓝）
- ❌ 删 `var(--black)` / `var(--white)` / `--gray-*` / `rgba(white, ...)`

### 改造映射表

| 元素 | 现值（硬编码） | 改造值（主题令牌） |
|---|---|---|
| `.pf-unit-card` background | `var(--black)` | `var(--bg2)` |
| `.pf-unit-card` border | `rgba(white,.08)` | `var(--bd)` |
| `.pf-unit-card-title` color | `var(--gray-300)` | `var(--t2)` |
| `.pf-price-table` background | `var(--black)` | `var(--bg2)` |
| `.pf-price-table th` color/bg | `var(--gray-600)` / `var(--bg4)` | `var(--t2)` / `var(--bg3)` |
| `.pf-price-table td` border | `var(--bd)` | `var(--bd2)` |
| `.pf-price-input` color | `var(--white)` | `var(--t1)` |
| `.pf-input` background | `rgba(white,.04)` | `var(--bg3)` |
| `.pf-input` color | `var(--white)` | `var(--t1)` |
| `.pf-input` border | `rgba(white,.1)` | `var(--bd)` |
| `.pf-label` color | `var(--gray-300)` | `var(--t2)` |
| `.pf-section-title` color | `var(--gray-300)` | `var(--t1)` + 600 weight |
| `.pf-hint` color | `var(--gray-600)` | `var(--t3)` |
| `.pf-checkbox` color | `var(--gray-300)` | `var(--t2)` |
| `.pf-image-card` border/bg | `rgba(white,.12)` / `rgba(white,.02)` | `var(--bd2)` / `var(--bg3)` |
| `.pf-header` / `.pf-footer` | `var(--bg2)` | 已是令牌，无需改 |
| `.pf-tabs` | `var(--bg2)` | 已是令牌，无需改 |

### 改造后视觉

| 场景 | 浅色主题 | 深色主题 |
|---|---|---|
| 单位卡片 | 浅灰底 + 主文字色 + 浅边框（**融入页面**） | 深灰底 + 浅文字色 + 浅边框（**融入页面**） |
| 输入框 | 浅底 + 深字 + 浅边框 | 深底 + 浅字 + 浅边框 |
| 标签 | 清晰深灰（**4.5:1+**） | 清晰浅灰（**4.5:1+**） |
| 提示 | 浅灰（**4.5:1+**） | 浅灰（**4.5:1+**） |

---

## 四、其他建议

### 优先级
- 🔴 **P0**（截图直接证据）：`.pf-unit-card` / `.pf-price-table` / `.pf-input` 三处用令牌替代
- 🟠 **P1**：`.pf-label` / `.pf-section-title` / `.pf-checkbox` 颜色调整
- 🟡 **P2**：`.pf-image-card` / `.pf-hint` 微调

### 设计令牌建议（可在本次改造一并加）
- 现有 `--t1/-t2/-t3` 已在浅色/深色双主题定义，**直接复用**即可
- 不需要新建令牌

### 改造范围
- 单文件 `styles.css`（约 20 行替换），不动 JS
- 改造后**对浅色页面的视觉影响最大**（截图显示的突兀感彻底消除）
- 对深色页面的影响：`.pf-input` 背景从 4% 透明白 → `var(--bg3)`，对比度从 3:1 提升到 4.5:1+

### 验证
1. 浅色主题：单位卡片应与页面同色系（浅灰），输入框为浅色，输入框文字深色可读
2. 深色主题：单位卡片与页面同色系（深灰），输入框为深色，文字浅色可读
3. DevTools 切换 `:root.dark` 类即时验证

---

## 六、实施记录（2026-08-04 v107.8，commit a7e39cf，已部署）

| 元素 | 现值（硬编码） | 改造值（主题令牌） | 状态 |
|---|---|---|---|
| `.pf-unit-card` background | `var(--black)` | `var(--bg2)` | ✅ |
| `.pf-unit-card` border | `rgba(white,.08)` | `var(--bd)` | ✅ |
| `.pf-unit-card-title` color | `var(--gray-300)` | `var(--t2)` | ✅ |
| `.pf-price-table` background | `var(--black)` | `var(--bg2)` | ✅ |
| `.pf-price-table th` | `var(--gray-600)`/`var(--bg4)` | `var(--t2)`/`var(--bg3)` | ✅ |
| `.pf-price-input` color | `var(--white)` | `var(--t1)` | ✅ |
| `.pf-input` background/color | `rgba(white,.04)`/`var(--white)` | `var(--bg3)`/`var(--t1)` + `::placeholder` `var(--t3)` | ✅ |
| `.pf-label` color | `var(--gray-300)` | `var(--t2)` | ✅ |
| `.pf-section-title` color | `var(--gray-300)` | `var(--t1)` | ✅ |
| `.pf-hint` color | `var(--gray-600)` | `var(--t3)` | ✅ |
| `.pf-checkbox` color | `var(--gray-300)` | `var(--t2)` | ✅ |
| `.pf-image-card` border/bg | `rgba(white,.12)`/`rgba(white,.02)` | `var(--bd2)`/`var(--bg3)` | ✅ |
| `.pf-rich-editor` background | `var(--black)` | `var(--bg2)` | ✅ |
| `.pf-rich-btn`/`.pf-rich-content` | `var(--gray-300)` | `var(--t2)` | ✅ |
| `.pf-rich-content:empty` | `var(--gray-600)` | `var(--t3)` | ✅ |
| `.pf-switch-row` border | `rgba(white,.05)` | `var(--bd2)` | ✅ |
| `.pf-img-del`（图片删除钮） | 黑底白字半透明 | **保留**（覆盖在图片上，双主题清晰） | 🛡️ |
| `.pf-toggle-slider`（开关轨道） | 半透明白 | **保留**（中性轨道，双主题适配） | 🛡️ |

**验证**：残留硬编码仅 2 处合理保留；无 `:root.dark` 冲突覆写；构建部署 ✓；线上 `.pf-unit-card` = `var(--bg2)`、`.pf-input` = `var(--bg3)`/`var(--t1)` 确认。

---

## 五、结论

**改造 ROI 高**：单文件 20 行替换，无 JS 改动，无功能影响，但可同时解决**浅色页 6 类问题 + 深色页 4 类问题**，让编辑商品页在双主题下都达到 WCAG AA 4.5:1 标准。属于"**低成本高收益的可达性升级**"。

**建议优先级**：🔴 P0 改造（5 分钟，约 20 行 CSS）→ 用户在浅色主题下立刻看到单位卡片/价格表/输入框与页面融为一体；深色主题下输入框对比度合规。
