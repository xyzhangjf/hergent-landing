# 报单汇总表现状梳理 · 本期预报主表设计 · 报单导入模版设计

> 依据：附件1《2026年9月13日到货下单表.xlsx》、附件2《价格表20260913.xlsx》、附件3《调拨订单导入模板_20260913.xlsx》、附件4《自提订单导入模板_20260913.xlsx》—— **全部为逐单元格实读**，非推测。
> 核对对象：`hergent-erp` 现有实现（`erp_db.py` / `routers/forecast.py` / `routers/import_router.py`）。
> 日期：2026-09-13

---

## 摘要（先讲结论）

1. **附件1 不是一张表，是三张表拼在一张表里**：商品区（A–K，来自价格表）、报单区（L–AF，21 个「人名列」= 报单矩阵）、下单区（AG–AM，全部是**派生列**）。**下单区一列都不该由人手填**，包括现在被人工覆盖的「最终下单」。
2. **系统里已有 70% 的实现**：期次表 `forecast_periods`、报单提交与明细 `forecast_submissions/_items`、条码→单位映射 `forecast_barcode_units`、**报单对象映射 `report_mapping`（就是 L–AF 列头的字典）**、加单表 `forecast_extra_qty`、以及**舟谱模板生成引擎**（`routers/forecast.py` 的 `ZT_HEADERS`/`DB_HEADERS`）。真正缺的是 **2 处字段、1 张渠道价表、3 处口径修正**。
3. **实读发现 6 个必须修的问题**（详见 §1.5），其中两个是「钱会算错」级别：合计行公式只求和到第 109 行（王琴列合计显示 0，实际 136）；「最终下单」列 43 行人工敲入，偏差从 −14 到 +198，**取整规则在现实里根本不存在**。
4. **导入模版只需要 3 个必填字段**：报单对象、商品条码、报单数量。其余全部自动带出 —— 附件1 自己就是证据：单位列 159 行**全空**、三个渠道价列**全空**，用户从来就不填这些。

---

# 一、现状梳理

## 1.1 四份附件的关系总图

```
                    ┌──────────────────────────────┐
                    │ 附件2 价格表20260913.xlsx     │  商品主数据字典（176 行）
                    │ 数据源：代码/条码/永辉代码/    │  ← 单位、规格、品牌、4 个渠道价的来源
                    │ 简称/规格/单位/分销价/         │
                    │ 永辉价/沃尔玛价/美联价         │
                    └───────────┬──────────────────┘
                                │ 三键映射：代码 ⇄ 厂家编码
                                │          永辉代码 ⇄ 永辉编码
                                │          条码 ⇄ 条码
                                ▼
      ┌────────────────────────────────────────────────────────────┐
      │ 附件1 2026年9月13日到货下单表.xlsx                          │
      │ sheet「9月9日报单-9月13日到货」159 行明细 + 1 行合计          │
      │                                                             │
      │  ┌── 商品区 A–K（11列）──┐┌─ 报单区 L–AF（21列）─┐┌─ 下单区 AG–AM（7列）─┐
      │  │ 编码/条码/规格/简称/   ││ 唐成 黄家伟 易胜琳 … ││ 合计 件数 加单 最终下单│
      │  │ 单位(空)/品牌/分销价/  ││ 美联保康             ││ 厂价 下单金额 订单排期 │
      │  │ 永辉价(空)/沃尔玛价(空)/││ = 每个报单对象一列   ││ = 全部派生列          │
      │  │ 美联价(空)            ││                      ││                      │
      │  └──────────────────────┘└──────────────────────┘└──────────────────────┘
      └───────────────┬────────────────────────┬───────────────────────────┘
                      │ 按报单对象类型分单       │
        counterparty_type = self_warehouse      │ counterparty_type = customer / store
                      ▼                        ▼
      ┌──────────────────────────┐  ┌──────────────────────────────────┐
      │ 附件3 调拨订单导入模板     │  │ 附件4 自提订单导入模板            │
      │ 13 列 / 必填 5 / 338 行    │  │ 21 列 / 必填 6 / 168 行            │
      │ 总仓 → 8 个业务员仓        │  │ 9 个客户/门店，9 张单              │
      │ 单号 DB2026091301–08      │  │ 单号 ZT2026091321–29              │
      │ 数量 = 附件1 该业务员列的值 │  │ 数量 = 附件1 该客户列的值          │
      │ 单价：无                  │  │ 单价 = 按客户取价（分销/永辉/美联）│
      └──────────────────────────┘  └──────────────────────────────────┘
                                ▼
                         舟谱 ERP（外部，导入这两个模板）
```

**一句话**：附件1 是**汇总输入**，附件2 是**字典**，附件3/4 是**同一份汇总按报单对象类型拆出的两种下游单据**。

## 1.2 附件1 的字段全清单与实际用途

39 列 = 商品区 11 + 报单区 21 + 下单区 7。

| 列 | 表头（原文） | 类型 | 实读状态 | 用途 | 来源 |
|---|---|---|---|---|---|
| A | 厂家系统商品编码 | 文本/数字混合 | 158/159 有值 | 匹配价格表 | 附件2 A「代码」 |
| B | 永辉系统商品编码 | 数字 | 90/159 有值 | 匹配价格表 | 附件2 C「永辉代码」 |
| C | 条码 | 数字(13位) | 154/159 有值，**3 个重复** | **唯一业务键** | 附件2 B「条码」 |
| D | 规格 | 数字 | 14 种取值 | 件数 = 数量 ÷ 规格 | 附件2 E |
| E | 简称 | 文本 | 159 个独立值 | 展示名 | 附件2 D |
| F | 单位 | — | **整列 0 值** | 下游单据必填字段 | **附件2 F（下单表不带）** |
| G | 品牌 | 文本 | 4 个值 | 品牌筛选 | 主档 |
| H | 分销价格 | 数字（**公式**） | 159 行 | 生成附件4 时的折后价来源 | `=(AK/D)/0.9` 或 `=(AJ/D)/0.9` ← **两套写法并存** |
| I | 永辉价 | — | **整列 0 值** | 生成附件4（永辉门店） | 附件2 H |
| J | 沃尔玛价 | — | **整列 0 值** | 生成附件4（沃尔玛门店） | 附件2 I |
| K | 美联价 | — | **整列 0 值** | 生成附件4（美联店） | 附件2 J |
| L–AF | **21 个人名/门店名** | 数字 | 各自非空 | **报单数量（最小销售单位）** | 业务员填报 |
| AG | `合计（最小数量）`（含换行） | 数字（公式） | 160 行 | 报单合计 | `=SUM(L:AF)` |
| AH | 件数 | 数字（公式） | 159 行 | 换算整件 | `=AG/D` |
| AI | 加单 | 数字 | **仅 1 行有值**（第96行=18） | 整件追加 | 人工 |
| AJ | 最终下单 | 数字（**116 公式 + 43 手填**） | 159 行 | 实际下单量 | `=SUM(AH:AI)` 或人工 |
| AK | 厂价 | 数字 | 159 行 | 下单金额基数 | **独立维护（价格表无此列！）** |
| AL | 下单金额 | 数字（公式） | 159 行 | = AK × AJ | 派生 |
| AM | 订单排期 | 文本 | 18/159 有值 | 下单节奏备注 | 人工 |

**报单区 21 列 = 21 个报单对象**，合计值（按明细 2–160 行实算）：

| 列 | 对象 | 合计 | 下游 | | 列 | 对象 | 合计 | 下游 |
|---|---|---|---|---|---|---|---|---|
| L | 唐成 | 312 | 自提 | | X | 刘小顶 | 929 | 调拨 |
| M | 黄家伟 | 0 | — | | Y | 谢总 | 1163 | 自提 |
| N | 易胜琳 | 171 | 自提 | | Z | 吾悦 | 220 | 自提 |
| O | 胡奎奎 | 76 | 自提 | | AA | 东津 | 275 | 自提 |
| P | 朱青峰 | 110 | 自提 | | AB | 民发 | 253 | 自提 |
| Q | 刘正宝 | 1496 | 调拨 | | AC | 沃尔玛 | 0 | — |
| R | **王琴** | **136** | 调拨 | | AD | **美联檀溪** | **887** | **无单** |
| S | 程欢欢 | 1559 | 调拨 | | AE | **汴河** | **345** | **无单** |
| T | 刘善涛 | 1881 | 调拨 | | AF | 美联保康 | 408 | 自提 |
| U | 毛辉 | 949 | 调拨 | | | | | |
| V | 周运潘 | 1591 | 调拨 | | | | | |
| W | 田顺达 | 2265 | 调拨 | | | | | |

## 1.3 数据流转关系（一条报单的完整生命周期）

```
① 业务员/客户报数
        │  （现在：微信群/纸质 → 文员手工填入附件1 的 L–AF 列）
        ▼
② 附件1 商品行 × 报单对象列 = 报单矩阵
        │
        ├─ ③ 横向求和  → AG 合计（最小数量）
        ├─ ④ 除以规格  → AH 件数（= AG ÷ D，可为小数）
        ├─ ⑤ 人工追加  → AI 加单（整件，本期只用了 1 次）
        ├─ ⑥ 定最终量  → AJ 最终下单（应取整；**实际 27% 是人工拍板**）
        ├─ ⑦ 乘厂价    → AL 下单金额（= AK × AJ）
        └─ ⑧ 标注节奏  → AM 订单排期
        │
        ▼
⑨ 按报单对象的类型拆分（依据 report_mapping.counterparty_type）
        ├─ self_warehouse（业务员自管仓）→ 附件3 调拨订单：总仓 → 该业务员仓
        └─ customer / store（客户/门店）  → 附件4 自提订单：按客户分单 + 按客户取价
        │
        ▼
⑩ 导入舟谱 ERP
```

## 1.4 下单环节 vs 舟谱自提订单模版生成环节：字段用途对照

**这是本次梳理的核心区分点。** 两个环节用到的字段**几乎不重叠**：

| 字段 | 下单环节 | 生成附件4（自提订单模版） | 说明 |
|---|:---:|:---:|---|
| A 厂家编码 / B 永辉编码 / C 条码 | ✅ 匹配键 | ✅ 匹配键 | 两环节都要 |
| D 规格 | ✅ **算件数必需** | ❌ | 下单才需要换算整件 |
| E 简称 | ✅ 展示 | ❌ | — |
| F 单位 | ❌（列空） | ✅ **必填字段** | 来自附件2，下单不需要 |
| G 品牌 | ✅ 筛选 | ❌ | — |
| H 分销价格 | ❌ | ✅ **单价(折后价)候选** | 来自附件2 G |
| I 永辉价 | ❌ | ✅ **单价(折后价)候选** | 来自附件2 H；**实际列空** |
| J 沃尔玛价 | ❌ | ✅ 候选（本批未用到） | 来自附件2 I；**实际列空** |
| K 美联价 | ❌ | ✅ **单价(折后价)候选** | 来自附件2 J；**实际列空** |
| L–AF 报单数量 | ✅ **下单的数量来源** | ✅ **数量来源** | 两环节都要 |
| AG 合计 | ✅ 下单基数 | ❌ | — |
| AH 件数 | ✅ **下单必需** | ❌ | — |
| AI 加单 | ✅ 下单必需 | ❌ | — |
| AJ 最终下单 | ✅ **下单结果** | ❌ | — |
| AK 厂价 | ✅ **下单金额必需** | ❌ | — |
| AL 下单金额 | ✅ | ❌ | — |
| AM 订单排期 | ✅ | ❌ | — |

**结论：**
- **下单环节**用的是一把「厂价 + 规格 + 报单数量」，产出「最终下单 + 下单金额」，**完全不碰渠道价**。
- **生成附件4 环节**用的是「条码 + 单位 + 报单数量 + **按客户身份选一个渠道价**」，**完全不需要规格/厂价/件数**。
- ⚠️ 这与需求描述**有一处需要修正**：附件1 里 **物理存在** H/I/J/K 四列表头，不是「表里没有」；准确表述是 **I/J/K 三个渠道价列整列为空、下单环节不录入它们**，H 分销价格是**派生公式列**（= 厂价÷规格÷0.9）而非录入字段。
- ⚠️ 附件2 **没有「厂价」列**。厂价（AK）在附件1 里独立维护，**来源不在价格表**。这是设计主表时必须显式解决的缺口（见 §2.4）。

### 附件4 的单价到底取自哪里（实读结论）

按客户逐行比对附件2 的四个价列，**168 行 100% 可解释**：

| 客户 | 行数 | 取价列 |
|---|---|---|
| 唐成 / 宜城谢总 / 易胜玲 / 朱青峰 / 胡奎奎 | 14/21/10/15/11 | **附件2 G 分销价格** |
| 永辉东津店 / 永辉吾悦店 / 永辉民发店 | 17/28/14 | **附件2 H 永辉价格** |
| 美联（保康）店 | 38 | **附件2 J 美联价格** |
| 沃尔玛价 | 0 行 | 本期无沃尔玛客户 |

→ **单价不是固定取一列，而是「按客户身份选价列」**。这正是需求里「这些字段只在生成舟谱自提订单导入模版时才使用」的准确含义。

## 1.5 实读发现的 6 个问题（按严重度排序）

| # | 问题 | 证据 | 后果 | 严重度 |
|---|---|---|---|---|
| **1** | **合计行公式只求和到第 109 行**（明细到 160 行） | `L161=SUM(L2:L109)`；王琴列合计显示 **0**，实算 **136** | **7 个对象的合计全部偏小**（田顺达 2265→1953、刘善涛 1881→1617）。拿合计行对账 = 直接少订货 | 🔴 钱会算错 |
| **2** | **「最终下单」无取整规则** | 116 行是 `=SUM(AH:AI)`（**保留小数**，如 3.625）；43 行人工敲入，偏差 −14 ~ +198（第2行 15→13、第4行 12→210、第21/62/64/70 行 AH>0 但 AJ 直接填 **0**） | 厂商不拆零发货，但表里的下单量含小数；且 27% 的行靠人拍板，**无法复现、无法审计** | 🔴 业务规则缺失 |
| **3** | **渠道价三列全空** | I/J/K 非空值 = 0 | 附件4 的单价目前**只能靠人工从价格表另查**，附件1 自身推不出 | 🟠 依赖人 |
| **4** | **17 个条码拿不到有效分销价** | 10 个条码在附件2 查无此商品 + 7 个有商品但分销价为空 | 生成附件4 时这些行**会缺价** | 🟠 数据缺口 |
| **5** | **条码不唯一** | `6934665094026` / `6934665093944` / `6934665091254` 各出现 2 次（恒滋下单 / 福宝下单 两条品牌记录共用条码） | **条码不能单独做主键**，必须带品牌维度 | 🟠 建模陷阱 |
| **6** | **美联檀溪(887) / 汴河(345) 有量却无下游单据** | 两列合计非零，但附件3 的 8 个仓、附件4 的 9 个客户里**都没有** | 这两个渠道的货**根本没下单**（或走了另一条人工流程） | 🔴 可能漏单 |

> 补充：附件3 的 8 张单据、附件4 的 9 张单据，**数量合计与附件1 明细实算值 100% 吻合**（8/8、9/9）。说明拆分逻辑本身是对的 —— 错的只有那张合计行。

## 1.6 系统侧补充核实（本轮新增，全部为读生产代码取证）

| # | 发现 | 证据 | 后果 | 严重度 |
|---|---|---|---|---|
| **7** | **付款计算用「销售价」顶包「厂价」** | `routers/forecast.py:290` 与 `:341` 均为 `COALESCE(p.sale_price,0) AS price`，而同文件 `:248`、`:270` 的注释写的是「**定稿量 × 出厂价**」 | 「本期需付款」按 `sale_price` 算 → **金额偏大**（销售价 > 厂价），且该数字**已通过企微推给老板** | 🔴 钱会算错 |
| **8** | **`products` 表没有任何厂价字段** | `products` 建表 `erp_db.py:1321` + 全部 `ALTER TABLE products` 补列清单里都**没有** `factory_price`；只有 `purchase_price`（进货价）/ `sale_price`（销售价）/ `wholesale_price`（批发价） | 用户要求的「厂价必填」**当前无字段可落** → 必须先补列 | 🔴 阻塞建模 |

> 这两条是**同一根因的两个侧面**：厂价在数据模型里缺位，于是付款计算被迫拿一个"长得像"的价格顶包。
> **修复顺序必须固定：先补 `products.factory_price` → 再改 `payments/compute` 的两处 `sale_price`**。顺序颠倒的话，改完仍然无价可取。
> 这也印证了一条通用铁律：**注释里写的口径 ≠ 代码实际取的口径** —— 读价格/口径类代码必须直接看 `SELECT` 列表。

---
---

# 二、表结构设计（系统「本期预报」主表）

## 2.0 先说结论：不是从零建，是「补 3 列、加 1 表、修 3 处口径」

对照现有 `hergent-erp` 实现：

| 需求 | 系统现状 | 处置 |
|---|---|---|
| 期次（下单窗口 / 到货日） | ✅ `forecast_periods(id,name,order_start,order_end,arrival_date,status)` | 复用 |
| 报单对象字典（L–AF 列头） | ✅ **`report_mapping`**（`report_alias` / `counterparty_type` / `system_name` / `src_wh` / `dst_wh` / `order_template`） | 复用，**这就是本次设计的关键发现** |
| 报单事实（谁报了多少） | ✅ `forecast_submission_items(submission_id,product_id,product_name,spec,unit,quantity,price,amount,barcode)` | 扩展 |
| 单头 | ✅ `forecast_submissions(user_id,store_id,store_name,order_date,status,period_id,...)` | 复用 |
| 条码→单位映射 | ✅ `forecast_barcode_units(barcode PK, unit, product_name)` | 复用 |
| 加单 | ⚠️ `forecast_extra_qty(period_start,period_end,product_id,...)` **按日期窗口键** | **改 `period_id` 键** |
| 厂价（下单结算价） | ❌ **`products` 无此列** —— 付款计算改用 `sale_price` 顶包（见 §1.6） | **products 补列 + 档案必填** |
| 分销价 / 厂家编码 | ✅ `products.dist_price` / `product_code` | 复用 |
| **渠道价（永辉/沃尔玛/美联）** | ❌ 无 | **新增表** |
| **永辉系统商品编码** | ❌ 无 | **products 补列** |
| **取整方向（向上/向下）** | ❌ 无 | **products 补列** |
| 舟谱模板生成 | ✅ `routers/forecast.py`（`ZT_HEADERS`/`DB_HEADERS`/`_guess_unit`） | 复用，接取整后的值 |

## 2.1 三层结构总览

```
┌──────────────────────────────────────────────────────────────┐
│ ① 期次层  forecast_periods                                    │
│    一期一条：下单窗口 order_start~order_end + 到货日 arrival_date │
│    ★ 已有。注意：期次窗口允许重叠，所有查询必须带 period_id      │
└────────────────────────┬─────────────────────────────────────┘
                         │ 1 : N
┌────────────────────────▼─────────────────────────────────────┐
│ ② 【主表】forecast_period_items  —— 本期预报主表              │
│    粒度：期次 × 商品  =  附件1 的一行                          │
│    承载：商品快照 + 下单派生列（合计/件数/加单/最终下单/金额/排期）│
└────────────────────────┬─────────────────────────────────────┘
                         │ 1 : N
┌────────────────────────▼─────────────────────────────────────┐
│ ③ 子表  forecast_period_reports  —— 报单明细                  │
│    粒度：期次 × 商品 × 报单对象  =  附件1 的一个格（L–AF）      │
│    承载：报单数量（最小销售单位）                                │
└──────────────────────────────────────────────────────────────┘

支撑表：
  products                商品主档（+2 列）      ← 附件2 A–E
  product_channel_prices  渠道价（新增）          ← 附件2 G–J
  report_mapping          报单对象字典（已有）    ← 附件1 L–AF 列头
  forecast_barcode_units  条码→单位（已有）       ← 附件2 F
  forecast_extra_qty      加单（已存在，改键）
```

**为什么「主表」是 `forecast_period_items` 而不是报单矩阵？**
因为附件1 的**行**才是业务主体 —— AG–AM 那 7 列（合计/件数/加单/最终下单/厂价/金额/排期）**全部是「按商品」的**，与「谁报的」无关。报单对象只是这个商品行的**分解维度**。主表放行、子表放分解，正好对应附件1 的「行」与「列」。

## 2.2 主表 `forecast_period_items`（本期预报主表）字段清单

| # | 字段 | 类型 | 约束 | 用途 | 来源 | 派生 |
|---|---|---|---|---|---|---|
| 1 | `id` | INTEGER | PK AUTOINCREMENT | 行标识 | — | |
| 2 | `period_id` | INTEGER | NOT NULL, FK→`forecast_periods.id` | 归属期次 | 页面选定 | |
| 3 | `product_id` | INTEGER | NOT NULL, FK→`products.id` | 商品 | 条码匹配 | |
| 4 | `barcode` | TEXT | NOT NULL | **条码快照**（附件1 C） | 主档 | |
| 5 | `brand` | TEXT | DEFAULT '' | **品牌快照**（附件1 G）—— 条码不唯一，需带品牌 | 主档 | |
| 6 | `product_code` | TEXT | DEFAULT '' | 厂家系统商品编码（附件1 A / 附件2 A） | 主档 | |
| 7 | `yh_code` | TEXT | DEFAULT '' | 永辉系统商品编码（附件1 B / 附件2 C） | 主档【**补列**】 | |
| 8 | `short_name` | TEXT | DEFAULT '' | 简称（附件1 E） | 主档 | |
| 9 | `spec` | REAL | DEFAULT 0 | **规格**（附件1 D）—— 算件数的分母 | 主档 | |
| 10 | `unit` | TEXT | DEFAULT '' | **单位**（附件1 F，下单表不带） | 主档 / `forecast_barcode_units` | |
| 11 | `factory_price` | REAL | DEFAULT 0 | **本期厂价**（附件1 AK）—— 下单时快照，**行级填写可覆盖主档** | 行级填写 ?? `products.factory_price`（**必填**） | |
| 12 | `report_qty` | REAL | DEFAULT 0 | **报单合计**（附件1 AG，最小销售单位） | Σ子表 `qty` | ✔ |
| 13 | `pack_qty` | REAL | DEFAULT 0 | **件数**（附件1 AH）= `report_qty ÷ spec`（保留 4 位） | 计算 | ✔ |
| 14 | `extra_qty` | INTEGER | DEFAULT 0, CHECK(整件) | **加单**（附件1 AI）—— 整件 | 录入 | |
| 15 | `round_mode` | TEXT | CHECK IN ('up','down','none'), DEFAULT 'up' | **取整方向**（厂商不拆零发货） | 行覆盖 / 主档默认【**补列**】 | |
| 16 | `final_qty` | INTEGER | DEFAULT 0 | **最终下单**（附件1 AJ）= `ROUND(pack_qty, mode) + extra_qty` | 计算 | ✔ |
| 17 | `order_amount` | REAL | DEFAULT 0 | **下单金额**（附件1 AL）= `factory_price × final_qty` | 计算 | ✔ |
| 18 | `order_rhythm` | TEXT | DEFAULT '' | **订单排期**（附件1 AM） | 录入（期次默认 + 行覆盖） | |
| 19 | `created_at` / `updated_at` | TEXT | — | 时间戳 | — | |

**唯一约束**：`UNIQUE(period_id, product_id, barcode)` —— 防止同一期次同一条码重复行。
（条码在**跨期**可以重复，在**同期**不应重复；若确实需要「恒滋/福宝」两条记录，靠 `brand` 区分，唯一键扩为 `(period_id, barcode, brand)`。）

## 2.3 子表 `forecast_period_reports`（报单明细）字段清单

| # | 字段 | 类型 | 约束 | 用途 |
|---|---|---|---|---|
| 1 | `id` | INTEGER | PK | — |
| 2 | `item_id` | INTEGER | NOT NULL, FK→`forecast_period_items.id` | 归属主表行 |
| 3 | `period_id` | INTEGER | NOT NULL | 冗余（按期次直查，避免 JOIN） |
| 4 | `report_alias` | TEXT | NOT NULL | **报单对象简称**（附件1 L–AF 列头）；关联 `report_mapping.report_alias` |
| 5 | `counterparty_type` | TEXT | CHECK IN ('store','customer','self_warehouse') | 快照：决定走调拨还是自提 |
| 6 | `qty` | REAL | DEFAULT 0, CHECK(>=0) | **报单数量**（最小销售单位） |
| 7 | `created_at` | TEXT | — | — |

**唯一约束**：`UNIQUE(item_id, report_alias)`。

> **为什么数量用最小销售单位而非件数？** 附件3 的 `*订单数量` 合计 1881 = 附件1 刘善涛列合计 1881，说明下游单据要的就是**最小销售单位数量**（配「单位」列使用）。件数只是下单环节的**中间换算量**。

## 2.4 支撑表：新增与补列

### （a）新增 `product_channel_prices`（渠道价表）—— 对应附件2 G–J

```sql
CREATE TABLE IF NOT EXISTS product_channel_prices (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    barcode TEXT NOT NULL,
    channel TEXT NOT NULL CHECK(channel IN ('dist','yh','walmart','meilian')),  -- 分销/永辉/沃尔玛/美联
    price REAL DEFAULT 0,
    updated_at TEXT DEFAULT (datetime('now','localtime')),
    UNIQUE(barcode, channel)
);
```
- `dist` 分销价 → 附件2 G / 附件1 H
- `yh` 永辉价 → 附件2 H / 附件1 I
- `walmart` 沃尔玛价 → 附件2 I / 附件1 J
- `meilian` 美联价 → 附件2 J / 附件1 K

> **为什么不在 `products` 上加四列？** 因为渠道价是「商品 × 渠道」的二维关系，且渠道会增（将来加「大润发价」）。四列写死 = 加渠道要 ALTER TABLE。一表两维是最小改动面。
> **为什么不复用已有的 `customer_prices(customer_id, product_id, price)`？** 它表达「**某客户某商品一个价**」，用于一客一价的谈判价；而渠道价表达「**某渠道的统一价**」，用于批量兜底。**两者层级不同、都需要**：先生效 `customer_prices`（个案），没有再回落到 `product_channel_prices[客户所属渠道]`。

### （b）`products` 补三列 —— 其中 **`factory_price`（厂价）是本轮最关键的缺口**

```sql
ALTER TABLE products ADD COLUMN factory_price REAL DEFAULT 0;                  -- ★厂价（下单结算价），必填
ALTER TABLE products ADD COLUMN yh_code TEXT DEFAULT '';                       -- 永辉系统商品编码
ALTER TABLE products ADD COLUMN order_rounding TEXT DEFAULT 'up';              -- 'up' 向上 / 'down' 向下 / 'none' 不取整
```

> 🔴 **为什么厂价必须单独建列，而不是复用 `purchase_price`（进货价）？**
> 实读生产代码发现：**`products` 表里没有任何厂价字段**，只有 `purchase_price`（进货价）/ `sale_price`（销售价）/ `wholesale_price`（批发价）。而 `routers/forecast.py` 的付款计算（`payments/compute`，第 290、341 行）**注释写着「定稿量 × 出厂价」，代码取的却是 `COALESCE(p.sale_price,0)`（销售价）** —— 两者不可能相等，付款额必然算错。这是「注释口径 ≠ 代码口径」的典型，也是厂价必须独立成列的硬理由。
>
> **厂价的必填约束（用户 2026-09-13 明确）**：「厂价」必须作为用户的**必填字段**，否则无法下单。落地为两级：
> 1. **商品档案层（权威）**：`products.factory_price` —— 保存商品时 `factory_price <= 0` 直接拦截，提示「厂价是下单必需字段，请填写」。
> 2. **报单导入层（兜底）**：导入时逐行校验 `行级厂价 ?? 主档厂价`，**两者都为空 → 拒绝该行**（不是警告）。这正是「无法下单就必须拦住」的落点。
>
> ⚠️ **厂价不可由分销价反推**：实读显示 `厂价 = 分销价 × 规格 × 0.9` 仅 108/159 行成立，简爱系列完全不成立 → 独立维护，不反推。
>
> ⚠️ **主表须存厂价快照**：`forecast_period_items.factory_price` 在下单时冻结。否则事后调档案厂价，会让历史期次的下单金额集体漂移（同「同屏数字口径必须同源」原则）。

### （c）`forecast_extra_qty` 改键（口径修正）

现状按 `(period_start, period_end, product_id)` 唯一 —— 但**期次窗口允许重叠**（已知问题），两个窗口相同的期次会互相覆盖。改为：

```sql
-- 建议：UNIQUE(period_id, product_id)，period_id 为主键维度；period_start/end 保留仅作展示
```

### （d）报单对象维护入口：**不新建，复用已有「报单配置」页**

用户 2026-09-13 明确：「我们系统里已经有『报单配置』这个页面，用户可以自己配」。

实读确认该页面已完整存在并可用：

| 项 | 现状 |
|---|---|
| 前端页面 | `src/pages/ReportMapping.vue`（504 行），标题「报单配置」 |
| 后端接口 | `GET/POST/PUT/DELETE /api/report-mapping*` + `-/toggle` + `-/health` + `-/refs` + **`-/import`（Excel 批量导入）** |
| 数据表 | `report_mapping`（`v109` 建表），字段 `employee_id / counterparty_type / counterparty_id / system_name / report_alias / order_template / src_wh / dst_wh / is_active` |
| 表单已覆盖的字段 | 员工、**对象类型（门店 / 客户 / 本人仓）**、对象全称（可搜索下拉）、报单简称（列头）、单型（调拨单/自提订单…）、源仓+目标仓（仅「本人仓」时显示） |
| 已有健康检查 | `report_mapping_health()` —— 检测 `report_alias` 重复 |

**因此设计结论：**
1. **不新增任何报单对象维护入口**，不新建表、不加页面 —— 现有页面就是唯一权威入口。
2. 报单导入模版的 Sheet3「报单对象对照」**由系统在下载模版时动态生成**（数据源 = `GET /api/report-mapping?include_inactive=0`），**不是**一张静态清单。
3. 「美联檀溪 / 汴河」走**自提** → 用户在「报单配置」页把这两个对象配成 **对象类型 = 客户**（`counterparty_type='customer'`，即自提），并填「报单简称」= `美联檀溪` / `汴河`、「对象全称」= 对应门店全称。**本设计不写死这两个对象**，由用户自助配置。
   - ⚠️ `counterparty_type` 只有 `store / customer / self_warehouse` 三值，**自提 = `customer`，调拨 = `self_warehouse`**，没有独立的「自提」枚举。
4. 导入校验命中未配置对象时，报错文案**直接给出页面路径**（`设置›报单配置`），把用户导向已有入口而不是让他找开发。

## 2.5 主键与关联关系，以及与附件的对应关系

### 关联关系

```
forecast_periods ──1:N──► forecast_period_items ──1:N──► forecast_period_reports
   (id)                        (period_id)                    (item_id)
                                 (product_id)◄──products.id
                                 (barcode)◄────product_channel_prices.barcode
                                                  forecast_barcode_units.barcode
                                                   (report_alias)◄──report_mapping.report_alias
forecast_periods ──1:N──► forecast_extra_qty (period_id)   ← 口径修正后
```

| 关系 | 键 | 基数 | 说明 |
|---|---|---|---|
| 期次 → 主表 | `forecast_period_items.period_id = forecast_periods.id` | 1:N | 一期一次订货 |
| 主表 → 子表 | `forecast_period_reports.item_id = forecast_period_items.id` | 1:N | 一行商品 = N 个报单对象 |
| 主表 → 商品 | `product_id` / `barcode` | N:1 | 条码匹配，**需带 brand 消歧** |
| 子表 → 报单对象 | `report_alias = report_mapping.report_alias` | N:1 | 列头字典 |
| 主表 → 渠道价 | `barcode` | 1:N（4 渠道） | 仅生成附件4 时读 |
| 主表 → 单位 | `barcode` | N:1 | 仅生成附件3/4 时读 |

### 与四份附件的逐项对应

| 附件 | 部位 | 落到哪 |
|---|---|---|
| **附件1** A/B/C | 厂家编码 / 永辉编码 / 条码 | `products.product_code` / `products.yh_code` / `products.barcode` |
| **附件1** D/E/G | 规格 / 简称 / 品牌 | `products.spec` / `products.name` / `products.brand`（**+ 主表快照**） |
| **附件1** F | 单位 | `forecast_barcode_units.unit` → 兜底 `products.unit` |
| **附件1** H/I/J/K | 4 个渠道价 | `product_channel_prices` |
| **附件1** L–AF | 21 个报单对象列 | `forecast_period_reports.report_alias` + `qty` |
| **附件1** AG/AH | 合计 / 件数 | 主表 `report_qty` / `pack_qty`（**派生**） |
| **附件1** AI/AJ | 加单 / 最终下单 | 主表 `extra_qty`（录入）/ `final_qty`（派生） |
| **附件1** AK/AL | 厂价 / 下单金额 | `products.factory_price`（**权威，必填**）→ 下单时快照到主表 `factory_price` / `order_amount`（派生） |
| **附件1** AM | 订单排期 | 主表 `order_rhythm` |
| **附件2** A/B/C/D/E/F | 代码/条码/永辉代码/简称/规格/单位 | `products` |
| **附件2** G/H/I/J | 分销/永辉/沃尔玛/美联价 | `product_channel_prices`（4 渠道） |
| **附件3** `*源单据号` | DB{日期}{01起} | **按 `period_id` 确定性生成**，建议落 `forecast_period_docs` 记录，防重复 |
| **附件3** `*调出仓/调入仓` | 总仓 / 业务员仓 | `report_mapping.src_wh` / `dst_wh`；`counterparty_type='self_warehouse'` |
| **附件3** `*单位/*订单数量` | 单位 / 数量 | `unit` / 子表 `qty` 汇总 |
| **附件4** `*源单据号` | ZT{日期}{21起} | 同上，确定性生成 |
| **附件4** `*业务员/部门/仓库` | 张俊峰 / 湖北福宝商贸 / 总仓 | 系统固定值（已在 `routers/forecast.py` 定义） |
| **附件4** `客户名称` | 9 个客户 | `report_mapping.system_name`（**全称**），对齐 `report_alias`（**简称**） |
| **附件4** `*单价(折后价)` | 按客户取价 | `customer_prices` → 兜底 `product_channel_prices`[该客户的渠道] |
| **附件4** `*数量` | 数量 | 子表 `qty` 汇总 |

> ⚠️ **附件4 客户名与附件1 列头不是逐字相同**，靠 `report_mapping` 的 `report_alias`↔`system_name` 映射消化：谢总→宜城谢总、易胜**琳**→易胜**玲**（错别字，建议以 `system_name` 为准）、东津→永辉东津店、吾悦→永辉吾悦店、民发→永辉民发店、美联保康→美联（保康）店。**这张表已经存在，不需要新建。**

## 2.6 布局与结构方案

### 先说清楚：「系统里彻底不存合计行」到底是什么意思

这是上一轮的第 4 条确认项，用大白话解释。

**Excel 的现状**：附件1 第 161 行是一行「合计」，每个格子里是一个公式，比如 `=SUM(L2:L109)`。它相当于**把总数抄在一张纸上，以后一直看那张纸**。一旦明细加到了第 160 行而公式还停在 109 行，那张纸上的数就是错的 —— 而且**看上去和真的一模一样**。这正是第 1 号问题的成因（王琴合计显示 **0**，实际 **136**）。

**"系统里不存合计行"的意思是**：

| | Excel 现状 | 系统方案 |
|---|---|---|
| 数据库存什么 | 每一格的数量 **＋ 一行合计** | **只存每一格的数量** |
| 合计怎么来 | 公式算一次，把**死数字**写进格子 | 每次要显示/导出时，**当场把明细加起来** |
| 明细改了 | 合计**不会自动跟着变**（除非公式正好覆盖到） | 合计**自动跟着变**（它本来就是明细算出来的） |
| 出错可能 | 公式漏行 → 合计偏小，**无从察觉** | **不可能对不上** —— 合计在定义上就是明细之和 |

打个比方：Excel 是"先把总数抄在纸上、以后一直看那张纸"；系统是"每次用之前重新数一遍"。

**具体落点**：
- 数据库里**没有** `total_qty` / `total_amount` 这类列；
- 页面上那个「合计」行是**渲染时算的**（对子表求 Σ，或由接口返回聚合值）；
- 导出的 Excel **可以有**合计行，但那是**导出那一刻算出来的静态值**，不回流数据库。

**这样做的好处**：从根上消灭"合计行与明细不一致"这一整类错误，不需要任何人记得去改公式。

### 存储用「长表」，展示用「宽表」

| | 长表（规范化） | 宽表（附件1 同构） |
|---|---|---|
| 结构 | 一行 = 一个格（商品 × 报单对象） | 一行 = 商品，一列 = 报单对象 |
| 加一个报单对象 | INSERT 一行 | **ALTER TABLE 加一列** |
| 跨期对比 | 直接 GROUP BY | 每期列不同，无法统一 |
| 人工核对 | 需要透视 | 所见即所得 |
| **结论** | ✅ **做存储** | ✅ **做展示/导出** |

**具体方案：**
1. **数据库 = 子表长表**（`forecast_period_reports`）——唯一事实源。
2. **页面/导出 = 透视成附件1 同构矩阵** —— 商品行 × 报单对象列（列由该期次实际出现的 `report_alias` 动态生成）+ 右侧下单列（主表 7 列）。
3. **附件3/4 = 导出视图，不是表** —— 每次生成时由「主表 × 子表 × report_mapping × 价格」实时拼装（现有 `routers/forecast.py` 已是这个模式），**不落中间表**，只在生成后记录单据号。
4. **建议新增 `forecast_period_docs`（单据登记表）**：`(period_id, doc_no, doc_type, counterparty_alias, generated_at)` —— 让「本期已生成哪些单、单号是什么」可查、可重生成时幂等。

### 建议的前端布局（对齐现有 Forecast.vue）

```
┌ 期次选择 [9月9日报单→9月13日到货 ▾]   状态: 待付款 | 到货 09-13 ┐
├─────────────────────────────────────────────────────────────┤
│ 视图: (•)汇总交叉表  ( )下单表  ( )报单明细                     │
├─────────────────────────────────────────────────────────────┤
│ 汇总交叉表（默认视图 = 附件1 同构）                              │
│  条码  简称  规格 单位 | 唐成 刘善涛 王琴 … 美联保康 | 合计 件数 加单 最终下单 厂价 金额 排期 │
│  6934… 红桶   10   桶  |  0    10    0  …     10    | 150  15   0    15    99  1485  订单+3 │
│  ...                                                        │
├─────────────────────────────────────────────────────────────┤
│ [下载报单导入模版] [导入报单] [生成舟谱模板 ▾ 自提/调拨] [付款确认] │
└─────────────────────────────────────────────────────────────┘
```

---
---

## 2.7 存量商品批量补厂价入口（用户 2026-09-13 第 4 点「要」）

### 为什么必须先有这个入口

厂价是**本轮新补的字段**，存量 SKU 的 `factory_price` 全部为 0。而「条码已在档案、但档案里从没录过厂价」的商品，在报单导入时会**被拒绝该行**（§3.4 规则 6）。
**没有批量入口 →「厂价必填」上线当天，所有老商品的报单全部导不进来。** 这是必须先修的前置。

### 入口位置（不新增一级按钮）

`商品档案`（Products）页 → 现有工具栏下拉里加一项：

```
[批量操作 ▾]
  ├─ 批量修改分类
  ├─ 批量调整售价
  └─ 批量补厂价        ← 新增（默认只列出 factory_price = 0 的商品）
```

### 交互设计（三步，尽量少点击）

**第 1 步 · 只列「缺厂价」的商品**（默认过滤 `factory_price <= 0 AND is_active = 1`）

| 勾选 | 商品条码 | 商品名称 | 规格 | 单位 | 现厂价 | 输入厂价 | 参考：分销价 |
|---|---|---|---|---|---|---|---|
| ☑ | 6934665094026 | 红桶 | 10 | 桶 | 0 | `[____]` | 4.2 |
| ☑ | 6934665091117 | CD杯8杯 | 200g | 瓶 | 0 | `[____]` | 3.6 |

- 「参考：分销价」放最右，**只作填值参考、不自动带出** —— 实读已证明「厂价 ≠ 分销价 × 规格 × 0.9」（该式仅 108/159 行成立，简爱系列完全不成立），自动带出等于**批量写错**。
- 表头提供「全选 / 反选 / 按品牌筛选 / 按分类筛选」，便于分批处理。

**第 2 步 · 两种填法任选**

| 方式 | 适合 | 操作 |
|---|---|---|
| **页内逐行填** | 数量少（< 30） | 直接在「输入厂价」列填，`Enter` 下移 |
| **导出回填**（推荐） | 数量多 | 「导出待补清单」→ 得到只含 `条码 / 名称 / 规格 / 空厂价列` 的 xlsx → 填好 → 「导入回填」 |

- **导出回填复用现有导入通道**：`/api/import/execute` 的 `products` 分类已有模版；只需在 `COLUMN_PATTERNS["products"]` 加 `"厂价"` 关键词、把 `factory_price` 加进 `_all_fields["products"]`。
- 回填的匹配键 = **条码**（与报单导入同一口径），避免同名不同品被改错。

**第 3 步 · 保存与反馈**

- 保存走新增的批量接口 `POST /api/products/batch-factory-price`，body `[{id, factory_price}]`，**一次事务**。
- 校验：`factory_price > 0`（`<= 0` 的行拒绝并列出）；**不校验其它字段**（这是补价，不是改档案）。
- 返回：`已更新 N 个商品，跳过 M 个（数值无效）`。
- **不做「填完即刻重导」的隐藏联动** —— 用户改完自己重新导入即可。

### 设计取舍

| 决策 | 理由 |
|---|---|
| 只列「缺厂价」的商品 | 补价入口不该变成改价入口，避免误改已维护好的价格 |
| 参考列给「分销价」而非自动反推 | 反推公式经实读**不成立**（108/159），自动带出 = 批量写错 |
| 保存时不校验其它字段 | 批量场景只想补一个字段，多余校验会制造失败行 |
| 复用 import 通道做导出回填 | 用户已熟悉「下载模版 → 填 → 导入」，零新学习成本 |

### 与本轮其它交付的关系

- 与 **§2.4(b)「`products` 补 `factory_price`」**：一个补字段、一个补数据，**必须同时交付**。
- 与 **用户第 3 点「允许存，到报单导入时才拦」**：档案保存时不拦厂价（存量可继续存），只在**报单导入**时按「行级 ?? 档案」判定，缺则拒行。本入口让用户能低成本把缺失补上。

> 本轮只交付**设计**；批量补厂价入口的代码（`Products.vue` 批量面板 + `batch-factory-price` 接口）待排期。

---

# 三、报单导入模版设计

## 3.0 方向修正：模版同时承担「建档」（用户 2026-09-13 第 1 点）

**用户原话**：

> 我们要从新用户的角度出发，现在有一个新用户要使用我们的系统，它可能只想用预报订单功能，所以就不想档案管理里去录入档案，直接到预报订单汇总表里点击「导入」下载导入模版，我们要做的是用户填好必填字段后，对应信息直接落到商品档案里，且要以条码为口径去重。

这一条把模版的职责从「只报单」升级为「**报单 + 建档**」，带来三个设计变化：

| # | 变化 | 原因 |
|---|---|---|
| 1 | 模版必须**自带建档案所需的列** | 系统若只拿到条码，建出来的档案没有规格/单位 —— 而**规格是算件数的必需项**（件数 = 报单数量 ÷ 规格），单位是舟谱单据必填 |
| 2 | 形态 = **宽表**（行=商品、列=报单对象），与用户现有报单汇总表同形 | 用户是从「预报订单汇总表」页的「导入」按钮下载的，那里就是交叉表；沿用同一形态 → 用户零学习成本，老表也能直接导入 |
| 3 | 报单对象列**按租户「报单配置」动态生成** | 用户下载到的就是自己的业务员/门店名，填完可直接回传，不必先改列名 |

**已落地（代码）**：

| 位置 | 改动 |
|---|---|
| `db/queries/products.py` | 新增 `product_get_by_barcode()`（精确查）与 `product_ensure_from_import()`（幂等建档） |
| `routers/import_router.py::_execute_forecast_cross` | 匹配不到商品时**自动建档**；返回 `products_created / products_reused / unmatched_products` |
| `routers/import_router.py::download_template_file` | 模版改 7 列三态 + 报单对象列按 `report_mapping` 动态生成 |

## 3.1 必填字段：分两级（必填 / 条件必填）

原则不变（**只让用户填「系统猜不到」的信息**）。但「新用户零档案」这个新场景下，名称/规格/单位/厂价**第一次必须填** —— 因为档案里还没有。于是分两级：

| 级别 | 标记 | 列 | 什么时候必填 |
|---|---|---|---|
| **必填** | 红底 `*` | 商品条码 | **永远** —— 它既是去重口径，也是商品的唯一识别键 |
| **条件必填** | 黄底 `☆` | 商品名称 / 规格 / 单位 / 厂价 | **档案里还没有这个条码时**；已存在的商品留空即可，导入时自动带出档案值 |
| 选填 | 无标记 | 品牌 / 分销价 | — |

**判据是「档案里有没有」，不是「第几次导入」** —— 这样同一份模版对新老用户都成立，不必准备两套模版，也不需要用户自己判断该不该填。

### 缺列时的分级兜底（不是所有缺失都同等严重）

| 缺什么 | 处理 | 为什么 |
|---|---|---|
| 缺 **厂价** | **拒绝该行** ★ | 下单金额的基数，缺了无法下单 |
| 缺 **名称** | 条码已在档案 → 取档案名；条码全新 → **拒绝该行** | `products.name` 是 NOT NULL，无名无法建档 |
| 缺 **单位** | 兜底「件」+ **警告** | 舟谱单据需要单位；但不该因此挡住报单 |
| 缺 **规格** | 建档成功 + **警告** | 件数 = 数量 ÷ 规格；缺规格则件数/取整/下单金额无法算，但报单本身仍有效 |

### 各字段的来源归属（谁填、还是系统带出）

| 字段 | 只有用户知道？ | 结论 |
|---|---|---|
| 谁报的（报单对象） | ✅ 是 | **必填**（列头即对象名，按「报单配置」生成） |
| 报的哪个商品 | ✅ 是 | **必填**（条码） |
| 报了多少 | ✅ 是 | **必填**（格=数量） |
| 商品名称 | ⚠️ 新商品必须填（建档用） | **条件必填** |
| 规格 | ⚠️ 新商品必须填（算件数用） | **条件必填** |
| 单位 | ⚠️ 新商品必须填（舟谱单据用） | **条件必填** |
| 厂价 | ⚠️ 新商品必须填；老商品可留空自动带出 | **条件必填（硬条件）** —— 两边都空 → 拒绝该行 |
| 品牌 | ❌ 仅共码商品需要 | **选填** |
| 分销价 / 永辉价 / 美联价 | ❌ 渠道价表有 | **选填**（生成舟谱自提模版才用） |
| 合计 / 件数 / 最终下单 / 下单金额 | ❌ 算出来 | 自动计算 |
| 订单排期 | ⚠️ 期次级默认，个别行不同 | 选填（行级覆盖） |

## 3.2 模版结构（宽表，与现有报单汇总表同形）

**获取方式**：预报订单汇总表页 → 工具栏「导入」→ 弹窗内「下载模板」
（`GET /api/import/template-file/forecast_cross` ← 前端 `downloadFcTemplate()`）

**文件名**：`预报订单导入模板.xlsx`

### Sheet1「导入模板」

**第 1 行 = 表头（7 列商品身份 + N 列报单对象）**

| # | 表头 | 级别 | 类型 | 说明 |
|---|---|---|---|---|
| A | `商品条码*` | 必填 | 文本(13位) | **建档与去重的唯一口径** |
| B | `商品名称☆` | 条件必填 | 文本 | 档案无此条码时必填（`products.name` NOT NULL） |
| C | `规格☆` | 条件必填 | 文本 | 算件数必需：件数 = 报单数量 ÷ 规格 |
| D | `单位☆` | 条件必填 | 文本 | 舟谱单据必填；缺省兜底「件」 |
| E | `厂价☆` | 条件必填 | 数值 > 0 | **缺 → 拒绝该行**（下单金额的基数） |
| F | `品牌` | 选填 | 文本 | 仅共码商品（恒滋/福宝）需填以区分 |
| G | `分销价` | 选填 | 数值 | 生成舟谱自提模版时的折后单价；下单环节不需要 |
| H+ | **报单对象列** | 至少一列有值 | 数值 ≥0 | **按租户「报单配置」动态生成**；未配置时回退「示例客户1~10」 |

**第 2 行 = 示例数据行** —— 填写说明第一条就是「先删示例行，否则会录入一个假商品」。

**样式**：`*` 红底白字 / `☆` 黄底深字 / 无标记加粗。

**实测（生产 tenant_1，2026-09-13 下载真机验证）**：

```
表头：商品条码*  商品名称☆  规格☆  单位☆  厂价☆   品牌  分销价  美联保康  刘善涛
底色： C0392B    FFF2CC    FFF2CC  FFF2CC  FFF2CC  无    无      无       无
```

后两列 `美联保康`、`刘善涛` **就是 tenant_1 真实的「报单配置」对象**（不是占位名）—— 用户下载到的模版直接可用。

### Sheet2「填写说明」（10 条）

先删示例行 / 表格怎么用 / 必填 / **条件必填是什么意思** / 厂价缺了会怎样 / 规格与单位的用处 / 品牌何时填 / 分销价的用途 / 报单对象列的来源 / 重复导入的覆盖规则。

### 与上一版的两处结构性改动

| 上一版 | 本版 | 为什么 |
|---|---|---|
| 长表（一行 = 对象 × 商品，含「报单对象」列） | **宽表**（行 = 商品，列 = 报单对象） | 用户是从「预报订单汇总表」页进入的，那里就是交叉表；同一形态让**老用户现有的汇总表可直接导入**，新用户也零学习成本 |
| 静态 Sheet3「对照表-报单对象」 | **删除** | 用户明确「系统已有『报单配置』页，用户可以自己配」；静态清单必然过期，改为按 `report_mapping` 实时生成对象列 |

## 3.3 自动建档机制（本轮新增，核心）

### 以条码为口径去重 —— 四种结局

`db/queries/products.py::product_ensure_from_import(barcode, name, spec, unit, brand, factory_price, dist_price)`
返回 `(product_id, action, detail)`：

| action | 触发条件 | 行为 | 写档案？ |
|---|---|---|---|
| `created` | 条码在档案里**找不到** | 用导入行信息新建商品（`source='forecast_import'`，可溯源） | ✅ 新增 1 行 |
| `reused` | 条码**已存在** | 复用该商品；**只回填库内为空的字段**（规格/单位/品牌/厂价/分销价） | ⚠️ 仅补空 |
| `conflict` | 条码已存在，但**名称不同 且 两边品牌也不同**（恒滋/福宝共码场景） | **不新建、不覆盖**；复用库内那条，冲突写入 `barcode_conflicts` 台账 | ❌ |
| `skipped` | 无条码；或有条码但缺名称 | 该行报单挂 `product_id=0`，并在返回的 `unmatched_products` 里列出 | ❌ |

### 三条设计铁律

1. **绝不覆盖档案已有值** —— 建档只补空字段，用户精心维护的档案不会被一次导入改坏。
2. **无条码不建档** —— 条码是去重口径；没有口径就建档，等于每导入一次多一个档案。
3. **共码不猜** —— 条码相同、名称与品牌都不同时不假设它们是一个商品，也不覆盖，而是**报冲突交人工裁决**（复用已有的 `/api/import/barcode-conflicts` 台账 + 页面）。

### 为什么条码能当去重锚点

`products.barcode` 上**已有部分唯一索引**（`erp_db.py` v89 迁移，早已存在）：

```sql
CREATE UNIQUE INDEX IF NOT EXISTS idx_products_barcode ON products(barcode) WHERE barcode!=''
```

所以 DB 层已经保证「非空条码不重复」；建档逻辑只需 INSERT 前先按条码精确查一次即可幂等 —— 并发下即使撞上唯一索引，也会回读复用（代码 `except` 分支处理）。

### 顺带修掉的一个既有缺陷（会张冠李戴）

旧 `_match_product()` 在条码**精确匹配失败**时仍 `return rows[0]["id"]`，而它查的是
`product_list(keyword=barcode)`（对 name/barcode/spec/mnemonic/alias 做 `LIKE %…%`）
→ **只要商品名称里恰好含这串数字，订单就会挂到别的商品上**。已改为：

- 条码 → `product_get_by_barcode()`（走唯一索引精确查）
- 名称 → `product_list` 后**完全相等**比较

### 「命中已有档案」也必须回填 —— 一个被真机 E2E 抓出来的静默缺口（2026-09-13 晚修）

原实现有**两条建档路**，只有一条会回填：

| 路 | 触发 | 原行为 |
|---|---|---|
| A | 条码在档案里**找不到** → 调 `product_ensure_from_import()` | `reused` 分支会「只补空」回填 ✅ |
| B | `_match_product()` **命中已有档案** | 只 `_prod_reused += 1` 就往下走，**从不调回填** ❌ |

后果：**「老商品档案里没厂价、这次导入行带了厂价」这个最常见的补价场景永远补不进去**，
而回执显示「复用 N 个」（不报错、不提示）——用户以为补上了。

**修法（两条路共用同一实现，不留第二份拷贝）**：

- 把「只补空、绝不覆盖」抽成 `db/queries/products.py::product_backfill_from_import(pid, …)`
  —— 只动 `spec/unit/brand/factory_price/dist_price` 五个字段，**绝不碰 `barcode`/`name`**
  （条码是去重锚点，自动填会撞 `barcode!=''` 的部分唯一索引）；
- `product_ensure_from_import` 的 `reused` 分支改为调用它；
- 报单导入执行体在 `_match_product` 命中时也调用它，并把结果并进回执
  （`products_backfilled` / `products_backfilled_count`）。

**顺带收紧的一处口径**：建档/回填用的厂价只认「**厂价列 → 通用单价列**」，
**绝不用分销价顶厂价**（分销价 ≠ 厂价，误写进档案会让付款金额整体偏高）；
行内金额估算仍保留三级回退（厂价 → 单价 → 分销价）。

### 导入返回新增的可观测字段

```json
{"results": {
   "products_created": ["蒙牛纯甄200g（6901234567890）"],
   "products_created_count": 1,
   "products_reused_count": 12,
   "products_backfilled": ["红桶（恒滋下单）（补 factory_price/dist_price）"],
   "products_backfilled_count": 2,
   "unmatched_products": ["红桶：条码 69346… 是新商品但缺「商品名称」，无法建档"]},
 "barcode_conflicts": [{"barcode":"…","type":"existing_name_conflict","message":"…"}]}
```

前端据此提示「本次导入新建 N 个商品、复用 M 个、**补进已有档案 K 个**、有 J 行无法建档」，
并在导入结果弹窗里逐条列出「新建 / 补录」明细（各前 5 条）。

## 3.4 校验规则（导入时逐行执行）

| # | 规则 | 处理 | 提示文案（中文、带行号、给改法） |
|---|---|---|---|
| 1 | 未识别到报单对象列 | 拒绝整单 | `未识别到报单对象列：请在预览里把客户列映射为「客户」` |
| 2 | 报单对象不在「报单配置」中 | **警告，不阻断** ★改 | `「美联檀溪」未在报单配置中维护，已按名称原样落库，请到 设置›报单配置 补配` |
| 3 | **条码为空且名称也匹配不到** | 拒绝该行 | `「某个散货」既无条码、名称也未匹配到档案，无法建档` |
| 4 | **条码是新的但缺商品名称** | 拒绝该行 | `条码 6934665010569 是新商品但缺「商品名称」，无法建档` |
| 5 | 条码**已存在**档案 | 复用，不报错 | 静默；回填空字段时记入 `products_reused_count` |
| 6 | **厂价缺失**（行级「厂价」与档案**都为空**） | **拒绝该行** ★ | `第 5 行商品「红桶」没有厂价，无法下单 —— 请在档案补填，或在本行「厂价」列填写` |
| 7 | 缺**单位** | 警告 + 兜底「件」 | `第 5 行商品缺单位，已按「件」处理，生成舟谱单据前请补录` |
| 8 | 缺**规格** | 警告 | `第 5 行商品缺规格，件数与下单金额无法计算` |
| 9 | **加单不是整数** | 拒绝该行 | `第 5 行「加单」必须为整件（整数），当前为 1.5` |
| 10 | 数量为空 / ≤ 0 | 跳过该格 | （不计入报单） |
| 11 | 同条码但**名称/品牌都不同** | 记入冲突台账；落库仍复用原商品 | `条码 X 已是「A」(甲品牌)，与导入行「B」(乙品牌) 冲突` |
| 12 | 整表 0 行有效 | 拒绝整单 | `未识别到有效数据行，请确认表头未被删除` |
| 13 | 期次已定稿 / 已付款 | 拒绝整单 | `本期待预报已定稿，如需修改请先撤销定稿` |

> ★ **规则 2 由上一版「拒绝该行」降为「警告」**：报单对象由用户自己在「报单配置」维护，而**新用户第一次用时配置可能还没建全** —— 拒绝会让他连第一次导入都完不成。改为按原样落库 + 提示去补配，事后补上即恢复为正常走单（原「防漏单闸门」的意图仍保留在这条提示里）。
>
> 规则 3/4 取代了上一版的「条码在商品主档查不到 → 拒绝该行」：现在条码查不到是**可建档**的正常路径，只有「既无条码、名称也匹配不到」或「新条码但没有名称（name NOT NULL）」才真的无法建档。

## 3.5 导入后的自动计算链（用户不需要填的 7 个字段）

```
子表 qty 汇总                    → 主表 report_qty    （附件1 AG）
report_qty ÷ spec                → 主表 pack_qty      （附件1 AH）保留 4 位小数
取整(pack_qty, round_mode) + extra_qty → 主表 final_qty（附件1 AJ）★整数
factory_price × final_qty        → 主表 order_amount  （附件1 AL）
```

**取整规则（本次新增，修复 §1.5 问题 2）**：
- `round_mode='up'` → `CEIL(pack_qty)`（不拆零发货，宁多不少）
- `round_mode='down'` → `FLOOR(pack_qty)`（控库存，宁少不多）
- `round_mode='none'` → 保留小数（仅调试用）
- **默认 `up`**；可在商品档案或单行覆盖。
- `final_qty` 必须为整数 —— 生成附件3/4 时若出现小数，**直接报错**（对应现有 `_fmt_qty()` 的静默 `int()` 截断，建议改为显式校验）。

## 3.6 兼容模式：老用户直接用附件1 宽表导入

系统**已有**这套能力（`routers/import_router.py` 的 `forecast_cross` 识别 + `_CROSS_SKIP_KEYWORDS` 跳过「合计/件数/最终下单/下单金额/分销价格/加单/订单排期」等计算列）。本轮在其上做了三处修正，均已落地：

| # | 改动 | 为什么 |
|---|---|---|
| 1 | **`_detect_forecast_cross` 改为「一列只归属一个身份字段」**，并按特异性重排 `COLUMN_PATTERNS` 顺序（`barcode/spec/unit/brand/factory/dist/rhythm/price/name`） | 旧实现允许同一列写进多个字段：`name` 的关键词含泛词「商品」→ **「商品条码」「商品编码」列会同时被 `name` 命中**，导致商品名取到条码值。同时 `factory/dist` 必须排在通用 `price` 之前，否则「厂价/分销价」被「价格」吃掉 |
| 2 | `_CROSS_SKIP_KEYWORDS` 已含「分销价格/永辉/编码」等 | 否则附件1 里若填了渠道价，这些列会被当成**客户列**（凭空多出几个「客户」） |
| 3 | 宽表导入与长表导入**落同一套子表**（`forecast_submission_items`） | 两种入口产出同一份事实，不另起一套 |

**实测（2026-09-13）**：附件1 形状表头 →
`identity = {name:0(简称), barcode:2(条码), spec:3, unit:4, factory:5(厂价), dist:6(分销价格), rhythm:7}`
客户列 = `[唐成, 黄家伟, 王琴]`；「商品编码 / 合计 / 件数 / 最终下单」**均未被误判为客户列**。

---

## 附：需确认事项

### 第 1 轮 5 条 —— 用户已答复（2026-09-13 上午）

| # | 问题 | 用户答复 | 已落到设计哪里 |
|---|---|---|---|
| 1 | 取整方向 | **按「商品级 + 默认向上」** | §2.4(b) `products.order_rounding`、§2.2 主表、§3.5 计算链 |
| 2 | 厂价来源 | **附件1 下单表里就有；厂价必须作为用户的必填字段，否则无法下单** | §1.6 问题 7/8、§2.4(b)、§3.1、§3.4 规则 6 |
| 3 | 美联檀溪 / 汴河 | **走自提**；且**系统已有「报单配置」页，用户可自行配置** | §2.4(d)（不新建入口）+ §3.2 对象列动态生成 |
| 4 | 「不存合计行」 | 用户表示**未理解** | §2.6 新增「是什么意思」专节（大白话解释） |
| 5 | 是否要实体 xlsx | 要求**据本轮新情况调整模版** | §3.2（现为宽表 7 列三态） |

### 第 2 轮 4 条 —— 用户已答复（2026-09-13 下午，**本轮据此改代码**）

| # | 问题 | 用户答复 | 落地情况 |
|---|---|---|---|
| 1 | 新用户不想先录档案怎么办 | **从预报汇总表页点「导入」下载模版 → 填好必填字段 → 信息直接落商品档案，以条码为口径去重** | ✅ **已实现并部署**：`product_ensure_from_import()` 幂等建档（4 种结局）；模版改 7 列三态宽表；对象列按「报单配置」动态生成。见 §3.0 / §3.2 / §3.3 |
| 2 | `payments/compute` 用 `sale_price` 顶包厂价要不要修 | **要修** | ✅ **已实现并部署**：先补 `products.factory_price`（建表 DDL + `v132` 迁移），再改 `forecast.py:290/:341` 两处取价。**采用「厂价优先 + 缺则回退销售价 + 显式回报回退行数」**，避免存量 SKU 厂价全为 0 时付款额静默归零。E2E 已验证 `price_stat` 在生产返回 |
| 3 | 档案「厂价必填」拦在哪一步 | **允许存，到报单导入时才拦** | ✅ 已落到 §3.4 规则 6（导入时按「行级 ?? 档案」判定，缺则拒行）；档案保存不设拦 |
| 4 | 要不要存量商品批量补厂价入口 | **要** | ✅ 设计已出：**§2.7**（商品档案 → 批量操作 → 批量补厂价，页内填 / 导出回填两种方式）。代码待排期 |

### 本轮已直接落地（代码已部署生产，无需再确认）

| 文件 | 改动 |
|---|---|
| `erp_db.py` | `products` 建表加 `factory_price`；新增 `v132_products_factory_price` 迁移；re-export 两个新函数 |
| `routers/forecast.py` | `payments/compute` 与 `payments/preview` 取价改 `factory_price` 优先 + 回退；返回新增 `price_stat` |
| `routers/import_router.py` | 自动建档接线；`_match_product` 改精确查（修张冠李戴）；`_detect_forecast_cross` 改「一列只归属一个字段」（修 `name` 吞掉「商品条码/商品编码」）；`COLUMN_PATTERNS` 重排优先级；模版改 7 列三态 + 对象列动态生成 |
| `db/queries/products.py` | 新增 `product_get_by_barcode()`、`product_ensure_from_import()`；`product_create/update` 白名单补 `factory_price`（+ `source`） |

**部署与验证（2026-09-13）**：4 文件 scp 到 `/opt/hergent-erp/` 扁平布局 → health `200` → 落点 grep 全部命中 → 生产 3 个库（`erp.db`/`tenant_1`/`tenant_10`）`products` 均已含 `factory_price` → 下载模版实测表头为 `商品条码* 商品名称☆ 规格☆ 单位☆ 厂价☆ 品牌 分销价 美联保康 刘善涛`（末两列 = tenant_1 真实报单配置对象）→ `payments/preview` 返回 `price_stat.caliber`。

### 第 3 轮 4 条 —— 用户已答复「1234 全按建议」（2026-09-13 晚，**全部已落地并上线**）

| # | 问题 | 答复 | 落地情况 |
|---|---|---|---|
| 1 | 模版「厂价」列保留否 | **保留**（两条路都留） | ✅ 条件必填维持 4 列；理由：新用户零档案时没这列算不出金额。说明文案已写清「条件必填」 |
| 2 | 存量批量补厂价入口做不做 | **做** | ✅ 已上线：商品档案工具栏「补厂价 N」入口（仅缺价 > 0 时出现）+ 行内改 + 批量面板（页内填 / 导出回填 xlsx/CSV）。见 §2.7 |
| 3 | 前端显示建档结果 | **做** | ✅ 已上线：导入结果弹窗新增「商品档案」块（自动建档 / 复用已有 / **补进已有档案** + 新建与补录明细 + 未建档与条码冲突提示） |
| 4 | 4 个后端文件 commit | **commit** | ✅ 后端 `c3567b9`（5 文件 +443/−60）、前端 `1e57276`（3 文件 +347/−13），均在各自主干分支 |

### 本轮（第 3 轮）实际交付清单

| 侧 | 文件 | 关键改动 |
|---|---|---|
| 后端 | `db/queries/products.py` | 新增 `batch_set_factory_prices()`（只写厂价、非法值逐条回报、commit 后留痕）；新增 `product_backfill_from_import()`（「只补空」唯一实现） |
| 后端 | `routers/import_router.py` | 修「命中已有档案不回填」；`_CROSS_IDENTITY_FIELDS` 收敛为单一真相源（原执行体手写 6 元组漏了 `factory/dist/brand` → 模版厂价被**静默丢弃**）；厂价只认厂价列/单价列；回执加 `products_backfilled*` |
| 后端 | `server.py` | 新增 `POST /api/products/batch-factory-price` |
| 后端 | `routers/data.py` | `bulk_upsert_products` 的厂价改「显式提供才写」（否则 Forecast 网格保存会把刚补的厂价清零） |
| 前端 | `ProductArchive.vue` | 工具栏补厂价入口 + 厂价列行内可编 + 批量面板（筛选/全选/导出待补清单/导入回填）+ 详情与导出补厂价 |
| 前端 | `Forecast.vue` | 预览识别面板拆出「规格列/单位列/厂价列」（厂价未识别标黄）+ 导入结果「商品档案」块 |
| 前端 | `api/modules.js` | `productsApi.batchFactoryPrice()` |

**验证**：隔离测试 32/32 + 18/18；生产真机 E2E（HTTP）22/22；生产真机 E2E（浏览器实跑）全流程通过；
测试数据与留痕全部还原。生产后端备份 `/root/backup_v157c_20260913/`、前端 `hergent-cn-v2-bak-20260913-2030.tar.gz`。

### 仍待你拍板

1. **导入行自身的「规格/单位」缺省时，要不要从档案带出？** 模版填写说明写的是
   「已存在的商品留空即可，导入时会自动带出档案里的值」，但**当前只有「导入行 → 档案」方向的回填**；
   导入行若把「规格」留空，行内规格就是空，而「件数 = 数量 ÷ 规格」的换算会受影响。
   两种口径要选一个：① 导入行缺省时**按档案补齐**（说明文案即成真，但会改变留空行的换算行为）；
   ② 或把说明文案改成「规格/单位请自行填写」。**当前实现是 ②（未改文案、也不补齐）**，需拍板。
2. **线上仓库 HEAD 不自洽（既有问题，非本轮引入）**：`Workbench.vue` 引用 `stripAllFences`，
   而该导出只存在于**在途未提交**的 `composables/useCardTrigger.js` 里 → 单独 checkout HEAD
   再 `npm run build` 必失败。把「卡片协议 / CSS 全局层」那批在途文件一起提交即可解，
   属于并行在途工作，本轮未动。

---

## 附录：本次实读的原始证据索引

| 结论 | 证据 |
|---|---|
| 合计行只求和到第 109 行 | `L161=SUM(L2:L109)`、`AG161=SUM(AG2:AG109)`、`AJ161=SUM(AJ2:AJ109)`；王琴列实数据在 R111–R158 |
| 王琴列实际合计 136 | R111=8, R118=24, R119=24, R126=10, R127=10, R132=6, R133=6, R139=12, R145=24, R158=12 |
| 件数 = 合计 ÷ 规格 | `AH2=AG2/D2` = 150/10 = 15 |
| 加单为整件且仅用 1 次 | `AI96=18`，其余 158 行全空 |
| 最终下单无取整规则 | 116 行 `=SUM(AH:AI)`；43 行手填；差值分布 0→117 次、−14→1 次、+198→1 次 |
| 渠道价三列全空 | `I1="永辉价" J1="沃尔玛价" K1="美联价"`，非空值均为 0 |
| 分销价是派生公式 | `H2=(AK2/D2)/0.9`（90 行）、`=(AJ2/D2)/0.9`（49 行） |
| 附件4 单价按客户取价 | 唐成 14/14 命中附件2 G；永辉东津店 17/17 命中 H；美联保康 38/38 命中 J（共 168/168） |
| 附件3 数量与附件1 吻合 | DB2026091301–08 数量合计 1881/929/1496/1591/949/136/2265/1559，与附件1 8 列实算**完全一致** |
| 条码不唯一 | `6934665094026`、`6934665093944`、`6934665091254` 各出现 2 次 |
| 17 个条码缺有效分销价 | 10 个查无商品 + 7 个有商品无价 |
| 系统已有舟谱模板引擎 | `routers/forecast.py:403 ZT_HEADERS`、`:409 DB_HEADERS`、`:421 _UNIT_HINTS` |
| 系统已有报单对象字典 | `erp_db.py:609` `report_mapping(report_alias, counterparty_type, system_name, src_wh, dst_wh)` |
