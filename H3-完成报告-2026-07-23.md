# H3 完成报告 — DB 连接池 + 幂等迁移（去 duplicate-column 刷屏）

> 执行日期：2026-07-23
> 执行者：Senior Developer（高级开发工程师，自主推进）
> 生产环境：47.113.224.140:8700（`/opt/hergent-erp/`，systemd `hergent-erp`）
> 触发指令：用户「启动 H3」

## 一、目标

1. **根除启动刷屏**：`erp_db.py` 启动 `_safe_migrate` 全量跑 145 条 `ALTER TABLE ... ADD COLUMN`，已应用的列每次启动都抛 `duplicate column name`，被 `except` 捕获后又触发 `UNIQUE constraint failed: _migrations.name` → **约 171 行 `[Migration] FAILED` / 次启动**。
2. **消除连接抖动**：`db/connection.py` 每 `get_db()` 都 `sqlite3.connect()` 新建连接、`finally` 里 `close()`，无复用 —— 高并发下连接创建开销大、易现 `database is locked`。

## 二、改动明细（3 个 commit，均已部署生产根）

### H3.1 幂等迁移（commit `1c37c3c`，`server/erp_db.py`）
- 新增助手：
  - `_migration_applied(name)`：查 `_migrations WHERE name=? AND status='ok'`，已应用直接跳过。
  - `_is_idempotent_migration_error(err)`：识别 `duplicate column name` / `already exists` / `duplicate index` 等幂等型 DDL 报错。
  - `_record_migration(name, status, error)`：`INSERT OR REPLACE`（消除原 `UNIQUE constraint failed: _migrations.name` 的二次报错）。
  - `_migration_log_error(e, name=None)`：吞掉幂等型报错、只在真实失败时打印（H3.1b 复用）。
- 重写 `_safe_migrate` / `_safe_migrate_script`：已应用跳过；幂等型报错 → 静默记 `ok` 不刷屏；真实报错 → 记 `failed` + 打印。

### H3.1b 历史 ALTER 路径（commit `2711490`，`server/erp_db.py`）
- 发现 ~40 个 `_migrate_vXX` 旧函数用裸 `executescript`/`ALTER` + 各自 `except: print("[Migration] FAILED: ...")`，**未走** `_safe_migrate`，H3.1 未覆盖。
- 全局替换 ~40 处 `print("[Migration] FAILED: {str(e)[:200]}", ...)` → `_migration_log_error(e[, name])`，幂等报错统一吞掉。
- 效果：启动 `[Migration] FAILED` 从 H3.1 后的 13 行 → **2 行**（仅剩 2 条真实错误，见第五节）。

### H3.2 按线程 SQLite 连接池（commit `3be2e19`，`server/db/connection.py`）
- 设计：**按线程缓存连接**，key = `(thread_ident, db_path)`；保留默认 `check_same_thread=True`（连接永不跨线程，合法安全）。
- `_sqlite_connect()`：同线程已缓存 → 直接复用；重入（同线程嵌套 `get_db()`）→ 返回一次性连接（不污染缓存）。
- `_sqlite_return(conn)`：rollback；若 `conn is _sqlite_cache.get(key)` 则留用并递减 refcount，否则关闭一次性连接并递减 refcount。
- `get_db()` / `get_db_tx()` 的 `finally` 从 `conn.close()` 改为 `_sqlite_return(conn)`。
- 用 refcount dict 追踪嵌套深度，避免 `set` 方案在嵌套 > 2 层时误释放外层缓存连接。
- 本地压测通过：连接复用、重入安全、12 线程压测 `cache_size` 有界、事务提交隔离、死连接自动重建。

## 三、部署流程（铁律）

```
commit 进 git（upgrade/v84-international，分别按文件提交）
→ 精确 scp server/erp_db.py 与 server/db/connection.py 到 /opt/hergent-erp/（禁用通配符）
→ chown hergent:hergent
→ IMPORT_OK 预检：python3 -c "import erp_db" / 启动 reload 前 import 根
→ systemctl restart hergent-erp
→ 验证 /api/health=200 + 启动日志零刷屏 + 连接池冒烟
```

## 四、验证证据（生产实测）

| 指标 | 改造前 | 改造后 |
|---|---|---|
| 启动 `[Migration] FAILED` 行数 | ~171 行/次 | **2 行/次** |
| `GET /api/health` | 200 | 200（无回归） |
| `_migrations` 表 | — | `ok=205` / `failed=17` |
| 连接池冒烟 | 无（每次新建） | 6 次读 200 + 真实写 `{"success":true,"id":2926}` 200 + 列表 200 |
| 池错误（locked/check_same_thread/traceback） | — | **0** |

- 启动日志 `duplicate column` 类刷屏**已完全消除**。
- 连接池冒烟用真实建联系人写操作 + 列表读操作，**零** `database is locked` / `check_same_thread` / traceback；测试联系人已清理出 `tenant_1.db`。

## 五、残留 2 条真实迁移错误（非刷屏，建议跟进）

H3 只消除"幂等型刷屏"，以下 2 条是**真实失败**，保留打印：

1. **`v89_products_barcode_unique: UNIQUE constraint failed: products.barcode`**
   —— 业务**数据**中存在重复条码值（真实数据冲突）。建议：加数据告警 / 去重任务，而非抑制。
2. **`default value of column [created_at] is not constant`**
   —— 旧版 SQLite 不支持非常量 `DEFAULT`（如 `DEFAULT (datetime(...))`）DDL 的历史限制，环境相关，非代码缺陷。

## 六、刻意未做（控制范围）

- **高频查询补索引**：报告其余项（26 → 更多索引）未做，可并入后续或 L1。
- **Alembic 迁移框架**：按需幂等改写已达标，不必强上框架（避免引入复杂度）。
- **并发写 SQLite 文件锁限制**：仍属 L1（Postgres / 读写分离）方向，本次不解决。

## 七、结论

H3 **完成并上线**：启动稳定（刷屏消除）、连接复用池化、迁移失败仅 warn 不中断 —— 三项预期目标全部达成。生产功能无回归。

下一步建议：H4（订单明细导入，解领域引擎真值）/ 补完 gl_dimensions CRUD / 补高频索引 / 处理重复条码数据冲突告警。

## 附：Git 提交

- `1c37c3c` H3.1：idempotent `_safe_migrate` — kill startup 'duplicate column' migration spam
- `3be2e19` H3.2：safe per-thread SQLite connection pool (kill connection churn)
- `2711490` H3.1b：route legacy `_migrate_vXX` raw-ALTER failures through idempotent-aware logger
