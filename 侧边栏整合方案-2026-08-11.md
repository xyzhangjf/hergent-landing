# 侧边栏功能入口清单与整合方案（2026-08-11）

> 范围：生产环境 `erp.hergent.cn` 当前前端侧边栏（sidebar-v2 + ModuleRegistry 动态组）。
> 数据源：`static/js/modules/sidebar-v2.js`、`static/js/core/module-registry.js` 及全部 `ModuleRegistry.register(...)` 调用。

---

## 一、当前侧边栏完整清单（按所属模块分类）

侧边栏由两部分构成：
1. **顶层固定入口**（`sidebar-v2.js` 的 `render()` 硬编码，始终显示）。
2. **动态注册组**（`ModuleRegistry._rebuildSidebar()` 注入，默认折叠，位于「设置」之前）。

### 1.1 顶层固定入口（硬编码，非折叠）

| 所属区块 | 入口名称 | 内部 ID | 说明 |
|---|---|---|---|
| （无标题，顶部） | 消息 | `dami` | AI 同事 / 消息中心 |
| （无标题，顶部） | 通讯录 | `contacts` | 联系人 |
| （无标题，顶部） | 审核中心 | `review-queue` | 审核待办 |
| 经营业务 | 经营看板 | `dashboard` | 数据看板 |
| 经营业务 | 档案管理 | `archive-hub` | 商品/客户档案 |
| 经营业务 | 采销管理 | `trade` | 订单/采购 |
| 经营业务 | 仓配管理 | `stock-hub` | 库存/OperationsHub |
| 经营业务 | 资金管理 | `finance-hub` | 资金流水 |
| 经营业务 | 财务管理 | `accounting-hub` | 账务 |
| 经营业务 | 数据报表 | `reports-hub` | 报表 |
| 管理 | 设置 | `settings` | 系统设置 |

> 「经营业务」固定 7 项 + 顶部 3 项 + 设置 1 项 = **11 个固定入口**。

### 1.2 动态注册组（ModuleRegistry，默认折叠）

| 注册分组 | 入口名称 | 内部 ID |
|---|---|---|
| **仓库操作**（3） | AI采购控制台 | `procurement-console` |
| | 临期看板 | `expiry-dashboard` |
| | 报损管理 | `wastage` |
| **财务工具**（11） | 付款批 | `paymentrun` |
| | 催收管理 | `dunning` |
| | GL维度 | `gldimensions` |
| | 税务管理 | `taxmanager` |
| | 流水导入 | `payment-import` |
| | 一键报税 | `tax-filing` |
| | 银企互联 | `bank-api` |
| | 工资条群发 | `salary-send` |
| | 五险一金 | `insurance-stats` |
| | 发票扫码 | `invoice-scan` |
| | 凭证模板 | `voucher-templates` |
| **系统工具**（8） | 批量打印 | `batch-print` |
| | 智能导入 | `smart-import` |
| | 打印模板 | `print-templates` |
| | 电子档案 | `document-archive` |
| | BPM流程 | `bpm` |
| | 租户管理 | `tenantadmin` |
| | 开发者后台 | `devdash` |
| | Feature Flags | `featureflags` |
| **扩展功能**（4） | 预报订单 | `forecast` |
| | 数据源 | `datasources` |
| | 盈亏快报 | `profit-report` |
| | 专属价查询 | `pricing-explain` |

> 动态注册组合计 **26 个入口**（仓库操作 3 + 财务工具 11 + 系统工具 8 + 扩展功能 4）。

### 1.3 统计

- 当前侧边栏入口总数：**37 个**（固定 11 + 动态 26）。
- 当前顶层区块标题：**6 层**（顶部无标题区 / 经营业务 / 管理 / 仓库操作 / 财务工具 / 系统工具 / 扩展功能 —— 实为 1 个无标题区 + 6 个标题）。
- 冗余点：同一个「仓库/财务/系统/扩展」语义被拆成多个并列顶层标题；消息/通讯录/审核中心/设置 作为散落的顶层按钮，与「经营业务」平级，层级扁平且冗长。

---

## 二、整合方案

### 2.1 设计原则

1. **经营业务 7 项原样保留**，作为整合后的「核心业务」子分组，不增不减。
2. **除经营业务外的所有入口**（顶部 3 项 + 设置 + 4 个动态注册组）全部**归并到「经营业务」这一单一顶层之下**，作为可折叠子分组。
3. 顶层区块标题从 6 层收敛为 **1 层**（仅「经营业务」），其余全部降级为子分组，默认折叠，点击展开。
4. 所有入口语义不丢、ID 不变、跳转逻辑不变 —— 仅调整「归属分组」与「层级」。

### 2.2 整合后结构（单一顶层「经营业务」）

```
经营业务（顶层，唯一标题）
├─ 核心业务（默认展开）★ 原经营业务 7 项，完整保留
│    ├ 经营看板 / 档案管理 / 采销管理 / 仓配管理
│    └ 资金管理 / 财务管理 / 数据报表
├─ 协同办公（折叠）        ← 原顶部散落入口
│    ├ 消息 / 通讯录 / 审核中心
├─ 仓库作业（折叠）        ← 原「仓库操作」
│    ├ AI采购控制台 / 临期看板 / 报损管理
├─ 财务中心（折叠）        ← 原「财务工具」
│    ├ 付款批 / 催收管理 / GL维度 / 税务管理 / 流水导入
│    ├ 一键报税 / 银企互联 / 工资条群发 / 五险一金 / 发票扫码 / 凭证模板
├─ 系统工具（折叠）        ← 原「系统工具」
│    ├ 批量打印 / 智能导入 / 打印模板 / 电子档案
│    └ BPM流程 / 租户管理 / 开发者后台 / Feature Flags
├─ 扩展功能（折叠）        ← 原「扩展功能」
│    ├ 预报订单 / 数据源 / 盈亏快报 / 专属价查询
└─ 系统设置（折叠）        ← 原「管理 → 设置」
     └ 设置
```

### 2.3 前后结构对比

| 维度 | 整合前 | 整合后 |
|---|---|---|
| 顶层标题数 | 6 层（无标题区 + 经营业务/管理/仓库操作/财务工具/系统工具/扩展功能） | **1 层**（仅「经营业务」） |
| 经营业务 7 项 | 保留 | **保留不变**（核心业务子分组） |
| 消息/通讯录/审核中心 | 顶部散落按钮 | 并入「协同办公」子分组 |
| 设置 | 「管理」单独标题下的按钮 | 并入「系统设置」子分组 |
| 仓库操作(3) | 独立顶层标题（折叠） | 并入「仓库作业」子分组 |
| 财务工具(11) | 独立顶层标题（折叠） | 并入「财务中心」子分组 |
| 系统工具(8) | 独立顶层标题（折叠） | 并入「系统工具」子分组（同名降级） |
| 扩展功能(4) | 独立顶层标题（折叠） | 并入「扩展功能」子分组（同名降级） |
| 入口总数 | 37 | **37（不删任何功能，仅调整层级）** |

### 2.4 原入口 → 目标子分组 映射明细

| 原入口 | 原所属 | 整合后归入 |
|---|---|---|
| 经营看板 / 档案管理 / 采销管理 / 仓配管理 / 资金管理 / 财务管理 / 数据报表 | 经营业务（固定） | **核心业务**（保留，不变） |
| 消息 | 顶部无标题区 | 协同办公 |
| 通讯录 | 顶部无标题区 | 协同办公 |
| 审核中心 | 顶部无标题区 | 协同办公 |
| 设置 | 管理 | 系统设置 |
| AI采购控制台 | 仓库操作 | 仓库作业 |
| 临期看板 | 仓库操作 | 仓库作业 |
| 报损管理 | 仓库操作 | 仓库作业 |
| 付款批 / 催收管理 / GL维度 / 税务管理 / 流水导入 / 一键报税 / 银企互联 / 工资条群发 / 五险一金 / 发票扫码 / 凭证模板 | 财务工具 | 财务中心 |
| 批量打印 / 智能导入 / 打印模板 / 电子档案 / BPM流程 / 租户管理 / 开发者后台 / Feature Flags | 系统工具 | 系统工具（降级为子分组） |
| 预报订单 / 数据源 / 盈亏快报 / 专属价查询 | 扩展功能 | 扩展功能（降级为子分组） |

---

## 三、实施说明（若确认执行）

涉及改动：
1. **`sidebar-v2.js` 的 `render()`**：将原本硬编码的「消息/通讯录/审核中心/经营业务 7 项/管理→设置」重写为单一「经营业务」伞形结构 + 多个可折叠子分组；移除对 `ModuleRegistry._rebuildSidebar()` 注入独立顶层标题的依赖（或让注册组改用新的子分组名）。
2. **`module-registry.js` 的 `GROUP_ORDER` / 注册分组名**：将原分组名 `仓库操作/财务工具/系统工具/扩展功能` 映射为新子分组名（或直接复用同名降级），并调整 `_rebuildSidebar()` 使其把组渲染为「经营业务」下的可折叠子分组而非顶层标题。
3. 入口 ID、`navTo`、各模块 `render_*` 函数 **一律不动**，保证功能与交互零回归。
4. 构建 `VITE_BASE=/ npm run build` → 部署 `rsync` 到 `/opt/hergent-erp/static/`，并验证新 hash 生效。

---

## 四、风险与注意事项

1. **默认折叠**：除「核心业务」外全部折叠，避免整合后反而显得更长；首次进入默认展开核心业务。
2. **权限**：当前注册组对所有角色可见，整合不改变权限逻辑；如需按角色隐藏某子分组，需在渲染时叠加权限判断（本次方案不引入新权限）。
3. **OperationsHub 依赖**：仓配管理面板内仍直接调用 `render_stock_lock / render_barcode_ops / render_merge_orders`（上次已移除这三项侧边栏入口但保留功能），与本次整合无关，不受影响。
4. **移动端**：`mobile.html` 为独立页，不受本侧边栏改动影响。

---

## 五、待确认点

- 是否严格将所有非经营业务入口（含「设置」）都纳入「经营业务」之下？还是「设置」保留为顶层独立项更直观？（方案默认纳入，可按需调整为例外。）
- 子分组命名（协同办公 / 仓库作业 / 财务中心 / 系统设置）是否采用，或沿用原分组名（仓库操作/财务工具/系统工具/扩展功能）仅做降级。
