# Web 端「按角色连接」实施方案（对比分析 + 推荐设计）

> 任务 2 交付物｜2026-08-21｜SeniorDeveloper
> 前置：任务 1（Web 头像移植，对齐桌面版 256×256 裁剪 + `static/role-avatars/<tenant>/<role>.png` 存储 + `custom_avatar` 标志）已生产真机验证通过。

---

## 1. 结论（一句话）

**推荐 Web 端采用与桌面版完全一致的「按角色连接」模式**，并复用现有 `ai_roles` 的多租户隔离范式。当前 Web 连接是「租户/用户维度」（企微配置甚至只存 `localStorage`，未后端化、无角色绑定），无法支撑「不同 AI 角色把消息推到不同员工手机」的多人协同场景，而这是低温奶经销商（老板+运营+会计+客服多人）的核心诉求。

---

## 2. 模式对比分析

| 维度 | 桌面版（现状，目标对齐对象） | Web 端（现状） | Web 端（本方案，推荐） |
|---|---|---|---|
| **绑定维度** | **按角色**：`channels[platform][role] = {connected, config}` | 按租户/用户：企微 `wecom.linked` 一个全局开关 | **按角色**：`ai_role_channels(tenant, role, channel)` |
| **数据落地** | 本地 `channels.json`（`userData` 目录） | 企微仅 `localStorage`（`hergent_wecom_config`）；畅捷通/金蝶走后端 OAuth | **全部后端化**，租户隔离，密钥加密 |
| **多角色** | 每个角色可独立连/断某渠道，渠道卡片显示已连角色头像 | 不支持，所有角色共用一个连接态 | 每个角色独立勾选渠道，卡片显示已连角色头像（复用头像端点） |
| **配对方式** | `promptPairingCode(channel, role)` → 手机扫码/验证码配对 | 无（仅填 corpId/secret） | 复用同款「渠道凭证(租户级) + 角色绑定(角色级)」两层结构 |
| **消息路由** | 角色回复 → 推到该角色已连渠道（手机收） | 无推送能力 | 角色回复 → 查 `ai_role_channels` → 推已连渠道 |
| **隔离/安全** | 单用户本地文件，无租户概念 | localStorage 明文、无加密、易丢 | `_auth` + `tenant_id` 隔离 + 密钥 AES-256-GCM |
| **一致性风险** | — | 与桌面版割裂，老板换端体验断裂 | 与桌面版同构，跨端心智一致 |

**判断**：桌面版的两层结构（渠道凭证在平台级、角色绑定在角色级）天然契合本产品的多角色副驾设计。Web 直接复用该结构，成本最低、最一致。

---

## 3. 推荐实施方案

### 3.1 数据模型设计（两张表，租户隔离）

复用 `ai_roles.py` 的 `_get_db()`（全局 `erp.db`，`ERP_DB_PATH` 可覆盖）与 `datasource_store` 范式。新增两张表，置于 `server/ai_roles.py` 或新建 `server/ai_channels.py`。

#### 表 A：`ai_channel_configs`（渠道凭证，租户级，管理员配一次）

```sql
CREATE TABLE IF NOT EXISTS ai_channel_configs (
  id           INTEGER PRIMARY KEY,
  tenant_id    INTEGER NOT NULL,
  channel      TEXT NOT NULL,          -- 'wecom' | 'feishu' | 'dingtalk'
  config_json  TEXT NOT NULL,          -- 加密后的凭证：{corpId, corpSecret(密), agentId, ...}
  enabled      INTEGER DEFAULT 0,      -- 租户级渠道是否可用
  created_at   TEXT DEFAULT (datetime('now','localtime')),
  updated_at   TEXT DEFAULT (datetime('now','localtime')),
  UNIQUE(tenant_id, channel)
);
```

- 等价于桌面 `channels[platform]` 的平台级配置。
- `corpSecret` 等敏感字段写入前用 `datasource_store` 的同款 AES-256-GCM 加密（与 `tenant_audit`/凭据存储一致），绝不明文落库。
- 此前 `localStorage` 里的 `hergent_wecom_config` 仅作一次性迁移源，迁移后弃用。

#### 表 B：`ai_role_channels`（角色-渠道绑定，角色级）

```sql
CREATE TABLE IF NOT EXISTS ai_role_channels (
  id            INTEGER PRIMARY KEY,
  tenant_id     INTEGER NOT NULL,
  role_id       TEXT NOT NULL,         -- 关联 ai_roles.role_id
  channel       TEXT NOT NULL,         -- 'wecom' | 'feishu' | 'dingtalk'
  connected     INTEGER DEFAULT 0,     -- 该角色是否连了该渠道（等价于桌面 platformRoles[role].connected）
  role_config   TEXT,                  -- 可选：每个角色在渠道内的路由配置，如 {wecomUserId, groupId}
  created_at    TEXT DEFAULT (datetime('now','localtime')),
  updated_at    TEXT DEFAULT (datetime('now','localtime')),
  UNIQUE(tenant_id, role_id, channel),
  FOREIGN KEY (tenant_id, role_id) REFERENCES ai_roles(tenant_id, role_id)
);
```

- 等价于桌面 `channels[platform][role]`。
- 一张角色可连多个渠道；一个渠道可被多个角色连。
- `connected=1` 即「该角色的消息推到这部手机/这个群」。

> 与任务 1 的衔接：`ai_roles.custom_avatar=1` 时，连接卡片里已连角色的头像直接渲染 `<img src="/api/ai/roles/{role_id}/avatar">`，与桌面 `avatar://{role}.png` 同效。

---

### 3.2 连接建立与管理交互流程

**两层操作，职责分离（与桌面一致）：**

**第一步：配渠道凭证（租户级，老板/管理员做一次）**
1. 打开「连接中心 / 能力中心」→ 企业微信卡片 → 填 `corpId / corpSecret / agentId`。
2. 点「保存连接」→ 前端 `PUT /api/ai/channels/config`（不再写 localStorage）→ 后端加密落 `ai_channel_configs`，`enabled=1`。
3. 卡片状态变「已连接」（租户级）。此时渠道可用，但**还没连任何角色**。

**第二步：按角色绑定渠道（角色级，可多人）**
4. 同一页「按角色连接」区：列出本租户全部角色（来自 `list_roles`），每行是渠道勾选矩阵。
5. 老板勾选「经营副驾 → 企业微信」「会计 → 企业微信」等 → 保存 → `PUT /api/ai/roles/{role_id}/channels` 批量写 `ai_role_channels.connected=1`。
6. 渠道卡片下方显示已连角色头像标签（复用头像端点），与桌面 `chp-role-tag` 同观感。

**第三步：对话即推送（连接手机）**
7. 副驾切到「会计」角色对话 → 助手回复生成后，后端查 `ai_role_channels` 中 `role_id=accountant AND connected=1` → 调既有 `feishu_notify.push_to_wecom(...)` 把回复推到老板/会计手机。

**第四步：管理/移除**
8. 连接中心取消勾选，或角色页点头像标签上的 × → `DELETE /api/ai/roles/{role_id}/channels/{channel}` 删行。
9. 渠道级「断开」→ `ai_channel_configs.enabled=0`（保留凭证，便于重连）。

**入口双触点（提升可达性）：**
- `ConnectCenter.vue`：渠道卡片 + 角色绑定矩阵（主管理台）。
- `RoleManage.vue`：每个角色卡显示已连渠道徽标 + 「连接渠道」快捷按钮（开同一弹窗）。

---

### 3.3 接口改造点（file-level）

#### 后端（`server/`，复用 `_auth` + `db.get_tenant_context()`）

新建 `server/ai_channels.py`（或并入 `ai_roles.py`），并在 `server.py` 注册路由（走 RBAC `/api/ai` → `chat` 模块已覆盖）：

| 方法 & 路径 | 作用 | 改动点 |
|---|---|---|
| `GET /api/ai/channels/config?channel=wecom` | 取租户级渠道凭证（密文解密后返回非敏感字段） | 新增 |
| `PUT /api/ai/channels/config` | 保存/更新渠道凭证（加密落库） | 新增，替换前端 localStorage 写入 |
| `GET /api/ai/roles/{role_id}/channels` | 取某角色已连渠道列表 | 新增 |
| `PUT /api/ai/roles/{role_id}/channels` | 批量设置角色-渠道绑定（body: `[{channel, connected, role_config}]`） | 新增，等价于桌面 `channels:save(channel, role, config)` |
| `DELETE /api/ai/roles/{role_id}/channels/{channel}` | 移除单条绑定 | 新增，等价于桌面 `channels:remove(channel, role)` |

- 安全：所有端点 `_auth(request)` + `tenant_id` 过滤；密钥写入前 AES-256-GCM 加密，读取时只回显非敏感字段（`corpId`/`agentId`，不回显 `corpSecret`）。
- 推送复用：`server/scheduler.py` / `feishu_notify.py` 已有 `push_to_wecom` / `push_to_feishu`，在副驾助手消息落库后挂一个 `_push_role_channels(tenant_id, role_id, text)` 钩子。

#### 前端（`hergent-cn-v2/src`）

| 文件 | 改动 |
|---|---|
| `pages/ConnectCenter.vue` | ① `saveWecom()` 改为 `PUT /api/ai/channels/config`（删 `WECOM_KEY` localStorage 逻辑，L477-500）；② 新增「按角色连接」区：读 `list_roles` + 各角色 `GET .../channels`，渲染勾选矩阵；③ 渠道卡片 `chp-role-tag` 改渲染 `<img :src="/api/ai/roles/{role}/avatar">`（复用任务 1 端点）；④ 飞书/钉钉从 `disabled` 升级为可按角色绑定（与企微同构）。 |
| `pages/RoleManage.vue` | 角色卡增加「已连渠道」徽标 + 「连接渠道」按钮，打开与连接中心同源的绑定弹窗。 |
| `components/CopilotDrawer.vue` | （可选 P1）角色发送消息后，若 `connected` 渠道存在，显示「已推送至企业微信」小标；推送动作本身在后端钩子完成。 |

---

### 3.4 「连接手机」推送落地（关键能力）

桌面版靠 `promptPairingCode(channel, role)` 做手机配对。Web 版简化：
- **企微**：租户级凭证（corpId/secret/agentId）配好后，角色绑定即代表「把该角色的回复推到指定 `wecomUserId`/应用」。首次推送走企微「应用消息」接口（无需逐人扫码，老板在企微里点开应用即可收）。
- **飞书**：同理走 `push_to_feishu`。
- 推送钩子位置：副驾 `chat` 生成助手消息 → 落库后调 `_push_role_channels` → 失败 fail-closed（不影响对话，仅记日志 + 可选回话内提示）。

> 注：逐人「扫码配对」是桌面单机模型的产物；Web 多租户 SaaS 更适合「租户凭证 + 角色路由」的中心化推送，体验更顺、也更好支持多人。

---

### 3.5 安全与多租户隔离（必做，不可省略）

- 所有新端点强制 `_auth(request)` + `db.get_tenant_context()`，`ai_role_channels` / `ai_channel_configs` 查询均带 `tenant_id` WHERE（对齐 `T5 写工具零越权审计` 铁律）。
- 密钥 AES-256-GCM 加密落库（复用 `datasource_store` 加密范式），API 读取不回显 `secret`。
- 角色绑定删除角色时级联清 `ai_role_channels`（或 `delete_role` 内补 `DELETE FROM ai_role_channels WHERE tenant_id=? AND role_id=?`）。
- 飞书/钉钉上线前先 `tools.exclude` 收窄写权限（沿用 Hermes MCP 安全范式）。

---

### 3.6 迁移与兼容

- `localStorage.hergent_wecom_config` 仅本机未部署数据，风险低：在 `PUT /api/ai/channels/config` 首次成功写入后，前端 `localStorage.removeItem(WECOM_KEY)` 即可，无需后端迁移脚本。
- 旧 `ai_roles` 表无需改动（仅新增独立关联表）。
- 飞书/钉钉卡片从 `disabled` 切为可用，但后端接口先只接企微，飞书/钉钉按 P1 节奏接 `push_to_feishu`。

---

## 4. 落地优先级

| 优先级 | 内容 | 说明 |
|---|---|---|
| **P0** | 后端化企微凭证（`ai_channel_configs` + 2 个端点）+ 角色绑定表（`ai_role_channels` + 3 个端点） | 打通「配凭证→按角色绑→存储」主链路，替换 localStorage |
| **P0** | `ConnectCenter.vue` 角色绑定矩阵 + 头像标签渲染 | 管理台可用 |
| **P1** | 副驾推送钩子 `_push_role_channels`（企微应用消息） | 真正的「连接手机」价值闭环 |
| **P1** | `RoleManage.vue` 角色卡渠道徽标 + 快捷绑定 | 双触点可达性 |
| **P1** | 飞书 `push_to_feishu` 接入（去 disabled） | 扩渠道 |
| **P2** | 逐人扫码配对（若老板要求「每个员工独立手机」） | 增强，非必需 |

---

## 5. 待拍板问题（执行前需老板确认）

1. **推送粒度**：租户级凭证 + 角色路由（推荐，中心化）是否可接受？还是坚持桌面式「逐人扫码配对」？
2. **企微推送形态**：走「企微应用消息」（老板在应用里收，最简单）还是「企微群机器人/单聊」（需额外配置 webhook）？
3. **飞书/钉钉**：本轮是否一并去 disabled 上线，还是仅企微先跑通？
4. **密钥托管**：确认复用 `datasource_store` 的 AES-256-GCM 主密钥，还是为渠道凭证单独起一把密钥？

> 任务 1（头像）已交付并真机验证；任务 2 本方案为评估+设计文档，不含代码部署，故无需 E2E。确认上述 4 点后可进入 P0 编码。
