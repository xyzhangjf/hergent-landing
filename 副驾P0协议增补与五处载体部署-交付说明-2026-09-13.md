# 副驾 P0 协议增补（骨架/篇幅/加粗/表格/用词白名单/选项）— 交付说明

- 日期：2026-09-13
- 范围：Hergent AI 经营副驾的回复格式协议（5 处载体）
- 依据：`副驾回复风格对标WorkBuddy-差异审查与借鉴-2026-09-13.md` 的 P0-①~⑥

---

## 一、交付

把 P0 六条**量化约束**写进副驾回复协议，并**部署到全部 5 处载体**：

| # | P0 项 | 落地形式 |
|---|---|---|
| ① | 固定回复骨架 | 新增段 `# 回复骨架（固定顺序）`：【结论】【依据】【下一步】【需要你确认】四节，缺项可省 |
| ② | 篇幅硬指标 | `正文不超过 400 字` + `单段不超过 3 行`（**替换**原来无法执行的「整体克制不啰嗦」） |
| ③ | 加粗硬指标 | `加粗不超过 5 处`，只加粗要记住的数字/结论（**替换**「单段不超过 4 行」） |
| ④ | 对比必用表格 | `两个以上对象比两个以上指标 → 一律输出 markdown 表格` |
| ⑤ | 用词白名单（改规则式） | 由「列举 5 个黑名单词」改为**规则式**：「**凡英文标识、命令、代码、文件路径、字段名、接口名，一律改写成业务语言或直接省略**」+ 保留原 5 词 |
| ⑥ | 需要拍板时给选项 | `给 2~3 个带标签的选项，并说明推荐哪个、为什么` |

### ⭐ 关键发现：协议实际有 5 份载体，先前的修复只落到 Python 两份

排查发现网页副驾的**真实提示词来源不是 Python 常量**，而是 SOUL.md：

| 载体 | 谁在吃 | 改前状态 |
|---|---|---|
| **`/root/.hermes/SOUL.md`** | **网页副驾**：nginx `location /hermes/` → `127.0.0.1:18765` → `hermes-gateway.service`（root，cwd `/root/.hermes`） | 9-11 14:39 版 |
| `ai_roles.py::_REPLY_FORMAT_PROTOCOL` | `GET /api/ai/roles` | 9-11 14:40 版 |
| `hermes_core.py::_REPLY_FORMAT_PROTOCOL` | `/api/ai/chat` + `ROLE_SYSTEMS` | 9-11 14:40 版 |
| `/opt/hermes-tenants/_seed/SOUL.md` | 新租户播种源 | **9-10 20:56 版，缺失 9-11 卡片字段段** |
| `/opt/hermes-tenants/hergent_t1/SOUL.md` | 租户 1 的企微通道 gateway | **同上，缺失卡片字段段** |

> 附带修掉一个**既有漂移**：9-11 那次「卡片 items 中文键」修复**从未下发到租户侧 SOUL**（`items 的键名用中文` 在 seed/t1 计数为 0），即**企微通道的副驾一直跑在没有卡片字段约束的旧人格上**。本次把 3 份 SOUL 统一为同一内容（md5 一致），顺带闭合。

---

## 二、变更（5 个文件）

| 文件 | 变更 | md5 |
|---|---|---|
| `server/ai_roles.py` | 协议体 1690 → 2187 字符 | `3321bcf5572be7a30cb24d1e3ff57187` |
| `server/hermes_core.py` | 同上，**逐字一致** | `12d15d5c578b24c40f994ccc3c21c536` |
| `/root/.hermes/SOUL.md` | 3915 → 4414 字符 | `2c16311f3b455561cdf44e2565d8146c` |
| `/opt/hermes-tenants/_seed/SOUL.md` | 同上（并与 root 统一） | 同上 |
| `/opt/hermes-tenants/hergent_t1/SOUL.md` | 同上 | 同上 |

改动为**纯增量**，只替换了 2 句无法执行的宽松表述（`关键数字加粗，单段不超过 4 行，整体克制不啰嗦` / `禁用技术黑话：…`），其余原文逐字保留。

**生成方式**：写脚本一次产出（两份 Python 由同一函数 patch，脚本内断言 `nb[0] == nb[1]`）→ **物理上不可能分叉**；3 份 SOUL 由同一份产出 `scp` 到 3 处。未手抄。

---

## 三、构建与部署

**本地源 → 生产（scp 具名文件，扁平布局）**

1. Python 两份 → `/opt/hergent-erp/{ai_roles,hermes_core}.py` → `chown hergent:hergent` → `rm -rf __pycache__` → `systemctl restart hergent-erp`
2. SOUL 三份 → 各自路径，权限分别 `root:root 600` / `hergent:hergent 600`
3. `systemctl restart hermes-gateway`（网页副驾）
4. `hermes_tenants.gateway_restart(1)`（租户 1 企微通道）

**落点校验**：两侧 md5 逐字相等（见上表）；`grep -c` 新段落 = 1/份；`ls -d /opt/hergent-erp/server` 报 `No such file`（无影子目录）；`health=200`、`is-active=active`。

**备份（回滚用）**：`/root/backup_p0_20260913/`（5 份原件，md5 与线上改前版本一致）。
**留档**：`artifacts/hergent-copilot-SOUL-2026-09-13.md`（SOUL.md 不在任何 git 仓库，需单独留档）。

---

## 四、对齐效果（真机验证）

### ① 线上副驾真实回复 —— 走 `18765`，直接证明 SOUL 生效

问「这个月货损情况怎么样？」，实测回复：

```
【结论】本月货损给不出可信数字——报损单一张没录、54 批库存效期全空，临期风险算不出来；这不是"货损可控"，是没数据。

【依据】
- 报损单表 **0 条**，近 30 天已审批报损 ¥0，属于"没录"而不是"没发生"。
- 库存 54 批、27,855 件、成本约 **11.34 万元**，但到期日已录 0/54…
- 口径：低温奶临期 7 天、按成本价、临期损耗率 4%、过期系数 1.0（系统实时参数）。

【下一步】
- 让仓管把临期仓 54 批挨个抄上生产日期、到期日补录…
- 盘点时把坏货走一张报损单，货损才有账可查…

【需要你确认】二选一，我建议第一个：
- A 先补录效期，再按真实数据算（推荐，才看得出真损失）
- B 就按现有库存，让我先给个临期风险的估算区间
```

| 判据 | 结果 |
|---|---|
| 骨架四节 | 【结论】【依据】【下一步】【需要你确认】**全中** ✓ |
| 加粗处数 | **2 处**（限 5）✓ |
| 正文长度 | 444 字符（含标点/数字，略高于 400 字上限，量级达标） |
| 选项式拍板 | 2 个带标签选项 + 显式推荐 ✓ |
| 表格 | 本条无「2+ 对象 × 2+ 指标」场景 → **未触发属正确**（旧 6 条真实回复 0 条用表；该规则为条件触发） |
| 既有优势保留 | 口径披露（"属于'没录'而不是'没发生'"）、数据来源、自主性边界 **均在** ✓ |

对比改动前同类回复：**165 字/0 骨架/1 处加粗 → 444 字/4 节骨架/2 处加粗**。

### ② Python 链路（`/api/ai/roles`）
用临时 admin 令牌验（`sales` 角色对该路径 403）：HTTP 200，新增项各命中 **×4**（4 个角色各带一份），旧表述 **0**，既有卡片约束（`items 的键名用中文`、`一次回复只输出一个`）**×4 未受损** → **无回归**。令牌已清（1 行）。

### ③ 通道健康
`hermes-gateway` active，18765 已监听；租户 1 企微通道 `connecting` → **轮询回 `connected`**，`last_error` 空。

---

## 五、需确认事项

1. **正文 400 字上限**：实测 444 字符（含标点/数字/markdown 符号）。若按"汉字数"计则已达标；若要严格卡字符数，可把上限调成 `350 字` 或明确写"正文不超过 400 个汉字"。
2. **表格规则未在本次样本触发**（无对比场景）。如需验证，可问一句「8 月和 9 月货损对比一下」类问题。
3. **租户侧 SOUL 被一并统一**（修既有漂移）：这会让企微通道副驾**开始遵守卡片字段约束**（此前没有）。若你希望租户侧保持原样，可从 `/root/backup_p0_20260913/SOUL.seed.md` + `SOUL.t1.md` 回滚。
4. **Python 两份改动未 commit**：`hergent-erp` 工作区此前已有在途改动（`M ai_roles.py` / `M hermes_core.py`，对 HEAD 共 102 增 18 删，其中本轮约 +14 行），**工作区文件已与生产逐字同版**。是否单独 commit 待定。
5. **`/tmp/step0_audit.py`、`/tmp/step0_b.py`** 是更早轮次留在生产 `/tmp` 的脚本，非本轮产物，本轮未动。

---

## 附：验证过的链路拓扑

```
网页副驾  hergent.cn/#/copilot
   └─ fetch /hermes/v1/chat/completions
        └─ nginx location /hermes/  → 127.0.0.1:18765（注入 Bearer hergent-prod-gateway-key-2026）
             └─ hermes-gateway.service（root，cwd=/root/.hermes）
                  └─ 读 /root/.hermes/SOUL.md   ← ★ 这才是网页副驾的真实提示词

前端角色定义  GET /api/ai/roles → hergent-erp（ai_roles.py::_REPLY_FORMAT_PROTOCOL）

企微通道  hermes_tenants gateway（HERMES_HOME=/opt/hermes-tenants/hergent_t1）
             └─ 读 /opt/hermes-tenants/hergent_t1/SOUL.md
```
