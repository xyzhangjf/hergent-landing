# 目标与返利 · 达成填报：月度目标口径修复 + 全年月度对比

**日期**：2026-09-13　**页面**：`目标与返利 → 达成填报`（前端 `src/pages/Rebate.vue`）
**问题**：蒙牛低温类的目标值是**年度目标**（`rebate_target_rules.target_value` = 全年总额，月度口径在 `monthly_amounts[MM]`），而达成值是**按月**填报的。旧版达成填报表的「目标值」列直接显示**年度总额**，达成率也用年度目标算 → 月度达成 / 年度目标 = 严重失真（如 96 万 / 200 万 = 48%，实际按月度目标 90 万算应是 106.7%）。

## 改动清单（Rebate.vue，纯前端，未动后端）

| # | 位置 | 内容 |
|---|---|---|
| 1 | `buildAchvRows()` | 行内新增 `monthTarget`：月视图取 `monthTargetOf(rule, year, month)`（即 `monthly_amounts[MM]`，与仪表盘排行同源口径）；季/年视图回退 `target_value`（年度） |
| 2 | 模板表头 | 月视图列头显示「月度目标」，季/年视图显示「目标值」 |
| 3 | 模板单元格 | 「目标值」列改用 `fmtAchvTarget(row)`（优先显示月度目标） |
| 4 | `fmtAchvTarget`（新） | 按口径格式化目标：月视图=月度目标，其它=年度目标值 |
| 5 | `achvRateText` / `achvRateCls` | 达成率分母由 `row.target_value` 改为 `row.monthTarget` |
| 6 | 新增 `loadYearAchievements` / `currentYear` / `achvMeasure` / `yearMonthRows` | 拉取全年 12 月达成（`/api/rebate-achievements?year=`），按月份聚合月度目标与达成 |
| 7 | `switchTab` / `onAchvPeriod` / `onAchvPeriodInput` | 进入填报页或切口径时一并加载全年数据 |
| 8 | 模板新增「全年月度对比（目标 vs 达成）」卡 | 1~12 月每行：月度目标 / 实际达成 / 达成率，满足"各月度同时呈现、按月度对比" |
| 9 | 样式 `.achv-year` | 对比卡上间距（复用 `.card`/`.panel-hd`/`.tbl`） |

## 口径纪律（与现有实现一致）
- 月度目标一律走 `monthTargetOf()`，**绝不**用年度 `target_value` 去比月度达成（dashboard 排行 v150 已立的规矩，本次把达成填报表补齐）。
- 聚合对比表：存在金额类年度规则→按金额汇总，否则按数量汇总；金额/数量不跨口径相加。
- 无月度分解的月份 → 目标显示「—」、达成率「—」，不臆测均分。

## 构建与部署
`Rebate-BWI3fvlD.js` / `Rebate-CxDaZpxY.css` → rsync + chown → 双侧 md5 **MATCH_OK**
（`78b02a54…` / `Rebate-CxDaZpxY.css`；index CSS `f5b046b4…` 未变）。

## 真机验证（生产 demo 租户 tenant_10）
造带标记 `VERTMP` 的 12 月达成数据（验完已删，回读 0 行）。**0 JS 报错**，关键断言全过：
- 月视图列头 = 「月度目标」✓
- 达成填报**不再显示年度 2,000,000**，9 月显示月度目标 **¥900,000** ✓
- 达成率按月度目标算 = **106.7%**（旧逻辑会是 48% 失真）✓
- 「全年月度对比」= 12 行；8 月目标 ¥1,100,065（rule1 8月月规则 65 + rule2 月度分解 1,100,000 两条生效规则聚合）、9 月 ¥900,000，达成率均正确 ✓

## 已知范围
- 本次只改**月视图**口径（用户明确点名"在月度视图中"）；季/年视图维持原年度目标显示。
- 对比表按"全部生效规则"聚合，多品牌/多维度会汇总成一行整体月度对比；如需按品牌拆分行，可后续加维度拆分。
- 本地改动**未 commit**（延续在途改动同批）。
