# 预报订单管理板块全面审核报告

> 审核日期：2026-07-24 ｜ 审核人：Senior Developer（高级开发工程师）
> 板块范围：**预报订单管理**（前端 Forecast.vue / ForecastHistory.vue / ReportMapping.vue ＋ 后端 forecast / forecast_config / forecast_audit / forecast_submissions 四个 router ＋ erp_db.forecast_order_board 聚合）
> 审核维度：功能逻辑 / 前端交互 / UI 样式 / 后端性能健壮性 / 前后端一致性

---

## 〇、审核范围与方法

| 层 | 文件 | 规模 | 审核方式 |
|---|---|---|---|
| 前端主页面 | `hergent-cn-v2/src/pages/Forecast.vue` | 5677 行 | 全量分块通读（script + style） |
| 前端历史看板 | `src/pages/ForecastHistory.vue` | 86 行 | 全量通读 |
| 前端报单配置 | `src/pages/ReportMapping.vue` | 506 行 | 全量通读 |
| 前端 API 层 | `src/api/modules.js` / `client.js` | 305 行 / 关键段 | 全量通读 |
| 前端路由/令牌 | `src/router/index.js` / `src/styles/variables.css` | 63 行 / 223 行 | 全量通读 |
| 后端 | `server/routers/forecast.py` | 792 行 | 全量通读 |
| 后端 | `server/routers/forecast_config.py` | 1011 行 | 全量通读 |
| 后端 | `server/routers/forecast_audit.py` | 435 行 | 全量通读 |
| 后端 | `server/routers/forecast_submissions.py` | 477 行 | 全量通读 |
| 后端聚合 | `server/erp_db.py::forecast_order_board()` | L13255-13339 | 全量通读 |
| 注册顺序 | `server.py`（submissions→config→audit→forecast） | — | 确认路由归属 |
| 端点比对 | `/tmp/check_endpoints.py`（提取 APIRouter prefix 后拼接） | — | 前端 18 引用端点全比对 |

**关键背景（影响结论）**：router 注册顺序为 `forecast_submissions(752) → forecast_config(753) → forecast_audit(801) → forecast(817)`，`/api/forecast/accuracy`、`/api/forecast/column-schemes` 均被 **config 版先行注册覆盖**，forecast.py 内同名端点为**死代码**。

---

## 一、功能逻辑层（A 类）

### A1 🔴 P0 ｜ submit_order 的 `all_done` 恒真（forecast.py:132）
```python
all_done = len(stats) >= b.get("expected_submitters", len(stats))
```
- **问题**：`expected_submitters` 未传入时以 `len(stats)` 自身兜底，`n >= n` 恒真——"全员已报"状态永远成立，业务上失去"还差几家没报"的判断能力。
- **建议**：`expected_submitters` 从期次配置表取（期次应带报单人数目标），缺失时显式返回 `null` 并在前端展示"未配置目标"，而不是静默恒真。
- **理由**：该状态直接喂给小程序/Web 的定稿提示，恒真会让老板误判报单进度。

### A2 🔴 P0 ｜ submit_order 取"最新一期"而非 period_id 关联期（forecast.py:132）
```python
period = db.forecast_period_list()[0] if ... else {}
```
- **问题**：提交时传了 `period_id` 但后端一律取最新一期，历史期补单/跨期提交会被记到错误期次。
- **建议**：优先按 `body.period_id` 查期次，无匹配再回退最新一期并打日志。
- **理由**：报单归属期次错误会污染 forecast_order_board 聚合与后续对账。

### A3 🔴 P0 ｜ 审核保存写死 `period_id=0`（forecast_audit.py:374）
```python
# save(): items 逐条 INSERT forecast_orders，写死 period_id=0
```
- **问题**：审核采纳后的订单全部挂期次 0，与真实期次脱节，历史查询/对账全部失效。
- **建议**：从请求上下文/期次表解析真实 `period_id`，校验期次存在后再写。
- **理由**：`forecast_orders.period_id` 是后续 accuracy（命中率）与报单统计的核心关联键。

### A4 🟡 P1 ｜ delete_period 返回 200 伪错误（forecast.py:94）
- **问题**：删除失败返回 `{"success": False, "detail": ...}` 但 HTTP 200，前端无法用状态码区分。
- **建议**：改 HTTP 404 + 统一错误信封 `error_envelope(404, msg)`。
- **理由**：符合 H1b 已落地的统一信封契约，错误语义正确。

### A5 🔴 P0 ｜ 返利冲刺预警恒空（Forecast.vue:3681 `loadRebatePush`）
- **问题**：前端期待 `rb.balances`（按目标/已达成聚合），后端 `/api/forecast/rebate-gap`（forecast.py:748）只返回 `{"projection": ...}` 或 `{"projection": null}`——**该面板实际永远渲染空数组**，是"看起来有功能、实际没数据"的隐性缺陷。
- **建议**：二选一——① 后端 rebate-gap 补齐 `balances` 字段（按返利规则聚合 bought/target）；② 前端改消费 `projection` 并调整面板语义。推荐 ①（面板本身设计意图就是预警）。
- **理由**：用户看到的空面板会误以为"没有返利风险"，比报错更危险。

### A6 🟡 P1 ｜ forecast_order_board 合成行负 id ＋ 状态恒 open（erp_db.py:13255-13339）
- **问题**：未被任何期次区间覆盖的报单日期会生成 `id=-1,-2,...` 的合成行（`name=f"{d} 报单"`，`status` 恒 `"open"`）。前端对合成行执行删除/关闭/回载操作无实际意义（负 id 不存在于期次表）。
- **建议**：前端识别 `id < 0` 的行——仅展示、屏蔽行内操作按钮；或后端为这类未覆盖报单自动建真实期次。
- **理由**：保证"保存过的单始终可见"的兜底逻辑是对的，但不能让用户对不可操作行执行操作。

### A7 🔴 P0 ｜ accuracy 数据口径 bug（forecast_config.py:177，当前生效版）
- **问题**：`WHERE fs.period_id = ?`——但**小程序通过 order_date 创建期次不落 period_id**，实际历史命中率查询查不全。
- **建议**：`period_id` 匹配不到时按 `order_date` 区间 fallback；两列都建索引。
- **理由**：准确率是"预报准确性"板块的北极星指标，口径错则整个面板失真。

### A8 🟡 P1 ｜ accuracy / column-schemes 双份定义死代码（forecast.py:787/107/114）
- **问题**：forecast.py 版与 config 版参数名/逻辑不同（`period: str="week"` vs `period_id`），注册顺序决定 config 版生效，forecast.py 版永不执行——**维护陷阱**（改错文件无感）。
- **建议**：删除 forecast.py 死代码版本，全部收敛到 forecast_config.py。
- **理由**：消灭双份实现是消除"改了没生效"类事故的最直接手段。

### A9 🟡 P1 ｜ payments_compute 依赖未建表静默吞错（forecast.py:277）
- **问题**：无 Pydantic 校验，底层表不存在时异常被吞，返回空结果。
- **建议**：表存在性检查 + 明确报错；接入 Pydantic Body。
- **理由**：静默降级掩盖数据缺失，用户以为"没有工资数据"实则"表没建"。

---

## 二、前端交互层（B 类）

### B1 🟡 P1 ｜ 编辑态默认开启 ＋ 宽网格溢出视口
- **问题**：`onMounted` 按 `editMode` 默认进入 `loadEditGrid()`（编辑态），全量商品主档 ＋ 20+ 列矩阵超出视口宽度，横向滚动无 sticky 首列。
- **建议**：默认只读态（loadCross），"编辑"按钮显式进入编辑态；横向首列 + 商品名列 sticky，最小列宽兜底。
- **理由**：只读是高频态，编辑是低频态；默认进入编辑态既增加误改风险也加重首屏渲染。

### B2 🟡 P1 ｜ 行删除为软删，保存后 reload 恢复
- **问题**：编辑态删除行仅前端标记，`saveEdits` 后 `await loadEditGrid()` 重载，删除行又回来——用户感知"删不掉"。
- **建议**：明确"暂存删除"语义（UI 标注待删除态），保存时传删除清单到服务端物理删除。
- **理由**：当前行为与用户心智模型（删除=消失）冲突，属易用性缺陷。

### B3 🟡 P1 ｜ 粘贴/填充双模式无提示
- **问题**：`onPaste` 支持"表头映射粘贴"与"无表头默认顺序"两种模式，但用户无感知当前命中哪种；`doFill` 填充范围无边界提示。
- **建议**：粘贴完成后 toast 提示命中的模式与影响范围；填充越界时提示并截断。
- **理由**：Excel 式交互的杀手锏是"可预期性"，静默歧义会直接造成错填。

### B4 🟢 P2 ｜ ForecastHistory 无移动端适配
- **问题**：`.forecast-history` 区块 `min-width:900px` 固定，无 `@media` 断点。
- **建议**：加响应式断点，窄屏降级为卡片列表。
- **理由**：小程序侧是主战场，Web 历史看板未来会在手机浏览器打开。

### B5 🟢 P2 ｜ ForecastHistory 无独立路由
- **问题**：Forecast.vue 行 1425 直接 `<ForecastHistory>` 引用（内部标签页），router/index.js 无独立路由。
- **建议**：可接受（标签页架构），但需保证侧边栏/文档导航文案一致，避免"找不到入口"。
- **理由**：架构上不算缺陷，属一致性问题，列为低优先级。

### B6 🟡 P1 ｜ 虚拟滚动/分组头的列宽记忆跨端不一致
- **问题**：`loadColWidths` 本地 localStorage 存储，多端（Web/小程序/不同浏览器）列宽不一致，且无重置入口。
- **建议**：加"重置列宽"按钮；列宽可考虑下沉到服务端配方（每客户差异化）。
- **理由**：与"配方化 recipe-driven 差异化"战略一致，老板电脑/员工电脑布局可统一。

---

## 三、UI 样式与视觉规范层（C 类）

### C1 🟡 P1 ｜ heatStyle 硬编码色值（Forecast.vue:1510-1520）
- **问题**：`'#1D9E75' / '#5DCAA5' / '#C0DD97' / '#04342C' / '#173404'` 等直接内联，未走 `variables.css` 令牌。
- **建议**：抽为语义化 CSS 变量（如 `--heat-0..4`），在 `variables.css` 中随主题定义；深色主题配深色系热力值。
- **理由**：**设计令牌是站点级规范（--p/#06b6d4、--suc、--war 等），热力色脱离令牌 = 主题切换后刺眼/失配**。

### C2 🟡 P1 ｜ ReportMapping.vue 组件样式独立于全局令牌
- **问题**：自带 `.card / .btn* / .tag* / .toolbar / .table-wrap` 全套覆盖样式（`.tag` 自定义 `purple/teal` 色），未复用全局 `.card/.btn/.tbl` 体系。
- **建议**：改用全局令牌组件类，删除本地重复定义；主题色统一走 `--p` 系。
- **理由**：局部覆盖会造成页面间视觉漂移，长期维护双份规范。

### C3 🔴 P0 ｜ 深色主题未适配的硬编码浅底色
- **问题**：`calc.sum / col-total` 及多处面板硬编码 `#fffbeb / #fef3c7 / #cffafe`（浅暖黄/浅青底），深色主题下高亮刺眼、文字对比度不足。
- **建议**：全部替换为 `var(--bg3)` / `var(--t1)` 派生或新增语义令牌（如 `--sum-bg`），随主题切换。
- **理由**：**站点已声明双主题（variables.css 含 `:root.dark`），硬编码浅底色是明确违规**，且直接影响夜用/护眼场景。

### C4 🟢 P2 ｜ 其他散点硬编码色
- **问题**：`#A32D2D / #BA7517 / #9A6B00 / #FBEEDA / #7C3AED` 等零星内联。
- **建议**：收敛进令牌（分别映射 --dan / --war / 辅助强调色）。
- **理由**：与 C1/C3 同理，一次扫清防复发。

### C5 🟢 P2 ｜ 状态色语义建议
- **问题**：报单状态色（进行中/未开始/已结束）与返利状态色分散定义，无统一映射。
- **建议**：建 `--state-progress/--state-pending/--state-done` 三个语义令牌，供历史看板/期次/返利三处复用。
- **理由**：状态语义统一后，跨页面认知成本下降。

---

## 四、后端性能与健壮性层（D 类）

### D1 🔴 P0 ｜ N+1 查询清单（4 处）
| 位置 | 问题 | 建议 |
|---|---|---|
| `forecast_audit.search_products`(69) | 每商品 2 次（`_current_stock` + `_avg_daily_sales`） | 批量 IN 预取库存/日均销量 |
| `_avg_daily_sales` | 每商品重复执行 `(SELECT MAX(order_date) FROM sale_orders)` 子查询 | 查询一次最大日期，内存复用 |
| `forecast_config.safety-suggest`(466) | 每商品单独查 `sale_order_items` | 单 SQL 按 product_id 分组聚合 |
| `forecast_config.ts-forecast`(631) | 每商品 `_rows` 一次 | 一次拉全量历史，Python 侧分组回归 |
- **理由**：商品量 ~430，审计/建议类接口一次渲染 = 数百次 DB 往返，首屏秒级变 10 秒级。

### D2 🔴 P0 ｜ save_matrix 无统一事务（forecast_submissions.py:233-302）
- **问题**：`forecast_submission_create` 在 `get_db_tx()` **之外**调用，extra_qty 与 period_confirm 各自开独立事务——中途失败会出现"部分保存"。
- **建议**：整个保存循环包进单一大事务（先校验全部，再统一写入），失败整体回滚。
- **理由**：矩阵保存是"一次点保存"的原子操作，部分落库会造成交叉表数据错乱。

### D3 🟡 P1 ｜ reject 空 body 500（forecast_submissions.py:396）
- **问题**：`d = await request.json()` 无 try，空 body → `JSONDecodeError` 500。
- **建议**：改 Pydantic Body 模型（可选字段），空体返回 422 而非 500。
- **理由**：500 会触发前端"系统错误"误报，422 可给业务提示。

### D4 🟡 P1 ｜ PUT /staff/{id}/stores 非数字 500（forecast_submissions.py:454）
- **问题**：`ids = [int(x) for x in ...]`，非数字输入直接 ValueError 500。
- **建议**：Pydantic `list[int]` 校验，非法值 422。
- **理由**：小程序/Web 双端共享该接口，脏数据进入即崩。

### D5 🟡 P1 ｜ barcode-units / payments_compute 等无 Pydantic 校验
- **问题**：多个 POST 端点裸 `request.json()` + 手写解析。
- **建议**：按 H1a 已定规范补 Pydantic schema。
- **理由**：与 H1a 批次推进（#165）口径一致，避免历史遗留清单外再新增裸奔端点。

### D6 🟡 P1 ｜ realtime-warn 全表扫描（forecast_config.py:135）
- **问题**：products + inventory 全表扫描，无分页无索引。
- **建议**：加 `LIMIT` + 时间窗/状态索引。
- **理由**：实时预警若挂定时任务（scheduler），全表扫描每次拖垮 SQLite 主连接。

### D7 🟡 P1 ｜ purchase-order id 用 `max+1` 生成（forecast_config.py:742）
- **问题**：并发插入会撞 id 覆盖。
- **建议**：UUID / 自增主键 / SQLite `rowid`。
- **理由**：多端并发（小程序+Web）下 max+1 必然有碰撞窗口。

### D8 🟢 P2 ｜ nl-edit 无长度上限（forecast_config.py:558）
- **问题**：用户输入直接拼 LLM prompt，无 max_len。
- **建议**：截断到安全长度（如 2000 字符）+ 提示。
- **理由**：防 prompt 注入式超长输入拖垮 LLM 调用成本。

### D9 🟡 P1 ｜ data-gaps `MAX(batch_no)` 存在性判断语义 bug（forecast_config.py:416/434）
- **问题**：`MAX(batch_no)` 判断存在性，空串时 MAX 可能返回空串而非 NULL，误判"已补"。
- **建议**：`EXISTS(SELECT 1 ... WHERE batch_no IS NOT NULL AND batch_no != '')`。
- **理由**：数据缺口面板会漏报真实缺口，误导补数。

### D10 🟢 P2 ｜ forecast_config 表无索引
- **问题**：`(kind, payload, updated_at)` 单表 JSON 存储，无索引，`_rows` 全扫。
- **建议**：`CREATE INDEX idx_forecast_config_kind ON forecast_config(kind)`。
- **理由**：配方类配置随客户数线性增长，索引成本极低收益确定。

### D11 🟢 P2 ｜ search_products 无分页（forecast_audit.py:69）
- **问题**：关键词命中全部商品一次性返回。
- **建议**：`limit/offset` 分页（默认 50）。
- **理由**：430 商品规模尚可，扩到多客户大 SKU 后必现。

---

## 五、前后端数据一致性层（E 类）

### E1 🟡 P1 ｜ 响应信封混合：`success` vs `ok`
- **问题**：forecast.py / forecast_audit.py 用 `{"success": True}`；forecast_config.py / forecast_submissions.py 用 `{"ok": True}`；forecast_submissions.summary 返回 `{"ok": True, **data}`（**无顶层 data 键**，整包返回）。
- **结论**：`api()` 对无 `data` 键的包**原样返回**，前端 `d.rows / r.success / r.adopted` 消费均正确 ✓；`auditApi.compute` 返回 `{"success": True, "data": results}` 会被 `api()` 展开为 results，前端 `res.data || res.results || res` 兼容 ✓。
- **建议**：新端点统一 `{"ok": true, "data": ...}`（H1b 信封），存量端点迁移时同步改前端；**改前必须逐端点核对 `api()` 展开行为**（有 data 键会被剥壳，无则整包）。
- **理由**：语义混用是"改一个字段悄悄坏一片"的温床。

### E2 🔴 P0 ｜ RBAC 403 与小程序审批流冲突
- **问题**：`_PATH_MODULE_MAP` 将 `/api/forecast` → 模块 `reports`，而 `_DEFAULT_PERMS` 中 **supervisor / staff / sales 默认无 `reports` 权限** → 小程序销售/主管走 `/api/forecast/submission` 等审批端点**前置 403**；而 `forecast_submissions` 前缀映射 `data` 模块。同一业务（预报审批）被拆在两个 RBAC 模块，权限碎片化。
- **建议**：① 将预报审批端点前缀收口到 `data` 模块或新增独立模块映射；② 扩展 `_DEFAULT_PERMS`（supervisor 已手工补过，需写入默认值并迁移全部租户库）；③ 用真实登录态（sales/supervisor）回归验证 401 vs 403 区分。
- **理由**：**未登录先 401、已登录但无权限才 403，只有真实登录态才能复现**；此问题会直接卡死小程序端"报单-审批"主流程上线。

### E3 🟢 已核对通过项（记录备查）
| 项 | 结论 |
|---|---|
| 前端 18 个引用端点 | 全部存在（含 `{oid}/{pid}` 路径参数正则排除后）✓ |
| `accuracy` 参数 | `period_id` 前后端对齐 ✓（config 版生效） |
| `save-matrix` 返回 | 无 `data` 键 → 整包返回，`r.success` 消费正确 ✓ |
| `rebate-gap` | ❌ 前端期待 `rb.balances`，后端无此字段（见 A5） |
| `forecast_order_board` | `reporter_count/total_qty/total_amount/finalized/status` 字段对齐 ✓（合成行负 id 见 A6） |

---

## 六、优化优先级汇总

| 优先级 | 项 | 一句话理由 |
|---|---|---|
| **P0（阻断性，建议立即修）** | A1 all_done 恒真 | 报单进度判断失效 |
| | A2 取最新一期 | 历史期补单归属错误 |
| | A3 period_id=0 | 审核单与期次脱节 |
| | A5 rebate-gap 恒空 | 返利预警假功能 |
| | A7 accuracy 口径 | 命中率指标失真 |
| | C3 深色主题浅底色 | 双主题规范违规 |
| | D1 N+1 ×4 | 接口性能数量级劣化 |
| | D2 save_matrix 事务 | 部分保存数据错乱 |
| | E2 RBAC 403 | 小程序审批流主流程被卡 |
| **P1（重要）** | A4/A6/A8/A9、B1/B2/B3/B6、C1/C2、D3-D7/D9、E1 | 见上文逐项 |
| **P2（优化）** | B4/B5、C4/C5、D8/D10/D11 | 见上文逐项 |

**修复顺序建议**：E2（RBAC）→ D2（事务）→ A1/A2/A3（期次/状态）→ D1（N+1）→ A5/A7（口径）→ C3/C1/C2（样式令牌）→ 其余按批次。

---

## 七、需确认事项

1. **E2 RBAC 修复方向**：预报审批端点收口到 `data` 模块，还是为 `supervisor/sales` 扩 `reports` 权限？（涉及 `_DEFAULT_PERMS` 迁移全部租户库，建议选前者——不动权限基线、只动路由映射）
2. **A5 返利预警**：倾向后端补 `balances` 字段（保持面板语义），还是前端改消费 `projection`？（推荐前者）
3. **A6 合成行**：前端屏蔽操作 vs 后端自动建真实期次？（推荐前端屏蔽，改动面最小）
4. **A8 死代码**：确认可删除 forecast.py 的 accuracy/column-schemes 两个死代码端点？
5. **B2 行删除**：确认"保存时传删除清单到服务端物理删除"的语义（与现有软删字段兼容性需实现时核对）。
