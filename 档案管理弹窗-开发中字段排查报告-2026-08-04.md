# 档案管理弹窗「开发中」字段排查报告（v107.28）

> 日期：2026-08-04 · commit `827283b` · 线上 bundle `main-e7mvbLZG.js`
> 排查对象：侧边栏 hover「📁 档案管理」面板（`static/js/modules/archive-hub.js`，数据驱动 GROUPS，`dev:true` 项显示 🚧 开发中）

## 核心结论

档案管理面板共 **9 个「开发中」项**：**8 项功能早已开发，只是面板没接通（漏接 act）**，其中期初录入还差后端 HTTP 路由（此前提交即 404）。仅 2 项（科目/库存成本期初）确缺专用表单。**本轮已全部处置：7 项接通可用，2 项标注恢复建议。**

---

## 逐项排查清单

| # | 字段名 | 修复前状态 | 功能是否已开发 | 对应代码位置 | 原因分析 | 处置 |
|---|--------|-----------|--------------|-------------|---------|------|
| 1 | 往来标签 | 🚧 开发中 | ✅ 已开发 | 前端 `app.js:1775` `contact_tags`（`showTagManager` 支持客户+供应商，tag_type=contact）；后端 `/api/tags/contact` | 纯漏接：archive-hub GROUPS 只写了 dev，没写 act；「客户标签」项已接同一功能 | ✅ 已接通 → `openRefData('contact_tags')` |
| 2 | 角色权限配置 | 🚧 开发中 | ✅ 已开发（完整） | 前端 `app.js:7592` `showPermissionManager()`（角色 tab/模块权限勾选自动保存/新建角色/员工角色分配）；后端 `/api/permissions/modules` `/api/role-permissions` `/api/users` | 纯漏接：功能完整且有独立入口，archive-hub 未关联 | ✅ 已接通 → `showPermissionManager()` |
| 3 | 应收款期初 | 🚧 开发中 | ✅ 已开发（半成品） | 前端 `import-wizard.js:227` `showOpeningBalance('ar')`；数据层 `erp_db.py:6258` `save_opening_balance`；**HTTP 路由此前缺失** | 三件套缺一环：前端表单 ✓ + 数据层 ✓，但 `/opening-balances/ar` 无路由 → 提交 404 | ✅ 已接通（补路由 + 接 act） |
| 4 | 应付款期初 | 🚧 开发中 | ✅ 同上 | 同上（'ap'） | 同上 | ✅ 已接通 |
| 5 | 预收款期初 | 🚧 开发中 | ✅ 同上 | 同上（'pre_ar'） | 同上 | ✅ 已接通 |
| 6 | 预付款期初 | 🚧 开发中 | ✅ 同上 | 同上（'pre_ap'） | 同上 | ✅ 已接通 |
| 7 | 现金流量期初 | 🚧 开发中 | ✅ 同上 | 同上（'cashflow'） | 同上 | ✅ 已接通 |
| 8 | 科目期初 | 🚧 开发中 | ⚠️ 数据层已备、**前端专用表单缺失** | 数据层 `erp_db.py:6275` `save_account_opening`；`import_zhoupu.py:365` `import_coa_openings`（舟谱导入已实现） | 通用金额表单（仅金额+备注）无法录入「科目+借/贷」，需专用 UI 或走舟谱导入 | 保持 dev，建议走「智能导入」或补专用表单 |
| 9 | 库存成本期初 | 🚧 开发中 | ⚠️ 同上 | 数据层 `erp_db.py:6269` `save_inventory_opening`；`import_zhoupu.py:255` `import_opening_inventory` | 需选「商品+数量+成本」，通用表单不适用 | 保持 dev，同上建议 |

## 本轮修复内容

1. **后端 `server/routers/purchase.py`**：新增 `POST /api/opening-balances/{btype}` 动态路由
   - 白名单 `ar / ap / pre-ar / pre-ap / cashflow`；`inventory / account` 拦截返回 404（防误入专用接口）；非法类型 400
   - 前端 `showOpeningBalance` 的路径式提交此前全部 404，现可正常落库（`save_opening_balance` 写入 `opening_balances` 表）
2. **前端 `static/js/modules/archive-hub.js`**：往来标签 / 角色权限配置 / 期初 5 项接上 act（均有 `if (window.xxx)` 兜底，模块未加载时 toast 提示）；科目/库存成本期初保留 dev 并加注释说明恢复建议

## 验证

- `node --check` ✓ / `py_compile` ✓
- 路由冒烟：`/opening-balances/ar`、`/pre-ar` 匿名 401（已注册且受保护）；非法类型 `/xxx` 401；部署前线上为 404 → 部署后 401，路由生效
- 数据层：临时库 `save_opening_balance('ar'/'pre_ar')` 写入 + `get_opening_balances` 查询 ✓
- 线上：首页 200、`/api/health` ok、新 bundle 200

## 附注：其他「开发中」标注（非档案管理面板）

| 位置 | 标注 | 功能现状 |
|------|------|---------|
| `app.js:6302` AI 员工「渠道接入」卡片（飞书/企业微信/钉钉/短信） | 点击 toast「XX配置开发中」 | **确属未开发**：只有卡片 UI，无接入配置逻辑（AI 员工消息渠道需后续开发） |
| `operations-hub.js`（侧边栏「经营/业务」面板，姊妹面板） | 30+ 项 dev | 多为舟谱对标功能，**抽查发现大量已实现但漏接**：收款单/付款单（`showPaymentForm`）、预付款单（`showPrepaymentForm`）、客户对账单（`showReconciliation`）、库存查询（stock 页）、临期预警（已接 navTo('expiry')）等；另有司机任务看板/盘点点写单/组拆单等可能确未实现，需专项核查后决定恢复哪些 |
| `product-form.js` 商品编辑页 AI 搜图 / 视频上传 / AI 商品名按钮 | toast「AI功能开发中」等 | 确属未开发（AI 能力规划中） |

## 建议后续

1. **operations-hub 专项核查**：与本次同模式，逐项对照 `showPaymentForm`/`showPrepaymentForm`/`showReconciliation`/stock 等已存在入口批量接通，预计可恢复 10+ 项
2. 科目/库存成本期初：补专用录入 UI（科目选择+借贷 / 商品选择+数量+成本），或引导用户用「智能导入」
3. AI 渠道接入（app.js:6302）：属 AI 员工能力域，建议排入 P1 计划
