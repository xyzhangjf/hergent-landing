# 2026-07-22 工作记录

## erp.hergent.cn 全面审查报告
- 用户要求对 Web ERP（erp.hergent.cn，v90）做全面审查：①产品对标(用友/金蝶/SAP) ②定位匹配 ③前端优化 ④后端优化 ⑤优先级排序
- 产出：`erp全面审查报告-2026-07-22.md`（在当前工作目录）
- 实地核实的最新代码指标：
  - 前端：static/index.html 108个同步script标签、static/js 2.4MB、app.js 7466行、styles.css 6148行、93个JS模块、暗色为唯一主题(浅色token死代码)
  - 后端：63 routers、erp_db.py 677函数/10633行、229张表仅2外键/26索引、裸except已降至18处(v90清理)、Pydantic schema仅4文件(auth/inventory/order/product)
  - 关键安全/权限证据：
    - import_router.py:156-157 文件上传仅校验扩展名，无MIME/大小限制 → XXE/zip炸弹风险
    - routers/sales.py:70-74 operator_id由前端参数传入，后端只_auth校验登录，无数据级隔离(业务员可看全租户订单)
    - core.py:215-217 默认角色仅boss/accountant/sales，缺导购/司机；driver_id存名字非用户ID
    - db/connection.py 多租户靠分库(contextvars+每租户.db)，业务表无tenant_id列
- 核心结论：功能广度接近用友/金蝶中小版，但"低温奶经销商AI系统"定位未落地(通用ERP外壳)；差距在行业专属规则未默认化、五角色数据隔离缺失、UI暗色+导航过满、AI入口(企微Bot)未建
- 优先级：高优先立即做——恢复浅色默认、按角色砍导航、业务员数据隔离、文件上传校验、tenant_id列；本月做FEFO/专属价默认化+企微Bot
- 注意：既有UI审查报告(2026-07-21)审查的是桌面版(hergent.cn)非本对象，已在新报告注明区分

## Phase K — 激活 AI 可视化卡（闭环，本轮确认）
- 部署目标真相：生产跑根级扁平代码 /opt/hergent-erp/server.py（systemd），非本地嵌套 hergent-erp/server/server.py
- server.py 注入 4 类 viz 卡片(profit/anomaly/forecast/expiry)，在 ai_chat 三处 return 出口拼接；前端 renderVizCard 早已上线，零改动
- 修 hermes_core.py last_tool→tool_name 笔误（盈亏/日报类 Hermes 查询原走"假无数据"）
- 修 memory.py get_smart_suggestions import re 分支内遮蔽 → UnboundLocalError（expiry 500 根因）
- 验证：四类 viz 全 200；空态因 sale_order_items=0 + 当月无订单（真实数据缺口），非 bug

## Phase L — P0 API 校验 + 错误不吞（本次）
- 目标：堵注入/越权与"假成功/信息泄露"类坑（用户选 P0 开工）
- 改动文件：/opt/hergent-erp/server.py（根级）+ /opt/hergent-erp/core.py
- 加 _err() 助手：只回通用文案+`trace_id`，完整异常留服务端日志，绝不外泄 str(exc)
- 替换 4 处散落 `return {"success":False,"error":str(e)}`（品牌图标/备份状态/备份触发/日志读取）→ `_err(..., log_exc=e)`
- 改 core.register_exception_handler：未捕获异常不再回传 str(exc)[:200]，改 `internal_error`+`trace_id`+`X-Trace-Id`/`X-Request-ID` 响应头
- Pydantic 试点：/api/chat、/api/correct 入参用 BaseModel 校验（text/role/model/original/params 类型+max_length）；非法入参→422 安全信封
- 踩坑：_err 初版误用 `core.err_dict` 但 server.py 未绑定 `core` 名 → NameError 把 422 变 500；改显式 `from core import err_dict` 修复
- 验证（直连生产）：四类 viz 仍 200；int/超长/list 三类非法→422 无泄露；全局 500→generic+track_id；backup_status 等错误不再外泄内部路径
- 已知遗留（非本次范围）：①未知 /api/* 被 SPA 兜底返回 200 HTML 而非 404 JSON ②租户中间件 L125 except 后静默放行（隔离可能失效）③_per_endpoint 仍返 200+success:False 而非 4xx/5xx（契约问题）④仅 2 接口试点 Pydantic，其余 280 路由待推广
- 备份：server.py.bak-* / core.py.bak-* / hermes_core.py.bak-*（本轮）+ 本机 /tmp/prod_*_phaseL.py 编辑副本
