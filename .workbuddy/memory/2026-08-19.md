# 2026-08-19 工作日志

## 定时推送闭环补全（wecom webhook 自助配置）
- **根因**：`get_wecom_webhook()` 误读不存在的 `settings` 表 → 永远读不到 webhook → `configured:false`，导致「定时推送」推不出去（功能半成品）。
- **修复**：① `get_wecom_webhook`/`send_wecom` 改读主库 `system_config.wecom_webhook_url`（与 `set_config` 一致）；② 新增 `POST /api/notify/wecom/config`（_admin，保存/清除 webhook，校验 `https://qyapi.weixin.qq.com/` 前缀）；③ 前端 CronJobs 推送卡加输入框「保存并启用」。
- **多租户坑（重要）**：`get_db()` 在 `set_tenant_context(tid)` 下连 `tenant_N.db`，主库全局表读不到。webhook 读写 + demo-skip 的 tenants 查询都改为**直连主库** `sqlite3.connect(DB_PATH)`。否则演示租户(tid=10)会把演示数据推到老板企微。
- **验证全通**：保存→状态翻转(configured=true)；错误地址拒(400)；演示租户 today-refresh → `skipped/demo tenant`；真实租户 → `success attempts=1`；清空→configured=false。生产已部署(commit c7fd460 + 后续 3 个 fix commit)。
- **待用户操作**：在 hergent.cn 定时任务页粘贴自己的企业微信群机器人 webhook 即可启用推送（无需 SSH）。

## AI 进化日志 + 定时推送（前序 8-18 收尾）
- 进化日志端点 `/api/ai/evolution` 生产已验证（8 技能 + 1 轮 curator）；前端「能力中心-进化日志」tab 已部署。
- hergent 用户读 /root 权限已修（chmod o+x 链 + o+rX curator/skills）。

## 技能卡片富化（P0 收尾，8-19）
- ConnectCenter.vue 技能 tab：前端加 `SKILL_META` 映射（8 技能 icon/中文名/能干嘛/何时触发），把机器名+技术描述 → 老板可懂卡片。自进化技能区也富化（✨+name+desc）。已构建部署，富化文案进 bundle 验证。
- 数据源 `aiSkillsApi.list()` = `/api/ai/skills`（Hermes 代理，生产正常返回 8 个，含 hergent-erp-diagnostics）。注意：不走本地目录，所以不受 /root 权限影响。
- **修复进化日志页权限回归**：`/root/.hermes` 及 skills/logs/curator 被 Hermes 重置为 700 → hergent 用户读不到 → 进化日志 skills 空。chmod 755 /root/.hermes + o+x logs + o+rX skills/logs/curator 恢复（现在 skills 8 个回来了）。⚠️ 此 chmod 可能被 Hermes 再次重置（根治=evolution 端点技能读取改走 Hermes 代理 /v1/skills，而非 os.walk 读 /root 目录）
- 进化日志技能区小瑕疵（非阻塞，可选修）：title 读 SKILL.md 首行 # 失败回退机器名；嵌套 erp/ 目录导致怪名 erp-hergent-erp-diagnostics

## 适配畅捷通 · 前后端开发排期（用户要）
- **重大发现（改写了工作量评估）**：后端**已有金蝶连接器范式**——`server/kingdee_connector.py`（OAuth2+自动刷新+30+业务API+map_product/map_contact+kingdee_tokens 表）与 `server/routers/datasources_v2.py` 的 `/kingdee/*` 端点。畅捷通与金蝶同属「OAuth+OpenAPI」型，适配≈**克隆金蝶改 API 地址**，非从零。前端连接中心只挂了企微+MCP(舟谱)卡，金蝶/畅捷通 UI 未暴露。
- **金蝶模式两缺口（畅捷通多客户必须补）**：① `kingdee_tokens` 是全局主库表无 `tenant_id`，畅捷通须每租户隔离凭据（新建 `datasource_bindings`，加密落库）；② 金蝶同步写 `products/contacts` 带 `source='kingdee'`，6 引擎能否消费取决于是否写入租户库。
- **排期落盘**：`适配畅捷通-前后端开发排期-2026-08-19.md`。P0≈8.5-10.5人天(T1克隆连接器/T2 chanjet端点/T3每租户凭据隔离/T4前端连接卡/T5写工具零越权)；P1≈9人天(T6同步写租户库/T7批次效期映射/T8定时同步/T9引擎适配/T10数据来源徽标)。**建议先用 `hermes-mcp-chanjet/` 跑副驾Q&A 验证付费意愿（零后端改造）**，再投 P0/P1。
- 复用清单：kingdee_connector.py→chanjet_connector.py；datasources_v2.py 加 chanjet_*；crypto_utils.py 加密；ConnectCenter.vue 企微卡→畅捷通卡。

## 进化日志技能区根治（8-19 收尾，已落地）
- **根因**：`/api/ai/evolution` 技能库部分 `os.walk('/root/.hermes/skills')` 直读文件系统；Hermes 周期性重置目录权限为 700 → hergent 用户读不到 → items 空 → 前端进化日志技能库 `v-if` 为假整块不显示（即"进化日志技能区"空白）。chmod 临时修复会复发。
- **根治（双管齐下）**：① 后端 `ai_evolution_log` 技能库部分改调 `routers.ai_skills._hermes_skills()`（Hermes `/v1/skills` 代理），稳定且带中文描述，Hermes 未连时降级为空不报错；② 前端 `ConnectCenter.vue` 进化日志技能库区数据源由 `evSkills.items`（文件系统）改为复用 `aiSkills`（onMounted 已加载、`/api/ai/skills` 代理同源），并用 `SKILL_META` 富化（icon/中文名/能干啥），统计行改 `aiSkills.length`。
- **验证**：`py_compile` OK；`vite build` OK（63 modules）；生产部署后 `curl` 实测 `/api/ai/evolution` 技能 `skills_total=8/items=8`、`curator runs=1`，`/api/ai/skills`=8。根治生效，不再依赖 chmod。
- **部署要点**：前端 `dist/`→`/opt/hergent-cn-v2/`（hergent.cn 根）；后端 flatten→`/opt/hergent-erp/`，**务必 `--exclude=static`**（否则 rsync 因 static 冲突 exit 23 且 server.py 可能未同步）；restart `hergent-erp`→health 200。

## 竞品研究：畅捷通（8-19）
- 用友旗下小微 SaaS 龙头（港股01588，付费用户超102万），好生意云进销存已覆盖饮料批发/快消经销商，精易版(980/年)内置 AI 开单/问数/分析。
- 结论：Hergent 勿拼进销存广度，护城河在低温奶专属决策算法（配方化 skill）+ 副驾形态；「AI 问数」已成标配需叠加垂直价值。对标报告=`竞品对标-畅捷通-2026-08-19.md`。

## 畅捷通开放平台启发（8-19 续）
- 开放平台支持 **MCP 协议 + 600+ API + 应用市场(4万+应用/4600+ ISV)**，自身在做 Agentic ERP(双引擎/58智能体)。
- 启示：对**畅捷通/T+ 系客户**走 MCP/OpenAPI **生态集成**（Hermes 原生支持 MCP），而非舟谱式对抗；可评估把低温奶 AI 副驾做成 ISV 应用借渠道获客。但命门不可绑死平台，须保留"导入Excel/CSV 无 ERP 也能跑"降级路径。报告=`竞品启发-畅捷通开放平台-2026-08-19.md`。

## 探路研究：Hergent×畅捷通 技术可行性（8-19）
- **结论：技术可行且门槛低于预期**。畅捷通官方已有 MCP 服务 **CJTMSP-MCP**（MCP 市场 mcphub-admin.static.chanjet.com/marketplace，把 SaaS OpenAPI 封装为标准化 MCP）；社区开源 cjt2mcp(glama)/Chanjet MCP Server(lobehub) 可自托管多租户（每客户=一个自建应用，凭据加密）。
- 认证链：注册开放平台→企业认证→建应用拿 appKey/appSecret→客户 OAuth 授权换 openToken(6天可刷)→调 600+ API。Hermes 原生支持 MCP，接入=加一段 MCP server 配置。
- 边界：注册/企业认证/客户授权须用户本人（手机号+资质）。探路清单7步见 `Hergent×畅捷通-技术可行性探路-2026-08-19.md`。护城河仍在低温奶配方化算法，连接本身标准化非壁垒。

## Hergent×畅捷通 步骤4-7 接入脚本套件落地（8-19）
- 用户确认"要"先把步骤4-7脚本备着。已落地 `laozhangai-product/hermes-mcp-chanjet/` 套件（5文件）：`setup_chanjet_mcp.py`(步骤4注入，支持路线A官方CJTMCP/路线B自托管cjt2mcp、多租户命名、Bearer走${ENV}不落明文、tools.exclude只读过滤、--dry-run)、`verify_and_scenario.py`(步骤6三条只读查询verify + 步骤7低温奶场景scenario，subprocess调`hermes` CLI)、`config.routeA.yaml`/`config.routeB.yaml`(模板)、`README.md`(7步手册)。
- **Hermes MCP 真实配置格式（已核实 v0.18.2/v0.19.0 通用）**：`~/.hermes/config.yaml` 顶层 `mcp_servers:` 段（非 `mcp:`）；HTTP型=`url`+`headers.Authorization: Bearer ${ENV_VAR}`；CLI=`hermes mcp add/test/list/configure`；安全=先只读测试+`tools.exclude`收窄写操作+密钥走.env不落明文。本机 config 现无 mcp_servers 段（待注入）。
- **验证通过**：py_compile OK；self-re-exec 兜底（系统python3无yaml自动重跑hermes venv）生效；临时副本真实注入 chanjet_acme 成功、原config未被误改(grep mcp_servers=0)。注册拿到MCP URL/Key后：`python3 setup_chanjet_mcp.py --route A --name chanjet --url <URL> --env-var CHANJET_MCP_KEY`→写.env→`hermes mcp test`→`verify_and_scenario.py verify/scenario` 即可真机联调。
- 边界：注册/企业认证/客户授权须用户本人（手机号+资质）；真机联调等注册后。护城河仍在低温奶配方化算法，连接标准化非壁垒。

## 战略讨论：产品未来发展路径（用户问"你觉得未来怎么走"）
- 给出五阶段路线图（见上方 show_widget / 对话内嵌 SVG）：P0 基础就绪(已完成)→P1 验证模式0-3月(轻量MVP每客户隔离实例+Excel起步,先收钱再重投)→P2 双客群+生态借力3-9月(客群A换系统/客群B畅捷通MCP;评估ISV上架;不绑死平台)→P3 真SaaS 9-18月(用付费数据找技术合伙人/融资,真多租户云)→P4 平台化18月+(低温奶→快消垂直AI应用平台+行业skill市场)。
- 核心判断：①护城河=低温奶配方化决策算法(货损/返利/预报/工资),不是"能连ERP"(连接标准化巨头也会做);②"AI问数"已被畅捷通58智能体商品化,须叠垂直决策价值;③1人非技术但恰适合"预置式ISV"(你有经验通用SaaS没有);④命门不可绑死任何平台,保留Excel/CSV降级路径;⑤巨头窗口期有限,先用轻量MVP收钱验证付费意愿,再重投入。
- 下一步建议：用 demo 入口+轻量 MVP 找 1-2 同行验证付费；并行你注册畅捷通开放平台(脚本已备)验证生态路线；现在勿重投做完整多租户 SaaS。

## 技术讨论：适配畅捷通的前端/后端优化（用户问"需要哪些优化"）
- 关键判断：agent 侧连接（Hermes MCP）已由 `hermes-mcp-chanjet/` 脚本解决 70%；剩余是**把能力产品化/按租户落地 + 让 6 引擎能吃畅捷通数据**。
- 核心架构改造=**DataProvider 抽象层**：统一读接口路由到三种源（本地 tenant_N.db / 畅捷通 MCP 同步缓存 / Excel 导入降级）。6 引擎+副驾都走这层。
- 后端优先级：P0=数据源绑定模型+加密凭据库(datasource_bindings: appKey/appSecret/openToken+刷新)+ConnectCenter"连接畅捷通"OAuth向导+按租户 MCP 路由(现脚本是全局单配置需改每租户)+延续"AI只建议不擅自下单"(MCP写工具排除+确认);P1=DataProvider抽象+畅捷通同步服务(定时拉库存/应收/销售进本地读模型,限流感知,last-sync状态)+畅捷通schema→内部模型映射;P2=真多租户云+API计费/韧性。
- 前端优先级：连接中心加畅捷通连接卡+OAuth流+绑定状态;数据来源感知徽标(库存/应收/销售页显示"来源:畅捷通·最后同步");同步状态+手动同步按钮;保留Excel/CSV导入醒目(降级);副驾对话标注"基于畅捷通数据"+写操作二次确认;租户管理加数据源页。
- 不要现在做：①未签首单前重构成 6 引擎(先用副驾Q&A走Hermes MCP免费验证);②丢弃本地DB/Excel路径(独立获客+降级命门);③副驾暴露畅捷通写API无二次确认。
- 计算策略建议：6 引擎用"同步后本地计算"(确定性/快/离线)；副驾即问即答走 Hermes MCP 直读(轻量)。

## T5 写工具零越权审计（2026-08-19 完成·部署验证）
- **问题**：数据源 sync 端点 `_auth` 后直接 `db.get_db()` 写库，**无显式 `set_tenant_context`、无 fail-closed 守卫**；若租户上下文意外落到主库(tid=0)，`db.get_db()` 会静默把金蝶/畅捷通数据写进 `erp.db` 主库(跨租户/越权)。中间件虽按 X-Tenant-Id+check_user_tenant 设上下文，但 sync 自身不校验。
- **交付**：① 新建 `server/tenant_audit.py`：`assert_tenant_write_scope(tenant_id, source)` fail-closed 守卫(拒写主库 + 凭据须属本租户 T3 隔离) + `scan_tenant_isolation()` 只读扫描器(主库误存外部ERP行/各租户库统计→JSON 报告，生产可 `python3 tenant_audit.py` 直跑)。② `routers/datasources_v2.py` 的 kingdee/chanjet sync 写库前加守卫并 `set_tenant_context(tid)` 锁定范围。③ 新建 `tests/test_tenant_write_audit.py`(7 用例全过：守卫拒主库/须自有凭据、双租户写隔离、扫描器抓主库泄漏)。
- **验证**：本地 pytest 7 passed；生产 rsync flatten 部署 + restart + health 200；生产实跑 `scan_tenant_isolation` → `ok:true, violations:[]`，各租户库 external:0、主库无泄漏。
- **待补（原）**：金蝶卡 UI 仍待暴露。

## T6 同步写租户库（2026-08-19 完成·部署验证）
- **根因（比预期更深的洞）**：sync 之前"写租户库"只是设想——全仓库 `products` 表**无 `source`/`source_id` 列**、`contacts` 缺 `source_id`，导致 `INSERT INTO products(...source,source_id...)` 运行时崩溃，数据**从未落进 `tenant_{id}.db`**（6 引擎自然读不到）。生产核查 tenant_1/2/3 均确认缺列。
- **交付**：① `erp_db.py` 补列——`v4_products_source`/`v4_products_source_id`/`v4_contacts_source_id`（init_db 主库迁移）；并在 `_migrate_v68` 存量租户库 patch 循环补同样三列（双保险，照 v107.146 模式处理 schema 已分叉的旧租户库）。② 新建 `server/sync_writer.py`：`sync_source(source, client, map_product, map_contact, objects, modified_after, tenant_id)`，把路由内联写库逻辑抽成可测单一真相源，写 `products`/`contacts`(带 source/source_id upsert)，`set_tenant_context(tenant_id)` 锁定写范围。③ `routers/datasources_v2.py` 的 kingdee/chanjet sync 端点重构为委托 `sync_source`（响应结构不变），并删除不再使用的 `import erp_db as db`。④ `tenant_audit.scan_tenant_isolation` 增强：每表输出 `by_source:{kingdee,chanjet}` 计数，使"同步数据被租户库承接、6 引擎可见"可量化。⑤ 新建 `tests/test_tenant_sync_write.py`（4 用例全过：写正确租户库/不漏主库/租户隔离/upsert 幂等）。
- **顺手修的隐藏 bug**：重构时发现 contacts INSERT 参数顺序错位（type 列被填成手机号）触发 CHECK 约束失败，已修正。
- **验证**：本地 pytest 11 passed（T6 4 + T5 7 回归）；生产 rsync flatten + restart + health 200；生产确认主库+tenant_1/2/3 全补齐 `source`/`source_id` 列；部署后真实 `sync_writer` 烟雾测试写 tenant_999（1 商品+1 客户+1 供应商，errors:[]）端到端落库成功、主库 0 泄漏、tenant_999 已清理；`tenant_audit.py` 生产 `ok:true` 且 by_source 输出正常。
- **余下（T7-T10）**：T7 批次效期映射(库存 batch_no/expiry 入库→货损引擎可读)、T8 定时同步、T9 引擎适配(当前仅 products/contacts 可读，销售单/采购单/库存尚未同步)、T10 数据来源徽标；金蝶卡 UI 仍待补。
