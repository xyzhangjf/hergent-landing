# Hergent 项目长期记忆（索引）

> **本页只回答「该读哪一份」**：判据全文在 `memory/topics/`，只留「路由 + 触发词 + 最贵判据」。
> **上限实测：11.3KB 完整注入、14.5KB 截断** ⇒ 目标 ≤10.5KB｜版本史 / 起号规程 → `topics/version-history.md`

## 一、路由（改哪个面 → 读哪份）

- **前端**（页面/样式/表格/探针/数字格）→ `topics/frontend-ui.md`
  · 全屏图层 ⇒ 页头控件**物理不可达**，须**层内**补副本；让位三条件缺一 = **死按钮不报错**
  · v214-A 数字格病根在**输入层**：`type=number` 静默加工、`NFKC` 不折 `。`
  触发：全屏 · 工具栏 · 表格 · 数字格 · 全角数字 · 文字不居中 · **模块下架 · 移除侧栏入口 · 死链**
- **后端**（路由/数据/DB/权限/账号/报 500）→ `topics/backend-invariants.md` + `backend-auth.md`
  · RBAC 在最外层 ⇒ `_check_perm` 时**还没租户**，须显式传 `tenant_id=`
  · v211b 参数错回 500 = `ValidationError` ≠ `RequestValidationError`；索引须**表与索引同行**
  · v213/v215/v225 **恒空恒 0 且零报错 = 静默失效**（时区/参数名/裸 `except`/未定义名字）⇒ `tools/undefined-call-scan.py`；🔴 **候选 ≠ 缺陷**；🔴 **缺陷真实、读数却「碰巧对」**（配置从未填 ⇒ 恒 0 恰是事实）⇒ 问「**恒定值是否恰好等于当前事实**」；🔴 **SQLite DDL 是事务性的** ⇒ 建表须 `get_db_tx()`
  触发：权限 · 开账号 · 列权限 · 报 500 · 参数校验 · 索引 · 时区 · 乐观锁 · **静默失效**
- **部署 / 构建 / 上线 / 回读 / 旧前端 `static/`** → `topics/deploy-ops.md`（**§v230 / §v233 有新判据**；🔴 **加列不必手工 glob**：租户库**启动期列对账自动补列** ⇒ 验收 = 读 `[schema-sync] … 补列(+N)`）
  · **md5 一致只证明「传输没坏」** ⇒ 补验**产物 mtime ≥ 源码 mtime**
  · 🔴 **生产路径别靠记忆试**：后端 **FLAT** `/opt/hergent-erp/`；前端 `/opt/hergent-cn-v2` **本身就是 dist**。跨轮复核：后端比**本地工作区**（不是 HEAD ⇒ 含在途者**必然不等**）；前端比**入口 chunk 名 + md5**
  · 🔴 **批量判「本地 == 生产」**（= 整文件可入库）= ① md5 **逐文件全等**（非抽样）**且** ② **无源码文件 mtime 晚于构建时刻** ⇒ **判「哪些 hunk 属于我」之前，先判「该文件整体是否已上线」**
  · 🔴 **「口径重写」型术语批次不能用等长判据**（v226 是重写）⇒ 只能**逐个 dump 全文人工读**
  · v214b/v219/v224 事后复验（→ `deploy-ops.md §v224`）：第⑥层 **token 对齐**；只能用**判别串计数**（**正反例同结果 = 探针作废**）；切分 **`drop_plus_lines`** + **`dropped`**（暂存 0 且工作区 >0）；`own_hunks` 是行号
  · 🔴 **本仓无 production 分支** ⇒ 生产基线只能靠**生产文件系统**反推；🔴 **生产 `assets/` 是历次构建的并集**（实测 132 = 4 次残留，一次全新构建仅 51）⇒ **判别串只在「生效批次」里查**，直接扫会得到反向结论
  触发：构建 · 上线 · 回读 · 差集 · scopeId · 重编号 · 事后复验 · **受控提交 · 依赖闭包 · 夹带 · 在途被并进我的 hunk · 提交==上线 · 字节对齐 · 全站排查 · 生产基线 · 未上线**
- 🔴 **受控提交 / 收索引三条纪律**（→ `deploy-ops.md §v230`）
  · 临时索引提交后**必须复位共享索引**（残留 `MM`/`D ` 会被下次 commit **回退成旧版**）；取路径须 `-z`（中文路径带引号 ⇒ `git reset -- <它>` **静默 no-op**）
  · **绝不用 `git checkout -- <目录>`** 收索引（连带抹掉该目录下**所有**已跟踪文件的未提交改动，**不可逆**）
  · **只交「已改的跟踪文件」= 坏提交**：`app.json`/`require` 指向未入库文件，git、`node --check`、`py_compile` **全不报** ⇒ 必做**可达性审计** + **页面完整性审计**
  触发：受控提交 · 夹带 · 索引 · 遗留未提交 · 坏提交 · 已部署从未提交
- **预报主表 / 期次 / 导入 / 到货周期 / 报单基准 / 价格单位** → `topics/forecast-order-domain.md`（最长）
  · 口径类缺陷**先问当初拍板的口径**；**崩溃时机即分类器**（点删才崩=字段缺失 / 进页即崩+栈溢出=自递归）
  · v215 报单基准 = **期次商品清单**非档案，三处走同一函数
  · 🔴 **报单单位铁律（v223 拍板 / v233 落地）**：**不是整箱报单** —— 三级用**中单位**、两级用**小单位**，**永不落大单位** ⇒ `箱数 = 数量 ÷ perCase` 本来就对。✅ **v233 真身 = `products.order_unit`**（留空跟随 `unit`；主表「单位」与舟谱模板「*单位」**同源**）⇒ 🔴 **改单位「必带」数量换算**，换算不出 ⇒ **退回明细单位 + 下载告警点名**。⚠️ 别拿 `products.unit` 当报单单位
  · 🔴 **厂价 = 元/箱** ⇒ **`单价(厂价/箱)` 就是厂价本身**；v223 删掉 `priceAuto` 里的 `× perCase`（**用户 v217 报的「99→990」就是它**）。⚠️ **三轮都在调「乘多少」，没人问「该不该乘」** ⇒ 修「值不对的公式」先判**这个运算该不该存在**
  · 🔴 `purchase/sale/dist_price` = **元/小单位**，`factory_price` = **元/大单位** ⇒ v226b 已删该档回退；🔴「进价」下**两条链口径有意分家**（**金额基准**只认 `factory_price` 不回退 / **档案完整性**保留回退）⇒ **别再把两条「对齐」回去**
  · 🔴 **对外单据量纲**：舟谱「*单价(折后价)」与「*单位」成对 = **元/单位**（而报单明细价 = 进价元/箱）⇒ **v232 起模板取渠道价**（连**单位名**一起返回；量纲不明**宁可回退**，绝不填不知道量纲的数）
  · v217 单位被改 = 档案优先 + 回填**只补空不覆盖**
  · 🔴 **期次列表出现「无按钮的行」= 合成行**（`id<0`，`forecast_order_board()` 按 `order_date` 补出、**非** `forecast_periods` 记录）⇒ `Number(row.id)>0` 全屏蔽**是设计如此**；清理**必带**同事务写的 `forecast_period_confirm`；影响面按**语义列**扫（值级扫零判别力 → 技能 §11）
  触发：预报 · 期次 · 定稿 · 合计(箱) · 导入顺序 · **单价 · 990 · 报单单位 · 单位换算 · 舟谱模板单价 · 缺规格 · 进价 · 金额基准 · 补价 134/124 · 静默丢价 · 合成行 · 孤儿报单 · 保存门禁 · 没有进行中的期次**
- **返利 / 目标** → `topics/rebate-domain.md`｜**货损 / 效期**（先分 `/loss` vs `/loss-accounting`）→ `topics/expiry-loss-domain.md`
- **报单 / 小程序 / 品牌 / 员工账号 / 提审** → `topics/miniprogram-and-brand-data.md`
  · **「能登录」≠「能干活」**：认证 `/api/auth` 豁免 RBAC、业务走模块中间件；角色缺 `data` ⇒ 每动作 403
  · v211 重开账号须先改名+停用旧的；新账号 `password_changed=0` = **首登强制改密**，密码 **≥8**
  · 小程序**「提交了但看不出成没成」**查四条（回执位置 / 回执条件死代码 / 单号可见 / 错误分级）；🔴 改行为**必须同步改测试清单**（`真机E2E测试清单.md`）
  触发：报单 · 门店范围 · 提审账号 · 首登改密 · 员工档案 · 收回门店 · **提交成功无反馈 · 一店一期一单 · 单号 · 结果卡 · 样单 · 复制生成样单 · 窗口未开始**
- **小程序「保活」/ 切出去回来数据没了** → `topics/miniprogram-keepalive.md`
  · 微信**根本不支持保活**（进后台约 5 分钟回收）⇒ 真解法 = 端上**状态韧性**（`onHide` 落草稿 → `onShow` 询问式恢复）+ cron + **企微应用消息**召回
  触发：保活 · 后台运行 · 切出去回来 · 草稿丢失 · 订阅消息
- **副驾 / AI 落点** → `topics/ai-copilot.md`（算·录·判·说；名带 `ai_` ≠ 用了 AI）｜**通知 / 工资条** → `topics/notification-center.md`
- **IM 渠道** → `topics/im-channels-v131.md`｜**业绩 / 提成 / 龙虎榜** → `topics/sales-reports-and-operator-attribution.md`
- **对账 / 流水 / 催收** → `topics/reconciliation-redo.md`｜**键对账 ≠ 账户对账**；**人工报告不是黄金标准** ⇒ 验收 = **逐笔可解释**
- **新用户建档 / 价格方案 / 单位换算** → `topics/onboarding-and-archive-gate.md`｜拦写入不拦浏览、隐式建档优先；🔴 六类档案之外还有**员工 + `report_mapping`** = 预报第一硬门槛
  · 🔴 **`contacts.channel_id` 列从未被创建过**（有守卫 ⇒ **永久落兜底价、零报错**）⇒ 「千店千价」先修这一列
  · 🔴 价格真身 = **`customer_prices`**（**非** `product_channel_prices`，后者 0 行）；**「三列并排」存三级单位价**、与舟谱同构 ⇒ **照抄，别再「加一列 unit」**；🔴「前端缺某字段」先分清它从哪条接口来（真源可能是 **`productsApi.grid()`** 主档）
  · ✅ **v229 三档价联动**：中/大单位价 = 小单位价 × 换算比（用户拍板「价是联动的」）⇒ **别再每档各存独立数**（必漂移）
  · 🔴 **换算唯一权威 = `products.large_ratio/medium_ratio`**；判据用 **`large_ratio > 0`**，**不要** `has_multi_unit`（**漏标 9 条**）；`unit_conversions` 0 行 ⇒ **别再新建换算数据**
  · ✅ **v222**：`perCase` = **档案换算优先 → 规格解析回退**；由 **`products_grid` 白名单**放行（**不加 = 全走回退**）。⚠️ **v223 修正**：「按件/箱报单箱数被除错」**是错的**；真缺陷只有 **spec 为空**
  触发：新用户 · 建档前置 · 空状态 · **价格方案 · 千店千价 · 单位换算 · 换算权威 · 装箱数 · 客户挂渠道 · 三列并排 · customer_prices · 三档价 · 合计(箱) 不对**
- 🔴 **跨域铁律 → `topics/cross-domain-iron-laws.md`** ← 动手前先扫一遍
- **对外材料** → `outputs/德邻杯-AI创业大赛-2026-09-19/02-脱敏口径清单.md`（金额→复杂度 · 截图用演示租户）

## 二、技能路由 → `topics/skill-routing.md`（含沙箱 `tools/sandbox_tenant.py`）

## 三、编号约定（⚠️ 起号前必做）

已用到 **v240**（v240 报单单位→报单默认单位：改下拉 + 迁到包装单位 Tab + 报单页新增单据带出；v239 价格矩阵 `product_unit_prices`；v237/v238 包装层级列与编辑表单；v234 前端下架「催收跟进」模块 = 7 落点）。
🔴 起号前三连搜：源码 `grep -rn -e "v20X" -e "V20X"`（多模式**必须 `-e`**）＋ `memory/` ＋ `tools/`。⚠️「已用到 vNNN」不可信（并发撞号 ⇒ 改号含生产重同步）；🔴 技能章节号同样会撞。**规程 / 版本史 → `topics/version-history.md`**

## 四、主体 / 脱敏

hergent-cn-v2（`laozhangai-product` → `/opt/hergent-cn-v2`）｜hergent-erp（FastAPI + SQLite `:8700` → **FLAT** `/opt/hergent-erp`）｜🔴 **脱敏红线**：返利率 / 进货价 / 客户名 / 区域销量 / 厂家政策。
🔴 **仓库内含生产凭据明文**（2026-09-21 核实）：Hermes 网关 Bearer 密钥在**已跟踪**的 `artifacts/nginx-hermes-proxy.conf` 等 **9 文件** / 历史 **3 提交**里；提审账号 `Mptest@1`/`Mpsup@1`（历史 11 提交 / 已跟踪 6+8 文件）与老板手机号（历史 4 提交）同样早已入库 ⇒ **远端仓库必须 private**；建议轮换网关密钥。**入库新文件前先跑凭据扫描**。

## 五、本机坑 → `topics/local-machine-pitfalls.md`

`grep "A\|B"` 静默失效 ⇒ 用 `-e A -e B`；**多条 grep 串 `&&` 会短路**（像"没命中"）⇒ 用 `;` 分隔。🔴 舟谱 xlsx / 真机读表 / cdp-lite 三坑见该 topics。
