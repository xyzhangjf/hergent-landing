# 2026-08-30 工作日志

- **Rebate.vue P0 改造上线（对标舟谱目标管理）**：落于 hergent-cn-v2 `src/pages/Rebate.vue`（生产主前端 `/opt/hergent-cn-v2`，vanilla 老前端 `hergent-erp/static` 是断链未部署，非真源）。四项 P0 全部完成且 build+rsync+chown 部署验证：①P0-1 维度入口卡片（品牌目标/商品目标 两类 + "创建品牌/商品目标"按钮，openCreate 预设 dimension）；②P0-2 度量口径业务化（amount→下单金额(元)、quantity→销售数量(大单位)，表单 option + 详情展示）；③P0-3 状态语义（statusOf 函数：进行中 ok / 未开始 warn / 已结束 灰，替代原 启用/停用）；④P0-4 详情弹层（openDetail + showDetail + modal-card，摘要=规则名/状态/维度/度量/作用对象/周期口径/阶梯门槛/返利方式/生效区间，复用现有 Vue 风格 `.tag ok|warn`/`.card`/CSS 变量）。新产物 Rebate-CK4LgEeM.js；生产 grep 四项文案全部命中。P1 项(复制/修改记录/三步向导)未做。
- **回退误改 vanilla 文件**：本轮初误改 `hergent-erp/static/js/modules/rebate-rules.js`（+102/-11 未提交），与生产真源重复且未部署，已 `git checkout --` 回退保持单一真源。
- ⚠️ 未做浏览器真机验证（沙箱连不上 hergent.cn）。

- **能力中心补「返利规则」入口（回应"看不到入口"反馈）**：根因=侧边栏 `目标与返利政策`(`Shell.vue:41` `<router-link to="/rebate">`) 早已指向 Rebate.vue(返利规则管理页)，只是页面标题写"目标与返利政策"用户没认出；而「能力中心」(`ConnectCenter.vue` `/connect`) 仅 连接器/专家/技能/进化日志 四 Tab、无业务配置区，故用户在此找不到。改动：在 `ConnectCenter.vue` 新增第 5 个 Tab「业务配置」+ 内容区卡片「返利规则 / 目标与返利政策」(`@click="router.push('/rebate')"`，复用 `.cc-card`/`.cc-action.primary`/`.cc-logo` 现有样式)。build+rsync+chown 部署；新产物 ConnectCenter-C2hqq41-.js；生产 grep `业务配置`=2/`返利规则 / 目标与返利政策`=1/`去配置`=1；Shell `/rebate` 入口仍在。未做真机验证。

- **「预报订单」小程序上线就绪度审计**：交付 `miniprogram-launch-readiness-audit.md`。结论：后端生产就绪（B1–B5 已修+部署，curl 验证 health=200、`/forecast-submissions/*` 401 路由存在）；前端优化 M7–M12/F1–F3 已改码但**从未上传发布**。4 个硬阻塞：①`appid=touristappid`(需真实账号) ②无隐私协议/用户协议 ③未配 request 合法域名(hergent.cn) ④未上传发布。功能缺口：移动端审批流完全缺失(后端有 /pending/approve/reject 但前端只读汇总)、"我的提交"无详情/编辑/撤回。监控埋点=0、测试仅 node --check、灰度回滚无文档。建议顺序：A1注册账号→A2配域名→A3隐私协议→B1上传发布→补功能/埋点/真机E2E/灰度文档。

- **小程序三项纯代码改动已落盘（A3/E2/D1）**：目录 `forecast-order-miniprogram-20260812T023419087Z/miniprogram/`（非 git 仓库，改动为本地未提交）。
  - A3 隐私合规：①新增 `../隐私保护指引.md`（提交微信平台用参考文本）；②`pages/login/login.js` 加隐私网关——`onLoad` 检测 `fs_privacy_agreed`，未同意则 `showPrivacy=true` 拦截登录，并尝试验证 `wx.getPrivacySetting`/`wx.requirePrivacyAuthorize` 官方授权；`onAgreePrivacy` 写标记放行、`onDisagreePrivacy` 保持弹窗；`login()` 双重保险拦截；补 `noop` 防冒泡。③`pages/login/login.wxml` 加隐私同意覆盖弹窗（可滚动全文+同意/暂不同意）。
  - E2 会话清理：`pages/mine/mine.js` `logout()` 现清 `fs_tenant_id` + `fs_period_id`/`fs_store_id`/`fs_cart`/`fs_redirect`，并清 `app.globalData.tenantId`，防退出租户残留串店/越权。
  - D1 静默 catch：`pages/fill/fill.js` 四处空 catch 改 `console.warn`；`loadPeriods` 由静默改为告警+仅当期次为空给非阻断 toast（防 onShow 刷屏，加 `_periodWarned` 守卫）；`utils/api.js` fs_redirect 空 catch 改 warn。
  - 校验：四个 JS `node --check` 全 OK。WXML 未编译（无独立 build）。⚠️ 仍未上传发布、A1/A2 仍须用户在微信公众平台操作。

- **小程序第二批可落地项完成（埋点 / 我的提交详情+撤回 / 移动审批）**：用户"要"继续做，全部落盘。
  - **后端（已外科手术式部署 2 文件 + 重启 + 生产验证）**：`server/erp_db.py` 加 `forecast_submission_recall()`（仅 pending、仅本人或 admin/boss/accountant/supervisor 可撤回，置 `recalled` 态）；汇总两处 `status != 'rejected'` 改为 `NOT IN ('rejected','recalled')`（撤回单不计入总表）。`server/routers/forecast_submissions.py` 加 `POST /{sid}/recall`。部署只 rsync 这 2 文件（不动分支上其余 11 个未推送 commit 的未提交改动，遵守"暂缓"决定）；`health=200`、`recall`/`pending` 假 token 均 401（路由已注册）、生产 grep 确认落盘。
  - **前端（4 JS `node --check` 通过、app.json 合法；尚未上传发布，需微信开发者工具上传）**：①埋点 `utils/track.js`=基于 `wx.reportAnalytics`(原生/无后端依赖/未开自定义分析时静默 no-op)；`api.js` 失败路径接 `reportError`；login/fill 接 LOGIN/LOGIN_FAIL/SUBMIT/SUBMIT_FAIL；mine/approval 接 PAGE_VIEW/APPROVE/REJECT/RECALL。②「我的提交」详情展开 + 状态徽标 + 撤回按钮(调新 recall 端点,仅 pending 显示)。③移动审批页 `pages/approval/*`：拉 `/pending`、按角色门控(admin/boss/accountant/supervisor 可见)、通过→生成采购申请、驳回→填原因；mine 页加审批入口卡片。
  - ⚠️ A1(touristappid)/A2(合法域名)/B1(上传发布) 仍须用户在微信公众平台操作；撤回/审批前端需随 B1 上传才生效。前置 A3/E2/D1 亦同。

- **撤掉能力中心「业务配置」入口（用户拍板 ①B保留侧边栏名 ②A撤能力中心）**：用户确认侧边栏「目标与返利政策」(→`/rebate`=Rebate.vue) 即返利规则入口、无需改名；能力中心应保持纯 AI 能力定位，故还原我上一轮误加的「业务配置」Tab。改动：`ConnectCenter.vue` 删 Tab 按钮(`tab==='config'`) + 删对应 `<template v-if="tab==='config'">` 业务配置内容块（含返利规则 cc-card）。默认 tab 仍 `ref('connector')` 不变。build+rsync+chown 部署；新产物 ConnectCenter-G_44TuhW.js；生产 grep `业务配置`=0、`/rebate`(Shell) 入口仍在。结论：返利规则单一入口=侧边栏「目标与返利政策」，不冗余。未做真机验证。

- **Rebate.vue 移除冗余「+ 新建规则」工具栏按钮（用户确认"要"）**：维度卡片(品牌目标/商品目标)已覆盖全部两维度创建入口，工具栏「+ 新建规则」(openCreate 无参→默认 brand) 与「品牌目标」卡重复，故删除；「试算」保留(独立测算功能)。同步改空状态文案 `点击「新建规则」创建`→`点击上方「创建品牌目标 / 商品目标」新建`(否则空状态找不到入口)；模态框标题 `新建规则`(line86 新建时显示) 保留正确。openCreate 仍被两卡片引用，无 dangling。build+rsync+chown 部署；新产物 Rebate-cEfxYXFY.js；生产 grep `+ 新建规则`=0、空状态新文案=1、卡片入口在、试算在。未做真机验证。

- **小程序第三批文档（E2E 清单 + 灰度回滚方案）**：用户"要"继续做。两份纯文档交付于 `forecast-order-miniprogram-20260812T023419087Z/`：
  - `真机E2E测试清单.md`：E1-E12(P0 发版前必过：隐私拦截/登录/合法域名/主流程/我的提交详情+撤回/移动审批通过+驳回/汇总复制/登出清租户/埋点)、B1-B8(边界异常：空购拦截/期次空/断网草稿/幂等/角色门控/已审不可撤回等)、C1-C4(双端/低基础库/弱网/长明细)+执行记录表。
  - `灰度与回滚方案.md`：前端=上传→体验版→审核(附隐私指引+生鲜类目)→分阶段(1%/10%/50%/100% 每阶段≥24h)；前端回退=微信后台版本回退；后端=仅 rsync 2 文件+restart(已验证)，**数据兼容风险**：回滚旧版后端会把 `recalled` 单重计入汇总(旧过滤仅排除 rejected)，需回滚前 UPDATE 置回 pending 或接受短暂多计；监控指标表+回滚触发条件(成功率骤降>30%/新 crash/5xx/数据错乱)。
  - 无代码改动无需构建。⚠️ 全部代码改动(前两批 A3/E2/D1/埋点/我的提交/移动审批+后端 recall)仍须用户本人完成 A1/A2/B1(微信公众平台)后才真机生效。

- **小程序第四批：微信提审材料清单（用户"需要"）**：交付 `微信提审材料清单.md`。先做代码事实核查（grep 全部 wx.* API）：**仅基础 API，未调用任何位置/相册/手机号/蓝牙/通讯录等敏感接口**；关键发现=用了 `wx.setClipboardData`(复制汇总) → 隐私指引**必须勾选「剪切板」**；另 `wx.getPrivacySetting/requirePrivacyAuthorize` 仅在后台隐私指引生效后才触发系统弹窗，未生效时走自定义弹窗兜底(已写)。清单 10 节：①主体资质(企业/个体+食品证备用,忌个人主体) ②注册换 appid(`project.config.json:47`) ③request 合法域名 `https://hergent.cn` ④类目建议(首选「电商平台-商家自营」/备选「工具-效率」/谨慎「食品」需食品证) ⑤隐私指引逐项勾选表(仅剪切板+账号用途,附可复制用途文案) ⑥**测试账号=最关键(漏填90%驳回)**，须在生产建专用 mptest 销售账号+主管账号并绑有商品门店，审后删 ⑦功能页面路径 5 条 ⑧补充说明模板(可直接粘,含"企业内部工具/不涉及支付/不采集位置相册") ⑨3-5 张真实截图要点(忌 touristappid/test 数据/报错 toast) ⑩常见驳回应对表+11 项打钩清单+时间预期(主体1-3天/审核1-7天/灰度4天起)。
  - ⚠️ 名称简介**忌出现「蒙牛」等品牌词**(触发商标审核)；免责说明类目资质以平台规则为准。

- **侧边栏「目标与返利政策」→「目标与返利」（用户要求改名）**：改 `Shell.vue:41` 侧边栏文案 + 同步 `router/index.js:39` `meta.title`(否则侧边栏与页面标题不一致)。build+rsync+chown 部署；新产物 Shell-c700STe8.js；生产 grep `目标与返利`=1、`目标与返利政策` 残留仅 1 处=**`BrandArchive.vue:6` 副标题说明文字**「维护品牌主档 · 支撑品牌目标与返利政策 · 服务无 API 的舟谱类用户」（是描述性文案非导航项，未擅自改，待用户决定是否顺带统一）。未做真机验证。

- **小程序第五批：生产建提审测试账号 + 打通全链路（用户"要"）**：
  - **已建账号（生产，提审用，审后须禁用）**：销售 `mptest`/`Mptest@1`(user_id=5, role=sales, employee_code=9001, 绑门店 2220 永诺旗舰店)；主管 `mptestsp`/`Mpsup@1`(user_id=6, role=supervisor, employee_code=9002)。走 `erp_db.staff_account_create()`（生产机 source .env 后调，_hpw 哈希+自动绑 tenant_id=1）。禁用：`UPDATE users SET is_active=0 WHERE username IN ('mptest','mptestsp')` + restart。
  - **排掉 3 个真实阻塞**（否则审核员登录进去什么都做不了）：①`employee_stores` 须插**租户库 tenant_1.db**（`employee_stores_get` 走 `get_db()` 做 JOIN contacts，插主库无效→`/stores` 空）；②原两条期次 8-26/8-28 已过期→`/open-periods` 空，新建 id=9「9月提审期-开放填报」(8-30~9-15, 到货 9-18, 双写新旧列)；③**`_DEFAULT_PERMS` 根本没有 supervisor 角色**→supervisor 虽过审批门控却被 `_check_perm(data)` 拒，已在主库 `role_permissions` 插 `('supervisor','["data","dashboard"]')`+restart（ROLE_PERMS 导入期加载，须重启）。
  - **全链路 curl 实测通过**（服务端等价真机）：登录→`/stores`=[永诺旗舰店]→`/fill-search` 真实商品→`/open-periods`=[期次9]→提交(id=142, 自动关联 period_id=9)→主管 `/pending` 含 142→销售 `/my` 含 (142,pending)→`recall` 撤回成功→`/pending` 不含 142（测试数据已 recalled 清理，不进汇总）。
  - **⚠️ 发现全系统缺陷**：`_hpw(pw)=bcrypt(pw+SECRET)`，SECRET 为 64 hex → bcrypt 72 字节上限被挤占，**任何账号密码最长 8 字符**，超限直接 `ValueError` 500。已记入 MEMORY.md 踩坑铁律（正解=HMAC-SHA256 预哈希再 bcrypt，但需全量重置密码，未动）。
  - 同步更新 `微信提审材料清单.md` 6.1/6.3 节为真实账号密码+密码≤8 说明。

- **小程序第六批：可打印提审打钩清单（用户"需要"）**：新增 `提审打钩清单.html`（自包含 11.4KB，无外部依赖）。结构=阶段0主体资质(5项,瓶颈1-3天)→阶段1改appid(4项)→阶段2平台配置(7项,含域名/类目/隐私勾选)→阶段3上传体验版(3项)→阶段4提交审核(5项)→阶段5审后收尾(4项,含禁用测试账号+处理期次9+灰度)，共 **28 项**；每项标「我」(须本人操作)/「AI」(已完成仅供确认)。功能：顶部速查卡(测试账号/域名/appid位置/类目/隐私勾选 + 密码≤8 警示)、勾选状态 `localStorage` 持久化(key=`hergent_mp_checklist_v1`)、进度条百分比、`window.print()` 按钮 + `@media print` 隐藏交互元素且 `break-inside:avoid` 防分页断裂。校验：li 开闭 28/28、checkbox 28 个、data-k 无重复、打印样式与持久化均在。
  - ⚠️ HTML 内含生产测试账号密码明文，属本地文档勿外传。

- **小程序第七批：用户发来证照 · 类目策略调整（用户发图 · 我看错被用户更正）**：
  - **正确证照**：**湖北省小赫智体数字科技有限公司**，统一社会信用代码 `91420606MAKF1YPG5Y`（⚠️ 2026-09-06 用户更正；本行原记 `91420600MAK1YFG5Y` 为误读，作废），**注册资本 50 万**（非 5 万），**法定代表人张俊峰（用户本人）**，类型=有限责任公司（**自然人独资**），成立 2026-05-26，住所=湖北省襄阳市樊城区王寨街道松鹤路社区创业路5号（原新华玻璃厂院内）。
  - **经营范围（一般项目）**：人工智能应用软件开发、人工智能基础软件开发、人工智能理论与算法软件开发、计算机系统服务、数据处理与存储支持服务、软件开发、软件销售、信息系统咨询服务、市场营销策划、咨询代理、广告制作、互联网销售（除销售需要许可的商品）、网络技术服务、数字内容制作服务（不含出版发行）。**无食品/乳制品**。
  - **⚠️ 我的判断错误（已承认）**：图被旋转 180 度发了，我按镜像读错法人为「朱X」/代码误为 `91420600MA49YFC03Y`/注册资本 5 万。**正确法人=张俊峰自己 → 主体验证可走法人微信扫码（无需拉他人），1-3 天最快**。
  - **策略调整（已更新文档）**：首选类目**改为「工具→效率」**（理由同上次：与"软件开发/人工智能应用软件开发"经营范围天然契合；不需食品证；与「企业内部订货工具」定位清晰匹配；避免商家自营对接支付/订单体系复杂度）。备选仍保留「电商平台→商家自营」。已同步更新 `微信提审材料清单.md` 第3节 + `提审打钩清单.html` 速查表的「建议类目」行。
  - **新发现利好（用户证照给到的）**：①自然人独资=决策链短（所有主体相关操作一人说了算）②经营范围含 AI 开发项=小程序**名称/简介里用 "AI" 词有证照背书**（与 hergent.cn AI 副驾定位契合，建议名称如「小赫 AI 订货助手」/「小赫智体 AI 预报」）③法人本人=主体验证最快径。

- **【重要】v109 达成数据模块上线（补"无 API 客户达成数据无入口"缺口，用户选方案 A）**：
  - **缺口诊断**：此前达成数据仅 3 条路径且对手动上传客户全不通——①`kpi_achievement_calc`(erp_db:12792) 从库内 `sale_orders` 汇总当月 delivered/signed，**但前端无销售单导入入口**且只能算全公司总额、不能按品牌/商品拆；②Rebate「试算」是 what-if 计算器**不入库**；③Forecast 冲刺看板用**本期预报**当"达成"（语义错位，漏算已达成→缺口偏大）。后端自标缺口：`forecast_audit.py:151` `# v1: 单品指标维度待录入`、`:16`「单品级厂家指标当前库无现成表」。结论=返利目标体系对主打客群(舟谱类无 API 经销商)此前立不住。
  - **后端**：新表 `rebate_achievements`(period_month/dimension/scope_key/scope_name/actual_amount/actual_qty/source/note + **UNIQUE(period_month,dimension,scope_key)** 支撑同月重导覆盖而非堆积)。建表双保险=主库 `_safe_migrate_script('v109_...')` + 租户补丁循环 `tdb.executescript`（严格照 v108 rebate_target_rules 模式）。新 router `server/routers/rebate_achievements.py`：GET 列表 / POST upsert / DELETE / `POST /import`(openpyxl+csv，中文表头别名映射，商品维度按名称或条码解析成 product_id 以匹配 rule.scope_key，单行失败不影响其余行)。注册于 server.py:709 import + :799 include_router。**模板下载走前端客户端生成 CSV**（与 DataFill/EmployeeArchive 一致），故未保留后端 /template 端点、避免死代码。
  - **前端 Rebate.vue**：加主 Tab「目标规则 / 达成填报」；填报 Tab = 月份选择 + 可编辑表格(达成金额/数量，@change 即存) + Excel 导入弹窗 + 模板下载 + 达成率着色(≥100% 绿 / ≥80% 琥珀)。行数据 = **启用中目标规则 ∪ 已填报达成**（保留无规则的达成行，防"导入了却看不见"）。
  - **前端 Forecast.vue**：冲刺看板改为 `达成 = 填报达成 + 本期预报贡献`（`reported + contrib`），新增「已达成(填报)」列；副标题由「历史已达成未含…」改为「达成 = 已填报达成 + 本期预报贡献…」；空态引导由已撤掉的「能力中心 → 返利目标规则」改为「目标与返利」。onMounted 增 `loadRebateAchievements()`。
  - **部署（关键经验）**：`server/` 下有 20 个改动文件含用户**暂缓推送**内容，**禁止整目录 deploy.sh**。先 scp 生产版 erp_db.py/server.py 到本地 diff，确认差异**仅**是本次新增（erp_db 46 行 v109×2、server.py 2 行注册）→ 说明生产已含用户其他改动 → 外科手术 rsync 仅 3 文件 + chown + restart。结果：health 200、两个端点 401(鉴权生效)、**表在主库 + 全部 10 个租户库均已创建**。⚠️ 坑：restart 后 `sleep 3` 不够(启动约 4s)导致首次 curl 返回 000，是**误报**不是故障，复查即 200。前端 build+rsync 部署；产物 Rebate-C5B1z1W6.js / Forecast-DSq7zI5_.js，grep 新文案全部命中。⚠️未做浏览器真机验证。

- **旧名「目标与返利政策」全站清零（用户"统一"）**：`BrandArchive.vue:6` 副标题 `支撑品牌目标与返利政策`→`支撑品牌目标与返利`（此前唯一残留，属描述性文案非导航项）。build+rsync+chown 部署；生产 `cat *-*.js | grep -o '目标与返利政策'`=0、`支撑品牌目标与返利`=1。全站术语现已统一为「目标与返利」。

- **【踩坑·重要】v109 接口上线后 403 —— 新路由必须登记 RBAC 路径→模块映射**：
  - 现象：前端调 `/api/rebate-achievements?month=2026-08` 返回 **403**（非 401）。
  - 根因：`server.py` 有两层拦截，新路由漏登记第二层。①租户中间件 `tenant_middleware`(server.py:79) — 无 tenant 上下文才 403；②**RBAC 中间件(server.py:596-648) `_PATH_MODULE_MAP` 按前缀匹配路径→模块，未命中则 fail-closed 直接 403 `MODULE_NOT_CONFIGURED`**(server.py:639)。`_PATH_MODULE_MAP` 共 292 条，`/api/rebate-rules` 登记为 `"stock"`(server.py:347)，新接口没登记 → 403。
  - 坑中坑：**未登录时 RBAC 先返回 401（server.py:630 在 module 判定之前）**，所以我先前用假 token 验到 401 就以为鉴权 OK，**401 无法区分"已登记"与"未登记"**——必须带真实登录态才测得出来。
  - 修复：`_PATH_MODULE_MAP` 增 `"/api/rebate-achievements": "stock"`（与 rebate-rules 同模块，遵循"能管目标的人就能填达成"）。部署前 diff 生产版确认差异仅此 2 行 → 外科手术 rsync server.py + restart；health 200、`grep` 生产已落地。用 AST 解析本地 server.py 复现前缀匹配，确认 `/api/rebate-achievements` 与 `/import` 均→stock 且无更早前缀遮蔽。
  - ⚠️ **权限边界（须告知用户）**：`core.py:237 _DEFAULT_PERMS` — `admin=["*"]`、`boss` 含 `stock`、**`accountant`(会计/文员) 只有 dashboard/accounts/reports/marketing，无 stock** → 文员若用 accountant 角色访问「达成填报」会被 403「不能访问 stock 模块」。这是沿用 rebate-rules 的既有口径（一致），若确需文员填报，须由 admin 在 `role_permissions` 给 accountant 加 `stock`（需重启，ROLE_PERMS 导入期加载）。
  - 另：用户贴的 `content.js` 报错（DISABLE_FEATURES/ENABLE_FEATURES/COMPUTE_PAGE_FINGERPRINT、`Identifier 'n' has already been declared`）均为**浏览器插件内容脚本**产生，与本项目无关，勿排查。

- **小程序第八批：分角色 UI 展示 + 修掉 tabBar 权限缺陷（用户问"不同角色界面不一样吧"）**：
  - **⚠️ 真缺陷（提审风险）**：微信 `tabBar` 在 `app.json` 静态配置、**无法按角色动态隐藏** → 原 3 个 tab（填报/我的提交/**汇总总表**）销售也能看见，但后端 `GET /summary` 门控 `role in (admin,boss,accountant,supervisor)` → **销售点进去直接 403「仅管理员/老板可看汇总」**。审核员用销售账号一点就见报错，很可能被驳回。
  - **修复**：①`app.json` tabBar 移除 summary（留 填报/我的提交 两个，微信最少 2 个合法）；②`mine.wxml` 在审批入口后加「汇总总表」入口卡（与审批入口**同一套门控** `role in (admin,boss,accountant,supervisor)`）；③`mine.js` 加 `goSummary()`。summary 仍留在 pages 列表（可 navigateTo 访问）。
  - **校验**：mine.js `node --check` OK；app.json 合法；tabBar 2 项；summary 仍在 pages；wxml 两入口门控角色各 2 次完全一致（admin/accountant/boss/supervisor）。
  - **角色可见性矩阵（回代码确认，双层门控=入口 wx:if 隐藏 + approval.js onLoad 二次校验）**：sales→仅 填报/我的提交（可撤回本人 pending），**无审批无汇总**；supervisor/admin/boss/accountant→多「审批预报单」+「汇总总表」两个入口。提交端点无角色门控（`role=u.role or 'staff'`），recall 限本人或审批角色。
  - 用 show_widget 内联展示销售/主管两套界面对比（含"修复前销售见 403 报错"的虚线屏）。

- **权限管理页上线（用户"做一个权限入口，让用户自己给文员配权限"）**：
  - **先查后做，避免重复造**：后端权限接口**早已齐全**——`GET/POST /api/role-permissions`(模块级 list 格式)、`GET/POST /api/role-permissions/detail`(CRUD dict 格式)、`DELETE /api/role-permissions/{role}`、`GET /api/users`、`PUT /api/users/{uid}/role`，且保存后调 `core.reload_perms()` **热生效无需重启**。**Settings.vue 里也已有角色×模块矩阵**（能用，但埋在设置页底部、且只能配角色不能给人分角色）。
  - **真缺口**：「**用户 → 角色**」分配**没有界面**（后端有 `PUT /api/users/{uid}/role` 但前端无人调用）→ 能配"财务能干什么"，却没法把"张三设成财务"。
  - **产出**：新建 `pages/PermissionManage.vue`（路由 `/permissions`，侧边栏+移动抽屉均加「权限管理」入口，在「设置」上方）。两区块：①**用户与角色**——列出用户(GET /api/users)，下拉改角色(PUT /api/users/{uid}/role，乐观更新+失败回滚，标"我")；②**角色权限矩阵**——从 Settings 迁入并沿用 **list 格式**（`_check_perm` 对 list 视为"全操作授权"，比 CRUD dict 更不易误配成只读导致写不了）。
  - **防自锁设计（重要）**：只有 admin/boss 能进本页（`/api/role-permissions`→hr、`/api/users`→data，其余角色被 RBAC 403 挡在外面），故**锁死 admin+boss 的 `hr`/`data` 两个模块为必选且禁勾选**，并加载时强制补回，杜绝"老板把自己锁在权限页外"。admin 角色整条不可编辑（`LOCKED_ROLES`）。
  - **去重**：Settings.vue 的角色权限区块改为一句引导「去权限管理」按钮，并清掉 permRoles/modules/loadPerms/savePerms/resetRole 等死代码（含 onMounted 里的 `loadPerms()`，否则运行时报未定义）。
  - 部署：build+rsync+chown；产物 PermissionManage-B9EE458R.js / Settings-C-SfGB-W.js / Shell-B-TxKc49.js；生产 grep `权限管理`/`用户与角色`/`保存角色权限` 命中、Settings `保存权限配置`=0（旧矩阵已移除）、Shell `/permissions`=2（桌面+移动）。⚠️未做浏览器真机验证。

- **【仅分析不改码】Web 端变更 → 小程序端同步分析报告**：交付 `web-miniprogram-sync-analysis.md`。方法=两仓库 git 事实(erp 20 提交+43 未提交文件 / cn-v2 8 提交+214 未提交文件) → 提取小程序实际调用的 11 个接口 → 逐项比对。
  - **核心结论：适配度良好**。期次规则(A+B：传 period_id 校验/不传后端自动取/取不到硬报错，order_date 服务端推导)、B1 门店越权、B4 幂等、B2 明细清洗、`tenant_id` 入登录响应(v108-fix)、M7 `/products/fill-search` —— **小程序均已覆盖**（fill.js:220 传 period_id 且不传 order_date，符合新规则）。
  - **必须同步仅 1 项（R1，高）**：新增 `recalled` 状态后 **4 处 status 过滤只有 2 处排除撤回单** —— `erp_db.py:12579`(期次聚合 `_agg` 的 reporter_count/total_qty) 与 `:13592`(`all_units` 门店名册) 仍为 `s.status != 'rejected'`。后果=销售在小程序撤回后，Web 端往期预报看板"已报件数/参与门店"不减少。建议抽常量 `_EXCLUDED_SUBMISSION_STATUS`。
  - **最大隐性风险 R2（中）**：Web 端新增「角色权限配置页」(`f8e3f96`) 让老板可改 `role_permissions`，而小程序审批/汇总依赖的 **supervisor `data` 权限是我上一轮手工插库补的**（`_DEFAULT_PERMS` 无 supervisor）→ 老板在 Web 端一取消勾选，小程序即 403，且 `ROLE_PERMS` 导入期加载**需重启才生效，故障滞后难排查**。治本=把 supervisor 写进 `_DEFAULT_PERMS`。
  - **建议同步**：`/summary` 新增 start/end 区间但小程序只传单日 date；B4 幂等下"覆盖更新/已通过禁止覆盖"文案未区分；403 未友好化(门店未分配/模块无权限)。
  - **可暂缓**：`/remind` 催单、`/save-matrix` 定稿、`forecast_col_schemes` 列方案(Web 端 `forecast.py:107/121`)、演示/注册入口 —— 均属 Web 端定位，小程序作填报端保持精简。
  - **R3 接口不兼容风险=低**：签名变更均为新增可选参数(`order_date=""`/`start_date/end_date=""`)，向后兼容。**R5 演示模式已确认安全**：`/api/auth/demo-login` 返回独立 demo 租户(`subdomain='demo'`)token，与小程序 tenant_id=1 租户隔离。
  - **待用户补充 4 项**：①首版是否要移动端定稿 ②汇总页是否要区间查询 ③真实审批角色是哪个(督导/老板/文员)以确定 supervisor 定位 ④Web 端预报列方案是否已上线 UI。

- **【新增战线·自媒体】交付 `Hergent-自媒体实施计划-2026-08-30.md`**（用户要"基于个人情况的自媒体实施计划"，需含定位/平台/内容/频率/冷启动/增长路径/阶段目标）。
  - **用户三个前置答案（AskUserQuestion 确认，直接影响方案）**：①内容形式=**混合：出镜口播为主＋录屏补充**；②可投入=**不确定，先最低成本试**（故按每周 2–3h 上限设计）；③目标受众=**其他快消/低温奶经销商**（B 端，为卖 Hergent 产品，非泛粉）。
  - **战略定位（关键认知）**：自媒体是**需求验证的杠杆**——把"一人公司挨个拜访经销商"变成"经销商主动来找你"。用户内容讲的痛点（临期/返利/报单/回款）**恰好就是 Hergent 要解决的问题** → 每条内容＝一次产品验证，评论区＝免费需求调研。内容⇄产品飞轮：讲痛点→收集反馈→Hergent 迭代→做成新素材→吸引更多同行。
  - **用户独有优势**：技术人做不出（不懂低温奶效期/蒙牛返利档/报单拍脑袋），普通经销商做不出（不会用 AI 造工具）→ **痛点×解法的交叉点**。
  - **平台选择：视频号为主（非抖音）**。理由=经销商活在微信生态；视频号可一键转发经销商群/朋友圈（抖音做不到）；社交推荐沿"同行→同行好友"精准裂变；直连公众号/企微/小程序，**转化路径最短**。公众号承接长文，抖音阶段 2 起零成本搬运（不单独做）。小红书/知乎/B站不做（受众错配）。
  - **人设三原则**：说人话（不谈赋能/抓手）、只讲自己真干过的、前 3 个月不卖货。**账号昵称避免直接用 Hergent**（产品名当账号名=广告号，降低关注意愿），建议真名或"老张的低温奶生意"。
  - **内容三大支柱**：①一线经销商干货 60%（口播，建立"懂行"信任）②AI 实战演示 30%（录屏，天然植入 Hergent）③创业真实记录 10%（情感共鸣）。铁律=**讲"我解决了什么生意问题"，不讲"AI 有多厉害"**。已列 **20 条可直接开拍的选题库**（含脱敏红线：返利率/进货价/客户名/区域销量一律脱敏，不评价厂家政策）。
  - **生产流程（对应每周 2–3h 约束）**：**月度内容日 3h**（批量拍 4 条口播＋录 2 条录屏＋粗剪）＋**每周 40 分钟**（精修发布 30min＋**评论区互动 10min**）。月均 ~5.7h（周均 1.4h），留余量。工具=手机三脚架+剪映提词器/剪辑+QuickTime 录屏。一鱼多吃：口播→音频转公众号稿/截图金句/抖音搬运/群分享。
  - **节奏**：阶段 0 基建(8/31–9/13，**不发**，试拍 2 条)→阶段 1 冷启动(9/14–11/8，**每周 1 条**×8)→阶段 2 深耕(11/9–1/3，每周 2 条＋公众号＋建经销商群)→阶段 3 转化(1/4–2/28，筛 5–10 家付费试用)。**从每周 1 条起步**：可持续性>>强度。
  - **北极星指标=`加微信的精准经销商人数`**。明确**不盯**播放量/点赞/粉丝数（B 端精准>泛流量，1 个经销商客户>>1000 泛粉）。阶段目标：①20–30 人加微 ②经销商群 80–150 人＋10 场深度访谈 ③5–10 家付费试用。
  - **冷启动关键**：前 50 播放全靠自己转发（视频号推荐逻辑=朋友点赞→朋友的朋友看到）；**每周那 10 分钟评论区互动是 ROI 最高的动作**（主动私信聊得来的同行）；蹭行业节点（月底冲量/旺季备货/年底回款）而非社会热点；访谈时**问痛点不问需求**（问"上次临期亏钱怎么处理的"，不问"你想要什么功能"）。
  - **10 条避坑**：追求播放量/讲 AI 概念/断更/过早推销/泄露商业机密/**AI 味文案**（AI 可改稿不能替说话）/闷头做/当主业/每条想爆/忽略私信。
  - **待用户拍板 2 项**：①账号昵称（真名 vs 品牌化）②第一条拍哪条（建议 #2「明明卖得多为什么没赚到钱」或 #6「月底冲返利档该不该压货」——最戳钱袋子且对应 Hergent 返利测算，可一石二鸟做需求验证）。后续可交付：前 4 条口播稿全文 / 经销商深度访谈提纲。

- **设置模块重构 + 权限内聚（用户 5 点要求：撤侧边栏入口、分组重构、对齐 Forecast 视觉、不改权限数据结构、校验路由无死链）**：
  - **收敛**：删除 `pages/PermissionManage.vue` 与 `/permissions` 路由，移除侧边栏+移动抽屉两处入口；权限功能全部搬进 `/settings`。删了才几天、无外部书签，直接删而非留重定向（避免死代码）。
  - **重构 Settings.vue 为 4 个 module-tabs**（沿用 Forecast 的 `.module-tabs`）：①**账号与组织**（当前账号/主题）②**权限**（成员授权 + 角色权限矩阵）③**AI 配置**（Hermes 连接 + AI 记忆）④**数据与系统**（库存效期补录）。原 Hermes/记忆/主题/数据维护逻辑**原样保留**，仅重排归属。
  - **视觉对齐 Forecast**：顶部 `.card.toolbar` + `.tb-group`/`.tb-right` + `.tb-search`(Icon+input)；表格用全局 `table.tbl`(th/td/.num/hover)；`.state-empty` 空态 + `.skel-line` 加载态；`.pager`/`.pager-info` 分页(成员表 10 条/页，搜索变化重置页码)；角色权限由"每角色一块"改为**模块×角色矩阵表**（行=模块、列=角色、格=勾选、列头带「恢复默认」）。配色全用 `var(--p/--t1/--t2/--t3/--bd/--bg2/--bg3/--suc/--dan)`。
  - **权限数据与校验逻辑未动**：仍走 `/api/role-permissions` **list 格式**（`_check_perm` 对 list 视为全操作授权），保存后后端 `reload_perms()` 热生效。保留防自锁：`PROTECTED_ROLES=['admin','boss']` × `CRITICAL_MODULES=['hr','data']` 锁定必选 + 加载时强制补回；`LOCKED_ROLES=['admin']` 整列不可编辑。
  - **⚠️ 「通知」分组未建（主动决定，须告知用户）**：后端 `notification_prefs.prefs` 仅一个**不透明字符串、无 schema、无任何消费方**（`erp_db.py:2789/2794`），前端无从渲染；渠道配置(企微/飞书/钉钉)在「能力中心→连接器」，搬进设置会与能力中心职责冲突且制造第二个入口。**不为它编造假界面**——若要做需先定义 schema + 后端消费方。
  - **校验（全绿）**：全库 `grep PermissionManage|/permissions` 仅剩 API 路径；生产 `Shell-*.js` `/permissions`=0、全站 `"/permissions"`=0、`PermissionManage-*.js`=0；`/settings` 路由在；侧边栏「设置」保留。产物 Settings-DdNCrnMY.js，四个标签页与「成员授权/角色权限/保存权限/恢复默认/库存效期补录」均命中。
  - **关于面包屑**：全库 `grep breadcrumb|面包屑` **无任何结果 —— 本系统当前不存在面包屑组件**，故无面包屑可校验；返回路径由侧边栏「设置」承担，已确认正常。⚠️未做浏览器真机验证。

- **【自媒体计划 v2】基于用户三点反馈补充（文档 `Hergent-自媒体实施计划-2026-08-30.md` 追加第 13–16 节）**：
  - **用户反馈**：①已有视频号**「老张ai实践记」**；②希望"能拍视频把 Hergent 卖出去当然好，但能涨粉赚广告分成当然好"（双目标）；③担心"只拍生意/AI 方面的内容，将来很容易无题可拍"。
  - **①账号名结论=保留不改**。「老张ai实践记」优点：人格化(老张)、"实践记"≠"教程"(容错高，不必每期正确)、**题材不设限**（叫"低温奶生意"就只能拍生意，叫"AI实践记"任何 AI 实践都能拍）。→ 天然兼容双目标：外层"AI实践"拉泛粉+拿分成，内核"低温奶生意"筛经销商卖产品。**只改简介**补身份锚点：`蒙牛低温奶经销商｜不懂代码，用AI给自己造经营工具｜讲生意里的坑，也讲我怎么用AI填`。⚠️切勿改名成 Hergent（产品名当账号名=广告号，关注意愿掉一半）。
  - **②广告分成=顺带的糖不是饭（已查证真实数据）**：视频号创作分成门槛=100 有效粉+实名+10 条原创(**≥1分钟**)+无违规；**万播收益 10–20 元常见；垂直优质内容(财经/法律/教育/职场/商业)20–100 元，是泛娱乐(0.3–1 元/千播)的 3–5 倍**；冷启动需 30–60 条；**评论区本身是广告展示位**（有评论更易展示广告→他原本"每周10分钟回评论"的打法直接提升分成，一石三鸟：拿分成+筛客户+收需求）；真人出镜审核更宽松；AI 低质内容被打击。推算：起步月分成约 16 元，半年后约 80 元，一年后 500–800 元。**关键对比：1 个付费客户(年费3000)≈150 万播放的分成**。→ 结论：**别为分成追泛流量**；垂直内容广告单价反而更高，两个目标在这点上一致。
  - **②解法=双轨内容策略**：A 轨泛 AI 实践 30%(拉流量/拿分成，受众=想用AI的中小老板) + B 轨生意干货 70%(筛客户/卖产品，受众=同行经销商)。**核心技巧=一鱼两吃**：同一素材换标题即两条（如"AI自动盯临期"→A轨"不懂代码，我让AI每天自动提醒我"／B轨"低温奶最怕的四个字：明天到期"）。B 轨占 70% 因产品才是真钱、且不可替代性在 B 轨。
  - **③内容枯竭=结构性可解（核心认知：题材不是"想"出来的，是从日常"捞"出来的；只要生意在转就永远有）**：
    - **源头1·每周5分钟选题打捞（5个固定问题）**：①这周哪儿亏钱了 ②这周哪儿浪费时间了 ③这周谁问我问题了(员工/同行/厂家) ④这周我用AI干了什么 ⑤评论区/私信有人问了什么 → 一次捞 2–5 条。
    - **源头2·一个母题拆4条（内容乘法）**：每个痛点 ×【①痛(坑有多痛) ②法(我怎么填) ③器(用什么工具/演示) ④事(真实故事/翻车)】。
    - **源头3·观众=选题库（真正永动机）**：评论/私信提问直接进"选题池"，**观众问得越多题材越多，唯一越做越轻松的内容模式**。
    - **产能账（消除焦虑的关键数字）**：生意痛点母题 ~20 个 × 4 拍法 = 80 条 + AI 泛题材 20 + 创业记录 10 + 观众提问∞ = **打底 110 条 → 每周1条够拍2年／每周2条够拍1年**。
    - 内容四条命（一鱼多吃：口播→公众号稿/朋友圈金句/抖音搬运/群分享）+ 老内容翻新(3个月前爆款换角度重讲) + 三习惯(囤10条库存/不追完美/允许水)。
  - **新增注意事项**：分成要求原创+≥1 分钟，口播稿 300–400 字≈1.5–2 分钟**达标**。✅AI 可改稿理结构；❌AI 直接生成整条口播照念=易判低质+观众听出AI味（老板尤其敏感）。**核心（经历/数字/判断）必须是你自己的**。

- **【架构分析·不改码】员工档案 / 账号与组织 / 权限 三模块整合分析**：交付 `员工-账号-权限三模块整合分析.md`。方法=先查代码 + 生产真实数据（非经验推断）。
  - **决定性事实：跨库隔离** —— `users` 在**主库 erp.db**（跨租户共享）；`hr_employees`/`departments`/`role_permissions` 读写在**租户库 tenant_N.db**。→ **数据库层不可能建外键，一致性只能靠应用层**；也决定不能物理合并（账号下放租户库会死循环：登录需先知道租户、知租户需先登录）。跨库访问既有写法=`from core import _master_db; conn=_master_db()`（见 erp_db.py:4898）。
  - **生产实测（tenant_1）**：7 员工（department_id 全 0）/ 5 账号 / 4 部门**完全未使用**。关联字段=`users.employee_code`↔`hr_employees.id`（`staff_account_create` 写 `employee_code=str(employee_id)`），**但仅有的两个值 9001/9002 是小程序提审测试账号编码，不对应任何员工 id(1-7) → 实际有效关联=0 条**。「张俊峰」两边都有（员工 id=7 / 账号 id=2 boss）却未关联，是冗余实证。
  - **字段语义错位（坑）**：`users.employee_code` 存的是**员工 id**，而 `hr_employees.employee_no`(EMP001) 才是真工号 → 名不副实。另有 `users.department_id`（迁移 v4 加）**从未被任何查询使用=死列**。
  - **🔥 顺带查出 P0 缺陷：`role_permissions` 主/租户分裂**。生产实测 erp.db 有 `supervisor→["data","dashboard"]`、tenant_1.db 有 `库管→["dashboard","stock","data"]`、tenant_tenant_1.db 空。建表走 `_safe_migrate`(主库) 但读写走 `get_db()`(租户上下文) → **权限页保存写租户库、`reload_perms()` 在请求上下文读租户库 → 主库那份 supervisor 丢失** → 小程序主管审批所依赖的 supervisor 权限，在老板保存过一次权限后即失效（直到重启）。与已知的「supervisor 是手工插库补的、`_DEFAULT_PERMS` 没有」是同一雷，现已实锤。
  - **离职联动当前不满足（用户约束①）**：`EmployeeArchive.vue` 的 `employeeToggle` → `employeeApi.toggle` → `server.py:4530 UPDATE hr_employees SET is_active=?`，**只改员工表、完全不碰 users.is_active** → 停用员工后账号仍可登录（安全漏洞）；权限回收同样没有。
  - **重要：员工档案页早已是「员工+账号」融合视图**（列含`小程序账号`，行操作含`开账号`/`门店`/`停用`，`saveAccount()` 调 `staffAccountApi.createAccount({employee_id,...})`）→ 我刚加的「设置→权限→成员授权」是**第二个账号入口**，构成冗余。
  - **结论=不物理合并，做逻辑整合+入口收敛**。SSOT：`hr_employees`=人（姓名/工号/岗位/部门/在职状态/薪资社保）；`users`=认证（用户名/密码/MFA）；`role_permissions`=角色能力（须统一到租户库）。职责：员工档案管"人"、权限页只管"角色×模块矩阵"、账号与组织管"部门树+当前账号+主题"。
  - **IA 调整**：M1 设置→权限移除成员授权（归位员工档案）／M2 员工档案加账号状态列（暴露"员工停用但账号在用"异常）／M3 启用闲置部门树／M4 停用弹窗加"同时停用账号并回收权限"联动选项（默认勾选）。
  - **分期**：P0(1-2天，止血)=离职联动+权限表收敛租户库+supervisor 写入 `_DEFAULT_PERMS`+补 `users.employee_id` 整型列；P1=入口收敛+员工列表 `account_status`+数据清洗配对回填；P2=部门树启用+姓名单向同步+离职回收门店/报单映射；P3(不建议)=账号下放租户库(🔴须重做登录)。
  - 复杂度标注：🟢<0.5天单文件／🟡1-2天前后端联动／🔴3天+跨库或数据结构变更。最小可交付=P0-1+P0-2（约1-2天）即可满足"离职自动停用账号并回收权限"并修掉权限表分裂。

- **【追问补充+已修复】报单配置是否也要整合（用户"要"）**：`员工-账号-权限三模块整合分析.md` 新增第八节。
  - **报单配置是什么**：`report_mapping`(租户库) = `employee_id` + `counterparty_type(门店/客户/自有仓)` + `counterparty_id` + `system_name` + `report_alias(列头简称)` + `order_template(单型)` + `src_wh/dst_wh`。即"哪个员工负责哪个门店、单怎么报"。`report_mapping_health()` 用 `hr_employees WHERE is_active=1` 比对输出 `unassigned_employees` → 设计上就是**员工的业务延伸属性**。
  - **结论=不并入权限页，维度正交**：权限=功能权限(角色能进哪些菜单)；报单配置=数据范围+业务规则(对哪些门店、怎么报)。经典 RBAC 两个正交轴，合并会让"权限"语义膨胀、低频权限页被高频报单配置淹没。且**它早已整合过一次**——`Forecast.vue:1421` 注释「原档案管理独立页，整合为标签页」，方向对（配置跟着使用场景走）。
  - **✅ 已修复：孤儿路由 `/archive/mappings`**。搬走时路由没删；实测侧边栏无入口、`Archive.vue` 无入口、Forecast 直接 `<ReportMapping />` 嵌组件**不走路由** → 全库 `grep archive/mappings` 零引用，只能手输 URL 打开且显示重复页面头。**这正是上一轮"移除入口后不得有失效链接"我漏查的一处**。已删路由 + 清理随之失效的 `const ReportMapping` 路由 import。生产核对：`archive/mappings`=0、`报单配置`=3、组件仍随 Forecast 打包(`报单简称`=2) → 功能完好、死链消除。复杂度 🟢。
  - **待处理：`employee_stores` 与 `report_mapping` 概念重叠**。两者都表达"员工→门店"：`employee_stores` 仅 2 列纯授权，生产**只有 1 行且是测试数据**(employee_id=9001，又是提审测试账号编码)；`report_mapping` 11 列含业务规则，2 行其中 1 行 `employee_id=NULL`(self_warehouse 仓到仓调拨本就不绑人)。→ 同一件事两处维护，这才是真该收敛的重叠（优先级高于"并进权限页"），列为 **P1-4**。
  - **⚠️ 关键语义差异：权限要回收，报单要交接**。离职联动**不能**顺带停用其报单配置——否则其负责的门店**没人报单、业务中断**。正确做法=停用时提示"该员工还有 N 个门店/客户的报单配置，请转交给：[下拉选人]"，批量改 `report_mapping.employee_id` 指向接任者。列为 **P0-4**。

- **【自媒体交付】口播稿第 1–4 条 → `老张AI实践记-口播稿-第1-4条-20260830.md`**（用户"好"确认后执行，我承诺的"写前4条口播稿全文"）：
  - **配比**：B轨生意干货 3 条 + A轨泛AI 1 条（呼应双轨 70/30），母题互不重复。每条 350–400 字 ≈ **1分40秒–2分钟，满足视频号分成「≥1分钟」硬要求**。
  - **4 条选题**：①**月底冲返利档，到底该不该压货？**（B轨·返利，钩子★★★★★，*建议卡月底前5–7天发蹭行业节点*）②**低温奶最怕的四个字：明天到期**（B轨·临期损耗，行业共鸣最强最易转发）③**报单靠经验？一个月多亏两万**（B轨·报单流程，对应 Hergent 核心能力）④**我不懂代码，却给自己造了个"AI伙计"**（**A轨**·拉泛粉拿分成，用生意做案例，结尾"你最烦哪件事"=选题永动机开关）。
  - **每条结构固定**：前3秒抛痛点（**禁止自我介绍**）→ 真实经历 → 方法/数字 → 金句 → **结尾提问引导评论**（评论区=广告位=分成+客户+选题，一石三鸟）→ 置顶自问自答引评（**自曝糗事让同行敢接话**）。
  - **稿件带实操标记**：`／`停顿半秒、`【慢】`加重、`【手势】`；含拍摄提示表、B-roll 建议（第4条建议切录屏，Mac ⌘⇧5 录 30 秒）、封面文案、⚠️脱敏检查项。
  - **拍摄前3件事**：机位略高于眼/看镜头不看屏幕、背景要仓库或店里有货不白墙、前3秒一口气说完别铺垫。
  - **拍完自检4问**：前3秒是否抛痛点／是否有具体数字场景（"两万块""一板货"比"很多损失"强10倍）／结尾是否引导评论／听起来像唠嗑还是念稿（念稿重拍）。
  - **金句（易被截图传播）**：①"为了返利压货，最多压到刚好够卖到下一批到货的量，多一箱都不进" ②"损耗不是靠节约省出来的，是靠提前知道省出来的" ③"报单不是要人更聪明，是要把该看的数摆到眼前" ④"**别问 AI 能干嘛，要问我最烦哪件事**"。
  - **下一步候选**：第 5–8 条（客户欠款催款话术／司机提成怎么定／一个低温奶经销商的一天）或**经销商深度访谈提纲**（10问，阶段2用）。

- **【P0 全套 + M1 已上线】员工-账号-权限整合实施（用户"全做"）**：报告新增第九节实施记录。
  - **后端 3 文件（外科手术式，diff 确认未夹带用户暂缓推送内容）**：
    - **P0-3**：`erp_db.init_db()` 加 `_safe_migrate('v110_users_employee_id','ALTER TABLE users ADD COLUMN employee_id INTEGER DEFAULT 0')` + **一次性回填**（`_migrations` 标记 `v110_users_employee_id_backfill` 防重跑，把历史 `employee_code` 数字串 CAST 进 `employee_id`）。坑：**`logger` 只是 `_ensure_tenant_module_tables()` 内的局部变量(line352)，`init_db()` 里没有** → 我最初写的 `logger.info` 会 NameError，已改用 `_safe_migrate` 同款 `import sys; print(...,file=sys.stderr)`。生产：`employee_id` 列已建，9001/9002 回填成功、真实员工(空 code)保持 0。
    - **P0-2**：①租户补丁循环补 `role_permissions` 建表（原先只在主库 `_safe_migrate` 建）②`core._DEFAULT_PERMS` 新增 `"supervisor": ["dashboard","data"]`（不再依赖手工插库）③加主库→各租户库一次性迁移（`INSERT OR IGNORE`，不覆盖租户自有配置）。生产：全部 11 库均有表、supervisor 已迁入、`tenant_1` 的「库管」未被覆盖。
    - **P0-1**：**修正此前判断**——联动代码其实一直存在（`UPDATE users ... WHERE employee_code=?`），但被 `except: pass` 静默吞掉，且只按 `employee_code` 匹配，而该列对真实员工为空（仅测试账号 9001/9002 有值）→ **从未真正生效**。改为优先 `WHERE IFNULL(employee_id,0)=? OR employee_code=?`、无 `employee_id` 列时回退纯 `employee_code`、异常打印告警、返回 `accounts_affected`。生产实测：停用/启用各影响 1 个账号，已还原。
    - **P0-4**：新增 `POST /api/employees/{eid}/transfer-mappings`（校验接任者存在且在职、不能是本人，返回 `moved`）+ `GET /api/employees/active`（在职员工简表）；停用响应回带 `report_mapping_count`。RBAC 已确认均映射到 `hr`（不会 403），端点 401 正常。
  - **前端**：**M1** Settings 权限 tab 移除成员授权（含 `users/userLoading/roleSavingId/memberQuery/memberPage/filteredMembers/pagedMembers/loadUsers/changeRole/isMe/roleOptions/MEMBER_PAGE_SIZE` 全部清理），生产 `成员授权`=0；**P0-1 UI** toast 改为「已停用员工，并禁用 N 个登录账号」让承诺对得上真实结果；**P0-4 UI** 停用后若 `report_mapping_count>0` 自动弹「交接报单配置」模态（说明不可作废原因 + 接任者下拉 + 确认转交/稍后处理）。补 `.req{color:var(--dan)}`（原未定义）。
  - **部署**：后端只 rsync `server.py/erp_db.py/core.py` + chown + 清 pycache + restart（health 200）；前端 build+rsync。
  - **⚠️ 遗留（P1，未做）**：**真实员工目前与账号仍是 0 条有效业务关联**（`employee_id` 全为 0，只有测试账号 9001/9002 有值）。P0-1 的联动能力已就绪，但要对真实员工生效，须先做 **P1-3 数据配对回填**，或在员工档案页为每位员工「开账号」（新开账号会自动写 `employee_id`）。另 **P1-2** 员工列表 `account_status` 体检列、**P1-4** `employee_stores` 与 `report_mapping` 重叠收敛，均未做。

- **【Bug 修复·员工档案三问题】继续完成未完成任务（用户"继续"）**：
  - **根因复盘**：①「添加」按钮 `:disabled="!newEmp.name"` 灰掉且无反馈 → 改名时直接无反应。②「修改资料」仍显示 boss 的根因 = 员工档案的「账号」按钮对已存在账号的员工仍打开**开通新账号**弹窗 → 用户误把"改自己账号成手机号"做成**新建第二个账号**(phone/admin, id=7)，而会话仍以 boss(id=2)登录，故修改资料显示 boss。③编辑/开账号整合决策=合并为单一「编辑」弹窗内的「小程序账号」区。
  - **后端（production 基线外科手术式，未夹带 11 未推送 commit）**：`staff_account_create` 重写——a) INSERT 写入 `employee_id`（修 v110 只写 employee_code 漏洞）；b) 新增**按员工去重守卫**（同 employee_id/employee_code 已存在活跃账号则拒绝"该员工已有关联账号"）；`employee_list` 富化返回 `account_user_id`/`account_active`（前端按 user_id 管账号）；新增 `POST /api/users/{uid}/password`(重置密码) + `PUT /api/users/{uid}/status`(启/禁用)，均映射 `data` 模块(admin/boss 可用)。
  - **前端（EmployeeArchive.vue）**：①「添加」按钮去掉 disabled，点无效时 toast「请输入员工姓名」。②「编辑」弹窗新增「小程序账号」区：无账号→内嵌开通表单(角色复用 ROLE_OPTIONS)；有账号→显示账号/角色/状态 + 重置密码/修改角色/禁用。③删除原独立「开账号」弹窗与行内「开账号/账号」按钮（整合进编辑）。`npm run build` 通过(产物 Archive-C0cw3z5j.js)。
  - **部署**：后端 scp production 基线+3补丁→`/opt/hergent-erp/{erp_db.py,server.py}`+chown+清pycache+restart(health=200，新代码 grep 全命中)；前端 rsync dist/→`/opt/hergent-cn-v2`+chown。⚠️ 沙箱未做浏览器真机验证。
  - **⚠️ 待用户拍板的数据清理**：生产已存在重复账号 id=7(phone/admin) 是本次误操作产生；建议二选一清理——A)禁用 id=7 后到「修改资料」把 boss 改名成手机号；B)保留 phone 账号、禁用 boss 账号、改用手机号登录。属生产数据改动，未擅自执行，待用户确认。

## 23:5x 员工档案「添加」复用编辑弹窗 + 编辑弹窗 UI 重排
- **诉求**：① 点「添加」应弹出跟「编辑」一样的弹窗（而非旧的行内快速表单）；② 编辑弹窗布局有问题，优化。
- **前端（EmployeeArchive.vue，仅前端，无后端改动）**：
  - 删除行内快速添加区(`newEmp`/`addEmployee`)，卡片头部加「＋ 添加员工」按钮 → `openCreate()` 打开**同一个编辑弹窗**(`isCreate=true`、`editTarget=null`)。
  - 弹窗标题动态：`新建员工` / `编辑员工 · 姓名`。保存逻辑合并为 `saveEmployee()`：新增模式 `employeeApi.create` 后**不关闭弹窗**，自动切到该新员工编辑态，便于接着开通小程序账号；编辑模式走 `update`。
  - 新增模式下「小程序账号」区显示提示"保存员工后可开通"，不展示开通表单（员工尚不存在）。
  - **布局重排**：编辑弹窗主体改为「基本信息 / 薪酬与账户 / 小程序账号」三段式（`df-sec` + 左侧 3px 品牌色竖条标题 `.df-sec-title`），字段两列网格、输入框统一 `width:100%;height:36px`；账号管理行 `<select>/<input>` 用 `flex:1` 占满、按钮自适应；弹窗宽 560→600px、内部 `max-height:80vh` 滚动；开通按钮改为整宽 `.btn-block`。`npm run build` 通过(产物 Archive-BHmIxno1.js)。
  - **部署**：rsync dist/→`/opt/hergent-cn-v2`+chown；新文案 grep 命中(新建员工/基本信息/薪酬与账户/添加员工)。⚠️ 沙箱未做浏览器真机验证。

## 19:4x 预报订单管理主工具栏 P0+P1 优化（落地检查报告结论）
- **来源**：`overview-预报订单管理工具栏检查报告.md`（23 项按钮全可触发、无死按钮；3 类布局问题 + 5 处语义冲突 R1-R5）。用户「做」→ 按 P0→P1 落地。
- **P0 布局三项**：①P0-1 右工具栏插 `.tb-sep` 分隔条，把[筛选/数据·导入/编辑·保存/智能建议/表格设置▾·高级▾]显式分五簇；②P0-2 三个状态徽标(已确认/草稿/已隐藏)移出按钮行，改独立 `.tb-status-row`；③P0-3 `.toolbar` 改 `flex-wrap:wrap` + 窄屏 `@media(max-width:768px)` 断点 `.tb-right` 换行，消除 overflow-x 藏功能。
- **P1 语义去重五项**：①R2 更多工具→🤖销量预测 改名「🤖运行预测」(保留高级▾分组开关「销量预测」不动，非真双入口，是"分组开关+运行动作"一致模式)；②R1「📝建议书」及面板/注释/toast 统一改名「下单说明」；③R3 审批→审批流、推送审批→推送企微审批、小程序审批→小程序端审批(按钮+面板标题+footer 推送共 4 处)；④R4 💰返利冲刺→💰返利缺口查、🧮凑单达返利→🧮一键凑单达返利 + 提示文案改写；⑤R5 将「🩺体检」与「💡数据健康分」从「更多工具」移入「数据质量」分组(原 hsOpen 无工具栏按钮，现补 loadHealthScore 触发)。
- **部署**：node 22.22.2-2 `npm run build` 成功(exit0, 产物 Forecast-gVpNAz2a.js 213.83kB) → rsync --delete dist/→`/opt/hergent-cn-v2` + chown → DEPLOY_OK。
- **核验**：生产包 grep 9 个新字眼(下单说明/审批流/推送企微审批/小程序端审批/返利缺口查/一键凑单达返利/运行预测/数据健康分/tb-status-row)全部≥1命中；旧字眼「建议书」「小程序审批」归零；「返利冲刺」/「凑单达返利」/「销量预测」仍各1次=预期保留(分别是冲刺看板标题/面板标题/高级▾分组开关)。⚠️ 沙箱未做浏览器真机验证。
- **未做**：报告 P2(高级▾内 section 平铺 / 更多工具 30 按钮折叠分类)，用户原话"P2 可另议"。
