# 2026-08-13 工作日志

## 读交接文档 + 返利/指标达成模块设计澄清
- 重读《Hergent-整合优化方案-2026-08-11.md》(最新总纲) 与 《预报订单小程序-MVP方案-2026-08-12.md》。
- **关键发现（必须记牢）**：系统里现有 3 套"目标/返利"，但都不是用户要的"厂家返利指标达成"：
  1. `sales_targets` = 业务员绩效目标（销售额/回款/新客/拜访），与厂家返利无关。
  2. `rebate_contracts` = **你给下游客户的年度返利**（按 contact_id 算，实际从 sale_orders 自动汇总，见 `get_rebate_progress`）。
  3. **厂家给你的返利/指标达成**（蒙牛等按你进/销其产品给的返利）= 用户真正要的；`rebate_rules`(昨天上线) 已建"目标值/品牌单品维度/周期/阶梯/返利比例"，但**缺一个存"实际达成数"的表**——上传的销量/销售额就是喂 `/simulate` 的 `achieved_amount/qty`。
- **用户诉求**：①实际达成数据靠手工上传 Excel，问入口放哪+字段怎么写；②目标/返利数据要能查到，用什么图表最直观。
- **我的设计方案（待用户确认后开工）**：
  - 新建一级模块「厂家指标与返利」，三标签页：规则配置(复用昨天 rebate-rules) / 实际达成导入(新) / 达成&返利看板(新)。不塞进进销存(已隐藏)、不塞经营看板。
  - 上传模板字段（经销商白话）：期间(YYYY-MM)、维度(品牌/单品)、厂家品牌、单品(单品维度才填)、实际销量(件,品牌维度灰掉)、实际销售额(元)。配 下载模板+逐行校验+错误报告；品牌维度禁用销量列(对齐规则"品牌只能选金额")。
  - 图表：达成率用半圆仪表盘 Gauge；阶梯返利用进度条("再卖X元返利率3%→5%多返¥Y")；总目标vs实际用对比柱；月度趋势用折线+柱；本期返利用按品牌汇总柱。看板首页=总达成率大数字+各品牌gauge排+返利汇总柱；点开品牌=趋势+阶梯详情。
- **待确认**：用户是否要我现在就建「实际达成导入」需要的存储表(新表 rebate_actual/achieved，按 period+dimension+scope_key 存)+前端三标签页模块，还是先只做导入+看板其一。图表库用轻量方案(ECharts via CDN 或 纯SVG，前端是 vanilla JS+Vite)。
- 已用 show_widget 出了一张「厂家指标与返利」模块草图给用户看（含上传入口位置+图表布局）。

## ⚠️ 用户指正：真正的交接文档是《赫金特产品观——给后来者的完全手册.md》(hergent-erp根目录)
- 这份才是权威交接文档（最后更新 2026-08-13）。第三十七章「v108 UI 改造蓝图落地」已把"目标与返利/上传实际数据/数据看板"写成明确产品规格，我前面的草图需按它纠正：
  - **页面不合并**（37.2#1）：「目标与返利政策」(配置/写) 与「数据看板」(消费/读) 是**两个独立一级页**，通过双向跳转打通；不是我草图里的三标签页合一。
  - **侧边栏标签**：政策页标"厂家规则"。
  - **上传入口=「上传实际数据」规范表单弹窗(modal)**（v6），非独立 tab。双通道（37.2#2）：主=舟谱"采购入库单/发货实绩"Excel导入(单品级)；兜底=文员手动填汇总级；舟谱优先、手动兜底、不叠加、留来源字段。表单中文标签+示例值+必填提示+字段说明表。
  - **字段（37.2#3 达成数据表）**：来源(舟谱/手动)+期间+商品+发货箱数+发货金额。舟谱导入映射按真实"采购入库单"字段，**需用户给样例**。
  - **图表=ECharts（不是 Chart.js！不是纯SVG）**（v6/v7/第四批）：趋势柱+折线复合图、对比柱状图可下钻、核销状态环形图 + 期间/维度/计量筛选。年返单独呈现(年度累计进度+剩余月份,37.2#5)。
  - **复用不重建**（37.4 第三批）：现有 `/api/rebate-rules/*`(昨天上线) 和 `/api/reports/rebate-progress` 已存在；按 37.2 三维自由搭配模型(周期×考核对象×计量×结构)扩展表结构，勿从零建。
  - **核销台账**：应返→已确认→已核销三级（v5）。
- **批次顺序**：第一批(经营工作台)已上线；第二批=预报汇总表+审核大脑；**第三批=目标与返利政策页+上传实际数据表单**；第四批=数据看板(ECharts)。用户的两问正是第三、四批。
- **前端接驳范式（37.3 教训）**：v108 新页用 `sidebar-v2.js` 硬编码导航+`_v2Nav` 路由（如 ops-workbench），**不注册 ModuleRegistry**（会进折叠组重复入口）。我昨天写的 `rebate-rules.js` 走的是 ModuleRegistry，手册记为"历史遗留 untracked, 勿删"——后续政策页建议改走 sidebar-v2 范式。
- **RBAC**：新模块须在 `server/core.py` 的 `_DEFAULT_PERMS` 给 boss/sales/guide 登记（admin通配，accountant/driver不需要）。
- **CSS 令牌坑**：`--dan-bg/--war-bg/--line` 不存在；预警背景用 `rgba(var(--red-rgb),.08)`，边框用 `--bd`（主题感知）。
- **阻塞项**：舟谱"采购入库单"真实字段样例（用户手头有 26年8月下单表 xlsx 65 sheet，但那是"下单表"非"发货实绩/采购入库单"，需用户另提供导入样例）。

## 优化 v108 蓝图（按我的判断重做，2026-08-13 晚）
- 用户要求"把蓝图按你的想法优化"。读完第三十七章 + 核实前端/后端现状后，产出《v108蓝图优化-厂家返利中心-20260813.md》(laozhangai-product 根)。
- **核实到的关键事实（优化依据）**：
  1. Vite 工程(static/) **无任何图表库**（Chart.js 是旧 vanilla 工程的，ECharts 也没装）→ 看板必须从零引入，建议 npm 装 echarts 打包进 bundle（不依赖 CDN，客户无外网也不挂）。
  2. 侧边栏双机制共存：ModuleRegistry(我昨天 rebate-rules 走它) + sidebar-v2.js 硬编码(ops-workbench 走它)。新页统一走 sidebar-v2，旧孤立入口下线并入。
  3. 生产库缺"实际达成"表：actual_achievement/achievement_data 不存在；只有 rebate_rules(已上线)、sales_targets(业务员绩效,无关)、rebate_contracts(你给客户返利,非厂家)。
  4. 原蓝图"舟谱采购入库单导入"强绑定单一数据源，对"卖给其他经销商"定位不友好。
- **优化拍板（7 条，见文档 §0）**：① 新建「厂家返利中心」统一分组收敛三页(规则配置/实际数据导入/达成&返利看板)，删旧财务工具孤立入口；② 导入做成独立页(非modal)；③ Excel 导入做通用模板+可配置列映射(内置舟谱预设，不阻塞等样例)；④ 图表用 ECharts 打包进 bundle；⑤ 注入 AI 副驾解读层(Hermes,核心卖点)；⑥ 响应式手机看核心数字；⑦ 核销环形明确为"厂家返利收入"视角(应返/已确认/待核销)。
- 落地批：第三批(后端补 sales_targets_mfr + achievement_data 两表 + 导入落库 + 中心分组+规则归并+导入页) → 第四批(看板 ECharts) → 4.5批(AI解读)。
- 已用 show_widget 出「优化后架构与看板」草图。下一步等用户说"做"就从第三批开工。

## 生成高保真 UI 实现原型（2026-08-13 晚）
- 用户要求"根据蓝图生成可直接运行预览的完整 UI 实现，视觉与既有风格一致"。我读完 variables.css(真实令牌) + styles.css(真实组件: .btn/.tbl-wrap/.stat-card/.modal/.badge 等) + rebate-rules.js/ops-workbench.js 写法后，产出零依赖 HTML 原型：`laozhangai-product/hergent-rebate-center-prototype.html`（双击浏览器即开，无需构建）。
- 严格套用既有风格：浅色 --bg #ececf0 / 卡片 #fff / 主色 --p #1f4e79(深蓝!非 #06b6d4) / 系统绿红橙 / 12-16px 圆角 / 柔和分层阴影 / 208px 侧栏 / SF Pro 栈；并内置浅↔深双主题切换(:root.dark 原样令牌)验证一致性。
- 覆盖蓝图三页：① 规则配置(示例表+冲突banner+新增modal:品牌锁金额/阶梯动态行/实时校验)；② 实际数据导入(下载模板卡+拖拽上传区+手动补录+来源留痕)；③ 达成&返利看板(筛选chips+AI解读卡+4图:趋势柱折/品牌达成率对比可下钻/核销环形/年返进度+手机预览)。图表用内联 SVG 还原外观（生产落地换 ECharts，原型不引外部库以保证零依赖可直接看）。
- ⚠️ 关键修正：variables.css 真实 --p 是 #1f4e79 深蓝；working_memory 记的"品牌青#06b6d4(=--p)"已过时，后续一律以 variables.css 为权威源。
- 交互均可用：侧栏切换三页、看板筛选切换、规则modal开关/校验、品牌柱下钻toast、上传拖拽高亮、主题切换、toast。数据均为示例演示。

## 优化 apps-builder 改造蓝图实例（2026-08-13 深夜）
- 用户要求：基于实例 `hergent-cn-ui改造蓝图-20260812T071553784Z/`（apps-builder 工作区：v7 主文件 + history/v1-v7 + MANIFEST + .apps-builder/project.json）做优化，输出优化后完整蓝图，**保持目标/技术栈/定位不变**。
- 实例原图（v7）要点：导航三分组（经营/数据/其他）+ 全局 AI 副驾单一入口；核心模块=经营工作台(KPI/AI晨报/批次流/待办)、预报汇总表(报单录入+商品管理+**智能审核大脑**：品牌总指标进度条+单品达成+三因子建议订单)、目标与返利政策(销售目标 brand/goods + 返利政策 ladder/gift/achieve + 核销台账三级闭环)、数据源(舟谱Excel)、数据看板(趋势柱折/对比柱状可下钻/核销环形)、上传实际数据(Excel+手动6字段)、消息推送、设置。技术栈：品牌青#06b6d4（浅色默认双主题）、ECharts via CDN、原生JS、`api()` 封装、租户从 JWT/会话推导（前端不传 X-Tenant-Id）。
- 三因子算法：`建议量 = 报单整箱 + max(指标补量, 返利冲刺量)`；返利差额>40箱不为返利压货；审核大脑只建议、采纳才写最终下单。
- **产出 1（文档）**：`hergent.cn-ui改造蓝图-优化版.md`（8 章）。§0 优化原则+「3个不变」(目标/技术栈/定位)；§1 信息架构+模块职责边界(10模块职责表+4条铁律：规则只从政策页写/原始数据只从数据源+上传实际写/审核大脑只建议/写走api()+后端校验 + 单向数据流图)；§2 阶段 P0-P7 优先级表+依赖DAG(关键路径 P0→P1→P2→P3→P4，P5/P6并行，P7收口)+每阶段DoD；§3 异常处理与边界场景(通用401/5xx/租户/乐观回滚/三态 + 逐模块边界表)；§4 组件复用与状态管理(组件库 KpiCard/TimeProgressBar/TierTable/Modal/Toast/FilterBar/ChartCard/三态 + 分区store + 作用域重渲染 + 图表治理)；§5 移动端适配规范(断点/底部Tab/抽屉/触控≥44px/数字键盘/性能)；§6 中文UX清单(货币¥千分位/单位箱件/百分比口语化/空值「—」/错误口语化)；§7 技术栈锁定；§8 全阶段验收清单。
- **产出 2（可预览原型）**：`hergent.cn-ui改造蓝图-优化预览.html`（直接浏览器打开）。在 v7 基础上应用优化，关键修复/新增：① 移动端桌面侧栏 + 手机底部Tab(5Tab:工作台/预报/政策/看板/更多)+抽屉(更多放消息推送/数据源/设置/AI)，`@media(max-width:1279px)` 收起侧栏为图标、`@media(max-width:767px)` 隐藏侧栏显底部Tab单列、`.ai-bar` fixed 到 `bottom:56px`；② 三态 `.skeleton` 动画 + `showDashboardSkeleton()`+`renderDashboardAsync()`(550ms先骨架再图，`typeof echarts==='undefined'` 降级 `fallbackTable()` 静态表并toast)；③ 内联校验 `validateAchUpload()` 用 `!/^\d{4}-\d{2}$/.test(period)` 校验期间，`.invalid`红框+`.au-err.show`红字，未过拦截；④ **作用域重渲染** 修复 v7 改一格重画整页丢焦点缺陷：`renderForecastRegionInner()`+`rerenderForecastRegion()`+`bindForecastRegion()`（区域内委托绑定 data-cell/data-extra/data-final/data-disable/data-enable/data-adopt，改一格只重画区域容器保焦点）。删全局 click 里重复 data-adopt 分支、confirm-import 改调 render()。补 `.vbadge-optimized` 四色样式。保留 v7 全部演示数据与品牌青视觉。JS 经 `new Function(code)` 校验通过(JS_SYNTAX_OK)。
- 注：本实例独立声明品牌青#06b6d4，与 hergent-erp 真实 variables.css 的 --p #1f4e79 不同；按用户"保持与原图一致"要求，本优化以蓝图实例自身声明为准，不引入 #1f4e79 修正。
- 状态：文档(#238)+预览(#237)已完成，尚未落地生产代码，等用户确认是否开工。

## CEO 视角产品战略建议（2026-08-13 深夜，用户问"该做什么产品"）
- 用户主动披露重大转型：因非技术 solo、纯靠 AI 写码的现实，暂缓企业级 AI 原生 ERP，专注 AI 层。要我以一线科技公司 CEO 视角给产品方向。
- 我的核心定调（已答用户）：**不做"又一个 ERP"，做「低温奶经销商的 AI 经营副驾」——系统之"记录"是舟谱地盘(无API/数据孤岛，solo非技术必输)，系统之"决策"才是空白且无人服务，且用户是同行、懂黑话算账逻辑(大厂抄不走)**。
- 楔子：返利达成(纯增量钱,ROI一眼可见,变现快)为主故事 + 预报订货(日频黏性钩子,已有引擎)为日常钩子；货损/对账/工资作后续增购。建议预报拉新、返利变现双线分主次。
- 交付形态（最关键的现实约束）：**非技术 solo 别假装做可规模多租户云 SaaS**；第一阶段=托管式服务/每客户隔离实例，按"找到的返利钱/省下货损"收费而非卖席位；先手把手跑 3-5 家同业 pilot 收真钱，再重投入真 SaaS+找技术合伙人。
- 护城河：代码不是护城河(对手也能AI写)；护城河=①被编码的领域逻辑(6引擎)②同行身份信任③数据飞轮。把领域经验写成 Skill/规则=真IP。
- 诚实风险：①非技术solo硬扛企业级多租户SaaS(稳定/支持/安全坑)→用服务优先绕开；②舟谱无API→只做导出→粘贴模板/CSV，不承诺自动直连；③技术合伙人在需求验证后找，不在验证前烧钱建架构。
- 90天：包6引擎进干净"经营副驾"对话+晨报界面→3-5家pilot手把手导数据跑价值→收月费/成果费→再定真SaaS架构+找技术合伙人。
- 待用户拍板：楔子先打返利还是预报；起步形态托管服务式还是直接真多租户云(我推荐前者)。

## 审核 hergent-erp 开发进度（2026-08-13 深夜，用户要审"另一个实例"部署到 erp.hergent.cn 的代码）
- 审核结论文件：`hergent-erp/开发进度审核-20260813.md`。双域名 HTTP 200，新接口均 401（fail-closed，已上线）。
- 已落地且质量高：① `ops-workbench.js`(经营工作台,177行,5接口全有后端,sidebar-v2接驳) ② `rebate-rules.js`(厂家返利规则,457行,XSS经esc转义,CRUD+冲突+校验+试算;后端rebate_rules.py 8路由) ③ 后端引擎 forecast.py全量/forecast_audit.py(审核大脑引擎)/dashboard today-profit/inventory expiry/forecast。
- 🔴 P0 致命：新模块 ops-workbench.js/rebate-rules.js 是 **untracked**，core.py/sidebar-v2.js/main.js 是 **uncommitted**，但生产已用它们部署。标准部署(`git reset --hard`+rsync)会清空未提交文件→静默回滚新功能。必须先 commit+push 止血。
- 🟠 P1 缺口：智能审核大脑**后端有、前端UI无**(forecast-console.js不含compute/审核大脑)；上传实际数据前端缺失(无achievement模块,生产库缺表)；数据看板ECharts(P4)未建。
- 🟡 P2：rebate-manager.js(你给客户返利合同/结算) vs rebate-rules.js(厂家给你返利规则) 都叫"返利"方向相反,老板易混淆。
- 🟢 P3：/api/rebate-rules 在 _PATH_MODULE_MAP 映射到模块"stock"(boss持有)→不会403,但语义错位(返利挂库存权限下)。
- 蓝图批次进度：第一批经营工作台✅、第二批预报汇总表✅/审核大脑后端✅前端❌、第三批规则配置✅/上传实际❌/核销台账❓、第四批看板❌。
- 下一步优先级：①立即commit+push止血 ②补审核大脑前端 ③补上传实际数据 ④补ECharts看板 ⑤收敛两返利模块命名。已向用户说明，待确认是否执行commit。

## 执行 P0 止血：commit+push（用户回"要"）
- ✅ 已执行：精确暂存 7 个安全文件（core.py/sidebar-v2.js/main.js + 赫金特产品观md + 新 ops-workbench.js/rebate-rules.js + 开发进度审核md），**未碰 .env/数据库/密钥**。
- 提交 `b83ab10`（7 files, +849/-5），常规快进推送（本地与远程原都 5f170e0，零分叉，**无需 --force**）。
- 推送遇代理瞬时 `502 CONNECT tunnel failed`（沙箱关闭后暴露真实错误），重试第 4 次成功：`5f170e0..b83ab10 upgrade/v84-international -> upgrade/v84-international`。
- 验证：远程 HEAD=b83ab109，本地领先 0，工作树干净。**P0 风险已闭环**——今后标准 `git reset --hard origin/...` 部署不会再静默回滚新功能。
- 提醒：出网代理对 GitHub 偶发 502，push 失败先重试几次而非改用 --force（避免误删远程提交）。
- 剩余 P1/P2 缺口（审核大脑前端/上传实际/看板/返利命名）仍待排期，未动生产代码。

## 排「智能审核大脑前端」落地计划（用户回"要"）
- 计划文件：`hergent-erp/审核大脑前端-落地计划.md`。
- 关键发现（决定分档）：后端 `/api/forecast-audit/compute` **当前只算库存覆盖建议量**(`suggested_qty`+文字`verdict`)；蓝图三因子的 `target_achievement`/`rebate_hint` 在后端**明确是 null（代码注释 v1 待补）**。→ 分 MVP(库存建议,今天能做) 与 完整版(需后端补2字段做三因子)。
- 定位：审核大脑=「预报汇总表」页内面板（蓝图§1/铁律C：只建议不擅自改单，采纳才写最终下单），非独立侧栏模块；改造目标文件 `static/js/modules/forecast-console.js`，复用预览原型作用域重渲染 `renderForecastRegionInner/bindForecastRegion/rerenderForecastRegion`。
- 后端契约核实：`/products`(搜商品) `/compute` `/rebate-summary`(返利达成) `/save`(落forecast_orders草稿) `/orders`；RBAC 已映射到 stock 模块(boss持有)，无需改权限。
- 全局 `api()`(ai-safety.js包装fetch,自动鉴权+toast) 为真封装；`apiGet` 仅为各模块内 `function apiGet(p){return ApiClient.get(p)}` 本地习惯——计划统一用 `api()`。
- 遗留：现有 forecast-console.js 走 `/demand-forecast/*`(另一套算法)，审核大脑作下方新增区域不冲突。决策点(嵌入vs独立模块 / MVP vs 完整版)已列明待用户拍板。
