## 2026-08-09

### 订单表单引擎 v107.118 — showOrderForm(config) 配置驱动 (08:00-08:30)
方案 A 落地（用户拍板）：抽取通用引擎 + 访销订单 0 回归。
- **新建 `static/js/modules/order-form/configs.js`**：`window.HERGENT_ORDER_CONFIGS` 注册表，9 个订单模块 config（visit_sale 完整接入，其余 7 个+调拨为阶段2-3 占位，未接菜单）。config 键：title/tabId/tabLabel/endpoint/partyType(客户|供应商|none)/partyLabel/partyEndpoint/warehouseLabel/dualWarehouse(调拨)/isReturn/showXxx(字段显隐)/priceLabel/qtyLabel/showQty2/stockCheck/primaryLabel/statusDraft/statusPrimary/minRows。
- **app.js 改造**：`showVisitSaleForm` → `_showOrderForm(cfg)`（IIFE 闭包内，不能独立文件）；顶部标题/字段区(客户meta/部门/仓库/订单时间/预提货/预配送/车辆/司机/地址/付款/加急/备注)/表格列头(价格/数量/盒份/仓库/仓位/库存)/底部按钮 全部 cfg 驱动 + 条件渲染。`window._vsfActiveCfg` 存当前 config 供 _vsfSaveAndClose/_vsfClose 读取。`showVisitSaleForm` 变薄包装 `_showOrderForm(HERGENT_ORDER_CONFIGS['visit_sale'])`。新增通用入口 `window.showOrderForm(type|cfg)`。
- **部署**：main-vwwiMiJq.js 上线 HTTP 200；静态验证：引擎函数/9 configs/标题均在 bundle，旧硬编码 endpoint 清零（config 驱动生效）。
- **commit `596b991`**。
- **★关键架构决策**：app.js 是 IIFE 闭包，`_tabs`/`_activeTab`/`renderTabBar`/`api` 等全是闭包变量 → 引擎必须留在 app.js 内部，config 纯数据可独立文件（Vite side-effect import）。
- **阶段2已知风险**：Promise.all.then 里 `initSearchableSelect('vsf-dept'/'vsf-driver'/'vsf-vehicle')` 无条件调用，模块 showXxx=false 时元素不存在可能抛错 → 阶段2 须加防御；`_vsfOpenCustomerPicker` 内部仍按 customer 语义（供应商细化在阶段2）；`_vsfSaveAndClose` 的 body 组装仍按 sale_orders schema（purchase/transfer/return 的字段差异在阶段2-3 处理）。
- 待用户登录后实测回归（验证清单：打开表单→选客户→选商品→价格联动→库存校验→提交→回执）。

### 阶段2: 5个销售变体订单模块 (v107.119, 08:26) ✅ 已部署 main-CUNhf3so.js
- **重要发现**：mega 菜单 TRADE_MEGA_COLS 早已为全部 8 模块预留入口（`create:"showSaleReturnForm()"` 等），只需实现这些 window 函数。
- 引擎增强：`_vsfAddRow` 行单元格按 config 条件渲染（与列头同步保证 th/td 数量一致，colgroup/列设置依赖此）；退单模块加退货原因(必选)+原订单选择器(initSearchableSelect('vsf-origin-order', /api/sale-orders?limit=100))；`doSubmit` 全部可选字段判空 + body 按 cfg.isReturn 分支（return-orders schema: sale_order_id+reason）；提交入口加退货原因必选校验。
- 实现 5 个入口：showSaleReturnForm/showVehicleSaleForm/showVehicleReturnForm/showPickupForm/showPickupReturnForm。
- **阶段2 风险化解**：`initSearchableSelect` 已有 `if(!wrap)return` null 防御（无需改）；`_vsfRecalc` 对 qty2 判空（安全）；_vsfSyncDeliveryTime/初始校正均有元素存在判断。
- configs.js 5 个占位 config 直接可用（阶段1 已备好：visit_sale_return/car_sale/car_sale_return/self_sale/self_sale_return，含 isReturn/warehouseLabel='退货仓库'/priceLabel/qtyLabel/showQty2 等差异）。
- commit `a07ca05` (v107.119)。
- 待用户实测：mega 菜单→访销管理→访销退单（应有退货原因+原订单字段）；车销订单（无司机车辆、有盒份列）等。

### v107.120 fix: 车销/车销退/自提退无法创建 (08:37)
- **用户反馈**：能创建访销退单/访销订单/自提订单，无法创建车销订单/车销退单/自提退单。
- **根因（★重要教训）**：app.js 存在**老的车销订单(7253)/车销退单(8627)/自提退单(8656) 独立表单实现**，在 8237/8653/8683 行 `window.showXxx=showXxx` **覆盖了 v107.119 挂的引擎版本** → window.showVehicleSaleForm 变成老实现(`function(){ld(),setTimeout(...)}`)，点击"+ 创建"无反应。访销退单/自提订单无老实现故幸免。
- **排查方法（★可复用）**：browser-use 无头浏览器 + 生产登录(admin/admin123 有效!) → eval 检查 `window.showXxx.toString()` 确认函数真身（发现是 ld()/kn() 而非引擎调用）→ grep 定位覆盖点。
- **修复**：8237/8653/8683 老挂载点改 `window.showXxx=window.showXxx||showXxx`（引擎版本优先）。
- **验证**：无头浏览器实测三个表单全部正常打开（标题/退货原因/原订单/盒份列正确）。
- **部署**：main-mTg68xkT.js，commit `2e31bdf` (v107.120)。
- **教训**：app.js 里同名函数多处定义/挂载是历史债（老实现+装饰器+引擎并存），新挂载点必须放最后或加 `||` 保护。

### v107.121 fix: 退单原订单选择器 "(x||[]).map is not a function" (11:36)
- **用户反馈**：控制台 `(kt || []).map is not a function`（在 `#/dashboard` 路由下报，实为在该页打开退单表单 overlay 触发）。
- **根因**：退单模块原订单选择器 `originOrdPromise = api('/sale-orders?limit=100').then(r => (r.results)||(r.items)||r||[])`。后端 `sale_order_list` 返回 `{'orders':[...],'total':N}`（对象，既无 results 也无 items）→ `r` 为 truthy 对象 → `||[]` 不生效 → `.map` 抛错。minified 变量 `kt` = 该 promise 结果，唯一命中。
- **修复**：归一化覆盖 `results/items/orders/data/list/records`，先 `Array.isArray` 早返回，最终 `Array.isArray(arr)?arr:[]` 兜底；`.map` 前再 `Array.isArray(list)?list:[]` 双重保险。
- **排查方法（★可复用）**：生产 bundle 是单行长行(minified)，用 `python3` 按字符串偏移截取 `vsf-origin-order` 上下文定位 minified 代码；grep `\|\|\[\]\).map` 列出所有脆弱点只有这一处。
- **验证**：`VITE_BASE=/ npm run build` 通过；新 bundle `main-DVHJ5Ryu.js` 已确认含修复且旧 `(kt&&kt.results...)` 模式消失；rsync 到 /opt/hergent-erp/static 并 chown，生产 index.html 已引用新 hash。
- **附带**：顺带提交此前未提交的审计日志(server/db/queries/sales.py, server/routers/sales.py)与表单错误样式(static/styles.css)。commit `e181048` (v107.121)。
- **★API 信封约定（重要）**：Hergent 各列表接口返回形状不统一——`sale_order_list` 返 `{orders,total}`，前端 old 代码多处假设 `{results}/{items}`。凡 `.map` 前务必 `Array.isArray` 兜底 + 多键归一化，否则 truthy 对象让 `||[]` 失效。
- **另一条报错** "A listener indicated an asynchronous response..." 是 Chrome 扩展(非本应用)的 message channel 警告，与本 bug 无关，忽略。

### v107.122: 阶段3 — 采购/调拨订单接入通用订单引擎 (12:42) ✅ 已部署 main-CYb9tAOE.js
- **#176 供应商选择器**：`_vsfOpenCustomerPicker` 按 `_vsfActiveCfg.partyType` 分支（supplier→`cfg.partyEndpoint`=`/contacts?type=supplier`，缓存键 `_VSF_DATA.suppliers`/`_VSF_SUP_TOTAL`，表头/空态/仅匹配标签切换）；`vsf-customer-id` change 处理器对 supplier/none 提前 return（跳过应收/专属价/默认仓/欠款弹层）；B1 客户隔离仅 customer 型执行；对手方数据按 partyType 键缓存。
- **#177 调拨双仓库**：模板加调入仓字段 `vsf-dest-warehouse(-id)`（dualWarehouse 时）；列头+逐行仓库 `<td>` 在 dualWarehouse 隐藏（th/td 数量同步）；`_vsfLoadStock` 缺逐行仓回退主调出仓；`fillWhOptions` 无 wSel 时跳过并回退库存。
- **#178 提交体分支**：`_vsfSaveAndClose` 对 partyType==='none' 跳过 cid 必填（用 partyLabel 提示）+ dualWarehouse 调入仓必选；`doSubmit` 三分支：isReturn(原有)/supplier→`{supplier_id,items,warehouse_id,...}`(extra=allow 容忍 status/operator_id 等额外字段)/dualWarehouse→**逐行顺序 POST /api/transfer-orders**(后端单商品/单) + 逐行 `r.success` 校验(throw transfer_failed)；抽取 `window._vsfAfterSubmitSuccess` 共用清理(回执/清草稿/关 tab/overlay)。
- **#179 接线**：`window.showPurchaseForm` 引擎替换(legacy 8165 行改 `||` 保护, 吸取 v107.120 覆盖教训)；`window.showTransferOrderForm` 新增；mega 调拨 `create:"showTransferOrderForm()"`；库存页 legacy `showTransferForm` 保留；**采购退货(purchase_return) 暂缓**（后端无退货端点，config 的 endpoint 误指 /api/purchase-orders，不接线即不触发）。
- **★后端 schema 实况（本次核对）**：purchase.py 用**自己的** `PurchaseOrderCreate(_Base)`（`extra="allow"`+`note` 字段，非 order.py 的 remark 版）→ 前端多传 status/operator_id/attachments/idempotency_key 均被容忍；inventory.py `TransferOrderCreate` 单商品(extra=allow)，`transfer_order_create` 返回 `{success,order_no}`。
- **★补修**：调拨表单无 `vsf-customer-id`（partyType='none' 不渲染），草稿自动保存定时器 30s 直接 `.value` 会 null TypeError → 改 `(el||{}).value||''`。commit `523e889`。
- **部署**：commit `40f6ea8`(feat) + `523e889`(fix)；生产 main-CYb9tAOE.js HTTP 200，index 引用新 hash，bundle 含 showTransferOrderForm/transfer_failed/type=supplier 等阶段3 逻辑。
- **遗留**：本地 ~8 commit 未推 GitHub（origin 快进可安全 push，D1 待办）；purchase_return 后端端点待做；供应商/调拨表单待用户真机实测（mega→采购管理→采购订单 应打开引擎采购表单；mega→调拨管理→调拨订单 应打开双仓调拨表单）。

### v107.122 hotfix: cfg/_cfg 作用域错误 + 调入仓未绑定 (13:06) ✅ 已部署 main-LoY6f7aN.js
- **用户报错**：`ReferenceError: cfg is not defined (at window._vsfAddRow)` + `_cfg is not defined` — 生产所有订单表单打开即崩。
- **根因（★重要教训）**：`node --check` 只查语法，**查不出运行时变量作用域错误**。我上一轮在三个函数里用错局部变量名：`_vsfAddRow` 用 `cfg.dualWarehouse`（该函数局部变量是 `_cfg`）；`_showOrderForm` 的 `.then` 回调用 `_cfg.partyType`（回调继承外层参数 `cfg`）；`_vsfSaveAndClose` 用 `cfg.partyLabel/warehouseLabel`（局部是 `_cfg`）。**构建通过 ≠ 运行时无错**，必须真机验证。
- **顺带发现**：调拨"调入仓"字段模板渲染了但 `.then` 里漏了 `initSearchableSelect('vsf-dest-warehouse')` 绑定 → 无法选择调入仓 → 无法提交。补上。
- **修复**：6 处变量名改正 + 1 处绑定补齐。commit `6f754b2`。
- **★运行时验证方法（可复用）**：`browser-use` 无头浏览器 + 生产登录（admin 会话 cookie 在 daemon 里持久）→ `window.__errs=[]` 装 error/unhandledrejection 钩子 → eval 直接调 `window.showXxx()`（绕过 UI 导航，直达报错路径）→ 检查 overlay/行数/关键元素/errs。实测：采购/调拨/访销三表单 16 行全渲染、errs 空；供应商选择器 38 条（蒙牛酸奶/2923 可选中）；调入仓下拉 3 选项（临期仓/测试仓库/总仓）选中写入 hidden。**注意**：合成 `el.focus()` 不一定触发 searchable-select 的 focus handler，用 `el.dispatchEvent(new Event('input'))` 更可靠。
- **遗留**：本地 10 commit 未推 GitHub；purchase_return 后端端点待做。

### v107.123: 采购退单接入订单引擎 — 修复无法创建 (13:25) ✅ 已部署 main-BYcY9-yL.js
- **用户反馈**：无法创建采购退单。
- **真根因（纠正先前误判）**：后端 `/api/purchase-returns` 端点**一直存在**（server.py:1153 → `purchase_return_create` erp_db.py:2686，含库存回冲 ref_type='purchase_return' + AP 冲减，返回 {success,id,total}）。我先前总结"后端无退货端点"是**错的**。真正坏的是前端入口：mega(530)/buying页(7406) 都**无参调用** `showPurchaseReturnForm(oid,sname)`（旧函数签名需要 oid）→ `submitPurchaseReturn(undefined)` 永远失败。两个调用点从未传过参 → 该功能从建起就没可用入口。
- **修复**：configs.js purchase_return endpoint 修正 `/api/purchase-orders`→`/api/purchase-returns`（原误指向采购单，会建采购单！）；加 requireOrigin/originEndpoint=`/purchase-orders?limit=100`/originLabel='原采购单'；引擎原订单字段配置化+数据源归一化(v107.121 模式)+供应商名展示；_vsfSaveAndClose requireOrigin 必选校验；doSubmit isReturn 分支区分 supplier(采购退货 body=purchase_order_id) vs customer(销售退单原样)；window.showPurchaseReturnForm 引擎薄包装后注册覆盖 legacy。
- **★运行时验证发现**：原单选择器"无匹配数据"是 **token 失效连锁反应** —— ApiClient 任一请求 401 会清 localStorage token 弹登录页(_fetch 36-44行)，后续所有请求全 401 → api() 返 null → []。**不是代码 bug**。且 eval 赋值+click 登录不触发 input 事件=假登录，须用 browser-use input/click 真实键入。
- **验证**：生产无头浏览器：表单16行零报错/供应商38条/原采购单选择器 PO单号+供应商名(选中写hidden=168)/未选原单提交被拦"请选择原采购单"不产生数据/后端 curl 200(168采购单)。commit `1502294`。
- **遗留**：本地 11 commit 未推 GitHub；buying 页"退货单"tab 的列表视图(render_buying('returned'))是否展示采购退货单待用户实测。

### v107.124: mega 菜单调拨入口走引擎版 — 修"调拨订单 UI 与访销不一致" (14:47) ✅ 已部署 main-BFQAhCBD.js
- **用户反馈**："创建调拨订单"UI 样式没有延用"创建访销订单"。截图实际是**老库存调拨 modal**（标题"库存调拨 · undefined"，小弹窗 + 硬编码"默认仓库"/"分仓A"）。
- **★根因（重要教训：模块多源陷阱）**：mega 面板真实数据源是 **`static/js/modules/trade-mega.js`**（独立模块，行 35 create:"showTransferForm()"），通过 `main.js:112` side-effect import 并替换 #tradeMenu 元素，是 mega 面板渲染的**唯一数据源**。我阶段 3 改的 `static/app.js:533` 的 `TRADE_MEGA_COLS` 是 **dead code**（mega 面板不读它），所以 mega 调拨"+ 创建"一直走老的 `showTransferForm()` 小 modal（legacy 库存单商品快捷调拨，需要参数 pid/pname/fromWh，无参调用永远 undefined 标题）。阶段 3 交付时漏改了真实数据源文件 → 用户从 mega 进入一直是另一套 UI。
- **修复**：trade-mega.js:35 `create:"showTransferForm()"` → `"showTransferOrderForm()"`（引擎版全屏），加 `isNew:true` 与其他新模块一致。**legacy showTransferForm 保留**给 app.js:671/7442 两处库存页单商品快捷调拨 quick-action（用途不同：单商品快捷 vs 多商品订单）。
- **验证**：bundle main-BFQAhCBD.js 中 mega-transfer 只剩 1 处且指向引擎版；浏览器实测引擎版打开"创建调拨订单"+ 双仓库 + 16行 + 无报错，截图与访销同 UI 系统。
- **★教训**：①模块化项目多文件时，**配置/菜单注册可能分布在多处**，grep 字符串字面量时要广搜整个 static/（不只是 app.js）。②Vite side-effect import 的模块是 bundle 真实入口（main.js:112 验证 trade-mega 被引入）；改 app.js 不一定改 bundle。③legacy 保留边界：本次明确"引擎版多商品表单 vs legacy 单商品快捷 modal"是两种 UX，分别服务于不同场景，**不应用引擎版替掉 legacy quick-action**。
- **遗留**：本地 12 commit 未推 GitHub。
- v107.124 补: 移除 mega 调拨菜单 isNew 绿点 — 上轮误加(面板内其它项均无此标记), commit `5a00150`, 部署 main-Dfl_sJYR.js。

### v107.125: 调拨表单对齐舟谱 — 裁剪噪声字段+双侧库存列+红色预警 (15:27) ✅ 已部署 main-myxpCW_i.js
- **用户反馈**: 拿舟谱(portal.zhoupudata.com)与我们的调拨表单对比, 指出"我们的复用了销售订单模板, 调拨不需要的价格/营销/业务属性字段全留下了, 又把调拨需要的司机/拟办人/双侧库存丢了"。
- **P0 改造**:
  - 裁剪: transfer config showMkt/showBizAttr/showPrice/showActualPrice/showAmount/showToolToggle 全 false → 行级列从 14 缩到 10, 顶部节省 1 行 toggle
  - 双侧库存: showStockFrom/showStockDest 配 stockFromLabel/stockDestLabel → 可用库存(调出仓)防超调 + 可用库存(调入仓)验接收容量
  - 红色预警: _vsfLoadStock 查询后比较 need>v → style.color=var(--dan)
- **★隐藏列防崩加固**: _vsfRecalc/_vsfValidate/items 收集/_vsfAutoSaveTimer/_vsfAmtChanged/_vsfLoadStock 全部判空防 .vsf-actual / .vsf-amount 隐藏后 null.value 崩。**这是阶段3留下的隐藏性技术债** — 当时只针对 transfer dualWarehouse 隐藏单列, 现在多列可隐藏, 暴露更多裸 querySelector。
- **验证**: 调拨 12 列(10 业务) + 16 行 + 零报错 + 双侧库存数据加载 + 数量5>0 双侧红; 访销 16 列(14 业务) 无回归。
- **★测试教训(再次踩坑)**: 浏览器 eval `u.value='admin'; b.click()` **不触发 input 事件**, 登录逻辑拿不到输入值, 实际是"假登录"→Promise.all 全 401 但不抛错→errs 空, UI 能开但数据全空。要点: 用 browser-use input/click **真实键入**。
- **P0 待补**: 司机/拟办人字段; P1: 信息架构分组+备注独立行; 库存预警增强。
- v107.125 P0 余项: 调拨补司机(showDriver:true, 提交并入 note '司机=xxx' — transfer_orders 表无 driver 列)+经办人(引擎 operatorLabel 配置化, transfer='经办人'); 验证字段区=*经办人/*部门/*调出仓/*调入仓/订单时间/送货司机/备注 与舟谱对齐; commit `dc0dacc`, 部署 main-C_5LtDMV.js。P0 全部完成。P1 待做: 信息架构分组+备注独立行+字段顺序(舟谱:调出/调入/日期/司机/拟办人 在前)、视觉打磨。
- v107.125 P1: 字段排序(grid order)+ 备注独立行(grid-column:1/-1, _ord/_noteStyle helper); styles.css 全局视觉打磨(.vsf-fields gap 8/12→10/14, .vsf-items td padding 8/10→9/12 height 40→42, .vsf-r 700→800+13px); 验证左偏移 235/472/710/947/1184/1421 = 调出仓→调入仓→订单时间→送货司机→经办人→部门, 备注跨行 width 1647; 访销无回归; 部署 main-BHtv-fbF.js + main-DWmeGzR9.css; commit `857b1bf`。
- v107.126: 7 模块对齐舟谱(用户重发 7 张 portal.zhoupudata.com 截图, 逐页优化) — commit `b6d33fe`, 部署 main-T0KmLP68.js:
  · 7 模块 config: visit_sale_return/car_sale/car_sale_return/self_sale/self_sale_return/purchase/purchase_return 补 showDriver/showPayment/showQty2/showAmount/noteFullRow/showPaySummary/showExpectedDate/showStockSmallUnit/showStockLargeUnit
  · 引擎新增 3 模板块: 预计生产日期(datetime-local)+采购汇总 vsf-card(应付款合计自动算+预付金额用户填)+双单位库存 4 列(可用/实际 × 小/大单位)
  · _vsfLoadStock 扩展: 一次 /inventory/available 拿 quantity 后按 prod.medium_ratio/large_ratio 拆 4 列(后端无小/大单位独立库存, 前端估算, 标签诚实)
  · 7 模块浏览器实测全通过(标题/字段/列/备注独立行/双单位列数据), errs 空
  · 剩余 P2: 实际库存列语义(舟谱可用+实际双列, 我们 quantity 单口径; 后端 inventory/available 不区分预订单占用)
  · 教训: bash commit message 含 / 触发的字符会被当作 history expansion, 用 commit -m 时避免这些特殊字符
- v107.127: 访销退单备注改回并列 — 用户反馈 v107.126 加的 noteFullRow 备注独占一行太浪费; 移除 noteFullRow → grid 自然换行(第二行与退货原因/原订单并列), 部署 main-BcrDOD73.js, commit `fc521d7`。**教训: 对齐舟谱的 noteFullRow 不是所有模块都适用, 用户实操体验优先于截图对齐**; 车销退单/自提退单/采购退单保留 noteFullRow(用户未反馈)。
- v107.127 补: 备注字段移到原订单之后(引擎模板 isReturn 行提到 showNote 前), 退单字段流=退货原因→原订单→备注, 三者同排一行(left 235/472/710), 部署 main-DOIYsJ8d.js, commit `b635712`。
- v107.127 补: 车销退单/自提退单/采购退单移除 noteFullRow(用户要求同访销退单), 部署 main-N1GtIq0U.js, commit `e880955`; transfer 保留 noteFullRow。**4 个退单模块(访销/车销/自提/采购)备注全部并列排布, 仅调拨独立行**。
- v107.128: 移除订单表单'商品明细'卡片标题 — 引擎模板共用段 (app.js:5766) 删 1 行 → 9 模块全覆盖, 部署 main-DRLbedco.js, commit `90658c5`。**注**: 用户文字写'订单明细'但截图红框是'商品明细', 按截图处理; 其他位置的'商品明细'(报价单/列设置面板)不动。
- v107.129: 自提订单列表 P0 三项 — commit `73395a5`, 部署 main-BVTJPjNO.js:
  · **客户名 undefined 根因**: `ICONS.copyable` 字典缺失(图标收编 5c04e20 时遗漏 copyable 键) → saleRows/poRows 拼接 `customer_name + undefined` → '侯建国undefined'. 补 feather copy SVG。
  · 提货日期列: render_sales 表头+saleRows 加 delivery_date(引擎提交已带, v4 迁移字段)
  · 状态映射补全 8 状态中文 + badge 色块(draft灰/pending橙/delivered蓝/signed绿/rejected+returned红/cancelled橙)
  · **教训**: 图标收编时漏键导致所有引用处 undefined 拼接 — 以后删/改 ICONS 键要全局 grep 引用
- v107.130: 自提订单列表筛选区 + 修复 tab 过滤/分页顺序 bug — commit `ff4670f`, 部署 main-CsbBin9-.js:
  · render_sales 加 filterBar(客户/单号关键字+日期起止+重置, 前端 _salesQ 内存过滤)
  · **修复潜在 bug**: 原分页 slice 在 tab 类型过滤之前 → 类型过滤不作用于列表(可能混入非自提单); 重排=类型过滤→query→分页
  · 提货核销路径已存在(delivered→签收按钮=核销), 无需新增
  · 待后端: 取消订单端点(sale_orders 有 cancelled 状态但无 cancel API)
- v107.131: 自提订单列表 P1-P3 全量 — commit `70c4a28`, 前端 main-DloRyozj.js, 后端重启:
  · **后端取消闭环**: sale_order_cancel(draft/confirmed/pending_approval 可取消)+POST /sale-orders/{oid}/cancel
  · **前端**: 排序(列头点击 订单号/客户/日期/金额 _salesSort)、合计行(过滤后金额/未收/单数)、列设置齿轮(提货日期/未收/备注显隐 localStorage)、取消/复制单号/打印配送单按钮、备注列
  · **★scp 部署教训**: `scp a.py b.py user@host:/dir/` 两个同名文件扁平化到 /dir/ 相互覆盖且不进子目录 → 必须分别指定完整目标路径(server/db/queries/sales.py 和 server/routers/sales.py 同名不同目录!)
  · 验证: 排序/合计/列面板/按钮全通过; cancel 端点 404 语义=业务 Not Found(路由已注册)
