# 2026-09-03 工作日志

## 品牌/商品目标「阶梯返利」规则档位编辑器——前端补全并部署
- **触发**：用户确认「直接补上编辑器」——要在创建品牌目标/商品目标选「阶梯返利」时，能按达成率分档配置（例：9月蒙牛低温目标100万，90%→10%、100%→15%）。
- **现状诊断**：后端早已支持规则档位（`rebate_target_rules.tiers_json`），档位用 `from_pct`/`to_pct`（达成率小数）+ `rebate_rate`，且有阶梯版本管理 API；但前端规则创建表单只让选 `trigger_mode='tiered'` 并填单一返利率，**没有档位编辑界面**，能填档位的「阶梯设置」弹窗只挂在年度合同（绝对金额口径）。属"半完成"功能。
- **改动文件**：`hergent-cn-v2/src/pages/Rebate.vue`（7 处，单条 Edit 逐条落盘 + Read 复验；本轮 Bash 工具报 "command expected string" 故障，改用子代理完成构建部署）。
  - 新增 `ruleTiers` ref + `parseRuleTiers()`（DB 小数↔UI 百分比，落库转小数）。
  - `openCreate/openEdit/dup` 初始化/解析档位；`save()` 在发送前把 `ruleTiers` 序列化为 `tiers_json`（to_pct=0→哨兵 999 表示∞，规避后端 `from_pct<to_pct` 严格校验），并做前端校验。
  - 模板：阶梯模式下隐藏单值「返利值」行，插入「阶梯档位（按达成率）」编辑区（`from_pct/to_pct` + 按 `rebate_basis` 切换比例/固定金额，末档结束达成率填 0=∞）。
- **关键业务口径**：用户例子对应「计法=全量按档(non_graduated)」——达成100万整单×15%、90万整单×10%；若选「累进」则100万只算(100-90)万×10%=1万，不符预期。已在 UI 提示文案说明。
- **后端不动**：用哨兵值 999 规避 `to_pct=0` 被后端拒绝，计算层末档 `_band_hi` 仍当 ∞，行为一致。
- **构建部署**：`npx vite build` 通过 → rsync `--delete` 至 `/opt/hergent-cn-v2` + chown → 远程 grep 确认「阶梯档位（按达成率）」已上线（`ruleTiers` 因压缩改名属正常）。
- **结论**：品牌/商品目标规则现已可配置达成率分档返利，用户例子可直接配出。刷新页面（强刷）即可见。

## 返利冲刺看板「剩余下单机会」计算错误修复
- **触发**：用户报「返利充值面板」显示"下单截止 9月3号、只剩 1 次下单机会、均单 108万"——明显不对，下单窗口应更长。
- **根因**：`Forecast.vue` 的 `rebateSprintOrders`（原 5087-5094 行）用 `cross.period.order_end`（当前这一单报单期次的 order_end=2026-09-03）当"剩余下单窗口"终点。今天9月3号窗口不足1天 → `Math.max(1,…)` 兜底成 1 次，均单=缺口÷1=108万。
- **修正口径**：剩余窗口应到"返利目标规则生效期末日(effective_end)"，即活动/合同截止，而非单个报单期次 order_end。新增 `rebateCampaignEnd` computed（取活跃规则 effective_end 最大者，无则回退 period.order_end）；`rebateSprintOrders` 改用它。面板文案"下单截止"→"返利周期截止"，显示该日期。
- **用户拍板**：窗口截止取**9月底 2026-09-30**（保持月度目标语义，不动数据库）。生产核对：蒙牛低温(rule8)/简爱(rule9) 生效期均为 2026-09-01~2026-09-30。
- **构建部署**：`npx vite build` 通过 → rsync `--delete` + chown 至 `/opt/hergent-cn-v2` → 远程 grep 确认「返利周期截止」已上线。效果：9月3日→9月30日约 14 次下单，均单≈7.7万（原108万）。
- **备注**：若日后要算到一月底，需把目标生效期改到 2027-01-31（用户本轮未选，保持月度）。

## 招投标雷达（商机订阅）——需求验证原型
- **用户提问**：低温奶参与招投标，能否做页面定时抓 ccgp 等网站招投标信息让客户及时掌握。
- **结论/定位**：能做，但只能做成"招投标雷达/商机订阅"卡片（监控+导流），不能做"数据聚合产品"——踩合规红线（政务数据不可整篇搬运转卖）+ 运维坑（反爬/异构源）+ 战略偏离（护城河是配方算法非爬虫）。先需求验证。
- **探针发现**：生产机 IP 首次裸请求 ccgp 即被"访问过于频繁"限频；带浏览器 UA+Referer 后正常返回（**头敏感+限频**，非硬封 IP）。印证爬虫运维成本真实。
- **产出脚本**：`/Users/zhangjunfeng/Documents/hergent-erp/server/bid_radar_collect.py`（stdlib-only，浏览器头+3s 礼貌限速，输出 Markdown 清单：标题/采购人/地区/发布日/类型/原文链接；合规只摘要+链接）。已 scp 生产 `/opt/hergent-erp/bid_radar_collect.py`。
- **验证结果**：近 30 天 6 关键词（牛奶/酸奶/学生奶/乳制品/鲜奶/低温奶）命中 **62 条**，绝大多数是真商机（学校营养改善计划/学生奶、医院食堂乳制品、幼儿园、单位食堂）；混有少量"乳制品精深加工生产线/加工设备"CAPEX 噪声→产品里需过滤。
- **样本**：`/Users/zhangjunfeng/Documents/laozhangai-product/招投标雷达验证样本-2026-09-03.md`（生产机 `/tmp/bid_sample.md`）。发给微信精准经销商群验证需求。
- **下一步（待用户定）**：①群里看要不要；②要做产品内页则加调度+落库+副驾卡片+AI问答；③过滤 CAPEX 噪声；④如需持续新鲜度再上每周定时采集（暂未建 automation）。

## 招投标雷达——产品化完成（"有人要"后落地）
- **架构（解耦）**：采集器(独立 sqlite + 系统 cron) ←→ ERP 后端只做只读 API ←→ 副驾前端页。不污染业务库。
- **后端**：`server/routers/bid_radar.py` 新增 `GET /api/bid-radar`（region/type/keyword/date_from/date_to/capex/page 过滤+维度下拉），读 `/opt/hergent-erp/bid_radar.db`（只读 URI 打开，避写锁）；挂 `_PATH_MODULE_MAP` → `data` 模块；`server.py` import+include_router。
- **采集器升级**：`bid_radar_collect.py` 加 `store()`（UPSERT bid_notices 表，标记 is_capex，清理>120天）；CAPEX 负向关键词（加工设备/生产线/精深加工…）自动标记，前端默认过滤；DB 默认路径 `/opt/hergent-erp/bid_radar.db`（修了一处 `..` 误指 /opt 的 bug）。
- **前端**：`hergent-cn-v2/src/pages/BidRadar.vue`（列表+筛选栏+类型色标签 活跃青/成交绿/其他灰+原文链接+合规脚注）；`router/index.js` 加 `bid-radar` 路由；`Shell.vue` 侧栏+移动抽屉加「招投标雷达」入口。
- **部署**：`deploy.sh` 部署后端 + `vite build` + rsync 前端；生产机跑一次采集器填 62 条（9 CAPEX）；系统 cron `10 7 * * *` 每日跑采集器（crond active）。
- **真机验证**：agent-browser 无头 Chrome 登 demo → 进 `/#/bid-radar`，表格渲染 53 条（默认排除 CAPEX）、筛选下拉含地区/类型、行含真实商机（无锡市第二人民医院/浙江省人民医院…），**零 console error**；API 验 401无token / 200有token(total53) / capex=1→62。截图 `招投标雷达-真机验证.png`。
- **合规**：仅摘要+原文链接，不整篇搬运；头敏感+限频（带浏览器头+3s 限速）。
- **已知限制/待办**：①详情页预算/截止日需抓详情（phase2，增加限频风险）；②多源（军队采购网/省市交易中心）未接；③AI 副驾问答「本周湖北有哪些学校奶招标」未接（data 已在库，加 hermes 工具即可）。

## 招投标雷达——地区解析修复（用户反馈"可选择的地区不全"）
- **根因（实读量化）**：`_region()` 只在 buyer+title 里匹配**省名**，而公告写的是市/县/校名（无锡→江苏、永清→河北、旬阳→陕西、红原→四川…）→ 62 条里 **52 条(84%) region='—'**；且 API 下拉用 `SELECT DISTINCT region`，只列得出 6 个省。
- **修复（三处）**：
  1. 新增 `server/bid_region.py`：全国 34 省全量 + **340+ 地级市/县级地名 → 省份**映射 + **知名高校 → 省份**映射（西南交通大学这类校名无地名词）。长名优先索引（长度降序）解决歧义：海南州(青海) vs 海南(省)、吉林市 vs 吉林(省)。采购人优先于标题。
  2. 采集器 `bid_radar_collect.py`：删内联 PROVINCES 改 import `bid_region`；新增 `backfill_regions()` + `--backfill-regions` flag（**零网络请求**重算库里 region='—' 的历史行，store 后自动跑一次）。
  3. `routers/bid_radar.py`：下拉改为返回**全量 34 省 + 各省条数** `[{name,value,count}]`，按 count 降序（无数据保持原序置尾）；计数用 base_where（**不含 region 自身筛选**，切换地区时各省条数稳定）；未知归为「其他」。前端 `BidRadar.vue` 渲染 `省名（N）`、count=0 显示「（无）」并 disabled，标题显示「全部地区（N 个省有商机）」。
- **效果**：覆盖率 **16%(10/62) → 100%(62/62)**；生产 backfill 52/52；下拉 34 项、15 省有数据。
- **踩坑**：ccgp 限频窗口比想象长——连续两次探测后生产机 IP 被判"访问过于频繁"，sleep 20s 仍未恢复。**启示：解析器迭代必须走"拉库到本地离线验证"路线，不能靠重抓**；`--backfill-regions` 正是为此设计（规则升级后零请求修补历史）。
- **验证**：自测 16/16；API 34 项/15 省命中/湖北过滤返 2 条（秭归县民政局）；真机 agent-browser 选「河北」→ 共 11 条全为河北公告（永清县/平泉市），零 console error。截图 `招投标雷达-地区修复验证.png`。

## 返利冲刺看板 L1+L2：达成改「到货口径」+ 各品牌到货周期（任务 #230→#235）
- **L1（达成按到货）**：冲刺看板月份归属由「下单月(order_start)」改为「到货月(arrival_date 优先)」——`rebateSprintMonth`/`sprintAchvMonth` computed + `watch(rebateSprintMonth, loadRebateAchievements)` 防期次切换竞态；`loadRebateAchievements` 改用到货月查 `rebate_achievements`。
- **L2（各品牌到货周期）**：`rebate_target_rules` 加 `arrival_cadence_days`(0=用全局默认) + `arrival_base_dow`（如 2,4,6，1-7）。冲刺看板每行按各规则各自周期算 `orders=ceil(daysLeft/cadence)`、`perOrder=gap/orders`，表头加「到货周期」列。全局默认天数走 `system_config` KV（键 `default_arrival_cadence_days`，默认 "2"），复用既有表规避新建表迁移风险。
- **后端改动**（真实目录 `/Users/zhangjunfeng/Documents/hergent-erp/server/`，非 laozhangai-product 旧副本）：
  - `erp_db.py`：两处 CREATE TABLE 加列；三处 ALTER patch（`tenant patch 循环`/`v116` 主库 ALTER/`_ensure_rebate_columns`）+ 幂等降级；新增 `get_tenant_param`/`set_tenant_param`。
  - `routers/rebate_rules.py`：`_row_to_dict`(老库降级) / `validate_rule` / `create_rule` / `update_rule` / `/meta` 五处支持两新字段。
  - `server.py`：`_PATH_MODULE_MAP` 加 `"/api/params":"data"`；新增 GET/PUT `/api/params`（PUT 校验 1-30 整数，否则 422）。
- **前端改动**：`Rebate.vue` 规则表单加「到货周期(天)」「基准到货星期」+ 全局默认到货周期设置条（`loadDefaultCadence`/`saveDefaultCadence` PUT `/api/params`）；`Forecast.vue` 删 `REBATE_CADENCE_DAYS` 常量改 `rebateGlobalCadence`+`loadTenantParams`，L1 到货月归属 + L2 各行 cadence/orders/perOrder。
- **部署验证（全绿）**：`npx vite build`(node22)→rsync `--delete` 至 `/opt/hergent-cn-v2`+chown；后端走 `deploy.sh`（安全变体，排除 .env/*.db，未出 2026-08-22 事故）；服务 `active(running)` 健康 `HTTP 200`；**11 个租户库 `rebate_target_rules` 均含两新列**；`system_config` 全库存在；KV 往返 get/set/reset=2/3/2 正常；`/api/params` GET/PUT 与 `/api/rebate-rules` 均 401（路由已注册且受 `_auth` 拦截）。
- **带登录态真机浏览器验证（agent-browser 无头 Chrome）**：
  - `POST /api/auth/demo-login` 取 demo 租户 boss token；进入 `#/rebate` → 切换「目标规则」tab，页面渲染「全局默认到货周期」设置卡 +「到货周期」文案 + 新建按钮，**零 console error**。
  - 临时将 demo rule 1 的 `effective_end` 由 2026-08-31 改为 2026-09-30 以模拟当月有目标；刷新 `#/forecast` 后，返利冲刺看板渲染「到货周期」列头 +「返利冲刺看板」+「剩余 N 次到货」建议文案，**零 console error**；证明 L1（到货月口径）与 L2（各品牌周期/均单追加）前端渲染链路正常。
  - 验证后已将 demo rule 恢复为 `effective_end=2026-08-31`（status=expired），未污染真实用户数据。
- **范围拍板**：L3（下游经销商维度到货周期，配客户档案）下一轮；本轮仅 L1+L2。

## 招投标雷达——多源接入（军队采购网 plap.mil.cn 落地）
- **用户要接**：军队采购网 / 各省市交易中心 / 高校平台 / 阳光平台。实测探针逐站定生死：
  - **ggzy 全国公共资源交易平台**：已覆盖 各省市交易中心 + 央企招投标 + 高校（单一源解决用户 3/4 的诉求），JSON API 稳定 → 不用再动。
  - **ccgp 中国政府采购网**：覆盖 政府采购 + 高校(中央本级) → 已接。
  - **plap 军队采购网**：旧域名 plap.cn 拒绝连接(111)，**plap.mil.cn 可达(200)** 且 freecms REST `selectInfoMoreChannel.do?title=&pageNo=&pageSize=` 返回 JSON（含 noticeTime 发布日 + pageurl 详情相对路径）。→ **新接入**。
  - **南方电网 bidding.csg.cn**：可达但只招电力设备，与低温奶无关 → **不接**（接了只增噪声）。
  - **中国电信阳光采购 caigou.chinatelecom.com.cn**：SPA + 需登录 token(401) → **不接**。
  - **中国移动 b2b.10086.cn**：生产机不可达(000) → 不接。
  - **高校专有平台**：分散、北科大等不可达(000)，但 ccgp中央本级 + ggzy 已覆盖高校采购 → 不再单独爬。
- **新增 `PlapSource(BaseSource)`**（`server/bid_sources.py`）：REST 取 `pageurl` 拼 `https://www.plap.mil.cn` 得详情URL；`noticeTime[:10]`→date；标题正则推断 type（招标/中标/竞争性谈判/采购意向…）；`resolve_region` 从标题/采购人推省份（部队代号无地名→"—"，可接受）。**专属关键词**=牛奶/乳制品/酸奶/鲜奶/低温奶/学生奶/**副食品/给养**（军队常把牛奶塞进「副食品集中配送包」，标题只写"副食品"不写"牛奶"，不补会漏掉大量商机）。
- **两处配套修复**：① `给养器材/给养设备` 加进 `CAPEX_NEG`（军队"给养"含装备非食品，默认过滤）→ plap 34 条里 10 条装备被正确标 CAPEX，剩 24 条真实军队食品/牛奶商机；② `_http_get` 加 **5xx 退避重试**（军队网偶发 502）。
- **采集结果**（30天）：plap **34 条**，其中 24 条真商机（驻延安/商洛 副食品配送含牛奶、长春某学校乳制品、自助酸奶饮品采购等）；真实样本：`驻延安地区副食品集中配送包2…饮品、牛奶等`、`商洛市副食品集中配送项目-2包牛奶、饮料…`。
- **顺手修了一个隐藏 bug**：`routers/bid_radar.py` 来源下拉 return 的 listcomp 用了 `x["k"]/x["n"]/x["c"]`，但 `sources` 在 dedupe 修复时已改成 `{value,name,count}` 结构 → **每次调用都 KeyError:'k' 抛 500**（dedupe 那轮引入、之前未被发现）。改为 `x["value"]/x["name"]/x["count"]`，重启服务修复。
- **部署**：`bid_sources.py` + `routers/bid_radar.py` + `bid_radar_collect.py` scp 至 `/opt/hergent-erp` + chown hergent；清 `__pycache__` 后 `systemctl restart hergent-erp`。
- **验证（API 端到端）**：`GET /api/bid-radar` → 200，total=**138**(默认去重过滤CAPEX)，`sources` 下拉=**[ggzy 114, ccgp 53, plap 24]**，24 省有数据；`?source=plap`→24；`?region=陕西` 正常。前端 source 下拉**数据驱动**(`v-for="s in meta.sources"`)→**零前端改动**，新源自动出现。DB 总 252(ccgp62/ggzy156/plap34)。
- **未做**：无头浏览器真机截图（本环境 agent-browser CLI 未安装、需代理下 Chromium，成本高）；但前端数据驱动 + API 契约已验证，回归风险极低。cron `10 7 * * *` 已含 plap 全源采集。

## 招投标雷达——收尾：AI 副驾问答 + 前端高亮（全做落地）
- **AI 副驾问答**：新增 `server/bid_radar_ask.py`（stdlib，解析 时间窗/来源/省份/关键词 → 查 `bid_radar.db` 返回 原文链接清单，LIMIT 15），scp 至 `/opt/hergent-erp` + chmod 755 + chown hergent，冒烟通过（「本周湖北学校牛奶招标」→4 条；「军队采购网最近牛奶招标」→0 条，因军队牛奶商机已超 7 天默认窗，行为正确）。
- **Hermes skill 注册**：`/tmp/hergent-bid-radar-SKILL.md` 经 scp 落到生产 `/tmp` 再 `cp` 进 `/root/.hermes/skills/hergent-bid-radar/SKILL.md`（root:root 700/644）；`ls /root/.hermes/skills/` 已含 `hergent-bid-radar`。副驾现在能直接回答「本周湖北有哪些学校/军队牛奶招标」类问题。
- **前端增强**（接上一轮两个待确认项）：`hergent-cn-v2/src/pages/BidRadar.vue` 修复了表格「来源列」缺失的隐患（7 表头/6 单元格错位），新增 `todayCount` + 红色「新」徽标（it.date==今日）+ 两个快捷 chip（军队采购网 / 仅看今日新增），source 标签色 `src-plap` 红。已 `vite build` + rsync + chown 部署。
- **生产复核全绿**：DB `bid_notices` 252 行(ccgp62/ggzy156/plap34)；`/api/bid-radar` 鉴权后 200（带 token；无 token 401 属正常鉴权门）；前端资产 `BidRadar-awlSAmAS.js`+`.css` grep 确认含「今日新增」「src-plap」；cron `10 7 * * *` 在。
- **结论**：用户最初四条诉求（军队采购网 / 各省市交易中心 / 高校 / 阳光平台）已全部覆盖——plap 直采军队网、ggzy 单源覆盖 各省市交易中心+央企阳光+高校、ccgp 补政府/中央高校；并提供「今日新增」高亮 + 副驾自然语言问答两条增值能力。

## 返利规则列表排序：新规则置顶
- **需求**：规则列表「新规则放上面、旧规则放下面」，便于查看。
- **改动**：`server/routers/rebate_rules.py` `list_rules` 的 `ORDER BY priority DESC, id` → `ORDER BY priority DESC, id DESC`（仅改同优先级内的次级排序，不影响「优先级」字段置顶功能）。
- **部署**：`bash deploy.sh`（安全变体，排除 .env/*.db）已落地；`rebate_rules.py` 时间戳 16:55 确认生效；`/api/health` 200。前端无需重建。多规则时同优先级下 id 倒序=新规则在上。

## 冲刺面板接「到货模式」引擎（v118 收尾）
- **问题**：简爱改成「按星期」模式后，冲刺面板仍显示"2天/次"。根因=冲刺计算 `rebateSprint` 旧代码只读了 `arrival_cadence_days`，完全无视 `arrival_mode`。
- **改动**：`Forecast.vue` 的 `rebateSprint` 增加按 `arrival_mode` 分支——weekday 模式用 `parseArrivalWeekdays`+`countWeekdayArrivalsInWindow`（[今天,到货截止日] 闭区间命中星期计数），文案 `cadenceLabel` 改为「每周 N 次（周X、周Y）」；interval 模式保留「N 天/次」。星期契约与后端 `domain/arrival_schedule.py` 一致（1=周一..7=周日）。
- **部署纪律**：本次 rsync 部署用「本地 md5 vs 服务器 md5 比对」确认落地（此前多次 rsync 未生效导致旧 bundle 在线）。Forecast-n5n-JkFL.js / Rebate-zhWgzDzW.js 均 MATCH_OK。
- **生产核对**：tenant_1 简爱两条规则——id=7(interval,8月生效,9月被月份过滤)、id=9(weekday,'2,5',9月生效)。9月冲刺仅显示 id=9 → 「每周 2 次（周二、周五）」，剩余到货 8 次（9/3→9/30）。

## 预报汇总表工具栏：导入Excel→导入 + 新增导出按钮
- **需求**：工具栏「导入Excel」改为「导入」；新增「导出」按钮，导出当前预报订单汇总表为 Excel。
- **改动**：`Forecast.vue` 工具栏(原:37)加「导出」按钮（icon download，title 说明），新增 `exportAllXlsx()`（紧挨 `ctxExportSel`，复用 `buildXlsx(cross.value.rows, '<期次>_汇总')`，受筛选影响、沿用既有列/金额/建议口径）。
- **部署**：`npm run build`（Forecast-gmpq7U7s.js）→ rsync + chown，本地 vs 服务器 md5 `MATCH_OK`（落地确认）。
- **E2E**：无头浏览器确认按钮「导入/导出/导入汇总」均在，点「导出」→ toast「已导出预报订单汇总表」，无 console error。

## 字段语义对齐：厂家编码(product_code)
- 用户明确：汇总表「厂家编码」= 该商品在**上游厂家订单系统**里的编码，用途是**直接复制粘贴到厂家系统去下单**（非 Hergent 内部 ID、非条码）。
- 与现有设计一致：Forecast.vue:3384「一键复制厂家下单文本」本就按「厂家编码+数量」生成，可直粘厂家系统。已写入 MEMORY.md「关键字段语义」节，后续凡"对接厂家系统/下单/导入厂家订单"的字段映射必须优先 product_code、缺失才回退条码。

## 导入/导出图标优化（视觉一致性）
- **问题**：汇总表工具栏 导入(import=文档+下箭头)、导出(download=托盘+下箭头) 两个都是"向下箭头"，长得像、且 导入 方向反了（应是数据进系统）。
- **改动**：`Forecast.vue:37-38` 导入图标 `import`→`upload`(向上箭头，托盘同族)，导出保持 `download`(向下箭头)；并补两按钮 title。形成"上=导入/下=导出"的对称图标对，同族易辨。
- **部署**：`npm run build`(Forecast-D2G0ZuA_.js)→rsync+chown，本地 vs 服务器 md5 `MATCH_OK`。

## 校验按钮：进编辑态自动跑一次（角标即时显示）
- **需求**：用户选方案2——进入编辑态时自动跑一次全表校验并显示「待修正」角标，不用手动点「校验」。
- **改动**：`Forecast.vue` `enterEdit()` 在 `loadEditGrid()` 完成（finally）后新增 `errCount.value = validateAll().length`。原角标仅靠草稿 watch 节流 1.5s 更新，进编辑态要等输入/延迟才出；现一进编辑态即算，角标即时显示。
- **部署**：`npm run build`(Forecast-3JLMB-Fh.js)→rsync+chown，本地 vs 服务器 md5 `MATCH_OK`。

## 预报订单模块按钮审计（用户专项请求）
- **范围**：常驻主工具栏/编辑态工具栏/子页签/下拉面板入口按钮，逐项验证触发+业务逻辑，评估布局与冗余。
- **验证**：代码静态核对（所有 @click 均有真实函数，无死按钮）+ 无头 E2E（demo token 注入：`location.hash='#/forecast'` 而非 reload——守卫会把首次 goto 的 #/forecast 重定向到 #/login，reload 重载的是登录页，此坑已踩）。核心链路（改单→编辑态四按钮、校验 toast、导出 toast）E2E 通过，无 console error。
- **结论**：功能完整无死按钮；布局双 primary 失衡+编辑簇拥挤+"设置/工具箱"随模式漂移；冗余 4 类（进编辑入口×3、质量类×4、AI散落、历史×4）。
- **交付**：`预报订单模块按钮审计报告.md`（含逐项表+布局评估+重叠分析+★排序优化建议）。用户未要求落地，待其选编号。
- **E2E 坑记录**：agent-browser eval 偶发挂起（CDP timeout），单次脚本勿过长；用 `timeout` 包裹单步 + 后台跑避免前台 137 被杀。
