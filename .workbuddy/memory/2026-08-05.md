# 2026-08-05 工作日志

### 全站设计风格统一审计（v107.44, commit b944c18, main-BRh5jv0y.js, 已上线）
- 审计: 全站 19 文件含硬编码 hex; 7 文件 style 属性内 49 处老色偏离设计令牌
- 优化: 49 处 style 内老色 → 令牌(var(--t1/t2/t3/p/pl/suc/dan/war/bd/bg2/bg3)),
  双主题自动适配; 文件: app.js(13)/review-queue.js(16,偏离最重)/ai-accuracy.js(9)/
  cold-chain-zone.js(5)/customers-v2.js(3)/unified-dashboard.js(2)/payment-import.js(1)
- 方法论: 内联 style 老色替换必须"按整个 style 属性值整体处理内部全部 hex"(贪婪按前缀匹配
  只换同属性最后一个 hex — v107.37 教训重现); canvas/图表颜色数组保留具体色值不换 var()
- 可访问性基座确认无需改: *:focus-visible 全局焦点环 + btn 专用环 ✓
- 验证: node --check ×7 ✓ style 内 legacy 残留 0 ✓ 构建部署 ✓ 线上 200 ✓

### 客户/供应商档案名称品牌色链接（v107.45, commit 31694a7, main-CFeFDNJ2.js, 已上线）
- 与商品(v107.43)同款: 客户/供应商 表格行+卡片视图 名称改 .name-link 品牌色+pointer+点击编辑
- CSS: .product-name-link 选择器扩展为 .product-name-link,.name-link(共用一套视觉)
- 顺带修复回归: v107.43 移除商品行 data-id 导致右键菜单([data-id] 取 id)商品"编辑"静默失效;
  本轮商品/客户/供应商名称链接统一补 data-id 属性 → 右键编辑恢复

### 采销订单列表订单号链接跳详情（v107.46, commit 45f37aa, main-B9Yxa1mX.js, 已上线）
- 销售/采购列表字段本无行内编辑(只读 ✓); 订单号 copyable → .name-link 品牌色链接点击跳详情
- 新增 _showSaleDetail/_showPurchaseDetail 详情弹窗(复用 .modal): 头信息卡(客户/供应商/日期/状态/
  金额/已收已付/未收未付) + 商品明细表 + 按状态操作按钮(发货/签收/确认入库/费用分摊)
  _openOrderDetailModal 通用渲染, 操作后关闭+400ms 刷新列表
- 后端零改动: GET /api/sale-orders/{oid} 与 /api/purchase-orders/{oid} 早已存在(返回 {order, items})
- 覆盖: 销售/车销/自提/退单(saleRows) + 采购/退货(poRows)
- 验证: node --check ✓ 构建 ✓ 线上 200 + 两详情接口匿名 401 ✓
- 方法论: 订单详情"页"不一定要新页面 — 后端详情接口(含 items)通常已存在, 前端弹窗即可承载;
  先查 GET /api/{orders}/{id} 是否已注册再决定后端是否要动
- 验证: node --check ✓ 构建部署 ✓ 线上 200 ✓

### 访销订单页面（v107.47, commit a7f240e, main-hegqKNgR.js, 已上线）
- 参照舟谱截图: 单页全屏 modal 复刻(订单信息区 2 列网格 + 商品明细表 12 行预置 + 实时金额计算)
- 复用 searchable-wrap/initSearchableSelect/icon/esc/fmt 工具 + 全站令牌(品牌/语义/表面/文字/边框)
- 选商品自动填充: 列表缓存→/products/{id}/detail 回填条形码/单位/销售价/实际价
- 行操作: 新增/删除 + 清理 watcher + 重新编号
- 提交: POST /api/sale-orders(已存在); 访销特有字段(加急/付款/货车/地址)写入 note 字段
- mega-sale-order 入口改用 showVisitSaleForm(替代通用 5tab showSaleForm)

### 访销订单接口 404 修复（v107.48, commit 073227d, main-DbhFuaGN.js, 已上线）
- 症状: /api/warehouses 404×2 → 访销页 Promise.all 失败整个初始化不执行(员工空/商品空)
- 根因: 客户端 api('/warehouses') 多处早已调用(app.js 5632/5817), 后端只有 /warehouses/full 无裸路由
- 修复: ①后端 inventory.py 加 @router.get('/warehouses') → db.get_warehouses_full()(同源兼容)
  ②前端访销页 Promise.all 改各接口独立 .catch(()=>[])(单个失败不阻断整页)
- 线上验证: warehouses 匿名 404 → 401 ✓
- 方法论: 客户端调用过的接口名必须回查后端路由是否真存在(裸路由 vs /xxx/full 是典型坑);

### searchable filter 炸裂修复（v107.49, commit f4bce3d, main-DrpYsyzk.js, 已上线）
- 症状: 搜索框输入时 Uncaught TypeError: S.filter is not a function (main-*.js:317 L)
- 根因: GET /api/contacts 返回 {items,total} 对象, 但 9 处旧调用(sale/purchase/预收预付/费用单/
  new-features×4)用 r||[] 把对象传进 initSearchableSelect → items.filter 炸(仅输入时触发, 长期潜伏)
- 修复双层: ①组件级防御 dataPromise.then 改 items=Array.isArray(data)?data:
  (data&&Array.isArray(data.items)?data.items:[]) — 根治全部 12+ 调用点
  ②9 处 /contacts 调用 r||[] → (r&&r.items)||r||[]

### mega 菜单"+创建"按钮无效修复（v107.50, commit 82e5e3d, main-2PCbzCeh.js, 已上线）
- 症状: 侧边栏采销管理→访销订单旁 "+创建" 按钮弹出的是 Hergent 原 5tab 销售单向导, 不是舟谱访销订单
- 根因: mega 菜单由 js/modules/trade-mega.js 独立模块渲染, 自带硬编码 mega 菜单数据;
  v107.47 我只改了 app.js:517, 而 trade-mega.js:19 仍硬编码 create:"showSaleForm()"
- 修复: trade-mega.js:19 create 改 showVisitSaleForm()
- 教训: 共享数据多源时改一处必须全栈 grep 同步(trade-mega.js 自带数据 vs app.js TRADE_MEGA_COLS)
- 验证: node --check ✓ 构建 ✓ 线上 200 ✓

### 访销订单全屏铺开录入界面（v107.51, commit f4ce576, main-BDYKV1Tv.js, 已上线）
- 需求: 录单高频, 替代弹窗; 全屏铺开 + 保留左侧主导航
- 改造: modal-overlay(居中+圆角+阴影) → position:fixed left:var(--sw) top:0 right:0 bottom:0
  让出 sidebar 区(sidebar 仍可见, 主区满铺); 去掉圆角+阴影+居中
- 顶部: 浅色标签条(filter-chip 风格) + '自营'绿chip + 创建/上传附件/✕按钮
- 字段: 2列网格 → 5列紧凑网格(3行), 对齐截图:
  第1行: 客户*/业务员*/部门*/单据日期/预提货日期
  第2行: 送货司机/送货车辆/收货地址/付款方式/是否加急
  第3行: 欢数/营销类型/销售仓库*/备注(跨2列)/复选框区
- 商品明细: 12行 → 16行预置
- 新增 _vsfClose: 点✕或保存后关闭+刷新销售列表
- 新增 vsf-driver 司机字段(演示版空数据, 搜索接口暂缺)
- 验证: node --check ✓ 5 处新引用 ✓ 构建 ✓ 线上 200 ✓
- 方法论: 高频录入页用全屏(modal-overlay 居中遮盖)= 错误; 让出 sidebar 用 fixed left:var(--sw) 是正确姿势
- 演示版边界: 司机/地址/车辆 搜索接口未实现, 仅占位; '改订单金额时变金额'/'按小单位金额' 复选框 hooks 预留

### 访销订单业务员无法选择修复（v107.52, commit 2906b5b, main-S_BlhdQp.js, 已上线）
- 根因(3 层): ①数据: 生产 hr_employees 0 条(懒创建从未种子)→ /api/employees 空 → 下拉无选项
  ②语义: 后端 operator_id 存 display_name(姓名, B1 隔离按姓名), 前端却传员工 id
  ③体验: 空列表无声空白, 无引导
- 修复: ①生产种子 7 名员工(users→hr_employees, 排除 admin_ 公司账号/纯手机号, 岗位映射
  boss→总经理/accountant→会计/sales→业务员/warehouse→仓管/driver→司机)
  ②前端 vsf-operator 选项带 data-name, 保存 operator_id 取姓名(对齐后端惯例)
  ③空员工 toast 引导"去员工管理添加"
- 教训: SQL 跨库查询须 ATTACH(不支持 FROM erp.db 点号语法); bash 转义地狱场景先写
  .py 文件 scp 执行最稳; users 表是真实人员单一事实源, 业务员应从 users 同步而非手敲
- 验证: 生产种子 7 条 ✓ node --check ✓ 构建部署 ✓ 线上 200 ✓
- 方法论: 通用组件必须对数据形态做防御(Array.isArray), 不能假设调用点都传对;
- v107.53: 访销录入页工具栏布局优化(commit 7ee0e11, main-kHVzcD0H.js) — 根因: .vsf-field 输入控件与
  searchable-wrap input 均未设 width:100% → 输入框默认宽度不撑满格子, 显得窄且参差;
  修复: 5列内联 → .vsf-fields 类 4列网格(minmax(0,1fr)) + width:100%+box-sizing:border-box+min-height:34px
  + gap 12/18 + 响应式降列(1200→3列/900→2列/600→1列, 备注 span3 ≤1200 全宽) + 底栏 flex-wrap
  接口返回 {items,total} 是标准形态, 组件应统一兼容数组与对象两种
- 验证: node --check ×2 ✓ 构建 main-DrpYsyzk.js ✓ 线上 index 引用新 hash 200 ✓
  新页面并行接口加载务必各 .catch 独立兜底, 别用裸 Promise.all(一个 404 全挂)
- 演示版边界: 部门/车辆/地址下拉使用静态数据; 上传附件/快捷键说明 仅 toast 提示
- 验证: node --check ✓ 17 引用 ✓ 构建 ✓ 线上 200 ✓

### 访销订单全面对齐舟谱 P0+P1（v107.54, commit 1510c69, main-Bz0eEawg.js, 已上线）
- 字段区重排: 核心四必填(客户/业务员/部门/销售仓库)集中行1; 删送货司机/订单级营销类型(下移商品行)
- 欢数→欠款: 语义修正=所选客户历史欠款(contacts 列表自带 ar_balance, 客户选中 change 监听更新),
  不再误当订单合计(合计只在底部栏)
- 创建按钮 蓝→绿(对齐舟谱主操作色)
- 商品表新增 3 列: 业务属性(select) + 仓位(select 静态) + 可用库存(新接口实时查询)
- 后端新增 GET /api/inventory/available?product_id=&warehouse_id= (inventory 表 SUM quantity)
- 复选框区+快捷键迁移到商品表上方; 新增改下方插入(_vsfAddRow(anchor)); 行号列 '+→№'
- 两复选框真联动: 改订单金额时变金额(金额列变input反算实际价 _vsfAmtChanged);
  按小单位金额(盒份列按 ratio 联动, unitMode 用大单位比)
- 顺修隐藏 bug: 保存 items 用全局 i2 索引取 vsf-unit-id, 删行/插行后取到不存在元素 → 改 tr 相对查找
- 方法论: ①对比截图逐项列差距清单再实施, 优先级 P0核心/P1体验/P2可选 ②舟谱"欠款"是客户历史余额
  不是订单合计, 语义不能想当然 ③遍历收集行数据用 tr.querySelector 相对查找, 禁全局索引(增删行必错位)
- 边界: 业务属性/营销类型/仓位保存进 item 但后端只落 product_id/quantity/unit_price(多余 key 安全忽略),
  要持久化需扩展 sale_order_items 表(P2)
- v107.54b: 业务属性列移至"单位"列之后(thead+行渲染两处同步, 相对查找逻辑无需改, commit ede341d, main-BtHNo69m.js)
- v107.55: 创建访销订单页顶栏被遮挡修复 — 根因: #topBar 普通流 z-index:10 vs vsf-overlay fixed top:0 z-index:9998
  盖住; 修复 top:0 → top:var(--th)(52px); 教训: 全屏 overlay 若要让顶栏可见必须 top:var(--th) 而非 top:0,
  fixed+z-index 高≠顶栏自动可见(顶栏非 fixed 时); 全站扫描同类仅此一处 (commit 68398c1, main-D0xUxhoM.js)
- v107.56: 访销订单页看不到 #tabBar 多页签导航修复 — 产品 v105+ 已有 renderTabBar()(#tabBar 在 contentArea
  顶部, navTo→openTab, 点档案管理→客户档案顶部出现"客户档案导航"); vsf-overlay 覆盖 contentArea 遮住 tabBar;
  修复 ①overlay top → calc(var(--th) + tabBar.offsetHeight 动态高度) ②switchTab 时先移除 vsf-overlay 再跳转
  (commit e28adf0, main-CDemkqiU.js); 教训: 全屏 overlay 让位要数清被盖的层级(顶栏→tabBar→内容), 逐个让出
