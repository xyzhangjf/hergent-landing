# 2026-08-22 工作日志

## Forecast.vue 网格增强（全部落地 + 部署生产）
- 完成 18 项网格增强：基础 8 项（撤销/重做、校验、区域复制、金额列、导出 Excel、草稿、筛选排序+口径、方向键导航） + 智能/提效 10 项（P3-1 AI建议/P3-2 条件格式/P3-3 多期对比/P3-4 冻结小计/P3-5 快捷键填充柄/P3-6 sparkline/P4-7 批量编辑/P4-8 快照对比/P4-9 打印/P4-10 列方案）。
- 构建通过（`✓ built in 1.28s`，产物 `dist/assets/Forecast-CGUDyijG.js`）。
- 部署：`rsync dist/ → root@47.113.224.140:/opt/hergent-cn-v2/` + chown；生产 `curl` 该包 HTTP 200。
- 设计要点：`saveEdits` 只序列化 `qtyByUnit`+主档字段，新增 `suggest/history/_diff` 行字段不落库；AI 建议为可替换启发式（注释标明可由后端 recipe 替换）；列方案存 `localStorage:forecast_col_schemes`。
- 待办：登录态真机 E2E 建议用户实测（生成建议/对比/趋势/批量/快照/列方案/快捷键/打印）。

## 下一轮：4 项下一代增强（已上线生产）
- #1 配方化建议引擎：重构 `computeSuggestion(r,recipe)` 支持 4 策略（安全库存/历史均值/趋势外推/加权混合）+ 可调参数；`⚙ 建议配方` 面板选策略/调参/存命名配方（localStorage `forecast_suggest_recipe_<uid>`）。后端 `/api/forecast/recipe` 已留 GET/PUT 就绪钩子（前端已调用，404 时回退本地）。
- #2 任意两期对比：对比面板新增「对比期」下拉（`compareablePeriods`，来自 `forecastApi.periods`，排除当前期），保留「⇄ 上期」快捷；`loadComparePeriod(pid)` 替换原 `loadPrevPeriod`。
- #3 快照差异导出：快照栏「导出差异」按钮 → SheetJS 导出 _diff（每行每客户 Δ + 当前/对比/合计 Δ）。
- #4 列方案云端化：`SCHEME_KEY` 改为按 `auth.user.id` 租户隔离；`loadSchemes/saveScheme` 优先后端 `/api/forecast/column-schemes`，失败回退 localStorage。
- 构建 `✓ built in 1.25s`，产物 `Forecast-BUrGnMcD.js`；部署 rsync + chown；生产 `curl` HTTP 200。
- 后端两个端点（`/api/forecast/recipe`、`/api/forecast/column-schemes`）尚未部署，前端已就绪钩子，后端落地后无需改前端即可生效。

## 关联
- 本轮交付总览见 `overview.md`。
- 设计铁律（AI 只建议不擅自下单）：采纳建议按钮仅写入 units[0]，仍需人工确认提交。

## P5/P6/P7 下一代增强（10 项全部上线生产 + 后端真正部署）
- 前端 10 项全部落地 `src/pages/Forecast.vue`：P5-1 货损联动告警(lossWarn/loss-badge)、P5-2 返利行内冲刺(rebatePush)、P5-3 MOQ起订量+到货周期(moq/lead_days 行内可编辑+localStorage)、P5-4 季节同环比(loadYoY/yoyMap/yoyPct)、P6-5 提交前AI体检(healthIssues 扫描+jumpToRow)、P6-6 单元格批注(notesMap+prompt)、P6-7 网格内搜索(Ctrl+F/gridFind)、P6-8 企微飞书推送快照(pushForecast→forecastApi.push)、P7-9 大数据分页(pagingOn/pageSize=50/curPage)、P7-10 云端配方部署(forecastRecipeApi/columnSchemeApi 前端已带 localStorage 兜底)。
- 构建 `✓ built in 1.29s`，产物 `Forecast-Bin6FKPn.js`（gzip 124.71kB）；`rsync dist/ → /opt/hergent-cn-v2/` + chown；生产 `curl https://hergent.cn/assets/Forecast-Bin6FKPn.js` HTTP 200，9/10 特性字符串已编入产物验证。
- **后端真正部署（收尾 P7-10 + 补 P6-8 推送通道）**：
  - 新建 `server/routers/forecast_config.py`：per-tenant `forecast_config(kind PRIMARY KEY, payload, updated_at)` 表；`GET/PUT /api/forecast/recipe`、`GET/PUT /api/forecast/column-schemes`、`POST /api/forecast/push`（复用 feishu_notify.push_to_wecom/push_to_feishu，失败 soft_fail 不阻断）。
  - `server.py` 补 `from routers.forecast_config import router as forecast_config_router` 并 `app.include_router(forecast_config_router)`。
  - 经 `bash deploy.sh` 安全部署（保留 .env/DB，systemctl restart）；首跑 health 因 3s sleep 过短显示 HTTP 000，复检 HTTP 200；三路由未授权探测均 401 → 确认已挂载。
  - 此举使「配方/列方案」从前端 localStorage 兜底升级为真正 per-tenant 云端随租户下发；推送审批不再仅剪贴板降级，可真发企微/飞书（通道异常时静默降级）。
- 已知非阻塞：server 启动期迁移告警（webhook_events 缺表、若干 duplicate column / 历史 FAILED migration）为既有问题，与本变更无关，服务正常起（Uvicorn :8700 已起）。

## 待办 / 建议
- 登录态真机 E2E 建议用户实测（10 项新功能 + 配方/列方案云端下发 + 推送审批通知实际到达企微/飞书）。
- 推送到达确认：需用户在企微/飞书侧确认收到「预报单审批」通知（依赖 feishu_notify 通道配置）。
