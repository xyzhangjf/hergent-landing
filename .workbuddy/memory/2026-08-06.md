# 2026-08-06 工作日志

## 访销订单模块按钮审计（未改代码, 纯审查）
- 结论: 上传附件确认纯演示(_vsfUpload 仅 toast '附件上传演示版(未实现)'), 无文件选择/上传/后端
- 9 处未实现/非功能: ①上传附件 ②快捷键说明(仅'暂无') ③部门(硬编码3假部门) ④送货车辆(initSearchableSelect([])空)
  ⑤送货司机(HTML有但从未initSearchableSelect → 死控件) ⑥收货地址(空) ⑦行复选框+全选(无批量动作消费)
  ⑧单据日期/预提货日期/预配送时间(保存时未读进body → 数据静默丢失bug) ⑨提交并审核(与保存同逻辑,始终draft,名实不符)
- 真发后端: 创建/保存/提交并审核(同走 _vsfSaveAndClose → POST /sale-orders) + 可用库存GET + 商品详情GET
- 报告: /Users/zhangjunfeng/Documents/laozhangai-product/outputs/访销订单模块按钮审计.md
- 方法论: 审查按钮真伪 = 沿 onclick→window._xxx 函数体看有无真实逻辑/后端调用; 特别注意'有渲染但从未 init'的死控件
  和'UI可填但保存时未读取'的数据丢失bug(比纯演示更严重)

## 访销订单多页签 tab 联动（v107.57, commit 812120c, main-nGSzj41x.js, 已上线）
- 症状: 打开访销订单创建页后, tab 栏仍显示打开前的激活页(经营看板), 不是"创建访销订单"
- 根因: 访销订单是 showVisitSaleForm 全屏 overlay, 不走 navTo/openTab → _tabs 无对应项
- 修复(5处):
  ①showVisitSaleForm 打开时注入 {id:'visit-sale-form', label:'创建访销订单'} tab + 激活 + renderTabBar
  ②_vsfClose 关闭时移除 tab + _origNavTo 回上一个 tab
  ③switchTab 点该 tab → overlay 未开则 showVisitSaleForm() 重开
  ④closeTab 点 × → 复用 _vsfClose()
  ⑤_vsfSaveAndClose 保存成功分支同样移除 tab 防残留
- 方法论: overlay 型全屏页要融入 tab 体系 = 手动管理 _tabs 生命周期(开注入/关移除/点重开),
  不能假设它走 navTo; 每个关闭路径(✕/保存成功/切tab)都要清理, 否则残留 tab

## 访销订单工具栏紧致化（v107.58, commit 024f0b9, main-CXWf7ZtC.js, 已上线）
- 任务: 减少工具栏垂直高度, 给商品明细表腾可视空间
- 优化(3处):
  ①顶部标签条 padding 8→6 + 删'自营'chip(冗余)
  ②字段区 padding 14/18→10/14, gap 12/18→8/14, label margin 4→2, input min-height 34→30, padding 7/12→5/10 — 4 行累计 ~50px
  ③复选框区(改订单金额时变金额/按小单位金额/快捷键说明)从商品明细表上方独立行合并到顶部标签条右侧,
    浅灰 | 分隔与按钮组区分, 节省独立行 36px
- 累计节省 ~110px → 1080p 屏商品表多显示 2-3 行, 减少滚动
- 功能零损失: 全部操作入口保留, money-mode/unit-mode → _vsfRecalc 联动仍生效
- 风格保持: 浅色标签条/品牌色按钮/searchable 圆角/var(--p) 沿用

## 访销订单字段水平排列（v107.59, commit 565a746, main-CVO3rcnw.js, 已上线）
- 用户要求仿舟谱截图: label 左 + input 右 同行(form-inline 风格), 而非之前的 label 上 + input 下
- 改造: .vsf-field 改 flex 横排, label 固定 84px(容 4 字 + 红*), input flex:1 占满
- 响应式: @media (max-width:768px) 自动回退到 label 上 + input 下(保证可读性)
- 4 列网格保留, 每列里字段横排; 字段区总高再省 ~52px(208→156)
- 备注 textarea 同样横向布局占 1 行的 4 列宽
- 验证: 构建 ✓ 部署 ✓ 线上 200 ✓

## 访销订单工具栏进一步紧凑化（v107.60, commit efa0c6b, main-Bg1E4GAw.js, 已上线）
- 用户反馈: 对比截图发现备注行高 80px vs 其他行 40px(视觉割裂)+ 行3 第4格空白
- 优化(5项):
  ①label 84→72px(中文 4 字 label 实际 50-60px, 缩窄给 input 空间)+ 右对齐 + padding-right 2px
  ②field gap 8→6px, grid gap 8/14→6/12, padding 10/14→8/12 — 行间更紧凑
  ③textarea min-height 34→30px + resize:none + line-height 20 + overflow-y auto — 备注行高
    从 80px 压到 30px, 与 input 行严格对齐, 长备注滚动
  ④删 textarea inline style min-height:34px + resize:vertical (CSS 统一管理)
  ⑤行3 第4格填'自定义'齿轮按钮(舟谱原参考, 演示版占位 — toast 提示, 后续可拓展
    快捷备注模板/自定义字段管理)
- 累计优化: v107.58/59/60 字段区从 ~220px(4行 label 上)→ ~120px(4行 label 左 + textarea 同高)
- 验证: node --check ✓ 构建 ✓ 部署 ✓ 线上 200 ✓

## 访销订单全盘对齐舟谱 6 列布局（v107.61, commit 2662f0d, main-B0yZyF6b.js, 已上线）
- 用户对比截图: Hergent v107.60 (4列×4行) vs 舟谱 (6列×2行), 选全盘对齐
- 核心改造:
  ①网格 4→6列, 响应式降列 1200→5/1024→4/768→3/600→2/400→1
  ②label 宽 72→60px(列更窄)
  ③字段重排 6×2 = 12 格:
    行1: 客户/业务员/部门/销售仓库/单据日期/预提货日期(核心四必填+日期)
    行2: 送货车辆/收货地址/付款方式/是否加急/备注/自定义(次要+备注+齿轮)
  ④欠款从字段区移除, 合并到底部操作栏左侧(与数据次数/合计同行, 由客户change监听维护ar_balance)
  ⑤备注从全宽单独行 → 1列宽度(与input同高30px, 长备注滚动)
  ⑥自定义按钮: 蓝色实心 → 透明+图标+文字chip(.vsf-custom-btn, 舟谱风格)
  ⑦移除 vsf-span2(无跨列字段)/vsf-note 全宽规则(备注正常1列)
- 字段区总高: v107.60 ~120px → v107.61 ~80px, 再省 ~40px
- 累计节省: v107.58/59/60/61 从 ~220px 压到 ~80px, 总省 ~140px
- 验证: node --check ✓ 构建 ✓ 部署 ✓ 线上 200 ✓
- 方法论: 全盘对齐参考产品 = 列数重排 + 字段语义重定位(欠款→底部) + 视觉重塑(实心按钮→chip),
  不能只改 CSS 不改 DOM 结构 — 字段去留需同步重排

## 访销订单红圈问题修复（v107.62, commit 287993f, main-BG3Gf_V5.js, 已上线）
- 用户用红圈标注访销订单工具栏问题: 左侧3小红圈(客户/销售仓库/收货地址 ▼箭头)
  + 右下大红圈(整片备注+自定义)
- 修复:
  ①searchable-wrap ▼ 缩小(font 10→8px, opacity:.5, right 10→7px)+ input padding-right 30→22px(给input空间)
  ②备注 label 纯化: 去掉内嵌'0/500'(改放 textarea 内右下角悬浮, position:absolute pointer-events:none)
  ③自定义按钮精简: button 内'自定义'文字去掉, 只留 ⚙ icon(label 已有'自定义'文字)
  ④textarea 强制 height:30px + line-height:20px + padding 5px + font-family:inherit — 与 input 严格同高
- 验证: node --check ✓ 构建 ✓ 部署 ✓ 线上 200 ✓

## ▼ 箭头视觉冗余终极修复（v107.63, commit 80d5473, main-CFsBQo0X.js, 已上线）
- 用户再次红圈标注 ▼ 箭头问题 → 怀疑是浏览器缓存(bundle 验证 v107.62 已部署),
  同时 v107.62 未碰日期字段的浏览器原生 ▼ 箭头
- v107.63 彻底解决 ▼ 问题:
  ①searchable-wrap .dropdown-arrow: 默认 opacity:0(完全隐藏), 仅 hover/focus-within 时
    opacity:.5 — 已选项 ▼ 完全不显眼
  ②input[type=date]::-webkit-calendar-picker-indicator: 默认 opacity:.35(focus/.7)
    — 浏览器原生日期 picker 图标淡化
- 验证: 构建 ✓ 部署 ✓ 线上 200 ✓
- 方法论: ▼ 箭头这类视觉冗余 = 默认隐藏 + hover/focus 显现, 渐进式 affordance(渐进增强),
  比"缩小并淡化"更彻底; 原生 input[type=date] 用 ::-webkit-calendar-picker-indicator 私有 vendor 控制

## ▼ 箭头常驻输入框内部（v107.64, commit fd316c2, main-_uljDZiB.js, 已上线）
- 用户反馈: v107.63 默认隐藏▼(hover才显示) 用户不认可 — 希望▼常驻输入框内部右侧(对齐 select 原生箭头)
- 修复: dropdown-arrow opacity:0 → .45(常显), hover/focus 加深 .75; right:8px + line-height:1
  垂直居中; 定位保证: searchable-wrap position:relative + input width:100% → arrow 在 input 边框内侧
- 教训: "视觉冗余"判断要听用户 — 用户觉得 select 原生箭头是标准形态, 隐藏▼反而违反直觉;
  箭头常驻+淡化 才是对(默认 .45 / hover .75)

## ▼ 箭头 box-sizing bug 修复（v107.65, commit b293a61, main-DX7sC8ep.js, 已上线）
- 用户反馈 v107.64 箭头仍然在输入框外 → 真因排查:
  .searchable-wrap input 默认 box-sizing:content-box, width:100% 不含 border+padding,
  → input 实际渲染宽度 = wrap 宽 + 22px(超出 wrap), 而 dropdown-arrow right:8px 相对 wrap 边缘
  → arrow 落在 wrap 内但**input 边框外**14px(看起来总差一点)
- 修复: input 加 box-sizing:border-box → width:100% 真正等于 wrap 宽, arrow 落在 input 内部
- 教训: input width:100% 必须配 box-sizing:border-box, 否则 border+padding 撑出容器;
  这种 bug 视觉上看起来"几乎对但差一点", 用 dev tools 量 box 尺寸最容易定位
- 验证: 构建 ✓ 部署 ✓ 线上 200 ✓

## v107.66 — ▼ 箭头默认隐藏 (6387361, bundle main-BbEVEmNR.js)
- v107.64 常显(.45) 用户反馈"显示出来不好看" → 改回默认隐藏
- dropdown-arrow opacity:.45 → 0(默认隐藏), hover/focus-within 时 .6
- 保留 v107.65 box-sizing:border-box (箭头位置仍正确, 万一未来要显也能定位对)
- 验证: 构建 ✓ 提交 ✓ 部署 ✓ 线上 bundle 含 opacity:0 ✓ (grep 误判: 先命中 HTML <span class=dropdown-arrow>▼</span> 标记, 非未上线)
- 用户偏好确认: searchable ▼ 箭头 = 隐藏态最优; 反复横跳(v63隐/v64显/v66隐)说明此细节用户要自己看实际效果定

## v107.67 — 非全屏下访销订单工具栏字段重叠修复 (7e6b6b7, bundle main-B4YCZuD2.js, css main-C760URCr.css)
- 用户截图反馈: 非全屏/分屏模式下工具栏字段互相重叠/截断(客户旁"选择"、备注"点击备"、0/500 拥挤)
- 根因分析:
  1. .vsf-fields 固定 6 列网格: 非全屏列宽被压到 <180px, label 占 60px 后 input 只剩 ~100px,
     date/select 等 input 有固有最小宽度, 撑出单元格 → 视觉重叠
  2. .vsf-field input/textarea/select 用 width:auto, 极窄 flex 子项不主动收缩到容器边界
  3. 备注 placeholder '点击备注查看更多' 过长, 窄列截成 '点击备', 与 0/500 计数器挤
- 修复:
  - .vsf-fields 改为 repeat(auto-fit, minmax(200px, 1fr)) — 宽屏自动 6 列, 窗口缩窄自动换行, 字段永不 <200px
  - .vsf-field 加 overflow:hidden, 极端溢出也不覆盖邻居
  - input/textarea/select: width:auto → width:100%, flex:1 → flex:1 1 0%, 可正确填充并收缩
  - .searchable-wrap 同步 flex:1 1 0% + min-width:0
  - 备注 placeholder 缩短为 '备注', 避免截断
- 验证: node --check ✓ 构建 ✓ 部署 ✓ 线上 200 ✓ 线上 CSS 含 auto-fit ✓ 线上 bundle 含 placeholder='备注' ✓
- 方法论: 固定列数网格在响应式场景是重叠隐患 — auto-fit + minmax(200px,1fr) 是更稳健的
  响应式表单布局模式; 长 placeholder 在窄列中必截断, 应使用短占位或 title 提示

## v107.68 — 工具栏⚙️移至商品明细表头作列设置 (2a1ccdd, bundle main-B1zHGmSm.js)
- 用户需求: 1号(工具栏"自定义"字段的⚙️)删除, 2号(访销订单商品明细表 vsf-items)新增⚙️代表列设置
- 删1号: app.js 移除 '自定义' vsf-field(含 vsf-custom-btn 齿轮), styles.css 清理 .vsf-custom-btn 死代码
- 2号列设置实现:
  - vsf-items thead 紧接全选框后新增齿轮列 th(onclick=toggleVsfColConfig, title=列设置), 用 icon('settings',12)
  - 9个可配置列 th 加 data-col(mkt/barcode/unit/ba/actual/qty2/zone/stock/remark),
    核心列(商品名称/销售价/订单数量/订单金额/销售仓库/操作)固定不隐藏
  - tbody _vsfAddRow 同步加空占位列(对齐 thead 第2列齿轮列)→ 18列对齐
  - initVsfColVis/applyVsfColVis/toggleVsfColConfig:
    * 点击齿轮弹面板(9个可配置列复选框), 勾选实时显隐(th+td 按 thead 索引对齐设 display)
    * 状态存 localStorage(hergent_vsf_col_vis), 重开保留; 定位齿轮下方, 点外关闭
- 验证: 线上 bundle 含 toggleVsfColConfig/applyVsfColVis/data-col=remark, vsf-custom-btn=0 ✓
- 方法论: table-layout:fixed 表格隐藏列 = th+td 同列索引设 display:none, 用 thead 索引对齐 td 比给每个 td 加 data-col 更省改动;
  列设置面板定位用 getBoundingClientRect 而非写死 top/right, 适应性更强

## 2026-08-06 08:20 | v107.69 | 列设置齿轮收进序号列表头
- 需求: 用户红框标注齿轮应跟"序号"同列
- 改动:
  - `static/app.js`: thead 删除独立 32px 齿轮列, 把 ⚙️ 并入 '№' 列表头(显示 "№ ⚙️", 宽 48px)
  - tbody 删除每行空齿轮占位 td, 行列对齐(17列); table min-width 1400→1370
  - `_vsfDelRow` 的 `children[1]` 在 v107.68 实际指向空齿轮单元格(序号更新 bug),
    删除占位列后自然指向序号单元格, bug 顺带修复
- 验证: node --check ✓ 构建 ✓ 部署 200 ✓ 线上 bundle 含 "№ settings" 合并结构 ✓
- 方法论: 图标与文字共列时, th 内用 inline-flex + gap 保证视觉一体; 删除列后必须检查
  所有 `children[n]` 硬编码索引(本例 _vsfDelRow 的序号重排)

## 2026-08-06 08:28 | v107.70 | 序号列头去掉 № 只留 ⚙️
- 需求: 用户要求列头只保留齿轮, 删除 '№' 文字(序号数字在每行已标识列用途)
- 改动: thead 序号列 th 改纯 ⚙️ (13px), 加 aria-label='列设置' 保无障碍; 列宽 48→36px, table min-width 1370→1358
- tbody 每行序号数字不变; 验证: 构建 ✓ 部署 200 ✓ 线上 bundle 无 "№ settings" 连写 ✓

2026-08-06 08:31 访销订单页面对比分析 (分析, 未改代码)
- 用户要求对比舟谱云管家与 Hergent 创建访销订单截图, 输出设计优化建议
- 报告已生成: outputs/订单页面对比分析报告.md
- 核心结论: Hergent 在字段/列设置已追上, 但在卡片分区、表格可读性(字号/行高/列宽)、
  业务快捷开关(改金额变数量/按小单位销售)、客户上下文信息、底部 sticky 操作栏等方面落后
- 优先级 P0 落地项: ① 表单/表格卡片分区 ② 表格可读性三连调 ③ 补齐预配送时间/送货司机
- 方法论: 对比竞品截图时应先列字段清单、再看信息层级、最后看交互效率; 建议要带代码级落地
  位置和预估工时, 方便用户决策先做哪些

2026-08-06 11:23 落地 P0 三项优化 (代码已改, 已部署 v107.71, commit 94ba3f2, bundle main-ZvdLMdoq.js)
- 卡片分区: 订单信息(vsf-fields)+商品明细(vsf-items)各包 .vsf-card 白卡片(--bg2浮在--bg灰页面)
  + .vsf-card-title 标题; 外层 overflow-y 容器 padding:12px 16px; vsf-fields 背景改透明
- 表格可读性: td padding 4/6→8/10 + font-size 12→13 + height:40px; thead th 加字号+color t1+
  底边框2px; tr:hover bg2→bg3; 表格 min-width 1358→1480
- 补齐冷链字段: 预配送时间(input type=time)+送货司机(searchable 占位)
- 验证: node --check ✓ 构建 ✓ 部署 200 ✓ 线上 CSS/JS 全含新样式与字段 ✓
- 注意: 颜色令牌 --bg=#ececf0 页面灰, --bg2=#fff 卡片白, --bg3=#fbfbfd 斑马/hover; 深色模式
  下三者分别为 #000/#1c1c1e/#2c2c2e, 卡片比页面略亮仍可区分(已兼容)

## v107.72 访销订单 P1 三改(用户明确: 列设置⚙️不动, 只做其它)
- 业务开关: 原顶部2条 checkbox 删除 → 商品明细卡片内表格上方 .vsf-item-toolbar 改用 Toggle Switch(.vsf-switch),
  复用同 id(vsf-money-mode/vsf-unit-mode), _vsfRecalc 逻辑不变; 文案改为"改订单金额时变更数量"/"按小单位销售"
- 客户上下文: 客户字段包 .vsf-customer-wrap, 下方 .vsf-customer-meta 显"欠款¥X"+"信用额度¥X";
  顺手修原 change 监听 bug(对<span>设.value无效→.textContent), 信用取 cust.credit_limit(无则¥—)
- 底栏真吸底: 根因 modal overflow:auto+内层min-height:100vh>modal高→modal自身滚动致底栏滚走;
  改 modal display:flex column+overflow:hidden, 内层 flex:1 min-height:0, 内容区 min-height:0,
  底栏 .vf-footer(sticky bottom:0+上阴影); 内容任意高度底栏始终钉在可视底部
- 验证: node --check ✓ 构建 ✓ 部署 ✓ 线上bundle含 vsf-item-toolbar/vsf-customer-meta/.vsf-switch/.vf-footer,
  toggleVsfColConfig 仍在(列设置未动) ✓

## v107.74 访销订单真实化 — 生产部署阻塞修复 + 端到端验证
- 背景: 上一轮已改完前端(上传/司机/车辆/地址/日期/提交语义)与后端(routers/sales.py upload 端点 +
  sale_order_create 扩 15 列 + erp_db init_db 加 delivery_time/attachments 两列迁移), 但部署后发现
  **tenant_1.db 真实业务库缺 delivery_time/attachments 两列** → 新 15 列 INSERT 在租户库必失败(订单建不出)。
- 根因: init_db() 的 _safe_migrate 只作用于 master db(get_db()), 租户库由 erp_db.py 模块级独立回填循环
  (~11913 `for _tp in sorted(glob tenant_*.db)`) 处理, 该循环此前只补 contacts 列, 没补 sale_orders。
- 修复(双保险):
  ① 永久: 扩展该回填循环加 `_TENANT_SALE_ORDER_COLS=[delivery_time, attachments]`, 对全部 tenant_*.db 幂等生效
     (init_db 自身也已含这两列迁移, 覆盖 master/erp.db)。
  ② 立即: 直连生产 tenant_1.db + erp.db ALTER 加两列; WAL 模式改完必 chown hergent:hergent + systemctl restart
     强制 checkpoint(否则改动卡在 -wal 不落主文件)。
- RBAC 漏配修复: 新端点 /api/upload + /api/uploads 未进 `_PATH_MODULE_MAP`(server.py ~289) → 403
  MODULE_NOT_CONFIGURED。已加 `"/api/upload": "sales", "/api/uploads": "sales"`。前端 ApiClient.upload('/upload')
  经 _fetch 自动补 /api 前缀, 命中正确。
- 端到端验证(实弹, 后清理):
  • 建 draft 单(581) + pending 单(582) 均成功, tenant_1.db 确认 status 分别为 draft/pending、
    delivery_time/attachments/delivery_address/order_date 全部落库 → #8 日期防丢 + #9 提交语义 已真生效。
  • POST /api/upload 真存盘到 /opt/hergent-erp/uploads/(uuid 命名), 返回 url; GET /api/uploads/{name} 200 回文件;
    .exe 等非法扩展名 → 400 拒绝 → #1 上传真实化 已真生效。
  • GET /api/vehicles → [] : vehicles 表存在但**生产为空** → 车辆下拉已正确接真实数据但无行,
    需运营侧补录车辆(数据问题非代码 bug), 已反馈用户。
- 清理: 删除测试单 581/582(含 sale_order_items)、测试上传文件、smoke 测试 token; 重启后 health 200。
- 部署文件: erp_db.py(回填循环)、server.py(rbac map)、routers/sales.py(upload, 上一轮已部署)、
  static dist(main-DzEqri7K.js, 上一轮 rsync, 线上 bundle 已含 _vsfOnFiles/_vsfUpload/已提交并送审)。
- 方法论: 给租户库加列 = 必须同时改"模块级回填循环"(永久覆盖未来租户) + 直连生产 ALTER(立即);
  只改 init_db 对租户库无效。新业务端点上线前务必确认进 RBAC 路径映射表, 否则 403 静默不可用。

## 访销订单去重上传附件入口（v107.74-patch, main-DXYtvR-l.js, 已上线）
- 用户截图反馈: 创建访销订单同时存在两个上传入口(右上角“创建”旁 + 表单下方“附件”卡片里的“选择文件”)。
- 改动 static/app.js:
  • 删除表单下方附件卡片中的“选择文件”按钮和隐藏 file input(`id="vsf-file"`), 仅保留 `vsf-attach-list` 作为已上传文件展示。
  • `_vsfUpload` 改为动态创建临时 `<input type=file multiple>` 并触发, 不再依赖被删除的 `vsf-file`。
- 验证: node --check ✓, Vite build → main-DXYtvR-l.js, 线上 bundle 中 `vsf-file` 引用 0、旧 onclick 0、
  `vsf-attach-list` 仍保留 2 处, health 200。
- 部署踩坑: 正确 nginx root 是 `/opt/hergent-erp/static/`(不是 `/opt/hergent-erp/`);
  直接 rsync 到远程目录报 Permission denied, 改用 scp dist→/tmp/dist-new + 服务器本地 rsync 解决。
- 结论: 现在唯一上传入口为右上角“创建”按钮旁的“上传附件”。

## 访销订单客户字段改为舟谱式弹层表格选择器（v107.75, main-BjyYiP5X.js, 已上线）
- 需求: 截图中舟谱“客户”字段点击后展开表格弹层, 列：门店名称/编码、老板名称/电话/地址、片区、渠道,
  顶部搜索, 底部“仅匹配客户名称”复选框 +“查看更多”。
- 改动:
  - `showVisitSaleForm` 中客户输入框改为 `readonly` 触发器(`id="vsf-customer-name"`), 点击打开
    `window._vsfOpenCustomerPicker()`。
  - 不再调用 `initSearchableSelect('vsf-customer')`; 保留隐藏 `vsf-customer-id`, 选中后 dispatch change
    事件复用原有“带出地址/欠款/信用额度”逻辑。
  - 新增 `window._vsfOpenCustomerPicker`: 固定定位弹层 z-index 10000, 表格列对齐舟谱, 搜索过滤
    name/code/mnemonic/boss/phone/address/region/channel, “仅匹配客户名称”只查 name, 关键字高亮。
  - 行悬停青底色、门店名品牌青色、键盘上下/回车/Esc 支持; “查看更多”走 `/contacts?type=customer&limit=200&offset=`
    (当前后端分页参数未透传, 行为同前 200)。
  - 客户接口已在 `showVisitSaleForm` 缓存 `window._VSF_CUST_TOTAL`, 弹层优先用缓存, 未加载则实时拉取。
- 验证: node --check ✓, build → main-BjyYiP5X.js, 线上 health 200, bundle 含 `_vsfOpenCustomerPicker` /
  `门店名称/编码` / `仅匹配客户名称` / `查看更多`; API 返回 contacts 含所需字段(c.*)。

## 访销订单去掉下方空附件卡片（续, main-DRzSqoAm.js, 已上线）
- 用户反馈: 删“选择文件”后仍留了个空 `vsf-card` 占位卡片, 没必要。整段删除(含 `vsf-attach-list` 容器)。
- 为免上传后无反馈: 给右上角“上传附件”按钮加 `id="vsf-upload-btn"`, `_vsfRenderAttachments` 在无
  `vsf-attach-list` 容器时改为在该按钮上显示数量徽标 `上传附件 (N)`; 移除附件时同步更新。
- 验证: node --check ✓, build → main-DRzSqoAm.js, 线上 health 200, 下方卡片已无(bundle 仅余函数内防御性
  getElementById 检查 1 处), vsf-upload-btn 引用 2 处。界面仅保留右上角单一上传入口。

## 访销订单客户输入框→弹层表格实时搜索控件（combobox 化, main-szvd19DG.js, 已上线）
- 需求: 把“客户”输入框本身配置为弹层表格选择器的实时搜索框(不再只读触发器+弹层内独立搜索框)。
- 改动(app.js `showVisitSaleForm` 字段区 + `_vsfOpenCustomerPicker`):
  - `vsf-customer-name` 去掉 `readonly`, placeholder 改“搜索门店名称/编码/老板/电话...”, 加
    `onfocus="_vsfOpenCustomerPicker(true)"` + `oninput="_vsfCustomerQuery(this.value)"`。
  - 弹层: 去掉全屏半透明 overlay 与弹层内 `vsf-cust-search`; 改**非模态 fixed 下拉**(无遮罩, 点外/Esc 关),
    表格直接进滚动容器, thead sticky。
  - 新增 `window._vsfCustomerQuery(q)`: 弹层已开→`_vsfCustRender()` 实时过滤; 未开→`_vsfOpenCustomerPicker(false)`。
  - 新增 `window._vsfPickerOutside`: document mousedown 监听, 点面板外/字段外才关(函数引用稳定, addEventListener 幂等)。
  - `render()` 查询源从 `search.value` 改 `document.getElementById('vsf-customer-name').value`, 实时联动。
  - `select()`→`closePicker()`(移除面板+撤销 mousedown); fromFocus 打开后 `field.focus()+select()` 便于重搜,
    输入态(false)不 select 以免清空正在输入的查询。
  - 键盘: 字段 ↓ 进首行, 表格内 ↑/↓ 行导航, Enter 选中, Esc 关闭; 保留“仅匹配客户名称”+“查看更多”。
- 验证: node --check ✓, build → main-szvd19DG.js, 线上 health 200; bundle 含 _vsfCustomerQuery/_vsfPickerOutside,
  vsf-cust-search 与“点击选择客户”旧文案已无。

## 客户输入框“无法输入文字”修复（main-CxzHbqGe.js, 已上线）
- 根因: `_vsfOpenCustomerPicker` 的 `loadData(cb)` 在**异步**客户数据返回后执行 `field.focus(); field.select()`。
  用户在数据返回前(~50-100ms)打的字已落入输入框, 但异步回调一回来就把已输入文字 `select()` 全选,
  下一键直接替换→已输入文字被清空, 表现为“打不进字”。
- 修复: 把 `select()` 改到 `onfocus` 时**同步**执行(此时用户尚未输入, 不会清已打字); 删除异步回调里的
  `field.focus()+field.select()`。另给弹层加 `z-index:99999` 确保浮在 modal-overlay 之上(此前无 z-index 可能被遮)。
- 教训: 任何“聚焦后异步回填/选中”都要避免 select 已输入内容; select 只能放在同步焦点时刻。
- 验证: node --check ✓, build → main-CxzHbqGe.js, 线上 health 200, bundle 含 z-index:99999, 生产已部署。

## 客户框仍"打不进字" → 结构级重写（main-xUKdLH_J.js, 已上线）
- 背景: 异步 select 修复后用户仍反馈打不进字 → 判定根因不在异步 select, 需结构性排查。
- 关键事实: 访销表单**不走 modalOpen**(无 auto-focus/escFn), 而是自建 `.vsf-overlay`(fixed,z-index:9998)。
- 重写(app.js):
  - 客户输入框去掉内联 `onfocus/oninput`(minify/缓存下不可控), 改在初始化时 `addEventListener` 绑定
    focus/input/mousedown(stopPropagation)/click(焦点兜底); 并防御性 `removeAttribute('readonly'/'disabled')`+`type='text'`。
  - `_vsfOpenCustomerPicker`: 弹层已开 → **不再 remove**(改为 re-render), 消除"再点击输入框弹层闪没"；
    弹层 top/maxHeight 自适应(下方空间不足向上弹, 永不遮输入框)。
  - `select()` 仅当 `fromFocus && field.value` 非空时同步执行一次。
- 教训: 弹层类控件务必(a)事件用 addEventListener 而非内联 (b)点击触发器绝不拆除弹层 (c)异步回调绝不碰输入框。
- 验证: node --check ✓, build → main-xUKdLH_J.js, 线上 health 200; bundle 已无内联 onfocus、含 removeAttribute。

## ★ 客户框"打不进字"真正根因 = 全局 j/k 行导航吞键（main-CW8poJqD.js, 已上线）
- 用户自检: 光标闪烁(聚焦正常)、其它字段可打字 → 问题在客户框自身监听之外, 指向全局键盘监听。
- 用 browser-use 复现: 独立页复刻客户框逻辑打字正常(browser-use type 走 insertText 不触发 keydown,
  必须用 `keys` 发真实按键) → 加入 app.js:5326 全局 j/k 行导航 handler 后, 按 'j' 日志显示
  preventDefault 吞键 → 修复(加 `!e.target.closest("input,textarea,[contenteditable]")` 守卫)后
  value="ji" 完整输入。**根因确认**。
- 根因: app.js:5326 全局 j/k 行导航**缺输入框豁免**(全站其它全局 keydown 都有守卫)。
  → 拼音输入中 j/k(张记/酒店/科技/可口/家/金…)被 preventDefault 吞掉 → 汉字打不出 → "打不进字"。
  "备注"字段能打是因为其拼音(bei-zhu)恰好无 j/k。
- ★教训(前端铁律): **任何全局键盘快捷键监听必须对所有可输入元素(input/textarea/[contenteditable])做豁免**,
  否则中文拼音输入会被吞键; 排查"输入框打不进字"先查全局 keydown 对普通字符 preventDefault 的 handler。
- 验证: node --check ✓, build → main-CW8poJqD.js, 线上 health 200, bundle 已含 j/k 输入守卫。

## 客户弹层被"深色卡片"覆盖（main-Dvb_abkq.js + main-BBKgGMim.css, 已上线）
- 用户截图: 弹层打开正常, 表头/footer 两段被半透明深色圆角矩形覆盖, 中间客户行正常显示。
- 排查: production `styles.css:278` 全局 `thead th{position:sticky;top:0;background:var(--bg3);...font-size:10px;text-transform:uppercase;letter-spacing:.04em;z-index:5}` 干扰弹层 thead 的样式与背景。
- 修复(styles.css + app.js):
  - styles.css 新增 `#vsf-customer-picker` 专属样式, 全部 `!important` + 显式色值(`#fff`/`#f5f5f7`/`rgba(0,0,0,.1)`), 覆盖全局 thead th;
    加 `isolation:isolate` 创建独立堆叠上下文防外部元素穿透; `z-index:2147483647`。
  - app.js 弹层 HTML: 给 footer 加 `class="vsf-picker-footer"`; 去掉 thead `<tr>`/footer div 的内联 `background:var(--bg2)`(改由 CSS 接管, 避免 CSS 变量被全局覆盖时引发色值漂移)。
- 教训: **挂在 body 的 fixed 弹层务必配专属 CSS + `isolation:isolate`**, 不能仅靠内联样式或 `var()`, 全局表格/可搜索/hover-card 规则都可能穿透。
- 验证: node --check ✓, build → main-Dvb_abkq.js + main-BBKgGMim.css, 线上 health 200, bundle 含 vsf-picker-footer, CSS 含 #vsf-customer-picker{isolation:isolate}。

## 客户框仍打不进字 → 发现第二段 j/k handler 漏修（main-DZDVkD4n.js, 已上线）
- 背景: v107.78 修了一个 j/k handler 后用户仍报"打不进字"; 加专属 CSS 修深色卡片后仍报同样问题。
- 根因: app.js 里**实际有两段** j/k 行导航重复逻辑:
  - 外层 `document.addEventListener("keydown",...)` 内 line 985 的 `if(e.key==="j"||e.key==="k"){...}`
  - IIFE `document.addEventListener("keydown",...)` 内 line 5328 的同款逻辑
  v107.78 只补了 line 5328 的守卫, **line 985 的漏了** → 拼音里 j/k 仍被外层 handler 吞掉。
- 修复: 给 line 985 同样加 `&&!e.target.closest("input,textarea,[contenteditable])` 守卫。
- 教训(★): 排查"打不进字"时务必 `grep '"j"||"k"'` 找出**所有** j/k handler, 一并加守卫, 不要只补第一个找到的。
- 验证: node --check ✓, build → main-DZDVkD4n.js, 线上 health 200, bundle 含 2 段均带输入框豁免的 j/k handler。

## ReferenceError: c is not defined（main-CNbz06IW.js, 已上线）
- 用户贴控制台: `main-*.js:316 Uncaught (in promise) ReferenceError: c is not defined`。
- 根因: app.js:2626 客户 change 事件 handler 里, `api("/contacts/"+cid+"/detail").then(function(c){...})`
  之后另起 `api("/customer-prices/"+cid).then(function(prices){ window._custPriceLevel=c?c.price_level:"retail"; })`,
  **第二个回调引用第一个回调的参数 c(作用域外)** → 必抛 ReferenceError。
  触发条件: 任何 `.searchable-value`(含访销 `vsf-customer-id`) change 事件 → 选择客户即报错。
- 修复: 把 `window._custPriceLevel=c.price_level||"retail"` 移到第一个回调内(有 c 的作用域), 第二个回调只设置 _custPrices。
- 教训(★): promise 链里跨回调引用另一个回调的参数 = 必炸; 修 "选择客户后控制台报错" 类问题先查此模式。
- 验证: node --check ✓, build → main-CNbz06IW.js, 线上 health 200, bundle 含 `_custPriceLevel=n.price_level||"retail"`。

## ★ 网站 403 Forbidden（根目录）→ 部署 rsync --delete 误删 index.html（main-Dl5S7877.js, 已修复上线）
- 症状: `https://erp.hergent.cn/` → 403; `/assets/main-*.js` → 404; `/api/health` → 200(后端正常)。
- 根因链:
  1. 本地 `static/dist/` 构建产物**不完整**(只有 manifest/mobile/portal/vendor, **缺 index.html + assets/**)
     —— 疑似上一次 `vite build` 的 emptyOutDir 清空后写入不完整/中断。
  2. 部署脚本 `rsync -a --delete /tmp/dist-new/ /opt/hergent-erp/static/` 用**残缺源**同步,
     `--delete` 把服务器上"源里没有"的 index.html/assets/ 全删 → static 只剩 manifest/mobile/portal/vendor。
  3. nginx `location /` 无 index 文件 + autoindex off → 返回 **403 Forbidden**(而非 404, nginx 语义:
     目录请求无 index 且禁止列目录 = 403)。
- 证据: nginx error.log `directory index of "/opt/hergent-erp/static/" is forbidden` +
  `open() "/opt/hergent-erp/static/assets/main-C4u3scbm.js" failed (2: No such file or directory)`。
- 修复: 重新 `VITE_BASE=/ npm run build`(dist 恢复完整) → scp → **rsync 前先校验 /tmp/dist-new 有 index.html+assets** →
  rsync --delete 同步 → chown → 全部 200。
- ★部署铁律: **用 rsync --delete 前必须校验源目录完整性(存在 index.html + assets/main-*.js), 否则残缺源会把线上删光**;
  建议部署脚本内置 `test -f $SRC/index.html && test -d $SRC/assets || exit 1` 防呆。
- 验证: 根/ /bundle/css/favicon/api 全部 200, index 引用 main-Dl5S7877.js。

## ★ 客户框"打任意字母都不显示"真正根因 = 电话号格式化 handler 误匹配 placeholder 含"电话"（main-C4u3scbm.js→Dl5S7877.js）
- 用户: "打任意字母都不显示", 光标闪烁、其它字段正常、弹层能打开。
- 用 browser-use 登录绕过实测生产页: keydown prevented:false、beforeinput insertText 正常、input 事件触发,
  但 **value 立即变空** → 用 `Object.defineProperty` 拦截 input.value setter → 抓到
  `main-*.js:316:22849`(document input 监听)写入 value=""。
- 根因: app.js:2935 电话号自动格式化 handler 匹配 `input[placeholder*='电话']`,
  而客户框 placeholder "搜索门店名称/编码/老板/**电话**..." 含"电话" → 每次 input 都
  `value.replace(/\D/g,"")` 把字母删光 → 显示为空。
- 修复(双保险): ① 客户框 placeholder 改"搜索门店名称/编码/老板/手机/地址/片区/渠道"(去掉"电话");
  ② handler 条件加 `e.target.id!=='vsf-customer-name'` 排除。
- ★教训: 全局 input 监听用 `[placeholder*='xxx']` 匹配时极易误伤其它输入框; 排查"打字即被清空"用
  value setter 拦截抓写入者(栈里有 bundle 行号)。
- 另修健壮性: showVisitSaleForm 里 `vehiclesPromise` 未包 safe(), 该接口失败会令 Promise.all reject →
  then 内客户框事件绑定全不执行 → 弹层打不开; 已补 safe()。
- 验证(生产实测): 打字 value=[hiua] 持续累积, 弹层/过滤正常。

## 控制台 content.js 错误 + 弹层"暗色卡片"真相 = 浏览器扩展（非系统 bug）
- 用户贴控制台: `content.js:1 Uncaught SyntaxError: Identifier 'n' has already been declared`、
  `Content Script 未知消息类型: COMPUTE_PAGE_FINGERPRINT/DISABLE_FEATURES/ENABLE_FEATURES`、
  `asynchronous response ... channel closed` —— **全部来自浏览器扩展的 content script, 与 Hergent 无关**。
- 验证: 应用 bundle 内 grep `COMPUTE_PAGE_FINGERPRINT|DISABLE_FEATURES|ENABLE_FEATURES` = 0 处。
- ★重要: 用户此前截图的"深青色 #204e79 暗色卡片"在项目代码/CSS/bundle 中**完全搜不到**,
  该颜色亦非 --p(#1f4e79)…实际 #204e79 与 --p 相近但非同一值 → **极可能也是该浏览器扩展注入的覆盖层**。
  扩展(带页面指纹/功能开关特性)会向页面注入 DOM 覆盖层并可能拦截键盘事件,
  可解释"打不进字/暗色卡片"等怪象的一部分(另有一部分是我们修的 handler/j-k 真 bug)。
- 处置: 建议用户 chrome://extensions 排查/禁用可疑扩展(如 AI 助手/翻译/指纹类)后重测弹层。
- 教训(★): 前端 bug 排查时, 若某颜色/元素在源码和 bundle 中都搜不到, 优先怀疑浏览器扩展注入,
  用 `document.querySelectorAll('div')` 遍历 DOM 找非应用元素, 或建议用户开无痕/禁用扩展对照。

## v107.82 客户弹层表头遮挡表体 — 真正根因 + 弃用 table 重构
- 现象: 用户输入店名后, 弹层表头"覆盖"表体; 之前多次反馈的"深青色卡片遮挡"同根因。
- 真正根因(★): `_vsfOpenCustomerPicker` 里 `var panel = document.createElement('div')` 后**从未设置 `panel.id`**,
  导致 styles.css 全部 `#vsf-customer-picker{...}` 隔离规则(白底/isolation:isolate/z-index:2147483647/
  覆盖全局 thead th) **从未生效**; 全局 `thead th{position:sticky;top:0;background:var(--bg3);z-index:5}`
  直接穿透到弹层表头 → 表头悬浮盖表体 + 背景被 --p(--bg3 解析) 染深青色 → 用户看到"深色卡片"。
- 修复组合(同时下, 二者缺一不可):
  1. **panel.id = 'vsf-customer-picker'** — 让隔离 CSS 真正生效(关键! 此前所有 CSS 规则都是空气)
  2. **弃用 <table> 改两段式 flex**: 固定表头 `.vsf-picker-head`(独立 div, flex-shrink:0) +
     滚动表体 `.vsf-picker-scroll`(独立 div, flex:1, overflow-y:auto); 结构性免疫全局表格规则,
     桌面/移动端行为一致, 永远不出现 sticky thead 渲染冲突。
  3. 弹层宽度视口自适应: `width = min(720, max(r.width, vw-16))`, `left = max(8, min(r.left, vw-pw-8))`,
     窄窗口/移动端不溢出。
- 验证(真实浏览器): panelFound=true, bg=#fff, z=2147483647, isolation=isolate, head=35px 固定,
  scroll=800px 可滚动, 30 行渲染, scrollTop=400 时 head 位置不变、表体内容正常滚过。
- 教训(★): 写"命名空间/容器"元素时必须设 id, 否则后续所有 ID-选择器规则全是死代码; 排查"全局样式污染弹层"
  类问题, 先 getElementById 确认弹层确实带 id, 再看 CSS 规则是否生效。
- 部署: main-BtL0AQH7.js + main-CbzV6uZ7.css, health 200, 视觉截图(白底+固定表头+可滚表体+footer)正常。

## v107.83 访销订单"欠款"字段语义重复修复 — 三处重复 → 一处汇总+主动明细
- 现象: 客户字段下方同一数据被展示了三次 — 全局 creditInfo spot 的绿色"应收:¥X",
  vsf-customer-meta 的青色"欠款¥X", 底部 vf-footer 的"欠款:¥X"; 用户感受是"应收账款明细"与"欠款"重复。
- 真正根因: 三个 handler 同时维护同一份数据(cust.ar_balance), 但用了不同术语("应收"/"欠款")+
  不同位置(客户字段下/客户字段元信息行/底部操作栏), 让用户感觉是三个独立信息。
- 修复组合:
  1. **术语统一**: 全部统一用"应收未结"(财务规范术语, 避免"应收"/"欠款"混用)。
  2. **单一汇总 + 主动明细**: 客户字段下方只展示一个 chip `应收未结 ¥X` + `信用额度 ¥X` +
     `查看明细 →` 链接; 删除底部 vf-footer 的"欠款"字段(改为只显示订单本身的统计: 单据数 + 合计)。
  3. **新增应收账款明细弹层** (`window._vsfShowReceivables`):
     - 选客户若有应收未结自动弹出, 也可通过"查看明细 →"主动打开
     - 内容: 标题(客户名) + 4格汇总(合计未收/逾期未收/笔数/最早到期) + 7列明细表(订单号/日期/应收/已收/未收/到期日/状态)
     - ESC / × 关闭, 独立堆叠上下文 + z-index 2147483647
  4. **全局 creditInfo spot 跳过访销表单**: 通用 handler 在 vsf-customer-id 时 return, 避免再添加第三个"应收"chip
- 后端补: `GET /api/receivables` 加 `contact_id: int = 0` 查询参数(过滤特定客户应收明细),
  兼容旧调用(默认 0 = 不过滤)。
- 验证(浏览器实测 + 后端实测):
  - vsf-customer-meta: `.d-amt` 旧元素 NO-OLD, `.d-ar` 显示 "¥12,350.50", `.d-credit` 显示 "¥50,000.00",
    `.d-detail` display=inline (查看明细链接可见)
  - 应收账款弹层自动弹出, 标题"应收未结明细 — 檀溪美联", 4格汇总+7列表头+空数据友好提示
  - 后端 `?contact_id=2214` 返回 total:0 (该客户无未结应收, 过滤正确)
- 部署: main-DSLDqc6m.js + main-BygSNrcs.css + server/routers/finance.py + server/db/queries/finance.py,
  health 200, 所有新函数/ids/CSS 类均已上线。

## v107.84 客户字段网格对齐修复 — 元信息上移顶部条, 客户字段恢复纯净
- 现象: 客户字段下方"应收未结 + 信用额度 + 查看明细"占纵向空间, 撑高 grid 第一行,
  使第二行(送货车辆/收货地址等)与其他列错位(用户截图证实).
- 真正根因: `.vsf-fields` 用 `grid-template-columns:repeat(auto-fit,minmax(200px,1fr))`,
  客户字段内嵌 `.vsf-customer-meta`(display:flex gap:14px)导致该 grid item 高度 = 输入框 + meta (≈50-55px),
  而其他字段高度 = 输入框 (≈30px). grid 行高由最高列决定, 视觉上"客户"列下方多出空间挤压第二行.
- 修复(结构性): **把 .vsf-customer-meta 从客户字段内嵌改至 .vsf-card 顶部独立行 (.vsf-context-bar)**:
  1. HTML: 在 vsf-card 内、vsf-fields 之前新增 `.vsf-context-bar#vsf-customer-meta`, 客户字段 vsf-field-customer
     恢复纯净(仅 label + searchable-wrap 输入框, 无下方 wrap/meta)
  2. CSS: 新增 `.vsf-context-bar`(轻量顶部条, padding:10px 16px, 浅青底, gap:16px),
     删除原 `.vsf-customer-meta`(字段下方)+ `.vsf-customer-wrap`(纵向容器)
  3. change 监听: 改为 innerHTML 整体替换模式(原是逐个 element 设 textContent),
     填充 `客户: <strong>NAME</strong> | 应收未结 ¥X | 信用额度 ¥X | 查看明细 →`
- 验证(浏览器实测): 7 字段(fieldsY=[197,197,197,197,197,197,197]) **完全水平对齐**,
  uniqueYs=1, 顶部条高仅 39px 不占用字段下方空间, 选中客户自动弹弹层保留.
- 教训(★): 任何 grid 布局的 item 高度不一致都会破坏整行对齐, 解决思路不是 `align-self` 或 `subgrid`
  (治标), 而是把"额外信息"从 grid item 内**迁出到独立行/独立区域**(治本), 让每个 grid item
  保持简单单行内容.

## v107.85 顶部条移除客户名(避免与客户字段重复)
- 现象: v107.84 把客户上下文迁到顶部条后, 顶部条又显示了"客户: 檀溪美联", 与下方表单客户字段重复。
- 修复: change 监听 innerHTML 去掉 `vsf-context-cust` span(客户名), 顶部条只保留额外信息:
  `应收未结 ¥X | 信用额度 ¥X | 查看明细 →`。客户名由表单客户字段专有。
- 验证: 顶部条文本="应收未结 ¥12,350.50信用额度 ¥50,000.00查看明细 →", containsCustomerName=false,
  7 字段对齐保持 unique=1。
- 教训: 迁出到独立区域的"额外信息"只承载**主表单没有的内容**; 若主表单已有专属字段(客户名),
  独立区域不应再复述, 否则产生新的重复。

## v107.86 客户字段独占整行(grid-column:1/-1) — 最佳布局
- 用户反馈: v107.84/85 的"顶部条"虽然对齐好, 但把客户名与上下文割裂开, 视觉不连贯.
- 最佳方案: **客户字段独占整行** (`grid-column:1/-1`), 应收/信用/明细回到客户名正下方:
  - 语义紧密关联(财务上下文紧贴客户名)
  - 其他 12 字段保持 6×2 网格, 不受客户字段高度影响
  - 无顶部条割裂、无重复
- 实现:
  - HTML: 移除 `.vsf-context-bar`, `.vsf-field-customer` 内部恢复 `.vsf-customer-wrap`
    (input + meta 纵向), `.vsf-customer-meta` 跟随客户字段下方
  - CSS: `.vsf-field-customer{grid-column:1/-1;display:flex;align-items:flex-start;gap:10px;
    padding:4px 4px 12px;border-bottom:1px dashed var(--bd)}`
  - `.vsf-customer-wrap{flex-direction:column;gap:6px}`, `.vsf-customer-meta{flex-wrap:wrap;
    gap:16px;padding-left:70px(与输入框对齐)}`
  - change 监听: 改回更新 `.vsf-customer-meta`(去除顶部条后的位置)
- 验证(浏览器实测): customerField.gridColumn="1 / -1", width=1651px(独占整行);
  其他字段两行(7+5), 同行内 y 完全一致; metaText="应收未结 ¥12,350.50信用额度 ¥50,000.00查看明细 →",
  metaBelowCustomer=true(紧贴客户名下方), 弹层自动弹保留.
- 教训(★): Grid 布局中的"核心字段"(信息量大/有专属上下文)应**独占整行**, 而非挤在多列网格中.
  让核心字段宽度自由、下方 meta 自然归属; 其他次要字段排网格. 既消除错位, 又保持语义连贯.

## v107.87 舟谱布局 — 客户跨 2 行(非独占整行)
- 用户反馈: v107.87 给客户字段加了 `border/border-radius/background` 想强化"核心字段"感, 但造成:
  "独立小卡片"违和感, 与其他字段视觉权重不一致; 客户 meta 行字号过小, 视觉上"贴上去的异物".
- 真正做法(舟谱): 客户字段**与其他字段完全视觉一致**(无边框/无背景/无圆角), 仅靠 grid 布局
  实现跨 2 行. 视觉上它就是一个普通字段, 只是跨 2 行 + 多了 meta 信息.
- 修复:
  - CSS: `.vsf-field-customer` 去掉 border/background/border-radius, 仅保留 grid-row:1/span 2 + 内部纵向布局
  - meta 行字号 11→12px, 与其他字段 label 一致; gap:14→12px, padding 调整, 不喧宾夺主
- 验证(浏览器实测): border="0px none rgb(0,0,0)", background=rgba(0,0,0,0); grid-row=1/span 2;
  第一行 7 字段 top=158 完全对齐, 第二行 6 字段 top=209 完全对齐, meta 与第二行同水平线.
- 教训(★): **任何字段的视觉风格必须服从表单整体规范**, 不要因为"重要性"就给它加边框/背景 —
  那会破坏表单统一感. 重要性通过**位置/空间/内容**表达, 而不是装饰. 舟谱的"客户字段"就是普通字段
  之一, 它的"核心地位"通过跨 2 行的网格行为体现, 不需要任何视觉装饰.

## v107.87 舟谱布局 — 客户跨 2 行(非独占整行)
- 用户反馈: v107.86 让客户 `grid-column:1/-1` 独占整行, 虽然对齐好但实际把 13 字段挤成 3 行
  (客户 1 行 + 其他 6+6 折成 7+5), 顶部空间被挤压.
- 舟谱精确做法: **客户字段跨 2 行** (`grid-row:1/span 2; grid-column:1`), 内部 input + meta 纵向;
  其他 12 字段正常排成 7 列网格. 整张表单只占 2 行, 与舟谱一致.
- 实现:
  - CSS: `.vsf-field-customer{grid-row:1/span 2;grid-column:1;display:flex;flex-direction:column;
    align-items:stretch;border:1px solid var(--bd);border-radius:8px;background:var(--bg2);
    padding:6px 6px 10px}` (客户占左上 2 行 × 1 列, 带边框强化"核心字段"感)
  - CSS: `.vsf-fields{display:grid;grid-template-columns:repeat(7,minmax(0,1fr));
    gap:8px 12px}` (7 列固定网格, 13 字段=1跨+6+6 完美填满; 响应式 4/2/1 列退到窄屏)
  - HTML 无变化, `.vsf-customer-wrap` 内部 input + meta 纵向, meta 在客户字段内.
- 验证(浏览器实测): totalRows=2 (整张表单 2 行); customerRowSpan="1 / span 2"; 其他 12 字段
  otherFieldsAt=[158, 223] (两行整齐, 同 y); metaInCustomer=true; 弹层自动弹保留.
- 教训(★): Grid 布局中的"核心字段"应**跨 2 行**(占第 1 列)而非独占整行 — 这样:
  1) 客户与 meta 紧邻(语义关联), 2) 其他字段仍按多列网格排列(不被挤压成额外行),
  3) meta 与第二行其他字段在同一水平线, 整张表单保持紧凑 2 行高度. 关键: grid-column:1 而非 1/-1,
  让其他字段有机会填满剩余列.

## v107.89 客户字段 label 左 + 输入框右 — 与其他字段一致
- 用户反馈: v107.88 客户字段外层 `flex-direction:column` 导致 label 在输入框**上方**(与其他字段
  label 左 + 输入框右不一致), 看起来"特殊处理"过.
- 修复: 外层改 `flex-direction:row` + label 恢复与其他字段同款样式(60px 右对齐):
  - `.vsf-field-customer{display:flex;flex-direction:row;align-items:flex-start;gap:6px}`
  - `.vsf-field-customer > label{width:60px;flex-shrink:0;text-align:right;padding-right:2px;margin-top:5px}`
  - `.vsf-customer-wrap{flex:1;display:flex;flex-direction:column}` (wrap 内部仍纵向放 input + meta)
- 验证(浏览器实测):
  - labelOnLeft=true (label 左、输入框右, 与其他字段完全一致)
  - labelRight=293, inputLeft=299, gap=6 (label-input 间距 6px 与其他字段同款)
  - labelTopAlignWithOther=true (客户 label 顶部 y 与其他字段 label 完全一致)
  - metaBelow=true + metaLeftAlignInput=true (meta 在输入框下方 + 左边对齐输入框)
- 教训(★): Grid 跨 2 行的"核心字段"内部仍用**标准 label-input 横向布局**(与其他字段一致),
  视觉上和操作上与其他字段没有差异 — 只有"占据更大空间"和"有 meta 行"两点不同, 但展示方式必须统一.

## v107.90 客户下方独立新行 — 欠款/最大欠款天数/超最大欠款天数/挂账(默认隐藏)
- 用户需求: 4 字段(欠款/最大欠款天数/超最大欠款天数/挂账)从当前位置调整到独立新行,
  默认隐藏, 选中客户后显示.
- 数据映射:
  - 欠款 → cust.ar_balance (已有)
  - 最大欠款天数 → cust.credit_days (已有)
  - 超最大欠款天数 → fetch /receivables?contact_id=X, 计算最早未结 due_date 与今天差值(异步)
  - 挂账 → ar_balance > 0 ? "是" : "否"
- 实现:
  - CSS: .vsf-customer-extended{display:none;grid-column:1/-1;grid-row:3;...}
    + .vsf-customer-extended.is-shown{display:flex} (class toggle 控制显隐)
  - HTML: 在 .vsf-fields 内、客户字段后立即插入新 grid item(避免 grid-auto-flow 误填第 3 行其他列)
    + 4 个 span + strong id 锚点便于 JS 填充
  - JS change 监听: 默认 .is-shown 移除(隐藏); 选中客户时填充 4 字段 + 添加 .is-shown class(显示);
    异步 fetch receivables 计算 overdue 后更新对应 strong.
- 验证(浏览器实测):
  - 默认 display=none + visible=false (默认隐藏)
  - 选中客户后 display=flex + is-shown=true + height=40px
  - 4 字段全部填充: 欠款 ¥12,350.50 / 最大欠款天数 60 天 / 超最大欠款天数 0 天 / 挂账 是
  - gridRow=3 + gridCol=1/-1 (第三行, 跨满整行)
  - 布局: 第一行核心字段 + 第二行客户 meta + 第三行独立新行(浅青底色), 整齐 3 行, 客户字段跨 2 行不变.
- 教训: 独立 grid 行必须显式 grid-row 指定, 否则 grid-auto-flow 可能把后续 item 填充到空行,
  破坏布局. 显隐用 class toggle 而非 style.display 切换, 便于调试与维护.

## v107.91 删除客户字段下方重复 chip(应收未结/信用额度)
- 用户反馈: 客户字段下方的"应收未结 ¥X"和"信用额度 ¥X"与独立新行的"欠款 ¥X"重复
  (都是 ar_balance), 删除以避免冗余.
- 修复:
  - JS change 监听: meta.innerHTML 删除 .d-ar 和 .d-credit span, 仅保留引导文字+查看明细链接
  - 引导文案更新: "选择客户后将显示应收明细与账期信息" (不再提"应收未结/信用额度")
  - HTML 默认占位文案同步更新 (vsf-context-empty)
- 验证: metaText="更多应收信息请展开详情弹层查看明细 →", metaHasAR=false, metaHasCredit=false;
  独立新行仍正常显示 4 字段(extShown=true).
- 教训(★): **展示同一数据的两个 UI 入口必须果断删除冗余入口**, 即使"它们可能有用"也不保留 —
  用户认知负担大于功能价值. 应收账款明细由"查看明细 →"弹层提供完整明细(订单号/金额/到期日/账龄),
  独立新行提供汇总金额/账期/挂账状态, 两层职责清晰, 不再冗余.

## v107.92 客户字段改单行 + 送货车辆行向左对齐
- 用户反馈: 客户字段跨 2 行(grid-row:1/span 2)导致第二行"送货车辆"等字段从第 2 列开始,
  与第一行左边缘不齐, 希望送货车辆行左对齐.
- 实现:
  - HTML: 客户字段去掉 .vsf-customer-wrap 与 .vsf-customer-meta, 恢复 label + searchable-wrap 单行结构;
    "查看明细 →"链接移到独立新行(.vsf-customer-extended)右侧(添加 .ext-detail)
  - CSS: .vsf-field-customer{grid-row:1;align-items:center;display:flex;flex-direction:row;...}
    (不再跨 2 行, label + input 与其他字段完全一致)
  - JS change 监听: 删除 vsf-customer-meta 更新逻辑, 改为更新 #vsf-ext-detail 的 display
- 验证(浏览器实测):
  - customerGridRow="1" (客户字段单行)
  - row1Lefts=[233,471,708,946,1183,1421,1658] (7 字段)
  - row2Lefts=[233,471,708,946,1183,1421] (6 字段, **与第一行同左边缘**)
  - row2AlignedWithRow1=true ✅
  - 独立新行 extText="欠款: ¥12,350.50最大欠款天数: 60 天超最大欠款天数: 0 天挂账: 是查看明细 →"
  - oldMetaRemoved=true (旧引导文字已删)
- 教训(★): **Grid 布局中跨多行的"核心字段"会破坏同行其他字段的视觉对齐** —
  当用户要求"对齐"时, 优先考虑是否真的需要跨行; 若核心字段需要"上下文"(如 meta 行), 应
  放到**独立 grid 行**(如 grid-row:3)而非依赖"跨 2 行占位", 这样其他字段能完全
  从列 1 开始自然对齐. 跨行布局适合"内容型"字段(如文本域), 不适合"数据型"字段(如客户).

## v107.93 表单输入控件高度统一(select/time/addr → 30px)
- 用户反馈: 业务员/付款方式/是否加急(select)输入框与其它字段高度不一致.
- 根因: 实测各控件高度 — select=30px 但渲染视觉不同(appearance:auto 在 mac 上系统默认外观
  产生不同边框/内边距), input[type=time]=32px, 收货地址(vsf-addr 内联 padding:8px 10px)=34px.
- 修复:
  1. `.vsf-field select{height:30px;appearance:none;-webkit-appearance:none;padding-right:26px;
     background-image:url(SVG 箭头);background-position:right 9px center}` —
     去掉系统默认外观, 用自定义 SVG 下拉箭头, 与 input 完全统一
  2. `.vsf-field input[type=time],.vsf-field input[type=date]{height:30px}` — 修复 time 32px
  3. 收货地址去掉内联 padding:8px 10px(改由 CSS 统一 5px 10px), 修复 34px
- 验证(浏览器实测): uniqueHeights=[30] — **所有 13 个输入控件(客户/业务员/部门/销售仓库/单据日期/
  预提货日期/预配送时间/送货车辆/送货司机/收货地址/付款方式/是否加急/备注)全部 30px 完全一致**.
- 教训(★): **浏览器对 select/input[type=time] 等控件有系统默认外观(appearance:auto), 视觉高度
  与 input[type=text] 不一致**. 统一表单控件高度必须: select 加 appearance:none+自定义箭头,
  time/date 显式 height, 去掉内联 padding 依赖 CSS 统一.

## v107.94 客户→业务员→部门三级联动(舟谱式)
- 数据模型改造:
  - contacts 表 seed 4 个部门(销售一/二/三/四部) + 10 个员工(parent_id 关联部门)
  - 客户的 assigned_salesperson 字段已是员工名字字符串(无需迁移)
- 前端改动:
  - 业务员字段从 `<select>` 改为 `<div class="searchable-wrap">` + readonly input(点击触发弹层)
  - 新增 `window._vsfOpenOperatorPicker(cust)`:
    * 弹层列出全局员工(按客户 assigned_salesperson 优先排序, 推荐项加背景色+border 标识)
    * 每行显示"员工名 + 部门" (从缓存 _VSF_DEPARTMENTS 查 parent_id 对应的部门名)
    * 点击行 → 自动填 #vsf-operator-name/id + 自动填 #vsf-dept name/id(从员工 parent_id 关联) + 关闭弹层
    * ESC 关闭, × 按钮关闭
  - 客户 change 监听: 选客户后延迟 250ms 自动调 `_vsfOpenOperatorPicker(cust)` (无需用户点击)
- 数据流:
  - 客户 → 弹层列出全部员工(优先匹配 assigned_salesperson) → 用户选择
  - 员工.parent_id → 缓存的 _VSF_DEPARTMENTS → 部门名 + id 填到 #vsf-dept
  - 异常处理: 无关联员工时弹层显示"该客户暂无关联业务员"空态; 员工无 parent_id 时部门显示"未分配部门"且不联动
- 验证(浏览器实测):
  - 选客户 "檀溪美联"(assigned_salesperson=周运潘) → 250ms 后弹层自动打开
  - 弹层标题"选择业务员 (推荐: 周运潘)", 10 行员工列出
  - 选中"符号" → operatorName="符号" / operatorId=2944, deptName="销售四部" / deptId=2934, 弹层关闭 ✅
- 教训: 三级联动依赖**前端缓存员工+部门映射**避免每次 select 都 N 次 API; 弹层渲染时优先客户关联员工 + 视觉推荐标识提升体验;
  延迟 250ms 自动打开避免与客户弹层关闭动画重叠。

## v107.95 业务员字段下"应收"字段删除(creditInfo spot 作用域收紧)
- 现象: 选择业务员后, 业务员输入框下方出现"应收 ¥X"字段(不该有).
- 根因: 全局 change 监听(app.js:2626 creditInfo spot)匹配所有 `.searchable-value`,
  选择业务员后 `#vsf-operator-id`(也是 .searchable-value)触发它创建了 creditInfo spot 显示"应收".
  之前只跳过 vsf-customer-id, 未跳过业务员/部门/仓库等其它 vsf 字段.
- 修复:
  - 条件从 `if(e.target.id==='vsf-customer-id') return;` 改为
    `if(e.target.closest('.vsf-field') || e.target.id==='vsf-customer-id'){...}` — 访销表单内所有
    searchable-value(客户/业务员/部门/仓库/车辆/司机)都跳过 creditInfo spot
  - 同时清理历史遗留的 #creditInfo spot(如果在访销字段内已创建过)
- 验证(浏览器实测): 选业务员后 operatorName="符号", deptName="销售四部" 联动正常,
  creditInfoExists=false, creditInfoInOperatorField=false ✅
- 教训(★): **全局"change 监听 + 类选择器"副作用会污染新字段** — 用 closest('.vsf-field')
  收紧作用域到特定表单, 而不是逐个 id 排除. 每次新增 .searchable-value 字段都要检查全局监听是否误触.

