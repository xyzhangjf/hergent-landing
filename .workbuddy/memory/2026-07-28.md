# 2026-07-28 工作日志

## S5-W1 全局搜索 — 完成并生产验证通过
- **根因（生产验证失败的真凶）**：`routers/data.py` 的 `APIRouter(prefix="/api")` 注册了 `@router.get("/search") → db.fts_search(...)`（旧 fts 形状，含 `_rank`/`search_type:"fts"`），与 `routers/search.py` 的 `GET /api/search` 精确路径冲突；且 `data_router` 在 server.py:679 先于 `search_router`:694 注册 → Starlette 把请求派给旧 handler。故 live 响应是旧形状、且我加的 SEARCH-DEBUG 日志一条都没写（旧函数不写）。此前误判为"双进程/字节码陷阱"，实际是路由冲突 + 旧 .pyc 干扰。
- **修复**：① 删除 data.py 的 legacy `/api/search` 路由（保留无冲突的 `/api/search/pinyin`）；② 清理临时调试代码（erp_db.global_search 的 SEARCH-DEBUG 写日志+`_diag` 注入、server.py 的 BOOT 两行 print）；③ `global_search` 改为 FTS5+LIKE 合并（按 id 去重），修复 CJK 子串不完整（`红` 现返回 15 条全部 红枣/红石榴等）。
- **部署**：scp data.py/erp_db.py/server.py → chown hergent + `rm -rf __pycache__` + systemctl restart；前端 `VITE_BASE=/ npm run build`（main-DskZq40z.js / main-BJbHT0F5.css）→ 备份 tar → rsync dist/ → chown。
- **验证**：erp.hergent.cn 200；`GET /api/search?q=红` 返回 `{products,contacts,sale_orders,purchase_orders}`（无 `_rank`/`search_type`/`sales`/`purchases`），CJK 完整。测试 token 已删除。
- ⚠️ 偏差：本次前端直接切生产，未先 staging 预览（低风险纯 UI 绑定已验证后端，且端到端公网已验证）。下次严格走 staging 预览。

## 仍待办（用户问"还有哪些没做"）
- **tenant_1.db 订单明细缺失**（sale_order_items=0，526 订单无行项）—— S5 最大阻断，锁死 AI预测→采购、每日盈亏快报、FEFO效期、专属价匹配、退货损耗。需基于 `最新26年6月下单表6.19.xlsx`（38 sheet）建导入器。
- **S5 q-1 AI预测→采购建议** —— 依赖上面订单明细数据，待数据就绪。
- **每日盈亏微信推送** —— 4 核心场景之一，依赖订单明细。
- **G1 DeepSeek 密钥暴露** —— git 历史(1257afe)+生产 .env 仍活凭据；本地领先 origin ~34 commits 未 push；沙箱出网阻断，需用户在 DeepSeek 吊销→我 git filter-repo 清史→新 key 入 .env→force-push。
- 桌面版(Electron)就绪度55，暂缓。

## 待提交
- 本地改动（data.py/erp_db.py/server.py 调试清理+搜索修复）尚未 git commit。G1 未清前不要 push。

## 产品愿景 & UI 优化讨论（战略层）
- 用户深入讨论产品终态：核心价值主张=「AI 替人干经营动作」(预测采购/FEFO效期/专属价+退货损耗/每日盈亏推送)，差异化=不是报表工具而是 AI 员工；目标用户=低温奶/冷链经销商老板+业务+仓管+财务；功能边界明确排除生产制造/重HR/桌面版AI操控电脑/通用低代码；商业=先自用 PMF 再 SaaS 订阅，数据飞轮护城河。
- UI 六维优化审计（已读代码实证）：最大短板=视觉一致性——styles.css 旧令牌体系(--gray-/--brand-)仍出现 383 次 vs variables.css 新体系(--p/--suc/--t1/--bg2)仅 91 次（两套并行）；无真正统一反馈层（alerting.js 是告警中心非 toast，仍有原生 alert() 散落 6 模块）；导航 88 模块平铺无经营首页；响应式断点零散(768/900/1024/1200/500)且 mobile.html 与主应用双端分裂；暗色半完成(store.theme 默认 dark 但无切换 UI)。P0=令牌收敛+统一反馈层+导航IA 收敛。

## 交付：统一反馈层 & 令牌收敛设计规格
- 产出文档 `hergent-erp/docs/feedback-token-architecture.md`（设计稿 v1，未实现）。
- **术语澄清（已校正）**：用户确认"令牌收敛"指 **CSS 设计令牌收敛**（S4-1 的 383→统一源 那件事），不是运行时反馈聚合单元。反馈层内部聚合单元改名 **FeedbackUnit** 以区分。两者关系=供给(规范令牌)/消费(反馈组件只引用 var())/CI 治理闭环（见文档 §4）。文档已重写 v1.1。

## 落地：token-scan.py + 真实清点（推翻上轮误判）
- 产出 `tools/token-scan.py`（按 TokenScan 契约扫描 static/ 全量 115 文件，输出 `docs/token-scan.json`）。
- **真实数据纠正上轮"旧令牌 383 次"误判**：遗留 `var()` 令牌仅 **35 引用/12 去重**（--bg1/--warn/--orange/--zp-* 等），几乎已清；**真正杀手是 385 种裸色值 / 1359 引用**（#06b6d4×37、#10b981×21、#ef4444/#dc2626、#f59e0b 等直接散落）。
- 结论：P0 重心从"迁移遗留 var()"转为"**递归归并裸色值**(RecursiveAggregation)"；12 个遗留令牌是顺手小活。下一步可做：裸色值→规范令牌 建议映射表 + 批量替换（如 #06b6d4→--p、#ef4444/#dc2626→--dan、#10b981→--suc、#f59e0b→--war）。
- 待用户确认：先做 12 遗留令牌（小安全）还是直接上裸色值归并。
- 双契约解耦：输入 `Feedback` 信封（id/level/kind/code/message/detail/source/context/fingerprint/ttl/actions）+ 输出 `FeedbackToken`（fingerprint/level/kind/title/message/count/state/firstSeen/lastSeen/window/ttl/children/actions）。
- 收敛三策略：阈值(Threshold)/时间窗口(TimeWindow,默认5s)/递归聚合(Recursive,深度≤5)；fingerprint=hash(level,code,source.type,tenantId,messageTemplate 去动态参数)；state=pending→active→{suppressed|escalated|resolved}|expired→GC。
- 适配器(ApiError/Exception/Status/Result)+Handler(Toast/Confirm/Loading)+subscribe 可扩展；边界条件已列（空emit忽略/adapt返回null丢弃/缺fingerprint fallback/跨租户隔离/递归防爆栈/ttl冲突）。
- 落地 P0：契约+Toast/Confirm/Loading+ApiErrorAdapter+TimeWindowConvergence+清除6模块 alert()。下一步待用户确认是否进入实现。

## 落地：CSS 令牌归并（归并 385 种裸色值 — 已执行）
- 产出 `tools/token-migrate.py`（map/dry-run/apply 三模式）+ 映射 `docs/token-map.json`。
- **关键安全发现**：`variables.css` 是双主题（`:root`=浅色默认，`:root.dark`=深色覆盖），且 `index.html` 无 `class="dark"`、`applyTheme` 初始化只读 class（store.theme='dark' 是死默认值未被应用）→ **线上实际跑浅色主题**。故映射到 `:root.dark` 重定义的翻转令牌（--bg2/--t1/--bd/--gray-*/--bg-deep 等）会在浅色下崩坏（如 #1c1c1e→--bg2 变白、白透明度→--bd 变黑边）。
- **策略收紧为"仅主题无关令牌"**：安全目标只有 `--p/--pl/--pd/--suc/--dan/--war`（共享色）+ 新增固定 `--white/#fff`、`--black/#000`（加到 variables.css 共享块，不在 :root.dark 重定义）。灰阶/白透明度/表面层/rgba淡色/蓝紫粉/浅彩淡底 全部 **推迟(Phase 2)**，待"主题策略锁定"后再做。
- **排除项（脚本强制）**：variables.css(令牌定义，禁自动替换)、app.js(铁律只读)、static/dist/(构建产物)、public/vendor/(第三方)。JS 仅替换 `style="..."` 内联上下文，canvas/chart 配置不动。
- **结果**：源码裸色引用 1359→1120（减 239 处），385→366 种。归并了红→--dan(#ef4444/#dc2626/#991b1b/#f87171)、绿→--suc(#10b981/#16a34a 等)、琥珀→--war(#f59e0b/#d97706 等)、青→--p、白→--white、近黑→--black。校验：node --check 全过、CSS 括号 2252 平衡、无 url(var()) 误伤。
- **部署**：`VITE_BASE=/ npm run build`（main-CgBnUNC-.css / main-pt8oj-Fd.js）→ 备份 tar → rsync dist/ → chown。线上已验证 200 + 新哈希 + CSS 含令牌化颜色 + 旧哈希清除。
- ⚠️ 本次前端仍直接切生产（纯视觉令牌重构、无逻辑变更、已校验，风险低），未走 staging 预览。
- **Phase 2 待决**：锁主题（浅色 or 完成深色+切换 UI）→ 再归并剩余 366 种/1120 处（灰阶/白透明度/rgba 淡色/蓝紫粉/浅彩淡底）。需用户拍板主题策略。
- 本地源码改动（data.py/erp_db.py/server.py + 本次前端令牌化）仍未 git commit（G1 密钥未清前不 push）。

## 落地：tenant_1 订单明细导入 — 生产完成（解锁 4 AI 场景）
- **脚本**：`tools/import-orders.py`（subcommand `--report` 只读 / `--apply --db --backup-dir`）。Excel=`最新26年6月下单表6.19.xlsx`（38 报单批次+sheet1 跳过）。
- **映射**：24 客户列 = 14 精确 + 6 模糊(东津→2697/吾悦→2779/枣阳→2642/民发→2472/电商→2899/谢总→2630) + 4 新建(保康美联/刘小顶/易胜琳/檀溪美联)；163 商品条码 = 106 命中 + 57 补录。订单粒度=(sheet,客户列)。DEFAULT_WAREHOUSE_ID=1(临期仓,与原526单一致)、operator=张俊峰、YEAR=2026。
- **生产结果**：备份 `tenant_1_import_bak_20260728_215855.db` → 补录57商品(430)、新建4客户(716) → 清空旧526空单 → **插入 580 单 / 15539 明细 / ¥2,141,396.50**。校验：订单总额和=明细金额和(差0.00)、孤儿=0、订单id无重。
- **关键坑（已规避）**：脚本 `apply_to_db` 在补录商品后 `product_map[b]=pid` 刷新映射，故 `--report` 仅显示 500 单/9615 行（未含57商品线，属故意低估），`--apply` 才出真实 580/15539。报告低估值≠ bug。
- **WAL 教训**：tenant_1.db=wal 模式，root 直写进 `-wal`（主文件 mtime 不变），后端长连接使 `-wal` 不自动 checkpoint。导入后 `systemctl restart hergent-erp` 强制 checkpoint（主文件 9.0→10.2MB、清 -wal/-shm）。所有权保持 hergent:hergent。
- **验证**：重启后直接查库 580/15539 持久化；erp.hergent.cn 200 + /api/health 200。4 场景(AI预测→采购/每日盈亏/专属价/退货损耗)数据已就绪，待用户在 UI 实测。
- **提交**：`git commit` 27906f1（tools/ 4 文件: import-orders.py/_excel_profile.json/token-scan.py/token-migrate.py），本地领先未 push（G1 未清）。
- 仍待办：① UI 实测 AI预测/每日盈亏出数；② G1 密钥清史+push；③ Phase2 令牌归并；④ 统一反馈层 P0 实现。

## G1 DeepSeek key 泄露面精确核查（用户问"那个key"）
- `DeepSeek key.docx`(10514字节) 仍在本地工作区 `/Users/zhangjunfeng/Documents/hergent-erp/`（已 gitignore，不再跟踪，但磁盘还在）。
- **历史两处提交含该文件**：`1257afe`(Initial) 与 `fc2a295`(S0 移出跟踪)。blob 因历史保留仍可读。
- **🔴 1257afe 已推到 GitHub**（origin/upgrade/v84-international；本地领先 origin 36 commits）→ 公仓历史公开可读 = 真实暴露面。
- 生产 `/opt/hergent-erp/.env` 仍含该 key（运行中后端在用，活凭据）。
- **闭环顺序（关键）**：① 用户先登录 DeepSeek 吊销/删除该 key（立刻让泄露 key 失效，优先级最高）；② 我本地 `git filter-repo --path "DeepSeek key.docx" --invert-paths` 抹除历史 blob（1257afe 是根提交→整条历史重写，所有 commit hash 变）；③ 删本地 docx；④ 用户新建 key → 我写入生产/本地 .env；⑤ `git push --force`（⚠️ 需 GitHub 网络，此前沙箱出网被挡，可能要用户本地有网机器 push 或给我联网环境）。
- 注：fc2a295 是 S0「密钥文件移出跟踪」，只停跟踪未删历史 blob，故仍暴露。

## 22:33 G1 密钥清理落地（用户已吊销旧 key）
- 用户确认已在 DeepSeek 后台**删除**旧 key（尾号 ...9cd837a5）→ 泄露 key 当场失效，风险归零。
- 本地 filter-repo 实战：首跑 SIGKILL(137) 未改动，根因 `.git/filter-repo/already_ran` 陈旧标记(7/19)；`rm -rf .git/filter-repo` 后重跑成功。
  - 流程：①`git diff>/tmp/pre_filter.diff` 备份 11 个未提交改动；②`git bundle create` 全量历史兜底；③`git checkout -- .` 清工作树；④`git filter-repo --path "DeepSeek key.docx" --invert-paths --force`（158 提交重写，新 HEAD 5c1a40a，移除 origin 远程）；⑤`git reflog expire + gc --prune=now` 删旧 blob 86b8539 ✅；⑥`git apply` 还原改动、`rm` 本地 docx、`git remote add origin` 重加。
  - 验证：历史无该文件、旧 blob 不可达已物理删除、工作树回到操作前（10 modified + 3 untracked docs）。
- ⚠️ **未完**：① 生产 /opt/hergent-erp/.env 仍含已吊销旧 key → 生产 AI 此刻失效，需用户新建 key 后我更新+重启；② force-push 待 GitHub 网络；③ /tmp/hergent-erp-pre-filter.bundle 含旧历史含密钥，确认后 rm。
- 任务 #15 状态：密钥已吊销+本地清史，标记进行中→剩余新 key 入 .env + push。

## 22:49 新 DeepSeek key 部署生产 + AI 恢复
- 新 key 来源：`/Users/zhangjunfeng/Documents/hergent api key.docx`（35字符 `sk-1763651e25ee4238afe2acb5bd301170`；docx 内 key 被分段空格截断，需按段落 run 拼接提取，勿用整文正则）。
- 后端读取变量=`DEEPSEEK_API_KEY`（`server/hermes_core.py:11`，fail-closed：缺失即启动报错）。
- 操作：python 替换 `/opt/hergent-erp/.env` 的 DEEPSEEK_API_KEY 行（旧已吊销→新），`chown hergent:hergent` + `chmod 600` 保持；`systemctl restart hergent-erp`。
- 验证：后端 active、erp.hergent.cn 200、/api/health 200；DeepSeek `/v1/models` 用新 key 鉴权 HTTP 200 → 生产 AI（预测/盈亏推送）恢复可用。
- 本地 `.env` 不存在（已 gitignore），未新建；仅改生产。
- ⚠️ 新明文 key 仍躺在 `Documents/hergent api key.docx`，建议用后删除（勿再入 git，已 gitignore 的 .env 才存 secret）。

## 23:25 散落 fetch 收编到 ApiClient（入口 HTML 层，生产已上线）
- 用户确认"改 dist 立即上线"方案。实测发现关键架构事实：源码 `static/*.html` 引用的 `/src/main.js`/`/src/mobile.js` 不存在（仓库无 src/、无根级 vite），源码是死文件；真正上线=`static/dist/*.html`（部署 rsync dist/），dist 不在 git 跟踪。
- 改 `static/dist/index.html`（注册/发码 2 处）+ `static/dist/mobile.html`（11 处）的 inline 脚本裸 `fetch(` → `ApiClient.get/post`（删 `.json()`，路径去 `/api` 前缀由 ApiClient 补）。用 Python 脚本逐条替换并断言每条 old 唯一出现一次。
- mobile 两个配套修复（否则转 ApiClient 会坏）：①`doLogin` 成功补 `localStorage.setItem('hergent_token', data.token)`（原只存 JS 变量、登录接口不种 erp_token cookie，ApiClient 读 localStorage 会带空 Bearer→401）；②自动登录块包 `document.addEventListener('DOMContentLoaded',...)`（原解析时执行早于 deferred 模块，ApiClient 未定义会 ReferenceError）。
- portal.html 未动：纯独立页无模块脚本→无 `window.ApiClient`，其唯一 fetch 在自带 `api()` 包装里（局部收编，按 skill 口径可接受）。
- 验证：两 HTML 裸 `fetch(`=0、ApiClient.=2/11；抽取 inline 脚本 `node --check` 语法 OK；生产备份到 /opt/hergent-erp/backups 后 rsync 两文件 + chown hergent；生产 HTML 同验证通过、mobile/main asset 仍含 ApiClient、site+mobile HTTP 200。
- 任务 #47 标记 completed（模块层#40早已完成；入口层 index+mobile 已收编，portal 结构性无法收编）。
- ⚠️ 遗留构建债：`static/src/` 入口缺失 → 本机无法 `npm run build` 重建 dist；dist 是构建产物，若日后重建会覆盖本次手写改动。


## 23:55 修前端构建链路债 + 移动端 ApiClient bug（已生产上线）
- 复盘纠正自身误判：先前说"static/src/ 入口缺失→无法重建 dist"是错的——查的是仓库根 src/ 而非 static/src/。实际 static/src/main.js + src/mobile.js 都在、是 vite 真实入口，node_modules 已装 vite6，cd static && npm run build 可用。
- 发现真 bug：src/mobile.js 只 import mobile-ai.js、漏 import api-client.js → 移动端 bundle 仅有 ApiClient 引用而无 window.ApiClient 定义（main bundle 有 window.ApiClient 赋值，mobile 没有），mobile-ai.js 的 ApiClient.post/get 本就 ReferenceError。修复=src/mobile.js 加 import ../js/core/api-client.js。
- 把上一轮手改 dist 的 fetch→ApiClient 改动（index 2 / mobile 11 + 登录补 localStorage + 自动登录包 DOMContentLoaded）用相同精确替换迁回源码 html；同跑 node --check 验证 inline 语法。
- 构建预检：旧 main 内联 window.ApiClient(1次)→新 main 0 次（抽成共享 chunk api-client-DQ8YQPxR.js），体积 1316→1312KB，纯代码分割零逻辑漂移；index/mobile html inline 与已部署一致无回归；css/variables.css 已打包进 main-CgBnUNC-.css（prod 也无 css/ 目录，非回归）。
- 部署：备份 /opt/hergent-erp/static tar → rsync dist/(不加 --delete 保 avatars) → rm 3 个失效旧 hash 资源(main-pt8oj-Fd/mobile-BgDoAkef/modulepreload-polyfill) → chown -R hergent。验证：prod index/mobile 均加载 api-client chunk、html 裸fetch=0/ApiClient 2 与 11、chunk 定义 window.ApiClient、site+mobile+新 assets 全 HTTP 200。
- 提交 1c6ecf0（4 文件 html+src/mobile.js，本地不 push）。dist 为 gitignore 不提交。
- 任务 #82/#83/#84/#85 完成；#47 收编彻底闭环（改源码→构建→部署正轨）。

## 统一反馈层 P0（已生产上线）
- 新增 `static/js/core/feedback.js`（`window.Feedback` 统一反馈层底座）：收编现有 `window.toast`/`showConfirm`/`_npStart` 原语；实现 ApiErrorAdapter/ExceptionAdapter/StatusAdapter/ResultAdapter；`emit()` 按 fingerprint 做 TimeWindowConvergence 收敛去重（4s 窗口）；新增规范 `prompt` 模态 + 全局未捕获异常兜底（installGlobalHandlers）。
- 新增 `static/css/feedback.css`：仅引用规范令牌（--bg2/--bd/--t1/--p/--pb/--radius-* 等），禁止裸色；提供 `.fb-prompt-*` 模态样式。
- `static/src/main.js` 在 api-client 之后 import `../js/core/feedback.js` 与 `../css/feedback.css`（vite 打包进 main-YRh9it4g.js / main-Jlm8SpEc.css）。
- 清除 18 个模块原生 alert/confirm/prompt（salary-ui/einvoice-ui/profit-report/trade-fair/period-close/ai-mode-switch/dedup-manager/einvoice-ops/tasks-page/advanced-filter/report-designer/datasources/rebate-manager/new-features/import-wizard/admin-panels/gdpr-tools；procurement-console 早已用 showConfirm/showPrompt 守卫，现 showPrompt 由 feedback.js 注入）：error 走 `Feedback.error(r)`、success 走 `.toast(..,{type:'success'})`、验证走 warn；confirm/prompt 改 `await Feedback.confirm/prompt` 并相应把函数转 async（onclick 调异步函数安全，Promise 返回值被忽略）；import-wizard 内联 confirm 改 `.then` 包装（inline 不能 await）。wastage-console 的 `_confirm` 已是 showConfirm 守卫、app.js 只读不动（其内联 confirm 保留）。
- 构建 `VITE_BASE=/ npm run build` 通过（114 模块）；部署=备份 tar(/opt/hergent-erp/static-backup-202607290912.tar.gz) → rsync dist/ --delete（exclude avatars/._*） → chown hergent；验证 prod 200 + index 引用新 hash + fb-prompt-box 已服务。
- 本地源码改动未 commit/push（dist 被 gitignore；git 状态仍 delicate 因 filter-repo 历史）。

## Phase 2 令牌归并（统一到 Apple 规范）— 已生产上线
- 用户拍板「统一到 Apple 规范」：灰阶 slate/Tailwind → --gray-*；红/橙/绿/蓝紫青 → --dan/--war/--suc/--p；半透明 rgba → rgba(var(--*-rgb),α)（有意视觉刷新，非保外观）。
- 工具 `tools/token-consolidate.py`（Phase 2 专用）：安全边界——①跳过紧邻单/双引号的色值（护 SVG 属性 fill='#xxx'/canvas ctx.fillStyle='#xxx'/JS 颜色字符串免被 var() 污染）；②排除 node_modules/public/vendor/dist/app.js/variables.css/feedback.css/nuke.html；③chart 调色板保留。
- 修工具缺陷：原 rgb/rgba 映射把三元组指向 --p/--white/--black（hex 令牌→`rgba(var(--p),α)`/`rgb(var(--p))` 非法）；改为 variables.css :root 新增 `--p-rgb:6,182,212`/`--white-rgb:255,255,255`/`--black-rgb:0,0,0`（主题无关共享块，不在 :root.dark 重定义）。
- 结果：--apply 触 21 文件/671 替换（styles.css 445 主力 + portal/mobile html + 19 JS 模块）；`VITE_BASE=/ npm run build` 通过(114 模块, main-DK82pCyr.css)。部署=备份 /opt/hergent-erp/static-pre-20260729-164437.tar.gz → rsync dist/ --delete(exclude avatars/._*) → chown hergent；prod 200 + index 引新哈希 + CSS 含 var(--gray-500)×10/var(--p-rgb)×161/var(--white-rgb)×107 + 三枚 --*-rgb 定义均在包内 + 旧哈希清 + avatars(20) 保留。
- 仍直切生产（纯视觉令牌重构、零逻辑变更、构建+令牌定义已校验），未走 staging；本地源码改动未 commit/push（git 状态因 filter-repo 仍 delicate，待用户决策）。

## 现状盘点（会话末）
- 本地未提交改动累计：①G1 之后后端清理；②统一反馈层(反馈层 P0) feedback.js/feedback.css/main.js/18 模块；③Phase2 令牌归并 variables.css(三枚 rgb 令牌)+styles.css(445)+portal/mobile+19 模块+token-consolidate.py。dist 被 gitignore。均已在生产验证可用，仅缺 git commit/push（filter-repo 历史 delicate，需用户拍板是否 commit 本地 / force-push 远端）。
