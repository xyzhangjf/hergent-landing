# 2026-09-06 工作日志

## 1. Hermes 官方更新监控（自动化，只读）
- 官方最新 = GitHub **v2026.8.31 / v0.21.0「Pantheon」**（2026-08-31），晚于已评估的 v2026.8.27(v0.20.6)；PyPI 仍停 0.19.0。
- 浅克隆 v2026.8.31 到 /tmp/hermes-231-src 核查 5 耦合点：① /v1/chat/completions 在新 gateway api_server 原生提供（默认端口 **8642**、认证 API_SERVER_KEY）；② **v0.21.0 恢复了 GET /v1/skills**（v0.20.6 只有 /api/skills），返回 {object,data[]} 与 hergent 期望匹配；③ **cron bridge 依旧无回归**（全仓 0 结果，cron 走 HTTP /api/cron/jobs + /api/jobs）；④ config 的 api_server/cors_origins 挪到 extra 或环境变量，mcp_servers 顶层键仍读；⑤ ~/.hermes/SOUL.md 仍原样读取注入。
- 结论 **A：维持生产 v0.19.0**（新能力 90% 桌面/多机器人向，对 hergent 无增益；升级会破坏 cron 契约 + config 迁移 + 进程模型重架构）。
- 环境事实：本机代理 127.0.0.1:63932 已失效，**GitHub 直连可用**（克隆前须 unset 代理变量，否则报 Couldn't connect to 127.0.0.1:63932）。
- 报告：`docs/hermes-update-monitor/2026-09-06.md`

## 2. Hermes 引擎 × WorkBuddy 四维度差距分析（只读分析）
- 输出 `docs/Hermes引擎×WorkBuddy-四维度差距分析-2026-09-06.md`。
- 真实代码实测基线：前端 hergent-cn-v2 = Vue3.5+Vite6+Pinia，20,264 行 / 21 页面 / 10 组件，依赖仅 vue+router+pinia+xlsx；后端 hergent-erp = server.py 5,037 行 + 85 个 router + hermes_core.py 1,506 行。
- **关键发现（P0 隐患）**：`useCardTrigger.js:144` 图表数据是**硬编码假数据** `series:[210,380,150,420,290,510,320]`，全项目无图表库。
- 其他短板：`_call_llm` 用 urllib **同步非流式**（timeout 20s、json_object、MAX_AGENT_TURNS=5）；记忆仅 dami_memory.db 三表（无分层/无画像）；无工具调用过程可视化；无命令面板/快捷键/a11y 弱；Forecast.vue 6,564 行巨石；多租户简报未闭环（scheduler.py:657）；DataSourceAdapter 为桩。
- 优势（不可丢）：4 套配方引擎、垂直人格+经营卡+cards 意图围栏、微信/企微/小程序触达、无 API 也能接入（Excel+舟谱+金蝶）、表格对账 MCP 强制走工具。
- 优先级结论：P0 = 图表去假(3-5人日) / 多租户简报(2-3) / 配方口径同步(3-5) / 工具可视化(4-6) / AI 流式化(5-8)；明确**不建议**做多模态生成、代码沙箱、通用专家市场、追 Hermes 大版本。

## 3. 按 P0 顺序执行（含两条误判更正）
- **更正1 图表非假数据**：四张真实卡后端均返 `chart.series`（daily_trend），前端 ResultCard 已自绘 SVG sparkline；`useCardTrigger.js:144` 的数组仅 `demoCard()` 首屏示例，非真实业务数据。唯一改进=真实卡空数据时显式"暂无趋势数据"（替代静默隐藏）。
- **更正2 副驾早已流式**：副驾对话走前端 `hermesChat` → `/hermes/v1/chat/completions`（Hermes SSE 流式），**不是** hermes_core 二级路径。原"_call_llm 同步非流式"对副驾 UX 不成立；hermes_core 仅遗留 /api/ai 旧路径用，非阻塞。
- **H4 多租户简报（真实 bug，已部署）**：`domain/wastage_tracker.py` 加 `_ensure_wastage_tables(db)` 惰性建表，修 init_db 只跑主库迁移→租户库缺 wastage_orders→7/8 租户简报货损段丢失；另补建 tenant_2 空 users 表。生产只读验证：货损段 1/8→8/8、三段齐全 8/8。已 git commit。
- **H2 工具调用可视化（新能力，已部署）**：`client.js hermesChat` 增加 `event:` 行解析 + `onTool` 回调，匹配生产 Hermes v0.19.0 的 `response.output_item.added/done`（Responses 风格，item.type=function_call，带 name/arguments/status）；CopilotDrawer 每助手消息增 tools[] 并渲染步骤（转圈/打勾+工具名+参数预览）。生产 Hermes 源码只读核查确认格式；前端已构建部署。
- **H5 配方同步**：此前已落地（3 触发点接线 + 4 skill 实时口径区块），dry-run 验证通过，本次仅复核确认。
- 报告续节：`docs/Hermes引擎×WorkBuddy-四维度差距分析-2026-09-06.md` 末尾"执行记录与更正"。
- **P0 实际剩余缺口≈0**；建议下一步优先 P1 中 M2 命令面板/快捷键 与 M4 Forecast.vue 拆分。

## 4. P1 后续（M2 已做 / M4 评估暂不拆）
- **M2 命令面板已落地**：新增 `components/CommandPalette.vue`，`⌘Shift+K`/`Ctrl+Shift+K` 唤起，模糊搜索跳 14 页面 + 高频动作（问副驾/切主题），↑↓/Enter/Esc；保留 `⌘K` 开副驾不变（Shell.vue onKeydown 扩展 shiftKey 分支）。纯前端，已部署（`cmd-panel` 入生产 bundle）。commit ced33b7。
- **M4 Forecast.vue 巨石拆分 = 暂不拆**：6564 行，P11-P16 增强均真实接线（非死代码）；Excel导入/AI审核台/返利看板等子模块深度读写共享状态 cross.rows/cross.period/rebatePushList/tmplList，无低风险可抽边界。结论改渐进式：将来改某功能时顺带抽那一个子组件。理由：零直接可感知收益 + 8-13 人日 + 全链路回归 + 与「运维成本不可控」红线相悖。

## 5. 深挖第二期：WorkBuddy 产品机制层借鉴（只读分析）
- 输出 `docs/Hermes引擎×WorkBuddy-深挖第二期-产品机制借鉴-2026-09-06.md`。
- **修正第一期两条判断**（hergent 底子比报告强）：① 记忆自进化已存在——`memory.py` 三表 corrections/patterns/interaction_log + auto_learn_from_action + get_smart_suggestions；② 定时任务非纯硬编码——`erp_db.py:885 scheduled_tasks` 表 + `platform.py:321`/`cron_tasks.py:43` create_job；③ 动作确认已存在——hermes_core.py 写操作全带 risk_level + confirm_text。
- **七大增量（WorkBuddy 独有、hergent 缺失，按优先级）**：P0=①主动澄清+结构化选项（确认完全缺失，无 clarify 机制）②每日经营日志 append-only + 跨会话回忆（memory 三表无"经营日志"维度、检索仅字符级关键词）③动作分级护栏统一（"只建议"散落 confirm，缺可切换的动手级别开关）；P1=④结果统一呈现（产物右栏缺文件卡片+下载）⑤可视化意图路由（仅 sparkline 单形态）⑥长期画像+30天蒸馏；P2=⑦技能/配方自进化+审计（recipe_sync 半自动、无变更审计）。
- 核心洞察：hergent 缺的不是能力（记忆/自动化/确认都有雏形），而是把"功能"升级为"产品机制"（可见/可切换/可追溯/越用越懂）。最该先做=①主动澄清（唯一零架构风险、纯提示词+轻前端、体感收益最高）。

## 6. 大厂流程调研 + 一人公司产品开发 SOP 落地（选项 A 全部端到端）
- **背景**：用户问大厂产品开发流程 + 是否要做开发规范。结合用户一人公司 + AI 杠杆现状 + 已有 3 个项目（hergent-cn-v2 / hergent-erp / miniprogram）的实践沉淀，给出 3 个选项（轻/中/重 SOP），用户答「A」=极轻量化 SOP。
- **落地 9 个文件 = 1475 行**（无 git 改动，纯文档/Skill/CLAUDE.md）：
  1. **SOP 主文档** `laozhangai-product/.workbuddy/SOP/PRODUCT-DEVELOPMENT-SOP.md`（203 行）—— 一人公司 AI 杠杆版，5 条总原则 + 3 阶段（开发前/中/后）+ R0-R4 风险等级 + AI 工作流约定 + 5 种禁止说辞 + 与现有 Skill/Memory 协作 + 不做的事边界。
  2. **PRD 模板** `templates/PRD-TEMPLATE.md`（167 行）—— 10 段：元数据/用户场景/业务目标+验收/真伪需求判断（5W）/In-Out Scope/风险评估/11 共享接口检查/UX 要点/时间规划/用户确认 + 自查清单。
  3. **技术方案模板** `templates/TECH-TEMPLATE.md`（219 行）—— 10 段：元数据/架构图/5 技术选型/接口数据流/复用与依赖/部署与回滚/测试计划/风险缓解/工期/决策记录。
  4. **风险评估表** `templates/RISK-CHECKLIST.md`（175 行）—— 5 维 0-4 分（技术/业务/数据/部署/回滚）→ 取最高 = R0-R4 + 必走闸清单 + **Hergent 特殊 6 项**（11 共享接口/RBAC/ERP_SECRET/safe-delete/rsync 陷阱/Hermes SSE 直连）。
  5. **Postmortem 模板** `templates/POSTMORTEM-TEMPLATE.md`（173 行）—— 10 段：元数据/5W/时间线/影响/根因/修复+防复发/检测缺口/5W2H 行动项/4 去向沉淀（SOP/CLAUDE.md/Skill/MEMORY.md）/教训。
  6. **3 项目级 CLAUDE.md** —— 后端 `hergent-erp/CLAUDE.md`（144 行）、前端 `hergent-cn-v2/CLAUDE.md`（134 行）、小程序 `miniprogram/CLAUDE.md`（128 行）。每个聚焦本仓库铁律，**保留** laozhangai-product/CLAUDE.md 桌面版旧规范不动（用户已表态桌面版冻结）。
  7. **入口 Skill** `~/.workbuddy/skills/product-dev-sop/SKILL.md`（132 行）—— 触发词清单（中文/英文）+ 4 引用路径 + 4 类任务调用行为（新产品/修改/事故/决策）+ 6 种失败模式拦截 + 与 5 个 hergent-* 专项 Skill 协作 + SOP 自身迭代。
- **设计要点**：
  - SOP 不是评审会/角色矩阵/Jira 流程（不做重型）；**模板 + 触发式提醒 + AI 主动引用** 是一人公司最佳平衡点。
  - PRD 不写完不写代码（即便 5 分钟也强于「想到就开干」）；改完必真机验证（禁用 node --check 假阳性）；踩坑必写 Postmortem。
  - Hergent 特殊项（前 11 共享接口 / ERP_SECRET / safe-delete / rsync .env）全部内嵌到 风险评估表 + 3 项目级 CLAUDE.md，AI 改前 grep 即知。
  - Skill 入口触发词覆盖中英文常见说法，AI 接到「开发新产品/做流程/事故复盘」主动引用。
- **保留现状**：laozhangai-product/CLAUDE.md（旧桌面版 Electron 规范）不动；新增 3 个项目级 CLAUDE.md 与之互补（桌面版仍单独有 iron law 用法）。
- **后续迭代**：每月最后一周回看本月 Postmortem，TOP 3 进 SOP/CLAUDE.md/Skill。

## 7. 深挖第二期 · 7 项增量全量落地（P0→P1→P2）
- 用户「好，你按顺序开始做吧」→ 7 项端到端落地并生产部署。前 6 项（P0-①②③ / P1-④⑤⑥）已完成：① 澄清协议（useCardTrigger.js clarify 解析 + CopilotDrawer 选项卡 + 生产 SOUL.md 澄清协议）② 每日经营日志（erp_db daily_logs + log_daily_metric + scheduler 落日志 + CopilotDrawer 时间锚点 + GET /api/ai/daily-log）③ 护栏开关（CopilotDrawer aiGuard localStorage）④ 文件卡片（chat_attachment GET /download/{file_id} + 产物文件卡片）⑤ 可视化选型（ResultCard mini/bar/donut + 返利卡改柱状）⑥ 画像（GET /api/ai/profile + CopilotDrawer 10min 缓存注入）。
- **P2-⑦ 技能/配方自进化 + 审计（本轮收尾，commit 78211f4）**：
  - 新增 `server/routers/recipe_evolution.py`：`recipe_proposals` 提案表（租户库）+ GET/POST `/api/ai/recipe-proposals` + POST `/api/ai/recipe-proposals/{id}/review` + GET `/api/ai/recipe-audit`。
  - `record_recipe_audit()`：字段级 diff 写**主库** `audit_logs`（module=recipe，哈希链防篡改），`ref_id` 编码 `tenant_N:模块`，跨租户可追溯；`detail` 记录「谁改/改什么/影响哪家」。
  - 三处配方保存端点接入审计：货损 loss_workflow.py / 工资 payroll_workflow.py / 预报 forecast_config.py。
  - 审批即合并：accept → merge 进正式配方（_sanitize_recipe 边界校验）→ recipe_sync 同步 skill → 落审计；reject → 仅标记。严守「AI 只建议不擅自下单」。
  - **生产 E2E（demo 租户 tenant 10，未污染真实租户）**：建提案→列表→accept(threshold_days 7→8 合并)→reject 全链路通过；审计轨迹正确回显 `[419] demo_boss | 修改货损配方 | ref=tenant_10:loss_recipe | threshold_days: None→8`；复位后 `[420] ... 8→7`。
  - 部署 `bash deploy.sh`（health 200）；新端点经 /api/ai 前缀命中 RBAC "chat" 模块。
- 报告续节：`docs/Hermes引擎×WorkBuddy-深挖第二期-产品机制借鉴-2026-09-06.md` 末尾「§5 执行记录」。
- **7 项增量全部上线生产，护城河自进化闭环 + 多租户变更可追溯达成。**

## 8. P2-⑦ 前端入口接通（副驾对话内「AI 提案 → 一键采纳/忽略」闭环）
- **协议层**：生产 `~/.hermes/SOUL.md` 注入提案协议（已备份）。AI 发现「该改的口径」时输出 ```proposal 围栏（module/title/changes/rationale），只提案不擅改；已列明货损/工资/预报字段名供 AI 正确填 changes。
- **前端解析**：`useCardTrigger.js` 新增 `PROPOSAL_RE`/`extractProposal`/`stripProposalFence`（module 四选一校验 + changes 非空对象校验，非法/空返回 null）。
- **前端渲染**：`CopilotDrawer.vue` 提案卡片（模块标签/字段改动/理由 + 「采纳并写入配方」/「忽略」按钮 + 采纳后状态回显）。
- **applyProposal()**：采纳/忽略 = `POST /api/ai/recipe-proposals`(source=ai) → `POST /api/ai/recipe-proposals/{id}/review`(accept/reject)；采纳后端合并进配方 + 写审计。
- **验证**：node 单测 7 用例全过（正常解析/围栏剥离/非法 module/changes 非对象/空 changes/无围栏/多行+尾注）；build 成功；rsync 部署 + chown；生产 bundle `Shell--p_wkxJl.js` grep 命中 `recipe-proposals`/`采纳并写入配方`/`cp-prop-ops` 等。
- 前端 commit **7342f44**（laozhangai-product 仓库 main 分支）。后端 78211f4。**P2-⑦ 端到端闭环（AI 提案 → 老板审批 → 配方生效 → 审计留痕）全部打通。**

## 7. 对照 SOP 做自审计（用户问：现在在哪、还要做什么）
- **位置**：3 线全在"开发中"的维护+小特性迭代期——以「事故→止血→治本」和「用户提单→一次性端到端」为主，不是新立项/灰度阶段。
- **缺什么**：R0 缺口 2 项（Postmortem 缺归档 / PRD-Risk 未流程化触发）；R1 缺口 2 项（回滚策略模板化 / 监控告警）；R2 缺口 2 项（埋点 / CI/CD 全自动）。
- **本周建议**：① 归档 cookie 优先 + Vue3 unwrap 两次事故为 Postmortem；② 选 1 个未收尾需求（返利 L2 到货周期或舟谱模板）走 PRD-Risk-Tech 全链路首次触发；③ 验证 Skill 是否真在下次新需求主动问 5 必问。
- **不做**：CI/CD 全自动 / 测试覆盖率门槛 / 灰度发布 / 新工具栈 / 桌面版专项——合伙人或扩到 3-5 人再升级。
EOF\necho \"appended §7\"; wc -l /Users/zhangjunfeng/Documents/laozhangai-product/.workbuddy/memory/2026-09-06.md
## §8 深挖第三期（WorkBuddy 记忆/反馈/调度/会话机制层）
- 产出 `docs/Hermes引擎×WorkBuddy-深挖第三期-记忆反馈调度会话-2026-09-06.md`，6 项增量，与前两期零重叠。
- **核实发现**：① 副驾记忆单向——corrections/patterns 只喂旧 dami/accountant 角色，副驾主链路(Hermes SSE)不写也无反馈按钮；② 副驾会话仅 localStorage(hergent_chat_sessions_v1, 30 条)；③ **bug**：platform.py:321/327/332 调全仓不存在的 db.scheduled_task_create/list/delete → scheduled-tasks 三端点稳定 500 死代码；④ 无 AI 待办/提醒系统；⑤ 全仓无 token/usage/cost 计量。
- P0=①副驾反馈纠错闭环(👍👎→save_correction) ②AI待办/提醒；P1=③服务端会话持久化 ④自然语言排程+RRULE(修死代码)；P2=⑤跨会话检索 ⑥用量成本归因。

## 8. SOP 首次实战：归档 2 次事故为 Postmortem（2026-09-06 12:06）
- **背景**：落实 §7 自审计中 R0-必走第 1 条——「归档最近 2 次事故为 Postmortem」，按 `PRODUCT-DEVELOPMENT-SOP.md` 的 `POSTMORTEM-TEMPLATE.md` 10 段模板严格填。
- **产出 2 份 Postmortem（共 391 行）**：`laozhangai-product/.workbuddy/postmortems/` 目录新建，2 文件就位：
  1. **PM-20260905-cookie-priority.md**（195 行，P1）—— 22:31 boss 登录反复被踢。后端 `_extract_token` cookie 优先于 Authorization 头 → 候选短路 → 6 接口 401。 治本 = 候选容错解析；commit hergent-erp `91b20ce`。
  2. **PM-20260905-vue3-unwrap.md**（196 行，P1）—— 23:1x 点「改单」白屏。Vue 3 `<script setup>` 内普通函数闭包 `computed` 不会自动 `.value`，2 处函数体 `gapSet.has(...)` 漏 .value。 治本 = 2 处补 .value；commit hergent-cn-v2 `f4daf7a`。
- **每份 Postmortem 严格按模板 10 段**：元数据 / 5W 一句话 / 时间线（精确到分钟）/ 影响（量化）/ 根因（直接+根本+触发链+误判列表）/ 修复+防复发 / 检测缺口（监控/单测/e2e 三缺）/ 行动项（cookie 8 项 / vue3 9 项）/ 4 去向沉淀（SOP/CLAUDE.md/Skill/MEMORY.md 都答了）/ 教训 5 条。
- **自检 grep 命中**：
  - MEMORY.md L27「Vue 3 <script setup> 自动 unwrap 规则」——vue3 PM §9.4 已落 ✓
  - hergent-cn-v2/CLAUDE.md L32 §3「Vue 3 <script setup> unwrap 规则」——vue3 PM §9.2 已落 ✓
  - hergent-erp/CLAUDE.md L17 §1 ERP_SECRET / L23 `_extract_token _resolve_auth` / L45 §6 11 个共享接口 / L102 `.auth_resolve_test.py`——cookie PM §9.2 已落 ✓
- **遗留**（两 PM §9.1 一致）：SOP 主文档具体整合留待「月度 Postmortem 复盘」统一刷入——避免单次插入违反 SOP 的"月度聚合"节奏。
- **本月可触发的月度 Postmortem 复盘**（月底）：回看这 2 份 + 之前 3 次（rsync .env / safe-delete / bcrypt 8），TOP 3 进 SOP / CLAUDE.md / Skill（防 Skill 老化失真）。本月归档案将从 0 → 5。
- **触发验证**：本次写 Postmortem 时 AI 主动指出「这两份之前确实都没按模板归档——下次类似事故反馈时我会主动说『Postmortem 写了吗』」（按 SOP 触发词已生效）。
## 9. 返利板块「目标规则/年度合同」整合评估（业务口径对齐，未动代码）
- 纠正误判：**品牌目标(金额)=厂家年度合同的月度分解，同一数据源**（蒙牛1200万→8月110万/9月90万）；**月返以品牌月度目标为基准**；**单品目标=独立按月+坎级(阶梯)返利**。
- 用户决策：① 返利规则放「目标规则」+ 年度目标(月度分解)整合进目标规则，品牌目标金额+返利规则合一；② 品类/客户/渠道 3 维度基本不用，可删。
- AI 指出隐藏分层：年度合同 tab 混「配置层(年度目标/月度分解/比例/阶梯)」与「资金层(计提/结算/申领/余额)」两层；整合=配置层并入目标规则、资金层降级为「返利结算」并引用品牌目标。
- 待确认：品牌目标返利规则粒度(年度一套 vs 按月微调)；确认后分 3 阶段落地(删维度+改名 → 品牌目标承载月度分解 → 结算引用)。
## §9 第三期 6 项增量已全部上线生产（2026-09-06 12:xx）
- 后端 cda7e70+b97d3e8：新增 routers/ai_assist.py（①feedback②reminders③sessions⑤search-chat⑥usage）；scheduler.py 每分钟扫 ai_reminders 到点推送(daily/weekly/monthly)；删 platform.py /api/platform/scheduled-tasks 死代码(原 500)。
- 前端 b9a1fc8：CopilotDrawer(对/错反馈+reminder卡片+历史搜索+usage上报)、store 会话双写+跨设备拉取、useCardTrigger extractReminder、CronJobs「仅执行一次」。
- 生产 SOUL.md 注入 reminder 协议（要记得/提醒→围栏→记下卡片）。
- 部署：后端 health 200、前端 bundle grep 命中全部新端点；E2E 全通；reminder 解析 8 用例过。
- 踩坑：并行 Edit server.py import+include 竞态丢 import → NameError ai_assist_router → 补 import 再部署。教训：server.py 的 import 与 include 必须分两轮顺序 Edit，勿并行。
- 留白：⑥用量用字符量近似(SSE直连不走server.py，token精确需网关)；④完整RRULE未做(一次/永久+daily/weekly/monthly够用)；⑤检索用LIKE非向量。
## 10. 返利板块整合·最终口径敲定（品牌目标金额固定 + 返利率月度浮动）
- 用户最终拍板：**目标基本定下不变、按月调的可能性不大；返利每个月都会调**（这个月返10个点、下个月返8个点）。
- 结论：品牌目标 = **金额层(年框+12月分解,固定) + 返利层(按月一条,浮动)** 两层分开存；与单品目标(按月一条,目标+坎级都按月)形成对照。
- 此前 A/B 二选一(年度一套 vs 按月微调)都不成立——正确是"金额用A、返利用B"的混合。已写入 MEMORY.md 返利口径。

## §10 深挖第四期（交付·洞察·商业化层）
- 产出 `docs/Hermes引擎×WorkBuddy-深挖第四期-交付洞察商业化-2026-09-06.md`，6 项增量，与前两期零重叠。
- **核实发现**：① 副驾只会说不会存——无「存报告/归档/导出」，关了抽屉就丢（WorkBuddy present_files 机制缺失）；② `/api/ai/insights`→ai_tools.ai_analyze() 是规则模板非 LLM，主动推送(低库存/逾期/今日要务)全是 SQL 模板，无「LLM 自然语言经营洞察」主动通道（护城河正主空缺）；③ cron_tasks.py 仅 CRUD bridge，跑完结果不回流 hergent（无 execution_log）；④ usage 只计量无 quota/分级；⑤ /api/ai/profile 前端拼聚合数非自动提炼；⑥ 无 onboarding 向导。
- P0=①副驾分析结果固化归档 ②用量配额分级；P1=③个性化LLM经营洞察 ④cron结果回流；P2=⑤服务端画像自动提炼 ⑥onboarding向导。

## 11. 返利整合·阶段1（删 3 维度 + 改名）部署完成（2026-09-06 14:0x）
- Rebate.vue 删 category/customer/channel 3 维度全部代码点：维度下拉 option、channel select 分支与 scopeDatalist/scopePlaceholder 的 category/customer 分支、categoryOptions/loadCategoryOptions/customerOptions/loadCustomerCustomers 候选与校验提示、dimensionOptions 兜底、dimText 精简、onMounted 去掉两个 load、年度合同表单 cf-tip 文案去掉 3 维度。
- 改名：主 Tab「目标规则」→「目标与返利」；「年度合同」→「返利结算」（tab 标签/空态/副标题/跳转引导）；保留业务对象名「年度合同」（录入的确实是合同）。
- 部署：`Rebate-QYSvjK8Q.js` + `Rebate-CbbRyjs8.css`，双侧 md5 MATCH_OK（JS 2c2197b2… / CSS fda778cc…），index-CxLHZn5i.js 引用正确。上次 rsync 被中断未落地，本次重跑确认。
- 剩余阶段：2 品牌目标拆两层（金额层年框+12月分解 / 返利层按月）；3 结算引用配置层。

## §12 深挖第四期 6 项增量全量上线生产（2026-09-06 14:40）
- 用户「按顺序开始做」→ 6 项端到端落地：①②③④⑤⑥ 全部通过生产 E2E。
- **后端**（4 文件）：
  - `routers/ai_assist.py`（承载 ①②③⑤⑥）：①`ai_reports` 租户表 + POST/GET/DELETE `/api/ai/reports[/{id}]`；②主库 `ai_quota` + `/api/ai/quota`（tier={free:20万, pro:200万, enterprise:1000万} 字符/月）；③`/api/ai/llm-insight` 调 LLM；⑤`ai_profile_cache` + `/api/ai/profile-llm` 读+`/api/ai/profile-refresh` 写（读最近10会话+全部纠错→提炼JSON{preferences/calibers/summary}）；⑥`/api/ai/onboarding` 幂等写货损/工资/预报默认配方。
  - `server/ai_insight_llm.py`（**新增**，③核心）：`_gather_data_points()` 聚合客户近7天vs前7天/应收/临期/返利/货损；`build_llm_insight()` 调 `chat_ai` 生成 + 失败回退 `_rule_fallback`；`push_llm_insight()` scheduler 复用。
  - `scheduler.py`：新增 `_log_cron_execution(task,result,status)` 落 `cron_execution_log`；成功/异常均记；每分钟循环加 7:35 `llm_insight` 钩子（`_should_run("llm_insight", 86400, now)`）。
  - `routers/cron_tasks.py` + `import erp_db as db` + `GET /api/cron/executions`（limit 默认 50）。
  - `memory.py` + `get_all_corrections(user_id, limit=30)` 供画像提炼用。
- **前端**（4 文件）：
  - `pages/AiHub.vue`（**新建**）：单页聚合 ① 我的报告（CRUD+Markdown 导出）②用量配额（进度+套餐切换）③ AI 经营洞察（生成+数据要点）⑤长期画像（刷新+展示）。
  - `router/index.js` + `path:'ai-hub'` 路由；`components/Shell.vue` 侧栏（桌面+移动）在「定时任务」与「设置」间插「AI 中心」+菱形星标 SVG。
  - `components/CopilotDrawer.vue` header 加「存为报告」按钮（`:disabled="!hasChat || savingReport"`，存完 toast 引导）。
  - `pages/CronJobs.vue` + 「执行记录」卡（task_name/状态标签/结果摘要/时间，展开看完整 result_text）。
- **踩坑修正**：
  1. `ai_insight_llm.py` 初版 `db.get_db().execute(...)` —— `get_db()` 是 generator-based contextmanager → `with db.get_db() as conn:`；
  2. `ai_assist.py` 末尾误加 `logger_w = ...` 重复定义 → 清理为单一 `def logger_w(msg)`；
  3. `cron_tasks.py` 缺 `import erp_db as db` → 补；
  4. ④前端 CronJobs.vue 误插入点到 modal 之前 → 重读全文后改在 `<table class="tbl">` 闭合处下；
  5. E2E 初版 cookie session → 撞 CSRF → 改 demo-login Bearer 直调。
- **部署**：后端 `bash deploy.sh`（health 200，systemctl active）；前端 `mv dist /tmp/...` → `npm run build`（managed Node 22.22.2）→ rsync + chown。新 bundle `AiHub-D8mQ9xql.js` + `CronJobs-ByZPGaLX.js` 经 nginx `https://hergent.cn` 200 OK。
- **E2E**（demo 租户 tenant 10，Bearer-token）：reports CRUD 全过、onboarding 幂等写工资+预报默认配方、profile-refresh 提炼成功（含纠错「应该是5天」进 calibers）、llm-insight 实测应收 `300834.23` 正确（get_db 修复验证）、quota/executions 回显正确。
- **报告续节**：`docs/Hermes引擎×WorkBuddy-深挖第四期-交付洞察商业化-2026-09-06.md` 新增「§7 执行记录」5 小节（落地清单/代码钩子/踩坑/部署+E2E/商业化路径）。
- **里程碑**：hergent AI 中心从「副驾对话」进化为「结果沉淀 + 主动洞察 + 分级计费 + 客户冷启动」完整经营副驾闭环；与第三期（记忆/反馈/调度/会话基础设施）+ 第二期（机制层）+ 第一期（四维 gap）形成完整梯子。

## §13 深挖第五期（AI 工程化 · 数据治理 · 反脆弱韧性，只读分析）
- 产出 `docs/Hermes引擎×WorkBuddy-深挖第五期-AI工程化数据治理韧性-2026-09-06.md`，6 项增量，与前四期零重叠。
- **核实发现**：① 8 个生产租户 SQLite 从没自动备份过——`grep "sqlite3.*backup"` 0 处，deploy.sh 无备份段；② LLM 调用黑盒——`ai_engine.call_ai/chat_ai` 无 trace_id，正常调用失败只 stdout 日志；③ 配方/规则改了无回滚——`loss/payroll/forecast` 三处无 `export_recipe`/`import_recipe`；④ `audit_log` 已存在但仅 7 处（auth 5 + accounts 3），销售/收款/调价/对账 0 处审计；⑤ `ai_engine.py` 多模型仅全局 env 二选一，无场景化路由；⑥ AI 主链路输出无 confidence 字段，兜底升级 0 处。
- **意外发现**：特性开关其实已经半成熟——`server.py:4250 feature_flags_list/set/check` + `voucher_engine.py:222` 已读 `db.feature_flag_list` 并支持 rollout_pct；只是没扩散+无 UI。本期不重复造。
- P0=①多租户库自动备份 ②LLM全链路tracing+AI健康看板 ③配方导出导入；P1=④细粒度审计 ⑤多模型路由 ⑥AI兜底升级。
- **核心洞察**：第四期解决「会做得爽」；本期解决「做得稳、调得回、出问题救得活」——一人公司 solo 最怕的不是 AI 不够聪明而是哪天盘坏了 + 配方调崩了无人能救。一日不补 backup/diff/tracing/router/Fallback 这 5 条命门，一天崩盘全清零。
- **4 个用户待确认**：①启动 6 项？ ②备份保留策略 默认 7+4？ ⑤多模型路由是否要用户可手动覆盖？ ⑥兜底通知通道 默认 副驾+邮件+微信 全开？
- **跨期梯子**：①(功能)→②(机制)→③(底座)→④(交付/商业化)→⑤(工程化/治理/韧性)。

## §14 深挖第五期 6 项增量全量上线生产（2026-09-06 16:30）
- 用户「做」→ 6 项端到端落地（按 P0①②③→P1④⑤⑥ 顺序）。

### ① 多租户库自动备份 + 一键恢复（P0）
- **新发现**：现有 `_run_backup()` 自 2026-08-22 起静默停摆（15 天没新备份），且**只备 erp.db 不备 tenant_*.db**。8 租户生产数据 0 备份裸跑。
- 新增 `server/routers/admin_backup.py`：用 SQLite online backup API（`Connection.backup()`）+ 多 db glob（erp.db + tenant_*.db）；保留策略日7+周4；提供 list/restore(dry_run 默认)/run/status 4 端点。
- `scheduler.py` 02:00 cron 替换旧 `_run_backup()` 调新 `_do_backup()`；新增 `_cleanup_old()` 自动淘汰过期。
- AiHub 加「数据备份」卡：状态/最近/总占用 + 立即备份按钮 + 一演练恢复 + 列表。
- 端到端：跑了 1 次手动备份，11 个 db 文件全部成功；dry-run 返回 11 个文件 plan 全部 writable=true。
- **真实风险修复**：从此 8 租户生产数据每天自动备份。

### ② LLM 全链路 tracing + AI 健康看板（P0）
- **新发现**：`ai_engine.call_ai/chat_ai` 黑盒，失败只 stdout 日志，老板看不到。
- ai_engine.py 加 `_llm_log()`：trace_id（UUID12）+ tenant_id + provider + model + latency_ms + input_chars + output_chars + ok + error 落主库 llm_call_log。
- `call_ai/chat_ai` 每次调用（含 missing-key 早返）都落 log。
- 新增 `server/routers/ai_observability.py`：/health (24h 看板：成功率/平均延迟/失败归因/各租户分布) + /recent (最近 N 条) + /route-status (⑤配套)。
- AiHub 加「AI 健康看板」卡 + 成功率高亮红色 / 模型分布 / 失败归因。

### ③ 配方/规则导出 + 导入（P0）
- 新增 `server/routers/recipe_io.py`：GET `/api/ai/recipe-export` 导出 loss/payroll/forecast 三套为带 `_meta/schema_version/tenant_id/exported_at` 的 JSON；POST `/api/ai/recipe-import` 严格 schema 校验 + dry_run 默认 true（不写）+ 跨租户 warning。
- 导入复用各模块的 `_sanitize_recipe` + 审计（`record_recipe_audit`），实操导入逐套 upsert + 触发 `recipe_sync`。
- AiHub 加「配方导入导出」卡：导出按钮 + JSON 文件选择 + 演练 + 确认导入。
- 端到端：export OK、dry-run plan 正确、跨租户 warning 显示、实操导入 success。

### ④ 关键操作细粒度审计（P1）
- 新增 `server/routers/audit_query.py`：GET `/api/audit/logs` 按 module/user/keyword 过滤 + 分页；GET `/api/audit/modules` 列模块统计。
- **5 大写入路径全覆盖** audit_log：sales.py POST /sale-orders + cancel、finance.py POST /payments、data.py PUT /products/{pid}（仅调价字段）、inventory.py POST /inventory/transfer；reconciliation.py 已有（不重复）。
- **踩坑修正**：audit_log 写在租户库（_auth 后的 tenant context），audit_query 最初查主库→空。修复用 tenant context。验证后触发 price update → audit_logs 出现 product_price 模块、id=1/2 哈希链正确（105374395dfd → 94a5a65ff2c1）。
- AiHub 加「审计日志」卡：模块下拉 + 关键词搜索 + 时间倒序列表（含 hash 前 12 位）。

### ⑤ 多模型路由（P1）
- ai_engine.py 新增 `_pick_route(messages, hint)` 决策器：<1K → deepseek · >8K → qwen · 中间 → 默认 · 显式 hint → 强制；`_record_result()` 累计失败 streak，>=3 连续失败开 5 分钟 cooldown 强制本地 fallback。
- `call_ai/chat_ai` 接受 `model_hint` 参数（None/deepseek/qwen）。
- `get_route_status()` 暴露实时状态（fail_streak / cooldown_remaining_s）。
- 路由端点 `GET /api/ai/observability/route-status`；AiHub 加「模型路由」卡（双 provider pill）。
- **踩坑修正**：AI_KEY 缺失提前 return 让 streak 不累计 → 修复让 missing-key 也走 `_record_result`，让 dashboard 真实反映「真没配」状态。
- 端到端：4 次 llm-insight → qwen.fail_streak=4, cooldown_remaining_s=299s ✓
- **CopilotDrawer header 切换器** 暂略（v0.19.0 SSE 直连 Hermes 需协调，标记 P2 后续；功能上 model_hint 接口已就绪）。

### ⑥ AI 兜底升级给人工（P1）
- 新增 `server/routers/ai_fallback.py`：`record_fallback()` 落 ai_fallback_log（tenant）；连续 ≥2 次 60s 内自动推 ai_reminders 通知（复用第三期②通道）；提供 `/api/list`（含 only_unacked 过滤）+ `/api/acknowledge`（支持 ids 或 all）。
- ai_engine.py 在 missing-key / force_local / 异常 三处失败路径调 `_record_fallback`；chat_ai 成功后还做低置信度检测（`parse_confidence()` → confidence<0.4 也记兜底）。
- AiHub 加「AI 兜底记录」卡：失败/低置信度分类显示 + 「全部知道了」一键 ack。
- 端到端：3 次 llm-insight 触发 → 3 条 failure events，id=2/3 notified=True 自动推 reminders 2 条「副驾需要你帮忙」(created_by=ai-fallback)；ack 后 only_unacked=0 ✓

### 总览
- **6 项全部上线生产，TaskList 26/26 completed**。
- 跨期对比：①(四维gap)→②(机制层)→③(底座)→④(交付/商业化)→⑤(工程化/治理/韧性)→已补齐「运维三防线」：备份(可恢复) + tracing(可观测) + 审计(可追溯) + 路由(可控成本) + 兜底升级(降 AI 误信风险)。
- **关键价值**：① 修复 8 租户 15 天裸跑最高风险；② 让副驾从「黑盒」变「全链路」；③ 配方可回滚；④ 5 大关键操作全留痕；⑤ 按场景路由降本；⑥ AI 不会装外行——这五条共同支撑 hergent「做得稳、调得回、出问题救得活」的护城河。

## 14. 小程序注册信息·一键复制粘贴包（用户确认「要」）
- 产出 `forecast-order-miniprogram-20260812T023419087Z/小程序注册信息-复制粘贴包.md`（12 节）：注册流程/主体信息逐字段/经营范围原文/管理员/名称 3 候选/简介模板/类目/服务器域名/隐私勾选/测试账号/审核备注模板/拿 AppID 后动作/时间线。
- 主体事实（已核）：湖北省小赫智体数字科技有限公司，91420606MAKF1YPG5Y（**用户当日更正**；本轮首版误写 91420600MAK1YFG5Y，已全量更正），自然人独资，法人=张俊峰本人，注册资本 50 万，成立 2026-05-26，住所=襄阳樊城区王寨街道松鹤路社区创业路5号。经营范围含 AI 软件开发/互联网销售，**无食品/乳制品**。
- 关键指引：主体验证走**法人微信扫码**（1-3 天，本人即法人，最快）；类目=**工具→效率**（免食品证）；request 合法域名=`https://hergent.cn`；隐私指引仅勾**剪切板**+自有账号（代码无任何敏感 API）；测试账号 mptest/Mptest@1（sales）与 mptestsp/Mpsup@1（supervisor），**密码≤8 字符**（bcrypt 72 字节限制）。
- 建议并行：注册后 1-3 天平台处理期继续优化 Web 端，零冲突；仅部署后端重启会短暂中断两端。

## 15. 统一社会信用代码更正（用户纠正，已全量修正+生产部署）
- **正确值 = `91420606MAKF1YPG5Y`**（用户 2026-09-06 16:48 更正）。此前记录的 `91420600MAK1YFG5Y` / `91420600MA49YFC03Y` 均为我读图误读，**全部作废**。
- **影响面比预期大 —— 线上法务页也错了**：`hergent-cn-v2/public/legal/{privacy,terms}.html` + `dist/legal/*` 的「一、我们是谁」段落写的运营主体信用代码是错值（合规风险）。已 perl 批量替换 4 文件 → 单文件 rsync 到 `/opt/hergent-cn-v2/legal/` + chown → 生产 grep 复核新码命中、旧码 0 → 线上 `https://hergent.cn/legal/{privacy,terms}.html` curl 校验通过。commit `b4375d9`。
- 同步更正：`小程序注册信息-复制粘贴包.md:28`、`微信提审材料清单.md:44`、MEMORY.md / 2026-08-30.md / 2026-09-06.md 三处记忆（旧值旁标注作废原因，防下次翻历史又用错）。
- 仓库内 `备案材料.md:8`、`上架准备清单.md:15` 原本就是正确值 → 说明用户早给过正确代码，是读图环节失真。
- **教训（写入习惯）**：证照类关键信息（信用代码/注册资本/法人）**一律让用户以文本形式确认**，不要靠 OCR 读图 —— 已两次读错（法人、代码）。

## 16. 主体信息全仓同步扫描 + ICP 备案合规修正（用户「要」）
- 全仓扫「小赫智体/信用代码/ICP/备案号」→ 未发现第二套错误信用代码（仅 `备案材料.md`、`上架准备清单.md` 本就正确）。
- **新发现（真实合规缺口）**：ICP 备案其实**早已下号 = `鄂ICP备2026027973号-1`**，但 ① `备案材料.md` 仍写「首次备案，无」、第六节还是"审核通过后去加备案号"的过期指引 ② `上架准备清单.md` 标"待提交" ③ `Login.vue:74` 备案号是**纯 span 无链接** —— 工信部要求备案号须链接至 beian.miit.gov.cn ④ `landing-page/index-icp.html:167` 还是占位符 `鄂ICP备XXXXXXXX号-1（备案中）`。
- **修正并部署**：`Login.vue` 备案号改 `<a href="https://beian.miit.gov.cn/">` + 加 `.login-legal a.beian{color:var(--t3)}` 保持灰显；landing-page 占位符换真实号；`备案材料.md` 二/六节改已下号 + 悬挂位置表；`上架准备清单.md` ICP 改「已完成」。
- 构建踩坑：**managed npm 真实路径是 `versions/22.22.2-2/bin/npm`**（记忆里写 `22.22.2` 会 no such file）；且 `mv dist /tmp/...` 后 build 失败会**导致 dist 消失**，必须先确认 npm 路径再移。build 13.21s 成功 → rsync --delete → chown → 生产验证：Login-TsSC08zu.js 含备案号+链接、legal 两页信用代码正确、线上 curl 复核通过。commit `5c13bb7`。
- 记忆同步：MEMORY.md 补 ICP 备案号 + node 路径 `-2` 后缀 + 主体信息全量出现位置清单（防止下次更正再漏线上法务页）。

## 17. 小程序简介重写（微信 ≤120 字符）
- 推荐版（**76 字符**，已实测）：「本企业内部订货预报工具：销售按门店填报商品需求，主管在线审批后自动汇总生成采购计划。仅限企业员工账号登录，不涉及在线支付、对外销售，不收集任何个人信息。」
- 备选：精简版 66 字符 / 合规版 69 字符（若驳回要求改简介用）。
- **纪律**：首句必含「企业内部」（审核员判断资质的第一眼）；保留「不涉及支付/对外销售/个人信息收集」三连堵资质追问；**不写「低温奶/乳业/食品」**（会引向食品类目需食品证）；无极限词、无电话网址。
- 已同步 3 处：`小程序注册信息-复制粘贴包.md` §5（3 版 + 纪律）、`微信提审材料清单.md` 简介行 + 增补食品词警告、`提审打钩清单.html` 速查卡新增简介行 + s2-7 提示。HTML 结构校验 li 28/28、tr 8/8、key 无重复。

## 18. ⚠️ 服务类目更正：「工具→效率」是错的（用户后台实测指出）
- **微信《开放的服务类目》「工具」大类下没有「效率」** —— 那是**抖音**小程序类目（抖音路径：工具→实用工具→办公效率），我混淆了。**已按微信官方文档核实**并全量更正。
- 微信「工具」真实二级类目：记账、投票、日历、天气、备忘录、**办公**、图片处理、计算器、报价/比价、**信息查询**、兑换、预约/报名、健康管理、开票服务/企业代票工具、云手机、发票查询、视频客服、设备管理。
- **正确首选 = 「商业服务 → 企业管理」**，官方适用范围原文「适用于提供企业办公工具/办公管理、门店信息管理、展示等服务」，资质要求=**无** —— 与我们的企业内部订货工具（内部办公工具+门店信息管理+填报审批）逐字命中。备选「工具 → 办公」，兜底「工具 → 信息查询」（含订单功能）。
- 已更正 6 处：`小程序注册信息-复制粘贴包.md` §6（首选+备选表+纪律）、§10 审核备注模板（原写「服务类目：工具→效率」）、`微信提审材料清单.md` §3（整表重写+更正说明+真实类目清单）、§8 驳回应对表、`提审打钩清单.html` 速查卡、`miniprogram/CLAUDE.md` §2 与基础配置表。保留 5 处"更正说明"语境的提及作为防复发标记。
- **教训**：平台类目/规则属"会变的外部事实"，**必须查官方文档而非凭记忆**（本次凭记忆把抖音类目当微信类目）。与"证照信息须用户文本确认"是同一类错误——外部事实一律核实后再落文档。

## 16. 返利阶段2：品牌目标两层结构（金额层年框固定 + 返利层月度浮动）落地上线
- **业务口径已固化**（参见 MEMORY.md §返利业务口径）：品牌目标金额走年框（基本定下不变），返利率走月度（每月单独调 10点/8点）；单品目标按月带坎级独立体系。
- **后端改造**（server/erp_db.py + server/routers/rebate_rules.py）：
  - `rebate_target_rules` 加 `monthly_amounts`(TEXT JSON) + `monthly_rates`(TEXT JSON) 两列；建表 DDL（主库 v108 + 租户 init）+ ALTER 补丁（主库 `_safe_migrate('v119_...')` + 租户补丁循环 + 新租户 init ALTER）共 5 处覆盖全部库类型。
  - `_row_to_dict` 输出 `monthly_amounts`/`monthly_rates` 解析对象 + `is_monthly` 布尔标记，`created_at`/`updated_at` 保留。
  - `validate_rule` 有月度金额时自动抬年框=Σ月、有月度返利时兜底顶层 `re_rate`；`target_value<=0` 改为非硬错（月分解可兜底）。
  - 新增 `_parse_monthly_json` / `resolve_month_target` / `resolve_month_rate` 纯函数（与前端契约一致）。
  - `create_rule` / `update_rule` 的 INSERT/UPDATE 列对齐 31 列（含两列新增），临时库单测全过（INSERT_OK / UPDATE_OK / ROW_TO_DICT_OK is_monthly=True）。
- **前端改造**（hergent-cn-v2/src/pages/Rebate.vue + Forecast.vue）：
  - Rebate.vue 品牌维度专属「按 12 个月分解」勾选 + 月度编辑器（万元/% 表格 12 行 + 全年均分/全月同返利/清空三按钮 + 默认继承上月逻辑）。
  - 模板：当勾选月度分解时，自动隐藏触发方式=阶梯、返利形式切换、阶梯档位编辑器（与「按月比例」语义互斥）；period_type 强制 year。
  - 列表行加「月分解」tag；详情弹层加 12 月明细表（月份/金额/返利率三列）。
  - save() 组装：`buildMonthlyPayloads()` → 月合计覆盖年框（金额层同源）；月返利率可单独浮动。
  - Forecast.vue 5458 冲刺看板：`resolveMonthTarget` 取当月金额（mm= sprintMonth.slice(5,7)）；当月无金额时仍用 `target_value` 回退（老规则兼容）。
  - 全量 grep 复核：monthlyOn / buildMonthlyPayloads / monthlyRows / MONTHS_12 / splitAnnualToMonths / setAllMonthlyRate / clearMonthly / fillMonthlyFromRule / monthlyAmounts / monthlyRates 全部成对定义引用。
- **部署铁律验证**：
  - 前端 `npm run build` ✓ → Rebate-DaaCnw12.js + Rebate-s5FAfVTV.css + Forecast-Di1CgCAR.js + Forecast-Dxn2i3pw.css；rsync → `/opt/hergent-cn-v2/` + chown hergent:hergent；**双侧 md5 MATCH_OK**（JS `224066c8…` / CSS `b807b6fd…` 等）。
  - 后端 `chmod +x deploy.sh && ./deploy.sh`（之前无 x 位）：rsync server/ → `/opt/hergent-erp/`（含 hergent-erp 11 个未推送 commit 之上的 M 改动）→ chown → 清 pycache → systemctl restart hergent-erp → 健康检查 HTTP 200。
  - rsync 警告 `cannot delete non-empty directory: hermes-engine/.hermes/node` 等已知排除目录，文件正常同步，不影响。
- **生产验证四关全过**：
  1. 健康检查：`curl --noproxy '*' http://127.0.0.1:8700/api/health` 服务器侧 200（客户端 127.0.0.1 不是服务器故 000，已纠正）。
  2. 迁移生效：erp.db + tenant_1.db 的 `rebate_target_rules` 都有 monthly_amounts/monthly_rates（共 32 列）；tenant_1.db 已有 4 条品牌规则。
  3. API 透传：`GET /api/rebate-rules`（演示 token tenant=10）返回 `monthly_amounts`/`monthly_rates`/`is_monthly` 三字段全在；老规则三字段均 `{}/false`。
  4. E2E：演示租户 `POST /api/rebate-rules` 创建「测试月度品牌目标」id=2（含 8月110万、9月90万金额 + 9月8% 返利）→ HTTP 201；`GET /api/rebate-rules/2` 回读 `is_monthly=True`、`monthly_amounts={'08':1100000.0,'09':900000.0}`、`monthly_rates={'09':0.08}`、created_at 持久化正常。**id=2 保留作为演示种子**，用户登录演示空间可立即看到「月分解」标签与详情面板 12 月明细表渲染效果。
- **文档**：`docs/返利阶段2-品牌目标两层结构落库设计-2026-09-06.md` 已落（设计/兼容性/迁移/取舍点全记）。
- **下一步阶段3**：「返利结算」月度目标改引用品牌目标金额层，不再独立录；需先摸后端 `rebate_contracts.months` / `rebate_contract_months` 取数口径，待启动。

## 9. 咨询：微信小程序认证费用核实（2026-09-06 17:17，只读咨询无代码改动）
- **用户问**「小程序认证需要交 300 元费用对吧？」→ 核实 2026-09 现行政策后答复：**是的，但非一次性，是每年 300 元**。
- **费用事实**：企业主体（有限责任公司）300 元/次 + **每年年审再交 300**；个体工商户 30 元/年；个人主体不收费（但不支持认证/支付）；政府/基金会免费。300 元为第三方审核机构服务费，**审核不通过也不退**；有效期 1 年，到期不年审则取消高级功能。
- **用户的实际情况**：湖北省小赫智体数字科技有限公司 = 有限责任公司（自然人独资）→ **企业主体 300 元/年**，享受不到个体户 30 元优惠。
- **3 条省钱/避坑路径**（已写入 MEMORY.md「微信小程序认证成本」段）：① 复用已认证服务号资质可免费创建已认证小程序（顺序须先公众号后小程序）；② 注册时选「对公小额打款验证」免费完成主体验证（但 ≠ 微信认证，名称/头像设不了、支付与手机号授权受限）；③ 未认证不能提审发布正式版，只能停体验版（未认证未发布企业主体 30 项目成员 + 30 体验成员）。
- **判断建议**：内部工具场景（替代手工预报单流程、销售/主管用），若已确认流程有效 → 该交（300 元/年 ≈ 0.8 元/天，省的是文员人工）；若流程未验证 → 先免费用体验版跑 1-2 周再转正，避免 300 白交。另提醒：**认证 ≠ 能上线，还差 ICP 备案**（免费必做）。
- **待用户确认 3 项**：① 有无已认证服务号（决定能否走复用路径省 300）；② 当前小程序注册时选的验证方式（是否已交过钱）；③ ICP 备案是否已做。
EOF\necho \"appended 9\"
## 10. 澄清：hergent.cn 已备案 ≠ 小程序已备案（2026-09-06 17:23）
- **用户说**「hergent.cn 这个域名已做 ICP 备案」→ 属实（记忆中 `鄂ICP备2026027973号-1`，挂 Login.vue footer）。
- **但必须纠正的认知**：**域名备案 ≠ 小程序备案**（两个独立体系）。域名备案只说明"谁在运营 hergent.cn"；小程序备案是 2023-09 起工信部强制的独立备案类型，须在微信后台为每个小程序单独走一遍，生成 AppID↔Domain 的绑定记录。未完成小程序备案即提审 → `errcode 86369 小程序上架需先完成备案`。
- **hergent.cn 已备案的实际价值（2 条，都是真的）**：① 小程序后台服务器域名栏**只收已备案域名**，hergent.cn 满足 → 配 request 合法域名不会被卡（这是之前提过的上线前提，现确认已满足）；② 主体（湖北小赫智体）在工信部已有备案记录 → 小程序备案时主体信息可复用、初审快。
- **未变的结论**：**300 元认证一分没省**。顺序是「微信认证(300) → 才能填名称/类目 → 小程序备案(免费) → 提审发布」，未认证连名称和类目都设不了，更走不到备案。
- **备案 3 个坑（已入 MEMORY.md）**：① 工信部 12381 短信核验须 24h 内完成，超时自动退回；② 通讯地址精确到门牌号、与证件同省（湖北）；③ 小程序名称避开前置审批敏感词（商城/商贸/商厦/支付/金融/教育/医疗等）——内部订货工具建议中性命名，别叫"XX商城"。

## 15. 侧边栏结构决策：AI 中心瘦身 + 运维下沉设置（2026-09-06 17:2x）
- **问题**：用户质疑「AI 中心要不要整合进能力中心」。调研结论=**不该合并**（语义正交）：能力中心 1235 行=输入端/配置面（连接器/专家/技能/进化日志，改一次管很久）；AI 中心=输出端/结果面（干了什么、稳不稳、花多少）。合并会造 2000 行 10+ tab 巨型页，且「数据备份」挂「能力中心」下语义不通。
- **真问题在别处**：AI 中心 10 张卡内部分裂——前 4 卡（报告/洞察/画像/配额）是老板天天看的业务价值，后 6 卡（备份/健康/审计/路由/兜底/配方IO）是运维、出事才看。对外卖给非技术经销商老板时，「模型路由/审计日志/数据备份」占门面会劝退。
- **方案 B 落地**：新建 `src/pages/AiOps.vue` 承载运维 6 卡；AiHub 由 807 行精简到 353 行（只剩 4 卡）；`Settings.vue` 加第 5 个 tab「AI 运维」并 import AiOps；AiHub 页头加引导条「数据备份、AI 健康看板…已移入 设置 › AI 运维」（`router-link to="/settings?tab=aiops"`）；Settings onMounted 支持 `?tab=` 深链（白名单 account/perm/ai/aiops/system）。
- **删除手法**：用 python 锚点定位切片（非行号），删 template 211 行 + script refs 16 行 + ops funcs 169 行 + style 52 行，共 32925→13691 chars。锚点法避免行号漂移，比 Edit 大块 old_string 可靠。
- **部署**：仅前端（后端无改动）。chunks `AiHub-6PAefT2o.js`(9.4k) + `Settings-pkqtm_jl.js`(24.8k，AiOps 内联其中)。rsync + chown 完成。
- **真机 E2E（agent-browser）**：demo 登录后 `/#/ai-hub` 零错误、4 卡 + 引导条正常（配额显示已用 420/20.0万）；`/#/settings?tab=aiops` 深链直达、6 卡全渲染、真实数据（备份 3 份含 2026-09-06 11 库 46.5MB；健康看板 11 次调用失败归因 AI_API_KEY missing）；点引导链接 → URL 变 `#/settings?tab=aiops` 且「AI 运维」tab [ON]，零错误。**注意 hergent.cn 是 hash 路由（`#/path`），必须直连不走代理**（AGENT_BROWSER_PROXY=127.0.0.1:63932 会 ERR_PROXY_CONNECTION_FAILED）；demo 登录按钮选择器 `.demo-btn`。
- **顺手修复**：今日日志末尾曾混入 heredoc 垃圾行 `EOF\necho \"appended 10\"`，已清除。

## 11. 关键修正：「复用公众号资质免 300」对用户不适用（2026-09-06 17:28）
- **用户反问「有没有已认证的服务号」** → 查项目记录：**无任何已认证服务号痕迹**（自媒体规划里「公众号」属阶段 2 = 11/9 起才做，未提已注册/已认证；实际主阵地是视频号「老张ai实践记」）。
- **顺带纠正上一轮我说错的「省钱路径 A（最值）」**——其实对他不适用，且我原表述有误导性，已在 MEMORY.md 修正为「真实边界」：
  - 复用只在**创建新小程序时**勾「快速注册并认证」才生效；**已注册的未认证小程序大概率不能追溯复用**（微信开放社区官方答复如此；腾讯客服另有一条「已关联可复用」旧口径，规则曾反复——以后台有无该按钮为准）。
  - **只免首年**：官方明示「初始有效期与公众号一致，**后续年审需在小程序后台独立申请**」→ 第二年照样 300/年。
  - **为省 300 现去认证服务号＝不划算**：服务号认证本身 300/年，等于多花一份。
- **最终结论（已定）**：用户**直接交 300 认证小程序，别绕路**。hergent.cn 已备案（域名备案）不省这 300，域名备案与小程序备案是两回事。
EOF\necho \"appended 11\"
## 12. 小程序微信认证已交 300（2026-09-06 17:39）+ 代码侧阻塞项检查
- **状态推进**：用户已支付 300 元微信认证费，进入第三方审核（1-5 工作日）。上线链路剩余：认证通过 → 填名称/类目（工具→效率）→ 小程序备案（免费）→ 提审发布。
- **代码侧检查结果**（`forecast-order-miniprogram-20260812T023419087Z/miniprogram/`）：
  - ❌ **`project.config.json:47` appid 仍是 `touristappid`（游客 AppID）** —— 硬阻塞：游客 AppID **无法上传代码、无法提审**。必须在开发者工具用管理员微信登录后换成真实 AppID（后台【设置→基本设置→账号信息】可查）。此前记忆里已记过「appid 须真实账号(非 touristappid)」，现确认文件里仍未改。
  - ✅ `app.js:3` `apiBase: 'https://hergent.cn'` —— 与已备案域名一致，满足后台 request 合法域名配置要求。
  - ⚠️ `app.json` 无 `__usePrivacyCheck__`；隐私指引须在**微信后台**配置（记忆里有：须勾选剪切板 `wx.setClipboardData`），否则提审会被拒。
- **等待审核期间可提前做（不用干等）**：① 换真实 AppID；② 起 2-3 个小程序名称备选（避开商城/商贸等前置审批词）；③ 备备案材料（营业执照、法人/管理员身份证、精确到门牌号的湖北通讯地址）；④ 后台配服务器域名 hergent.cn + 隐私指引。
- **审核期注意**：第三方审核机构会打电话核实（0755/021 开头），漏接会卡住；300 元审核不通过也不退。
EOF\necho \"appended 12\"
## 13. 小程序上线关键推进（2026-09-06 17:59）
- **用户截 3 张后台图** → 我从图读出：① 通知中心「小程序微信认证-资质认证成功通知 2026-09-06」=**已通过**（支付到通过不到 1 小时，远快于预估的 1-5 工作日）；② 开发管理 → 开发设置 → AppID(小程序ID) = **`wxf78ce9bc8e4b5693be`**；③ 账号设置 → 基本设置 还是「请先完善小程序基本信息」+「前往填写」（认证刚过待补名称/类目/头像/简介）。
- **代码侧全仓体检**（5 项）：
  - touristappid 残留：仅 `project.config.json:47` 一处（CLAUDE.md 提及是文档）；已换为真实 AppID `wxf78ce9bc8e4b5693be`，复检 0 残留代码。
  - 旧 AppID 残留：0 命中（wx[0-9a-f]{16} regex）。
  - 隐私 API：`pages/login/login.js:31-34` 已调 `wx.getPrivacySetting` + `needAuthorization`，符合 2023-09-15 新规（后台隐私保护指引必须配——剪切板权限要勾）。
  - 剪切板：`pages/summary/summary.js:45` 已用 `wx.setClipboardData`（后台隐私指引须勾选）。
  - tabBar：仅「填报」+「我的提交」，汇总页迁 mine 页（符合 CLAUDE.md「tabBar 静态无法按角色隐藏 → 汇总入口迁 mine 页」约定）。
- **改动**：`forecast-order-miniprogram-20260812T023419087Z/miniprogram/project.config.json:47` `touristappid` → `wxf78ce9bc8e4b5693be`。
- **用户后续必做（认证已解锁，下一步 4 项）**：① 小程序基本信息（名称 / 类目「工具→效率」 / 头像 / 简介）；② 后台 server 域名配 hergent.cn（request 合法域名）；③ 后台「用户隐私保护指引」勾选剪切板；④ 小程序备案（免费，走工信部 12381 短信核验 24h 流程）。代码侧已无阻塞项。

## 16. 「为什么 WorkBuddy 简洁而我们不简洁」——产品门面判断标准（2026-09-06 18:2x）
- **用户提问**：WorkBuddy 界面简洁、没有 AI 中心/AI 运维这些页面，我们为什么需要？
- **结论（已写入 MEMORY.md 长期记忆）**：WorkBuddy 不是没有这些能力，是**复杂度被平台吸收**——数据托管在平台、LLM 可观测由平台 SRE 盯、计费按订阅扣，且**使用者=付费者=维护者三者合一**，所以界面只需一个对话框。Hergent 卖 B2B，**三者分离**（经销商老板用 / 老板付钱 / 我维护），分工一出现必然长出"谁看了什么、谁改了什么、出问题找谁、这个月花多少"。这是卖 B2B 的代价，不是自找复杂。
- **但直觉对一半，已诚实切分 10 张卡**：客户价值 5 张（我的报告/经营洞察/长期画像/用量配额/AI兜底升级）vs **我自己的运维自保 5 张**（数据备份/健康看板/模型路由/审计日志/配方IO）。后 5 张非客户价值——备份是"只有我怕丢"、模型路由"省的是我的钱"、配方IO"我部署新客户用"、审计"夫妻店不需要"。
- **确立判断标准（长期）**：新功能先问「谁会为它付费？谁会天天看它？」答不上来＝运维自保，不许进客户门面。正确姿势不是"不做"，是**做但别让客户看见 + 能砍就砍**（方案 B 已完成"藏"的一半）。
- **遗留可砍项（待用户拍板）**：① 模型路由可彻底去 UI（纯后台成本优化，客户零感知）；② 配方IO 本质是"我的运营后台"，不该在客户产品里；③ 审计日志当前过度（夫妻店），但卖到有业务员的中型经销商时老板会要"谁改了价"，建议等真客户问再提级。
- **顺手清理**：MEMORY.md 因超限被截断，已压缩重写（去重+收紧，保留全部 ⚠️ 铁律）；今日日志末尾再次混入 heredoc 垃圾行（`EOF\necho \"appended 13\"` + 小程序 sed 命令），已清除。**教训：往日志追加内容一律用 Write/Edit 或 python 文件写，不要用 heredoc 管道拼接。**

## 17. 模型路由 UI 撤除（2026-09-06 18:3x）
- **背景**：§16 确立「谁付费/谁天天看」门面标准后，判定模型路由＝纯后台成本优化、客户零感知，不该占卡。
- **改动**：`src/pages/AiOps.vue` 删模型路由卡（模板 26 行 + loadRoute 函数 8 行 + 样式 9 行 + ref/调用 2 行），原 ⑥ AI 兜底记录 重编号为 ⑤。`AiHub.vue` 页头引导文案去掉已移除的「模型路由」（改为「数据备份、健康看板、审计日志等」）。**后端零改动**，`GET /api/ai/observability/route-status` 保留（纯后台，我 debug 用 curl）。
- **手法**：继续用 python 锚点切片（find 起止字符串），三处 cut + 四处 replace，20317→18340 chars，一次成功无行号漂移。
- **构建/部署**：`AiHub-DM6brYES.js`(9.4k) + `Settings-BEQ5w6j7.js`(23.6k，较上轮 24.8k 减小)；dist 内 grep「模型路由」= 0。
- **真机 E2E（agent-browser，demo 登录）**：`/#/settings?tab=aiops` → tab「AI 运维[ON]」、卡片恰为「数据备份 | AI 健康看板 | 配方导入导出 | 审计日志 | AI 兜底记录」5 张、`body.innerText.includes('模型路由')` = **REMOVED**、零运行时错误；`/#/ai-hub` 引导条文案已更新、4 卡正常、零错误。后端 route-status 仍返 `qwen.fail_streak=3`（历史累计，功能未受影响）。
- **AI 运维面板现状**：5 卡（原 6）。待办候选砍项还剩 配方IO（本质是"我的运营后台"）与 审计日志（当前对夫妻店过度）。

## 12. 小程序主体+类目已落地（用户发后台截图）
- **微信小程序已注册**（`project.config.json:47 appid = wxf8ce9b8e4b5693be`，**2026-09-06 用户文本确认的权威值，18 位**）。
  - ⚠️ **我在此犯了一个典型错误**：此前我从项目文件读到 `wxf78ce9bc8e4b5693be`（20 位）并写进记忆当真值，还"自信"地告诉用户"已替换完成"。**AppID 是 18 位（wx+16 hex），20 位一眼假**，我却没有校验长度/格式就直接采信。教训：**凡是 ID 类凭据（信用代码/AppID/备案号/密钥），一律让用户文本确认 + 用正则校验格式（AppID=`^wx[a-f0-9]{16}$`）**，不靠读文件、不靠 OCR。已修项目 + CLAUDE.md 两处 + 记忆。
- **类目「商业服务 → 企业管理」状态=已通过**（2026-09-06 用户后台截图证实，我推荐正确，资质要求=无，与"企业内部订货工具"逐字命中）。
- **新增必要项（打钩清单 s1-1b）**：`project.config.json:8 urlCheck:false`（开发期绕过域名校验的开关）—— **真机前必须改回 `true`**，否则"域名已配"是假象、真机全 400。已加进 HTML checklist。
- **文档同步**：5 处 touristappid 残留全清（注册信息包/提审清单/CLAUDE.md/HTML 速查卡）；HTML 校验 li 29/29、tr 8/8、unique key 29。
- **小程序名拍板「小赫 AI 报单助手」**（用户定）：微信字符数 16（4–30 OK）、含 AI 有经营范围背书、无蒙牛/低温奶/食品词。已同步注册信息包 §4（含驳回备选链+AI 追问回复模板）、提审清单 §5、HTML 速查卡新增「小程序名称」行。HTML 校验 li 29/29、tr 9/9、unique 29。
- **小程序域名+urlCheck+备案三件事推进（用户发开发设置截图）**：
  - 用户截图证实 `request合法域名=https://hergent.cn` 已配（其余 socket/uploadFile/download 等未配，小程序 wx.request 已够用）。
  - **urlCheck:false → true 已改**（按上一条承诺的顺序：配域名→改 urlCheck）。改 true 后开发者工具本地调试会强制校验域名白名单，已配 hergent.cn 应该通过。
  - **新增《小程序备案速填包.md》**：11 节流程速览+主办者信息+负责人信息+小程序信息+服务内容+ICP+前置审批+提交前自检+12381 短信核验 24h 防超时 3 措施+审核期并行任务。已含所有预填字段（公司名/信用代码/邮箱/AppID/小程序名/ICP 备案号），法人身份证号/手机号留手填占位（敏感字段不写进交付物）。

## 19:20 备案放行检查（go/no-go）
- **查证纠正了一个会挡路的事实**：微信官方 FAQ 明确 **「小程序备案必须完成微信认证，无论是否涉及支付」**；后台文案「小程序备案：需先填写小程序信息和类目，完成小程序认证」。此前《备案速填包》只写"主体注册+类目已通过"即前置，**漏了最关键的微信认证**，已补 0-A 节「能不能现在就备案？三关自检表」并在第 7 节自检清单首行加「微信认证已通过」。
- 三关 = ①微信认证已过(300/年) ②小程序信息(名称/头像/简介)已填 ③类目已通过。当前 ③ 绿、② 待补头像、① 未确认（大概率未做，因前期记忆只到"直接交 300"）。
- 易混点写入文档：**主体验证（法人扫码/对公打款，免费）≠ 微信认证（300 元第三方审核）**，前者不解锁备案。
- 附带清理：MEMORY.md 由 16.5KB 压缩至 ~8.5KB（去重类目两条重复条目、精简长句），并把备案前置三件套写进小程序章节。
- **用户确认后台「微信认证」= 未认证/显示去认证** → 判定 **当前不能备案**，卡点=微信认证。已新建《微信认证300元-分步填写包.md》：入口+主体字段+主体验证三选一(选法定代表人验证)+联系人+发票+支付+**接第三方审核电话纪律(0411/028 固话)**+驳回 checklist+认证后立刻三件事(名称/头像144×144≤50KB/简介)+等待期并行任务。

## 19:26 备案放行：三关全绿
- 用户确认 **微信认证已通过 + 小程序信息已填**（含头像）→ 三关全绿，**可以进备案**。
- 《小程序备案速填包.md》更新：0-A 三关表状态改全绿 + 结论改"现在可以备案了"；**新增 0-C「备案提交 5 步顺序」**（主体信息→主体负责人→小程序信息含"前置审批选以上都不涉及"→小程序负责人复用+人脸核验白底→提交照第7节自检）。
- 下一步（用户侧）：设置 → 小程序备案 → 去备案；提交后进入 1-2 日初审 + **12381 短信 24h 内核验**（超时全部重填）。可通过上传体验版跑真机 E2E 并行推进。

## 19:29 备案第二步「服务内容」填法
- 用户卡在备案页「服务内容」下拉。
- **查证**：微信备案「服务内容」=国民经济行业分类下的类目，与后台服务类目是一对一关系（[官方映射表](https://developers.weixin.qq.com/minigame/product/record/receord_category.html)）。后台我们已选 **「商业服务→企业管理」**，对应备案服务内容**应选同名「商业服务→企业管理」**——释义「适用于提供企业办公工具/办公管理、门店信息管理、展示等服务」逐字命中。
- 速填包第 4 节原版混了"工具类"概念（错），已修正为：
  - 服务内容 = **商业服务→企业管理**（下拉搜"商业"或滚动找）
  - 兜底 = **信息传输、软件和信息技术服务业**（与经营范围软件开发匹配）
  - 前置审批 = **全不勾**（系统要求时选"其他/以上都不涉及"）
  - **备注（必填 20-200 字）** 给了 ≈120 字定稿版，涵盖"企业内部/不涉及支付对外销售个人信息/不提供新闻出版教育医疗金融等需前置审批服务"
- 0-C 第 3 步同步更新引用。

## 19:41 备案服务内容修正：生活服务→企业管理（不是商业服务）
- 用户截图证实下拉里**没有"商业服务"一级类目**（我先前判断错了）。
- 二次查证：微信官方「小程序备案服务内容选择指引」里的"商业服务"是**后台类目体系**（与备案体系不同）；备案体系里「企业管理」实际挂在「**生活服务**」一级类目下。
- **正确选择**：一级 **生活服务** → 二级 **企业管理**。释义「适用于提供企业办公工具/办公管理、门店信息管理、展示等服务」逐字命中。
- 速填包第 4 节 + 0-C 第 3 步同步修正：去掉商业服务选项，新增"点生活服务箭头展开二级"的步骤指引、常见混淆项对照表（不勾批发零售/软件开发等）。
- **后台类目「商业服务→企业管理」与备案服务内容「生活服务→企业管理」名字不同但释义一致**，属于微信新旧两套类目体系映射，不需在备注里特别解释。

## 19:54 备案已提交 + 开发者工具调试放行
- **备案已于 ~19:50 提交**，进入平台初审 1-2 日 + 12381 短信 24h 核验待办。
- 用户问"下载了开发者工具现在能调试吗" → **能**。查证结论：**备案只卡"提交审核/发布上线"，不卡开发验证**；模拟器/真机预览/真机调试/上传体验版在备案审核期全部可用（我们域名已 ICP 备案+已配 request 合法域名，连"不校验合法域名"都不用勾）。
- **主动做了生产环境预检（全绿）**：`/api/health`=200（db 15.84MB）；`mptest/Mptest@1` 登录成功得 43 字符 token；open-periods=id=9「9月提审期-开放填报」2026-08-30~2026-09-15（覆盖今天）；stores=2220 永诺旗舰店。**→ 用户导入项目即可跑通，不会撞"暂无期次/门店为空"**。
- 新建《开发者工具-导入与首跑清单.md》：导入 6 步（⚠️目录选到含 app.json 的 `miniprogram/` 层、AppID 用真实值别用测试号、不使用云服务）+ 首跑 6 项验收 + 3 个坑（urlCheck=true 域名校验/登录大小写/真机需真实备案域名）+ 备案期能做不能做对照表。
- 小程序请求路径备忘：登录 `POST /api/auth/login`、期次 `GET /api/forecast-submissions/open-periods`、门店 `/stores`、提交 `POST /api/forecast-submissions`、我的 `/my`、待审 `/pending`、汇总 `/summary`；`app.js:3 apiBase='https://hergent.cn'`。

## 19:59 用户截图 ICP 核验弹窗
- 截图显示 beian.miit.gov.cn 弹窗："尊敬的ICP用户：您的短信核验已全部完成，该请求将提交管局审核。" 顶部有"类ICP核"字样 + 时间戳 2026-09-04（推测被截断）。
- ⚠️ **疑似混淆**：19:50 才提交小程序备案、9 分钟后不可能完成"初审(1-2日)+12381短信+核验+管局"全流程。该截图**更可能是 hergent.cn 域名 ICP 网站备案的某条历史核验**（与今天的小程序备案无关），或截图误发。
- 决策：**让用户去后台核对实际状态**——微信公众平台 → 设置 → 小程序备案，看状态字段（初审中/待短信核验/管局审核中/已备案），以后台为准。
- 用户该状态真正变化时再触发后续动作（提审/绑定备案号/版本管理）。

## 20:02 尝试自动导入开发者工具
- **静态校验全绿**：5 个 page 的 .js/.wxml/.wxss 齐全、tabBar 2 项均已注册、8 个 JS 文件 `node --check` 全过、`appid=wxf8ce9b8e4b5693be`、`urlCheck=true`、`compileType=miniprogram`。导入后不会有低级报错。
- **CLI 自动化走不通**：`/Applications/wechatwebdevtools.app/Contents/MacOS/cli open --project <path>` 报 `IDE service port disabled`。试过 `printf y |` 和 `script -q /dev/null` 伪 TTY 均无效（CLI 要真交互 TTY 或已开启服务端口）。
- **AppleScript GUI 自动化也走不通**：`System Events` 报 -10004 权限违例（未授辅助功能权限）。
- 已做：`open -a wechatwebdevtools` 启动（此前已在运行）+ `open` Finder 到项目父目录，让用户**拖 miniprogram 文件夹进窗口**最快。
- ⚠️ **后续可优化**：用户一次性开启「开发者工具 → 设置 → 安全设置 → **服务端口**」后，CLI 即可用（`cli open/preview/upload/auto`），后续预览二维码、上传体验版、自动化测试都能由我代跑。**这是下次要让用户开的一个开关。**

### 20:07 CLI 自动化死路排雷（勿重复试）
- 已验证**五种**方法全失败，结论：**服务端口必须由用户在 GUI 手动开一次，无法绕过**。
  1. `printf y | cli open` → 无效
  2. `script -q /dev/null` 伪 TTY → 无效
  3. **Python `pty.fork()` 真 TTY** → y 确实发出（输出可见 `[>>> sent y]`），但 CLI 在打印错误后**立即退出不读 stdin**，"enter y" 是假提示
  4. 直接改配置：`~/Library/Application Support/微信开发者工具/`（Electron 用户数据目录）里 grep `servicePort` **无任何命中**，Preferences 只有 spellcheck，无 `~/.ide` → 配置在 leveldb 里，不可安全手改
  5. AppleScript `System Events` → -10004（需给 **WorkBuddy** 授辅助功能权限，不是给开发者工具；且授权后需重启 WorkBuddy）
- ✅ **可用**：`osascript -e 'tell application "wechatwebdevtools" to activate'`（Apple Events 激活窗口**不需要**辅助功能权限）+ `pbcopy` 预填 AppID。下次导入用这两招把用户手动步骤压到最少。
- 用户最终需手动：拖 `miniprogram` 文件夹进窗口 → Cmd+V 粘 AppID → 确定。

### 20:13 用户开辅助功能后仍 -10004（最终结论）
- 用户截图证实 WorkBuddy 在「辅助功能」白名单已勾选 ✅，但 `System Events` 仍报 -10004。
- 根因：**macOS TCC 缓存要求授权进程**完全重启**才生效**（已知行为，不是 bug）。重启 WorkBuddy = 断开当前对话、丢失所有上下文，**不可接受**。
- **结论：GUI 自动化到此为止是死路，不要再诱导用户"再试一次"。** 这条坑价值 ≥1 张 skill 卡，但写进 MEMORY.md 已足够。
- 下次直接给最短手动路径（拖拽 + 剪贴板 AppID）+ 推"开服务端口"长线方案。**别让用户反复配合自动化无果的事。**

### 20:16 用户重启 WorkBuddy 后仍失败 → 此路彻底放弃
- 重启后 `tell process "wechatwebdevtools"` 仍 -10004。
- **根因定位**：连最基础的 `tell application "System Events" to get name of first process` 也 -10004 → **是"执行 osascript 的宿主进程"未被授权**，与微信开发者工具无关。进程链 `ps` 查不到宿主（`$$` 链输出为空），无法定位该给谁授权。
- **最终结论：AppleScript GUI 自动化在本环境不可用，永久放弃。** 以后涉及 macOS GUI 自动化（开发者工具/原生 App）直接走 CLI 或让用户手动，不再让用户重启/授权。
- 唯一可靠的自动化通道 = **微信开发者工具「服务端口」**（用户 GUI 点一次即永久生效，CLI 可用）。

## 20:25 项目已在开发者工具创建成功（用户确认「已创建」）
- 导入目录 = `.../forecast-order-miniprogram-20260812T023419087Z/miniprogram`（含 app.json 那层）。用户两次问「目录是哪一个」→ 以后直接给绝对路径 + 复制到剪贴板 + `open` 打开 Finder，并说明判据「能看到 app.json」。
- 复验生产全绿：mptest/mptestsp 双账号 token 均 43 位；期次 `id=9 9月提审期-开放填报 2026-08-30~09-15`（到货 09-18）；门店 `2220 永诺旗舰店`。
- CLI 仍报「服务端口已关闭」→ 用户尚未在 GUI 开启；后续预览/上传仍需手动，或等用户开启后再代跑。
- 下一步=首跑 6 项验收（编译→登录→期次条→选门店→提交→我的提交待审核→撤回）；主管侧 mptestsp 看「移动审批」。

## 20:32 修复编译报错 + 生成品牌图标
- **WXML 坑**：mine.wxml:3 写 `{{(user.display_name || user.username || '员')[0]}}` → WXML Mustache 不支持 `(expr)[0]`。已挪到 mine.js:onShow 算出 `avatarChar = (name||'员').slice(0,1)` 传给模板。
- **品牌图标**：用 Chrome headless 把 `desktop-app/hegent品牌图标.svg`（青绿渐变 1024+H 字母）渲染为 1024 PNG，再用 sips 缩放出 512/256/144/108/81 共 6 份到 `forecast-order-miniprogram-.../小程序品牌图标/`。**144×144=16K 适合微信后台头像（要求 ≤50K、144×144 起）**。
- tabBar 现状纯文字，未自动配 icon；用户未明确，先不动。

## 20:40 tabBar 图标落地（购物车 + 清单勾，品牌青）
- **设计判断**：两个 tab 若都用同一 H 图形，未选中时都是灰 H，**用户无法区分** → 改用表意线性图标：**购物车=填报 / 清单带勾=我的提交**；颜色沿用 tabBar 既有 `color #8e8e93` / `selectedColor #06b6d4`（品牌青）。H 版也生成（`tab-h.png`/`tab-h-active.png`）留备用，换法=改 app.json 的 iconPath。
- 文件：`miniprogram/assets/tab-{fill,mine}{,-active}.png` + `tab-h{,-active}.png`，均 81×81 透明背景 RGBA，各 4KB（微信 tabBar 要求 81×81、≤40KB）。
- `app.json` tabBar list 已加 iconPath/selectedIconPath，4 路径校验存在。
- 预览图 `小程序品牌图标/tabBar-图标预览.png`（1120×940）。
- **可复用技巧（本机无 rsvg/inkscape/magick/cairosvg）**：SVG→PNG 用 **Chrome headless**：`Google\ Chrome --headless --disable-gpu --window-size=W,H --default-background-color=00000000 --screenshot=out.png file://x.html`；为避免 body 8px margin 偏移，用 HTML 包一层 `html,body{margin:0}` + `<img src="x.svg">`，多文件引用本地图需加 `--allow-file-access-from-files`；缩放用 `sips -Z`。
- **PNG 空图自检**：写最小 PNG 解码器（zlib 解 IDAT + 5 种 filter 还原）统计非透明像素占比与主色，避免交付空白图标。实测灰版主色 #8d8d92、彩版 #05b5d3，非透明 13-16%（线性图标合理）。

## 20:48 登录页 Logo 换成品牌图标
- `pages/login/login.wxml:3`：`<view class="brand-ic">预</view>` → `<image class="brand-ic" src="/assets/brand-logo.png" mode="aspectFit" />`。
- `login.wxss:3`：去掉青底白字样式，改 `{width:128rpx;height:128rpx;border-radius:32rpx;overflow:hidden;margin-bottom:24rpx;display:block}`（image 需 `overflow:hidden` 才生效圆角）。
- `assets/brand-logo.png` = logo-256.png 副本（256×256，@3x 显示 128rpx=192px 够用）。
- mine 页 `.entry-ic`（「审」「总」72rpx 青底白字）为**功能入口表意标记**，非品牌 logo，未动。
- 全项目静态复检：5 页文件齐、8 个 JS 语法过、**WXML 高危表达式（`)`+`[`、对象字面量、函数调用）全项目扫描已加进日常校验**、代码包 180K/2MB。

## 22:35 单品全量铺开 + 期次差异修复（已部署生产并实测）
- **期次根因（重要）**：`erp_db.forecast_open_periods()` SQL = `status='open' AND 今天 BETWEEN order_start AND order_end`。生产 id=8(08-28~08-28)、id=10(09-03~09-03) 是**单日窗口期次**，那天一过就被日期过滤，但管理员未在 Web 端关闭、status 仍 open → Web 端 4 条、小程序 1 条。**不是数据不同步，是小程序多了一层日期过滤。**
- **修复方式（零风险）**：**不动** `forecast_open_periods()`（它被 `forecast_submissions.py:81` 用于提交时自动选期次，放宽会让补报落过期期次）；新增 `forecast_open_periods_all()` 供列表用，返回所有 open + `in_window` 标志、**窗口内排最前**（保证默认选中有效项）。`/open-periods` 路由改调新函数。提交接口对 period_id 仍有二次校验，选过期期次提交会被 400 拒。
- **商品接口改造** `data.py:/api/products/fill-search`：加 `offset` + 返回 `total`；**N+1（每商品 2 次子查询）改批量 IN 预取，查询次数恒定 4**（复用 `forecast_audit.py` 既有批量逻辑）；limit 截 1~200、offset 非负；搜索字段（name/spec/product_code/pinyin_first）**完全未动**。
- **前端** `pages/fill/`：`products/total/pageSize=30/hasMore/loadingMore`；`loadProducts(kw,append)`；`onReachBottom` 触底加载；`clearSearch`；期次 `display` 标签 + `in_window===false` 红色提示条/toast；删除「常用品」横条（全量铺开后冗余）；新增 `fill.json` `onReachBottomDistance:120`。
- **实测**：428 个可预报单品；首页 30 条 **0.125s**；分页不重叠（1160-1189 / 1190-1219 / 末页 8 条）；库存批量预取正确（pid1353=745、1354=7、1435=24）；搜「蒙牛」161 条、搜厂家编码 2197762 精确命中 pid1166。
- **一致性**：与 Web 端同源同一 `products` 表 + `is_active` 过滤，无副本无同步任务，`onShow` 每次重拉第一页 → 配置变更即时生效。
- ⚠️ **deploy.sh 坑**：脚本里 `sleep 3` 后探活会误报 `health HTTP 000`（实际服务 5 秒才 ready）。**看到 000 先 `systemctl status` + 看 Uvicorn "Application startup complete"，别急着回滚。**
- 生产查库：`sqlite3` 不在 PATH，用 `python3 -c "import sqlite3..."`；db 在 `/opt/hergent-erp/tenant_1.db`。
- 遗留待定：购物车区在长列表下方，加购多了要滚很久才能改数量（可改吸顶/底部弹窗）；建议后台把 id=8/10 关掉。

## 行内填报（商品行直接填数量，2026-09-06 晚）
- 需求：用户要在铺开的商品行上直接填报单数量，不再「点 + 加购物车 → 滚到下面改」。
- 实现（`miniprogram/pages/fill/`）：
  - 行内控件 `− / input(type=number) / +`；已填行高亮（`.prod-row-on` 浅青底+青框），行尾加「库存 X · 日均 Y」辅助判断。
  - 数据：渲染走 `item.qty`（不用 `qtyMap[item.id]`，WXML 变量下标不稳），`data.qtyMap` 仅用于**分页/搜索回填**。
  - 更新用路径 setData `products[i].qty`（只更新一行，数百 SKU 不卡），且**值未变时不 setData**（避免受控 input 光标跳动）。
  - 统一入口 `setQty(id, qty)`：写 cartMap → syncCart（派生 qtyMap + cartQty）→ 回写行。购物车区 plus/minus/removeItem 走 `_syncRow()` 反向回写行内。
  - 提交成功/清空后 `clearRowQty()` 批量清零行内。
  - 「本次预报」区改**默认折叠**（显示 N 项 · 共 M 件，点击展开），避免与行内重复占屏。
- ⚠️ **后端 `qty = int(quantity)` 强制取整** → 前端只允许整数：onQtyInput 取**第一段连续数字**（`match(/\d+/)`），12.5→12。**别用 replace 去非数字**，那会把 12.5 拼成 125。
- 验证：写 node stub（wx/getApp/Page + setData 路径语法）跑 21 项逻辑测试全过（含加减/归零/上限99999/分页回填/提交清零/购物车回写）；生产 E2E 提交 pid1353 qty=24 → `total_qty=24` ✅，撤回 186 后主管 pending 不含该单 ✅。
- 死代码已清：`addProd`/`addToCart`/`hot`/`results`/`.prod-add` 全移除。

## 商品搜索增强（条码+名称同框，2026-09-06 深夜）
- ⚠️ **重大发现（之前一直错）**：`data.py /products/fill-search` 原 WHERE 只搜 `name/spec/product_code/pinyin_first` —— **332 个商品有 `barcode` 却搜不到**；`pinyin_first` **全库 0 条非空**占着搜索位；`medium_barcode`(78)/`large_barcode`(41)/`mnemonic`(371) 均未用。**78 个商品只有中包装条码无单品条码，不纳 medium_barcode 就永远搜不到。**
- 修复（`server/routers/data.py`）：
  - 新增 `_build_product_search(q, fields, cols, code_fields)`：动态按 `PRAGMA table_info` 探测列（兼容不同租户库 schema），编码类=barcode/medium_barcode/large_barcode/product_code，文本类=name/spec/mnemonic/brand（**移除 pinyin_first**）。
  - 相关度 `CASE` 排序。**纯数字**：精确条码0 > 条码前缀1 > 条码片段%q%2（后4位落这）> 名称前缀3 > 名称片段4 > 规格5；**非数字**：名称精确0 > 名称前缀1 > 名称片段2 > spec/mnemonic/brand 3 > 条码4。
  - 新增 `_ensure_search_indexes(conn)`：幂等惰性建 `idx_products_barcode/code/name`，key 用 `PRAGMA database_list` 的库路径（每进程每库一次）。
- 前端 `fill.js onSearch()`：最小触发长度 **纯数字 3 位 / 其它 2 字符**；未达阈值不发请求，只有从"有效搜索"退回才恢复全量（用 `_lastKw` 判断）；防抖 250ms。placeholder 改「搜商品名 / 条码（后 4 位也行）」。
- 验证：本地载入 430 条真实商品离线测试 **12/12 通过**（含 SQL 注入参数绑定）；生产 9 组查询全对（完整条码 total=1、后4位 2988→3条、8003→5条、厂家编码 2197762→1条），接口 118-140ms，索引已建。
- 已知限制：`LIKE '%q%'` 走不了索引（后缀匹配固有代价，430 行 <20ms；过万改 FTS5）；后4位有撞号（8003 有 5 个）。
- 交付文档：`forecast-order-miniprogram-20260812T023419087Z/商品搜索增强-2026-09-06.md`。

## 23:30-23:45 Web 搜索条码 / 数据链核查 / 提交后保留

**① Web 端搜索条码（已修并部署生产）**
- Web 端三处搜索口径不一：预报页「逐单补录」`/api/forecast-audit/products`（**不支持条码**，只搜 name/spec/product_code/pinyin_first）、商品档案 `/api/products`（**支持** barcode）、网格 Ctrl+F（纯前端只过滤 name，未改）。
- 修 `forecast_audit.py::search_products`：复用 `data.py::_build_product_search` 统一口径，字段扩到 barcode/medium_barcode/large_barcode/product_code + name/spec/mnemonic/brand，移除空的 pinyin_first，`ORDER BY id` → 相关度排序，返回加 `barcode`。
- ⚠️ **循环导入坑**：data.py 会 `from .forecast_audit import _connect`，所以 forecast_audit 里**必须函数内延迟 import** data，不能顶层导入。
- 前端 `Forecast.vue:1400` placeholder 改「搜索商品名称 / 条码（后 4 位也行）…」，结果项加 `· 条码 {{p.barcode}}`（`.sd-bc` 等宽）。
- 生产实测：完整条码1、后4位2988→3、莎莎→1、蒙牛前缀→217、厂家编码2197762→1，122-138ms。

**② 数据链核查结论：主体通，两个断点**
- ✅ **小程序 → Web 完全打通**：Web 端 Forecast.vue 汇总交叉表数据源 = `forecast_submission_summary()`（erp_db.py:14876），SQL 直接查 `forecast_submissions` + `forecast_submission_items`（`s.order_date BETWEEN 期次起止 AND status NOT IN ('rejected','recalled')`）。SQL 复刻实测：插入即出现、撤回即消失、0 孤儿。
- ✅ 商品主档同表同 `is_active` 过滤；期次同 `forecast_periods` 表。
- ❌ 断点1：Web 端「改单」走 `/api/forecast-submissions/save-matrix`（写 forecast_extra_qty），**小程序无任何地方读** → 管理员改过的量销售看不到。
- ❌ 断点2：生产 81 单中 **60 单 `period_id=0`**（Web 无期次「今日报单」），小程序必须选期次 → 看不到。设计使然。
- ⚠️ `/api/forecast-submissions/summary` **403「仅管理员/老板可看汇总」** —— sales 账号测会拿到错误信封，不是链路断。
- Web 端返回 4 个期次 vs 小程序 3 个：Web 用 `forecast_period_list()` 返回**全部含 closed**，小程序只 `status=open`。同表不同过滤。

**③ 提交后保留 + 跨期次沿用（已实现，27/27 测试通过）**
- 两套存储：`fs_last_cart_${storeId}`（门店维度，含 periodName/items，供一键带入）、`fs_sub_${storeId}_${periodId}`（门店+期次，防重复提交）。
- 提交成功后**不再清空** cartMap/行内数量；三态 banner：本期已提交(绿)/有上次且空(青,一键带入)/有上次且有内容(清空重填)。
- **重复提交保护**（关键）：同门店+同期次已有记录时先弹 modal「本期已报过…再提交会新增一单」，取消跳 mine。没有它，保留数据=误重复下单陷阱。
- 🐛 顺手修：`submit()` 两处 `wx.switchTab('/pages/summary/summary')` —— **summary 已移出 tabBar，switchTab 只跳 tabBar 页，会静默失败**。改跳 `/pages/mine/mine`。
- 仿真测试 27/27（含提交保留、门店隔离、重复拦截、带入回填、清空不删历史、失败不写回执）；行内填报回归 21/21。
- 交付：`Web搜索条码-数据链-提交保留-2026-09-06.md` + `小程序品牌图标/提交后保留-跨期次沿用-预览.png`。
