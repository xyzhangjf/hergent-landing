# 2026-09-17

## v181 诊断：「改单 → 取消」后仅剩「暂无预报数据」

**用户报障**：导入了「9月15日报单、9月19日到货」这一期的数据，点「改单」再点「取消」，
商品信息等基础信息全不显示，只剩「暂无预报数据」。

### 结论（与用户直觉相反）：**「取消」无辜**

取消=`exitEdit()`→`loadCross()` 重取服务端数据，它显示的**就是真相**。
真相是：**用户导入时那一期还不存在**，154 个商品登记进了**已关闭的 9 期**。

### 实证链（三道，全部只读）

1. **nginx access.log 复原时序**（`/var/log/nginx/access.log`，IP 27.27.121.222，07:21–07:32）
   - 07:21:58 登录 → 默认期次 9，`summary?period_id=9` → **7172 B（有数据）**
   - 07:22:40 `POST /api/import/execute` → **导入落 period_id=9**
   - **07:26:05 `POST /api/forecast/periods`（新建 12 期）+ `GET /periods`（333→366 B）
     → 其后 56 秒内没有任何 summary 请求** ← **本分支的核心指纹**
   - 07:27:01 首次 `summary?period_id=12` → **285 B（空）**（点「改单」时才第一次拉 12 期）
   - 07:31:30 / :42 两次 `save-matrix`；07:31:47/50/51 连点「取消」→ 322 B → 空态
2. **生产库只读**（`tenant_1.db`，`mode=ro` 含 WAL）
   - 期次 7/8/9/10 **全 closed**；12 =「9月15日报单9月19日到货」9/15~9/15、到货 9/19、**open**、创建 07:26:05
   - `forecast_import_products`：**period_id=9，154 行**，`imported_at=07:22:40`
   - `forecast_submissions`：9 期 2 条；12 期 21 条（save-matrix 写的**空**单）
3. **真实接口复算**（复用既有会话令牌，**仅 GET，零写入**）
   | | period_id=12 | period_id=9 |
   |---|---|---|
   | rows | **0** | 4 |
   | imported_products | **0** | **154** |
   | buildRowBase(showAll=false) | **0 行→空态** | **153 行** |

### 根因（代码级）

- `import_router.py:433` 的归属判据注释写明「导入落的期次**必须等于**用户打开页面时默认选中的那个期次（`current`）」
  → **后端按设计执行且生效了**（current=9 → 落 9 → 页面显示 9，三者一致）。设计没错。
- **坏在**：`createPeriod()`（`Forecast.vue:6654`）末尾只 `await loadPeriods()`，
  而 `loadPeriods()`（`:6829`）内部会把 `curPeriod` 改成新期次（新建后 current 变它）
  ⇒ **下拉跳到 12 期、表格仍挂 9 期的 154 个商品**，**从不重载**。
- **同族入口对照**：`onPeriodChange`(6294) ✅ / `gotoPeriod`(5515) ✅ / `confirmDelete`(6328) ✅ /
  `savePeriodEdit`(v180 已补) ✅ / **`createPeriod`(6654) ❌ 漏了** —— v180 补了"改期次"，同族的"新建期次"漏了。

### 修复方案（F1–F5，见交付说明；均未动手，待拍板）

- **F1(P0)** `createPeriod()` 末尾补 `onPeriodChange()`（**1 行**，复用期次切换的唯一收口，不新写第二份）
- **F2(P0)** 空态可行动：说出「最近一次导入的 154 个商品登记在『9月提审期-开放填报』（已关闭）」+ 跳转/重导
- **F3(P1)** 取消语义收口：① `enterEdit` 强改的 `viewMode='cross'` 取消不还原；
  ② 🔴 **`loadCross()` 会让 `watch(()=>cross.value)`（:4540）在 800ms 后 `saveDraftNow()`，
  用只读视图覆盖用户草稿** ⇒ 与取消弹窗里「已录入的数量仍存于本地草稿，再次点编辑可恢复」**承诺矛盾**。
  修法：`loadCross` 赋值前也置 `_ignoreNextWatch=1`（现只有 `loadEditGrid` 置）
- **F4(P1)** 导入落到 closed 期次要显式告知（`forecast_period_default()` 文档**自称「仅展示用」**
  且警告"放宽它会静默改变落库归属"，却被 `import_router` 当落库归属兜底）
- **F5(需授权)** 把那 154 个商品挪到 12 期。**推荐①重新导入一次**（已实测 `current` 现在 =12；
  台账 `INSERT OR IGNORE`+`UPDATE ... WHERE period_id=? AND product_id=?` **只增不删**，
  不清 9 期；「清切片」只清 `role='导入'` 的报单）

### 顺带确认的机制事实（复用价值高）

- `forecast_period_current()`（`erp_db.py:15094`）**只认 open**；`forecast_period_default()`（`:15120`）兜底可返回 **closed**。
- 导入台账唯一键 = `(period_id, product_id)`，**全仓无 `DELETE FROM forecast_import_products`**。
- 导入「清切片」= `DELETE FROM forecast_submissions WHERE role='导入' AND order_date=?`（`import_router.py:495`）。
- 前端行底 `buildRowBase`（`Forecast.vue:1937`）被 `loadCross`(6456) 与 `loadEditGrid`(2529) **共用** —— 两态行集一致。
- `Forecast.vue:2480` / `:1602` 的注释仍写「编辑网格 = 全量商品主档」，但自 v179 起代码已是「导入登记∪有报单」——**注释过期**。

### 技能回写

`hergent-data-staleness-diagnosis` 新增 **步骤 3b：分支 B2「上下文标签与表格数据不同源」** ——
含指纹判据（日志里「上下文变更 + 之后无取数请求」的空档）、复算要求（别只看响应字节数）、
根因模式（**凡改一个 ref 就换上下文，必须同时给出重新取数的路径，且要逐个入口列清单核对**）、
与步骤 3 的区别、以及复用「上下文切换唯一收口」的修复写法。

**交付物**：`outputs/改单取消后空表-排查结论-2026-09-17/改单取消后空表-排查结论-2026-09-17.md`

---

## 07:40–08:15 v181 返利冲刺看板「进度条 vs 时间进度」对比文案（前端）

**用户诉求**：进度条加文字说明 —— 三态（落后／持平／超前）+ 具体差值；
空间充足直接显示，不足则在进度条上悬停提示。

**⚠️ 版本号修正（教训）**：本轮一直用 v179 编号，**提交前才发现已被占用** ——
`3ae7de4`（主表行底 + 导入角标）已占 **v179**、`b6fac46`（期次归属引导 + 期次改名）已占 **v180**
⇒ 全部改为 **v181**：`Forecast.vue` **7 处**注释 + 3 个探针 + 说明。
判据：`git log --grep=v179` 会查到「主表行底」，与代码里讲「进度条文案」的注释对不上。
⚠️ 工作区里另有 **1 处** `v179` 属**并发会话在途**（`Forecast.vue:2524`「编辑态行底与查看态同源」）—— 未动。

**产品改动**（`Forecast.vue`，4 处 + 1 处注释订正）
1. `sprintPaceOf(ach)` —— 三态判定的**唯一**实现，返回 `{state, pp, cls, text, hover}`；
   前置条件 `ach != null && tp.frac > 0`（到货月未开始返回 `null`、不渲染）。
2. `sprintBarClass()` **改为复用它** —— 此前着色单独写了一份 `ach >= frac`，
   差 0.03 个百分点的一行会出现「文案写落后、进度条却判绿」（同屏口径必须同源）。
3. `sprintTimeProgress` 增 `pct1`（1 位小数）；页头「本月时间进度」与决策横幅改用它
   ⇒ 否则文案里的差值（0.4）与页头整数（57%）对不上，**用户自己一算就矛盾**。
4. 进度单元格加 `.sp-pace` + **按真实列宽测量**（判据 `scrollWidth <= clientWidth`）；
   放不下 → `visibility:hidden`（保留占位、行高不跳）+ 进度条挂 `title`。
5. 注释订正：初版写「`width:100%` ⇒ 不撑宽该列」，实测**撑了 72px**（168→240px）。

**🔴 单位口径**：按既有铁律用「**个百分点**」而不是「%」—— 两个百分数**相减**的结果单位是百分点。
交付说明里给了一行可切换的选项（`' 个百分点'` → `'%'`）。

**布局副作用（实测，非估算）**：`nowrap` 文案经 table auto layout **撑宽进度列 168 → 240px**
（= 文案 212 + padding 28），其他列各缩 ~7px。4 视口扫（1680/1440/1280/1100）：
**其他列零换行、页面零横向滚动**；1100 档容器内滚动**是既有的**（各列 min-width 合计 795 > 容器 772）。

**验证**：真机（最终线上包）**24/24**｜纯逻辑（`Forecast.vue` 真切片）**35/35**｜before 基线 6/6（`.sp-pace` 计数 0）
｜v178 回归 **20/20**｜v174/v175 回归 **41/41**｜`Forecast-B8x2rYFo.js`+`Forecast-CzJKAYe2.css` 双侧 md5 一致。

**🔴 只改注释也会变 chunk hash**：改 7 处注释后产物**字节数完全相同**（276733 / 70040），
但 chunk 名 hash 变了（`Forecast-CzbGvR4N.js` → `Forecast-B8x2rYFo.js`）⇒ Vite 的 hash 基于**源码**。
推论：**「双侧一致」不能看文件名，必须比内容 md5**；且改注释后必须重新构建 + 部署 + 复跑验证。

**✅ 已提交（2026-09-17）**
- 代码 `7933e21` `feat(forecast): 冲刺看板进度条新增「与时间进度对比」文案（三态 + 同源着色）（v181）` —— 仅 `Forecast.vue`，+81/−9
- 工具/文档 `3e23637` `test(tools,docs): v181 探针 + 两个回归探针补期次前置 + 交付说明与真机截图` —— 5 文件，+422/−5
- scoped 归属：`Forecast.vue` 20 hunk → 本轮 **12** / 在途 **8**（`101` 空行、`2500/2502/2513/2515` = 09-13 那批 sources 合并 + extraByPid 累加、`7358/7373` = `.imp-errs` **搬家两半**必须一起排、`7722` 空行）。提交后工作区仍余 **8 hunk**（在途完好；零夹带、零回退已复核）。
- spec 名：`fe-v181`（`Forecast.vue`，exclude_hunks）/ `fe-v181b`（两个回归探针，keep_all 干跑自证）—— 写在 `.workbuddy/tools/scoped_stage_by_marker.py`，**该脚本本身未纳入 git**（历轮同样处理）。⚠️ PNG 是二进制，**不能过该脚本**（会按 utf-8 读坏）⇒ 第二笔用 `git add` 落地。

**🔴 交付说明里的构建产物名会过时（本轮踩到）**：`outputs/进度条时间进度对比-2026-09-17/…md` 原写的是
**第一次**构建的 `Forecast-CzbGvR4N.js` / `3YhLLNBF.css`，而最终上线的是**注释重构建后**的
`Forecast-B8x2rYFo.js`(`63a0997c…`) / `Forecast-CzJKAYe2.css`(`9b329b27…`) ⇒ 提交前已回填最终名 + md5 表。
判据：**文档里写死 chunk 名/md5 时，若之后又构建过一次，必须回填**（否则文档与生产对不上）。

**两侧一致复核方式**：`find … | sort` 两侧**排序不同**，逐行 `diff` 会报一堆偏移噪音
⇒ **必须按集合比对**（本轮 53 个文件集合全等：无「仅本地 / 仅生产 / 同名不同 md5」）。

**遗留（已登记，勿重复踩）**：`sprintTimeProgress.pct`（整数版）现已**无任何引用**，保留未删 ——
删它要再走一次「重建 + 部署生产」，收益只是清掉一个死字段，不抵再次触碰生产的风险
⇒ **下次因别的原因重建 `Forecast.vue` 时顺手删除**。

**部署前差集（归一化后）**：26 个文件「字节数相同但 md5 不同」= 纯 chunk 名漂移（连锁改写引用方）；
真变化只有 `Forecast.js` / `Forecast.css` 两项，且**归一化后长度完全相同**（276688，仅压缩器局部变量名分配不同）。

**探针基础设施修复（与产品无关，但会让回归"假红"）**
- 🔴 既有探针假设「进页面选中的期次就有报单记录」—— 实测沙箱当期「9月15日报单9月19日到货」**为空**，
  有数据的是「9月提审期-开放填报」（29 行）。`enterEdit` 在空期次上**不报错、只 toast**「请先选择期次」
  ⇒ 网格零行 → 断言 FATAL `no col`，**看起来像功能坏了**。
  ⇒ 两个探针加「遍历期次挑有数据的」（`page.reload` 后用参数切回同一个，省二次遍历）。
- 虚拟滚动下远处行不可点（`Node is either not clickable or not an Element`）⇒ v178 F 段样本行**限前 12 行**，取不到就跳过。
- v178 G 段断言「网格行品牌非空数 == 档案商品品牌非空数」是**过强假设**（两者是不同集合：网格 158 行 / 档案 285 个，
  昨天凑巧都是 245 才绿）⇒ 改判据为**网格内品牌非空占比 ≥90%**（实测 156/158 = 98.7%，且草稿已清 ⇒ 证明不靠草稿）。

**沙箱**：9998 起两次、销两次均 `ZERO_RESIDUE`。第二次 `verdict=review` 已查明 ——
**今早 08:00 定时任务**写 `cron_execution_log` 3 行（沙箱窗口 07:52–08:02 跨过它）。
🔴 该表**每天固定时刻必写** ⇒ 窗口跨过 cron 时刻 `verdict` 必为 `review`，不是污染。

**交付物**：`outputs/进度条时间进度对比-2026-09-17/`

---

## v182 设计评估：「导入前必须先建期次 + 按新老用户区分清单来源」

**用户提案**：① 必须先点「新建期次」才能导入；② 新用户建期次后预制模版已显示（报单空，
可手填/粘贴，也可下载模版导入）；③ 老用户建期次后**自动复制上一期基础数据**（不复制报单），
不满意可导入覆盖；④ 点击导入后**自动覆盖已有基础数据**。
要求：合理性 / 优缺点 / 优化建议 / 流程步骤。

### 结论：方向对，但有两条必须纠正的前提

| 提案 | 判定 |
|---|---|
| ① 必须先建期次才能导入 | ✅ **强烈支持**（昨天事故的根治），但表述应为「**必须有 `status='open'` 的期次**」 |
| ② 新用户预制模版已显示 | ⚠️ 只对「已有商品档案」的租户成立；**全新租户 0 档案，没东西可预制** |
| ③ 老用户**自动**复制上期 | ⚠️ 改为「**创建对话框里默认勾选 + 可见 + 可撤销**」，不做静默自动 |
| ④ 导入自动覆盖基础数据 | 🔴 **必需，但今天没做到**；照现状实现会复活「159→275」老 bug |

### 🔴 纠正一：「基础数据」是**四个桶**，其中两个的归属键**不是期次**

（全文见 `topics/forecast-order-domain.md §跨期「复制上一期」必须先列「桶 × 归属键」矩阵`）

| 桶 | 归属键 | 可复制 |
|---|---|---|
| 商品清单 `forecast_import_products` | **`period_id`** | ✅ 唯一该复制 |
| 报单明细 `forecast_submissions` | `period_id` | ❌ |
| 加单 `forecast_extra_qty` | 🔴 **`(period_start, period_end)` 窗口** | ❌ 重叠即串期 |
| 定稿 `forecast_audit_decisions` | 🔴 **同上窗口** | ❌ 同上 |
| 客户列/户头 `report_mapping`·`business_profile` | **全局** | — 无需复制 |

### 🔴 纠正二：用户提案的**现象基础本身是 bug**

`Forecast.vue:6715 createPeriod()` 只 `await loadPeriods()`（刷下拉），**从不重载表格**；
`loadPeriods()` 内部改 `curPeriod` ⇒ 「**下拉=新期次、表格=上期完整视图（含数量）**」。
用户看到的「新建后上一期还在」是这个**残留**，不是复制。
判据：**「切换上下文后数据看起来还在」先分清「真被复制」vs「从未被重载」** —— 区别是**数量列有没有值**。

### 🔴 纠正三：第④条今天只满足一半 ⇒ 照现状实现 = 复活老 bug

- 报单明细：**覆盖**，但按 `order_date` 切片（非期次）⇒ 同天导覆盖、隔天导累积（`import_router.py:490-495`）
- 登记台账：**只增不删**（`INSERT OR IGNORE`+`UPDATE`，**全仓无 DELETE**）（`import_router.py:744-756`）
- ⇒ 「沿用上期 154 + 导入 159」= **并集** ⇒ 精确复现「模版 159、系统 275」
- 修法：台账加 **`origin ∈ {import, seeded}`**，导入按 origin 精确清理（替换 / 追加两模式）；
  ⚠️ 新增列须 `PRAGMA table_info` + 幂等 `ALTER TABLE`

### 建议流程（四阶段）

0. **闸门**：无 open 期次 ⇒ 顶部横幅 + 「导入」按钮**禁用**（不是点了才报错）
1. **新建期次**：表单加「本期商品清单从哪来」三选一（沿用上期 / 全部在售 / 空白），
   **默认项由数据判定**（有上期→沿用；无上期有档案→全部在售；都没有→空白并直接开导入弹窗）
   —— **不引入「用户类型」这个新维度**
2. **创建后立即重载表格**（补 1 行）+ 横幅「本期清单沿用自 X（N 个）［清空重选］」+ 留痕 `period_seed`
3. **填数三条路并列**：手工填/粘贴（**已实现**） · 导入（**默认替换本期清单**） · 小程序报单

### 顺带确认的既有能力（勿重复建设）

- 编辑态**复制粘贴已实现**：`Forecast.vue:824` `@paste` → `onPaste:2987` → `pasteRegion:4390`
- 「显示全部商品」开关**已存在**：`Forecast.vue:578`/`1902`/`1963`
- 模版客户列来自 `report_mapping.report_alias`，**全局不按期次** ⇒ 无需复制
- 导入**客户端从不传 `period_id`**（全文件核对）⇒ **闸门必须做在后端**

### 交付物

`outputs/期次数据流程优化方案-2026-09-17/期次数据流程优化方案-2026-09-17.md`
（含实现清单 P0×8 / P1×5 / P2×2，与 5 条待拍板事项）

**全程只读**：未改任何代码、未改任何生产数据。

---

## v183 设计修订：往期列表加「复制」+ 空表要展示列

**用户反馈**：觉得上一轮方案「交互有点啰嗦」。改为：
① 往期预报每期加「复制/修改」按钮，用户自行复制满意的那期，**复制后仅需修改名称即可使用**；
② 点「创建期次」后需把系统中**不可删除的列行**展示出来。问：是否可行 + 调整思路。

### 结论：两点都可行；第②点抓到的是**真缺陷**

### 🔴 「仅需修改名称即可使用」不能照做 —— 归属键是窗口不是名称

| 桶 | 归属键 |
|---|---|
| 报单明细 `forecast_submissions` | `period_id` |
| **加单 `forecast_extra_qty`** | 🔴 **`(period_start, period_end, product_id)`** |
| **定稿 `forecast_audit_decisions`** | 🔴 **`(period_start, period_end)`** |

复制后沿用旧窗口 ⇒ 两期窗口重叠 ⇒ 🔴 **两期共用同一份加单与定稿**，且同屏不报错。
✅ 解法成本极低：v180 已有 `parsePeriodDates` / `applyPeriodDates(dates, force)`
+ 期次名习惯带日期 ⇒ **复制弹窗里把「名称→日期」设为 force 联动**，
用户改名称里的日期 → 窗口自动变 ⇒ 真的只需改名称。

### 🔴 第②点的真相：空态把整张表吞掉了

`Forecast.vue` 表体渲染链（557–744）：
- `v-if="crossLoading"` 骨架
- **`v-else-if="!editMode && (!cross.rows.length || …)"` (561) —— 只读态 0 行 ⇒ 整块空态，
  🔴 连表头都不渲染**
- `v-else-if="!editMode"` (570) 只读表格
- `v-else` (744) 编辑态表格 —— **无条件渲染**

⇒ 「创建期次后看不到列」= 新期次零引用 ⇒ 走空态 ⇒ 表没了。
用户被迫先点「改单」才能看到表（这也解释了他为何把「改单」当查看入口）。
「不可删除」今天**只在列右键菜单**（`Forecast.vue:1530`），而右键要点在列头上 ⇒ 空期次里**不可达**。

**不可删除列的口径**：服务端权威 `forecast_columns.PROTECTED_COLUMNS` = 5 个
（`name`/`barcode`/`sale_price`/`dist_price`/`product_code`）；前端实际展示 **4 个**
（商品名称/条码/分销价/厂家编码，`sale_price` 已 v177 下线属惰性残留）。
⚠️ **列是全局的、不按期次**（`MASTER_COL_DEFS` 唯一渲染源派生 5 个面）⇒ 列**不需要复制**，只是空态看不见。

### 调整思路（要点）

- **A**：往期按钮 `查看/修改/关闭/删除` → `查看/`**`复制`**`/修改/关闭/删除`；
  **复制对 `id>0` 的所有期次开放（不限 open，源可以是已关闭的）**；⚠️ **必须屏蔽合成行 `id<0`**
  （`order-board` 的 `board` 含合成期次，无 `forecast_periods` 记录）
- **A2**：复制弹窗 = 名称预填**全选**（直接打字覆盖）+ 日期 force 联动 + 「顺延 N 天」便捷按钮 +
  「将带过来 N 个商品（不含数量/加单/定稿）」+ 复用 v180 的同名/重叠软警告
- **B**：把 561 的空态从「替换整张表」改成「**表体内一行**」→ 表头保留（含不可删除列，
  右键菜单可达）；那一行给三动作：［从上一期复制］［下载模板导入］［显示全部在售商品(N)］
- **C**（配套，不做会互相拖累）：① `createPeriod()` 补表格重载（1 行）；
  ② 登记台账加 `origin ∈ {import, seeded}`（幂等 ALTER）；③ 留痕 `period_copy`
- **D**：文件落点 = `erp_db.py`（`forecast_period_copy` + `origin` 列）/
  `routers/forecast.py`（`POST /periods/{pid}/copy`）/ `ForecastHistory.vue`（复制按钮）/
  `Forecast.vue`（复制弹窗 + 补重载 + 空态改造）/ `api/modules.js`（`copyPeriod`）

**交付物**：`outputs/期次数据流程优化方案-2026-09-17/期次复制与空表列展示-调整方案-2026-09-17.md`
（本方案**取代**同目录上一份的 §五「新建对话框三选一」，其余结论继续有效）

**全程只读**：未改任何代码、未改任何生产数据。

---

## 13:35–13:55 v182 · 冲刺看板对比文案单位「个百分点」→「%」

用户回复上轮「需确认事项」：**① 单位要「%」；② 页头保留 1 位小数**。

- 改动窄：`sprintPaceOf` 的 `head` 拼接 `' 个百分点'` → `'%'`（产物净 **−12 字节** = UTF-8 13B→1B），
  注释同步改为「按用户指定用 %，靠 hover 算式消歧」（hover 的「达成 61.4% · 时间进度 56.7%」可反推 4.7）。
- ⚠️ 与既有铁律冲突但**按用户决定执行**：数学上两百分数相减的单位是百分点，写 % 会被部分人读成比值。
  已在代码注释与交付说明双处记录「要切回只改一处字符串」。
- 提交：`664eb78`（代码 +7/−4）+ `92999e9`（探针适配 + mkdir 修补 + 说明/截图）。
  scoped 归属：`Forecast.vue` 12 hunk → 本轮 4（6276/6280/6283/6295）/ 在途 8，零夹带零回退。

### 🔴 本轮新知识（已写入技能 `hergent-frontend-deploy-verify`）
1. **Vue scoped scopeId = `hash(路径 + 源码)`** ⇒ 改某 SFC **任意一个字符**，它的全部 `data-v-*`
   **与派生的 `sk-*` 动画名**都会换新。实测：CSS 字节数**完全相同**（70040），但差异
   **5232 = 654 × 8 字节**全是 `data-v-53fd6f8d` → `267babec`，**样式规则零改动**。
   ⇒ 差集核查**必须**抹平 `data-v-*`（**base36**，正则用 `[0-9a-z]` 别写 `[0-9a-f]`）与 `sk-*`，
   否则每次都会误判成「CSS 全量变了」。
2. **构建可复现**：同源连续两次构建，md5 与文件名 hash **完全一致**（`f0735dac…` / `7ds2duDK`）
   ⇒ 差集法可信。例外是「仅注释不同」：字节数相同、md5 不同（压缩器标识符分配变）。
3. **`rsync --delete` 部署后旧产物本地就没了**（`dist/` 也在 .gitignore）⇒ 要对照组只能
   「临时回退该字符串 + `vite build --outDir /tmp/...` 隔离构建」（`--outDir` 不影响产物内容，实测同上）。

### 验证
真机 **24/24**（生产前端 + 沙箱 9998）｜纯逻辑真切片 **27/27**｜差集受控实验：真变化仅
`Forecast.js` −12 字节 ⇒ **零夹带**｜双侧 53 项 md5 全等｜布局：进度列 **240 → 192px**（文案变短的自然结果），
零换行零横向滚动｜沙箱两次销毁均 `ZERO_RESIDUE: true` / `verdict: ok` / `changed_tables: []`。

### ⚠️ 沙箱选源教训
`up --src 10` 当期只有 **1 条**目标规则生效（另一条是 8 月窗口）⇒ 只能测到「落后」一态；
`up --src 1` 有 2 条（简爱 + 蒙牛低温）⇒ 才拿得到「超前 + 落后」两态。
**选源前先查 `rebate_target_rules` 的「当期生效条数」**，别默认克隆哪个都一样。

### 待办（未做，低优先级）
- `scoped_stage_by_marker.py` **从未入版本库**（也未 ignore）—— 每次 scoped 提交都靠它，建议择机 `git add`。
- `sprintTimeProgress.pct`（整数版）仍无引用，登记为「下次因别的原因重建 `Forecast.vue` 时顺手删」。

---

## 15:10–15:40 v183 · 冲刺看板「进度」列文案 → 「达成率 X%」

**用户判据**（原话）：面板页头已展示时间进度、进度条上有虚线 ⇒ 用户能一眼看出超出还是落后，
「落后时间进度 X%」这类差值判语是**冗余信息**，删掉，只留简洁的达成率。
⇒ 一般化：**界面另有载体能让用户自行得出结论时，再把该结论显式写出来就是冗余。**

### 变更（`Forecast.vue`，唯一产品文件；5 处代码 + 2 处注释订正）
- 新增 `sprintAchText(ach)` = `'达成率 ' + paceNum(ach*100) + '%'`（1 位小数、整数不带 `.0`）
- 🔴 **`sprintPaceOf` 与文案解耦**：它只留 `{ state, pp, cls }` 供配色。**顺带修掉一个缺陷** ——
  v181/182 把文案挂在它身上，而它 `frac=0`（到货月未开始）时返回 `null` ⇒ 那时**连达成率一起消失**
- `sprintPaceMap` 携带 `{ text, cls }`；`paceHint` 回吐同一句文案；模板 `v-if` 按 `text` 判空
- 两处注释按实测值订正（v181 写的「本元素撑宽该列 72px」已随文案变短失效）
- **保留未动**：页头「虚线＝时间进度…超前/落后」图例（承载口径）、进度条红绿（与返利排行同口径）
- **颜色保留**：仍按与时间进度的对比给红/绿（用户只说去掉差值**文案**，颜色是与进度条同源的线索）

### 验证
| 项 | 结果 |
|---|---|
| 纯逻辑真切片（从 `Forecast.vue` 抠真函数执行） | **32/32**（含 `frac=0`：无色但**有**达成率） |
| 真机（生产前端 + 沙箱 9998 ← `tenant_1`） | **29/29** |
| 沙箱 | `ZERO_RESIDUE: true` / `verdict: ok` / `changed_tables: []` |
| 零夹带（抹平 chunk 名 + `data-v-*` + `sk-*`） | 真变化**仅** `Forecast.js`（−73 字节） |

实测：`达成率 61.4%`（真实 61.372%）/ `56.3%`（真实 56.250%）｜文字色与进度条配色同源｜
**页头 56.7% = 行内虚线 56.667% = 真实日期进度 56.667%**（用户「自己比」的前提）｜
表格行内旧判语计数 **0**、页头图例仍在｜列宽 **168px**、文案自然宽 **74px（占 44%）**。

### 🔴 三个新知识（已固化进技能）
1. **`dist/` 是共享可变产物**：本轮线上被并发会话 15:20 的构建覆盖（我 15:17 的 `De3UWvpU` 被
   `Cx2A1X5F` 顶掉），我的 rsync 推的是那一份 ⇒ **部署前必须核对「产物名 + 字节数」与自建一致**，
   不一致就查 `dist/` 的 mtime。
2. **`scrollWidth` 在未溢出时会被 `clientWidth` 抬平** ⇒ 它只能判「有没有溢出」，量不出文字自然宽
   （要量得先把宽度约束住）。v181/182 的注释把这点写错了，已订正。
3. **`table-layout:auto` 的列宽下限 = `max(td 自带 min-width, 文案 min-content)`**
   ⇒ 给 `td` 设 `max-width` **无效**（实测设 64px、实际 102.8px）；探针「压 td」的手法在文案变短后失效。

### 提交
代码 `0c542db`（13 hunk，+40/−32）｜工具/文档/截图 `00c1b79`（4 文件）。
scoped 归属：29 hunk → 本轮 13 / 在途 16（并发会话 v184）。⚠️ 归属**不能只靠关键词**
（`6291` 只改行尾注释、`6334` 只改函数体，都被漏判）⇒ 必须打印 hunk 全文人工确认。

### ⚠️ 未决 / 需用户决策
- 线上现含**并发会话 v184「复制期次」前端**（后端 `hergent-erp/server/routers/forecast.py:175/219`
  已实现但**未部署**；线上路由表 1125 条里无 `copy|seed`）⇒ 点「复制」会报错。
  建议让那个会话把后端一起部署，或把生产回滚到 v182 构建。
- v183 最后两次改动是**纯注释**，未重新部署（避免把并发会话更新的未验证字节再推一次）。

## 15:40–15:55 v183 收口（可复用脚本落库）

- **第三～五笔提交**（工具收口）：把三个**可复用脚本**从 `/tmp` / 工作区落库，并把交付说明
  第六节的 `/tmp` 路径改为入库后的工具路径（另补取「改前目录」的 rsync 命令，只取 js/css/html）。
  - `003768c`（3 文件，+209/−4）—— 落库
    `.workbuddy/tools/forecast-sprint-achv-logic-check.mjs`（从 `Forecast.vue` **真切片**取
    `paceNum`/`PACE_EPS`/`sprintPaceOf`/`sprintAchText`/`sprintBarClass` 执行，非复制逻辑，
    可替换 `frac` 做边界测试；入库后实跑自证 **32/32 PASS**）与
    `.workbuddy/tools/dist_normalized_diffcheck.py`（抹平 chunk hash / `data-v-*` / `sk-*` 后
    逐字节比，输出「派生噪声 N / 真变化 M」）。⚠️ 后者在 v181/v182/v183 被**现写重写过三次**，
    就此入库（脚本头部已写明「别再重写第四次」）。
  - `a994f50`（1 文件，+652）—— 落库 `.workbuddy/tools/scoped_stage_by_marker.py`。
    ⚠️ 它被技能 `hergent-scoped-commit` 引用 **6 处**（含 4 处调用示例）却**从未入库**
    ⇒ 换 clone / 重建工作树后技能会指向不存在的文件。**断链已消除**；v171→v183 共 19 棵
    SPEC 与逐 hunk 归属依据一并留档（=「当时这个 hunk 为什么归我」的审计档案）。
  - `ea15ed0`（1 文件，+12/−2）—— 复跑 `fe-v183` 时命中「黑名单里有不存在的 hunk（基线漂移？）」。
    **这是守卫按预期生效**：spec 记的是**提交前**的行号快照，v183 进 HEAD 后其后 16 个在途
    hunk 的 `old_start` 整体位移 ⇒ 名单必然全失效。已在 spec 顶部标 ✅「已提交 + 一次性留档」
    （那 16 个在途 hunk 从未提交，worktree 也复现不出当时行号），并给该守卫的报错补上行操作
    指引 —— 否则未来的人会把守卫当回归去「修」，**改了只会错得更隐蔽**。
- 快照核对：提交链 `0c542db` → `00c1b79` → `003768c` → `a994f50` → `ea15ed0`；索引已清空；
  `Forecast.vue` 在途 hunk 仍为 **16**（零夹带、零回退复核实证）；三个脚本 `git ls-files` 已跟踪。
- `/tmp` 本次临时产物（`v183_*` / `staged_*` / 消息文件，含两份生产 `dist` 副本与截图）已清理，残留 **0**。
- 收尾时踩到记忆里的老坑一次：`git ls-files --error-unmatch A && git ls-files ... B` 因前者
  **exit 1** 而整链中断（第二、三条根本没执行）⇒ 核对待查项一律用 `;` 分隔，勿用 `&&`。

---

## 15:00–16:00 v184 期次复制 + 空表列展示（设计拍板 → 落地 → 验收 → 三笔提交）

**用户拍板**（对上一版方案 3 问）：「1.开放；2.默认就顺延；3.保留」
= ①「复制」对**已关闭**期次开放 ②复制时窗口**默认就顺延**（不靠改名带动）③工具栏「新建期次」保留。

### 🔴 为什么「只改名称就能用」不能照做（本轮最该记住的判定）
期次下数据分属**四个归属键各不同**的桶，只有一个是期次 id：

| 桶 | 表 | 归属键 | 可复制 |
|---|---|---|---|
| 商品清单（导入登记） | `forecast_import_products` | `period_id` | ✅ 唯一 |
| 报单数量 | `forecast_submissions` | `period_id` | ❌ 那一期的事实 |
| 加单 | `forecast_extra_qty` | 🔴 `(period_start, period_end, product_id)` | ❌ 窗口键 |
| 定稿 | `forecast_audit_decisions` | 🔴 `(period_start, period_end)` | ❌ 窗口键 |

证据 `routers/forecast_submissions.py:462`、`erp_db.py:15200`。**沿用旧窗口 ⇒ 加单与定稿被两期共用
（窗口重叠即串期），同屏不报任何错** ⇒ 复制必须整体顺延（默认 +7 天，名称里的日期同步后移）。
⚠️ 顺延按钮是**相对源期次重算**，不是「在当前基础上再加 N 天」（否则连点叠加）。

### 交付与提交
| 仓 | commit | 内容 |
|---|---|---|
| hergent-erp `upgrade/v84-international` | **`ed47d2d`** | `_period_copy_products`（唯一实现）/`forecast_period_copy`/`forecast_period_seed` + `POST /periods/{pid}/copy`·`/seed` + `origin` 列 |
| laozhangai-product `main` | **`fc55ccc`** | 往期列表「复制」+ 复制弹窗 + **空态改「表头保留 + 表体内一行引导」** + `createPeriod` 补表格重载 + seed 守视图 |
| 同上（文档/截图） | **`cdca9f4`** | 交付说明 + 幽灵连接缺陷报告 + 11 张真机截图 |

新增端点 `POST /api/forecast/periods/{pid}/seed` = **填入已有期次**（空态引导行「从上一期复制清单」用，
「不必先删再建」）。已 `/openapi.json` 确认注册、双侧 sha256 逐字节一致、服务 `active`、health 200。
✅ **回应本日志 v183 节那条遗留**（「线上含 v184 前端、后端未部署 ⇒ 点复制会报错」）：**后端已部署**，该隐患已消除。

### 关键实现判定（都记住了）
1. **`loadPeriods()` 会把 `curPeriod` 无权重设成后端默认期次**（`forecast_period_default()` `erp_db.py:15261`
   只回答「默认该看哪一期」，**不是**「保持用户在看的那一期」）⇒ `seedFromPrev` 必须**守住视图**，
   否则 seed 到一个更老的空期次后视图被甩走、同屏不报错。对比 `createPeriod()` 里同名的 `loadPeriods()`：
   那里「切到新建的那一期」正是想要的 ⇒ **只有 seed 这条路需要守**。
2. **`origin` 列必须显式写**（`INSERT OR IGNORE` 在行已存在时整条跳过、列 DEFAULT **不会**被应用），
   且台账按 `(period_id, product_id)` 幂等**只增不删** ⇒ 「先复制上期 154 + 再导入 159」= 并集 ≈275，
   正是 8 月底那次「模版 159 个、系统 275 个」的症状。
3. **空态缺陷为真**：原「只读态 0 行 ⇒ 整块空态」**替换掉整张表**（连列头），而「哪些列不可删除」的提示
   挂在**列头右键菜单**上 ⇒ 空期次里完全不可达。已查清两条 `.ctx-menu` 的**唯一渲染出口在编辑态分支**
   ⇒ 查看态右击列头**不出菜单是既有边界**（v184 之前就有），故 D6 如实断言「查看态无菜单」。
4. 合成行（`id<0`，如「2026-07-24 报单」）**状态列也显示「进行中」** ⇒ 探针必须用
   `^\d{4}-\d{2}-\d{2}\s*报单$` 过滤，否则稳定命中它。

### 🔴 幽灵数据库连接（本轮最重的发现，产品级 P0，已单独出报告）
沙箱 `down`+`up` **重建同名库**后，服务进程里按 **(线程, 路径)** 缓存的连接仍指向**已被 unlink 的旧 inode**
（`_sqlite_connect()` `db/connection.py:200` 复用前只 `SELECT 1`，而 unlink 后旧 fd 依然有效）⇒
**异步端点（事件循环线程）读写幽灵库**，同步端点（线程池）开新文件。叠加 `set_tenant_context()`
的「文件缺失即自动建库」兜底（`connection.py:93`）后表现为：**`POST` 全回 200（还回 id=19/20 单调递增）
却不落盘、`GET` 看不到**；文件里 `sqlite_sequence` **恒为 12**（AUTOINCREMENT 序列是事务性的 ⇒ 该文件上从无提交）。
- 定案四实验：`commit_test`（调 API 前后 `max(id)` 不变）→ `http_vs_file`（HTTP 读得到我进程内插的 id=13、
  HTTP 写后文件不变）→ `inproc`（进程内直调**能提交** ⇒ 管道无恙）→ 重启服务后全正常。
- **规避（已写进技能）**：重建/替换租户库后**必须重启服务**，且顺序 = **先重建库、再重启**。
- 修复建议：把探活升级为 inode 校验（`os.stat` 比 `st_dev/st_ino`，约 8 行）+ 路径级 evict。
  ⚠️ 涉及连接层基础设施，**未动生产代码**，待用户确认。

### 验证
| 项 | 结果 |
|---|---|
| 真机（沙箱 9997） | **51/51 全绿**（A 合成行过滤 / B 弹窗顺延联动 / C 复制执行 / D 空表保留表头 37 列 / E seed 不甩视图 / F 新建重载） |
| 后端部署 | 三文件双侧 sha256 一致（`53d351b0…`/`d4f04729…`/`1a57fd8a…`） |
| 前端部署 | 归一化差集「仅 `Forecast.js` 变 +75 字节」→ 双侧 md5 一致（`10c02613…`） |
| scoped 归属 | 后端 11=6+5、2=1+1、3=3；前端 16=8+8、2=2、9=1+8；残留 hunk == 在途数**逐文件吻合**；在途符号零夹带 |
| 编译自证 | 后端 `py_compile` + **AST 模块级**；前端定点 SFC 编译（820 绑定）+ `node --check` |
| 沙箱 | 3 轮 up/down 全 `ZERO_RESIDUE: true` / `src_sha256_before == after` |

⚠️ 探针本轮踩到 3 个假结果，都写进技能：`data-pid` 挂在 **`<td>`** 不在 `<tr>`（`tr[data-pid]` 恒 0，
让一项**永真假 PASS**）；`select.sel-period` 只在「本期预报」tab（在历史 tab 读恒 null）；
`click({button:'right'})` 在 headless 下**不产生 contextmenu**（要 `dispatchEvent`）。

---

## 货损核算模块（月度）· 设计方案（2026-09-17，只设计未实施）

交付：`Hergent-货损核算模块设计方案-2026-09-17.md`（12 章，含 DDL / 接口表 / 12 条问题清单）

**先做的一件事（省了一整轮返工）**：用户说「设计货损计算页面」时先查仓库 —— 发现**已有
`LossWorkflow.vue`（349 行）+ `/api/loss` 三端点 + `workflow_recipes['loss_recipe']`**，
但那是**配方驱动的批次效期损耗（存量快照预测）**，与本需求的**期间流水核算（货损率）**不是一回事。
⇒ 结论：**新页面 + 老页改名**（否则一个产品里两个"货损"不同口径）。

### 🔴 舟谱导出真实结构（实读 4 份样本，最有复用价值）

样本：`~/Documents/hergent-erp/舟谱模版/采销管理/`（文件名是**整串 URL 编码**，需 unquote）

| 文件 | 版式 | 关键列 |
|---|---|---|
| 调拨单明细 | 单据头块(r3/r4) + 空行 + 明细块(r7/r8) + `合计` | 头：`单据编号/单据日期/调入仓/调出仓/创建人`；明细：`商品名称/条形码/单位/订单数量/出库数量/入库数量` |
| 退货订单-车销 | 单据头 + 明细 + 小计块 | 头有 `客户/业务员/仓库`；明细有 `退货价（折后价）/订单金额/退货仓库` |
| 退货订单-自提 | **扁平列表**（一单据一行） | `单据编号/客户/业务员/仓库/状态/订单金额`；**无商品明细列**；有 `总计` 行；**r3/r4 是筛选条件行**（含时间段） |
| 销售订单-车销 | 单据头 + 明细 + 汇总块 | 明细有 `销售价（折后价）/订单金额/销售仓库` |

**四条硬事实**（设计时逐条踩到）：
1. 🔴 **调拨单明细没有金额列**，只有数量 ⇒ 金额必须 `数量 × 单价` 折算（退货单反而自带 `订单金额`）
2. 🔴 **调拨单明细的 `条形码` 整列为空** ⇒ 商品匹配不能只靠条码
3. 🔴 **调拨单没有"业务员"列**，只有 `创建人`（制单人）⇒ 业务员归属**必须走「仓↔业务员」映射**，不许靠"仓名去仓字"猜
4. 🔴 **从未出现"良品仓"三个字**（只见 `临期仓/王琴仓/毛辉仓/刘善涛仓`）⇒ 「良品仓调至X」的口径**取不到数**，必须先确认仓名

其它：**无公式**、日期是**文本**、**表头不在第 1 行**、画布 200 行只用十几行、仓库名形如 `<业务员名>仓`。
真实枚举：客户 12 个（**含 `美联总仓`/`多多买菜`/`沃尔玛`/`嗨特卖` —— 不都是"直营门店"**）、业务员 6 个。

### 🔴 需求里我判定不合理的地方（最贵的几条）

| # | 问题 | 判据 |
|---|---|---|
| 1 | **分子混了两种币值**：退货额是「折后**售价**」（实测 `订单金额`=退货价×数量），调拨额只能是「**成本价**」（无金额列） | 实算（毛利率 20%）：混用 **6.75%** vs 一致口径 **7.63%** —— **低 0.88 个百分点 / 相对偏低 11.5%**，且随毛利率变化**事后无法修正**。⭐ 一致口径下**选售价或成本结果完全相同**（同比缩放），错只来自混用 |
| 2 | **「临期货销售额」抵扣 = 售价减成本** | 售价 > 成本 ⇒ 分子为负 ⇒ **负货损率**；且"卖得多率就低"可被刷数。正解用**成本额**抵扣；若坚持售价，必须配「临期货折扣率」列防刷 |
| 3 | **三个率分母各不相同却并列**（门店销售额 / 良品仓发货额 / 公司销售额） | 不能相加、不能平均、不能比大小 ⇒ 必须改名 + 列头标分母 + 公司率注明"不是上面几行的合计" |
| 4 | **良品仓直调 / 报损没有分母** | 给"率"就是凭空造分母 ⇒ 只给金额 + 占比 |
| 5 | **"两类数据"不够**，实际要 4 类 | 缺「临期货销售额」（销售订单筛 `销售仓库`=临期仓）与「门店销售额」（分母）；报损舟谱**根本没有**，Hergent `wastage_orders` 也**整表 0 行** |

### 🔴 复用的既有设施（不重造）

- **报单对象字典 `report_mapping`**（`erp_db.py:638`）：`counterparty_type('store'/'customer'/'self_warehouse')` + **`src_wh`/`dst_wh`** ⇒ 门店白名单 + 仓↔业务员映射都塞这里
- **导入两阶段** `import_router.py` 的 `/preview`（只读）+ `/execute` + `FIELD_LABELS`（唯一一份）
- **留痕** `db/queries/entity_logs.py` + `routers/_field_log.py::safe_log`（事务提交后写）
- **RBAC**：`/api/loss` **已映射 `stock`**（`server.py:314`）⇒ 挂 `/api/loss/accounting/*` **无需新增登记**；否则 fail-closed 403
- **月账设计**：`loss_monthly` 用 `period` 做键**一月一行**（可日更）+ `loss_daily` 日快照留痕 + **未结账标识 + 结账锁定** —— 否则一个月出现 30 条互相矛盾的"9月货损率"
- ⭐ **免费的数据完整性闸**：自提版退货单 r3/r4 就是筛选条件行 ⇒ 可**自动读出用户当时筛的时间段**，做重叠检测 + 缺口告警 + MD5 幂等

---

### ✅ 用户答复三条（2026-09-17 下午）⇒ 设计**修订 1**

用户原话：
1. 「**总仓**，但不同的经销商叫法不一样，可以默认是总仓，但**要支持用户自己改名字**」
2. 「我们公司是**自提订单和自提退单**，但还是一样的问题，别的经销商可能叫**销售单/销售退单**」
3. 「**按售价**，代表给公司**挽回了多少金额**」

**答复① 引出的最大增量（比三条答案本身更重要）**：一切「叫法」必须可配 ⇒
🔴 判据：**任一硬编码在代码里的舟谱字面值 = 一个「只能卖给一家客户」的暗桩**。
落地：新增 `loss_roles`（显示名唯一一份）+ `loss_role_aliases`（`UNIQUE(alias)` 库层防歧义）
+ `source='learned'` 沉淀 + `hit_count` 反假旋钮 + **未归类带量告警** + **改别名必须给重算路径**（前提：事实表存原文）。
详见文档 §3.5 / §11 铁律 13。

**答复③ 的关键结论（量化）**：用户选「按售价抵扣」语义合理（净损失 = 实际损失 − 已挽回），
⚠️ **但必须把 `pricing` 也切 `sale`**，否则是「组合 C 混用」：
- 组合 C（成本调拨 + 售价抵扣）**低估**，多扣的正好是**临期销售那批货的毛利**
- 实算（毛利率 20%、抵扣额 12,000、成本分母 100,000）：**C = −2.00% vs B = 0.40%，差 2.4 个百分点**；
  而 A（全成本）与 B（全售价）**只差 0.4 个百分点** ⇒ **混用偏差比口径选择本身大 6 倍**
- ⇒「选哪个口径」听用户的，**「是否混用」不能听** —— 必须按自洽落地

**答复② 引出的新发现（自提退单实测复看）**：
- 自提退单**自带 `业务员` 列**（调拨单没有）⇒ 业务员维度拆分可行
- 🔴 有 **`状态` 列**（`已完成/待审核/已取消`）⇒ **必须只取 `已完成`**（新增第 4 道闸），否则已取消单据算进分子 → 虚高
- 🔴 **`仓库` 列在两个版式里语义相反**（退货单=退到哪 / 销售单=从哪发）⇒ 必须版式分流后**分别**映射
- ⚠️ 「自提**订单**」版式**本轮仍未实测** ⇒ 解析设计基于「与退单同构」假设，**已标为待验证**

**文档改动**：`Hergent-货损核算模块设计方案-2026-09-17.md` **修订 1**（680 → 960 行）——
§1 改「已定案 + 🔴4 架构需求」/ §2.1 仓表不再当角色来源 / §2.3 补自提版 4 个发现 / §2.4 改「已确认」+ 桶×消费方矩阵 /
§3.2 加 ⑥⑦ 两表 / §3.3 判定条件改「角色式」+ 未归类 fail-closed / **§3.5 新增「叫法可配置」** /
§4.2 加毛率·净率·折扣率列 / §4.3 公司行毛净两式 / §5.3 映射表改别名来源 / §5.4 三道闸 → **五道闸** /
§6 加 6 个 role/alias 端点 / §7 界面改真实单据名 + 毛净对照 / §8.3 重写（已定案 + 组合 C 量化）/
§10 叫法映射提 **P0-1** / §11 铁律 13 / §12 改「已定案 + 新 3 问」

**技能沉淀**：`hergent-rebate-caliber-consistency` 加 **§11 第五类口径问题（抵扣项与被抵扣项不同币值）**；
`hergent-excel-attachment-to-schema-design` 加 **§十一「叫法」层**（+ §十.7 自查项）。

---

## 16:00–17:00 v184b「到货周期」固定列（主表新增 + 导入模版同步 + 页面对齐导入）

⚠️ **命名说明**：本轮的 `v184b` 与并发会话的 `v184`（期次复制 + 空表列展示）**不是同一件事**，
仅因同一天两线并行而共用前缀。提交信息里已写明。

**诉求（用户一条命令，八项硬要求）**：主表新增**固定列**「到货周期」（一眼识别每个单品 +几天到货，
如 +3天 / +4天）＋ 导入模版同步新增该列；明确字段来源/映射规则、展示格式、默认值与空值处理、
固定列位置与滚动行为、模版列名/位置/校验规则；**页面显示与导入解析必须一致**；模版里**不设为必填**。

### 关键侦察结论
- 🔴 **物理列早已存在**：`products.arrival_lead_days`（`erp_db.py` v109 迁移，`INTEGER DEFAULT 0`），
  导入早已在回填它，**前端从未引用过** ⇒ 本轮是「把已有数据接出来」，不是新建字段。
- 🔴 **三个「天数」概念必须区分**（口径撞车高发）：
  `arrival_lead_days`（本列，默认 0）／`lead_time_days`（补货算法提前期，默认 7，进安全库存公式）／
  `order_cadence_days` + `forecast_audit.DEFAULT_ARRIVAL_CYCLE=2`（品牌级到货**频率**，也叫「到货周期」）。
  ⇒ 三者是三个量，代码注释与交付文档都逐条写清。
- 🔴 **发现产品缺陷①（本轮修的实质）**：`import_router.py` 原正则 `\+(\d+)\s*到货` **只认「+3到货」**，
  而页面列名与模版说明对用户说的都是「+3天」⇒ 用户照模版填「+3天」**静默算 0**、页面永远「—」、
  **不报错**。这正是「页面显示与导入解析不一致」的典型形态。
- 🔴 **发现产品缺陷②**：`forecast_submission_summary` 的 `imported_products` 查询未下发该字段，
  且前端 `asProdRow` 是**显式白名单** ⇒ **off-archive 行**（已停用/已删除但被本期引用，不走
  `/api/products/grid`）恒显示「—」而 DB=3。真机复现：沙箱 tenant_9999 期 13 的 pid 1160。
- 🔴 **发现产品缺陷③**：`ctxClear`（右键「清空此单元格」）是**绕过 `writeCellVal` 的第二条写路径**
  （直接 `rw[key]=''`）⇒ 只堵 `writeCellVal` 时，清空会把界面置「—」但刷新又变回 +3天。
  「清了会自己回来」比「清不动」更糟 —— 用户会以为系统在丢数据。

### 落地（两仓已提交）
- **后端 `60a8f80`**（hergent-erp，分支 `upgrade/v84-international`，3 文件 +55/−5）：
  `data.py::products_grid` 下发字段（只读，**不进** bulk_upsert 白名单）；`import_router.py` 8 处
  （正则扩展 `[+＋]\s*(\d+)\s*(?:天到货|到货|天)` 含全角＋ / 关键词补「到货周期·到货天数」新词置前 /
  标签「订单排期」→「到货周期」/ 模版第 8 列 `required=False` 选填 / 示例值用页面同款「+3天」/ 填写说明改写）；
  `erp_db.py` 的 `imported_products` 查询补 `COALESCE(p.arrival_lead_days,0)`（+6 行）。
- **前端 `d7f977f`**（laozhangai-product，分支 main，8 文件 +1048/−28，`Forecast.vue` 39 hunk）：
  列定义 `edit:'ro'` + `fixed:true` + 文案唯一实现 `arrivalCycleText`；
  **冻结偏移泛化**（`frozenShift(base)` 硬编码 200 → `frozenLeftOf()` 按 `colW()` 累加；
  原 200 与 `COL_DEFAULTS.name=210` 不符 ⇒ 恒偏 10px）；`FROZEN_COLS` + `visibleCols` **强制归位**；
  `isFrozen` 认 `fixed` + `colOrderList` 透传；`isLockedCol` 统一守卫（不可隐藏/删除/拖拽）；
  `edit:'ro'` 只读渲染 + `writeCellVal` 拦并返回布尔 + `ctxClear` 独立闸门 + `clearRange` 实际计数；
  两条加载路径（`loadCross`/`loadMatrix`）都补字段；6 处冻结样式统一；顺手修弹层读错键名
  （`prodProfile.lead_days` 行对象里没这个键 ⇒ 该格一直是空的）。
  另附 6 个新探针/自检脚本一并入库。
- **模版列**：`到货周期` 第 8 列（紧接商品身份列之后、客户列之前 —— 它属于「这一行商品」的属性，
  放客户列后面会被误读成某个客户的属性），`required=False`，真实 xlsx 表头无 `*` 无 `☆`。

### 验收（全绿）
- 后端规则自检 **23/23**（AST 真切片取 pattern/关键词表/模版字段，非复制规则）。
- 真机·本期主表 **31/31**（横滚后该列 left **540→540 不变**、**对照品牌列 632→212 同步左移**防假 PASS、
  表尾另一张 table 同样 sticky、虚拟滚动遍历取文案、编辑态 0 input、表尾不进合计、只读闸门生效）。
- 真机·往期 **6/6**（列存在、位置一致、同样冻结、不是全「—」）。
- 真机·导入落库 **6/6**：`+4天`→4、`＋3天`→3、`+3到货`→3、**留空→3 不清零**、`随便写`→0 不写入、
  停用商品 `+3天`→3。
- 部署核对：前端产物 md5 与生产落点**逐字节一致**；后端落点 `grep -c arrival_lead_days`=3；`/api/health` 200；
  沙箱 `ZERO_RESIDUE: true`。
- 先取回生产 `erp_db.py` 与本地 diff ⇒ **差异只有 6 行本轮改动**，无他人成果被回退。

### 🔴 本轮新增的可复用判据（已写进技能）
1. **「页面显示 == 导入解析」的同源判据**：自由文本列的正则必须与「**模版示例值** + 填写说明」
   逐字一致（示例是用户唯一会照抄的东西）；中文输入法**全角＋**要一并收，否则静默算 0。
2. **字段要下发三处**：`/api/products/grid` 白名单 ／ `imported_products` 查询（off-archive 行）／
   前端 `asProdRow` 白名单。⚠️ 前端还有**两条加载路径**（`loadCross` 本期 / `loadMatrix` 往期）。
3. **固定列不变式**：偏移按 `FROZEN_COLS` 累加且**禁止硬编码**；冻结区必须**连续**（`visibleCols` 强制归位，
   否则偏移算到别的列头上、错位且不报错）；`isLockedCol` 是唯一规则（**列设置菜单的 `c` 来自 `colOrder`，
   只有 name 带 fixed，不能读 `c.fixed`**）。
4. **只读列必须堵两条写路径**（`writeCellVal` / `ctxClear`）；`writeCellVal` 返回布尔后 `clearRange`
   才能按**实际**计数报数。
5. **探针两个「假失败」形态**：① 编辑态 `Delete` 清空**不可达**（`@keydown` 挂 `<table>`、td 无 `tabindex`
   ⇒ 焦点只能落 input，而 `onGridKey` 对 INPUT 放行删除）⇒ 要测真实入口 `ctxClear`；
   ② 编辑态表尾是**第三张 table** 且 td **不带 `colCls`** ⇒ 必须按**列序**对齐找。
6. ⚠️ **对照断言必须选「原本非空」的格**：本轮第一版拿「第一个 fc-text 列」当对照，而那格本来就空 ⇒
   「清空后仍为空」恒真 = **空断言假 PASS**，自查时才发现。

**技能沉淀**：`hergent-forecast-column-registry` **大幅扩写**（122 → ~215 行）——
新增 §1.1「列的三类形态（普通/固定/只读）」、§2b 加固定列 7 步、§2c 加只读列 6 步、
**§8 固定列不变量**、**§9 只读列两条写路径**、**§10 页面==导入的同源判据与三处字段来源**；
§7 陷阱区补「三个天数列对照表」（并订正旧条目里 `lead_days` 的歧义：那是**不带后缀**的键，
别据此误判 `lead_time_days`/`arrival_lead_days` 不存在）、探针假失败两形态、toast 3s、
scoped CSS 产物形态；§5 补「加固定列/只读列时的额外断言」；§6 补「别复跑上一轮已提交的 spec」
与「一次性 spec 写进 `SPECS` 留档」。

---

## 21:25 追问「到货周期可编辑吗？」→ 全路径实测 + 注释订正（`3a60c4d`）

**结论：网页里不可编辑（只读），全仓唯一写入口 = 预报订单导入模版的「到货周期」列。**

**全路径实测（以代码为准，不凭记忆）**：
- 前端全 `src` grep `arrival_lead_days` ⇒ **只命中 `Forecast.vue`**；`ProductArchive.vue`（档案管理→
  商品档案）**零命中** ⇒ 独立档案页**根本没有这个字段**。
- 后端全 `server` grep ⇒ 唯一**写**语句是 `import_router.py:709`
  `arrival_lead_days=CASE WHEN ?>0 THEN ? ELSE arrival_lead_days END`；其余全是读
  （`data.py:403` grid 下发 / `erp_db.py:16514` summary）或 DDL（v109 / `_TENANT_COL_SYNC`）。
- `bulk_upsert_products` 的 fields 白名单**不含** `arrival_lead_days` ⇒ 网格即便能改也存不回去。
- 模版覆盖：`_TEMPLATE_FIELDS["forecast_cross"]` **有**该列；`["products"]`（商品导入模版）**没有**。

**可编辑性总表**（答用户时用）：

| 位置 | 能看 | 能改 |
|---|---|---|
| 本期预报主表「到货周期」列 | ✅ | ❌ 只读（`edit:'ro'`） |
| 预报页 → 右键商品名 → 商品档案弹层 | ✅ | ❌ |
| 商品档案页（档案管理） | ❌ 无此字段 | ❌ |
| **预报订单导入模版** 第 8 列 | ✅ | ✅ **唯一入口**（选填） |
| 商品导入模版 | ❌ | ❌ |

**顺手订正两处会误导下一人的旧注释**（纯注释，无行为变化）：
① `data.py::products_grid` 原写「写入口有**两处**：① … ② 无」—— 自相矛盾，且用的是**改名前的
标签「订单排期」**；改为「全仓唯一写入口」，并补上「为什么不能收进 `bulk_upsert`：**收了反而危险，
网格每次保存都会把导入来的值覆盖成空**」。
② `import_router.py::_TEMPLATE_FIELDS` 原写「解析：`+N天` / `+N到货` / **`N`**」—— **裸 `N` 实际不认**
（正则要求加号 + 「天/到货」收尾），写在模版字段表注释里 = 教用户填一个会被静默忽略的值；
改为列出真实可写的四种写法并写明「必须带加号及其原因」。
🔴 **教训**：注释改动**不触发部署**，随下一次后端部署一并生效 —— 别为注释单独走一遍部署。

**给用户的候选（若要做成可编辑，见答复里的 A/B/C）**：最贴合语义的是放在**商品档案**（它是商品主档
属性，不是报单数据），而非网格直编 —— 后者要把 `edit:'ro'` 改成可输入，会连带「文本列怎么输入」
与「会不会进表尾合计（只有 `edit:'num'` 进）」两个新问题。

---

## 21:40 货损核算页面落地：手工填报可用 + 导入预留 501（⚠️ **未部署**）

**用户指令**：「先完成页面整体搭建，第一步优先实现手工填报功能，确保用户可以手动录入并提交数据；
数据导入功能暂时不做，留待下一步再实现。请在代码中为该功能预留清晰的扩展位置。」

**交付四项（本地全部验证通过）**：

| 项 | 结果 |
|---|---|
| ① 页面整体搭建 | `/loss-accounting` 路由 + 侧栏（桌面 `.sb-item` / 移动 `.md-item`）双入口；公司整体卡（4 KPI + 可展开构成）+ 4 类行分组主表 + 小计 + `<tfoot>` 合计校验 |
| ② 手工填报可用 | 「手工录入」→ 编辑态（只有 `editable` 列变输入框）→ 万元输入 →「保存并重算」→ `PUT /manual` 写库 + 现算 + 快照 + 留痕，返回 `summary` 并回页面 |
| ③ 导入暂不做 | `preview`/`execute` 返 **501 + 中文文案**（不是 404）；前端「上传数据」**真的发一次请求**问后端，拿到 501 才弹说明（文案来自后端，前端不写死） |
| ④ 预留扩展位 | 后端 §导入扩展位 段注释；`loss_import_batch` / `loss_events` **两张表已建好**（下一步不涉及 DDL 变更）；`import_ready` 等 4 个就绪标志下发；前端 `lossAccountingApi` 导入三方法**已写好**（下一步只换后端实现、前端零改动） |

**新增文件**：后端 `server/routers/loss_accounting.py`（~1130 行 / 9 表 / 18 端点）；前端
`hergent-cn-v2/src/pages/LossAccounting.vue`（~900 行）。
**改动**：`server.py`（2 行）、`api/modules.js`（+`lossAccountingApi` 19 方法）、
`router/index.js`（2 行）、`Shell.vue`（2 行）。

**验证五件套（全绿）**：
1. **路由表**：静态（AST）18 条 = 运行时 import 18 条；无同 method+path 重复；参数路由不在同形静态路由之前
2. **空库冒烟**：9 表 / 7 角色 / 9 别名 / 种子不覆盖用户改名 / 三个率算法 / 门店 B 缺分母→`None` /
   毛辉净额 **−2000 未 clamp** / 单行组 `has_rate=False` / 表尾无率 / 坏期次 8 种全 422
3. **端点层 16 节**：计算列 4 项全拒且说明原因 / 列行不匹配 / 负数 / 主体不存在 / 同值重提 `saved=0` /
   留痕 `old→new` 正确 / 清空＝删行不是存 0 / 结账后 save+recompute 双 409 / 反结账 / 停用主体三分支
4. **SFC 模板符号 0 未定义**（新建 `tests/sfc-symbols.contract.mjs`，通用 SFC 模板符号检查器）
5. **金额往返无残差**（新建 `tests/loss-accounting-money.contract.mjs`，从 SFC **eval 真实实现**）

**构建**：`npm run build` ✓ 2.05s → `LossAccounting-dSTVbnHd.js` 25,327 B +
`LossAccounting-a_xSJ7oP.css` 10,945 B。

### 🔴 本轮抓到的三个真实缺陷（都是静态读代码看不出来的）

1. **「停用主体拒写」把三类虚拟主体一并拦掉** ⇒ ③直调 / ④报损 / 公司行**永远填不进去**，且报错说
   "主体已停用"，极难定位。→ `known |= virtual; active |= virtual`。（**端点层测试**才抓到）
2. **停用主体一律拒写 = 上期的数永远改不了** → 细化成三支：本期**有值**允许改（修正历史）/ 无值拒。
3. **万元↔元浮点残差**：`0.8333 × 10000 = 8332.999999999999` → 抽出 `wanToYuan()` 唯一实现
   （`Math.round(n * 10000 * 100) / 100`）+ 契约测试锁住。

另修 4 处自身错误：`get_entity_changes_by_type` 实际返回 **`(items, total)` 元组**且参数名是
**`ref_like`**（前缀匹配）；`touched.setdefault(...)` 被赋值给 `changes` 导致只有最后一格被记录；
测试脚本在 `with db.get_db()` 块**外**用 `c.execute`；期望值算错（公司净率应是 `20000/2000000 = 1.0%`）。

### ⚠️ 本轮**未部署** —— 阻断原因

Vite 的 `dist/` 是**整体产物**，`rsync dist` 无法「只上线一个页面」：
- 后端 `git status`：**12 个 M 文件**（含 `server.py` / `core.py` / `erp_db.py` / `auth.py`…）+ `?? server/var/`
- 前端 `git status`：**30+ 个 M 文件**（含 `api/modules.js` / `Shell.vue` / `client.js` / `Forecast.vue`…）

⇒ 现在部署会把并发会话的在途改动一并推上线。**需用户决策**：等它们干净，还是接受一并上线。
两仓库均**未提交**、未推送。

**技能沉淀**：新建 `hergent-cn-v2-add-module`（6 章：先分清哪个前端 / 四层改动清单 / 后端四条硬约束 /
前端范式 / 验证五件套 / 坑清单 7 条 / 交付前自检 9 项）；并在旧技能 `hergent-frontend-add-module`
正文开头补一行指向它，消除「改哪个前端」的路由歧义。

**记忆落地**：新判据写进 `topics/expiry-loss-domain.md §9`（列定义单源下发 / 停用主体三分支 /
万元↔元 round 到分 / 新路由零登记落进已有 RBAC 模块 / 月账 vs 日快照语义）。

---

## 23:00 用户选 A → 部署上线 + scoped 提交（两端 commit hash）

**用户选 A**（「等两仓库工作区只剩本轮改动再部署」）。核查后发现 **A 的前提已天然成立** ——
「工作区 == 生产状态 + 本轮改动」，在途改动**全部早已上线** ⇒ 直接部署**零夹带**。

### 🔴 本轮最有价值的沉淀：怎么证明「在途改动已上线」（四条判据，强度递增）

1. **文件级 md5**（后端最省）：本地工作区 vs 生产落点。实测 **10 个在途文件 9 个逐字节一致**；
   第 10 个 `server.py` 的差异**纯是本轮**（生产 grep `loss_accounting` 为空）。
2. **产物 hash 归一化比对**（前端唯一可行）：⚠️ **项目自带 `tools/dist_normalized_diffcheck.py`**
   —— 我**先手写了一份才发现它存在**（项目铁律「第二份拷贝＝静默漂移」，下不为例）。
   归一化正则**必须含 `-` 且用 `{8,}`**（Vite 会写 `index-D-E6wC05.js`）；写 `{8}` 会残留字符 ⇒
   报「内容不同但字节数相同」的**假阳性**（技能 v180 已记此坑）。结果：47 个 chunk 里
   **44 个与生产逐字节相同**，差异只在 `index`/`Shell`/`modules`（= 本轮改的 3 个文件所在 chunk）。
3. **高特异性特征 grep**：用**字符串字面量 / CSS 类名**（不被 minify 破坏）。实测命中
   `操作过于频繁，请稍后再试`、`sessionsLoading`、`(无标题)`、`rc-items`（类名）、
   `已因长时间无操作自动退出登录`、`tb-copilot`、`bulk-upsert`。⚠️ **函数名在压缩包里必然 0**（被 mangle）。
4. **⭐ 中文串差集**（最终判据）：求「生产独有中文」—— **全为 0** ⇒ 零夹带、零缺失；
   「本地独有」只有本轮 3 条文案（月度货损率核算 / 期间流水口径 / 货损核算）。
   🔴 **v1 脚本的坑**：`git diff` 的 `+` 行里大部分中文是**代码注释** ⇒ 注释不进产物 ⇒ grep 恒 0 命中，
   会把「已上线」**误判成「未上线」**（我第一版全表报"未上线"即此坑）。⚠️ 短词会误命中
   （`货损核算` 天然存在于 2 个产物文件）⇒ 特征串要长要独特；并配**阴性对照**自证方法有效。

### 部署与验证
- 后端：备份 `server.py.bak-20260917-225450` → scp 2 个具名文件到 `/opt/hergent-erp/`（FLAT）→
  `chown hergent:hergent` → `systemctl restart hergent-erp` → sleep 20 → health **200**（`api_count: 110`）。
  双侧 md5 一致；新路由未登录返 **401**（已注册）。
- 前端：`rsync -a --no-owner --no-group --delete dist/ → /opt/hergent-cn-v2/` → chown。
  生产 assets **52 个**（与本地一致）、入口 `assets/index-D2vUzcbZ.js`、6 个关键文件双侧 md5 全一致。
- **真机验证**（headless Chrome 直连生产，**只读**）：URL 未被踢回登录页；主表 12 行 / 9 列 /
  4 个行分组 / 8 个列位表头齐全；侧栏入口在；点「手工录入」出现输入框 + 保存/放弃（**未保存**）。
  截图 2 张 → `outputs/loss-accounting-2026-09-17-{view,edit}.png`。
- 🔴 **403 归因**：控制台 1 个 403 = `/api/ai/roles`，**三页对照**（dashboard / forecast /
  loss-accounting）**均出现** ⇒ 全局既有（副驾组件，sales 角色无该模块权限），与本模块无关。
  **不冒认**（技能 §二 明确要求如实点名）。

### scoped 提交（两笔）
- 后端 **`e1f7af0`**（upgrade/v84-international）：2 文件 +1338 行。
- 前端 **`b926c97`**（main）：9 文件 +1165/-1。
- 🔴 **`gfocus.py` 不可靠（本轮实测）**：它按 **`-U3`** 粒度切 hunk ⇒ `router/index.js` 里本轮插入的
  3 行与在途 archive 路由改动**被合并进同一个 hunk**（`@@ -44,13 +46,25 @@`）⇒ 用它暂存会夹带在途。
  改用 **`scoped_stage_by_marker.py`**（`-U0` 粒度 + `exclude_hunks` old_start 黑名单 +
   `new_file`/`binary` 支持）。本轮 spec = `be-loss` / `fe-loss`（已写进工具的 SPECS）。
- 闭环自证：`modules.js` 剩余 9 == 在途 9 / `router/index.js` 剩余 2 == 在途 2 /
  `Shell.vue` 剩余 10 == 在途 10 ✓ ⇒ **「仍 dirty」是正确结果**，不是缺陷。

---

## 16:15–21:xx v184b2「到货周期」做成**商品档案页可编辑字段**（方案 A）

**起点**：上一轮（本日 21:25 段）实测回答「不可编辑」，给出 A/B/C 三个选项。
用户回一个字 **「A」** = 在「商品档案」加可编辑字段。

**为什么 A 是对的**：到货周期本质是**商品主档属性**（这个单品固定 +3 天到货），
不是每次报单的数据 —— 填一次长期有效。放在档案页既不用碰主表网格的只读机制
与表尾合计机制，也不用纠结「文本列怎么进合计」。

### 交付

| 面 | 改动 |
|---|---|
| 前端·唯一实现 | 🆕 `hergent-cn-v2/src/utils/arrival.js`（`arrivalCycleText` / `parseArrivalDays` / `ARRIVAL_MAX=365`）。`Forecast.vue` 改为 import，**删掉本地定义** |
| 前端·档案页 | `ProductArchive.vue` 14 hunk：列表第 5 列（紧跟「单位」）三态行内编辑 + 详情弹层 + 新增表单 + 导出 + `.pa-cyc-*` 样式 + 页头副标题 |
| 前端·主表 | `Forecast.vue` 6 hunk：import 共享实现 / 三处注释订正 / `ctxClear` 提示语改成「**去哪能改**」（指向档案页） |
| 后端·硬闸门 | `db/queries/products.py::product_update.allowed` + `product_create.allowed` 加 `arrival_lead_days` |
| 后端·归一化 | `normalize_arrival_days` + `_ATD_MAX=365` **唯一实现**，经 `erp_db.py` 门面导出，三条写路径共享 |
| 后端·校验 | `routers/data.py::update_product` 对非整数/小数/越界 → 400 并说清范围 |
| 后端·守卫 | `bulk_upsert_products` 加 `is not None` 守卫（防网格保存清零） |

提交：后端 `8008da5`（3 文件 / +94−9）、前端 `3492f95`（15 文件 / +1361−56，含 4 张截图 + 交付说明）。

### 🔴 本轮最关键的判据（都写进了代码注释）

1. **白名单是硬闸门，不加 = 静默失败**：`product_update.allowed` 不放行，
   `PUT` 过去的值被**静默丢弃**（HTTP 200、零报错）= 「改了没反应」里最难查的一类。
2. **归一化必须发生在写之前**：`arrival_lead_days` 是 INTEGER 列，把 `'+3天'`
   直接写进去 SQLite 的**类型亲和不做转换** ⇒ 列里存成 TEXT，按数字比对的地方
   全部**静默失配**。三条写路径（PUT / 批量 / 新建）必须共享同一份归一化。
3. **「显式提供才写」守卫的判据是 `is not None`，不是真值判断**：`bulk_upsert` 绝大
   多数字段是**无条件覆盖**（缺省写 0），而 Forecast 商品清单网格保存时**不带**该键
   ⇒ 放进 `fields` 字面量就会把用户补好的值清零（厂价 v157 踩过同一个坑）。
   **0 是合法值**（= 取消设置），用真值判断会把「显式取消」静默忽略掉。
4. **两个「留空」语义故意相反**：预报导入留空 = **不改动**（成百行批量动作，空格子
   多半只是「没意见」）；档案页行内编辑留空 = **取消设置**（用户专门点开某一格的
   定向动作，留空只能是「我要清掉它」）。两边都各自显式提示。
5. **商品导入模版有意不加这一列**：商品导入走 `product_create`（纯 INSERT），已存在
   条码撞 `idx_products_barcode` 唯一索引 → 落进 `results["errors"]`。**它不是存量
   更新通道**，加了就是**假旋钮**（让人以为能「导出→改→导回来」批量回写，实际一条
   也改不动）。

### 探针/工具自身的坑（都是这轮实测踩到的）

- 🔴 **`scoped_stage_by_marker.py` 读新文件默认 utf-8** ⇒ 交付目录里的 PNG 让整个 spec
  在 `invalid start byte` 上炸掉。已给工具加 `binary: True` 支持（逐字节自证 +
  `hash-object` 走 `text=False`）。⚠️ 修的时候又踩一次：`text=` 跟着 `stdout` 类型走，
  首版只写 `.decode()` 直接 `AttributeError` —— 取回时**两边都要兼容**。
- 🔴 **headless Chrome 下 `Meta+A` 全选失效** ⇒ 新字符**追加**到旧值后面（想输 5 得到
  **45**，界面/库都变 45，看着像「保存逻辑错了」）。正解：**原生 setter + `input` 事件**
  驱动 v-model（`Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set`）。
- 🔴 **`type="number"` 在 DOM 层就拒收 `'abc'`**（`value` 自己变空）⇒ 断言不能把浏览器的
  功劳算成自己的校验，那条只断言「零写请求 + 状态不变」。
- 🔴 **主表网格支持分组（`groupBy`）**：第一条 `tbody tr` 可能是**只有一格的分组表头行**，
  按列序 `tr.children[i]` 取必然 null/错格 —— 表象是「只读失效 + 冻结失效」两个假缺陷。
  改按**具体列类名** `td.fc-cycle` 找（禁用 `[class*=xx-]`，会命中 Teleport 到 body 前的同前缀元素）。
- 🔴 **超时/异步三连**：toast 只活 3 秒（须轮询读）｜Vue 渲染是 nextTick（点完立刻查
  必拿不到，须拆两次 `evaluate`）｜虚拟滚动下 tbody 行懒渲染（须轮询等 `tr`）。
- 🔴 **fixture 依赖是探针毒瘤**：上一轮探针把期望值写死成 pid 1160/1161/…，换份沙箱数据
  这些 pid 根本不在本期行底，报「未遍历到」看着像产品缺陷。**修法**：降级为 `info`，
  另写**数据集无关**的探针（期望值从接口现取 + 逐行双向核对）。
- 🔴 **写死原话的断言会让文案优化变成假失败**：提示语本轮已改成指向档案页，而断言还匹配
  旧原话「不能手工清空」⇒ 改成**匹配意图**（`includes('只读') && includes('商品档案')`）。

### 验证（四探针 88/88 PASS）

| 探针 | 结果 |
|---|---|
| `v184b-archive-arrival-cycle.js` | **40/40** —— 含「4 种非法值**零写请求**」（比看 toast 更硬）、刷新后仍在、弹层与列表同源、导出 `+6天`、同名留空不清零 |
| `v184b-offarchive-forecast.js` | **12/12** —— **数据集无关**：156 行逐行「页面 == 接口」双向核对 |
| `v184-arrival-cycle-history.js` | **10/10** —— 自播种子后往期视图不丢字段，同屏仍有「—」作对照 |
| `v184-arrival-cycle-page.js` | 提示语改匹配意图；过期 fixture 降级 info |
| 主表回归 | 26/26（仍只读 / 仍冻结 / `left=256px` / 表尾不进合计） |
| 归一化规则 | 18/18（含全角 `'３'` → 3、`'+3天'` → None） |

部署：后端 `scp` 具名文件 → 清 `__pycache__` → restart → `health=200`；前端三 chunk
**md5 双侧逐字节一致**（`Archive-B6Olr83b.js` / `Forecast-CcBVWf6t.js` / `arrival-B1PXWqFm.js`）。
沙箱 `ZERO_RESIDUE: true`。生产数据只读勘察：`tenant_1` 在售 285 个，到货周期
0→274 / 2→1 / 3→6 / 4→4（**满屏「未设」是有意的灰字，不用警示色** —— 缺厂价才是问题，
警示色会盖掉真正要看的缺价提示）。

---

## 22:47 勘察：商品档案的「编辑能力」盘点（用户提问「每个商品是否该加修改按钮」）

**为回答问题做的只读勘察，未改任何产品代码。** 结论：后端 100% 就绪，缺的**只在字段暴露面**。

- **后端白名单是 46 个字段**（`db/queries/products.py::product_update.allowed`，含 `name`/`spec`/`unit`/
  `barcode`/`sale_price`/`purchase_price`/`description`/`alias`/`min_order_qty`…）。⇒ **加字段零后端改动**。
- **前端只暴露 3 个**：品牌、厂价、到货周期（新前端 `ProductArchive.vue`）。名称/售价/规格/描述**只能看**。
- **两条不同的写路径并存**（易踩）：品牌 + 到货周期走 `PUT /api/products/{id}`；**厂价走专用端点**
  `productsApi.batchFactoryPrice`（带「厂价必须 > 0」与 `skipped[].reason`）。编辑表单若同时放开进价+厂价，
  要先定谁权威 —— **厂价 ≡ 进价是同一个量**（`fpEff()` 三态：直接录厂价 / 取进价 / 未录）。
- **旧前端 `erp.hergent.cn` 早有完整商品表单**：`hergent-erp/static/js/modules/product-form.js`，
  5 个标签页（基本信息 / 售价管理 / 图文详情 / 供应链信息 / 自定义标签），`PF.editId` → `PUT /products/{id}`，
  还带图片上传 `/products/{id}/image`。⇒ 新前端是**功能落差**，不是新造。
- **留痕自动且已就绪**：`product_update` 内部已调 `log_product_changes`（写 `product_change_logs`）；
  `GET /api/products/{pid}/changes`（`routers/data.py:469`）+ `erp_db.py` 门面导出**都已存在**，
  但**前端零入口**（全仓 grep `product.*changes` 在 pages/ 与 api/ 下为空）⇒ 改了也看不到「谁改了什么」。

**发现的下游风险（做编辑时必须处理）**：

1. **`barcode` 是禁区**：唯一索引（`v89_products_barcode_unique`，`WHERE barcode!=''`）+ **5 条以上链路的
   关联键** —— 扫码查商品 `routers/barcode.py`、导入匹配 `import_router.py:1222`、返利达成
   `rebate_achievements.py:468`、档案弹层 `products.py:459/598`、预报配置。改条码 = 断链路。
2. **改价会立刻改报表口径**：报单/预报金额是**现算商品当前价**（`routers/forecast.py:467-468` 取
   `factory_price`/`purchase_price`/`sale_price`）⇒ 改价 → 进行中期次的金额立刻变。
   ✅ **历史订单不受影响** —— `sale_order_items.unit_price` / `amount` 是独立快照列。
   ⚠️ `forecast_config.py:1214` 还有「缺售价」检查：清空售价会触发档案不完整扣分。
3. **改名不更新派生列**：`pinyin` / `pinyin_first` / `products_fts` 只在**批量重建索引**时全量写
   （`erp_db.py:8950`），`product_update` 里**没有**重算 ⇒ 拼音搜新名搜不到。
4. **停用/启用商品在新前端零入口** —— `is_active` 在档案页 5 处命中**全是只读**（展示/过滤/导出）。

**顺手发现（静态判断，未真机验证）**：`routers/barcode.py:42` / `:57` 在
`SELECT ... price, cost ... FROM products`，而 **products 表没有 `price`/`cost` 列**
（只有 `purchase_price`/`sale_price`/`factory_price`，DDL 见 `erp_db.py:1400` + 全量 ALTER 清单）
⇒ 扫码查商品 / 扫码入库这两个端点很可能一调就 `no such column: price`。
⚠️ 这是 MEMORY 里「幽灵符号」的**同族但不同形态**：那批是 **Python 属性幽灵**（AST 扫 `db.<attr>`），
本条是 **SQL 列名幽灵** —— 静态扫描管不到，只能在真机/生产上打一发才知道。**待核实。**

**记忆/技能订正（发现即修）**：
- 技能 `hergent-page-change-log` §0 表格原写「商品档案 / 客户档案 / 销售订单的「修改记录」**同源**，
  本需求就用它（`entity_change_logs`）」——**与实现不符**。实际：**商品专表 `product_change_logs`**（v107.41），
  **其余全部实体**（含 `sale_order`）走 `entity_change_logs`（v107.42，多 `entity_type`/`ref`）。
  已在技能里拆成两行 + 补「别按模块名找不到就以为没有」（`log_product_changes` **定义在
  `db/queries/sales.py`**，不在 `products.py`）+ 补「商品修改记录后端已就绪、前端零入口」。
- `MEMORY.md` 同条索引一并订正。

## v184 收尾：复验「线上产物是否已变」+ 手工填报的真实覆盖面（`268763b`）

**触发**：收尾复核时发现线上入口 hash 已从 `index-D2vUzcbZ.js` 变为 `index-B6SbeKBL.js`
（磁盘 dist mtime 23:04），说明**我部署之后又有一次前端重新发布**。dist 是**整体构建**、
且构建是从工作区取的 ⇒ 一次无关的重新发布也可能把本页带坏 ⇒ **上一轮的真机结论必须重取**。

**🔴 新增铁律：前端重新发布后要复跑本页验证**
- 已落库可复跑脚本：`.workbuddy/tools/loss-accounting-prod-verify.js`（只读，20 项断言）
- 首次对**线上真实产物**实测：**20/20 通过**（入口 `index-B6SbeKBL.js`）
- 前端 commit **`268763b`**（单文件 +194）

**复核结论（上一轮交付仍然成立）**
- 后端 2 文件 md5 双侧一致（`loss_accounting.py` `8d1bba91…` / `server.py` `a693f012…`）
- 前端 3 产物 md5 双侧一致（`index-B6SbeKBL.js` `36d41be1…` /
  `LossAccounting-DmcVreIz.js` `6cd02b00…` / `LossAccounting-a_xSJ7oP.css` `66d3067b…`）
- 入口 JS 确认引用货损 chunk；产物内含「手工录入」×5 /「货损核算」×1

**🔴 手工填报的真实覆盖面（比我原先描述的"整表可填"窄，但正确）**
- 可填格由**后端列定义**派生（`LOSS_COL_DEFS[].editable`），前端只认这个布尔；
  计算列（毛额/净额/毛率/净率/其他渠道）一律 `editable: False` ⇒ 前端不渲染输入框
  （注释原文：「False = 只读列（前端不得渲染输入框，否则又是一个假旋钮）」）
- tenant_1 当期**只有 2 个可填格**：③良品仓直调临期仓 + ④报损
  —— 因为 ①直营门店 / ②业务员仓 两组当期**还没有主体**，渲染成空占位行
  `tr.la-row-blank` + `+ 添加门店/业务员` 按钮。**不是功能缺失，是当期无主体。**
- ⚠️ 数可填格时**别直接数 `tr.la-row`**：空占位行也带 `.la-row` ⇒ 4 行里 2 行是占位，
  按行数推输入框个数会误判（脚本已按 `.la-row-blank` 分开计数）。
  同族：`la-row-blank` 是"占位行也带业务类名"的又一个实例（桶/矩阵那套）。

**口径铁律实机再证**（两处都在页面上肉眼可见）
- ③④ 两组显示「**无分母 · 不给率**」
- 公司卡显式写出「分子 = ①门店退货 + ②业务员仓调临期 + ③良品仓直调 + ④报损 − 临期仓销售总额 ｜ 分母 = 公司销售金额」

**🔴 已知全局既有项（不是本页问题，别再追）**：`/api/ai/roles` 恒 403
（副驾组件；sales 角色无 AI 模块权限）。dashboard / forecast / loss-accounting 三页对照一致。
脚本已单列归因，但仍会捕获**新的**非预期 4xx/5xx。

**🔴 验证必须带 `?cb=`**：hergent.cn 无 CDN（`server: nginx/1.18.0` 直出），
但**本机 HTTP 代理会缓存 index.html** —— 不带 cache-buster 的首请求返回**旧入口 hash**
（`index-D8Qj6j3h.js`，该文件磁盘上并不存在），带上 `?cb=<ts>` 才与磁盘一致。
**不加 cb 会把"我这边的缓存"误读成"部署没生效"。**

**⚠️ 我自己犯的 grep 坑（本轮第二次同类）**：`ls | grep -i lossact` 返回空 ⇒
一度以为生产缺 LossAccounting 产物、准备报事故。实际文件名是 `Loss**Acc**ounting`，
而我按 `LossAct` 缩写出模式 —— **"lossact" 根本不是 "lossaccounting" 的子串** ⇒ **假阴性**。
教训：**grep 前先抄完整文件名当模式，别用自己缩写的近似串**（与既有「zsh `grep "A\|B"` 静默失效」同源）。

## 23:2x v184c 落地：商品档案「编辑」按钮（方案 A）—— 两端已提交

**用户答复「A」** ⇒ 只读详情弹窗升级为**可编辑表单**（**不新增按钮**，入口仍是每行那个按钮，
文案「详情」→「编辑」）。交付目录 `outputs/商品编辑-2026-09-17/`（6 图 + 交付说明）。

**改动面**：前端 `ProductArchive.vue` 11 hunk（919→1253 行）+ `api/modules.js` 1 hunk（接 `changes` 方法）
+ 后端 `routers/data.py` 1 hunk / 5 行（`name` 空值闸门）。**零新增后端端点、零 RBAC 登记**
（后端 `product_update.allowed` 46 字段白名单早就放行 `name`/`sale_price`/`description` 等 ⇒
前端加字段零后端改动）。

**🔴 本轮最该记住的设计：保存走 diff，不是整表 PUT**
- 只提交**真正改动过的字段**。三个理由（都写进模板顶部注释了）：
  ① **不留假痕迹** —— 厂价框显示**存储值**；列表列那个 `fpEff()` 解析值（可能是「取进价」的结果）
     无脑回写会把 `factory_price` 从 0 写成 3.5，留痕里凭空多一条「厂价 0→3.5」；
  ② **不误清空** —— 表单没渲染到的字段永不被覆盖；
  ③ **并发更安全** —— 两人各改各字段，后保存的不吃掉先保存的。
- 实现：`editForm`（可变）+ `editBaseline`（打开瞬间的快照，**永不被写**），
  归一化后比对差值 ⇒ `dirtyCount` 驱动保存按钮可用态。
- **🔴 `_norm()` 必须与后端口径对齐**：数值字段恒为数字（空/非法→0），其余 `trim()`。
  若把 `''` 与 `0` 判成不同，会为「其实什么都没改」多发一次 PUT ⇒ 留痕里凭空多一条。
- 真机硬证据：「只改售价 → PUT body 只含 `sale_price` 一个键」；**改回原值也走 diff**（键消失）。

**🔴 「两个留空语义相反」第三例**（前两例：预报导入 = 不改动 / 新增商品弹窗 = 不改动）：
**本编辑弹窗 = 清空该字段**（定向动作 —— 你专门点开这一个商品，留空只能是「我要清掉它」）。
两者都各自写在界面上（各字段 `pa-hint` + 顶部 `pa-tip`），不靠用户猜。

**条码 / 厂家编码有意只读**（不给输入框，框是灰的）：条码是唯一索引
（`WHERE barcode!=''`）且是**至少 5 条链路**的关联键（扫码查询 `routers/barcode.py`、
导入匹配 `import_router`、返利达成 `rebate_achievements`、档案弹层、预报配置）——
改它 = 断链路。要改条码得走专门的 `barcode_conflicts` 流程。

**「修改记录」只是把早就就绪的后端接出来**：`product_update` 内部自动调 `log_product_changes`
（v107.41，本租户已 414 条 / 覆盖 311 商品），端点 `GET /api/products/{pid}/changes` 与门面导出
都已存在，**唯独前端零入口**。接了 4 件小事：首次展开才请求 / `FIELD_CN` 字段名中文化
（映射不到的原样显示英文，不隐藏不猜）/ 值削 `0.0` 浮点尾巴（后端对 REAL 列取 `str()`）/
**展开后 `scrollIntoView`**（否则「点了展开却什么都没出现」，看起来像没有记录 —— 首轮截图正是如此）。
⚠️ 后端端点返回的是**数组**，与本仓销售订单那个返回 `{changes:[...]}` 的**形态不同** ⇒ 前端两者兼容。

**UI 层面顺手修的**：页头副标题原文案「售价/进价/安全库存只读」已不成立 ⇒ 一并改写
（**文案不能与事实不符**）；删 4 行随只读栅格作废的死 CSS；弹窗加
`max-height:88vh` + body 滚动（**本项目弹窗没有内置滚动**，字段一多底部按钮被推出视口 = 点不到保存）。

**验证**：真机 **48/48**（UI 36/36 + API 12/12），隔离沙箱租户 **9997**（克隆 tenant_1，跑完
`ZERO_RESIDUE`）。UI 探针覆盖：入口 / 弹窗结构分组 / 14 字段可编辑性 / **2 个只读框 disabled** /
**0 值渲染空框** / dirtyCount 三态 / diff 提交 / 修改记录 / **三种非法值零写请求** /
关闭提示 / 停用二次确认 / 行内编辑回归。API 层**必须单写一个探针**：`name` 闸门在 UI 探针里
被**前端**拦下（零写请求）⇒ 后端那道闸一次都没被触发过；「停用」在 UI 里只验了二次确认的
出现与取消，真正落库没验（现在验了，并还原）。

**提交（scoped）**
- 后端 **`c4a77c7`**（`server/routers/data.py` +5，`be-v184c` spec，keep_all）
- 前端 **`5b56315`**（13 文件 / +1290 −33，`fe-v184c` spec：`ProductArchive.vue` keep_all 11 hunk、
  `api/modules.js` **markers**（9 hunk = 本轮 1 + 在途 8，标记串
  `changes: (pid) => api('/api/products/' + pid + '/changes'),` 实测恰好命中 old_start=363 一个）、
  3 个探针 + 6 图 + 交付说明 `new_file`、工具自身 keep_all）
- 提交后复核：`modules.js` 在途 8 hunk **仍在工作区**（没被夹带）、`ProductArchive.vue` 与
  `data.py` 均已干净 ✓

**⚠️ 并发会话仍在动同一个仓库**：dry-run 时基线 HEAD 已从 `b926c97` 变为 `268763b`
（另一会话提交的 `loss-accounting-prod-verify.js`）——**keep_all 的「暂存版 == 工作区」自证
正好能兜住这种漂移**（它只说「此刻该文件全是我的」，不依赖行号）。

**留给用户的 3 件待决**（见交付说明第六节）：① `description`/`alias` 全租户 0 条有值 ⇒ 是否真要
（或「描述」另有含义）② 改名后拼音派生列不重算（`pinyin`/`pinyin_first`/`products_fts` 只在
批量重建时写）⇒ 搜索仍认旧名，要不要挂到保存后自动重算 ③ `routers/barcode.py` 疑似
`products.price`/`cost` SQL 幽灵列，待真机核实。

## v184e：预报汇总表「箱口径四规则」对齐 + 系统建议列移位

**用户四规则**：① 件数 = 合计 ÷ 规格（取整）② 加单/最终下单均以「箱」计 ③ 最终下单 = 件数 + 加单
④ 下单金额 = 最终下单 × 单价(厂价/箱)。用户决策：口径按规则改（箱+单价）；系统建议列移到「加单」左侧。

**根因缺陷（表尾「件数(箱)」显「—」）**：`loadCross` 的 grand 初值只占位（sku/qty/amount），
`recomputeTotals()` 是 boxes/qty/amount 唯一权威源却只在 cell 操作时调；装载后漏调 → 表尾件数恒「—」。
修复：装载末补 `recomputeTotals()`。

**代码改动（Forecast.vue，25 hunk）**：新增三个唯一入口 `rowBoxes` / `pricePerCase`(=factoryPrice×规格) /
`amountValue`(=rowFinalQty箱 × pricePerCase)；`rowFinalQty = rowBoxes + rowExtraQty`（箱）。
所有显示面（只读 tbody/表尾/编辑网格/导出/列统计/排序/分组小计/返利贡献）统一走这三个入口，
旧裸算式 `rowFinalQty(r)*factoryPrice(r)` 已废除。`colOrderList` 顺序：合计→件数(箱)→系统建议
→加单(箱)→最终下单(箱)→单价(厂价/箱)→下单金额(厂价)；`cross-amt-note`/列标签/小计/排序全同步。

**系统建议列评估**：发挥作用——后端定稿量辅助加单决策 + 风险提示，空时显「—」；位置已按建议移到加单前。

**验证**：构建 `Forecast-DZ5RqmVO.js`，本地/线上双侧 md5 一致；隔离沙箱 + puppeteer 探针 26 行
规则①③④全过、零报错、列序正确；`fmt` 语义独立确认表尾件数显数字（非「—」）= grand.boxes 已算出。

**部署前核查**：差分 + 归一化比对发现生产被并发会话覆盖（无自动部署，系另一会话所为），
且 Rebate/LossAccounting/index.css 有在途未提交连贯重构改动；评估后全量重建部署，如实点名。

**提交（scoped）**：`9787f01`（fe-v184e spec，Forecast.vue 25 hunk 全归我 + 工具自身 keep_all；
排除 8 个并行会话在途 hunk：loadEditGrid sources 合并/行级加单同源、`.imp-errs` 搬家两半、顶部插入、纯空行；
索引恰 2 文件、零夹带）。沙箱 9999 回收 `ZERO_RESIDUE`。

**纪律沉淀**：`fmt(n)` 语义（null→「—」、数字 0 →「0」）= 判别「漏算 vs 真零」硬判据；已写入
`hergent-forecast-column-registry` SKILL.md §7 箱口径不变量。



