- v107.132: 移除销售订单列表统计卡片+页面标题 — 用户反馈红框区域多余(销售/车销/自提/退单 4 类 tab 覆盖)
  · render_sales 删除 var stats stat-grid 3 卡 + c.innerHTML 内 page-hd '销售管理' + +stats+ 引用
  · 保留合计行(上一轮 P3 加的)
  · commit `1953d0a`, 部署 main-pdaQCeS9.js; hasPageHd=false, statCardCount=0, 合计行正常
- v107.133: 销售列表功能区两行紧凑布局 — commit `cb30d4a`, 部署 main-Cc1ugXyc.js; 第1行 tabs+操作按钮(margin/padding/gap 减小), 第2行筛选输入(去 bg2 卡框+加 dashed 分隔); 垂直压缩约 30-40px。
- v107.134: 工具栏 P0(对齐舟谱) — commit `65a31e6`, 部署 main-BR_jWJjg.js:
  · 主按钮 +icon 突出 / 顶部打印按钮 printSalesList(打印当前筛选列表, 原 printDeliveryNote 是残废版 tbody 空) / 日期快捷 chip 今日近7天近30天(applyQuickDate) / window._salesList 存筛选结果
  · **★作用域教训**: applyQuickDate 误引用 _showOrderForm 函数内局部变量 _p2d(行5746 var 声明在函数体内) → 全局函数调用 ReferenceError 中断无报错表象; 已改本地 pad。**写全局函数时注意 app.js 的 IIFE/函数局部变量边界**
- v107.135: 工具栏 P1 — commit `cbaac19`, 部署 main-Dp4h4gJ0.js:
  · 高级筛选▼(金额起/止, _salesQ.amtMin/amtMax, 有值显示●) / 多空格关键字(OR 分词, 对齐舟谱多单号) / 批量操作独立按钮(toggleBatchMode 展开 batchBar)
  · 验证: 金额>=500 过滤生效; 工具栏 7 按钮齐
- 工具栏整体进度: P0(主按钮icon/打印/日期快捷)+P1(高级筛选/多空格/批量) 完成; P2(TOOLBAR_CONFIG 模板抽离跨模块)待做
- v107.136: 复刻舟谱工具栏 — commit `2ad4d11`, 部署 main-DaYyROpF.js:
  · filterBar 第1行(舟谱单行布局): 单据日期select(今日/近7天/近30天) + 起止 + 单号 + 客户 + 商品 + 重置 + 更多选项▼ + 右侧[查询蓝主+search icon]
  · 第2行展开(更多选项): 业务员select / 部门input / 仓库select / 状态6选1 / 金额起止
  · _salesQ 扩展 8 键(orderNo/customerName/productName/operator/dept/warehouse/status/d0/d1/amtMin/amtMax/kw)
  · 商品筛选待后端联(订单需 JOIN items 才能按商品名过滤); 部门字段无独立字典暂用 input
- v107.137: 创建订单按钮弹窗→引擎全屏页 — commit `b7aa6fa`, 部署 main-Dg5707Iq.js:
  · 12 处 showSaleForm() 调用(工具栏/空状态/菜单/dashboard) → showVisitSaleForm()
  · **★replace_all 误伤教训**: `showSaleForm()` pattern 匹配到 `function showSaleForm(){` 定义行(函数名后跟`(`时), 把定义行也改名 → 函数声明提升覆盖引擎版入口。必须恢复函数名 + 文件末尾覆盖。
  · 6 个旧弹窗增强器(智能补全/草稿/条码/移动按钮/进度/历史, 2700-3050)随弹窗版退役(预期)
  · 验证: 点击新增销售单 → vsfOverlay 全屏"创建访销订单", 无弹窗; modalOverlay 是更新日志欢迎弹窗与按钮无关
- v107.138: 采销其它订单类型入口统一引擎全屏版 — commit `1a57b2c`, 部署 main-g41plBES.js:
  · **★IIFE 作用域根因**: app.js 是 IIFE, onclick 内联属性走 window.*(6121-6129 引擎版✓),
    但 IIFE **内部直接引用函数名**(如 4064 商品快采 typeof showPurchaseForm==="function")
    解析到 IIFE 局部同名函数声明(弹窗版) → 弹窗。文件末尾赋值覆盖局部标识符即可统一。
  · 覆盖 7 个: showVehicleSaleForm/VehicleReturn/Pickup/PickupReturn/Purchase/PurchaseReturn/TransferOrderForm = _vsfOpen(config)
  · 验证: 采购/退货列表按钮全屏 ✓
- v107.139: 访销退单按钮打开列表页 — commit `8a934b8`, 前后端部署(main-CAWuGYh_.js + erp_db.py 重启):
  · **根因1**: mega-sale-return 导航(4442) render_sales 后又 setTimeout(showReturnForm) 弹创建表单
  · **根因2(数据层)**: return_order_list 原查 sale_orders.status=returned(空表!) — 退货单真实存 return_orders 独立表(return_order_create 写入), 但无列表接口
  · 修复: 后端 return_order_list 改查 return_orders(JION 原单号+客户名); 前端新增 renderReturnOrders(); 导航接入
  · **教训**: 退货单列表一直没实现 — 创建(return-orders POST)与列表(GET 查错表)链路断裂; 功能验收时要验证数据真实存储位置
- v107.140: 访销订单入口被车销列表覆盖 — commit `7ae3050`, 部署 main-DgtySswf.js:
  · **根因**: navTo mega-sale-order(4441) 未重置 window._tradeTab; render_sales 按 _tradeTab 类型过滤, 先前访问车销/自提后状态残留 → 点访销显示车销列表
  · 修复: 4441 重置 _tradeTab='' + _salesFilter='all' + _salesQ 清空 + render_sales('all')
  · **★跨页导航状态教训**: 共享全局状态(_tradeTab/_salesFilter/_salesQ)的入口必须显式重置, 否则上一次访问的筛选/类型残留串页
- v107.141: 新建按钮文案统一"新增+类型名" — commit `7a9139b`, 部署 main-BKi5UK0M.js; 6 处按钮文案(访销订单/车销订单/自提订单/访销退单/采购退货单/空状态)。
- v107.142: 车销列表空状态按钮误显示'新增访销订单' — commit `d23aa28`, 部署 main-C8RUwykS.js:
  · saleRows emptyState 按钮文案写死'访销订单', 车销/自提列表无数据时显示错文案
  · 修复: 空状态按钮按 window._tradeTab 动态化(5 类型映射+默认); isVehicle 隐藏重复'+ 车销'按钮
- v107.143: 采购订单/退单列表对齐访销列表格式 — commit `0115efa`, 部署 main-DB1laCr7.js:
  · render_buying 重写: 删 page-hd/统计卡, 加筛选区(供应商/单号 OR 分词+日期快捷 applyBuyingQuickDate)+合计行+紧凑 tabs
  · poRows 状态 7 映射+色块; 采购退单(tab=return)同格式
  · 至此 销售/车销/自提/采购/退单 全部列表 UI 统一
- v107.144: 采购退单空状态按钮误显示'新增采购单' — commit `8c66afe`, 部署 main-DxOlNWT1.js; poRows emptyState 按 _buyingTab 动态化(标题/副文案/按钮/onclick)。
- v107.145: 加强 _vsfClose 健壮性(覆盖所有 9 个引擎订单) — commit `4ecdd30`, 部署 main-BcV_KyAx.js:
  · 根因: _vsfClose 调 _origNavTo(next) 抛 ReferenceError → 中断 renderTabBar/render_sales → tab 残留"无法关闭"
  · 修复: try/catch 包裹每步 + 兜底 navTo + 清 _vsfActiveCfg
  · **教训**: IIFE 局部 var 跨函数调用要 try/catch 隔离, 防止单步异常中断后续清理
- v107.146: 采销管理专业ERP审核清单全量落地 — commit `aad2bfc`, 前后端部署:
  · P0: 调拨多行明细表+多行过账 / 采购 expected_date 落库 / 采购计划转订单端点
  · P1: 销售补 customer_po_no+promise_date+tax_rate(迁移+引擎字段) / 信用管控(credit_limit+逾期) / AI 审核+执行端点(幂等+审计)
  · P2: batch_id 批次回写 / AI 补货+价格+交期端点 / 发货生成出库单(pick_wave)+列表
  · **★5 个部署坑**: ①迁移必须放 _ensure_tenant_module_tables 租户循环内(仅 master 不生效, 已踩) ②customer_prices 无 id 列(联合主键, 别 ORDER BY id) ③products 售价/进价列是 sale_price/purchase_price(非 price/cost_price) ④新 API 路径要加 server.py 权限映射表 ⑤ALTER 多语句用 execute 只执行第一条, 需拆分逐条
  · 验证: 迁移 6 列+3 表生效; 6 端点全通
- v107.147: 清理残留占位文本 — 用户截图 dashboard 顶部'login'小字: 全源码+生产 bundle grep 验证无残留(浏览器固执缓存旧版本导致); 加注释触发 bundle 重生成; 用户需硬刷新 Cmd+Shift+R

## 仓配管理缺失功能全量开发 (v108) — 前端+后端+部署+验证 全完成
- **临期预警表 (expiry-report.js 新建)**: showExpiryReport() 全屏弹层, 调 /api/expiry/forecast; 红(≤30天)/黄(31-60天) 统计卡+明细表(产品/仓库/批次/效期/数量); operations-hub 入口由 navTo('expiry') 改为 showExpiryReport()。
- **WMS 控制台 (wms-console.js 新建, 覆盖旧 stub)**: showWmsConsole() 全屏弹层, 复用既有端点 /api/wms/replenishment/needed + /api/wms/cross-dock(自动加载) + /api/wms/putaway-strategy(product_id) + /api/wms/pick-path(wave_id); operations-hub 入口由 dev:true 改为 showWmsConsole()。
- **库存锁定 expires_at 落地**: lock_stock 加 expires_at 列(INSERT+返回); stock_lock.py Val0 加 expires_at 透传; stock-lock.js 加 datetime-local 输入 + 修正到期判断(用 _slDt 把 SQLite 'YYYY-MM-DD HH:MM:SS' 转 Date 比较, 旧代码直接比 ISO 字符串恒 false)。
- **条码操作后端修复 (barcode.py + erp_db.py)**: db.batch_in 参数顺序修正 (product_id,warehouse_id,quantity,cost); db.batch_out_fefo 修正 (product_id,warehouse_id,quantity); 新增 barcode_scan_log/barcode_scan_log_list 函数 + v108_barcode_scan_logs 表(master 迁移 + 租户 backfill)。
- **★生产踩坑: 遗留脏 schema**: 服务器上 barcode_scan_logs 表已存在且是旧 schema(scan_type/ref_type/ref_id), 导致我的 CREATE TABLE IF NOT EXISTS 不生效 + INSERT 报 no such column: direction。已 DROP+重建为 direction/batch_no/cost(两库均验证 insert/select 通过); 并在 erp_db.py 的 v108 迁移+租户 backfill 加 self-heal(检测到 scan_type 即重建), 防复发。
- **side-effect 修复**: main.js 重复 import wms-console.js(行55+行78) → 删行55 重复; 去重改动(本会话更早): dunning/expiry-dashboard/profit-report/merge-orders/payment-import/payment-run/print-templates/smart-import/tax-filing/tax-manager/voucher-templates/gl-dimensions 共 ~11 入口 ModuleRegistry.register 注释掉(去重)。navTo 对未注册模块静默 return 不崩, 安全。
- **部署**: 前端 vite build → main-BjcTE3GO.js + main-DWmeGzR9.css; rsync dist/ → 47.113.224.140:/opt/hergent-erp/static/(保留 avatars)。后端 scp erp_db.py+barcode.py+inventory.py+stock_lock.py → /opt/hergent-erp/server(/routers/); chown hergent + rm __pycache__ + systemctl restart hergent-erp。
- **验证**: /api/health=200; /api/barcode/scan-in、/api/expiry/forecast、/api/wms/*、/api/stock-locks 均返回 401(路由通+鉴权生效, 非404); 两库 barcode_scan_logs 列=direction/batch_no/cost; 生产 index.html 引用新 hash。本地改动未 commit(含去重+仓配, 待用户确认后提交)。
