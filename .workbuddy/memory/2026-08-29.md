# 2026-08-29

- **商品档案筛选布局修复**：ProductArchive.vue 的 `.pa-filters` 原 `flex-wrap:wrap` + 关键词 `flex:1;min-width:220px` 导致关键词/品牌/分类挤成三行。改为 `flex-wrap:nowrap` + 关键词 `flex:1 1 200px`(min 150) + 品牌/分类 select `flex:0 0 140px` + 含停用/重置 `flex:0 0 auto`，窄屏 `overflow-x:auto` 横向滚动而非换行。已 `npm run build` + rsync 至 `/opt/hergent-cn-v2`。

- **商品档案新增/导入/导出（方案A）**：
  - 前端 ProductArchive.vue 头部加 `+新增 / 导入 / 导出` 按钮（与商品数统计并排，筛选行保持单行）。
  - 新增弹窗：名称*/条码/规格/单位/品牌(datalist复用)/分类/进价/售价/安全库存/保质期，走 `productsApi.bulkUpsert([row])`（source=web_grid）。
  - 导入弹窗：复用 `importApi`（templateFile 下载 xlsx 模板 + preview 自动映射 + execute，category=products），后端 import_router 早已完整支持 products（含条码冲突预检/重复检测）。
  - 导出：用已装 `xlsx@0.18.5` 生成当前筛选结果 `.xlsx`（code-split 成独立分包，不拖主包）。
  - 后端 data.py `bulk_upsert` 加防护：仅当 r 显式传 `category` 键才写入，避免 Forecast 网格不传 category 时清空已有商品分类。
  - 构建部署：前端 build+rsync；后端 `bash deploy.sh`。真实验证：demo token 下 bulkUpsert 带 category 写入成功、读回 category=测试分类Z，已停用清理测试商品(PID=17)。

- **Shell.vue 三处布局整合**（用户明确指令）：
  - ① 删侧边栏上方「上午好」问候语（`.aibar-greet` 及 `greeting` computed 整段移除）。
  - ② 用户名(张俊峰)与退出登录整合：左下角侧栏 `.sb-foot` 退出登录 + 移动端「更多」抽屉退出登录 一并删除，登录态入口统一到右上角 `.tb-user` 下拉菜单（含修改资料 + 退出登录）。仅保留 1 处退出登录。
  - ③ 天气 + 问AI副驾整合进同一行：aibar 左侧加「Hergent AI 副驾」品牌标识（图标+文字），右侧 group 天气组件 + 问AI副驾按钮。移动端隐藏品牌标签（顶栏已品牌化）。
  - 清理死代码：`.sb-foot` CSS 规则、`computed` 导入。
  - 构建部署：前端 build + rsync 至 /opt/hergent-cn-v2；构建产物核对（上午好=0、Hergent AI 副驾=1、sb-foot=0、, 退出登录=1）。

- **Shell.vue 头部两行合并（腾头部空间）**：用户希望把「品牌图标那一行(topbar)」与「天气那一行(aibar)」整合为一行。做法：删除独立 `.aibar` 区块及其重复的「Hergent AI 副驾」标签，把天气组件 + 「问AI副驾 ⌘K」按钮移入顶栏形成 `tb-ai` 中间组（顶栏变为 品牌 / tb-ai / 用户 三段式）。CSS 删除 `.aibar*` 规则、改 `.tb-ai`/`.tb-copilot`；移动端 hidden `.tb-sub`(AI经营副驾副标签) 与 `.tb-cp-k`(⌘K提示) 防溢出；`.wx` 移动端仍隐藏天气。已 build+rsync；产物核对（aibar=0、tb-ai=1、tb-copilot=1、问AI副驾=1、wx-now=1）。

- **全面删除页面内重复侧边栏标题（腾页面空间）**：用户指出预报页 `h2 预报订货管理` + `page-sub 报单汇总 · 智能审核大脑` 与侧边栏标签重复、浪费一行。全面排查 8 个侧边栏功能板块，确认其中 7 个页面内部 `<h2>` 重复了侧边栏同名标题，统一删除其 `page-hd`（h2 + page-sub）：Workbench(经营工作台)、Forecast(预报订货管理)、Rebate(目标与返利政策)、Dashboard(数据看板)、Archive(档案管理，保留其 module-tabs)、ConnectCenter(能力中心)、Settings(设置)。保留项：子页面（员工/客户/品牌/商品档案、各工作流、报单配置、库存效期补录）h2≠侧边栏标签，属上下文子标题，不删；`CronJobs`(定时任务) 经核查无 h2 重复，不改。已 build+rsync 至 /opt/hergent-cn-v2；产物核对：精确标语「报单汇总 · 智能审核大脑」=0（已删）；各页面分块自身 h2 标题=0；`目标与返利政策` 仅存 index 路由 meta（文档标签名，不在页内渲染，合法）；Archive 分块残留的「能力中心/目标与返利政策」来自子组件按钮文案（去能力中心/品牌说明），非标题。

- **本期预报页(Forecast.vue)功能精简——方案①全做（用户「做」确认）**：
  - 顶部工具栏精简：删 `余额&付款`(openPay)、`返利测算`(openRebate)（收进「提交闭环」向导3步）、`下游闭环向导`(改名`提交闭环`保留入口)、`查看身份`(role-switch 常驻label)、`view-tabs`(列表/交叉切换)、列表专属`智能审核`/`保存草稿`。保留6核心：导入Excel / 编辑 / 保存调整 / AI审核 / 生成舟谱模板 / 复制全部下单 / 提交闭环 + 状态pill(已确认·草稿待经理确认)。
  - 双视图合并：列表(草稿填报`draft`并行录入路径)与交叉(导入汇总)切换移入内容区，改名为干净分段「导入汇总 / 草稿填报」（`switchView` 不变，仅 UI 位置与文案）。
  - 草稿面板头部迁入「智能审核 / 保存草稿」按钮（原工具栏删除项），保留 `draft`/`addToDraft`/`runAudit`/`saveDraft` 工作流。
  - 4 个平铺分组(数据质量/智能预测/协同闭环/更多工具含20+低频项) → 编辑态下 1 个「高级工具 ▾」折叠抽屉（`advToolsOpen` 状态），展开后再展开分组面板。
  - 样式：删 `.view-tabs` 改 `.view-seg`/`.ph-actions`；`.tbl-toolbar` 弱化为次级浅色条。
  - 向导弹窗标题「下游闭环向导」→「提交闭环」与工具栏按钮统一。
  - 构建部署：前端 `npm run build` + rsync `--delete` 至 `/opt/hergent-cn-v2/` + chown hergent；新产物 Forecast-FzsrTylM.js。产物核对：view-tabs=0、查看身份=0、余额&付款=0、下游闭环向导(标题)=0、提交闭环=2；view-seg/ph-actions/高级工具/导入汇总/草稿填报/报单草稿/智能审核(3)/保存草稿 均存在。
  - ⚠️ 未做浏览器真机验证（沙箱代理连不上 hergent.cn），请用户强刷(硬刷新)确认：①顶部按钮压减为6核心+提交闭环+状态pill；②内容区出现「导入汇总/草稿填报」分段；③编辑态出现「高级工具▾」折叠抽屉；④草稿面板内有「智能审核/保存草稿」按钮。

- **表内工具栏整合进主工具栏（用户「表内工具栏，能否整合到主工具栏」）**：
  - 诊断：上一轮「高级工具」抽屉写在 `v-else-if="!editMode"` 区块内却带 `v-if="editMode"`，实际永不渲染（隐性 bug）；且只读/编辑两模式各有一整条 `.tbl-toolbar`（搜索+分组/冻结/单价口径/显示AI建议+编辑动作+缩放），形成"第二工具栏"。
  - 主工具栏(`card.toolbar` 的 tb-right)整合：① 搜索框上移为内联 `tb-search`（单例 id=gridFind，原两处重复已去）；② 只读视图的网格显示选项(分组/冻结列/单价口径/显示AI建议)收进「表格设置 ⚙」弹层(`tbSettingsOpen`，v-if=!editMode)；③ 编辑态动作(生成建议/审批/推送审批/打印PDF)+4分组收进「高级工具 ▾」抽屉(`advToolsOpen`，v-if=editMode)；④ 状态pill保留。
  - 删除两处 `.tbl-toolbar` 块，仅保留活动筛选 chips 行(`.filter-row`，顺手去掉重复 colFilterSet)；缩放控件迁为全屏网格内 `.zoom-group`(is-fs 时浮右上角)。
  - 遮罩 `.pop-overlay` 与面板可见性对齐：`(tbSettingsOpen && !editMode) || (advToolsOpen && editMode)`，避免编辑态下孤儿遮罩拦截点击。
  - 样式：`.tb-search`/`.tb-pop`/`.tb-pop-panel`(z-index 1101)/`.pop-overlay`(z-index 1100，复用 col-menu 层叠方案，`.card` 无 overflow:hidden 不裁切)/`.filter-row`/全屏缩放定位。
  - 构建部署：build+rsync+chown；产物核对 tbl-toolbar(JS)=0、表格设置=1、高级工具=1、gridFind=2(1 id+1 getElementById，无重复输入框)。
  - ⚠️ 未做浏览器真机验证；请强刷确认：①主工具栏出现搜索框+「表格设置⚙」(只读)/「高级工具▾」(编辑)；②网格上方仅剩活动筛选 chips（无整条工具栏）；③点「表格设置/高级工具」弹出下拉，点外部遮罩关闭；④全屏时右上角出现缩放条。

- **预报页全屏/缩放控件重叠修复**：原全屏按钮 `.grid-fs-btn`(absolute top/right z-index30) 与全屏态缩放条 `.zoom-group`(absolute top/right z-index1102) 都钉在表体右上角 → 互相遮挡且缩放条层级更高盖住退出全屏按钮；非全屏态全屏按钮也绝对定位浮在表头末列上方。整改：把「全屏按钮+缩放条」合并为表体上方一行 `.grid-ctl-row`(flex 流，`margin-left:auto` 右对齐)，置于表格之上、与表格靠 `.grid-area` 的 `gap:10px` 自然留白，二者同排互不遮挡、随容器走永不压表体；缩放条改为非全屏/全屏均常驻（更实用）。只读区(446)与编辑区(586)各一套。删失效 `.grid-area.is-fs .grid-fs-btn`/`.is-fs .zoom-group`/`.not(.is-fs) .zoom-group`/死 `.tbl-toolbar`/`.edit-ops` 留白规则；`.grid-fs-btn` 去 absolute 改为带边框的流内按钮。build+rsync+chown 已部署；产物核对旧漂浮规则=0、grid-ctl-row/right/zoom-group/grid-fs-btn 各=2。⚠️未做浏览器真机验证。

- **全屏/缩放控件视觉微调（用户三诉求）**：①全屏按钮 `.grid-fs-btn` 恢复上一版**无边框**样式（`border:none;background:none`，去掉上轮加的边框/底色，用户嫌丑）；②整体收窄省空间：控制条 `min-height:34→30px`、`grid-ctl-right` gap 10→8、`zoom-group` gap 8→6、滑块 `.zb-range` 140→100px、数值 `.zb-val` min-width 46→38 且字号 12px、标签字号 12px；全局进度条 `.progress` 高度 8→6px；③四个图标统一 26×26（全屏=缩放减/增/重置按钮同尺寸，放大/缩小/返回/全屏一样大）。build+rsync+chown 已部署；产物核对 grid-fs-btn 26x26 无边框、zb-range 100px、grid-ctl-row 30px、progress 6px 均落地。⚠️未做浏览器真机验证。

- **全屏按钮回到上一版悬浮位（用户反馈"还没改过来"）**：上一轮只把全屏按钮去边框、但仍留在顶部控制条内，用户要的是**浮在表格右上角的图标**（重叠修复前的原样）。整改：把 `<button class="grid-fs-btn">` 移出 `.grid-ctl-right`、作为 `.grid-area` 直接子元素 `position:absolute;top:8px;right:8px;z-index:30`（压在 sticky 表头 z-index:7 之上，保证常显）；全屏态 `.grid-area.is-fs .grid-fs-btn{top:14px;right:14px}` 避开 12px padding；缩放条留控制条内并改为左对齐（移除 `grid-ctl-right` 右对齐包裹），与右上角悬浮按钮不冲突；`.grid-fs-btn` 无边框(`border:none;background:none`)+26×26 与缩放三按钮同尺寸。只读区(446)与编辑区(586)两处同改。build+rsync+chown 已部署；产物核对 grid-fs-btn 浮动无边框+is-fs 偏移均落地、grid-ctl-right 已清除。⚠️未做浏览器真机验证。

- **全屏按钮图标修复（用户截图：显示为横点）**：根因是 Forecast.vue 内手写 SVG 的 path 数据 `M8 3H5a2 0 00-2 2v3...` 中，`H5a2` 的 `a2` 会被浏览器解析为非法的 arc 命令（缺少 rx/ry 参数），导致图标只渲染出一小段横线。整改：把两处 `<button class="grid-fs-btn">` 里的手写 SVG 替换为项目内置 `<Icon name="fullscreen" size="16"/>`（Icon.vue 中已定义正确的四箭头全屏图标，path 参数合法）；同时去掉 `cp-icon-btn` 类避免外部样式干扰。build+rsync+chown 已部署；产物核对非法 arc 模式 `a2 0 00-2`=0，正确 fullscreen 四条 path 各=1。⚠️未做浏览器真机验证。

- **主工具栏单行化（用户要求所有按钮同一水平行、不分多行、适配不同屏宽）**：根因是 `.toolbar` 样式（Forecast.vue:5302）用 `flex-wrap:wrap` 会换行，且 `@media(max-width:768px)`（5719-5721）在窄屏把 `.toolbar` 改成 `flex-direction:column` 纵向排列。整改：①`.toolbar` 改 `flex-wrap:nowrap` + `overflow-x:auto`（窄屏横向滚动而非换行），加 webkit 滚动条美化（height:6px、thumb 用 --bd）；②给 `.toolbar>.tb-group`(左右两组)加 `flex:0 0 auto` 不收缩、`white-space:nowrap` 保证按钮文字不折行；③删除媒体查询里的 `.toolbar{flex-direction:column;align-items:stretch}` 与 `.sel-period{width:100%}`（避免窄屏把期次 select 撑满换行），保留 padding/gap 微调。build+rsync+chown 已部署；产物核对 toolbar `flex-wrap:nowrap`+`overflow-x:auto` 各=1、`flex-direction:column`=0、`toolbar>.tb-group` 选择器=1、scrollbar-thumb=1。⚠️未做浏览器真机验证。

- **主工具栏按钮文案精简（用户建议省空间）**：顶部"导入 Excel"→"导入"、"保存调整"→"保存"（按钮 27/30 行，省横向字符）；内部提示语（留痕说明 903、草稿恢复横幅 1382、Excel 粘贴 toast 2308）引用的"保存调整"同步改为"保存"保持一致性。build+rsync+chown 已部署；产物核对"导入 Excel"/"保存调整" 残留=0、"导入"/"保存" 已落地。⚠️未做浏览器真机验证。

- **修复"高级/表格设置"下拉被遮挡（用户反馈下拉窗口被遮挡）**：根因是上一步 `.toolbar{Forecast.vue:5302}` 加 `overflow-x:auto` → 浏览器把 `overflow-y` 强制提升为 `auto`，裁切掉超出工具栏高度的绝对定位下拉面板 `.tb-pop-panel`（5468，原 `position:absolute;top:38px`）。两处弹层（表格设置 33-61、高级 66-78）结构相同共用该 class。整改：①把两个 `.tb-pop-panel` 用 `<Teleport to="body">` 包裹 + CSS 改 `position:fixed`（脱离 `.toolbar` overflow 裁切上下文）；②加 `popStyle`(reactive) + `settingsBtn`/`advBtn`(ref) + `positionTbPop()`(getBoundingClientRect 计算 left=r.right-240/top=r.bottom+6，底空间不足则翻到按钮上方) + `toggleTbPop(which)`(打开时 nextTick 定位)，按钮 click 由直接翻转改为调 `toggleTbPop`；③删旧 `top:38px;right:0`。`.grp-row`(738，分组内部面板) 在 grid-area 内不受影响。build+rsync+chown 已部署；产物核对 `.tb-pop-panel` CSS=`position:fixed`、旧 `top:38px`=0、`getBoundingClientRect`/ref名/`'settings'|'adv'` 参数串均在、旧直接翻转残留=0。⚠️未做浏览器真机验证。

- **"仅显示有报单"移到主工具栏常驻（用户：高频操作，规划位置）**：规划落点=主工具栏右侧组、搜索框正右侧(`导入`前)，与搜索框(同为行级筛选)紧邻、且不受 `v-if="!editMode"` 限制→编辑/只读两模式都可用（顺带修复此前该复选框藏在只读专属弹层内、编辑态不可达的遗漏）。从「表格设置⚙」弹层移除该复选框(弹层保留显示AI建议等其余项)。新增 `.tb-toggle` 样式(与.tb-search 同高32px/bg3/bd/radius8，accent-color 用品牌青)。工具栏状态徽标精简为「已隐藏 N 个零报单」(去掉与复选框重复的"仅显示有报单"前缀，保留数量反馈)。build+rsync+chown 已部署；新产物 Forecast-CAu3aVHj.js/CSS；产物核对 `仅显示有报单` 仅 1 处(主工具栏)、tb-toggle 样式已落地、弹层内残留=0。⚠️未做浏览器真机验证。

- **预报页新增"仅显示有报单"筛选（用户要求为"填报数量"列加筛选）**：新增 `hideZeroReport` 复选框（默认关），置于「表格设置⚙」弹层（与"显示AI建议"并列）。语义：商品行总报单量 `rowSum(r)<=0` 即"填报数量为0"，勾选后隐藏这些单品、仅显示有报单(数量>0)记录。三处统一生效：①`rowVisible()` 网格渲染过滤；②`filteredRowsForExport()` 导出 Excel 过滤；③`copyAll()` 复制下单过滤——保证筛选后复制/导出只看结果，便于查看与复制。另加状态徽标 `confirm-badge.filter`（品牌青）在工具栏实时显示"仅显示有报单 · 已隐藏 N 项"；新增 `zeroReportCount` computed。实时生效（renderModel 依赖追踪 hideZeroReport），不影响其他列展示。build+rsync+chown 已部署；产物核对 `hideZeroReport`/`仅显示有报单`/`已隐藏` 均在 Forecast 分包。⚠️未做浏览器真机验证，请强刷确认。

- **🐞 修复"仅显示有报单"在编辑态不生效（用户硬刷新反馈零报单行未消失）**：根因=预报页有**两套渲染表**靠 `editMode` 切换——只读表(460)行源 `vsWindow.items`→`flatItems`→`renderModel`→`rowVisible`（首轮只改了这里，只读态生效）；编辑表(622 `edit-tbl`)行源 `cross.rows`+`v-show="rowShown(ri)"`（首轮漏改 `rowShown`，编辑态完全不过滤→用户在编辑态测试看似无效）。修复：在 `rowShown(ri)`(3155) 加入 `if (hideZeroReport.value && rowSum(cross.value.rows[ri]) <= 0) return false`，覆盖编辑表；现两模式均实时过滤。build+rsync+chown 已部署；新产物 Forecast-CLfaJBXm.js（`hideZeroReport` 被 esbuild 压成短名，源码 3155 已确认；`仅显示有报单`/`已隐藏` 各 1 处）。**教训：行级过滤/显隐须同时核对只读表(`renderModel`/`rowVisible`)与编辑表(`rowShown`)两条链**。

- **删除工具栏"复制下单"按钮（用户要求）**：删除主工具栏 `复制下单` 按钮（原 line 66，`@click="copyAll"`），并一并清理已无任何其他引用的 `copyAll()` 函数（原 3122-3138，复制"所有客户所有商品下单文本"）。`copyAll` 全文仅按钮+定义两处，删除无副作用；单客户/单单元的区域复制(`copyUnit` 之类)仍在、不受影响。build+rsync+chown 已部署；新产物 Forecast-B2-TvV-e.js；产物核对 `复制下单`/`copyAll` 均为 0。

- **"AI审核"按钮改"下单核对" + 未连 ERP 占位提示（用户战略判定）**：用户指出 AI审核 有硬伤——AI 建议量吃本地库库存/销量，本地库数据要么手填要么连接器同步；手填库存+销量对经销商不现实，**只有 API/MCP 实时抓 ERP 才有意义**（与产品"数据接入走连接器"战略一致）。决策=保留占位、但：① 文案软化 `AI 审核`→`下单核对`（按钮 line 32 / 弹窗标题 / 2处下游校验 toast 同步改），因老板不完全信任AI、"AI审核"词过重；② 复用 chanjet/kingdee 状态接口(`/api/datasources/v2/{chanjet,kingdee}/status`)做 `erpLinked` 判定，`onMounted` 调 `probeErp()`，弹窗顶部加琥珀色警示块(`audit-erp-note`)：未连 ERP 时提示"智能建议需实时库存/销量依据、未连接仅供参考，可手动核对后直接确认定稿，或去能力中心连接 ERP"并带「能力中心」文字按钮(`goConnect`→`location.hash='#/connect'`)。代码确认审核逻辑 `_audit_items` 只读本地库、连接器就绪即自动变准，无需改审核代码——硬伤在数据管道非审核逻辑。build+rsync+chown 已部署；新产物 Forecast-DoUSiW32.js/CSS；产物核对 `下单核对`=4、`AI 审核`=0、`当前未连接 ERP`/`建议才会准确`=1、`audit-erp-note`/`link-btn`=2。⚠️未做浏览器真机验证。

- **"下单核对"→"智能建议" 再次改文案（用户最新要求）**：上轮的"下单核对"仍偏重，用户要求换更柔和的"智能建议"。四处统一替换：工具栏按钮(`下单核对`→`智能建议`)、弹窗标题(`下单核对本周期 · `→`智能建议本周期 · `)、2处下游校验 toast(`下单核对本周期 → 定稿`→`智能建议本周期 → 定稿`，replace_all)。弹窗内"AI 不下单（人定稿）"信任句保留。build+rsync+chown 已部署；新产物 Forecast-Wu2Ql-O0.js；产物核对 `智能建议`=5、`下单核对`=0。⚠️未做浏览器真机验证。

- **移除"生成舟谱模板"入口 + 去舟谱化（用户决策，原因：①非舟谱客户见舟谱字样困惑 ②模板易变、应改 AI 对话框入口由大秘定时任务调技能生成）**：范围=工具栏按钮 + 提交闭环向导内「舟谱订单导入模板」步骤都删（用户选推荐项）。改动：①删主工具栏按钮(line65)；②删向导原②舟谱步骤整块，向导重排为「①余额&付款 → ②返利冲档测算」两步（付款 footer 文案`下一步：生成舟谱模板→`改`下一步：返利测算→`且 wizardStep=2 指向返利；返利步骤 `wizardStep===3`→`===2`/`locked<3`→`<2`/编号3→2；返利 footer `←上一步` `wizardStep=2`→`=1`）；③周边残留文案清理：付款到账标题`③付款到账确认（…可生成下游订单/舟谱模板）`→`付款到账确认（到账后标记，用于下游结算）`、SOP提示`到货前1天出舟谱模板`→`到货前1天 AI副驾自动生成导入模板`、付款 toast`可生成下游订单/舟谱模板`→`下游结算信息已更新`、W5注释去掉舟谱；④删 dead code `genZhoupu`/`wizardGenZhoupu` 函数 + `genZpLoading` ref。**后端 `forecastApi.zhoupuTemplate` API 保留未删**——作为 Hermes 大秘"生成导入模板"技能+定时任务的底层底座（前端不再调用，但接口存在供 AI 未来直接调）。build+rsync+chown 已部署；新产物 Forecast-DXcrvlfv.js；产物核对 `舟谱`/`生成舟谱模板`=0、`AI 副驾自动生成导入模板`=1、`返利测算`=1。⚠️未做浏览器真机验证。

- **修复「返利冲刺看板」默认视图看不到（用户反馈"面板在哪儿"）**：根因=看板(及其下「厂家返利达成」)被 `v-show="viewMode === 'list'"` 限制，而 `viewMode` 默认 `'cross'`（导入汇总），故默认视图下整块藏住。修复=两块都改为**有期次即显示**：看板 `v-show="viewMode==='list'"`→`v-if="cross.period"`；「厂家返利达成」去掉 `v-show` 仅保留 `v-if="rebateSummary.length"`。数据源 `cross.value.rows` 由 `loadCross()` 在默认视图即填充、切 list 视图仍保留，故两视图数据一致。部署：npm build(✓1.47s)+rsync+chown；新产物 Forecast-DgVHhXPw.js（`返利冲刺看板`=1 已核对）。⚠️看板位于导入汇总表**下方**，需向下滚动；若用户希望置顶(表上方)可再调位。未做浏览器真机验证。

- **舟谱目标管理截图分析 → 两份产品参考文档（用户要求"需要"两份）**：基于 5 张舟谱创建目标截图 + 我们真实 `rebate_target_rules` 引擎（server/erp_db.py v108 + routers/rebate_rules.py + 前端 rebate-rules.js）对照产出：① `舟谱目标管理-设计借鉴清单.md`（六维亮点+七层借鉴建议，含 P0/P1/P2）；② `返利目标规则-改造PRD与字段映射.md`（定位边界声明 + 真实表结构 + 前端能力盘点 + 舟谱↔我们字段映射对照 + 分优先级改造项 P0-1~4/P1-1~5/P2-1~3 + 产品边界：不照搬"拆到员工/客户""拜访/铺货客户数"等考核语义、返利计算字段不动）。核心结论：舟谱值得学的是**前端交互层与信息架构**（卡片入口/三步向导/详情页/复制/修改记录/状态语义），后端字段我们已更强。两份文档置于 laozhangai-product 根目录（非 git 提交，仅为设计参考）。
