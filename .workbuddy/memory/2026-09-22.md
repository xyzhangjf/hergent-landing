# 2026-09-22 工作日志

## 全站部署状态排查（用户要求：找「已开发但未上线」的改动）

**交付**：`outputs/部署状态排查-2026-09-22/01-全站部署状态排查报告.md`

### 结论（反直觉，务必记住）

**「已提交但未上线」的改动 = 0 个。** 真正的缺口全是反方向的：

| 类别 | 内容 | 风险 |
|---|---|---|
| 前端**未上线** | `ProductArchive.vue`(23:56) + `Forecast.vue`(23:57) —— v240 报单默认单位（下拉 + 自动带出）；改动**晚于**最后一次部署 23:05 | 🔴 高 |
| 后端**已上线从未提交** | `products.py` / `data.py` / `erp_db.py`（v234，156 行） | 🟡 中 |
| 前端**已上线从未提交** | 同上两个 vue（v234 部分） | 🟡 中 |
| 小程序**已入库、微信线上未生效** | v227 样单 / v224 结果卡 / v211 埋点 —— **备案未获批，只上传未提审** | 🟡 中 |

### 🔴 新判据（重要，可复用）

1. **本项目无 production 分支** ⇒ 生产基线只能靠**生产文件系统**反推。
   - 后端：`/opt/hergent-erp`（已用 `systemctl cat` 确认是 `WorkingDirectory`；`/opt` 下的 `hergent-erp-prod` / `-staging` / `hergent` **无 `erp_db.py`**，是干扰项，不是生产）。部署 = `rsync server/ → /opt/hergent-erp/`（**不是 FLAT**，`routers/` `db/` `schemas/` 是子目录，相对路径可直接对）。
   - 前端：`/opt/hergent-cn-v2` 只有 `dist`。

2. 🔴 **生产 `assets/` 是「多次历史构建的并集」，不是当前构建**。
   实测 132 个文件 = 4 次构建残留（20:50 / 22:00 / 22:24 / 23:05）。
   ⇒ **必须按 mtime 分批，只在「生效批次」里查判别串**。直接扫 `assets/*.js` 会把 3 次历史残留算进来，得出「生产已有该功能」的**错误结论**。
   生效批次由 `index.html` 的入口引用 + mtime 双重确定。

3. 🔴 **判别串必须「同有同无」**才有效（正反例同结果 = 探针作废）。
   本次 decisive 的一例：生效构建「报单默认单位」命中 **0**、本地源码命中 **2** ⇒ v240 未上线；
   同批反向校验「价格矩阵」1/2、「业务范围」1/1 ⇒ v234 已上线。两组一起才敢下结论。

4. **源码 mtime vs 部署 mtime 是最快的分类器**：源码 23:56/23:57 > 部署 23:05 ⇒ 未上线。后端零文件晚于 23:05 ⇒ 已同步。

5. 后端全量 md5 双向比对：**202/202 一致**，唯一 only-local 是 `tests/test_recon_engine.py`（测试文件不部署，正常）。

### 其它核实到的事实

- 后端 65 个未推送提交（63 含源码）、前端 136 个未推送提交（68 含产品源码）—— **全部已上线**，未推送只影响备份/协同。
- 分支包含关系 `international-standards ⊂ main ⊂ upgrade/v84-international` ⇒ 无分支含 v84 之外的工作。`main` 落后 65 只是卫生问题。
- **后端零 v237/v240 标记**（前端已有）⇒ 但核实后 v240 只依赖已有 `products.order_unit`（v233 落地），v237 只展示已有 `large_unit/medium_unit` ⇒ **后端无需改动**。
- v234 的 8 列已真落地：`journalctl` 显示 23:06 重启后 `[schema-sync] 3 个库有差异，补列 24 个，告警 0 条`。
- alembic **实际未使用**（本地与生产都只有 `001_initial_stamp.py`），真实迁移走 `_safe_migrate`。
- 后端**无 `requirements.txt` / `pyproject.toml`** ⇒ 依赖无法版本比对、新机器无法重建。建议补。
- 未完成标记：后端真命中只有 `chanjet_connector.py:322/333/345` 的 3 处 `TODO(CONFIRM)`（字段名未确认，已上线）；`einvoice_engine.py:369` 是 stub 但**无调用方**且离线模式只查本地库（不编造假数）；feature_flags 有 CRUD 但**零处用于门控**（半成品）。**前端 TODO/FIXME/WIP/mock 全为 0**。
- 生产 `assets/` 有 81 个非生效残留文件待清理。

---

## v240 落地（用户答「要」后立即执行）

### 交付

| 仓库 | 提交 | 内容 |
|---|---|---|
| hergent-erp | **`af9f64a`** | 补交 v234 已上线从未提交的 3 文件（`products.py` / `data.py` / `erp_db.py`，156 行） |
| laozhangai-product | **`3e0f0e4`** | v240 前端：`ProductArchive.vue`(+240/−40) + `Forecast.vue`(+18/−1) |

**后端无需重新部署**：提交后复跑全量 md5，202/202 仍逐字节一致。

### 部署与三层验证（全过）

- 构建：入口 `index-Vxqnbvfr.js`（与提交前那次一致 ⇒ **构建确定性**成立，可放心用 hash 判对齐）。
- 部署：`rsync -av --delete` + 备份 `/opt/hergent-cn-v2.bak-v240-20260922-071730.tar.gz` + `chown -R hergent:hergent`。
  **assets 132 → 51**（清掉 81 个历次构建残留），删除范围经 `--dry-run` 确认**全在 `assets/` 下**，未误伤 index.html / legal / favicon.svg。
- ① 本地 dist vs 生产文件系统：**55/55 逐文件 md5 ALIGNED**。
- ② 公网 HTTP 回读：入口已切到 `index-Vxqnbvfr.js`；`index` / `Forecast` 两个 chunk 公网 md5 与本地**全等**。
- ③ 判别串：生产「报单默认单位」**0 → 2**（v240 上线）；「价格矩阵」1、「业务范围」1、「包装单位」2（v234 无丢失）。
- 冒烟 200 ×4；**阴性对照**：旧入口 `index-jc5bYq50.js` → **404**（证明切换真实生效，探针不是空判）。

### 本轮踩到的两个坑（复现时注意）

1. 🔴 **macOS 自带 rsync 不支持 `--chown=`**（报 `unrecognized option`，且**只有加了 `--dry-run` 才看出来** —— 不加 dry-run 会直接失败退出，误以为部署了）。
   正解：去掉 `--chown`，部署后 `ssh chown -R hergent:hergent`。
2. 🔴 **`grep -oE "…\.\(js\|css\)"` 又踩 `\|` 坑**（BSD grep 不支持 `\|`）：`grep -o` 配 BSD 基本正则时 `\|` 是字面量 ⇒ 返回空，**看起来像"公网没这个 chunk"**。
   已第三次踩同类 ⇒ **正则里要"或"一律用 `grep -E` + `|`**。

---

## UI 规范审查 ·【商品档案 → 编辑商品】（交付报告，未改代码）

`outputs/UI规范审查-商品档案编辑页-2026-09-22/01-商品档案-编辑商品-UI规范审查.md`

### 🔴 最重要的发现：17 个类「用了但零样式定义」

v234/v237/v240 三期新增的编辑弹窗主体 UI —— Tab 栏(`pa-tabs/pa-tab`)、面板(`pa-pane`)、
包装单位三卡片(`pa-card/pa-card-hd`)、默认单位与移动端可见(`pa-subsec/pa-checks/pa-cb`)、
价格矩阵(`pa-pm-wrap/pa-pm/pa-pm-input`)、`pa-c` —— **全仓 `src/` 下 0 处 CSS 定义**。
⇒ 元素退化为浏览器默认样式。不是"不好看"，是**整块新功能没有视觉实现**。
另有表格侧 `pa-brand-cell`/`pa-lu-cell`/`pa-mu-cell`/`pa-ou-cell`/`pa-log-list` 同样缺。

**判据（可复用）**：类差集审计 —— 抽 `<style>` 段去注释取 `.xxx` 定义集；抽模板取 `class="..."` 内类集；
差集 = 无样式类。脚本 `/tmp/pa_class_audit.py`。本次：模板用 92 / 样式定义 75 / **缺 17**。
⚠️ **这个坑以后每次新增大块 UI 都要跑一遍** —— 加了类名却忘了写样式，构建零报错、肉眼在源码里极难发现。

### 其它高严重度项

- **C1 遮罩点击即丢弃改动**：`@click="tryCloseEdit()"` 直接关闭，只事后 toast「已放弃 N 处」，改动已丢。
- **B2 无字段级校验呈现**：三处校验（名称空 / 到货天数越界 / 负数）一律 toast，且错误字段可能在**别的 Tab**，用户找不到。
- **D2/D3 无障碍**：无 `role=dialog`/`aria-modal`、无 Esc、无焦点陷阱、无初始聚焦（同仓 CommandPalette 有 role=dialog，本页没跟）。
- **A5 对比度**：`.pa-hint{font-size:11px;color:var(--t3)}` — `#a1a1a6` on `#fff` ≈ **2.5:1**，远低于 AA 的 4.5:1。

### 规范本身的问题（已记录，待用户裁决）

- **S1 无统一 Modal 层**：8+ 页各造一份，圆角 16/18、阴影两种、**z-index 六档**(950/960/980/990/1000/1001)。
- **S7 令牌与实际主流脱节**：`--shadow-lg`=`0 12px 40px .10`，但实际主流是 `0 16px 48px .18`；
  硬编码 16px 圆角 10 处 vs 用 token 9 处 —— **两边各占一半，无法判定谁"符合规范"** ⇒ 必须先裁决再统一。
- **S5 无表单错误态规范** ⇒ 各页只能用 toast 兜底（B2 的根源）。
- 另有 S2(z-index token) / S3(无 .btn-danger 全局层) / S4(无 .input:disabled) / S6(字号 9 档散落) / S8 / S9(4 个档案页 @media 全为 0)。

### 已符合的维度

页面骨架(`.page/.page-hd.split/.page-sub`)、按钮层级(`.btn-primary/ghost/sm`)、卡片与表格(`.card/table.tbl`)、
语义色走令牌、加载态、停用**内联**二次确认（良好实践）、保存只提交 diff。

### 剩余待办

1. 真机三项核对：包装单位 Tab「默认单位」下拉 + 29 个历史手填值能显示 / 预报页新增单据单位自动带出 / 手改单位不被二次覆盖。
2. 小程序备案通过后补上传 + 提审（v227 样单等），并补跑真机 D1–D17（前置：造 `order_start` 在未来的期次）。
3. 后端补 `requirements.txt`（当前无依赖声明）；确认远端仓库 private、评估轮换 Hermes 网关密钥。
4. 工作树仍有他人在途未提交：`.workbuddy/memory/2026-09-21.md`、`outputs/商品档案页面对比优化方案-2026-09-21.md`（**未纳入本次受控提交**）。

---

## §全项目代码审查（2026-09-22，只读）

建立定时任务「项目代码全面审查（每日凌晨2点）」（id `2db35a54-9258-4c9d-aff9-38d30d713eec`）
并**手动跑了第一轮**。报告：`docs/code-review/2026-09-22.md`（新建 `docs/code-review/` 目录）。
**18 条**：高 1 / 中 9 / 低 8；硬编码凭据扫描 **0 命中**。范围 = `hergent-cn-v2/src` +
`hergent-erp/server`（近 7 天改动的 40 文件优先），共扫 268 文件。

**唯一高危**：`Forecast.vue:8437-8441 setHeal` —— 乐观更新（8438-8439 先改本地）+ 8440
`catch (e) {}` 静默吞错 ⇒ 服务端写失败时界面已改、无回滚、无提示，正是本项目反复踩的
「提交了但看不出成没成」模式。同型：`Forecast.vue:8221-8224 saveVarAttr`、
`7106/7561/7575`（云端同步失败仍 toast「已存」）。

**其余值得记住的判据**
- `server/domain/forecast_engine.py:549-550`：效期解析失败 `continue` 丢行 ⇒
  `nearest_days/expiring_qty/expiring_value` **系统性偏小**，临期告警失真（错误地静默丢弃，非合理降级）
- `server/scheduler.py:637-638` `_days_until`：解析失败返回魔法值 **999** ⇒ 合同到期检查**永不触发**
- `server/hermes_core.py:21-24`：`except ImportError: def audit_ai_action(...): pass` ⇒
  5 处 AI 写操作（创建客户/商品/**收付款**/销售单/采购单）审计 fail-open 且无痕
- `server/db/queries/purchases.py:16-17`：裸 except 使审批阈值静默回落 5000 ⇒ 审批绕过得失取决于方向
- `fmtNum` 三套口径：`Workbench.vue:336`（千分位）/ `AiHub.vue:236-240`（折"万"）/
  `NotificationPanel.vue:146`（千分位+小数）⇒ 违反项目「统一万元口径」
- `hergent-cn-v2/src/utils/rebateCalc.js`（148 行）**整文件零 import**，口径可能已与 Rebate.vue 分叉

**踩坑复查**：`grep "A\|B"` 不带 `-e` 在本机**静默返回空** —— 差点据此误判
`chanjet_connector.py` 有 sqlite 连接泄漏（实读后：第 60 行唯一调用点，第 81 行有 `db.close()`，
**不成立**）。同理 `hermes_tenants.gateway_status` vs `ai_channels.gateway_status` 是
**委托包装**不是重复实现。**机械扫描结果必须读源码印证后再入报告。**

**未覆盖**（建议下轮）：`erp_db.py`(1.0MB)、`Rebate.vue`(208KB)、`server.py`(258KB) 仅抽查；
61 处带注释的宽泛 `except Exception` 未逐个判「该抛却吞」。

> 审查方法论可复用到后续轮：先跑 `/tmp/code_audit_scan.py` + `/tmp/dup_scan.py` 取候选，再逐条读码印证。

---

## §整改执行（同日，清单 16 项 → 实施 11 / 撤回 1 / 不适用 3 / 部分 1）

结果文档：`docs/code-review/2026-09-22-fix-result.md`。**未提交、未部署、未重启**。

**已改（前端 5 文件改 + 1 删；后端 9 文件改）**
- `Forecast.vue`：`setHeal` / `saveVarAttr` 加**留底回滚 + 失败提示**；`saveSuggestRecipe` /
  列方案存删 三处改 `cloudOk` 降级文案（不再"云端挂了还报成功"）
- `domain/forecast_engine.py` `_inject_expiry_alerts`：异常收窄 + **脏日期行不再丢数量/金额**
- `scheduler.py` `_days_until`：`999` → `None` + 留痕；3 个调用点同步（顺带去掉每条重复调 2 次）
- `hermes_core.py` 审计兜底加 warning；`ai_engine.py` `pass`→warning
- `purchases.py` 裸 except 收窄 + `DEFAULT_APPROVAL_THRESHOLD`；`inventory.py`
  `EXPIRY_CRITICAL_DAYS/WARNING_DAYS`；`import_zhoupu.py` 收窄
- `gl.py` 删 3 个 DEPRECATED no-op，`erp_db.py:4796` 导入收敛（无逻辑改动）
- `printable.js` 删 `downloadPrintable`；`client.js` `clearTenantCookie` 去 export；
  `Workbench.vue` 删死函数 `fmtNum`；**删 `utils/rebateCalc.js`**（148 行，全仓零引用）

**验证**：前端 `vite build` ✅ 2.03s；后端 `compileall` ✅ exit 0；3 套纯逻辑测试 ✅ 全绿
（46 断言）；影子库导入冒烟 9/9 ✅；**桩替换 DB 的行为验证**证明脏日期行由"丢弃"变为并入
（10→30 件）；真实库 `erp.db` md5 前后一致（零写入）。

**撤回 1 项**：清单第 9 条「`fmtNum` 三套口径不一致」**原判断不成立** —— `AiHub` 的是
调用次数/额度字符数，`NotificationPanel` 的是通知条数，量纲不同各用各的合理；`Workbench`
那份是从未被调用的死函数（已删）。**强行统一成万元口径会把计数类数字错误折算。**

**不适用 3 项**：`fmtDate` 去重（需新建公共模块，违反"不引入新方案"）；`Forecast.vue` 拆分
（大型重构）；`roles.js canUseMiniProgram` + `useCardTrigger` 四个 `strip*Fence`
（属已文档化的单一来源/协议对称面，删或接线都需拍板 —— `extractClarify/Proposal/Reminder`
**有**外部调用，实际剥离走 `stripAllFences`）。

**踩坑（第三次同源）**：`grep -e "^\s*except:"` 在本机 **BSD grep 不认 `\s`** ⇒ 统计"剩余裸
except=0"是假象。必须用 POSIX 写法 `^[[:space:]]*except:[[:space:]]*$`。另：全仓引用搜索
**必须含 `.mjs`** —— 差点漏掉 `hergent-cn-v2/tests/*.contract.mjs`（幸好它只走 HTTP 测后端）。

### UI 规范三阶段整改（v241）—— 全完成
- **阶段1 实现**：`variables.css` 补 `--z-*`/`--fs-*`/`.btn-danger`/`.input:disabled|[readonly]`/`.input.err`+`.field-err`；`ProductArchive.vue` 编辑弹窗：补齐 17 个零样式类（改复用全局 `main-tabs`/`tab-pane`/`sub-card`/`check-group`/`pm-input` 等）、C1 遮罩误丢改动→内联二次确认、B2 字段级错误呈现+跨 Tab 自动跳转、D2/D3 `role=dialog`+Esc+初始聚焦、A5 提示字 12px/--t2、A 组圆角阴影回归令牌、D1 640px 单列降级。提交 `变量`+`ProductArchive` → `npm build` 零报错 → 备份+`rsync --delete`（清 81 残留）→ 三层验证 55/55 对齐、公网 md5 一致、旧入口 404。**已上线**。
- **阶段2 规范文档**：新建 `hergent-cn-v2/docs/UI-SPEC.md`（设计令牌/组件用法/布局响应式/交互状态/命名与文件组织/维护方式），与 variables.css 同仓。
- **阶段3 全仓排查（只报告不改）**：扫描 49 个 .vue。结论：**0 阻断**（v241 已修唯一阻断）；🟡 建议 5 类：B3 z-index 散落 16 处(风险最高)、C 15 个弹窗缺 dialog 语义、B2 阴影硬编码、B4/B5 颜色字号硬编码(ConnectCenter/Forecast/AiOps 重点)、D 弹窗页 0 @media；🟢 可选 2 类：B1 圆角硬编码、D 非弹窗组件响应式。A 段"类差集"经抽样确认是动态绑定/前缀误报，**不计入**，仅留 `tb-cp-txt`/`forgot-body`/`brands` 等疑似项待人工复检。报告 `outputs/UI规范审查-商品档案编辑页-2026-09-22/02-全仓UI规范排查清单.md`，提交 `8d2dd20`。
- ⚠️ 复用提醒：自动扫描脚本 `/tmp/ui_audit_all.py` 的"类差集"会把 `:class="{on:tab==='x'}"` 的 `x` 当未定义类误报，判断前须抽样。建议把它移入 `hergent-cn-v2/scripts/` 做规范回归。

### Forecast.vue「写失败不再静默吞掉」—— 补交（已在线上）
- 用户答「要」。工作树 Forecast.vue(29+/8−)：`saveSuggestRecipe`/`saveScheme`/`delScheme` 云端同步失败不再谎报成功；`saveVarAttr`/`setHeal` 服务端写失败回滚本地并报错。
- 🔴 **决定性发现：该改动早已在线**——v241 构建用的是**工作树**（非 HEAD），Forecast.vue 的 mtime=07:53 早于 v241 构建，遂被一并打包上线。**「已上线未提交」又一次**（本项目第 N 次）。用隔离工作树（`git worktree add --detach /tmp/fe-v242 HEAD` + 仅叠加 Forecast.vue + symlink node_modules）构建，与生产**55/55 md5 全等** ⇒ 无需部署。
- 补交 `5f80403`（`git commit -F msg -- <path>` 路径限定提交，**不夹带**并发会话在 index 里的 `D rebateCalc.js`）。
- 🔴 **新判据：`git commit -F msg -- <path>`（路径限定提交）可安全用于「工作树有他人已暂存改动」的场景** —— 它只提交指定路径、**不收录**其它已暂存项（区别于普通 `git commit` 会夹带）。事后核对 `git show --stat` == 1 文件 + 他人 `D ` 仍在。
- ⚠️ **并发会话在途（未上线、未提交）**：`src/api/client.js`(clearTenantCookie 取消导出) / `src/utils/printable.js`(删 downloadPrintable、buildPrintableHtml 取消导出) / `src/pages/Workbench.vue`(删 fmtNum) / **`src/utils/rebateCalc.js` 整文件删除(148 行，已暂存)**。mtime 均 08:15，现 11:35（静默 3h+，应完成态）；悬空引用全 0。**留待用户决定是否同批上线**。

### 死代码清理批 —— 已上线（用户答「要」）
- 用户批准把并发会话的「死代码清理」批一起上线。4 文件：删 `src/utils/rebateCalc.js`(148 行，零引用) / `printable.js` 删 `downloadPrintable`+`buildPrintableHtml` 取消导出 / `client.js` `clearTenantCookie` 取消导出 / `Workbench.vue` 删未调用 `fmtNum`。**行为零变化**。
- 构建 → **整包 hash 变化**（27 chunk 重命名 + `index.html` 内容变更）：删死代码改变模块图 ⇒ minifier 变量重编号连锁（同 v241 删 `fmtNum` 的教训）。判别串全在（v240/v241/Forecast 修复各命中）。
- 备份 `/opt/hergent-cn-v2.bak-deadcode-20260922-113917.tar.gz` → `rsync --delete` → 三层验证：**55/55 md5 全等**、公网 md5 一致、冒烟 200、旧入口 `index-Boy5Wt3Z.js` **404**。
- 提交 `5e95b7c`（4 文件，5+/164−）。**工作树代码改动清零**。
- 🔴 **新坑：`git add <已删除文件>` 报 `fatal: pathspec ... did not match any files`**（工作树已无该文件）。正解：**不要 `git add` 那个删除路径** —— 若它已在 index 里是 `D `，直接沿用；只 `git add` 其余被改文件，然后 **`git commit`（不带 pathspec）**，提交前用 `git diff --cached --name-only | wc -l` 核对条目数 == 预期。⚠️ 注意：此时**普通 `git commit` 会把 index 里所有他人暂存项一并提交** ⇒ 必须先用条目数核验「index == 我的 N 项」。
- 本轮并发批的 `D rebateCalc.js` 是他人已暂存态，我沿用并提交，**未做任何 reset**（避免破坏其暂存）。

### 报单自动化归属评估（只读，未改代码）
- 用户问：「报单自动化」放在「创建品牌目标」是否合理、能否迁到「报单配置」。
- 取证：`报单自动化` = `ArrivalRhythmBlock.vue:50-94`（③ 区，开关 + 3 时点 + 6 期预览），渲染链 `BrandTargetForm.vue:35` → `TargetFormModal.vue`(标题 computed:201「创建品牌目标」) → `Rebate.vue`（侧栏「目标与返利」）。
- 🔴 **`报单配置`（`ReportMapping.vue`）是 `Forecast.vue` 的子页签（`activeTab='config'`，L9/L2140），职责 = 报单对象映射 + 模板参数** —— 与「期次何时开闭」不是同一件事。**两者跨模块**。
- 🔴 三条决定性事实：① `auto_*` 三时点**无任何调度器消费**（全后端除 `rebate_rules.py` 外仅 `erp_db.py` 3 处迁移）⇒ **只配置、不执行**；期次靠手动 `POST /api/forecast/periods`（「新建期次」），催单用**期次自身** `order_start/order_end`。② 后端 `rebate_rules.py:1144-1154` **租户内唯一**（"多个品牌各自开启会产出多套报单表冲突"）⇒ **租户单例伪装成品牌字段**。③ 前端**无唯一性前置校验**（`ArrivalRhythmBlock.vue:53-54` 纯表单字段）⇒ 填完整屏才被拒。
- 结论：**现状半合理，但现在不建议迁**；迁移只能"搬 UI 不搬数据" ⇒ 同时吃两边缺点（跨模块第二数据源 + 节奏/预览割裂 + 仍受租户唯一约束 + 回退 v121/v122）。**先做三处低成本修补**（唯一性前置提示 / 报单配置页加只读镜像卡+深链 / ③ 区文案点明"全局只能开一个"），**待拍板"是否让自动化真跑"再谈迁移**（届时性质从"目标属性"变"运行参数"，应迁到预报订单域并把配置上提为租户级）。
- 报告：`outputs/报单自动化归属评估-2026-09-22/01-报单自动化归属评估与迁移可行性.md`

### 报单自动化「真执行」——立项包（PRD + 风险评估 + 技术方案）
- 用户答「要」= 让自动化真跑（自动建表/自动关单），走「迁移 + 数据上提 + 接调度器」。
- 按项目 SOP（`product-dev-sop`）：大改动 ⇒ **PRD + 风险评估（R2+ 加技术方案）不写完不写代码**。本次 R4（技术维=调度器 infra；数据维仅 R1 因零 schema 变更）。
- 🔴 **设计阶段的关键发现（向用户提出、待其确认）**：原计划的「物理数据上提」**建议不做** —— 后端 `rebate_rules.py:1144` 已有租户唯一约束 ⇒ 那 4 列**语义上已是租户级**；物理迁移要动 11 个租户库（跨库=R4）却零功能收益。**改用「API 契约先收敛」**：新增 `/api/forecast/auto-period`（GET/PUT），前端只认此契约，将来真搬表前端零改动。
- 复用点（已定位）：排程纯函数 `_build_period_plan`/`_suggest_first_date`（在 `routers/rebate_rules.py`，**计划抽到 `server/domain/period_plan.py`**，与既有 `domain/arrival_schedule.py` 同构）；期次 `erp_db.forecast_period_create/close`；调度器挂载点 `scheduler._loop()` 的 `_should_run` + `_run_per_tenant`；新路由须登记 `server.py:_PATH_MODULE_MAP`。
- 决定性原则：**预览与执行必须同源**（一个 `build_period_plan`）；`auto_period_enabled` 默认 0 ⇒ **上线零行为**，先 `dry_run` 观察再真跑。
- 🔴 **最大兼容风险（A9）**：品牌目标表单移除 ③ 区后，`openEdit` 的 `{...r}` 会带回 `auto_*` 4 列 —— 保存时**必须原样透传否则静默清零**（v122 §6.2 已警告过同类风险）。
- 立项包：`outputs/报单自动化实现-2026-09-22/01-立项包-PRD-风险评估-技术方案.md`（含 A1–A10 验收口径、5 阶段实施、三重回滚、测试计划）。

### v242 报单自动化「真执行」—— 已上线（后端 scoped 部署 + 前端）
- 范围：接调度器 + 迁 UI + 三处修补 + API 契约收敛（用户拍板，放弃物理数据迁移）。
- **后端提交 4311db8**：① scheduler._check_auto_period（每 5 分钟逐租户，复用 domain.arrival_schedule.build_period_plan —— 与预览同源，不镜像）② 新增 GET/PUT /api/forecast/auto-period（租户级契约，存储复用 rebate_target_rules 四列 ⇒ 零 schema 变更；校验复用 rebate_rules.validate_rule，不另写一套）③ tests/test_auto_period_v242.py 15 项离线单测。
- 🔴🔴 **生产演练当场抓到的两个关键事实（若不复演会直接出事故）**：
  1. **生产 tenant_1.db 已有一条 auto_period_enabled=1**（规则#10「蒙牛低温2026年目标」，16:00 开 / 10:00 关 / 周期 2 天）⇒ PRD 的 A1「部署后零行为」**被生产数据证伪**，一重启就会真的动作。
  2. **期次日期约定不同构**：手工期次用 order_start=order_end=报单日（生产的 #16「9月20日报单9月25日到货」），而自动按「填报窗口」写 (D-1, D) ⇒ 只比日期对**永远比不中**，会建出**并存的 open 期次**（小程序按 status='open' 取期次 ⇒ 歧义）。⇒ 加硬闸 ②：**本租户已有任何 open 期次即跳过开表**（并打日志，不静默）；关单只匹配 order_start=报单日 OR order_end=报单日，兼容两种约定。
- 部署：**scoped rsync 仅 2 个文件**（生产 scheduler.py/routers/forecast.py 均 == HEAD 且与并发会话 8 文件零交集 ⇒ 安全）；重启后 health 200、新路由 401（已注册 + RBAC 通过）、两文件 md5 与本地全等。
- **真机取证（最重要的一条）**：调度器实测输出 `[Scheduler] auto_period: 跳过开表（已有进行中的期次#16 9月20日报单9月25日到货）—— 请先关闭它，自动开表才会继续` ⇒ **零业务写入**，硬闸在生产数据上生效。
- 前端：新建 components/forecast/AutoPeriodBlock.vue（基准品牌选择 + 只读节奏镜像 + 状态卡 + 唯一性提示 + 6 期预览），迁入 pages/ReportMapping.vue（报单配置页）；ArrivalRhythmBlock 移除 ③ 区并加「已移至 预报订单 → 报单配置」深链；同步清理 BrandTargetForm / TargetFormModal 的 props/emits/死预览请求（_opTimer + loadAutoPeriodPreview + apMissedSummary + adoptSuggestion）。三层验证 55/55 全等 + 公网 md5 一致 + 旧入口 404。
- ✅ **A9 已核**：auto_* 四列仍在 useRebateTargetForm.defaultForm() 与回填映射里，保存走 rebateApi.update(f.id, f)/create(f) 提交整份 form ⇒ **移 UI 不清零**。
- 版本号：v241 已被本会话 UI 整改占用 ⇒ 本批改用 **v242**。
- 🔴 教训：`grep "A\|B"` 坑**本轮又踩两次**（BSD grep 静默无匹配）—— 一律 `grep -e A -e B`。

### v242b 方案 A —— 「改节奏」直达入口 + 下拉手动可选 + 关闭语义修正（已上线）
- 用户反馈「报单自动化那个下拉只能选、没有把手」。方案 A（我推荐、用户选）：**节奏仍以品牌页为唯一真相源**，但补把手。
- 改动：① AutoPeriodBlock 镜像行加「改『X』的报单节奏 →」深链（`#/rebate?edit_rule=<id>`）；② `#/rebate?edit_rule=<id>` 支持 —— Rebate.vue 用 `useRoute()` 读参，`loadRules()` 后只认一次（`_routeEditDone`）并 `openEdit(r)`，避免保存后重复弹出；③ 下拉在**手动模式也可选**（先挑基准品牌再开）；④ 无候选时给「去创建品牌目标」空态；⑤ 🔴 后端语义修正：关闭 = 关掉 **`auto_period_enabled=1 AND is_active=1` 当前真正开着的那条**，而不是前端传来的 `rule_id`（下拉手动可选后，rule_id 可能已指向别的规则 ⇒ 会「关错人」，表现为"切了手动但自动化还在跑"）。
- 提交：后端 `63a889f`；前端见本轮提交。三层验证：前端 55/55 全等、公网 md5 一致、旧入口 404；后端 md5 一致；health 200（**第 3 次探测**才 200 ⇒ 启动约 6 秒，之前那次 `health 000` 是时序假象，不是崩）。
- 🔴🔴 **上线后第一次真实执行（16:30:01）**：`[Scheduler] auto_period: 已创建期次#17 (2026-09-22~2026-09-23, 到货 2026-09-27)`。成因链完整可见：16:14/16:19/16:25 三次「跳过开表（已有 #16）」→ 用户在 16:25~16:30 关了 #16 → 16:30 闸②放行 → 建出 #17。**自动化已从"配了不生效"变为真实接管**。
- ⚠️ **遗留偏差（须告知用户）**：自动建的期次写「填报窗口」(D-1, D)，手工建的是 (D, D) —— 两种约定并存（#17=9/22~9/23，历史 #16=9/20~9/20）。功能上无碍（`period_validate` 通过、期次内归属自洽），但显示口径不统一，若要统一须改 `_auto_period_open` 的日期或反向统一手工约定。**已向用户明示。**
- ⚠️ 日志坑：`journalctl --since "-3min"` 被我 `2>/dev/null` 吞了错误 ⇒ 得到**假空**结论，差点误报"零动作"。**查日志不要吞 stderr，用 `--since "HH:MM"` 或直接 tail。**

### v242c 期次口径统一为「填报窗口」（用户拍板：统一成填报窗口那种）
- 根因（找到了，不是我猜的）：**「新建期次」的名称解析** `applyPeriodDates()` 把 `order_start` 与 `order_end` **都设成名称里的报单日** `d0`；`openNewPeriod()` 预填也是同一天 ⇒ (D, D) 口径来自这里，与自动建表的 (D-1, D) 不一致。
- 改动（纯前端 3 处）：① `applyPeriodDates`：`order_end = d0`（报单日）、`order_start = d0 的前一天`（用 `new Date(y, m-1, d-1)` 让 Date 归一化做减法，**不用 `-86400000ms`** ——夏令时边界会偏一天）；② `openNewPeriod` 预填：报单日=今天 ⇒ 下单截止=今天、下单开始=**昨天**、到货=今天+4；③ 新增 `npSingleDayWarn` 非阻塞提示（窗口只有一天时，提示与自动建表口径对齐）；④ 新增 `.np-hint-warn` 样式。
- 🔴 **历史期次 #16 刻意不改**：它已 `status='closed'`，且项目铁律（MEMORY 记载）「加单/定稿的归属键是 `(period_start, period_end)` **日期窗口**，不是期次 id」⇒ 改它的日期会让**已归属数据认不到**。要改必须单独显式授权 + 配套迁移与验证。**#17 已经是新口径 (9/22~9/23) ✓**，无需改。
- 部署：三层验证 55/55 全等、公网 md5 一致（`index-VUbJOswl.js`）、旧入口 404、冒烟 200。
- 遗留：手工 vs 自动的**显示口径**现已统一；但 #16（历史）仍是 (9/20,9/20)，属"改不了的过去"，不影响今后。

### v242d 新建期次预填锚点校正（用户指定）
- 用户口径：**下单开始 = 今天 / 下单截止 = 明天**（即把「报单日」锚在**明天**，不是今天）。
- 改 `openNewPeriod()`：`order_start=今天`、`order_end=明天`、`arrival=今天+5`（= 明天+4，保持「到货 = 报单日 + 提前天数」算式不被静默改成 +3）。仍全部可手改。
- 实测（Node 复现同一算术）：今天 2026-09-22 ⇒ 22 / 23 / 27；跨月 9-30 ⇒ 09-30 / 10-01 / 10-05；跨年 12-31 ⇒ 2026-12-31 / 2027-01-01 / 2027-01-05 —— `new Date(y,m,d+n)` 归一化正确。
- 部署三层验证：55/55 全等、公网 md5 一致（`index-DowSIGb4.js`）、旧入口 404。
- ⚠️ 我的测试脚本里 `diff=Math.round((b-a)/86400000)` 对"带当前时刻 vs 午夜"的两个 Date 会得 0（打印"窗口天数=0"），**是脚本误差不是应用问题** —— 验日期一律用 `fmt()` 打字符串对比，不要用整日相减去 round。

### v242e 新建期次表单补可见 label（用户指出）
- 用户：「是不是应该在对应的时间前面把『下单开始/下单截止』写出来」—— 对。四个字段原先**只靠 placeholder 标识**，而 placeholder 一旦有值就消失；v242d 把预填改成「永远有值」后 ⇒ 打开表单看到**三个光秃秃的日期**，分不出哪个是下单开始、哪个是到货。
- 修：改成 `label` 上 + `input` 下的 `.np-fld` 范式（**与同页 2252/2288 行那组字段同范式** —— 该页别处本来就有 label，只有「新建期次」这行是异类）。`期次名称` 一并补标签（否则一行 4 个字段里 3 个有标签、1 个没有，视觉不齐）。`.np-row` 加 `align-items:flex-end` 让两个按钮与输入框底对齐；`.np-fld .input{flex:none;width:100%}` 解掉继承来的 `flex:1`（列方向会撑高输入框）。
- 部署三层验证：55/55 全等、公网 index md5 一致、旧入口 404。
- 🔴 复用判据：**表单只靠 placeholder 标识字段 = 有值即失去可辨识性**。凡是预填/回填后**必然有值**的字段，一律要有可见 label。

### v242f 新建期次加显式「取消」（用户指出）
- 用户：「新建期次要有个取消或关闭按钮，现在不想建了只能点刷新」。
- 事实：`openNewPeriod()` **本就是开/关切换**（`if (showNewPeriod) { showNewPeriod=false; return }`），但按钮**长得不像开关**（图标与文案都不变）⇒ 隐藏出路等于没有。**这是"可发现性"缺陷，不是能力缺陷。**
- 修：① 表单内加「取消」（放主按钮左侧，与全站弹窗底栏「取消 / 主操作」同序）→ `cancelNewPeriod()`：关表单 + 清空 np + `resetNpTouched()`（`npSoftWarn`/`npNameDates` 是 computed 会自动归零）；② 两处入口按钮的 `title` 随状态切换（「新建期次：…」↔「收起新建期次表单」），不换图标（避免图标名不存在的风险）。**取消不弹二次确认** —— 主动放弃与「误点遮罩」不同族。
- 部署三层验证：55/55 全等、公网 md5 一致、旧入口 404、冒烟 200。
- ⚠️ 我的验证脚本**第二次**栽在同一处：`E=$(grep -oE 'assets/index-...' dist/index.html)` 已含 `assets/` 前缀，我又写 `md5 -q "assets/$E"` ⇒ 路径变成 `assets/assets/...` ⇒ 本地 md5 为空、误报"MISMATCH"。**正解：抽出来的入口串已含目录，直接用；或一律用绝对路径。**
