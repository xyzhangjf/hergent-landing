# 2026-09-01 工作日志

## 移除侧边栏「经营趋势」入口（2026-09-01 早）
- **需求**：移除 hergent.cn 侧边栏「经营趋势」按钮，其他菜单结构与样式不变。
- **改动**：`hergent-cn-v2/src/components/Shell.vue` 删 2 处入口——桌面侧边栏 `router-link to="/dashboard"`（经营趋势）＋移动端底部 Tab「趋势」。**路由保留**（`router/index.js` 不动，避免 /dashboard 直链 404，默认跳转仍为 /workbench）。
- **验证**：npm build OK（新 Shell chunk `Shell-B_CGeKab.js` 53.89kB）；本地 grep 新产物：经营趋势/趋势=0，其余 7 项菜单（经营工作台/预报订货管理/目标与返利/档案管理/能力中心/定时任务/设置）各 1；rsync --delete 部署 /opt/hergent-cn-v2 + chown；生产核验：新 chunk 落盘、`经营趋势`=0、`经营工作台`=1、index.html 引用新入口 index-Br_I1aQl.js。
- 纯前端改动，无需动后端。

## 目标与返利 · 对标国际一线 ERP 优化方案（咨询报告，2026-09-01）
- **交付**：`.workbuddy/artifacts/目标与返利-对标国际ERP优化方案-2026-09-01.md`（三维度 × 六环节 + 三大复杂场景专题 + 路线图）。对标 SAP S/4HANA Settlement Mgmt、Oracle Trade Mgmt、D365 Rebate&Deduction。
- **实证结论（非推测，基于代码核验）**：
  - 数据模型：rebate_target_rules 维度仅 brand/product（**缺品类/客户/渠道**）；tiered 是「命中档全量计」非累进（而合同侧 calc_contract_amount **是累进**→同业务两套算法）；tiers_json 单块 JSON **无版本无有效期链**；settled 仅 0/1 **无状态机无审批无冲销**；achievements 无 source_ref/批次/审核态。
  - **口径债**：规则 rebate_rate=小数 vs 合同 rebate_pct=百分点（代码注释自认历史遗留）；前后端双份算法（computeRebateFront vs compute_rule_rebate）。
  - **全库检索 currency 零命中 → 当前无多币种能力**；单租户 SQLite。
  - **可复用资产（勿重造）**：`bpm_engine.py`=完整 BPMN-lite（节点/网关/定时器，可直接接结算审批）；`routers/reconciliation.py`=供应商对账已含 rebates 取数。
  - 前端 Rebate.vue 2122 行，4 Tab（仪表盘/目标规则/达成填报/年度合同）。
- **核心判断**：差距不在算法，在①无版本与有效期链（说不清当时按哪版算）②无状态机与审批③无 BV 自动累积。**反常识建议：不要先做多组织多币种**（零收益、成本翻倍、污染所有金额逻辑），只预留字段。
- **路线**：短期 22~28 人日（透明化算式面板 1 人日 / 累进开关 3~4 / 试算器 3~4 / 维度扩展 2 / 内核统一 3~4）；中期 30~40 人日（版本化 8~10 / Claim+审批 10~13 / 批量作业化 5~6 / 增量累积 5~6）；多组织 8~10、多币种 12~15 按需触发。
- **护城河判断**：算法非护城河（SAP/Oracle 更全），护城河=低温奶政策语义理解 + 老板看得懂的交互 + AI 把复杂配置变一句话。
- 待用户确认 10 个关键问题（Q1~Q10，报告 §0），确认后据以调整方案。

## 预报订单管理板块审核修复 — 端到端交付（2026-09-01）
- **交付**：`.workbuddy/artifacts/overview-forecast-fix-impl-20260724.md`。基于《预报订单管理板块审核报告-2026-07-24.md》（35 项）经"按你的推荐来"拍板后实施。
- **后端**（hergent-erp upgrade/v84-international，6 文件）：E2 `/api/forecast` 由 reports 收口到 data（supervisor 默认仅 dashboard+data，修复前预报页恒 403）；D2 save_matrix 四段事务合并单一大事务 + period_id 落库；A1/A2 all_done 恒真修复；A4 404；A5 rebate-gap 恒带 balances；A7 accuracy 双口径（period_id→order_date 回退）；A8 死代码删除×2；A9 幂等建表；D1 N+1 批量×4；D3/D4/D6/D7(UUID)/D8/D9/D10(索引)/D11。
- **前端**（hergent-cn-v2 main，4 文件）：B1 默认只读；B2 删除清单（_deleted 行保存时过滤→后端幂等重建即物理删除）；B3 只读粘贴提示；A6 合成行(id<0)屏蔽关闭/删除；C1 heatStyle→--heat-* 令牌；C2 ReportMapping 对齐全局令牌（新增 --purple/--teal）；C3 计算列/校验态背景令牌化（--sum-*/--final-bg/--danger-*/--resizer-bg）。
- **⚠️ 部署决策（重要经验）**：本地分支领先生产 15 commit/5000+ 行（v111 返利整合/AI 卡片/demo/PLG/智能导入，暂缓推送决策）。**未跑 deploy.sh 全量 rsync**，改为逐文件 md5+diff 核对（确认生产=本地−修复）→ 备份 /root/backup_forecast_fix_20260724/ → 精确 scp 5 文件 → restart。erp_db.py 已与生产一致。
- **生产验证**（真实登录态）：supervisor/sales 访问 /api/forecast/order-board 均 200（E2 解除 403）；accuracy 返回真实期次关联；rebate-gap 恒带 balances；DELETE 不存在期次 404。
- **待办**：accuracy hit_rate=0 为数据现实（提审期未结束）；后端 15 个未发布 commit 仍滞留分支，后续发布需单独评审。

## 目标与返利「第一批 9 项短期可落地改进」端到端落地（2026-09-01）
按"依次全做"授权，第一批全部交付并生产验证完毕：
- **#351 计算内核统一 + 契约测试**：后端已上生产（前序）。
- **#352 阶梯计法开关**：后端已上生产（前序）。
- **#353 维度扩展（品类/客户/渠道）**：后端维度 CHECK 已含 category/customer/channel，已上生产（前序）。
- **#354 透明化算式面板 / #355 返利试算器 What-if / #356 达成率可视化+异常预警**：前端共用算式链 Modal，前序已 build+deploy+E2E 通过（生产产物 `Rebate-DTdROfLQ.js` 含"查看完整算式"）。
- **#357 月度分解「按历史权重 + 已计提月份锁定」**：本会话落地（纯前端，复用 /overview、/months、rebate-summary 接口，零后端改动）。build OK（`Rebate-BRQ2AVFm.js` 93.68kB）→ rsync /opt/hergent-cn-v2 + chown。生产**临时合同往返 E2E**通过：建 2026+2025 临时合同→读 overview/months→保存 12 月各 10000→断言 contract_target=120000→再读→读 2025→summary 含新字段→最后 DELETE 两合同→断言 summary 不再含（零残留）。
- **§4.3 索引补齐 + SQLite WAL 调优**：经代码+生产核验**已落地，无需改动**。`db/connection.py:201` `PRAGMA journal_mode=WAL` + `:206` `synchronous=NORMAL`（生产 `tenant_1.db-wal` 存在即生效）；索引 `idx_rcm_uniq(contract_id,month)` `erp_db.py:585`、`idx_rba_uniq UNIQUE(period_month,dimension,scope_key)` `erp_db.py:650` 均已建。
- **§1.1 合同创建向导化（4 步 + 引用上一年度 + 引用模板）**：本会话落地（前端）。build+deploy 同上（`Rebate-BRQ2AVFm.js` 含"引用上一年度""基本信息"）。**无头 Chrome E2E 全通过**：登录→目标与返利→点「年度合同」子 Tab→录入年度合同→4 步走通（空步校验拦截 / 填 2027+3% / 平均填充→¥120,000 / 预览确认字段正确）→保存（合同入列表）→DELETE id=8 零残留。

### 验证关键事实（可复用）
- 前端 token 存 `localStorage['hergent_v2_token']`，租户由 token 解析（前端不发 X-Tenant-Id）。**UI 按钮登录在本机受代理/SSL 干扰会卡登录页（非代码缺陷）**；改用 token 注入 + 无代理启动 headless 可稳定进入已登录态。
- 返利向导入口在「目标与返利 → 年度合同」子 Tab（非默认仪表盘），E2E 须先点「年度合同」Tab。
- `saveContract` 路径 = `POST /api/rebate-contracts` + `PUT /api/rebate-contracts/:id/months`（与 #357 往返 E2E 同端点，已验证）。
- mptest/Mptest@1（sales，tenant_1）可创建/删除 rebate_contracts；summary 对空租户返回 []（无种子数据），但写操作正常。
- 临时脚本 /tmp/wizard_e2e*.cjs、/tmp/rebate357_*.py 已清理。

### 结论
第一批 9 项（#351~#353 后端 + #354~#356 前端 + #357 月度分解 + §4.3 索引/WAL + §1.1 合同向导）**全部生产就绪**。下一步可进第二批（版本化 tiers 有效期链 / Claim+审批状态机 / 批量计提作业化 / 增量累积 BV），待用户拍板。

## 预报板块审核 — 收尾（B6 + C4/C5，2026-09-01 晚）
- **补齐 35 项中最后 P1 缺口 B6 + P2 令牌违规 C4/C5**（前序 #208-#215 已覆盖其余 P0/P1）。
- B6：Forecast.vue 工具栏加「重置列宽」按钮 + `resetColWidths()`（清 localStorage `hergent-forecast-col-widths` + toast）。
- C4/C5：variables.css 新增 19 个语义令牌（浅+深双套：--sev-risk/warn/info/expired、--st-draft/submitted/approved/rejected/revised、--ok-green、--info-blue、--warn-amber、--confirm-green、--violet）。Forecast.vue 全部散点硬编码（#A32D2D/#BA7517/#9A6B00/#FBEEDA/#7C3AED/#F3E8FF/#FFF3D6/#DDF5E7/#FBE0E0/#E6EAF2/#E6F1FB/#2f9e44/#0e7490/#e0f2fe/#dc2626）→ 对应令牌。
- **交付**：`.workbuddy/artifacts/overview-forecast-audit-closure-20260901.md`（含 35 项终极覆盖映射表）。
- **验证**：干净 rebuild（rm -rf dist）+ rsync --delete 部署；生产 Forecast 分包 JS/CSS 200；prod JS 含 `hergent-forecast-col-widths`+`恢复默认列宽`、CSS 含 `var(--sev-risk)`；Forecast 组件 CSS 旧硬编码色=0（全局 index css 残留 14 处实为 variables.css 令牌声明，非使用处）。
- **35 项最终覆盖**：P0×9 全 ✅；P1×16 中 15 ✅（D5 仅部分，归 H1a）；P2×10 中 C4/C5/D8/D10/D11 ✅，B4（移动端）/B5（可接受不处理）/D5 余量 deferred。
- **部署铁律复用**：本轮纯前端改动，未动后端、未重启；先 rm -rf dist 消除上一轮残留产物再 rebuild，避免 Vite outDir 不清导致的混淆。

## 返利模块优化（"做1"核实后补完，2026-09-01 晚）
- **用户指令**：推进方向1 返利 Rebate.vue 落地，但明确①侧边栏入口现叫"目标与返利"②先看真实代码、已有功能优化不重造。
- **核实结论**：返利模块在 v112/v113 + 本日第一批 9 项已生产级完整落地（4 Tab、5 维度、算式链/试算/详情 Modal、Excel 干跑预览、合同计提结算、月度分解向导全在）。**原记忆"P0 改造"项均已就位，无重造空间**。
- **2 处真实补完式修复（低风险、非重造）**：
  - F1 `Shell.vue:65` 移动端底部 Tab 标签"政策"→"目标与返利"，与桌面入口名对齐（grep 全 src 仅此一处"政策"）。
  - F2 `Rebate.vue:135-138` 维度筛选下拉仅品牌/单品 → 补齐品类/客户/渠道（后端 filteredRules 本就支持 5 维，UI 筛不到）。
- **交付**：`.workbuddy/artifacts/overview-rebate-optimize-20260901.md`。build OK（Shell-mde43wg4.js / Rebate-Bua72scK.js）→ rsync + chown；生产 grep 确认 Shell 含"目标与返利"×2 且无"政策"、Rebate 含"品类维度/客户维度/渠道维度"。纯前端，未动后端、未重启。
- **经验**：记忆/规划文档里的"待做 P0"可能因后续迭代已落地而失效，**凡"落地某功能"类任务必先读真实代码核对现状**，避免重复造轮子。

## 返利 ①④ 核实（"做①④但别重造"核验闭环，2026-09-01 晚）
- **用户指令**：做①计算透明化面板 + ④达成率可视化/预警，但先看真实代码。
- **核实结论：①④（乃至②试算器、第二批 B2-1 版本化）已全部落地、真接线、生产可用，无需写新代码**：
  - ① `POST /api/rebate-rules/simulate`（rebate_rules.py:625）返回 steps[] 算式链 + tiers[]；前端 `simResults/simKey/simulateBatch`（Rebate.vue L1758-1784）消费 → "算式"按钮可展开。
  - ④ 仪表盘进度条+达成率+预警区（#356 已落地）。
  - ② `POST /simulate-batch`（L724）批量试算，前端统一走它、不再镜像算法（docstring 明示）。
  - **版本化已上线**（推翻"冻结"判断）：v114 tier-versions 全链路（L814-896：list/create/get/delete）+ `erp_db._pick_tier_version` 计提按月锁定版本；生产核验全在。
- **新增发现：维度扩展半闭环**——规则层 5 维 ✅（rebate_rules.py:48 DIMENSIONS + 存储 CHECK + v113 扩展），但**达成填报层 `rebate_achievements.py:95-98` `_norm_dimension` 仅认 brand/product，L130 `or "brand"` 静默降级** → 品类/客户/渠道规则填报达成会被悄悄写进 brand（返利算 0 + 数据污染）。修复约 1 人日（扩 3 维映射 + 非 5 维 422 拒绝），属③范畴待拍板。
- **交付**：`.workbuddy/artifacts/overview-rebate-verify-20260901.md`。本轮零代码改动（核实闭环）。
- **经验**：上轮"版本化冻结/维度仅2维"两判断均已被代码更新推翻 → **判断类结论须与代码现状二次对齐，勿沿用历史结论**。

## 达成填报维度静默降级 Bug 修复（"现在修"拍板，2026-09-01 晚）
- **根因**：规则层已 5 维（v113 §1.3），达成填报层 `rebate_achievements.py` 仍 `DIMENSIONS=("brand","product")` + `_norm_dimension` 只认 2 维 + upsert/import 两处 `or "brand"` 静默降级。
- **危害链（已核实）**：`erp_db.accrue_rebate` 按 `(period_key, dimension, scope_key)` 3 元组匹配达成（L9179）→ category 规则达成被降级写 brand 后计提**恒查不到 → 返利恒算 0**；同名 brand 规则还会被污染。前端 `buildAchvRows` 为 5 维规则生成行后行内保存即触发。
- **修复（1 文件 4 处）**：DIMENSIONS 扩 5 维（对齐 rebate_rules）；`_norm_dimension` 补全 5 维中英文别名（品类/类目/客户/经销商/渠道，尾空格容错）；upsert 非法维度 → 422 拒绝；import 非法维度行 → skip + errors 提示（保持逐行容错）。
- **验证**：本地冒烟 40/40（复刻 3 元组匹配语义）；生产 E2（sales mptest）：category 落库正确 / xxx→422 / 中文别名 200 / 列表过滤生效 / 测试数据零残留。
- **部署**：分支领先生产 15 commit → 拉生产 diff 确认 29 行差异 100% 本轮修复 → 备份 /root/backup_rebate_dimfix_20260901/ → 精确 scp 1 文件 → restart → health 200。
- **注意**：降级污染的历史数据无法自动追溯（此前未建品类/客户/渠道规则则无污染），建议人工对账时抽查。
- **交付**：`.workbuddy/artifacts/overview-rebate-dimfix-20260901.md`。全链路（规则 5 维→填报 5 维→计提 3 元组匹配）现闭环，前端无需改动。
- 顺带确认：supervisor（mptestsp）无 `sales` 模块权限（`_DEFAULT_PERMS` 仅 dashboard+data），rebate-achievements 属 sales 模块 → **E2 需用 sales 账号（mptest），非权限 bug**。

## 返利「第二批 4 项（中期）」端到端落地并生产验证（2026-09-01 晚）
按"要"授权，第二批全部交付 + 生产 E2E（18 步全绿，零残留）。

### B2-1 阶梯版本化有效期链（#367，高优先级）
- **后端**：`erp_db.py` 租户补丁新增 `rebate_tier_versions` 表（rule_id/version_no/effective_start/end/tiers_json/scale_type/rounding_mode/rounding_digits/note）+ `rebate_accruals` 补列 `tier_version_id`/`tiers_snapshot`。
- **逻辑**：`accrue_rebate` 计提时按月份经 `_pick_tier_version` 锁定当时生效版本（`_rule_with_versioned_tiers` 应用版本 tiers/计法/舍入），写 `tier_version_id`（单版本命中）或 `tiers_snapshot`（多规则）；overview 试算同样按月份锁定版本。`routers/rebate_rules.py` 新增 list/create/get/delete 4 个 `tier-versions` 端点（归 sales）。

### B2-2 Claim + 审批状态机（#363）
- **后端**：`rebate_claims` 表（contract_id UNIQUE + CHECK 状态机 none/submitted/approved/rejected/reversed）。`submit_rebate_claim`→submitted；`review_rebate_claim(approve→settle_rebate / reject / reverse→uns_settle_rebate)` 门控驱动。端点 `claim`(GET/POST) / `claim/review`(POST) / `claims`(GET)（server.py:3824-3858，归 sales）。

### B2-3 批量计提作业化（#366）
- **后端**：`batch_accrue_rebate(month)` 遍历租户全合同调用 `accrue_rebate`（单合同失败不影响其余）。端点 `POST /api/rebate-contracts/batch-accrue`（server.py:3861）。

### B2-4 增量累积返利余额 BV（#364）
- **后端**：`contact_rebate_balance(db, contact_id)` 实时聚合该联系人全部合同 `receipt_id=0` 计提合计（无需持久化表，避免双写漂移）；`get_rebate_summary`/`get_rebate_contract_overview` 暴露 `contact_rebate_balance`；端点 `GET /api/rebate-contracts/:cid/balance`（server.py:3875）。

### 前端（#365，Rebate.vue）
- 合同卡：claim 状态徽标（submitted/approved/rejected/reversed）+ 申领/审核按钮；月度 totals 显 `contact_rebate_balance`；accrue 详情显 `tier_version_id` 出处；规则表「版本」按钮 + 版本管理 Modal（list/create/delete）；工具栏「批量计提本月考」按钮。build OK → rsync /opt/hergent-cn-v2 + chown。

### 生产验证（真实部署代码 + tenant_1 库）
- **DB 函数级 E2E（18 步全 PASS）**：临时合同(contact 990001)+规则+2 阶梯版本(H1 011-0630 / H2 0701-1231)→计提 2026-03 锁 V1、2026-09 锁 V2（tier_version_id 断言通过）；balance=25.0；claim submit→submitted→approve→settled(ar 生成)→balance 归零→reverse→uns_settle→balance 恢复；隔离租户 9999 跑 batch_accrue（ok=1/1, rebate 15.0）；`try/finally` 兜底清理，最终 `contracts=0 rules=0 accruals=0` **零残留**。
- **HTTP 层冒烟（mptest 真实登录）**：`/api/rebate-claims` 200、`/api/rebate-contracts/1/balance` 200、`/api/rebate-rules/1/tier-versions` 200、`/api/rebate-contracts/batch-accrue` 200 → **RBAC(sales) 放行新端点、路由已上线**。

### ⚠️ 待确认事项（发现，非 B2 阻塞）
1. **新租户 onboarding 潜在 bug**：`tenant_db_init` 新建租户库 `rebate_contracts` 缺 `brand_id`/`scale_type`/`rounding_mode`/`rounding_digits` 补列（tenant_1 因历史部署已有，未触发）；新客户开库后 `create_rebate_contract` 会报 `no column named brand_id`。建议补 `tenant_db_init` 走与现有租户相同的补列补丁（约 0.5 人日）。prod 当前无影响。

### 结论
返利模块对标国际 ERP 的「短期 9 项 + 中期 4 项」**全部生产就绪**。剩余路线（多组织/多币种）按前序报告"按需触发、暂不投入"。待确认项 1 建议单独排期修复。

## 待确认项 1 修复：新租户 onboarding 缺 rebate 列（2026-09-01 晚，用户拍板"需要"）
- **根因（精确）**：`tenant_db_init`（erp_db.py:10316）复制 **master schema** 建新租户库；master 的 `rebate_contracts` 建表（:2812）仅 7 基础列、`rebate_target_rules` 建表（:975）仅基础列，均缺 `brand_id/scale_type/rounding_mode/rounding_digits`（contracts 还缺 contact_name/tiers_json/contract_type/settled_amount/settled_date/receivable_id，rules 还缺 contract_id）。这些列靠 :616-633 的**租户补丁**补齐，但该补丁只遍历**已有**租户库，`tenant_db_init` 不调用 → 新租户库全缺列。tenant_1 因历史部署走过补丁才有。
- **修复（1 函数 + 1 调用）**：新增 `_ensure_rebate_columns(tenant_path)`（幂等 ALTER 14 条：contracts 10 列 + rules 4 列，复用 :617-633 同款 DDL），在 `tenant_db_init` 内 `_migrate_v68(tenant_path)` 之后调用。`py_compile` OK。
- **部署**：`bash deploy.sh`（rsync server/ → /opt/hergent-erp 携 --exclude='*.db' --exclude='.env'，不动生产机密与数据）→ chown → restart。重启后 health HTTP 200（`deploy.sh` 内 sleep 3 略短于 startup ~4s 故初始 curl 000，二次直连 200，服务正常）。日志 MIGRATION WARN/FAILED/hr_employees 均为历史既有非致命项，与本次无关。
- **生产 E2E（隔离租户 9997，零残留）**：`tenant_db_init(9997)` → `rebate_contracts` 17 列全齐（含 brand_id/scale_type/rounding_mode/rounding_digits）、`rebate_target_rules` 24 列全齐；直接 INSERT 带 brand_id+scale 列 OK；刻意删 `tenant_9997.db` 回收。**原"create_rebate_contract 报 no column named brand_id" 已消除**。
- **结论**：原待确认项 1 已闭环。新客户开库后建返利合同/规则不再 500。建议今后任何 onboarding 新列的改动，同步在 `tenant_db_init` 补（与租户补丁双保险），防再漏。

## 侧边栏「目标与返利」图标与「定时任务」重复 → 最终换靶心（2026-09-01 晚）
- **问题**：`src/components/Shell.vue` 桌面侧边栏(L41)、移动端底部 Tab(L65) 的「目标与返利」与「定时任务」(L46/L78) 共用同一时钟 SVG（`<circle r="9"/> + <path d="M12 7v5l3 3"/>`），四处字符串完全一致 → 两个入口图标一样。
- **过程**：先换为奖杯 trophy 并部署；用户改选「靶心」语义更贴"目标"。
- **最终修复**：仅改「目标与返利」两处（桌面 L41 + 移动端 L65）为**靶心 target** 同心圆图标（外 r=9 / 中 r=5 / 内点 r=1.5，加 `stroke-linecap/linejoin="round"`，与现有 lucide 风格一致）；「定时任务」保留时钟（语义正确）。
- **部署**：`npm run build`（产物 Shell-X5VG1DlH.js）→ rsync -a --delete dist/ → root@47.113.224.140:/opt/hergent-cn-v2 → chown hergent。编译后格式校验（SFC 属性编译为 `r:"5"` 而非源 `r="5"`）：本地产物与远程均 `r:"5"`×2、`r:"1.5"`×2（两处靶心）、奖杯(`M8 21h8`)残留 0、定时任务时钟(`M12 7v5l3 3`)保留。前端纯静态无 .env 风险。

## 侧边栏宽度可拖拽调整（2026-09-01 晚）
- **需求**：侧边栏偏宽 → 支持鼠标拖动右缘自由调宽，实时更新，设 min/max 限幅。
- **改动**：`src/components/Shell.vue` ① `.body` 加 `position:relative`；② 侧栏后插入 `.sb-resizer` 绝对定位手柄（left:var(--sidebar-w)，hover/拖拽显青色指示线），`v-show="store.ui.sidebarOpen"`（折叠/移动端隐藏）；③ 拖动逻辑 `startResize/onResizeMove/stopResize`：mousedown 记起点+起点宽，mousemove 改 `:root` 的 `--sidebar-w` 实时、clamp 限幅，mouseup 持久化 localStorage；④ 拖拽中 `.shell.resizing .sidebar{transition:none}` 关闭 .2s 过渡保证跟手，并禁 body 文本选中/设 col-resize 光标；⑤ min=180 / max=420；onMounted 读 localStorage 恢复宽度。
- **部署**：npm run build OK（Shell-DM2Sxaun.js）→ rsync --delete dist/ → /opt/hergent-cn-v2 → chown hergent。远程核验：Shell.js 含 `sb-resizer`、Shell.css 含 `sb-resizer{position:absolute;...left:var(--sidebar-w)...}` + `:hover:after` 青线 + `shell.resizing .sidebar[data-v]{transition:none}`。
- 纯前端，无需动后端；默认宽仍 248px(源 variables.css)，用户可拖至 180–420。

## 战略咨询：Hergent 未来路径（2026-09-01 晚）
- 用户疑虑：通用 AI(WorkBuddy/豆包/DeepSeek)+ERP 厂商 AI(金蝶/用友) 两头夹击，Hergent 还有必要自研 AI/保留 Hermes 吗？是否该做"被通用 AI 调用的 ERP 插件"？
- 结论（已答复）：①"夹在中间"是缝隙非危险，危险的是去做通用能力；②"做工具被调用"方向错——工具化=放弃服务关系/订阅/留存/护城河，只可作获客杠杆；③Hermes 保留但重定位=行业配方运行载体，配方(L2)+数据接入(L3)永远自研，壳可迁移；④通用 AI 越强行业垂直层越值钱。
- 分层模型（自研边界）：L4 服务形态(副驾/简报/微信推送)=自研·收入；L3 数据接入(Excel/连接器/舟谱模板)=自研·地基；L2 行业配方(货损/返利/预报/工资)=自研·护城河；L1 Hermes 引擎(记忆/Skills/定时/多平台)=自研壳·接通用模型；L0 基础模型=采购不自研。
- 关键事实：舟谱无开放 API→"通用 AI 连 ERP"对无 API 客户不成立，Hergent 客户恰在此死角。
- 行动建议价值排序：★配方层打磨 > ★服务形态守住 > ★数据接入普适化 > ☆可选公开 MCP server 获客。
- 已整理存档：`Hergent-未来路径备忘-2026-09-01.md`（项目根目录，含分层模型/三方盲区/Hermes去留/行动建议★排序/一句话总结）。

## 行动建议 × 代码现状差距分析（2026-09-01 晚）
- 交付：`Hergent-行动建议差距分析-2026-09-01.md`（对照表+3点前置工作+执行顺序）。全部结论基于真实代码/生产核验，非推测。
- **关键事实（代码证据）**：
  1. 配方层"部分满足"：后端4套配方引擎齐备（loss_workflow.py:23-30 DEFAULT_RECIPE / payroll_workflow.py / forecast_config.py:64,71,346配方模板库,961配方市场 / erp_db 返利规则引擎+版本化阶梯）；生产4个行业 skill 包存在 `/root/.hermes/skills/hergent-milk-{expiry,forecast,commission,rebate}/SKILL.md`（43/38/37/38行，真 know-how）。缺口=**配方参数↔Skill提示词口径不同步**（提示词静态文本，改配方后 AI 说一套算一套；4个 skill 仅2个提及配方）；**本地无行业 skill 包**（只有 architecture/collections/erp-tools，开发机无法调试）。
  2. 服务形态"部分满足"：scheduler.py:104-106/657 每天8:05简报、:107-109预报催单、:212/:230/:265/:282 多类主动提醒、notify/wecom_notify.py:63 + feishu_notify 推送。缺口=**多租户简报未实现**（scheduler.py:662 自注"多租户需另循环"，商业化硬阻塞）；**微信/小程序订阅消息推送缺失**（仅 forecast_submissions.py:163 一个标记）。
  3. 数据接入"部分满足"：Excel 摄入真实可用（import_router.py:442/530/1156/1168/1274）；舟谱模板真实（forecast.py:390 逆向2026-08真实样本、:671生成、:723端点）；金蝶连接器真实（kingdee_connector.py:66-153 OAuth + :190 客户端13类对象 + :316映射）；畅捷通 chanjet_connector + 探路PoC。缺口=**datasource_adapter.py:122-130 ExcelUpload/Kingdee 两 Adapter 仍是桩**（raise NotImplementedError，:167 _ENABLED 全 False），实际仍直写本地表、解耦未成立；**用友连接器未实现**（仅 :11 注释提及）。
  4. 对外 MCP "未实现"：仅内部先例 /opt/hergent-mcp-spreadsheet/server.py（193行生产在跑），无对外公开包。
- 执行顺序：Step1 多租户简报(P0) → Step2 口径同步(P0) → Step3 Adapter去桩(P1) → Step4 用友(需先探路,可能不可行) → Step5 对外MCP(低,仅获客且防 know-how 泄露)。

## Step1 多租户服务形态落地（2026-09-01 晚，生产已验证）
- 交付报告：`Hergent-多租户服务形态落地报告-2026-09-01.md`；改动全在 `hergent-erp/server/scheduler.py`。
- **背景**：差距分析判定「多租户简报未实现」= 商业化硬阻塞（第二家客户收不到简报）。代码勘察发现缺口远不止简报：11 个业务 monitor 全无租户遍历、推送走全局 env webhook 会串群。
- **改动**：
  1. 简报拆三段：`_build_copilot_brief_lines`(657,当前租户聚合) / `_push_tenant_channels`(717,租户凭证优先·配过渠道绝不回落全局防串台) / `_push_copilot_brief_current`(765) / `_push_copilot_cards_brief`(785,遍历)。标题带租户名。
  2. 新增通用包装器 `_run_per_tenant(fn,label)`(212)：只 set_tenant_context→fn()→finally 复位，不动 monitor 内部；12 个业务 monitor 全部套壳(30-117)。**刻意未动**备份/session清理/webhook投递/慢查询（多在主库，套壳会破坏行为）。
  3. 给无节流的 _check_low_stock/_check_overdue_receivables 加节流 1800s/3600s（原每分钟无条件跑且无去重=刷屏，多租户后按租户数放大）。
- **★修掉两个生产真实 bug（`_tenant_ids()` 852，同时影响 _push_forecast_reminder 与 _check_channel_health）**：
  - **BugA 重复推送**：原 `replace("tenant_","").replace(".db","")` 把异常命名的 `tenant_tenant_1.db` 解析成 id=1 → 与 tenant_1 撞号 → **真实租户每次收 2 条简报、预报催单也重复推**。改严格 `re.fullmatch(r"tenant_(\d+)\.db")`。
  - **BugB 主库误写**：`set_tenant_context(0)` 中 0 被当无租户 → 回落主库 erp.db → 业务 monitor 在主库执行（跨租户污染）。改排除 tid<=0。
  - 教训：文件名解析必须 fullmatch 而非 replace；上下文函数要防 0/falsy 被当"无值"。
- **生产租户库现状**：tenant_0/1/2/3/4/5/7/8/10 + 异常 `tenant_tenant_1.db`(10MB完整库,疑为副本,勿直接删)。tenant_2/3/4/7/8 为历史旧版建的僵尸测试库（仅 rebate_contracts，缺 message_center/cron_tasks/price_lists/erp_contracts），遍历后每分钟约20条 `no such table` 噪音（fail-closed 无害）。注意 `message_center` 等表其实已在 `tenant_db_init`(erp_db.py:10300-10310) → **新库没问题，只是老库未补**。
- **验证**：生产 E2E 隔离租户9996，`ok=21 fail=0`，零残留（租户1消息按 rowid 精确回收、隔离库删、主库 tenants 记录清）。服务 active / HTTP 200。
- **待确认**：①僵尸测试库处置（A清理/B幂等补表/C维持）；②`tenant_tenant_1.db` 与 tenant_1.db 差异待查，勿直接删；③节流间隔是否接受（低库存30min/逾期1h）。

## Step2 配方口径同步（方案 B，2026-09-01 晚，生产已验证）
- **目标**：消除「后端算的」与「AI 说的」两套文本口径漂移——让 4 个行业 skill 包的口径与后端实时配方同源。方案 B（已确认）= 生成脚本重写静态 skill 包 + 配方保存事件自动触发，信任押确定性管道而非 LLM 运行时自觉调 API。
- **交付**：`hergent-erp/server/recipe_sync.py`（确定性管道：读 tenant_1 `workflow_recipes`/`forecast_config` → 渲染 4 段 `AUTO_RECIPE` markdown → 正则替换 4 个 skill 文末标记）；3 个保存端点 `routers/loss_workflow.py`/`payroll_workflow.py`/`forecast_config.py` 的 PUT `/recipe` 保存后 `trigger_sync_from_service()`（fail-closed）；4 个生产 SKILL.md `/root/.hermes/skills/hergent-milk-{expiry,forecast,commission,rebate}/SKILL.md` 文末加 `AUTO_RECIPE` 区块 + 正文可变态数值改引该区块；sudoers `/etc/sudoers.d/hergent-recipe-sync`（已 visudo 校验）。
- **漂移分级（实读比对，非推测）**：货损 7/cost/0%/1.0 原写死=高；工资起征点 5000 原写死=中；预报出厂默认 1.2/2-3/5 天仅存 skill 文本=低；返利 erp_db 引擎常量 graduated/half_up/2=低。高/中已同步，低已渲染当前配置。
- **★生产权限陷阱（关键排雷）**：后端服务 `User=hergent`，而 `/root/.hermes/skills` 是 `root:root 700`（且 `/root` 本身 700，hergent 无法遍历）→ 服务进程直接写会 `PermissionError` 静默失败（try/except 吞掉，skill 永不更新）。**修复**：服务经免密 sudo 委托 root 运行 `recipe_sync.py` 完成写入（`trigger_sync_from_service()` 内 `subprocess.run(["sudo","-n","/usr/bin/python3","/opt/hergent-erp/recipe_sync.py"])`）；sudoers 仅放行这一条精确命令。手动 root 运行 + hergent 经 sudo 运行均验证可写，hergent 直接写报 Permission denied（证明委托必要且生效）。
- **验证**：recipe_sync.py 独立(root)填充 4 块成功；3 个 router 触发已部署(grep 各 2 处)；以 hergent 身份 `sudo -n recipe_sync.py` 退出码 0、skill 同步时间更新；业务数据零改动（`workflow_recipes` 仅读，当前无自定义配方走兜底默认）；服务健康 HTTP 200。本地单测用阈值=5/过期系数=2.0/自定义预报配方验证渲染与正则替换正确。
- **待确认**：①生产 Web 把货损阈值 7→5 保存一次，肉眼确认 SKILL.md AUTO_RECIPE 变 5（自动化链路已逐项验证，仅此 HTTP 往返需账号点一次）；②Hermes 若常驻内存缓存 skill，需触发一次 skill 重载才读新口径（文件已同源）；③多租户需对目标租户各跑一次（已支持 `--tenant-id`）。
