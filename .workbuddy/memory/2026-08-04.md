
### 全站清理页内'← 返回'按钮（commit 0230bda, bundle main-kXK3MFNn.js, v107.0）
- 用户反馈: 顶部 tab × 关闭 + 侧边栏导航已覆盖返回路径, 页内'← 返回'冗余; 上一轮(v106.9)我加的 7 处全部回退
- 清理: app.js 7 处 refData 系列(商品分类/品牌/单位/通用/价格体系/标签/上次售价); 改为直接渲染 toolbar/list, 不再有辅助行
- 保留: product-form 商品创建/编辑全屏表单页(无 tab, 需返回)+ review-queue 审核详情子页(无 tab, 需返回审核列表)
- 教训: 上一轮我判断'保留 ← 返回作辅助元素', 用户本次明确否决——tab × + 侧边栏已足够, 不需要冗余. 用户偏好极简, 宁可少功能也不要冗余按钮
- 验证: node --check ✓ app.js ← 返回残留 0 ✓ 构建部署 ✓ 线上 main-kXK3MFNn.js 200 ✓ 备份 /tmp/static-backup-20260804-072809.tgz

### 登录页安全与可用性修复（commit b1da8cf, bundle main-DKZBNz33.js, v107.1）
- 🔴 安全: 删"测试账号 admin / admin123"明文; placeholder admin/admin123→请输入用户名/请输入密码
- 🟠 可用: 必填星号; autofocus; maxlength(32/64); 登录按钮空表单禁用(updateLoginBtn); 忘记密码链接(toast 联系管理员)
- 🟡 a11y: 修复 #togglePwd 死按钮(无事件绑定)→全局 click 委托 toggle; aria-label/title; _eyeOpen/_eyeOff 图标切换; #loginError role=alert+aria-live; .login-btn:disabled
- 注意: doLogin 已有 loading/错误恢复, 无需再加
- 验证: node --check ✓ 敏感残留 0 ✓ 构建部署 ✓ 线上 main-DKZBNz33.js 200 ✓ 备份 /tmp/static-backup-20260804-081100.tgz

### 会话过期回登录 + URL 同步（commit 17e1e72, bundle main-274d6AeW.js, v107.2）
- api-client 401: 清 token 后弹回登录 overlay + hash #/login(此前静默, 操作全失败)
- showLoginOverlay() 统一登录显示(init/会话失效) + hash 同步; afterLogin hash 回正 #/dashboard
- 审查结论: 后端登录锁定已实现(auth.py _check_login_lockout 5次/15min→30min锁 429+原因, doLogin 已展示); 统一"用户名或密码错误"文案是防枚举正确做法; showRegister 非死链
- 验证: node --check ✓ 构建部署 ✓ 线上 main-274d6AeW.js 200 ✓ 备份 /tmp/static-backup-20260804-081413.tgz

### 登录卡视觉重构：深色玻璃拟态（commit 3c4bf26, bundle main-BTbDT__s.js, css main-Dm6D218L.css, v107.3）
- 根因: 浅色主题 .login-card 用 rgba(255,255,255,.02)+backdrop-filter, 浏览器 fallback 走 :root.dark 路径(--bg-elevated:#fff 浅色默认), 渲染成白卡与深黑 #loginOverlay 突兀
- 设计: 确定性深色玻璃(不依赖主题变量, 浅/深主题一致) — 渐变 #1c1e26d1→#12141ac7 + 28px blur + 1px #ffffff14 微亮边 + 4层阴影(inset 高光+25/60+8/24+80px 品牌辉光)
- 输入框: rgba(0,0,0,.32) 深底 + 白字 + 35%白 placeholder + hover/focus 加深 + 品牌色 focus 边
- 按钮: 品牌色渐变 + 白字 + 悬浮 translateY(-1px) + 品牌色阴影
- 错误: 玻璃化(blur 8px)深红
- 链接: 品牌色/半透白/20%白分隔符
- 移除 :root.dark .login-card 浅色强制
- #loginOverlay::before 静态品牌辉光(替换 gridDrift 漂移)
- 验证: node --check ✓ 构建部署 ✓ 线上 200 ✓ 备份 /tmp/static-backup-20260804-082025.tgz

### 登录卡 v107.4 鲁棒化（commit d69e056, css main-DNIHW7Dg.css, js main-CMwaSW-o.js）
- v107.3 仍依赖 backdrop-filter + 半透明 rgba, 浏览器渲染层 fallback 出白卡(用户报告)
- v107.4 放弃 backdrop-filter, 改完全不透明深色渐变 + 5 层阴影(inset 亮边+品牌色外描边+25/60 强悬浮+8/24+80px 品牌辉光)
- 任何环境(浏览器/扩展/低性能/隐私模式)都确定性深色, 不再 fallback
- 教训: 关键 UI 元素避免 backdrop-filter + 超低 alpha 组合, 用不透明 + 渐变高光更稳

### 登录卡 v107.5 终极修复 — 删除 styles.css:3435 旧 .login-card 覆写（commit 4a37568, css main-mJ-ydGqi.css, js main-xYue1Rt_.js）
- 根因(实锤): styles.css 第 3435-3441 行有旧 .login-card 覆写 .login-card{background:var(--bg-elevated)...} 在第 72 行新主规则后, 同 specificity 后者覆盖前者 → 浅色主题 var(--bg-elevated)=#fff 渲染白底
- v107.3/v107.4 部署完全正确, 但被这条旧规则静默覆盖
- 教训: 改 CSS 时必须 grep 该选择器的**所有出现位置**; 多个 .login-card 规则会导致 specificity 战争
- 验证: 线上 .login-card 唯一规则 = linear-gradient(160deg,#1c1e26,#12141a 60%,#0e1015), 应渲染深色

### 登录页设计系统一致性审查（2026-08-04 上午，未改代码）
- 报告: laozhangai-product/登录页-设计系统一致性审查-2026-08-04.md
- 结论: 色彩/字体/布局 3 维度一致; 4 处低风险差异:
  ① #loginOverlay::before 用品牌青 rgba(6,182,212,.08) 非令牌色(建议改 --pl rgba(37,99,235))
  ② .login-btn 圆角 10px 不在令牌刻度(建议 --radius-md 12px)
  ③ .login-card input 圆角 10px(建议 12px)
  ④ 登录卡阴影硬编码(可选前置 --shadow-xl 令牌)
- 登录卡深灰渐变 #1c1e26→#0e1015 与产品浅色主题差异=品牌门面决策(用户拍板), 非冲突
- 待用户确认后实施(未动代码)

### 登录页设计系统一致性统一已实施（commit e206d99, css main-Bz5xrLdH.css, js main-DS1521ZR.js, v107.6）
- 4+1 处令牌对齐: ①#loginOverlay::before 品牌青→rgba(37,99,235,.08)(--pl) ②.login-btn 圆角 10→12px(--radius-md) ③.login-card input 圆角 10→12px ④.login-card 阴影前置 var(--shadow-xl)(保留品牌色 inset 描边) ⑤主背景第二处品牌青 rgba(6,182,212,.12)→rgba(37,99,235,.12)
- 验证: 登录区 10px 圆角残留 0; 品牌青残留 0(仅 6577 行 AI 工具 chip 保留, 与登录无关); 线上 .login-btn/.login-card input border-radius:12px 确认; 备份 /tmp/static-backup-20260804-083* 

### AI 员工头像丢失修复（9 个真实头像，2026-08-04 上午，无 commit）
- 症状: 大秘/会计/订单文员/运营主管/程序员/作家/编剧/私教/客服 等 9 个 AI 员工头像空白
- 根因(实锤): 服务器 /opt/hergent-erp/static/avatars/ 8 个 .png 全部 = 1×1 透明占位 PNG(70B); teacher.png 完全缺失(app.js:1122 AV 数组第 9 项引用)
- onerror 不触发原因: 1×1 PNG 加载"成功"(HTTP 200 + 完整 PNG 头), naturalWidth=1 但 onerror 只在请求失败/解码错误时触发
- 修复: PIL 12.3 生成 9 个 256×256 品牌色圆形渐变头像(径向渐变 + 苹果顶部高光 + 白色首字), 与产品令牌色系一致:
  dami(秘/品牌蓝) accountant(计/绿) secretary(订/橙) manager(营/品牌青) programmer(程/紫) writer(作/玫红) screenwriter(编/红) tutor(教/青绿) teacher(师/靛蓝)
- 上传服务器替换 8 个 + 补 teacher.png; chown hergent:hergent + chmod 644
- 验证: file 显示 9 个全 256x256 RGBA; 线上 HTTP 200 + content-type image/png + content-length 24-30KB; 备份 /tmp/avatars-backup-20260804-083959.tgz
- 加载逻辑 0 改动(无需 commit): 全部 <img src="avatars/xx.png"> 自动恢复
- 教训: <img onerror> 无法捕捉"加载成功但图片无内容"(1×1 占位), 如需更鲁棒应在 onload 检 naturalWidth<=1 才显示

### 移动端用户头像对齐桌面版（commit 520f6b1, v107.7, mobile.html 单文件）
- 差异: 桌面版 .tb-user(pill 容器)+.tb-avatar(30px 圆形, linear-gradient(135deg,var(--p),var(--pl)) 品牌渐变底+白首字 12px/700); 移动端 #userInfo 纯文本无头像
- 改动: HTML #userInfo 内加 mAvatar(tb-avatar)+mUserName; CSS .topbar .user pill 化(半透明底+圆角20px)+.tb-avatar 渐变圆(与桌面 1:1)+.m-user-name 姓名; JS 两处登录/自动登录设 mAvatar 首字(toUpperCase)+mUserName
- 技术栈/结构不变, 仅用户区; 验证: node --check ✓ 构建含 5 处引用 ✓ 部署 ✓ 线上 mobile.html 200 + 3 处 mAvatar
- 备份: /tmp/static-backup-20260804-0846*

### 编辑商品页双主题对比度审查（2026-08-04 上午，仅报告未实施）
- 报告: laozhangai-product/编辑商品页-双主题对比度审查-2026-08-04.md
- 根因: styles.css:6110-6130 的 .pf-unit-card/.pf-price-table/.pf-input/.pf-label/.pf-hint 等全部硬编码 var(--black)/var(--white)/--gray-*, 完全忽略主题变量
- 浅色主题问题(截图实证): 三个单位卡片黑底在白底页面突兀 / 输入框黑底白字 / 标签 gray-300 对比度仅 1.6:1(WCAG AA 不达标)
- 深色主题问题(代码推断): 卡片与 --bg-deep 同色仅 8% 白边辨识 / 输入框 4% 透明白底对比度 < 3:1
- 改造方案: 单文件 styles.css 约 20 行替换(用 var(--bg2)/var(--bg3)/var(--t1-t3)/var(--bd) 替代硬编码)
- ROI: 高, 0 JS 改动, 5 分钟解决双主题 6+4 类问题
- 待用户确认是否实施

### 编辑商品页双主题对比度统一已实施（commit a7e39cf, css main-BgZap-b5.css, v107.8）
- 根因: .pf-* 组件体系硬编码 var(--black)/var(--white)/gray-300/600/rgba(white) 忽略主题变量
- 改动: styles.css 单文件 ~14 处主题令牌替换(.pf-unit-card/.pf-price-table/.pf-rich-editor→var(--bg2); .pf-input→var(--bg3)/var(--t1)+::placeholder var(--t3); 标签类 gray-300→var(--t2/t1); 提示 gray-600→var(--t3); .pf-image-card→var(--bd2)/var(--bg3); .pf-switch-row→var(--bd2))
- 保留合理: .pf-img-del(图片删除钮黑底白字, 覆盖图片上) / .pf-toggle-slider(开关轨道中性色)
- 效果: 双主题卡片/输入框/标签融入页面, 对比度达 WCAG AA 4.5:1+; 无 :root.dark 冲突覆写
- 验证: 构建部署 ✓ 线上 .pf-unit-card=var(--bg2) ✓ .pf-input=var(--bg3)/var(--t1) ✓ 备份 /tmp/static-backup-20260804-0855*
- 报告: laozhangai-product/编辑商品页-双主题对比度审查-2026-08-04.md

### 全站硬编码主题色同类问题修复（commit 5f39b96, bundle main-DDgJZa2x.js, v107.9）
- 同类问题: 与 .pf-*(v107.8) 同模式 — 组件硬编码浅色主题值, 深色主题翻车
- 扫描范围: styles.css 159 处(多数合理品牌用法) + modules/*.js inline + app.js inline + 两 html
- 修复 2 模块 19 处:
  ① expiry-dashboard.js 8 处: 卡片 white→var(--bg2); 表头 var(--white)→var(--bg3); 行分隔→var(--bd2); 预警条 #fef3c7→var(--pb); 筛选钮 #f3f4f6/#6b7280→var(--bg3)/var(--t2); 灰字→var(--t3/t1); 保留数字语义色 #dc2626/#6b7280
  ② module-compare.js 11 处: mc-wrap #f5f5f7→var(--bg); 徽章/表格 #fff→var(--bg3); 首列 #fafafa→var(--bg4); 灰字→var(--t2/t3); 边框→var(--bd/bd2); 保留品牌青/绿
- 合理保留: showPrintPreview/配送单打印/分享卡/二维码/签名画布(打印或外发场景必须白底)
- 验证: node --check ✓ 残留 0 ✓ 构建部署 ✓ 备份 /tmp/static-backup-20260804-0901*
- 报告: laozhangai-product/全站硬编码主题色同类问题修复报告-2026-08-04.md
- 方法论沉淀: 扫同类问题 = grep 'background:(white|#fff|#f5f5f7|#fafafa)' + 'var(--white)' 作背景 + gray-* 文字, 逐处判断场景合理性(打印/外发保留)

### 商品编辑页顶部导航 tab（commit cab7882, bundle main-Be51Fs-I.js, v107.10）
- 需求: 顶部导航栏显示"编辑商品"标识当前位置; 移除页内"编辑商品"标题 + "← 返回"按钮
- app.js: 新增 _setPfTab(title) 创建 id=product-form tab(label 动态=编辑商品/创建商品); navTo 加 product-form 特判(表单开着则停留防丢输入); closeTab 加 product-form 特判(_pfCancel 清状态+渲染列表, 移除 tab, 回 data tab)
- product-form.js: hdr() 返回空(删返回按钮+h2); _openProductForm 调 _setPfTab; _pfSave 保存成功 closeTab 关 tab
- 交互: 打开编辑→tab'编辑商品'(×可关)+表单; 保存→自动关 tab 回列表; tab×→清状态回商品档案; 点 tab(表单开)→停留
- 复用模式: 与 refdata tab 机制一致(v107.2), 独立 id 避免与 pgRefData 冲突
- 验证: node --check ✓ 构建部署 ✓ 线上 main-Be51Fs-I.js 200 ✓ 备份 /tmp/static-backup-20260804-0937*

### 全站页面审核 + 响应式适配修复（commit 66e5a7c, bundle main-BAg_P1LH.js, v107.11）
- 页面清单: 36 个 pg 容器 + 扩展模块(gaap/consolidation/einvoice/forecast/quality/rma/wms/ifrs15/billing 等)
- 扫描 7 类共性问题: A硬编码色(0残留) B模块白底(0) C重复标题/返回按钮(0) D响应式(发现4处!) E敏感文案(0) F布局错位(0) G占位文案(合理保留)
- 修复 D 类 4 模块(固定侧栏窄屏挤压): accounting-standards(240px)+consolidation-ui(280px) 加 flex-wrap:wrap+min-width; contacts-v2(300px) 加 data-mobile 模式(innerWidth<=768, 列表全宽45vh+下边框); report-designer(200px) flex-wrap
- 判定保留: console.log 6 处为模块版本横幅/功能调试(offline-sync replay/payment-run 数据); "开发中"占位为 archive-hub/operations-hub 收敛策略; product-form AI 按钮占位
- 方法论: 扫响应式问题 = grep 'width:Npx;flex-shrink:0'(固定侧栏) + 确认无 flex-wrap/min-width/@media 兜底
- 验证: node --check ✓ 构建部署 ✓ 线上 bundle 18 处响应式代码 ✓ 备份 /tmp/static-backup-20260804-0945*

### 全站直渲页导航 tab 统一（commit 40d175d, bundle main-DYcAJc0g.js, v107.12）
- 需求: 商品编辑页导航改造(v107.10)后全站检查同类问题 — 不经 navTo 的直渲页无顶部 tab
- 发现 2 个同类: 审核中心(review-queue, 5 处 page-hd 重复标题) + 通讯录(contacts-v2, 无 tab)
- 修复: app.js _setPfTab 泛化为 _setNavTab(modId,title)(兼容保留); navTo 加 contacts/review-queue 特判(走 _v2Nav 重新激活); sidebar-v2 两分支调 _setNavTab; review-queue 删 5 处重复标题(保留信任级别按钮右对齐+子页返回)
- 判定不改: chat 走 navTo; hub 面板是 hover 面板; 客户/供应商表单是 modal(自带遮罩)
- 验证: node --check ✓ 构建部署 ✓ 线上 bundle 3 处 _setNavTab ✓ 备份 /tmp/static-backup-20260804-1006*

### 客户/供应商表单改全屏 Page 模式（commit 5f68b99, bundle main-DNR6m9yh.js, v107.13）
- 需求: 用户拍板从 Modal 改全屏交互(对齐商品编辑页 v107.10)
- data-form-enhance.js: showContactForm modalOpen → 渲染 dataContent 全屏(复用 product-form-page/pf-body, 720px 居中); 删 h3 标题(tab 承担); 取消钮 → _dfCancel(清状态+清草稿+closeTab); _setNavTab('contact-form',title) 顶部 tab; 保存成功 closeModal → closeTab 回列表; auto-save 监控 .modal → #dfName
- app.js: closeTab 加 contact-form 特判(同 product-form); navTo 加 contact-form 特判(表单开着停留)
- 效果: 客户/供应商编辑 = 全屏页 + 顶部 tab(编辑客户/供应商) + × 关闭, 与商品编辑一致
- 验证: node --check ✓ 构建部署 ✓ 线上 main-DNR6m9yh.js 200 ✓ 备份 /tmp/static-backup-20260804-1020*

### 新增客户/供应商同一界面修复（commit 878fc2e, bundle main-Od7rFNDM.js, v107.14）
- 症状: mega 菜单'新增客户'/'新增供应商'打开同一表单
- 根因(3层): ①菜单 action 只调 showDataForm() 无类型参数, 依赖全局 _dataTab(默认 products)→走 else=新增供应商 ②btnProducts/Customers/Suppliers 从未被创建(无 id HTML), b.click() 死代码(历史'切tab再开表单'分离逻辑在 696e7e7 删 tabs 时失效, 死代码未清) ③菜单缺'+ 新增供应商'; 保存 body.type 也依赖 _dataTab(会误存)
- 修复: showContactForm(cid,kind) 显式 kind(customers/suppliers) 兜底 _dataTab; _openContactForm(cid,kind) 透传; _editingContactType 记录(保存用); submitContactForm 读 _editingContactType; mega 菜单 3 项显式传类型+新增供应商项+清死代码; 列表编辑按钮(5158/5160)显式 kind
- 教训: 删 UI(tabs) 时必须同步清理引用其 id 的 action 字符串, 否则静默失效; 表单类型应显式传参而非依赖全局 tab 状态
- 验证: node --check ✓ 构建部署 ✓ 线上 main-Od7rFNDM.js 200 ✓ 备份 /tmp/static-backup-20260804-1306*

### 舟谱客户表单 4 区段 + 布局优化(commit 18eb6ab/9f2baed, main-YWm-NKDQ.js, v107.15+107.16)
- 需求: 舟谱'新增客户'截图(30+字段 4区段) → 项目中无复刻代码 → 新做
- 后端: erp_db.py 22 个 _safe_migrate 新列 + 租户回填块(对 tenant_*.db 加 4 列 mnemonic/sales_region/license_name/spec)
- 数据层: contacts.py 白名单扩展 24 字段, 全部用 tenant 库既有舟谱列名(remark1/remark2/level/assigned_route/dedicated_employee_id/service_employee_id/default_warehouse_id/food_license_no/business_format/pos_usage/refrigerator_count/images)
- 前端: data-form-enhance.js showContactForm 重构客户 4 区段(基本+业务+财务+经营), 供应商保持原 4 区段; 字段 id(dfGrade/dfNote/dfPosStatus 等) 与保存 body key(remark1/level/pos_usage 等)解耦, html id 保持稳定仅 body key 适配列名
- 布局优化: .pf-body max-width 720→1080 + padding 16→24/32/80 + box-sizing:border-box → 1440 屏左右空 ~688px→~180px
- 验证: py_compile ✓ / node --check ✓ / 临时 DB 冒烟 22 列命中 ✓ / 生产租户 5 库 4/4 列回填 ✓
- 关键发现: 租户库(tenant_1..5.db) 早已有 v93 时代舟谱列(food_license_no/business_format/pos_usage/refrigerator_count/level/assigned_route/dedicated_employee_id/service_employee_id/default_warehouse_id/remark1/remark2/images 等 25 列), 与主库 erp.db 共享 67 列(主库额外 16 列含我加的 14+debt+tenant_id); 我设计的 22 列名与既有舟谱列**重复且命名不一致**, 必须以租户既有列名映射
- 教训(重要): 添加表列前必须先 PRAGMA table_info 现有库对比, 不能假设空白库! 业务库可能已有半成品/历史迁移的同名/异名列, 直接 ADD COLUMN 会与既有冲突造成数据不一致
- 教训: 后端部署**真实路径是 /opt/hergent-erp/*.py(扁平结构)**, 不是 /opt/hergent-erp/server/*.py(server/ 是遗留目录); WorkingDirectory=/opt/hergent-erp, ExecStart=python3 server.py, 部署前必须 ls 真实文件位置
- 教训: contact_create/update 数据层是白名单(allowed)机制, 前端保存 body 字段名必须与白名单列名一致, 否则被静默丢弃
EOF\necho done- v107.17(commit 4ba2ebd): 用户二次反馈留白 → .pf-body max-width:none + margin:0 彻底铺满内容区(客户/供应商表单); 剩余 720px 属 product-form 商品页保留; 提示: 用户二次反馈大概率是缓存未强刷, 需引导 Cmd+Shift+R 验证
- v107.18(commit be8c154, 紧急): 用户报 RangeError 栈溢出 — _dfCancel↔closeTab('contact-form') 无限递归
  - 根因: v107.13 引入 contact-form tab 时 closeTab 特判调 _dfCancel 清理, 而 _dfCancel 内部又调 closeTab → 互递归; product-form 的 _pfCancel v107.10 已修(不再调 closeTab), contact-form 漏网
  - 修复: _dfCancel 加 _dfCancelling 防递归 flag(try/finally); node 模拟两路径(取消按钮/tab×)无栈溢出 ✓
  - 教训: "A 清理函数调 B, B 特判又调 A"是经典互递归陷阱; 任何 closeTab 特判回调清理函数时, 清理函数内禁止再调 closeTab(或加 flag)

### 供应商表单全字段复刻舟谱(commit e27ef63, main-BgSzzV9t.js, v107.19)
- 需求: 舟谱新增供应商 2 截图 18 字段全复刻(基本信息/状态/经营信息/结算信息 4 区段)
- 后端: erp_db.py 主库 +3 列(supplier_batch_code/bank_account_name/auto_writeoff) + 租户回填 _TENANT_EXTRA_COLS 扩 7 列(4+3); contacts.py 白名单 +3
- 前端 data-form-enhance.js 供应商分支重构 6 区段: 基本信息(舟谱 11 字段含助记码*/类别*下拉6项/联系人*/电话*/所属员工/省市区 3 框/编码/批次码/备注) + 状态(正常/停用→status) + 门店与片区(自研保留: 老板/渠道/门店状态/片区) + 财务(保留) + 经营(执照编号/相关附件) + 结算(银行户名/开户行/账号/自动核销)
- 字段 id 迁移: dfPerson/dfSupplierCat/dfBizLicense 从银行区段迁入基本信息/经营区段(id 不变, 保存映射自动生效); 新增 dfMnemonic/dfEmp/dfProvince/dfCity/dfDistrict/dfBatchCode/dfSupStatus/dfImages/dfBankAccountName/dfAutoWriteoff
- 保存兼容: body.images = dfAttachments||dfImages; body.auto_writeoff parseInt; body.status 默认 active
- 验证: node --check ✓ py_compile ✓ id 引用一致 ✓ 构建部署 ✓ 5 租户库 3/3 新列 ✓ 线上 200 ✓ 备份 /tmp/static-backup-20260804-1445*
- 注: "保存并新增"按钮舟谱有但本项目未加(产品可后补); 类别 6 项为标准项, 用户可自定义

### 供应商表单精简 — 移除门店/财务区段(commit 5501b07, main-BGTvFuvb.js, v107.20)
- 用户反馈: 供应商不需要"门店与片区""财务信息"(老板/渠道/门店状态/结算/赊账/信用是门店客户概念)
- showContactForm: 供应商分支门店与片区 → ''; 财务区段 → isCust ? 客户财务 : ''
- 供应商表单最终 = 舟谱 4 区段(基本信息/状态/经营信息/结算信息), 客户不受影响
- 关键修复: submitContactForm 保存段 fv→_g(缺失元素 undefined) + _put(undefined 跳过) — 供应商编辑时缺失字段不覆盖已有值(region/settlement_method 等); 数字字段仅在元素存在时 parse
- 教训: 表单按类型动态渲染后, 保存逻辑必须做"元素存在性"守卫, 否则移除区段会静默清空 DB 既有字段
- 验证: node --check ✓ 构建部署 ✓ 线上 200 ✓

### 编辑商品基本信息空白修复(commit 1e4f870, main-DENqex8C.js, v107.21)
- 症状: 点击编辑按钮进入"新建商品档案"页面(基本信息空白, 无回填)
- 根因(实锤): product-form.js _pfSwitch 每次切换 tab 都 body.innerHTML=[tab1..tab5][tid-1]() 重建 DOM; _pfLoadForEdit 流程"填tab1→切2/3/4/5→最后_pfSwitch(1)"重建 tab1 清空了已填的基本信息
- 修复: _pfLoadForEdit 末尾 _pfSwitch(1) 后重新 _pfFillInput 填充 tab1 字段(1 处 +4 行)
- 模拟验证: 旧流程 tab1 空(复现)/新流程重填正确 ✓
- 教训: "tab 切换重建 DOM"的组件, 异步回填必须放在最终展示 tab 重建之后, 或回填前先切到目标 tab; 排查"编辑空白"类 bug 先看渲染函数是否重建 DOM

### 商品图文详情上传图片卡缩小(commit 517b827, main-Bhc2qEo_.js + main-COozbk2I.css, v107.22)
- 症状: 图文详情页上传图片卡过大(~220px+ 正方形)
- 根因: .pf-image-grid{repeat(3,1fr)} 三卡均分容器全宽, 容器越宽卡片越大
- 修复: grid 改 repeat(3,minmax(0,1fr)) + max-width:440px(桌面每卡 140px); @media(max-width:480px) max-width:none(移动端 ~100px 自适应); 占位符 + 28→22px/文案 11→10px; 视频卡 200×120→160×100
- 交互零改动(点击卡 input.click / _pfHandleImg / 删除 PF.images[i]=null 保留)
- 验证: node --check ✓ 尺寸计算 140/100px ✓ 构建部署 ✓ 线上 200 ✓

### 商品图文详情辅图行紧凑化(commit dfe1956, main-Di12Pc9J.js + main-B6kLAx0m.css, v107.23)
- 症状: 辅图行被排成 3 行(辅图 / 0 / 张), 底部独立'张'字
- 根因: tab3 把 辅图 + finp + 张 拼到 .pf-section-title 内, 而 .pf-section-title 是 block + padding-bottom:8px + border-bottom 容器; input.pf-input 默认 width:100% inline-block 在 div 内撑满宽度,把'张'挤到下一行, 跨过底边线
- 修复: tab3 改用 <div class=pf-aux-row>辅图 input 张</div>(flex 紧凑行); CSS .pf-aux-row{display:flex;align-items:center;gap:8px;margin:16px 0 12px;padding-bottom:8px;border-bottom:1px solid var(--bd);font-size:14px;font-weight:600;color:var(--t1)}; input#pfAuxCount{width:70px;flex:none}
- 教训: 不要把 form input 塞进 .pf-section-title(或其他带 border-bottom 的容器) — input 默认 width 100% 会撑断布局; 用 flex 行级布局
- 同时: 用户截图里出现一个'客户'超大输入框, product-form.js grep '客户' 字面 0 命中(确认非代码引入), 判定为浏览器旧 bundle 缓存, 强刷可消失

### 档案联动打通(commit 12126d7 + 63a235b, main-gB20Aani.js, v107.24+24b)
- 需求: 商品档案'单位'字段联动单位档案(点击弹出单位列表选择); 全站排查所有本应联动未联动字段
- 排查结论: 单位字段一直是 finp 纯文本, 从未与 /unit-archive 联动(非失效, 是未实现)
- 修复: _pfPickUnit(targetId) 弹窗(搜索+列表[名称+类型+换算比]+点击回填); 后泛化为 _pfPickArchive(targetId, apiPath, labelKey, suffixFn) 通用档案选择器
- 接入: 商品表单 单位×3(pfUnit/pfMedUnit/pfLargeUnit) + 品牌 pfBrand(/brands) + 主供应商 pfMainSupplier(/contacts?type=supplier)
- 数据源同一: 与档案管理共用 GET /unit-archive /brands /contacts → 档案增删改自动同步
- 全站联动清单(未实施): 类别 pfCategory(需确认分类API) / 客户供应商员工字段 dfEmp/dfAssigned/dfServiceEmployee(员工无HTTP路由, 需加 GET /employees) / 省市区(无行政区划数据源) / 供应商类别写死6项(可字典化)
- 验证: node --check ✓ node 模拟 ✓ 构建部署 ✓

### 员工档案联动（commit e8b89b9, main-4rfSq6RH.js, v107.25）
- 需求: 上轮待办第 1 项 — 客户/供应商表单员工字段联动员工档案
- 关键发现(修正上轮结论): GET /api/employees 路由**早已存在**(server.py:3351, RBAC 映射 hr server.py:474), 后端零改动; hr_employees 表在**租户库**(_init_payroll_tables 懒建), 生产 0 条员工; contacts 三员工列(dedicated_employee_id/service_employee_id/assigned_salesperson)**全 TEXT 且既有数据存姓名**(生产实证: 周运潘/田顺达等)
- 实现: data-form-enhance.js 新增自包含 window._dfPickEmployee(targetId) — 读 /api/employees(与员工管理/薪资同源), 弹窗列表[姓名+岗位+工号]+搜索过滤(_dfPickFilter)+点击回填(_dfPickEmpSet), 空态引导去「员工管理」创建; 供应商 dfEmp + 客户 dfAssigned/dfServiceEmployee 三字段加 ▾ 按钮(flex 容器, input flex:1;min-width:0 覆盖 .modal-field input width:100%)
- 决策: 回填**姓名**(非 id) — 三列均 TEXT 且既有数据存姓名, 与手打语义一致; 选择器只写输入框, 保存映射(dedicated_employee_id/assigned_salesperson)零改动
- 验证: node --check ✓ 构建 main-4rfSq6RH.js ✓ 线上 200 + bundle 含 _dfPickEmployee ✓ /api/employees 匿名 401 ✓ 备份 /tmp/static_backup_20260804_160245.tar.gz
- 上轮待办更新: 员工联动 ✅ 完成; 剩余: 商品类别联动(需确认分类 API)/供应商'保存并新增'按钮/省市区级联(P2)/远端 GitHub 同步

### 全站页头工具栏单行化（commit d4dcbb6, main-DDUPA4rJ.js, v107.26）
- 需求: 商品档案工具栏 3 行 → 1 行; 全站逐页审查同类问题一并优化(功能零缺失)
- 核心: 商品档案页头 3 行(搜索行/7筛选行/操作行, 约144px) → 1 行(~44px): 搜索+状态/品牌/分类+查询 同行 + 操作按钮(创建/导入/导出/批量/视图)右置; 次要筛选(主供应商/图片/条码/保质期/标签)收进「更多筛选」下拉(_toggleMoreFilters, 点外关闭); clearAdvFilter 补 afSupplier 重置
- 全站 14 处: 客户/供应商档案、调拨、目标管理、员工交账、员工列表、会计凭证、客户排行Top10、项目管理、固定资产、销售单、采购单、财务管理、市场费用(3→2层)、告警管理 — 统一模式=筛选chips行右置操作按钮/标题行内嵌按钮/删独立 toolbar 行
- ⚠️ 注入兼容: dedup-manager(重复检测)/customer-hierarchy(层级视图) 原注入 .data-toolbar/.tbl-toolbar(已并入单行) → 更新为 .data-filter-row 优先
- 窄屏降级: 全部 flex-wrap + 操作区 justify-content:flex-end 可换行; 合并进 section-title 的按钮加 text-transform:none
- 验收清单: laozhangai-product/全站页头工具栏单行化-验收清单-2026-08-04.md
- 验证: node --check ×4 ✓ 构建 ✓ 部署 ✓ 线上 main-DDUPA4rJ.js 200 + _toggleMoreFilters 在包 ✓
- 方法论: 工具栏单行化统一模式 = ①筛选chips行与操作按钮行合并(space-between) ②单按钮并入标题行(page-hd/section-title flex) ③次要筛选收进'更多筛选'下拉 ④全局检查向旧 toolbar 类注入按钮的模块并更新选择器

### 侧边栏收窄 240→208px（commit 40f5716, css main-CBChXCB9.css, v107.27）
- 需求: 侧边栏比当前更窄, 内容完整不截断, 响应式不变
- variables.css: --sw 240→208px(双主题共用; 折叠态 --sw-min 56px 不变)
- styles.css 8 处内部紧凑化: sb-nav 12→8px / sb-item 12→10px+垂直10→9px / sb-main-item 16→12px+margin 8→6px / sb-label 18→14px / sb-divider 16→14px / sb-toggle margin 16→12px / sb-role-item 18→14px / sb-sub-item 28→24px
- 文字零改动: 16px 图标+10px gap+14px 字号在 208px 内容区(192px)完整显示
- 部署要点: 线上 static/ 无 css/ 目录 — variables.css 是**纯构建输入**(vite 合并进 assets/main-*.css), rsync --delete 历次已删线上 css/ 属正常; 改令牌只改源码再构建即可
- 验证: 构建 ✓ 部署 ✓ 线上 css 200 + --sw: 208px 在包 ✓

### 档案管理弹窗「开发中」项排查恢复（commit 827283b, main-e7mvbLZG.js, v107.28）
- 排查: archive-hub.js(侧边栏 hover 档案管理面板) 9 个 dev 项 → 8 项功能早已开发只是漏接; 2 项(科目/库存成本期初)确缺专用表单
- 漏接清单: 往来标签(contact_tags 已存在) / 角色权限配置(showPermissionManager app.js:7592 完整) / 期初×5
- 期初真相: 前端表单(import-wizard showOpeningBalance)+数据层(save_opening_balance erp_db:6258)已有, 但 /opening-balances/{ar,ap,pre-ar,pre-ap,cashflow} **HTTP 路由缺失 → 404**
- 修复: ①后端 purchase.py 加 POST /opening-balances/{btype} 动态路由(白名单 5 类, inventory/account 拦截) ②archive-hub 接 act(contact_tags/showPermissionManager/showOpeningBalance)
- 部署要点: 生产后端 = /opt/hergent-erp/routers/*.py(非扁平!), scp 后 chown + rm __pycache__ + systemctl restart
- 验证: node --check ✓ py_compile ✓ 路由匿名 401(部署前线上 404 → 部署后 401) ✓ 数据层写入查询 ✓ 线上 health ok
- 报告: laozhangai-product/档案管理弹窗-开发中字段排查报告-2026-08-04.md
- 遗留: operations-hub 姊妹面板 30+ dev 项中大量已实现漏接(收款/付款/预付/对账等), 建议专项核查; app.js:6302 AI 渠道接入确属未开发
- 方法论: 排查"开发中"标注 = ①找数据驱动面板的 dev 标记 ②对照已实现功能入口(openRefData 字典/全局函数) ③区分纯漏接 vs 半成品(缺路由/缺表单) vs 真未开发; 期初类提交 404 是"前端+数据层有、HTTP 层无"的典型半成品

### operations-hub 批量恢复（commit 01128be, main-DGvIiWoM.js, v107.29）
- 承接 v107.28: 姊妹面板(仓配/资金/财务/报表 4 组)同模式排查 — 40 处 dev 实为已实现漏接, 全部接通
- 接通入口: showDeliveryList(配送/线路) / renderStockTake(盘点) / render_assembly(组拆) / showBankReconList(银流水对账)
  / showPaymentForm(收付款/收入单) / showPrepaymentForm / showExpenseForm / showReconciliation('customer'/'supplier')
  / showWriteoffForm / showSettlementForm(员工交账) / navTo('ledger')(凭证/科目余额表) / renderAssets(资产管理)
  / triggerDepreciation(折旧) / navTo('gaap'/'consolidation'/'reports') / navTo('sales'/'buying'/'accounts')
- 保留 dev 85 项: 无独立前端(入库单/成本类/资金转账等, 部分后端有接口)、舟谱对标概念(静态盘占/铺市分析)、报表占位 40+
- 全部带 window.xxx 存在性兜底
- 验证: node --check ✓ 构建 ✓ 部署 ✓ 线上 bundle 200
- 报告: laozhangai-product/operations-hub-开发中项恢复清单-2026-08-04.md
- 建议: 成本类(fund/cost 后端接口已在, 补前端表单可恢复 3-5 项) / 报表类按模块分批落地
- 方法论: 批量恢复面板 dev 项 = 先 grep 候选入口存在性(showXxx/renderXxx/navTo 模块), 分组后整块 Edit 替换, 带存在性兜底防模块未加载报错

### 应收款期初误跳设置页修复 + 系统性跳转核对（commit 9f289c9, main-BvYaTYSV.js, v107.30）
- 症状: 档案管理→应收款期初 点击跳到设置页
- 根因(实锤): archive-hub renderPanel 第 5 列(期初录入=GROUPS[6]) key 的 gi=4, 但 go() 只特判 gi===3(合并列), gi=4 走 else 取 GROUPS[4](员工相关) → 应收款期初(idx0) 映射到 '员工与部门'(showSettings) → 设置页; 自 v105.4 面板创建即有, 期初全 dev 不触发, v107.28 接通 act 后暴露
- 修复: archive-hub.js go() 加 gi===4 → GROUPS[6] 分支
- 系统性核对(模拟脚本): archive-hub 35 项 + operations-hub 152 项(去重后) key→item 映射全部正确 ✓
- 连带修复: ①盘点点写单/资产管理 直写容器(stockContent/reportsContent)未激活则渲染到隐藏容器 → act 先 navTo('stock'/'reports') 再 setTimeout 渲染 ②组拆单/组合拆分汇总表: render_assembly 死代码(assemblyContent 容器+导航都不存在, if(!c)return) → 改回 dev, 需先补容器与导航
- 验证: node --check ✓ 模拟脚本 35/152 ✓ 构建部署 ✓ 线上 200

### 侧边栏删除重复入口 商品档案/客户/财务（commit 0b6bbcd, main-BMMRK4sJ.js, v107.31）
- 需求: 删除「数据报表」后的 商品档案/客户/财务 三个入口(与其它菜单重复)
- 改动: sidebar-v2.js render() 删除 item('products'/'customers'/'finance') 三行
- 重复关系: 商品档案→档案管理面板; 客户→通讯录+档案管理面板客户档案; 财务→财务管理+资金管理
- 安全确认: 全仓无 registerModule 注册这三 id / 无其它 _v2Nav、data-mod 引用; _v2Nav 内特判保留为死代码(防间接引用)
- 结构: 经营业务区 7 项(经营看板/档案管理/采销/仓配/资金/财务/数据报表) 层级完整
- 验证: node --check ✓ item 引用 0 ✓ 构建部署 ✓ 线上 200
- 方法论: 排查"点击跳错页" = ①面板类查 go() 的列→GROUPS 索引映射(列布局与数组索引不一致即错位) ②核对 act 调用的渲染函数是否直写容器(需先激活页面) ③核对目标容器/导航是否存在(死代码) ④模拟脚本遍历渲染+分发验证全部 key 映射

### /api/customer-hierarchy 500 修复（commit 2695af0, v107.33, 后端 erp_db.py）
- 症状: 控制台 /api/customer-hierarchy 500 重复报错(客户层级视图接口)
- 根因(服务日志实锤): erp_db.py get_customer_children SQL SELECT ar_balance/last_order/store_status —
  ar_balance/last_order 是**计算列**(erp_db.py:4315 用 receivables 子查询联表), contacts 无此真实列;
  store_status 租户库也无 → no such column → 500; 主库/租户库全复现(本地验证均 False)
- 修复: SQL 只查基础列 id/name/phone/type/credit_limit(前端 renderTreeNodes 仅用这 5 个)
- 部署: scp erp_db.py + rm __pycache__ + systemctl restart; ⚠️ restart 后须等服务就绪再 curl(启动~3s, 过早请求会 502 误判)
- 验证: py_compile ✓ 本地 get_customer_tree 冒烟 ✓ 线上匿名 500→401 ✓ 日志 0 条 500
- 方法论: 控制台报错先分类 — content.js/元宝AI/YUANBAO/COMPUTE_PAGE_FINGERPRINT/PWA 横幅均为浏览器扩展噪音;
  真 bug 看 /api/ 500 + journalctl 堆栈; 查"列不存在"类 500 先 PRAGMA 确认真实列(计算列/租户缺列是高频坑)

### hover 弹窗与侧边栏无缝衔接（commit 0512ee8, main-DStUZ-cS.js, v107.32）
- 症状: 鼠标从侧边栏移向弹窗, 经过 6px 空隙 → _startLeaveGrace → 弹窗中途关闭
- 修复: archive-hub.js / operations-hub.js show() left 定位 rect.right+6 → rect.right-2(重叠 2px)
- 保持机制复核: 弹窗 top=锚点顶部(水平右滑全程在弹窗区); 文档级 mouseover 委托 PANEL 内 clearTimeout; 离开宽限 120-200ms 兜底
- 经验: hover 面板定位必须与触发元素重叠(≥2px), 任何间隙都会在鼠标过渡时触发离开; 同款代码同步修(archive-hub/operations-hub)
- 验证: node --check ×2 ✓ 构建部署 ✓ 线上 main-DStUZ-cS.js ✓

### 内联 onclick 传参炸裂修复（commit 3ecdcc9, main-BFJrry9a.js, v107.34）
- 症状: 点击商品列表'复制'后 /#/data:1 Invalid or unexpected token(用户提供关键线索: 点击复制后出现)
- 根因(实锤+模拟): 内联 onclick 用 esc() 传用户数据 — esc 把单引号转 &#39;, 但 HTML 属性值在 JS 执行前
  先实体解码回裸引号 → copyText('O'Reilly 酸奶') 字符串提前闭合 → 语法错; 反斜杠/换行同样裸传
  (esc 是 HTML 转义, 不是 JS 字符串转义)
- 修复: utils.js 新增 jsEsc()(先转义 JS 危险字符 \\ ' \r \n 再 HTML 实体化) + window.jsEsc;
  精准替换 11 文件 56 处 onclick 属性内 esc( → jsEsc((onclick 外 HTML 文本 esc 保持)
- 方法论: "点击某按钮后才出现的 SyntaxError" = 内联 onclick 传参转义缺陷; 验证必须模拟完整链路
  (HTML 属性实体解码 → JS 解析), 只测 JS 层会漏判; 排查范围含复制/详情/编辑/拜访等所有传用户数据的内联点
- 验证: node --check 12 文件 ✓ 真实链路模拟: 修复前炸裂/修复后通过 ✓ 反斜杠+换行 ✓ 构建(main+mobile) ✓ 部署 ✓

### 商品列表'复制'误报失败修复（commit 9dc1b7a, main-PszNN8in.js, v107.35）
- 症状: v107.34 修好语法炸裂后, 点复制提示'复制失败'(剪贴板实际已复制成功)
- 根因: copyText 成功分支 el.innerHTML — 商品列表卡片/表格复制按钮 copyText('名称') 未传按钮元素,
  el=undefined → undefined.innerHTML TypeError → catch 误报'复制失败'
- 修复: ①copyText el 存在性保护(utils.js, el 可选) ②商品列表 2 按钮补 ',this'(成功显示'已复制') ③右键菜单 3177 esc→jsEsc + 传 null
- 经验: copyText(text, el) 的 el 参数是可选的, 调用点传不传都该工作; 公共工具函数对可选参数必须做存在性保护
- 验证: node --check ×2 ✓ 模拟 el 缺失不再误报 ✓ 构建部署 ✓ 线上 200

### 商品列表'复制'改为复制商品（commit 1a72162, main-D6X0J6yN.js, v107.36）
- 需求: 点复制 → 跳商品编辑页, 预填完整副本(名称/价格/库存/图片/描述), 修改后保存为新商品
- 实现: product-form.js 新增 window._cloneProduct(id) — _openProductForm() 新建模式(PF.editId 未设→保存 POST 新建)
  + /products/{id}/detail 用提取的 _pfApplyData 预填全部字段(编辑/复制共用)
- 图片: _pfSave 新建时透传 PF.images 非 data: URL → body.image_url/image_url2/image_url3(后端 product_create 白名单已含)
- app.js: 表格行+卡片复制按钮 → window._cloneProduct(id); 右键菜单'复制名称'保留(语义独立)
- 方法论: "复制成新记录" = 打开新建模式 + 复用编辑填充逻辑 + 保存走 POST(editId 不设); 图片等引用字段
  检查后端创建白名单是否支持直接传 URL
- 验证: node --check ×2 ✓ 构建部署 ✓ 线上 200

### 全站内联事件传参排查收尾（commit 5c84374, main-XwkHKETa.js, v107.37）
- v107.34 盲区复盘: 当时按 onclick=" 前缀非贪婪匹配, 只换每个属性内第一个 esc(, 同属性第 2+ 个全漏;
  且只覆盖 onclick, onchange/onkeyup 等其它事件属性漏网
- 本轮修复: 以整个事件属性值为单位替换内部全部 esc(→jsEsc(, 6 文件 +8 处
  (app.js editBrand/showEditUnitForm 多参数, unified-dashboard/new-features/report-designer/tenant-admin/review-queue)
- 验证清零: 事件属性内 esc 残留 0 / href=javascript 0 / 右键 action 字符串 0 / 非事件属性 esc 594 处保持(HTML 上下文)
- 方法论: 内联传参转义排查的正则必须"按整个属性值贪婪匹配后内部全部替换", 非贪婪按前缀匹配会漏同属性多参数;
  扫描要覆盖全部 on* 事件属性而非只 onclick; 换行符可当分隔符验证属性边界

### 商品档案删除功能（commit b1d3939, main-TbKGFKwZ.js, v107.38）
- 设计: 软删除(is_active=0) — 列表隐藏/历史订单报表不受影响/可恢复; 物理删除有被订单引用风险不默认
- 三入口: ①工具栏'批量操作▼'→批量删除(红)+批量恢复 ②表格行'更多(···)'→删除(红) ③卡片视图操作区→删除图标(红)
- 交互: 单选/批量均弹 showConfirm(红色, 说明可恢复)防误操作; 删除后 _selSales.clear()+renderDataList() 即时刷新
- 后端: erp_db batch_set_active(ids,active) 新增; batch-update 路由支持 is_active
- ⚠️ 顺带修复隐藏 bug: 原'批量停用'调 batch-update 传 is_active 但路由只处理 category/brand → 静默失效多年
- 部署注意: 后端 db/queries/sales.py 必须传到 /opt/hergent-erp/db/queries/(非根目录), 误传根目录副本要清理
- 验证: node --check ✓ py_compile ×2 ✓ 冒烟 is_active 0/1 ✓ 构建部署 ✓ 线上 200+health ok
- 方法论: 加"删除"功能前先查后端是否有批量状态接口/软删字段(is_active)及其路由是否真处理该字段 —
  前端按钮可能长期静默失效(batch-update 只处理 category/brand 是典型)

### 表格行详情字段升级（commit 7e86dfa, main-DT-B8qVJ.js, v107.39）
- 表格行'更多-详情' openPeek 4 字段(规格/品牌/进价/售价) → 与卡片视图一致 8 字段(助记名/条码/规格/单位含换算/品牌/分类/进价/售价)
- 统一表格/卡片两视图详情信息密度; 单行改动, 复用卡片同款数组
- 验证: node --check ✓ 构建部署 ✓ 线上 200

### 商品详情弹窗 TypeError 修复（commit 55f2779, main-CT7Q6E1s.js, v107.40）
- 症状: 点详情报 Cannot read properties of null (reading 'classList') @ bundle Kt (onclick 触发)
- 根因: index.html 只有 #peekPanel, 缺 #peekOverlay(styles.css 样式早有, 元素漏建);
  openPeek 里 overlay.classList.add('show') 操作 null → TypeError(既有 bug, 点详情暴露)
- 修复: ①index.html 补 <div id='peekOverlay' onclick='closePeek()'> ②openPeek/closePeek panel/overlay 存在性保护
- 方法论: "点击后才炸的 null.classList" = 组件引用的伴随元素(遮罩/容器)在 HTML 里漏建; 样式与元素常分离,
  排查先 grep index.html 的 id 与 JS getElementById 的对应关系
- 验证: node --check ✓ index.html 含 peekOverlay ✓ 构建部署 ✓ 线上 200

### 商品档案"修改记录"按钮（v107.41/41b, commits 14ba736+697bb35, main-CWcUYZV3.js, 已上线）
- 需求: 删除按钮前加"修改记录", 真实展示商品修改历史(时间/字段/前后值/操作人), 倒序
- 关键排查: 旧 audit_logs(294条) 仅动作级文本(无字段级 before/after) → 历史无法回溯, 必须新表从现在记录
- 实现: ①新表 product_change_logs(id,product_id,user_name,action,field_name,old_value,new_value,created_at)
  ②sales.py log_product_changes/get_product_changes(倒序) + **写/查时 _ensure_product_change_logs 幂等建表**
  ③products.py 埋点: create 记录+update 逐字段 diff(空→空跳过, bool 归一化 0/1, 异常 try/pass 不影响主流程)
  ④server.py batch-update 软删/恢复→每商品 action=delete/restore ⑤data.py 新 GET /api/products/{pid}/changes
  ⑥前端 app.js: 表格行更多菜单+卡片视图 两入口, _showProductChanges 弹窗(modal 体系, 45+字段中英映射,
  is_active→启用/停用, 金额¥, 长文本截断+悬停全文, 空态引导)
- ⚠️ 教训: **租户库不走主库 init_db** — 建表 SQL 加进 erp_db.py 初始化块后生产租户库仍无表;
  解法=写/查时幂等 ensure + 生产手动补建(tenant_1.db)。与 hr_employees 懒创建同模式
- 验证: node --check ✓ py_compile ✓ 冒烟 create/update/软删/restore diff 全对 ✓ 线上 200 + 匿名401 ✓

### 全站"修改记录"补齐（v107.42, commit 6e08c88, main-DWaMcxHg.js, 已上线）
- 梳理结论: 全站档案类=商品(已实现)+客户+供应商(data tab 三大档案); 字典类(openRefData 品牌/分类/单位/仓库)
  仅 POST 创建无统一编辑路由; 员工无 employee_update 编辑接口 → 均未纳入(清单说明)
- 基建: 新增 db/queries/entity_logs.py 通用 entity_change_logs 表(entity_type+entity_id)+
  log_entity_changes/get_entity_changes; erp_db 暴露; 写/查幂等 ensure(租户库懒建教训复用)
- 埋点: contacts.py contact_create 记 create + contact_update 逐字段 diff, **排除 SENSITIVE_FIELDS**
  (phone/phone2/email/wechat/bank_account/tax_id/boss_phone 防 PII 泄密), 空→空跳过, bool 归一化
- 路由: data.py POST/PUT contacts 传 _user + GET /api/contacts/{cid}/changes
- 前端: _showProductChanges 弹窗抽公共 _renderChangeLogModal(title,list,F,moneyFields,emptyHint),
  商品/客户/供应商共用; 新增 _showContactChanges; 客户/供应商 表格行+卡片视图 3 处 clock 按钮
- 冒烟坑: 本地建表 contacts 缺 pinyin 列(生产有) → 冒烟环境需对齐生产 schema
- 验证: 冒烟 创建→改名→账期→价格等级→信用额度 diff 全对 + phone 零泄漏 + 倒序 ✓ 线上 200 + 匿名401 ✓
- 方法论: 档案"修改记录"通用化 = ①通用表(entity_type)一表多实体 ②PII 加密字段 diff 前排除 ③弹窗抽公共渲染
  ④实体无编辑接口(员工)/无统一编辑路由(字典)则暂不纳入并如实说明

### 商品列表参照舟谱改造（v107.43, commit 92b49ac, main-D2SmCNDh.js, 已上线）
- 参照截图 portal.zhoupudata.com: 列表所有字段只读, 商品名称改品牌色+pointer+点击跳编辑
- 改: app.js 表格行 5 处去掉 editable-cell(name/brand/spec/purchase_price/sale_price);
  name 改 <a class="product-name-link" onclick="_openProductForm"> 品牌色+加粗+pointer+hover 下划线
- CSS: 删除 .editable-cell 两条规则, 新增 .product-name-link
- 快捷入口保留: 双击/Enter 行 → 触发该行'编辑'按钮(从旧触发首个 editable-cell 改为编辑按钮)
- 旧行内编辑 click 处理代码整段移除(失效)
- 保留: 行操作按钮(编辑/复制/···更多)/卡片视图/全站其他页面 不动
- 验证: node --check ✓ editable-cell 残留仅注释 ✓ 构建 ✓ 线上 200 ✓
