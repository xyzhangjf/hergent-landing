# 2026-08-27 工作日志

## 「报单映射」前端显示文案改为「报单配置」（2026-08-27 早）
- 用户（非技术）反馈"映射"一词不好懂，经选项对比后选定「报单配置」。
- 仅改**前台显示文案**，不动数据库表 `report_mapping`、后端接口 `/api/report-mappings`（零数据风险）。
- 改动文件：
  - `src/pages/ReportMapping.vue`：标题「报单映射配置」→「报单配置」；体检区「映射体检」→「配置体检」、「活跃映射中简称重复」→「活跃配置中简称重复」、「未配置映射的门店/客户」→「尚未配置报单的门店/客户」、「未分配任何映射的员工」→「尚未配置报单的员工」；按钮「+ 新建映射」→「+ 新建配置」、「暂无映射…」→「暂无报单配置…」；弹窗「编辑映射/新建映射」→「编辑配置/新建配置」、「停用映射」→「停用配置」、「Excel 批量导入映射」→「Excel 批量导入配置」。
  - `src/router/index.js`：meta title「报单映射配置」→「报单配置」。
  - `src/components/Shell.vue`：桌面侧边栏 + 移动端抽屉菜单「报单映射」→「报单配置」（2 处）。
  - `src/api/modules.js`：注释「报单映射配置（v109）」→「报单配置（v109）」。
- Forecast.vue / ResultCard.vue 里"值映射/未映射表头/sparkline 映射"属 Excel 列映射、图表术语，与报单模块无关，**不改**。
- 验证：grep 旧短语无残留；`npm run build` 通过（ReportMapping-CqXz6cqD.js 15.03kB）；rsync+chown 部署生产 DEPLOY_OK。

## 「报单配置」整合进「预报订货管理」标签页（2026-08-27）
- 用户建议把报单配置整合到预报订单管理，作为「往期预报」右侧的独立标签。同意：报单配置本质是报单简称↔客户/仓库对照字典，与预报订单解析链路强相关，整合后工作流连续。
- 做法（低侵入，不改动 ReportMapping 本体）：
  - `src/pages/Forecast.vue`：`module-tabs` 加第三个按钮「报单配置」（activeTab='config'，置于往期预报右侧）；`import ReportMapping`；`activeTab==='config'` 时用 `<div class="config-panel"><ReportMapping/></div>` 渲染（与 ForecastHistory 同模式，v-if 切换即挂载/卸载并重载数据）；scoped 加 `.config-panel :deep(.page){padding:0;margin:0}` 中和嵌套 `.page` 双内边距（`.page` 全局无 padding 定义，属防御性）。
  - `src/components/Shell.vue`：删除档案管理桌面侧栏 + 移动端抽屉里的「报单配置」入口各 1 处，避免与标签页重复。
  - `src/router/index.js`：**保留** `/archive/mappings` 路由（旧书签/深链仍可用，ReportMapping 独立页照常跑），未删。
- 验证：grep 仅剩合理残留（路由深链、标签页按钮、组件自身 h2 标题、空态文案、注释）；`npm run build` 通过（Forecast-D--qbh_L.js 496.21kB 含内嵌报单配置）；rsync+chown 部署生产 DEPLOY_OK。
- 待用户真机确认：硬刷新后「预报订货管理」出现第三个标签「报单配置」，点开即原报单配置页（含体检红标/新建/导入），且双重内边距已消除。
- 用户反馈嵌入后小标题「报单配置」与标签重复，要求"只留内容"。改为 `.config-panel :deep(.page-hd){display:none}`，仅隐藏嵌入态标题（独立深链页 `/archive/mappings` 的标题不受影响）。重新 build+rsync+chown 部署 DEPLOY_OK。

## 删「预报审批」独立页死代码 + 报单汇总表「已审批/已确认」标记（2026-08-27 晚）
- 背景：用户拍板"业务经理在报单汇总表改完订单点保存即完成审批，老板进汇总表看数据即可；用我们软件的多是小公司，没那么复杂"。原 `ForecastApprove.vue` 重型审批流（待审批列表/驳回/通过生成采购申请）判为错误方向，按"彻底删除代码"方案清理。
- 前端清理（删除审批流，保留汇总核心）：
  - `src/router/index.js`：删 `ForecastApprove` 懒加载 import + 整条 `{ path:'forecast-approve' }` 路由。
  - `src/pages/ForecastApprove.vue`：**整文件删除**（rm -f）。
  - `src/api/modules.js`：`forecastApproveApi` 仅删 `pending/approve/reject` 三方法，保留 `summary/saveMatrix`（报单汇总表核心）；注释标注"原审批流 pending/approve/reject 已废弃，见决策 2026-08-27"。
  - `src/pages/Workbench.vue`：删无用 `forecastApproveApi` import + "待审批"待办卡片（含 `prio:'red',path:'/forecast-approve'`）。
  - `src/components/Shell.vue`：早前已删桌面侧栏「预报审批」入口（移动端本无）。
- 后端「保存即审批定稿」标记（轻量，不动报单数据本体）：
  - `server/erp_db.py` `_ensure_forecast_tables` 惰性建表 `forecast_period_confirm(period_start,period_end,confirmed_by,confirmed_at,PRIMARY KEY(period_start,period_end))`，对所有租户库生效。
  - `server/erp_db.py` `forecast_submission_summary`：当传入 start/end 时带出 `confirmed={by,at}`（无则 null）。
  - `server/routers/forecast_submissions.py` `save_matrix`：保存成功后 upsert 该期次确认记录（确认人取 `display_name||username||'经理'`，时间 `datetime('now','localtime')`）——即"经理保存汇总表=审批定稿"。
- 前端汇总表确认徽标（`src/pages/Forecast.vue`）：
  - 新增 `confirmInfo` ref；`loadEditGrid`(~2029) 与 `loadCross`(~4910) 两处 `summary` 后赋值 `confirmInfo.value = (d.confirmed || null)`。
  - 工具栏加徽标：已确认=绿底「✓ 已确认 · 定稿人」，草稿=灰底「草稿 · 待经理确认」。样式 `.confirm-badge/.ok/.draft`。
- 验证：grep 全网零 `ForecastApprove`/`forecast-approve`/`forecastApproveApi.(pending|approve|reject)` 残留；`py_compile` 后端无语法错；前端 `npm run build` 通过（Forecast 496.52kB）；后端 `bash deploy.sh` 实际 health 200 OK；真机 E2E 在 2099 隔离窗口跑 demo token → summary(confirmed:null) → save-matrix(return confirmed by 张记乳品（演示）) → summary(confirmed{by,at}) 闭环通过，随后 SSH 遍历 tenant_*.db 清理测试数据（9 库已清）。
- 待用户真机确认（沙箱无登录态）：硬刷新(Cmd+Shift+R)后「预报订货管理」→当期预报交叉表工具栏即显示确认徽标；经理点"保存调整"即标记已确认，老板进汇总表一眼区分草稿/定稿。
- 可选跟进已落地（用户"全做"）：①历史往期预报批量补确认标记；②已开账号但停用的员工用灰阶徽标区分。见下方独立小节。

## 全做：历史确认标记回填 + 停用员工灰阶徽标（2026-08-27 晚）
- **历史预报确认标记回填（生产库）**：
  - 本地 repo 的 tenant_*.db 为空骨架，真实数据在 `/opt/hergent-erp/tenant_*.db`（SSH 直连）。
  - 先备份受影响库：`/opt/hergent-erp/backups/forebackfill_20260827_131843_tenant_1.db`、`_tenant_10.db`（python sqlite3 在线备份，验证大小）。
  - 回填脚本 `/Users/zhangjunfeng/Documents/laozhangai-product/.workbuddy/backfill_confirm.py`（幂等：仅当「导入」报单存在且无确认记录时 INSERT，INSERT OR IGNORE）。
  - 落地：tenant_1 补 1 期（2026-08-22）、tenant_10 补 5 期（08-18~08-22，均单日窗口），confirmed_by='系统回填'，confirmed_at=该期最后保存时间。其余租户无数据跳过。已 SSH 核验落库。
  - 语义：老期次（曾被经理保存过矩阵）不再显示"草稿·待经理确认"，与"保存即审批"一致。
- **停用员工灰阶徽标（前端）**：
  - `EmployeeArchive.vue`：role 徽标改为 `:class="['r-'+role, {stopped: is_active===0}]"`；「已开通」绿色仅当 `is_active!==0`（停用转灰）；删冗余「小程序已禁用」文案（与「已停用」标签重复）。
  - 新增 `.df-role.stopped{background:#e5e7eb;color:#9aa0a6}` 灰阶覆盖角色色。
  - `npm run build` 通过（EmployeeArchive 19.30kB）→ rsync+chown 部署 `/opt/hergent-cn-v2/` 成功。
  - 效果：停用员工整行已灰（原 .stopped 行样式）+ 角色徽标/已开通转灰，一眼区分在用/停用。

- **boss 账号显示名修正 + 清理测试残留账号（生产数据）**：
  - 排查：前端 Shell.vue 取 `store.user.name = display_name||username`；登录认证走**主库 erp.db**（login 用 `db.get_db()` 无租户上下文连主库），tenant_1.db 的 users 不参与登录。
  - ⚠️ 教训：首改误改 tenant_1.db（无效），更正为 `erp.db` 的 boss `id=2`（username='boss'）`display_name`「王老板」→「张俊峰」（示例/占位残留）。**重登即生效**。
  - 清理 tenant_1.db 中 3 条测试残留 boss（id 5 兴旺商贸/6 恒达贸易/7 新鲜注册公司）：删 users + user_tenants(2) + tenant_members(1)；report_mapping 中 id=1（刘善涛仓→调拨单）原绑错员工 id=6，已置 employee_id=NULL 保留映射待重绑。
  - 备份：`/opt/hergent-erp/backups/clean_20260827_160238_{erp,tenant_1}.db`。终验：5/6/7 全库零引用，boss 仅剩 id=2 张俊峰。

- **支持用户自定义用户名/昵称（前端+后端）**：
  - 后端 `auth.py` `PUT /profile` 扩展支持 `username`：长度 2-32 校验 + 主库全局唯一性（`SELECT id FROM users WHERE username=? AND id!=?`，冲突 400「该登录账号已被占用」）；display_name 维持 COALESCE 原逻辑；返回更新后 user + audit_log「修改个人资料」。
  - 前端 `Shell.vue`：右上角 `tb-user` 改为点击展开下拉（修改资料/退出登录 + 遮罩关闭）；新增「修改资料」modal（显示昵称 + 登录账号两输入框），`saveProfile` 调 `api('/api/auth/profile',{method:'PUT',body})`，成功更新 `auth.user`+`store.user.name`+toast。
  - 关键点：登录认证走主库 erp.db、session 按 user_id 存 → 改 username **不需重登**（仅下次登录用新账号）；昵称立即生效。
  - 部署：前端 build 通过（Shell 48.88kB）+ rsync/chown；后端 deploy.sh 重启（health 200）。真机 E2E：demo 账号 PUT /profile 幂等改 display_name 成功 + username 撞 'boss' 返回 400 拦截，均通过；demo 数据未污染。

## 天气预报面板（2026-08-27 晚 → 上线）
- 用户需求：「卖低温奶特别关注天气，加一个天气预报面板」，放置位=顶栏「下午好」问候与「问 AI 副驾」按钮之间；城市按浏览器访问 IP 自动定位。
- 后端（源码真身在 `/Users/zhangjunfeng/Documents/hergent-erp`，deploy 时该仓库 `server/` 扁平化到生产 `/opt/hergent-erp/`；laozhangai-product 工作区的 `server/` 只是旧 credits 服务，与 ERP 无关）：
  - 新增 `routers/weather.py`：读真实 IP（X-Forwarded-For/X-Real-IP）→ ipinfo.io 解析城市 → Open-Meteo 取当前温度+4 天预报（WMO 天气码映射中文+ emoji）；30 分钟按 IP 内存缓存；本地/默认回退郑州。
  - `server.py`：`_PUBLIC_PATHS` 加 `/api/weather`（免登录）；`from routers.weather import router as weather_router` + `app.include_router(weather_router)`。
  - 已 SSH 核验：weather.py 存在于 /opt/hergent-erp/routers/，server.py 三处已接线；`curl /api/weather` 返回 success/city/temp/days。
- 前端：`hergent-cn-v2/src/components/WeatherWidget.vue`（新建）：问候语与 AI 副驾之间插入剂型天气胶囊（城市·温度°·图标），点开看未来 4 天（温度区间+降雨概率）；`Shell.vue` 引入并置于 `.aibar-greet` 与 `.aibar-copilot` 之间；移动端 `@media(max-width:768px)` 加 `.wx{display:none}` 隐藏。`npm run build` 通过 + rsync/chown 部署。
- ⚠️ 更正（同日稍晚复核）：**源码已在仓库内**。真正 ERP 后端源码 `/Users/zhangjunfeng/Documents/hergent-erp` 已含 `server/routers/weather.py`（新建未跟踪）+ `server/server.py` 三处接线（whitelist 590 / import 739 / include_router 822，属未提交工作树改动）。之前误判「本地缺失」是只在 laozhangai-product 工作区 Glob（那里只有旧 credits 的 server/）。结论：天气改动已在「本地 hergent-erp 工作树 + 生产 /opt/hergent-erp」双份落地，**无丢失风险**。git 状态：weather.py `??` 未跟踪、server.py `M`（含 845 行未提交改动，与既有 11 个未推送 commit 同批暂缓推送）；保持暂缓、不单独提交 weather.py 以免与 server.py 接线脱节。

## 天气面板：用户可自由选择城市（2026-08-27 晚）
- 用户需求：「把天气设置成用户可自由选择城市」（在 IP 自动定位基础上，增加手动选城市）。
- 后端 `hergent-erp/server/routers/weather.py` 改造：
  - `GET /api/weather` 新增可选参数 `city` / `lat` / `lon`（优先级：显式坐标 > 城市名 > IP 自动定位；无参数时仍 IP 自动定位，向后兼容）。`city` 经 Open-Meteo geocoding API（`geocoding-api.open-meteo.com/v1/search?name=&language=zh`）转坐标；返回新增 `lat/lon/loc_source`（coord/city/ip/default）。
  - 新增 `GET /api/weather/geocode?q=` 城市名搜索，返回候选项 `[{name,lat,lon,region}]`（含省/国，便于重名消歧，如多个"北京"）。
  - 缓存键按 定位类型 区分（ll:坐标 / city:名 / ip），30 分钟。
  - `server.py` `_PUBLIC_PATHS` 显式加 `/api/weather/geocode`（虽 startswith('/api/weather') 已覆盖，但显式更稳）。
- 前端 `hergent-cn-v2/src/components/WeatherWidget.vue` 重写：
  - popover 顶部加城市搜索框（输入即防抖 300ms 调 geocode，下拉候选 + 省/国标注，回车选首项）；点选后存 `localStorage['wx_city']={name,lat,lon}` 并按坐标重新取天气；显示「我的位置」按钮可一键恢复 IP 自动定位。
  - 默认加载：有 localStorage 选城则用坐标，否则 IP 自动定位。胶囊按钮 title 区分"已选城市/按IP定位"。
- 验证：`py_compile` 后端 OK；前端 `npm run build` 通过（Shell 53.09kB）→ rsync/chown 部署；`bash deploy.sh` 部署后端。生产 SSH 核验：`/api/health` ok；`/api/weather/geocode?q=北京` 返回 3 候选（北京/重庆/四川）；`/api/weather?city=上海` 返回上海(loc_source=city)；`/api/weather?lat=39.9&lon=116.4&city=北京` 返回北京(loc_source=coord)。三模式全通。
- 文件状态：weather.py `??` 未跟踪、server.py `M`（连同上次 IP 版天气改动，仍是未提交工作树，与 11 个未推送 commit 同批暂缓）。

## 天气城市显示/搜索全汉字（2026-08-27 晚，接上节）
- 用户反馈：城市的显示和搜索都要用汉字，不要用拼音。
- 排查：搜索接口（Open-Meteo geocoding `language=zh`）本就返回汉字；**拼音只出现在 IP 自动定位的默认城市**——ipinfo.io 返回拼音（Zhengzhou）。
- 修复 `routers/weather.py`：
  - 新增 `_reverse_geocode(lat,lon)`：用 BigDataCloud 免费反查（`reverse-geocode-client?localityLanguage=zh`，免 key）把坐标转中文城市（郑州市→去尾"市"得郑州）；失败则回退把 ipinfo 拼音城市再经 `_geocode`（lang=zh）转中文；再失败用 DEFAULT_CITY。IP 分支改用此中文名。
  - `_geocode` 过滤：跳过纯拼音/非汉字结果（正则 `[\u4e00-\u9fff]`），保证搜索下拉全汉字（去掉了 Zhengzhou 等拼音候选项，主结果郑州仍居首）。
- 验证：`py_compile` OK；`bash deploy.sh` 部署。`curl /api/weather`（默认/无参）→ `city:"郑州"`；`curl /api/weather/geocode?q=Zhengzhou` → 仅汉字结果（郑州/政洲/郑周村… 主结果郑州）；`geocode?q=北京` → 多个"北京"候选带回 province。
- 结论：显示（默认+已选）与搜索均汉字，无拼音残留。

## 天气弹窗：点击外部关闭 + 预报延长至 7 天（2026-08-27 晚）
- 用户需求：① 城市选好后点击旁边空白应关闭弹窗（此前不关）；② 预报天数从 4 天提到 7 天。
- 后端 `routers/weather.py`：`_weather` 的 `forecast_days=4` → `forecast_days=7`（Open-Meteo 默认支持，返回 7 条 daily）。
- 前端 `WeatherWidget.vue`：
  - 新增 `root` ref 绑到 `.wx` 根 div；`onMounted` 注册 `document.addEventListener('click', onClickOutside)`，`onBeforeUnmount` 移除；`onClickOutside` 逻辑：`open && !root.contains(e.target) → open=false`（点胶囊本身在 root 内不触发关闭，点 popover 内不关闭，点外部关闭）。
  - `.wx-days` 改 `overflow-x:auto;max-width:340px` 横向滚动；`.wx-day` 缩到 `min-width:44px;flex:0 0 auto`，避免 7 天把顶栏撑爆。
- 验证：`npm run build` 通过（Shell 53.29kB）；`py_compile` OK；前端 rsync/chown + 后端 `deploy.sh` 部署完成。生产 SSH 核验：`/api/health` ok；`/api/weather?city=郑州` → `days:7`（2026-08-27~09-02）。点击外部关闭为前端交互，_build/逻辑_已验证（标准 Vue 文档监听模式），建议硬刷新后点弹窗外区域实测确认。
- 文件状态：weather.py / WeatherWidget.vue 仍在未提交工作树（与 11 个未推送 commit 同批暂缓）。

## 天气：默认 7 天一屏，超 7 天横向滚动（2026-08-27 晚）
- 用户需求：「默认 7 天一屏显示，7 天以上横向滚动显示」。即 7 天铺满不滚动，>7 天才滚动。
- 后端 `weather.py`：`forecast_days=7` → `forecast_days=16`（Open-Meteo 免费接口上限即 16 天，当日已确认）。
- 前端 `WeatherWidget.vue` CSS：`.wx-days` 由 `gap:8/max-width:340`（7 天原被迫滚动）改为 `gap:6/max-width:348`；`.wx-d-t` 字号 12→11px。算账：7 天 = 7×44+6×6=344 ≤ 348 → 一屏不滚；16 天 = 16×44+15×6=794 > 348 → 横向滚动。完全符合「7 一屏、超 7 滚动」。
- 验证：`npm run build`（Shell 53.29kB）+ `py_compile` OK；前端 rsync/chown + 后端 `deploy.sh` 部署。生产 SSH：`/api/health` ok；`/api/weather?city=郑州` → `days:16`（2026-08-27~09-11）。

## 天气：借鉴墨迹天气 — 昨天今天明天标签、日期、温度折线图（2026-08-27 晚）
- 用户需求：参考墨迹天气截图，① 日期标签用「昨天/今天/明天/周X」；② 在「周几」下面加 MM/DD 日期；③ 弹窗顶部加最高温/最低温 SVG 折线图。
- 后端：无改动，继续返回 16 天 daily 数据。
- 前端 `WeatherWidget.vue`：
  - 新增 `dayLabel(dateStr)`：与今天差值 -1=昨天、0=今天、1=明天，其余=周X；同时返回 `{text, date}` 用于两行书签。
  - 模板列表改为 `.wx-d-day`（昨天/今天/明天/周X）+ `.wx-d-date`（MM/DD）。
  - 新增 `.wx-chart` 区域：320×100 viewBox SVG，用 `<polyline>` 画两条折线（高温 `#ff6b6b`、低温 `#339af0`）；`chartDots` 计算属性按 7 天切片、等分 x 轴、按温度范围线性映射 y 轴；每个点标注温度数字和日期简写；背景用卡片样式。
- 验证：`npm run build` 通过（Shell 55.45kB）+ 前端 rsync/chown 部署。生产 `/api/health` ok；后端仍返回 `days:16`。折线图/日期标签为纯前端渲染，建议硬刷新后在浏览器实测（本机无头浏览器代理不稳）。
- 文件状态：WeatherWidget.vue 仍在未提交工作树。

## 天气：折线图不再单独占容器 + 去除重复日期行（2026-08-27 23:27）
- 用户反馈截图：① 折线图不要单独做容器，直接放天气下面以便对齐；② 折线图下方重复出现「今天/明天/周几」一行要去掉（折线图下方的 label 与卡片里的 day label 重复）。
- 前端 `WeatherWidget.vue` 改动：
  - 删除独立的 `.wx-chart` 容器；改为在 `.wx-days-wrap`（position:relative）内放 `<svg class="wx-chart-bg">`（absolute，top/left/right:0，height:80px，z-index:0），作为背景层浮在每天卡片上方。
  - `.wx-days` 加 `padding-top:74px` 给折线留白，`position:relative;z-index:1` 压在折线之上。
  - `chartDots` 的 x 改用百分比 `((i+0.5)/n*100)%`，与 flex 等分的 `.wx-day` 列居中对齐，SVG viewBox 改 `0 0 100 100`、`preserveAspectRatio=none`，宽度随弹窗自动铺满。
  - **移除 svg 里那段日期 label `<text>`**，只保留高温/低温数字；日期标签只在 `.wx-day` 卡片里出现一次。
  - 折线字号/点半径调小（font-size 6、r 1.8）适配窄列。
- 验证：`npm run build`（Shell 55.31kB）+ rsync/chown；生产 SSH 当场 grep 确认 `/opt/hergent-cn-v2/assets/Shell-CfM7O8c7.js` 含 `wx-chart-bg`（22:58）。**沿用前次教训：部署后 SSH 当场 grep 生产 bundle 核验已生效**。
- 文件状态：WeatherWidget.vue 仍在未提交工作树（与 11 个未推送 commit 同批暂缓）。

## 天气：删除折线图（2026-08-27 23:33）
- 用户要求删掉温度折线图。前端 `WeatherWidget.vue`：删除 `.wx-chart-bg` SVG 整块、移除 `chartSlice/chartDots/highPoints/lowPoints` 计算属性、移除 `.wx-chart-bg` 样式、`.wx-days` 去掉 `padding-top:74px` 与上一条线相关的 `position/z-index`、删除未使用的 `computed` 导入。
- 验证：`npm run build`（Shell 53.74kB，dist 和 prod 均 `grep wx-chart-bg = 0`）；rsync/chown 完成；SSH 当场核验 `/opt/hergent-cn-v2/assets/Shell-BljeFbYV.js` 无 `wx-chart-bg`、`wx-days` 仍在。天气面板现在只显示 16 天卡片（昨天/今天/明天/周X + MM/DD + 图标 + 温度 + 降雨概率），无折线图。
- 文件状态：WeatherWidget.vue 仍在未提交工作树。

## 天气：默认 16 天一屏显示（2026-08-27 22:36）
- 用户需求：「设置成默认一屏显示 16 天」（之前是 7 天一屏、超 7 天滚动）。
- 前端 `WeatherWidget.vue`：
  - `chartSlice` 由 `days.slice(0,7)` 改为全部 `days`（折线图覆盖 16 天）。
  - 折线图 viewBox `320×100` → `700×100`；`chartDots` 的 `width 320→700`、`paddingX 18→24`、`topY 12→14`、`bottomY 72→70`（比例重算，16 点等分）。
  - CSS：`.wx-chart` 固定 `width:700px`；`.wx-pop` 加 `width:max-content;max-width:calc(100vw-20px)` 跟随内容撑开；`.wx-days` 去 `max-width:348`、改 `gap:4`、`.wx-day` `min-width:44→40`，16 天一行不滚动。
- 部署教训：上一轮（墨迹天气那轮）rsync 报告 RSYNC_OK 但生产文件未更新（mtime 停在 22:12，grep 证明无 wx-chart）。本轮构建后**当场 SSH grep 生产 bundle 核验**才确认生效——以后前端部署必须 grep 生产 bundle 验证，不能只看本地 build / rsync 回显。
- 验证：build Shell-D0wYZagk.js 55.44kB；rsync/chown；SSH `ls+grep` 确认 `/opt/hergent-cn-v2/assets/Shell-D0wYZagk.js` 存在且含 wx-chart（22:38）。
- 文件状态：WeatherWidget.vue 仍在未提交工作树。
