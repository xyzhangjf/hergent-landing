# 2026-07-26

## S4-1 设计令牌苹果化 + 统一令牌源（已完成，已上 staging）
- 根因：旧 `styles.css` 顶部 `:root` 把 `--t1` 设成白字、却未覆盖 `--bg`（浅灰底），且它加载在 `css/variables.css` 之后 → 浅色默认（`index.html` 默认 light）下白字浅底不可见（线上靠打包顺序巧合压过才勉强可读，脆弱）。
- 修复：`css/variables.css` 设为**唯一令牌源**（迁入 styles.css 的 --font-sans/--t1..t4/--fs-*/--fw-*/--lh-*/--ls-*/--r，补 --ease-out）；删除 styles.css 顶部 :root 令牌块（保留 html/body/class 规则）。所有变量名保持不变，styles.css 引用契约不破。
- 苹果化数值：SF Pro 系统字体栈（-apple-system, BlinkMacSystemFont, "SF Pro Text/Display", 苹方…）；Apple 中性灰 #f5f5f7；系统色绿 #34c759/红 #ff3b30/橙 #ff9f0a；圆角 --r:12px；柔和分层阴影；color-scheme:light/dark；新增毛玻璃/导航半透明令牌 --blur-* / --nav-bg / --nav-border（S4-2 组件消费）。
- 构建：`VITE_BASE=/staging/ npm run build` → staging bundle `main-DOLxpfbA.js` / `main-BRIT7MT2.css`。部署 `/opt/hergent-erp-staging`（备份 hergent-staging-deploy-20260726124812.tar.gz，avatars 从生产拷贝）。
- 验证：staging / =200；css/js 正确 MIME；avatars/dami.png=200；served CSS 仅 2 个 --t1（浅黑 rgba(0,0,0,.88) + 深白 .92），旧白 --t1 已消失。
- 提交 `aac5975`。**生产未切**（遵守先 staging 再生产的红线，待用户预览 staging 后切）。
- 设计决策：保留青色品牌（#06b6d4）未改苹果蓝，仅中性/间距/圆角/阴影/字体走苹果语言；若用户要苹果蓝强调色再议。
- 下一步：用户在 https://hergent.cn/staging/ 预览 → 确认后 base=/ 重建并切生产（`dist/*` 覆盖 /opt/hergent-erp/static）；之后 S4-2 公共组件苹果化（按钮/输入/卡片/导航毛玻璃）。

## S4-1 已切生产 + S4-2 公共组件苹果化（已完成）
- 用户说"继续"= 确认 staging OK，授权推进。先切 S4-1 生产：base=/ 重建 dist → 部署 /opt/hergent-erp/static（备份 hergent-static-S4-1-20260726131746.tar.gz）。验证 erp.hergent.cn/ =200、main-BRIT7MT2.css/js=200、avatars=200、feature-flags/check=200。
- S4-2（commit `a29796e`，已上 staging main-DoAPgnne.css）：改 static/styles.css 公共组件——
  - 按钮：圆角 8px→`var(--radius-md)`(12px)，过渡改 `--ease-out`；`.btn-primary` 加 `--shadow-glow` 静止光晕+悬浮 `0 6px 18px` 抬升；`.btn-outline` 悬浮用 `--bg-secondary/--border-strong`(双主题自适应)；`.btn-sm/xs` 圆角 `var(--radius-sm)`；`.btn-danger` 悬浮加红色阴影；按压 scale .97。
  - 输入框：`.modal-field input/select/textarea` 圆角→`var(--radius-md)`，边框 `var(--border-default)`，`background:var(--bg-input)`，过渡 `--ease-out`（聚焦环保留品牌青 4px）。
  - 卡片：`.stat-card`/`.product-card`/`.chart-card` 由 `--bg3` 改白底 `--bg2` + `--border-subtle` 细边框 + `--shadow-sm` 静止阴影；悬浮 `--shadow-lg/md`+translateY 抬升（浅灰底上白卡更 Apple）。
  - 毛玻璃：侧边栏 `.sidebar` 改 `background:var(--nav-bg)` + `backdrop-filter:blur(20px) saturate(180%)`；`#peekPanel`/`#notifPanel` 改 `--bg-window` + `backdrop-filter:blur(30px)` + `--nav-border`。
- 验证 staging：/ =200、css/js=200；served CSS 含 `--nav-bg:`、`backdrop-filter:blur(20px)`、`border-radius:var(--radius-md)`、`box-shadow:var(--shadow-sm)`。
- 生产（erp.hergent.cn）仍是 S4-1（main-BRIT7MT2.css），S4-2 待用户预览 staging 后切。G1 密钥仍阻塞（未 push，本地领先 origin 32 commits）。
- 下一步：用户在 hergent.cn/staging 预览 S4-2 → 确认后 base=/ 重建并切生产；之后 S4-3 核心 6 页排版精修。

## S4-2 已切生产 + S4-3 核心6页排版精修（S4-3 已上 staging）
- 用户说"继续"= 确认 S4-2 staging OK，授权推进。先切 S4-2 生产：base=/ 重建 dist（CSS main-DoAPgnne.css，与 staging 同源）→ 部署 /opt/hergent-erp/static（备份 hergent-static-S4-2-20260726134300.tar.gz）。验证 erp.hergent.cn/ =200、main-DoAPgnne.css/js=200、avatars=200、feature-flags/check=200、侧边栏 backdrop-filter 已生效。
- S4-3（commit `1f81bfb`，已上 staging main-SgVa0sJF.css）：纯 CSS 排版精修，消费 S4-1/4-2 令牌——
  - 间距节奏：`.page-inner` padding 28/40→**32/48**（更大留白）；`.stat-grid` gap 16→20、margin 28→32。
  - 标题层级：`.page-hd` 字距 -.02em→**-.03em** + `text-wrap:balance`（标题平衡换行）；`.section-title` 字距 .8→**1px**。
  - 统计卡数字 Apple 化：`.stat-card-val` 由等宽 JetBrains Mono → **var(--font-sans)+tabular-nums**（系统数字字体、等宽对齐）。
  - 表格密度：`.thead th`/`.tbody td` padding 12/16→**14/18**、`tbody td` 行高 1.5→**1.6**、表头底边 `var(--bd)`→`--border-subtle`。
- 验证 staging：/=200、css/js=200；served CSS 含 `padding:32px 48px`、`font-variant-numeric:tabular-nums`、`text-wrap:balance`、`padding:14px 18px`。
- 生产（erp.hergent.cn）仍是 S4-2（main-DoAPgnne.css），S4-3 待用户预览 staging 后切。G1 密钥仍阻塞（未 push，本地领先 origin 33 commits）。
- 下一步：用户在 hergent.cn/staging 预览 S4-3 → 确认后 base=/ 重建并切生产；之后 S4-4 长尾页面 + 手机端适配。

## S4-3 已切生产 + S4-4 长尾页面 + 手机端适配（S4-4 已上 staging）
- 用户说"继续 S4-4"= 确认 S4-3 staging OK。先切 S4-3 生产：base=/ 重建（CSS main-SgVa0sJF.css 同源）→ 部署 /opt/hergent-erp/static（备份 hergent-static-S4-3-20260726135623.tar.gz）。验证 erp.hergent.cn/ =200、CSS/JS/avatars/feature-flags 全 200。生产现是 S4-3。
- S4-4（commit `f4841a6`，已上 staging main-BJbHT0F5.css）：分两半——
  - **手机端 safe-area（最大短板）**：`index.html` viewport 加 `viewport-fit=cover`（原无，safe-area 不生效）；`#topBar` 加 `padding-top:env(safe-area-inset-top)` + height calc、`#contentArea` margin-bottom 改 `calc(104px + env(safe-area-inset-bottom))`、`#mobileNav` height 改 `calc(56px + env(...))` + `padding-bottom:env(...)`。刘海屏/底部 home indicator 不再遮挡。
  - **长尾卡片统一苹果白底**：`.acct-card`/`data-stat-card` 由 `--bg3`/`#1A2336` 改 `--bg2`+`--border-subtle`+`--shadow-sm`（与 stat/chart-card 一致）；`.data-stat-num/label/sub` 硬编码色(#D0D8E8/#8892A0/#00B4FF)→var(--t1/2/3)/var(--p)；手机端 `.filter-chip` 加 min-height:38px 触控目标。
- 验证 staging：/=200、css/js=200；served index 含 `viewport-fit=cover`、CSS 含 `env(safe-area-inset-bottom)`、`.acct-card{background:var(--bg2)`、`.data-stat-card` 白底。
- 生产（erp.hergent.cn）仍是 S4-3（main-SgVa0sJF.css），S4-4 待用户预览 staging 后切。G1 密钥仍阻塞（未 push，本地领先 origin 34 commits）。
- 下一步：用户在 hergent.cn/staging 预览 S4-4（重点手机端真机/模拟器看 safe-area）→ 确认后 base=/ 重建并切生产；**切完 S4-4 即 S4 苹果化四阶段整体收官**，之后可进入 S5（业务功能/AI 团队深化）或回头处理 G1 密钥。

## S4-4 已切生产 → S4 整体收官（用户指令"切生产然后进入 S5"）
- 用户说"切生产然后进入 S5 业务深化"。先切 S4-4 生产：本地 `VITE_BASE=/ npm run build`（base=/，新 CSS 同源 main-BJbHT0F5.css）→ 部署 /opt/hergent-erp/static（备份 hergent-static-S4-4-20260726151632.tar.gz，rsync --delete exclude avatars/._*）→ chown hergent:hergent。
- 验证：erp.hergent.cn/ =200、index 引用 main-BJbHT0F5.css、served CSS 含 `safe-area-inset`(1)、index 含 `viewport-fit=cover`；staging 仍 /staging/ 不动。**S4 苹果化四阶段整体收官。**
- **S5 诊断（关键）**：查生产 tenant_1.db —— sale_orders=526、**sale_order_items=0**、products=373、purchase_orders=81、inventory=54；且该库**无 customers 表**（customer_id 疑指向 contacts，待确认）。订单明细缺失是 4 场景 AI 的总阻断。
- **数据源已定位**：`最新26年6月下单表6.19.xlsx`（openpyxl 可读）。38 sheet = 报单→到货窗口（如"新版4月2报单-4月6到货"）；每行=商品（A=序号,B=条码,C=规格,D=简称,E=分销价格,F=永辉代码）；G..AH 列=客户名（唐成/黄家伟/易胜琳/胡奎奎/朱青峰/刘正宝/王琴/程欢欢…）的订购量。即"行×列=订单明细"。526 订单≈38窗口×~14客户，可与 sale_orders(customer_id+delivery_date) 关联后回填 sale_order_items(order_id,product_id,quantity,unit_price=分销价,amount)。
- 待与用户对齐 S5 起点（先建明细导入器补齐数据解锁全部场景？还是先聚焦单场景端到端？）+ 首场景优先级，再动手（生产数据写入前须本地副本演练+用户绿灯）。G1 密钥仍阻塞。
