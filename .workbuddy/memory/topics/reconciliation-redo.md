# 对账（reconciliation）—— 判据与红线

> 触发词：**对账 / 收付账户 / 账户明细 / 银行流水 / 微信账单 / 支付宝账单 / 舟谱流水 /
> 应收核对 / 预付款核对 / recon / 差异归因 / 补录 / 冲销**
> 详案：`outputs/对账重做技术方案-2026-09-19/01-对账重做技术方案.md`（454 行，含全部取证命令）
> ✅ **P0 已交付**：`outputs/v198-对账算子-2026-09-19/`（交付说明 / 回放验收 / 回放脚本）
>   代码 = `hergent-erp/server/recon_engine.py` + `mcp/`，提交 `9bcdb11`

> 🔴 **两条入口判据（下笔前先分）**：
> 1. **键对账 ≠ 账户对账**。键对账要求两侧有**共同单号**（如两表同名列）。真实渠道流水之间**没有**
>    共同单号（微信是「交易单号」、舟谱是「单据编号」，两套体系）⇒ 账户对账**不能**用
>    `spreadsheet_reconcile_files`，必须走 `recon_engine.reconcile()`（金额 + 日期 + 方向配对）。
> 2. **PDF 银行流水本轮不做** —— 文案引导「请另存为 Excel」。

---

## 一、🔴 三条状态事实（先记住，能省一轮返工）

| 事实 | 内容 |
|---|---|
| **1** | **旧三步向导已撤下**（v197，2026-09-19）。`/reconciliation` → `redirect: /collections`；`Reconciliation.vue` 整文件删除。**后端 `/api/reconciliation/*` 暂留不删**（重做要用它做存量入口灰度）。 |
| **2** | **催收跟进已独立成页** `/collections`（`Collections.vue`），调 `/api/collections/{queue,rebuild,action,summary}`。侧栏位置不变（仍 13 项，在「算工资」之后）。**这是原页面唯一活着的功能：514 次真实调用。** |
| **3** | 撤入口共 **4 处**（不是 3 处）：`Shell.vue` 侧栏 + `Shell.vue` 移动抽屉 + `CommandPalette.vue` + **`Workbench.vue` 的「今日待办」催收项**。 |

## 二、🔴 决定架构的实测：系统侧没有资金流水

生产 `/opt/hergent-erp/tenant_1.db`（真实租户）实测：

- **`cash_flow` 仅 16 行**；账户取值是 `微信`/`现金`/`银行`，**不是**舟谱账户号（`1003011`/`10021`）
- **`bank_transactions` / `bank_flows` / `bank_statements` = 0 / 0 / 0 行**（表建了从未写入）
- **`accounts.code` 全空** ⇒ 系统账户与外部渠道账户**无连接键**
- 对照：`sale_orders` 580 · `sale_order_items` 15,539 · `receivables` 181 · `contacts` 730

**三条推论**：
1. 对账的**两个操作数都不在系统里** ⇒ 形态是 **「上传 A vs 上传 B」**，不是「系统 vs 上传」。
2. 需一层 **`account_channel_map`**（舟谱账户号 ↔ 银行账号 ↔ 微信商户号），否则 AI 归因没上下文。
3. **银行直连不要重启**（三张表建了从未写入 = 这条路没走通）；继续走上传，与既有战略一致。

## 三、🔴 真源在用户的文件夹里，不在代码里

`~/Documents/流水对账/` = 用户已跑通的**手工版**（重做 = 产品化它）：
**9 个数据源**（农信/农行/工行/建行/邮储个人/邮储公户 含 **PDF** + 支付宝/微信 + **舟谱「账户收支明细表」**）
+ **8 个脚本** + **15 份报告**（收付账户对账 8 份、货损 6 份、催收 1 份）。

从 `微信对账报告_2026年3月.md` 反推的 **5 条硬规则**（都是确定性的，不是 AI 判断）：
1. 匹配单位是**「组」**不是「笔」（130 组 vs 144 笔）；正文有「**微信40+20**」⇒ **组合匹配**
2. **跨账户动作单列排除**（提现 ¥78,070 不进四象限）
3. **退款识别并冲减**（舟谱 `¥-570` ↔ `¥570`）
4. 差异必须带**「原因 + 建议」且落枚举**（退款/补录/核查/跨账户）
5. **必须支持只有一侧数据**（工行 5 月报告开头即 `⚠️ 仅舟谱数据，缺少工行银行流水`）

## 四、旧版为什么必然失败（两条代码级实证）

1. `reconciliation.py:96-106` 的「自动匹配」是**假算法**：逐条**无条件**标 `matched` 并递减余额，
   `matched_total` ≈ 全部单据之和 ⇒ **它不在配对，在遍历**。
   根因是**建模错误**：用 `amount: float`（一个口述标量）做对账输入，承载不了「哪几笔、何时、什么方向」。
2. 同屏两个「系统应收」差 17.7 万：页面头部 = `receivables` **余额**；
   匹配结果 = `Σ sale_orders − Σ payments` **区间发生额**。**两者本来就不该相等，却都叫「系统」。**
3. **39 次访问 0 次确认是理性行为**：`confirm` 只写一条 `audit_log`，**不改状态、不解除阻塞、无后果**。

## 五、🔴 七步分工（用户原话的拆解）

`上传 → AI 认列定键 → 人确认键 → 确定性算子比对 → AI 归因 → 人确认 → 人改数`

| 步骤 | 代码 | AI | 人 |
|---|---|---|---|
| 1 上传 | 存 `file_id`、读 sheet/列/前 N 行 | — | 声明「这是什么」 |
| 2 认列定键 | 出候选（`COLUMN_PATTERNS`）+ `samples` + `validation` | 判定键列、给 `confidence` 与理由 | — |
| 3 人确认键 | 渲染可改表；**execute 照用 mapping** | — | 改判并确认（尤其收/支是否分两列） |
| 4 **确定性比对** | 按键配对（容差/组合/跨期/排除） | 🔴 **禁止参与** | — |
| 5 归因 | 只提供上下文（摘要/备注/操作人/历史） | **原因 + 建议，必须落枚举** | 否决/修正 |
| 6 人确认 | 写快照 + 解锁下游 | — | 确认 |
| 7 人改数 | 过写入口门禁 + 留旧值 | 可起草 | 点「补录/冲销」 |

**🔴 第 6 步必须产生后果**，否则重做后仍然 0 次确认。建议先挂**催收队列**
（514 次真实调用说明它是活跃场景，回报可感知）；「未对平不许结账」留到结账模块上线。

## 六、可复用资产

| 环节 | 位置 | 成熟度 |
|---|---|---|
| 认列 | `server/excel_recognizer.py`（`COLUMN_PATTERNS` 9 字段 + `detect_format` + `_fuzzy_detect`，193 行） | 🟢 高 |
| 认列确认 UI | `hergent-cn-v2/src/components/ImportMapping.vue`（v178 四处共用） | 🟢 高 |
| 认列协议 | `server/routers/import_router.py:171` `FIELD_LABELS`（标签唯一一份）+ `/preview` | 🟢 高 |
| 写入通道 | `/api/import/execute` **照用前端 mapping** ⇒ **改判真生效、零 DDL** | 🟢 高 |
| 比对算子 | ✅ **`server/recon_engine.py`（v198 新建，788 行，纯标准库无 IO）** | 🟢 高 |
| 单测 | ✅ `server/tests/test_recon_engine.py`（45 项断言，`python3` 直跑） | 🟢 高 |
| 键对账（旧语义） | `server/spreadsheet_query.py:102` `op="match"` → 已收拢进 `recon_engine.reconcile_by_key()` | 🟢 高 |
| 跨文件算 | ✅ `mcp/spreadsheet_server.py`（v198 入库；旧在 `/opt/hergent-mcp-spreadsheet/server.py:150`） | 🟢 高 |

## 七、✅ P0 已落地（v198 · 提交 `9bcdb11` · 2026-09-19）

`hergent-erp/server/recon_engine.py`（788 行，纯标准库、无 IO、可确定性重放，19 个导出符号）
+ `server/tests/test_recon_engine.py`（**45 项断言全绿**，不依赖 pytest：生产 requirements 未装，直接 `python3` 跑、失败退 1）
+ `mcp/spreadsheet_server.py` 与 `mcp/stubs/core.py` 纳入仓库（共 4 files, +1388）。

**7 项能力全部落地**：组合匹配（子集和，≤4 笔 + 金额倒序剪枝）· 日期容差 · 方向规范化
（`two_col` / `one_col_dir` 两种列布局）· 手续费拆分 · 跨账户排除 · 退款冲减 · 多账户（由调用方分组）。

**硬验收 —— 用微信 2026年3月真实数据复现，逐笔一致**：

| 侧 | 笔数 / 金额 | 结果 |
|---|---|---|
| 微信 收入 | 144 笔 ¥75,222.66 | ✅ 逐笔一致 |
| 微信 支出 | 50 笔 ¥46,956.12 | ✅ 逐笔一致 |
| 舟谱 支出 | 47 笔 ¥111,266.32 | ✅ 逐笔一致 |
| 提现（跨账户） | 6 笔 ¥78,070.00 | ✅ 不进四象限 |

四象限：**13 笔 ¥17,888.09 / 9 笔 ¥4,901.60 / 10 笔 ¥5,754.80 / 7 笔 ¥70,065.00** —— 全部可解释。
（与方案 §7 预估的 ¥17,991.59 / ¥11,814.00 有差，差额已在 04 节归因到人工报告本身。）

### 7.1 🔴 人工报告自己有 4 处错（本轮的意外结论）

回放不只是验引擎 —— 它**同时验了那份人工报告**。4 处差错**全部精确归因**：

1. **同额同日漏配** $103.50 —— 人的一对一配对被同额冲掉
2. **退款符号误记** $6,912.40 —— 舟谱退款单在「收入」列里是**负数**，报告把符号吃掉了
3. **合计剔除闭合验证**：`62,236.17 + 6,912.40 − 103.50 = 69,045.07` ✓ —— 三处错误恰好互相抵消，
   所以报告**总额看起来是对的**，只有逐笔回放才能抓出来
4. **组合匹配漏 1 笔**（把「1 笔对 2 笔」漏成 1 组）

⇒ **判据：人工报告不是黄金标准，只是「另一个待验证的实现」。** 验收口径 = 逐笔可解释，不是「与报告数字相同」。

### 7.2 🔴 反推人工算法的方法（本轮加速最多的技巧）

**别猜算法 —— 读报告自己给的计数。**

微信报告写「已匹配收入 **130 组**、支出 **40 组**」。而微信原始侧是 144 笔 / 50 笔。
用「金额相等 + 同日一对一」暴力匹配，得 `144−14=130`、`50−10=40` —— **与报告完全吻合**。

⇒ 人工算法 = **金额相等 + 日期容差 ≥1 天**。一次试算就锁定了容差参数，比读代码/问用户都快。
**通用判据：报告里的每一个「组数/笔数」都是一个约束方程，先把它当已知量解出参数，再动手实现。**

### 7.3 🔴 两个只有真实数据能抓出的引擎缺陷（45 项单测全绿也没抓到）

1. **`bisect` 要求被搜索的序列自身有序** —— 我把「按日期排序后的金额列表」喂给 bisect，
   真实数据上**已匹配组数从 170 掉到 13**。必须单独维护 `_by_amount = sorted(rows, key=(amount, ...))`
   这条平行数组。⇒ 新增 **T11 规模回归**（60 组乱序 + 同日多笔乱序）；小数组（1–5 行）上 bisect 会偶发命中，
   **所以单测绿 ≠ 引擎对**。
2. **退款冲减不能用「单据编号」当同键判据** —— 舟谱收款单是 `SK…`、退款单是 `XD…`，编号本就不同，
   永远配不上 ⇒ 该能力会**永久静默失效**。改为默认 `refund_group_field="counterparty"`（同结算单位）。

### 7.4 顺手还的三笔债

- `KIND_COMBINATION` 与 `combination_mode` —— 组合匹配默认 **`candidate`（只给线索、不判平）**，
  `"match"` 需显式开启。实测 3 组组合里 **1 组对方名零交集** ⇒ 默认保守是**正确**的不是偷懒。
- `reconcile_by_key()` 兼容层 —— 把既有两处同一套语义（`spreadsheet_query._run_query(op="match")`
  与生产 MCP 工具 `spreadsheet_reconcile_files`）收拢到一处，消除双份漂移源。
- 确定性契约：所有遍历顺序显式全序 `(date, amount, counterparty, key, orig_index)`；
  119,396 字符结果**两次字节级一致**。

## 八、✅ 债务已还：MCP spreadsheet server 已纳入版本控制（v198）

旧状态：`/opt/hergent-mcp-spreadsheet/server.py`（9,321 B，3 工具）**在仓库里 grep 不到**，
生产上有过手工改动留痕（`server.py.bak-reconcile-20260822-143000`），
而 `CopilotDrawer.vue:997` 的提示词**已要求 AI 调用它** ⇒ **「AI 承诺了后端没有的能力」**。

**上一轮的误判更正**：当时以为它「自带一份实现」，实际是 **import 后端复用** ——
真实风险只是「外壳不在版本控制」，不是「逻辑双份」。

v198 已入库 `hergent-erp/mcp/`（`spreadsheet_server.py` + `stubs/core.py`）：
- `_reconcile_files` 改调 `recon_engine.reconcile_by_key`，删掉内联比对逻辑
- **新旧实现业务等价** —— 唯一差异是 `mismatch` 列表顺序（我按 key 排序 = 确定性改进，旧实现依赖文件行序）
- 生产部署已验证：stdio 握手成功（`hergent-spreadsheet 3.4.7`）、3 工具完好、签名逐字一致
- 已备份 `server.py.bak-v198-20260919-104829`

> **通用判据（保留）：任何被写进 AI 提示词要求调用的工具，其实现必须在版本控制内。**

> **踩坑备查**：生产验证脚本不能用 `import server` —— `sys.path.insert(1, "/opt/hergent-erp")` 后
> 命中的是**后端主文件** `/opt/hergent-erp/server.py`，而 stub `core.py` 只提供 `_auth`，
> 报 `cannot import name '_hpw' from 'core'`。改用 `importlib.util.spec_from_file_location()` 按路径加载。
> 另：生产 MCP 用 `fastmcp 3.4.7`，列工具用 `mcp.list_tools()`（`_tool_manager._tools` 已失效）。

## 九、红线

1. **AI 不得参与比对判定** —— 算归代码、判归人；AI 只做归因与起草，且必须落枚举值。
2. **不回写客户 ERP / 舟谱原始账** —— 对账只发现问题、记录结论；改数由人去原系统做。
   一旦回写就从分析层越界成交易层，与「不造 ERP、订单 CRUD 冻结」直接冲突。
3. **组合匹配必须设上限** —— 子集和 NP-hard；≤4 笔 + 金额倒序剪枝，超限只给候选不判平。
   ✅ **已落地**为 `ReconSpec.combination_mode`，**默认 `"candidate"`**（`ReconGroup.kind = KIND_COMBINATION`，
   不占用四象限行），`"match"` 需显式开启。候选按「对方名是否有交集」分 `strong` / `weak`。
   实测真实数据 3 组组合里 **1 组对方名零交集** ⇒ **只能给候选、不能自动判平**，默认保守是正确取值。
4. **脱敏红线不变** —— 报告含客户名与真实金额 ⇒ 对外材料一律用**演示租户**。
5. **PDF 银行流水本轮不做** —— 6 家里 3 家给过 PDF；文案引导「请另存为 Excel」。

## 十、场景与顺序建议

| 场景 | 一侧 | 另一侧 | 频次 | 系统侧可用度 |
|---|---|---|---|---|
| **A 收付账户**（建议先做） | 舟谱账户收支明细表 | 银行/微信/支付宝流水 | 月末必做（8/15 份报告） | 🔴 零 |
| **B 下游客户** | 舟谱应收款明细表（**39,644 行**） | 客户认账 | 月/季 | 🟡 181+580 |
| **C 厂家预付款** | 舟谱预付款单 | 银行付款流水 | 按批次 | 🔴 零（1 行） |

**建议 A 优先**：需求已实证（8/15 份、跨 2—7 月反复做；B 类零份独立报告）·
B 在 Hergent 里**做不出有意义的结论**（系统 580 单 vs 真源 39,644 行，差异几乎全是「系统没录」）·
**A 是七项能力的最严苛测试床**（先在 B 上做会把 `recon_engine` 接口做窄，回头必须推翻重来）。
**C 并入 A** 作子视图（同一份银行流水按「对方户名=厂家」筛），不单开入口。
