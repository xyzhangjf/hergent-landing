# 2026-08-20 工作日志

## 金蝶卡链接 URL 修复（前端）
- `hergent-cn-v2/src/pages/ConnectCenter.vue` 金蝶弹窗平台链接由错误 `https://openapi.kingdee.com` 改为正确 `https://open.kingdee.com/K3Cloud/Open/Home.aspx`，文案「金蝶开放平台」→「金蝶开发平台」。Vite 构建 → rsync `/opt/hergent-cn-v2/` → chown，生产 bundle 校验旧网址 0 处、新网址 1 处。

## 战略讨论：平台优先（ERP 外包给畅捷通/金蝶）
- 用户（非技术 solo 创始人）明确战略收敛：只做行业垂直 AI 应用层，ERP 重活交给畅捷通/金蝶，将来客户足够多且有必要再考虑自建（但须警惕「将来自研完整 ERP」是陷阱）。
- 我的研判：方向对，但「深度绑定」应改为「生态集成但不绑死」；护城河=低温奶配方化算法+副驾交互，必须保持 connector 可插拔 + Excel 降级；差异化靠行业深度而非通用 AI 问数（畅捷通精易版已内置）。已记入 MEMORY.md 战略收敛条。
- 含义：已建的 kingdee/chanjet connectors(T3–T9) 正是该模式的集成层，价值凸显；原生厚 ERP 继续去权重。

## 副驾 UI 规范文档 + M1 开发（已上线）
- 产出 `Hergent-副驾UI规范-2026-08-20.md`：定位/6 原则/视觉令牌/ResultCard 范式(数据契约 message.card)/交互模式/Roadmap(M1–M5)/不要照搬边界。
- M1 落地：新建 `src/components/ResultCard.vue`（通用经营结果卡：场景徽标/摘要/指标网格/要点/状态留痕/操作区）；`CopilotDrawer.vue` 集成 `m.card` 渲染 + 欢迎页「查看示例经营卡」按钮(插入 demo 货损卡) + `onCardAction`(采纳/驳回翻转状态并回写对话)。消息模型由 `{role,content}` 扩展为 `{role,content,card?}`。
- 构建：`npm run build`（65 modules，managed node 22）通过；`rsync -a --no-owner --no-group --delete dist/ root@47.113.224.140:/opt/hergent-cn-v2/` → chown，生产 bundle 校验 `查看示例经营卡/本月货损核算/采纳建议` 均 1 处、index.html 已引用新 hash(index-DvGPMMZl.js)。CopilotDrawer 在 Shell.vue 已挂载。
- 待办：M2 渐进式访谈引导卡 / M3 任务进度步骤条 / M4 后端结构化卡片协议(Hermes 返 card JSON) / M5 微信语音本土化。真机浏览器交互验收建议用户在 hergent.cn 自测（点示例卡→点采纳/驳回）。

## 副驾 M2–M5 推进（已上线，2026-08-20 同日）
- 按进度推进 M2–M5，新增 `src/components/ProgressSteps.vue`（步骤条：done/active/todo 自动推断）。
- M2 渐进式访谈：`CopilotDrawer` 渲染 `m.followups`（澄清引导 chips，点选追加提问）；demo 串联展示。
- M3 进度透明 + 迷你图：`m.progress` 步骤条；`ResultCard` 图表占位替换为真实 sparkline（inline SVG，series→polyline）。
- M4 后端卡片协议前端适配：`extractCard()` 流式解析 Hermes 输出 ` ```card\n{...}\n``` ` 围栏，自动 attach 到 assistant 消息 `card` 字段——后端一旦吐信封前端零改造渲染（后端信封/企微直推仍待后端通道）。
- M5 本土化：`onCardAction` 加 `forward` 分支→转发面板（生成纯文本经营摘要 + 复制/navigator.share）；输入框加语音按钮（Web Speech API，`lang=zh-CN`）；detail 不再误翻状态（仅 adopt/reject 改 status）。
- 构建：`npm run build`（67 modules）通过；rsync→chown 至 `/opt/hergent-cn-v2/`，生产 bundle 校验 `转发到微信/按客户维度看货损/读库存` 各 1 处、index.html 引新 hash(index-CNwhs55B.js)。
- 规范文档 Roadmap 表更新 M1–M5 为 ✅（M4/M5 标注「前端侧完成，后端信封/企微直推待接」）。
- 真机验收仍需用户在 hergent.cn 自测：示例卡→followups chips→进度条→卡片迷你图→采纳/驳回→转发面板→语音按钮。

## M4 后端垂直线扩展：工资/返利卡（已上线，2026-08-20 同日）
- 用户确认把 M4 货损卡范式复制到「工资」「返利」卡（"要"）。
- 后端 `server/server.py` 新增 `GET /api/ai/payroll-card`（调 `db.calculate_commissions()`，当期月只读聚合：提成总额/人数/销售总额/人均 + Top3 + 迷你图，type=payroll）与 `GET /api/ai/rebate-card`（调 `db.get_rebate_progress()`，未结合同+已达成：合同数/目标额/已达成/预计返利 + Top3 达成率 + 迷你图，type=rebate）。复用 loss-card 同款范式：_auth 自动租户上下文、ai_disabled 短路、`{card:null,reason}` 静默降级绝不 500。
- 前端 `CopilotDrawer.vue` 新增 `PAYROLL_RE`/`REBATE_RE` 正则 + `fetchPayrollCard()`/`fetchRebateCard()`，复用 `fetchLossCard` 范式；`send()` 链 `.then` 改为 `Promise.all([fetchLossCard, fetchPayrollCard, fetchRebateCard])` 三联命中拉卡。卡片消息仍在流式 `.finally` 后 push（防 onDelta 错位）。`ResultCard.vue` 原生支持 payroll/rebate 场景，零改组件。
- 构建：`npm run build`（67 modules，index-BTo-CcDi.js）通过，bundle 校验含 `payroll-card`/`rebate-card` 各 1 处；前端 rsync→/opt/hergent-cn-v2/+chown。后端 flatten rsync server/→/opt/hergent-erp/（排除 *.db*/__pycache__/static，无 --delete 保 prod-only 目录）→rm __pycache__→chown→systemctl restart hergent-erp。
- 生产真卡验证（SSH localhost + demo-login token）：HEALTH 200；payroll-card→type=payroll「本月暂无已核算的提成数据」；rebate-card→type=rebate「当前无未结返利合同」；loss-card 回归→10 临期 SKU 正常。三卡范式联调通过，demo 租户无数据故走降级 summary 而非报错。

## 战略讨论：聊天在左 + 右侧 Artifact 面板
- 用户提出是否做「聊天在左，聊天产生的报告/图表/文件沉淀到右侧 Artifact 面板」（WorkBuddy 同款双栏模型）。
- 研判：这是借鉴点①（对话+产物双栏）的成熟形态，M4 已验证数据层（真实数据→卡片），缺的是给产物一个持久固定家。对「经营副驾」价值高（老板边看报告边追问）。但当前副驾是覆盖 ERP 的 420px 右侧抽屉，真双栏会与抽屉空间冲突、且非技术用户对"面板"概念认知成本更高——故分相推进（先抽屉内"可钉住产物栏"，再升格为真双栏）。

## 副驾方案 B 双栏 Artifact 面板（已上线，2026-08-20）
- 用户拍板「B」：副驾打开后=左聊天 / 右产物双栏，ERP 被临时盖住。
- `CopilotDrawer.vue` 重构：`.copilot` 宽 420px→`min(880px,94vw)`；新增 `.cp-split`(flex row) 包 `.cp-chat`(左，含原 cp-body+cp-foot) 与 `.cp-artifacts`(右 384px 固定，本次产物面板)。产物由 `artifacts` computed 从 `store.chat.messages` 抽带 `card` 的消息（最新在上）；每条渲染 `ResultCard :compact`；点击 `jumpTo(index)`→滚动并 flash 对应聊天消息。
- `ResultCard.vue` 新增 `compact` prop：隐藏要点/迷你图/操作区，仅留 徽标+标题+摘要+指标网格，作面板压缩瓦片。聊天内仍渲染完整卡（叙事），面板是持久索引——避免双份完整卡。
- 响应式：`@media(max-width:760px)` 下隐藏 `.cp-artifacts`、`.cp-split` 改竖排、宽 100vw，移动端退化为原单栏（内联卡仍可见）。
- 构建：`npm run build`（67 modules，index-D5hs-1zu.js）通过（Vue SFC 模板编译即结构校验）；rsync→/opt/hergent-cn-v2/+chown，bundle 校验含 `cp-split`/`cp-artifacts`/`本次产物`/`compact` 各 1+ 处。
- 验收缺口：本沙箱无 Chromium/Playwright，未做真机点击 through；建议用户在 hergent.cn 自测（开副驾→问"算工资"→卡同时出现在对话与右栏→点右栏瓦片跳回对话）。后续 forecast/reconcile/kpi 卡沿用同款自动入右栏（无需改面板）。

## 副驾全屏按钮（已上线，2026-08-20）
- 用户确认给副驾加「全屏」按钮（"要"）。定位：可选全屏态、**非默认全屏**——把当初否掉的方案 C 降级成抽屉内开关，保留方案 B 的上下文连续性，给大屏/深度分析一个放大出口。
- `CopilotDrawer.vue`：模板根 `<aside class="copilot" :class="{ 'is-typing':..., fullscreen: isFullscreen }">`；header 在关闭按钮前加全屏切换按钮（⛶ maximize 图标，`:title` 动态"全屏/退出全屏"，`@click=toggleFullscreen`）；script 加 `const isFullscreen=ref(false)` + `toggleFullscreen()`；`onMounted` 内 `window.addEventListener('keydown', onKeydown)` 监听 Esc 退出全屏，`onBeforeUnmount` 移除（import 补 `onBeforeUnmount`）。
- 样式：`.copilot.fullscreen{width:100vw;height:100vh;left:0;right:0;top:0;bottom:0;border-left:none;border-radius:0;box-shadow:none}` 铺满视口覆盖 ERP；`.copilot.fullscreen .cp-artifacts{flex-basis:460px}` 全屏下右产物栏加宽。窄屏(<760px)仍单栏（artifacts 隐藏），全屏按钮保留但窄屏意义低。
- 构建：`npm run build`（67 modules，index-i-zUwHpx.js + index-BYuME1kd.css）通过；rsync→/opt/hergent-cn-v2/+chown，bundle 校验含 `fullscreen`×2(JS)/×2(CSS)、`cp-artifacts`×1(JS)/×3(CSS)；`toggleFullscreen`/`isFullscreen` 被 terser 压缩不显明文但逻辑在（字符串类名 fullscreen 保留，SFC 模板编译过即结构正确）。
- 验收缺口：沙箱无浏览器，真机点击（开副驾→点全屏→双栏铺满、产物栏加宽→再点/Esc 还原）需用户在 hergent.cn 自测。

## 对标 WorkBuddy 借鉴点审计 + 增量补齐（2026-08-20 晚）
- 用户要"依次做但先看已有什么、不要重复做"。全面审计 hergent-erp/server + hergent-cn-v2 前端后结论：**多数借鉴点已存在，只补真缺口**。
- 审计结果（已存在→不重做）：
  - P0-1 自动化简报：`scheduler.py` 常驻守护线程（6:00 采购计划 / 7:30 AI洞察 / 8:00 盈亏快报 `profit_reporter.push_daily_report`→企微+`db.message_send`），`dami.py` 自然语言建定时任务，`feishu_notify.py`/`notify/wecom_notify.py` 推送通道，前端 `pages/CronJobs.vue` 管理定时任务 → 调度/推送/UI 全有。
  - P0-2 任务进度：`ProgressSteps.vue` 已接入 `CopilotDrawer`(line 92)，但 `m.progress` 从未被真实流赋值（line 339 是 demo 对象）→ **死组件，是真缺口**。
  - P0-3 对话内图表：`ResultCard.vue` 已带 `chart`(mini sparkline)，`Dashboard/LossWorkflow/Rebate/Reconciliation` 等页有图表 → 卡片图已覆盖。
  - P1-4 老板画像/记忆：`memory.py`「大秘自进化记忆系统」已记录纠错/模式 → 已有。
  - P1-5 能力市场：`ConnectCenter.vue` 能力中心(连接器+专家+技能+工作流) → 已有。
  - P1-6 数据溯源：卡片/结论不显示"数据来自哪" → **真缺口**。
- 增量补齐（均已上线生产 + 验证）：
  1. **P0-1 真缺口**：`scheduler.py` 新增 `_push_copilot_cards_brief()`（每天 8:05 跑），复用 `wastage_summary(30)`/`scan_expiry(0)`/`calculate_commissions()`/`get_rebate_progress()` 四个只读聚合 + `db.message_send` + `feishu_notify.push_to_wecom/push_to_feishu`（未配 webhook 自动 no-op）；fail-closed（单段异常仅跳过不崩）。把刚做的 3 张经营卡聚合成「经营卡简报」推送给老板。手动触发验证：`[Notify] Message: 08月20日 经营卡简报` + `Copilot cards brief pushed: 3 sections`，health 200。注：与盈亏快报一致按默认租户聚合（单租户/demo 行为，多租户需另循环）。
  2. **P0-2 真缺口（喂活死组件）**：`CopilotDrawer.vue` 新增 `REVIEW_RE`(复盘/经营分析/经营简报/整体经营…) + `REVIEW_STEPS`(货损卡→工资卡→返利卡→汇总报告) + `runReviewPipeline(q, progressMsg)`。`send()` 流式结束后若命中 REVIEW_RE，push 带 `progress` 的进度消息并按顺序跑三卡、逐步点亮 `ProgressSteps`（复用现成组件）；单卡意图(货损/算工资)保持原 `Promise.all` 并行不变→零回归。构建通过(index-B0Vnk4eX.js)，bundle 含 `复盘`×3/`汇总报告`×1，已部署。
  3. **P1-6 真缺口**：`server.py` 三个 `*-card` 端点各加 `source` 字段（表名+时间窗，如"报损审批单 + 库存效期表（近 30 天）"）；`ResultCard.vue` 渲染 `📎 {{ card.source }}`（全模式含 compact 右栏都显示）+ 样式 `.rc-source`。生产验证三卡均返回 `source`，health 200。
- 未做（更低优先级，非缺口）：P2-7 一键导出图片/PDF、P2-8 结论分享给员工、P2-9 UI 评审纪律（方法论已在用）。可作为下一步候选。
- 验收缺口：沙箱无浏览器，P0-2 进度条真机点亮需用户在 hergent.cn 自测（问"帮我做个月度复盘"→应见进度条逐个点亮+三卡依次出现）。

## 订单汇总/下单 流程全盘梳理 + 产品落地方案（2026-08-20 晚）
- 用户给了真实表 `舟谱导入模版/最新26年8月下单表8.18.xlsx`，并用 tencent-docs-sheetagent 子代理完整还原：不是一张表，是 **69 个工作表**（每表=双日报单周期 `M月D报单-M月D到货`），行=**商品×下单主体**（恒滋/福宝 拆两行，~160行），列=**动态客户**（业务员/导购/门店/渠道 19-21列，名单随周期变），格=订货数量（稀疏），计算列含分销价(÷0.9返点)/合计/件数/最终下单/下单金额；**缺失返利/库存/余额/付款/结构化日期**（日期塞在 `AG 订单排期` 自由文本）。数据质量痛点：永辉代码混填、编码不统一、无冻结、D列简称非结构化。
- 用户 6 步流程：①企微智能表填预报→②文员导出Excel粘贴下单表发审→③同事综合指标达成(总/单品+返利)+库存拍板最终订单→④文员查厂家余额、订单−余额=差额、微信群通知付款→⑤付款到账粘贴厂家系统下单→⑥到货前Hermes生成舟谱模板手动导入。每2天到货、提前4天下单，报单靠经验。
- **关键代码实证（不重复造）**：`forecast_submission_summary` 按 SKU×单位聚合 + `sources`(含 store_name/role/user_id/qty) → 前端透视即可还原"商品×客户"交叉表**不改模型**；`forecast_periods` 周期实体已存在；`contacts` 客户主数据已存在；`import_router` 成熟导入器；`scheduler` 推送；M4 副驾卡/双栏/全屏/进度条/溯源；`forecast_engine.forecast_product` AI建议量 hook；`forecast_submission_pending` 单条审批+库存对比；Hermes 舟谱模板。
- **真缺口（方案补）**：①二维交叉表视图(透视 sources) ②Excel导入 forecast_cross 类别 ③定时催单 ④副驾 forecast-card ⑤**周期级 AI 审核台**(指标+库存+返利→建议最终订单，竞品无，护城河) ⑥厂家余额&付款闭环 ⑦一键舟谱模板按钮 ⑧结构化日期/下单主体拆字段 ⑨返利关联。
- 落地方案已写成文档 `订单汇总下单-产品落地方案-2026-08-20.md` + 目标流程架构图（Visualizer）。建议先打 **P0 四件套**(交叉表+Excel导入+催单+副驾读)，纯前端为主零新模型风险，立刻替代企微+Excel手工链；P1-1 AI审核台为护城河放第二阶段。
- 3 个待定建模决策：交叉表"列"=报单单元(默认 store/可切业务员)；下单主体(恒滋/福宝)拆独立维度；返利/库存/余额走主数据录入+Excel导入降级(蒙牛/舟谱无API已定)。

## P0-1 交叉表视图 UI Mock 交付（2026-08-20 晚，用户"要"）
- 产出 `预报工作台-交叉表-Mock-2026-08-20.html`（自包含单文件，双 tab：界面效果/数据结构）：
  - 界面：周期切换+状态徽标+导入Excel+催未报者+一键生成下游订单(占位) 工具栏；SOP 固定条（8.14报单/8.18到货/恒滋福宝客户代码/单双号规则）；交叉表=冻结首列+冻结表头+业务员/门店分组列头+客户分级A徽标+单元格hover金额/返利档/距下档差+行小计(合计)/列小计+底部钉条(今日总订量/较上期↗/还差X人未报)；下市行置灰删除线；AI 建议徽标。
  - 数据结构：period(复用 forecast_periods) + units(报单单元=动态列，contacts 主数据，reported 派生) + rows(商品×下单主体，qty_by_unit 由 summary.sources 透视) + totals(行/列小计+总额)。计算列公式对齐 Excel(合计/件数=合计÷规格/最终下单=件数+加单/金额=最终下单×单价)。
- **关键决策**：先纯前端透视现有 `/api/forecast-submissions/summary` 的 sources → 零后端改动上线；量大多期再补 `GET /api/forecast-submissions/cross` 服务端直出。
- 视觉基准=现有 Forecast.vue 风格(page-hd/card toolbar/table-wrap/tbl/btn/tag)。mock 已对齐，等用户红框反馈后上码。

## P0-1 交叉表视图 端到端上线（2026-08-20 深夜，用户"继续"）
- **后端**（最小改动，向后兼容）：
  - `erp_db.forecast_submission_summary(order_date, start_date, end_date)` 增加日期区间过滤（`s.order_date BETWEEN ? AND ?`，另有 >=/<= 分支）；detail 查询补 `s.store_id` 字段 → sources 现含 store_id。
  - `routers/forecast_submissions.py` GET /summary 增加 `start`/`end` 查询参数透传。
- **前端**：
  - `api/modules.js` `forecastApproveApi.summary(date, start, end)` 三参拼接。
  - `Forecast.vue` 加 `viewMode`(list/cross) 切换 tabs（列表视图按钮条件渲染，零回归）；交叉表视图=周期 SOP 条(期次名/下单窗口/到货日) + 冻结表头/首列交叉表(行=商品含 spec/unit/AI建议行内提示，列=报单单元按 sources.store 去重保序，格=数量 hover 金额估算) + 计算列(合计/件数=合计÷规格/最终下单=件数(加单暂0)/单价=金额÷合计/下单金额) + 列小计行 + 底部钉条(总订量/总金额/SKU数/报单单元数) + 空态。数据=summary(start,end) 透视 sources 成 qty_by_unit，纯前端计算。
- **验证**：py_compile OK；vite build 67 modules(bundle index-tOthsBxp.js，含 交叉表视图×1/qtyByUnit×6/报单单元×3/cross-tbl×1)；双端部署+restart；生产 SSH 实测：HEALTH 200 → demo-login → POST 测试报单(2 items 42件) → `summary?start=2026-08-20&end=2026-08-20` 返 2 行、sources 含 store_id + 姓名补全；单日 `date=` 回归 2 行不破。demo 租户无 forecast_periods/历史报单 → 交叉表在 demo 显示空态（已造 2 条测试报单便于用户自测）；真实租户有期次+报单即出真表。
- 待办：P0-2 Excel导入 forecast_cross 类别、P0-3 定时催单（依赖"应报名单"口径确认）、P0-4 副驾 forecast-card；"还差谁没报"列暂缓（需报单单元候选口径）。

## P0-2 Excel 导入 forecast_cross 端到端上线（2026-08-20 深夜，用户"继续"）
- **后端**（routers/import_router.py，984 行类别驱动导入器扩展）：
  - COLUMN_PATTERNS/_all_fields/templates 增 `forecast_cross`（name/barcode/spec/unit/price + customer）。
  - `/preview`：category=forecast_cross 时 `_detect_forecast_cross` 识别商品身份列 + 动态客户列；未映射非空表头补 `suggested_field:"customer"`（⚠️ 坑：`_suggest_mapping_smart` 对**所有**表头都返回条目，used 集合须只取 `suggested_field` 非空的，否则客户列被挡）。
  - `/execute` 早退分支 `_execute_forecast_cross`：逐客户列聚合成 `forecast_submission_create`（role='导入'，store=客户列名，order_date 可指定=周期对齐）；幂等=先清同 order_date 的 role='导入' 数据再写入（重导即覆盖，不影响小程序报单）；商品匹配 `_match_product`（条码优先/名称次之，找不到 product_id=0 原样保留名称）。
  - erp_db：`forecast_submission_create` 增 `order_date` 参数（绑定参数替代 `date('now','localtime')` 字面量，防注入+可指定日期）。
- **前端**：modules.js `importApi.execute(file, category, mapping, extra={})` 支持附加表单字段；Forecast.vue 交叉表视图加「导入 Excel」按钮 + Teleport 弹窗（选文件→preview 显示身份列/客户列识别 + 矩阵预览→确认导入→结果/错误列表→刷新交叉表），order_date=当前期次 order_start。
- **两个真 Bug（E2E 揪出并修复）**：
  1. summary `sources` 按 `(product_id, unit)` 分组，未匹配主档商品 product_id=0 时**不同商品坍缩进同一桶**（红桶 sources 混入低脂高钙/每日鲜酪数量）→ 分组键改为 `(product_id, product_name, spec, unit)` 与 rows 聚合对齐。
  2. detail SELECT 漏 `i.spec`，新分组键访问 `d["spec"]` IndexError 500 → 补选。
- **生产验证**（SSH 构造仿用户布局 xlsx：商品名称/条码/规格/单价 + 唐成/王琴/吾悦 3 客户列）：preview 识别 identity{name,barcode,spec,price}+3 customers；execute success=3、order_date=2026-08-18；summary 区间 3 行、sources 精确（低脂高钙=唐成60/王琴6/吾悦18，每日鲜酪空值跳过）；**重复导入幂等**（success 仍 3、summary 仍 3 行不翻倍）；单日回归 2 行。HEALTH ok。
- bundle index-B3a7A0DC.js 含 导入 Excel/forecast_cross/客户列。demo 租户无真实商品 → 商品 product_id=0 名称保留（真实租户有主档即自动匹配）。

## P0-3 定时催单 + 真实表导入陷阱修复（2026-08-20 深夜，用户"继续"）
- **scheduler.py** 新增 `_push_forecast_reminder()`（按租户遍历 tenant_*.db，扁平租户库布局，set_tenant_context 逐个上下文跑，fail-closed）+ `push_forecast_reminder_current()`（供手动端点复用）+ `_forecast_reminder_state()` + `_tenant_ids()`；调度点 **每天 9:00/16:00**（仅报单窗口内、存在未报单元时推送）。
- **应报名单默认口径**（免配置）：近30天非『导入』报单过的单元（store_name 去重），自动跟随历史习惯；`role='导入'`（Excel 导入）不参与应报/已报统计。催单内容：期次名+下单窗口+未报单元名单+已报单元数/总订量/总金额 → `db.message_send`+企微/飞书（无 webhook no-op）。
- **手动端点**：`POST /api/forecast-submissions/remind`（当前租户上下文，pushed=true/false）。
- **生产验证**：建开放期次(08-20~08-22) + 直插历史候选(唐成 08-01) → `_push_forecast_reminder()` → `[Notify] 08月20日 预报催单 · 8月20报单-8月24到货` + `pushed: 2 missing`（解放路门店/唐成未报，测试门店已报）+ `tenant=10: pushed`；`POST /remind` → `{ok:true,pushed:true}`；测试数据已清理。
- **⚠️ 重大发现（用户已真实测过导入）**：tenant_10 里有用户拿真实下单表（8.18）试导入产生的 6 行垃圾——Excel 计算列（合计/件数/最终下单/下单金额/永辉代码/商品编码表头'3'）被当客户列导入（store=`3`、`合计\n（最小数量）`、qty 巨大如 13671000335582）。**修复**：`import_router.py` 加 `_CROSS_SKIP_KEYWORDS`（合计/件数/最终下单/下单金额/分销价格/电商/加单/订单排期/排期/状态/永辉/商品编码/编码/序号/备注/批次）+ 纯数字表头跳过（'3' 编码列）+ `identity_like` 排除所有命中身份关键词的表头（单价）。已清理 6 行垃圾（保留 23 行真实客户导入）。修复后真实结构 xlsx（含全部计算列）preview 客户列仅 [唐成,王琴,吾悦]、execute 只导真实客户、sources 精确。
- 部署：后端 rsync+restart（前端无改动）。真机验证需用户在 hergent.cn 重新导入一次真实表（旧垃圾已清，重导即覆盖）。

## P0-4 副驾 forecast-card 上线（2026-08-20 深夜，用户"继续"）
- 后端 `GET /api/ai/forecast-card`（server.py，chat 模块已授权）：复用 `scheduler._forecast_reminder_state()`（当前租户只读），组装 ResultCard 契约 `type:'forecast'`——title=周期名、summary=已报单元/总订量/金额/未报数、metrics=[已报单元/总订量/下单金额/未报单元(warn)]、points=[本期待报TOP + 还差X个单元未报]、chart=近7日每日报单件数(sparkline)、source=预报提交+期次。无开放周期 → card null 静默降级（reason=no_open_period）。
- 前端 `CopilotDrawer.vue`：`FORECAST_RE`(预报|报单|谁没报|还差.*报|订了多少|订货量|下单量|订单汇总|报单进度|催单|订货进度) + `fetchForecastCard(q)`（套 M4 范式，流式结束后 push 带 card 消息）；`send()` 并联链扩为 `Promise.all([loss, payroll, rebate, forecast])`。卡自动入右栏 Artifact 面板（artifacts computed 抽 m.card，零改面板）。
- 生产验证：无期次→null；建开放期次(08-20~08-22)→真卡（type=forecast、summary「已报 1 单元 · 42 件 · ¥3,546，还差 1 个单元未报」、points「还差 1 个单元未报：解放路门店」、chart series [0,0,0,120,0,0,42] 正确含 08-17 的 120 件）；清理。HEALTH ok，bundle index-DW67JEaP.js 含 forecast-card/谁没报/预报订单。
- **至此 P0 四件套全部落地**：P0-1 交叉表视图 + P0-2 Excel 导入 + P0-3 定时催单 + P0-4 副驾 forecast-card。下一序列= P1-1 周期级 AI 审核台（护城河，指标+库存+返利→建议最终订单）等。

## P1-1 周期级 AI 审核台上线（2026-08-20 深夜，用户"继续"）
- **审计发现**：`routers/forecast_audit.py` 的 `/compute`（草稿级单商品审核引擎：日均销量=近30天销售/天数，建议量=max(0, 日均×(lead+arrival)+安全库存−当前库存)，判定=高于/低于/吻合）已成熟 → **不重造，提升到周期级复用**。
- **重构**：compute 的审核循环抽成纯函数 `_audit_items(conn, items, arrival_cycle, lead_days)`（/compute 与 /audit-period 共用，零行为变化）。
- **新增端点**（routers/forecast_audit.py）：
  - `POST /api/forecast-audit/audit-period`：body={start,end} → `forecast_submission_summary(start,end)` 取整周期 SKU（仅 product_id>0）→ `_audit_items` 批量算建议量+判定 → 附 `_rebate_hint`（厂家指标达成率/返利档/冲档缺口，rebate_contracts+purchase_orders，无合同 None 优雅降级）→ 汇总（sku_count/总预报/总建议/缺货数/积压数）。纯只读（铁律：AI 只建议）。
  - `POST /api/forecast-audit/audit-period/adopt`：定稿落库新表 `forecast_audit_decisions(period_start/end, product_id/name/unit, requested/suggested/final_qty, verdict, adopted, decided_at/by)`，幂等（同窗口先清后写），AI 留痕 log_workflow_advice，绝不触发真实下单。
- **前端**：交叉表视图工具栏加「AI 审核本周期」按钮（需选期次）→ 弹窗：返利提示 + 汇总条（SKU数/预报/AI建议/缺货/积压）+ 逐 SKU 表格（名称/预报/库存/日均/建议量/定稿输入默认=建议/判定色）→「确认定稿（人点生效，AI 不下单）」。未匹配主档的 SKU 显示 error 行（防御渲染，不 NaN），定稿时过滤。
- **生产 E2E 验证**：造测试商品(pid 11, safety 5, lead 4)+库存(2)→建期次→报单 30 件→audit-period：suggested=3（5−2 ✓）、verdict「近30天无销售数据，请人工判断」、rebate_hint=None（demo 无合同）；adopt→decisions 落库 (11, 审核测试商品, 30, 3, 3) ✓；清理。bundle index-AiM-ITwW.js 含 AI 审核本周期/audit-period/定稿。HEALTH ok。
- 待办：单品级厂家指标表（target_achievement 目前 None，P1-1 v2）；交叉表"最终下单"列显示已定稿量（decisions 并入 summary）。

## P1-2 余额 & 付款闭环上线（2026-08-20 深夜，用户"继续"）
- **审计发现**：`routers/forecast.py` 已有 `/payment-check/{period_id}`（差额=forecast_orders 金额−公司账户余额）——但口径是 Web 草稿流+公司账户，不是定稿 decisions+厂家预存款，且无推送/到账确认 → **不重复造，补齐为闭环**。
- **erp_db 新增**：`supplier_balances`（厂家预存款，按供应商/下单主体取最新）+ `period_payments`（周期付款记录）+ 5 函数（upsert/list/save/confirm/get）。
- **端点**（routers/forecast.py，/api/forecast/payments/*）：
  - `GET|POST /payments/balance`：厂家余额列表/更新（文员查厂家系统后录入）。
  - `POST /payments/compute`：订单金额=Σ(decisions.final_qty×products.sale_price) − 厂家余额 → 差额，落 period_payments(pending)，差额>0 推付款提醒（message_send+企微/飞书），=0 推无需付款。
  - `POST /payments/confirm`：到账确认（status='paid'+paid_at）+ 通知"可生成下游订单/舟谱模板"。
- **前端**：交叉表工具栏「余额 & 付款」按钮 → 弹窗三区：①厂家余额（录/存+列表）②本期需付款（计算→订单金额/余额/差额，差额红显+推送状态）③到账确认。
- **生产 E2E**：存余额 5000 → 造商品(出厂价99)+定稿 100 件 → compute：order=9900、gap=4900、need_pay=true（推送付款提醒）→ confirm 4900 → period_payments status='paid' 全字段正确；清理。bundle index-BLG6v57N.js 含 余额&付款/payCompute/payConfirm。HEALTH ok。
- 至此 P1-1（审核台）+ P1-2（余额付款）落地；下一候选 P1-3 一键生成舟谱导入模板（Hermes 已有，接按钮）、P1-4 结构化日期/下单主体拆字段。

## P1-3 一键生成舟谱导入模板上线（2026-08-20 深夜，用户"继续"）
- **审计**：`/forecast/template/{period_id}` 是**提货单 HTML**（非舟谱导入文件）；`import_zhoupu.py` 是舟谱→Hergent 导入（反方向）；无 Hermes 舟谱 skill。→ **确定性后端生成**，不依赖 LLM。
- **端点** `POST /api/forecast/zhoupu-template`（forecast.py）：Σ定稿 decisions（final_qty>0）LEFT JOIN products → CSV（utf-8 BOM）：商品条码/商品名称/规格/单位/数量/单价/金额，文件名 `舟谱订单导入_{start}_{end}.csv`。
- **前端**：交叉表工具栏「生成舟谱模板」按钮 → POST → Blob 下载 CSV；无定稿商品时提示先走审核定稿。
- **生产 E2E**：造商品(条码 6934665094026, 出厂价 99)+定稿 40 件 → 端点返回 count=1、CSV 行 `6934665094026,红桶（恒滋下单）,10件/箱,件,40,99.00,3960.00` ✓；清理。bundle 含 生成舟谱模板/zhoupuTemplate。HEALTH ok。
- **6 步流程全闭环达成**：①小程序报单→②交叉表自动汇总（替代文员手抄）→③AI 审核台定稿（替代经验拍脑袋）→④余额付款闭环（替代手算差额）→⑥一键舟谱模板（替代 Hermes 手生成）。唯一仍手动=⑤粘贴厂家系统下单（蒙牛无 API，战略降级路径）。

## P1-4 结构化：下单主体/到货节奏拆字段上线（2026-08-20 深夜，用户"继续"）
- **schema**：products 加 `ordering_entity`(TEXT) + `arrival_lead_days`(INTEGER)——主库 `_safe_migrate('v109_*')` + **租户库补丁**（erp_db 模块级 tenant_*.db 循环里加 products 列检查，每次 import 自动 ALTER；⚠️ 只加主库迁移不够，租户库要走补丁块）。
- **导入解析**（import_router forecast_cross）：COLUMN_PATTERNS 加 `rhythm` 身份列（订单排期/排期/到货节奏…，与 _CROSS_SKIP_KEYWORDS 不冲突：命中 rhythm 即成身份列不再当客户）；execute 逐商品正则提取 ①下单主体「红桶（恒滋下单）」→ ordering_entity（⚠️ 真实格式"（恒滋下单）"括号内含"下单"二字，正则须 `[（(](恒滋|福宝)[)）]?\s*下单[)）]?`）②`+N到货` → arrival_lead_days；匹配主档且命中则回写 products（只填空值，不覆盖既有，去重每商品一次）。
- **summary**：rows 加 `(SELECT ordering_entity FROM products WHERE id=i.product_id)` → 前端交叉表行名旁显示下单主体徽标（恒滋=青、福宝=紫）。
- **生产 E2E**：导入带"红桶（恒滋下单）"+排期"+3到货"的文件 → products.ordering_entity='恒滋'、arrival_lead_days=3 ✓ → summary 返回 ordering_entity='恒滋' ✓。前端已部署（oe-badge 样式）。HEALTH ok。
- 说明：交叉表"按业务员/按报单单元切换列维度"留待 P2-2（报单单元概念细化），本轮只做拆字段打底。

## P2-1 返利冲档测算上线（2026-08-20 深夜，用户"继续"）
- **erp_db 新增 `rebate_gap_projection(supplier_id, extra_amount)`**：厂家采购额口径（purchase_orders 按年过滤，supplier_id/contact_id 兼容）→ 解析 `tiers_json` 阶梯（无则按 target_amount 两档兜底）→ 当前档/下一档/缺口/本期追加后是否冲档/冲档收益毛估（(achieved+extra)×(next_pct−cur_pct)）；**供应商链接校验**（contact_id 不存在→linkage_ok=false+警告；有合同无采购→警告），防绑定错误谎报。
- **端点** `POST /api/forecast/rebate-gap`；**前端**交叉表工具栏「返利测算」弹窗（供应商下拉=supplier_balances + 本期追加额 + 测算→档位/缺口/冲档收益/绑定警告）。
- **⚠️ 真实数据发现**：tenant_1（老板真实租户）`rebate_contracts` 仅 1 条未结合同，但 **contact_id=1 是无效引用**（租户无 id=1 联系人；真实蒙牛供应商拆成 2912 蒙牛酸奶-恒滋/2920 蒙牛乳饮(2682)/2921 蒙牛鲜奶(7579)/2923 蒙牛酸奶/2910 蒙牛奶酪 多个主体）→ 测算显示 achieved=¥0、gap=¥100000。代码正确（尊重合同绑定），**需老板确认返利总指标绑哪个蒙牛主体**（或合计口径）后修正合同绑定即准确。测算已带 linkage_warning 诚实提示。
- 生产验证：tenant_1 上下文 linkage_ok=false+警告 ✓；2921 无合同→None ✓；demo API 无合同→message 降级 ✓；前端已部署（bundle 含 返利测算/rebateGap/冲档）。HEALTH ok。

## 「导不进去」根因定位与修复（2026-08-20 深夜）
- **现象**：用户在 hergent.cn 点「导入 Excel」→ preview/execute 全部 200（日志实证 27.27.126.231 多次调用），但交叉表始终空态 → 用户以为导不进去。
- **根因**：导入其实成功（tenant_10 有 46 条 submissions 含用户真实导入），但 **`forecast_periods` 数 = 0**——用户从未创建"期次"，而交叉表视图强制 `if (!curPeriod.value) return` 且下拉无期次可选 → 数据永远不显示。
- **修复**（前端 Forecast.vue）：
  1. `loadCross` 无期次时默认「今日报单」窗口（order_start=order_end=本地今天，⚠️ 不能用 `toISOString()` 是 UTC 会差一天，须 `getFullYear/getMonth/getDate` 本地拼）→ 导入/报单数据立即可见；期次变为可选（正式双日周期管理用）。
  2. `switchView('cross')` 不再要求 curPeriod。
  3. 空态文案引导「点右上导入 Excel 或小程序报单」。
- **验证**：真实文件（8.18 下单表）打生产 preview→identity{name:3,barcode:1,spec:2,price:4,rhythm:35}+23 客户列；execute success=20、154 行；`summary?start=2026-08-21&end=2026-08-21` 返 **92 行**（鲜奶百利包 684/10 单元等真实数据）。已部署（bundle 含修复）。
- 备注：复现时在 tenant_10 留了 20 行 note='Excel导入 real.xlsx'（用户真实文件数据，同日期重导即覆盖，无害）；`[AI] call failed (deepseek) 401` = LLM key 失效，仅影响 AI 列映射兜底，不影响主流程（客户列按关键词+兜底识别）。
- 后续增强候选：导入时按文件活动 sheet 名（如"8月14报单-8月18到货"）自动建期次。

## 交叉表可编辑矩阵上线（2026-08-20 深夜，用户"导进去的字段都要支持修改"）
- **需求**：导进去的字段都要能改——调整门店订单（改数量）、增减商品行、增减客户列、修改商品名称。
- **架构决策**：编辑保存 = **重写该窗口的『导入』报单**（幂等：先清 role='导入' 再按矩阵重建，每客户一条提交）→ summary/审核台/舟谱模板 全部自动跟随新矩阵。商品名修改只写 item.product_name（该周期显示层），不动 products 主档；新增行 product_id=0 原样保留名称（AI 审核会跳过 product_id=0）；删客户/删行=不生成对应提交/item。
- **后端**：`POST /api/forecast-submissions/save-matrix`（forecast_submissions.py，async，body={start,end,customers[],rows[]}，qty_by_unit 按客户名取值，复用 forecast_submission_create，清理用 order_date BETWEEN start~end AND role='导入'）。
- **前端**（Forecast.vue）：工具栏「✏️ 编辑」/「取消」/「保存调整」；编辑模式=可编辑矩阵（数量格 input、商品名/规格/单位/单价 input、表头客户名可改+✕删列、列尾"＋客户名"回车加列、行尾✕删行、底部「＋商品」+提示）；「保存调整」→ save-matrix → 刷新回只读。只读视图保留（冻结/小计/钉条/AI建议）。loadCross 的 matrixRows 补 product_id（保存映射用）。
- **⚠️ 教训**：Vue 模板**两个连续 v-else 是非法**的（v-else 只能紧跟 v-if），工具栏"取消/保存"改成各自显式 v-if 才过编译。
- **生产 E2E**：payload(2 商品×3 客户，含改数量/新增行/删列语义)→ save success 3 客户 5 items → summary 红桶 qty=25(12+8+5)、测试新增商品 10(3+7) ✓ → 重复保存幂等 rows 仍 2 ✓ → 清理。bundle 含 saveMatrix/保存调整/编辑模式。HEALTH ok。
