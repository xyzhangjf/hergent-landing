# 「预报订单」小程序 · 上线就绪度审计与待办清单

> 审计对象：`forecast-order-miniprogram-20260812T023419087Z/miniprogram/`（4 页面 + utils/api.js + app.js）
> 后端对照：`hergent-erp/server/routers/forecast_submissions.py`、`erp_db.py`（已部署生产，curl 验证 health=200、/forecast-submissions/* 均 401 路由存在）
> 方法：静态代码审查 + 生产后端可达性探测 + 契约逐条比对 + 历史审核/修复记录复核
> 时间：2026-08-30

---

## 一、结论摘要（上线就绪度）

**后端（服务端）：已基本就绪** —— 核心读写、鉴权、越权防护、幂等、汇总过滤、AI 建议缓存（B1–B5）均已修复并部署生产，curl 实测路由存在且鉴权生效。

**前端（小程序）：代码完成度约 85%，但从未真正发布** —— M7–M12（搜索合并/购物车持久化/回执/常报门店/离线草稿/401 回跳）等优化已在代码中落地且 `node --check` 通过，但因微信小程序必须由"微信开发者工具上传 + 审核 + 发布"才对终端用户生效，**当前所有前端改动仍停留在源码，未发布**。

**正式上线最大阻塞不是代码质量，而是微信平台合规与发布流程**：当前 `appid` 是 `touristappid`（仅能跑体验版，不能审核/发布）、无隐私协议、未配置 request 合法域名、未上传。**这 4 项是"不发版"硬阻塞**。

**功能完整性缺口**：移动端**审批流完全缺失**（后端已有 `/pending`/`/{sid}/approve`/`/{sid}/reject`，但小程序仅有只读汇总页）；"我的提交"**无详情/编辑/撤回**（契约要求销售可改本人预报）。需产品确认移动审批是否纳入首版范围。

---

## 二、已验证完成项（无需重复做）

| 维度 | 状态 | 证据 |
|---|---|---|
| 后端鉴权/越权防护 B1 | ✅ 已修+已部署 | `create_submission` 校验 `store.id ∈ employee_stores_get(emp_id)` 否则 403；门店名以服务端为准 |
| 后端 product_id 容错 B2 | ✅ 已修 | `int()` 包 try，非法明细跳过，空清洗返回 400 |
| 后端汇总排除 rejected B3 | ✅ 已修 | `forecast_submission_summary` 聚合/来源 SQL 均 `AND s.status != 'rejected'` |
| 后端提交幂等 B4 | ✅ 已修 | 同 `(user,store,order_date)` 覆盖更新；已 approved 返回 409 |
| 后端 AI 建议性能 B5 | ✅ 已修 | `_forecast_product_cached` 1h TTL 缓存 |
| 前端 F1/F2/F3（门店常驻/401 跳登录/搜索防抖+竞态） | ✅ 已在代码 | `fill.js`、`login.js`、`api.js` 已改 |
| 前端 M7–M12 | ✅ 已在代码 | `loadProducts` 单接口、购物车持久化、回执、常报门店、离线草稿、登录回跳 |
| 生产后端可达 | ✅ 实测 | `health=200`；`open-periods/fill-search/summary` 均 `401`（路由存在、鉴权生效） |
| 价格/角色防篡改 | ✅ 服务端权威 | 忽略客户端 price/role，price 按 product_id 查表，role 取 token |

---

## 三、待办清单（按模块/维度 + 优先级 + 影响 + 建议顺序）

> 优先级：🔴 P0 = 不发版阻塞 / 数据正确性；🟠 P1 = 上线前强烈建议（体验/运维/合规风险）；🟡 P2 = 质量与一致性，可灰度后补。
> 建议顺序按"先打通发布链路 → 再补功能与质量"排列。

### 模块 A：合规与相关配置（🔴 全部 P0，真正的发版阻塞）

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| A1 | 🔴 P0 | **注册真实小程序账号并替换 appid**（当前 `project.config.json:47` 为 `"touristappid"`，仅体验版可用，无法审核/发布） | 不解决则永远只能本地/体验版调试，无法正式上线 | 1 |
| A2 | 🔴 P0 | **配置 request 合法域名 + uploadFile/downloadFile 域名**（微信公众平台→开发管理→开发设置）：必须含 `https://hergent.cn`（生产 API 域名）。当前 `urlCheck:false` 仅开发期绕过，真机必失败 | 不配置则真机所有接口请求被微信拦截（fail），App 不可用 | 2 |
| A3 | 🔴 P0 | **隐私保护指引 + 用户协议**：微信审核强制要求。登录即收集账号/密码等个人信息，必须①在公众平台填写《隐私保护指引》②首次启动弹窗告知并取得同意（或加"已读并同意《隐私协议》"勾选） | 无隐私协议 → 审核必驳回；合规风险 | 3 |
| A4 | 🟠 P1 | **选择正确类目**（如"工具-企业协同/供应链管理"或行业类目），部分类目需资质材料 | 类目不符 → 审核驳回或下架风险 | 4 |
| A5 | 🟡 P2 | 补充部署/接驳 README（如何微信开发者工具打开工程、填真实 appid、配域名、上传发布流程） | 降低后续维护与交接成本 | 11 |

### 模块 B：发布流程（🔴 P0 阻塞）

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| B1 | 🔴 P0 | **微信开发者工具上传 → 体验版 → 提交审核 → 发布**（含已落地的 M7–M12 前端改动，此前仅改码未发布） | 当前所有前端优化对终端用户不可见，等于未交付 | 4（同 A3 后） |

### 模块 C：功能完整性

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| C1 | 🟠 P1 | **移动端审批流**：后端已有 `/pending`、`/{sid}/approve`、`/{sid}/reject`，但 `summary.js` 仅只读汇总+复制，从未调用审批接口。需产品确认首版是否含移动审批（契约 P10-10 列了 5 项小程序侧待办，全部未做） | 若老板需在手机批单，则核心功能缺失；若首版仅"录入+查看"，可暂不在范围 | 6（需先拍板） |
| C2 | 🟠 P1 | **"我的提交"补详情/编辑/撤回**：`mine.js` 仅列表，无详情页、无修改/撤回。契约要求"销售可修改本人名下预报单" | 销售报错无法自纠，只能联系管理员，体验差 | 6 |
| C3 | 🟡 P2 | 提交成功后的"查看我的报单"当前跳 `summary`（汇总总表），应跳 `mine`（我的提交）更贴切（文案已写"查看我的报单"） | 文案与跳转目标不一致，轻微体验问题 | 11 |

### 模块 D：边界与异常处理

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| D1 | 🟠 P1 | **修复残留静默 catch**：`fill.js:loadPeriods`(约 L41)、`loadRecentStores`(约 L124) 仍为 `catch(e){}` 静默失败。期次加载失败 → 用户看到空期次下拉却仍能提交（后端会 400，但 UX 差且无提示） | 弱网/接口异常时用户无感知，可能误以为"没期次" | 7 |
| D2 | 🟡 P2 | `summary.js:onDate` 任意日期无校验；`copyAll` 无 `fail` 回调（F8） | 边界健壮性 | 11 |
| D3 | 🟡 P2 | 全局无网络错误重试（仅 M11 离线草稿补传），非提交类请求失败无重试 | 弱网体验 | 11 |

### 模块 E：数据校验与安全性

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| E1 | ✅ 已达标 | 后端 B1–B5 已落地：越权 403、product_id 容错、汇总排除 rejected、幂等、price/role 服务端权威 | — | — |
| E2 | 🟡 P2 | `mine.js:logout` 未清 `fs_tenant_id`/`globalData.tenantId`（F7） | 切换账号可能串租户上下文（轻量隐患） | 5（发布前顺手修） |
| E3 | 🟡 P2 | B6：`/stores` 对 `supervisor/accountant` 走 `employee_stores_get(emp_id=0)`→空门店，但 `/summary` 允许其查看，权限口径不一致 | 督导/会计无法在小程序提交，但能看汇总 | 11 |
| E4 | 🟡 P2 | B7 错误信封 `ok`/`success` 混用（create 返回 ok、save_matrix 返回 success）；B8 双写路径（forecast_orders vs forecast_submissions）并存；B9 `/my` 无分页游标 | 一致性/可维护性 | 11 |

### 模块 F：性能与兼容性

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| F1 | ✅ 已达标 | 搜索防抖+竞态丢弃、AI 建议缓存、单接口聚合 | — | — |
| F2 | 🟡 P2 | `apiBase` 硬编码 `https://hergent.cn`（F5），无 dev/staging 切换 | 联调/预发困难 | 11 |
| F3 | 🟡 P2 | `utils/api.js` 顶层 `const app = getApp()` 无兜底（F6），极端加载顺序可能 undefined | 极低概率崩溃 | 11 |
| F4 | 🟢 低 | 未做分包/图片优化：当前包体极小，无需处理 | — | — |

### 模块 G：权限与登录态

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| G1 | ✅ 已达标 | bcrypt+失败锁定+24h 随机 token；401 清态+跳登录+原路回跳（M12） | — | — |
| G2 | 🟡 P2 | 无 token 刷新机制（24h 过期后 401 重登，可接受）；无"修改密码/账号管理" | 体验 | 11 |

### 模块 H：埋点与监控（🔴 当前完全缺失）

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| H1 | 🟠 P1 | **基础错误上报**：`utils/api.js` 的 `fail`/非 2xx 统一上报到后端日志或微信自定义分析（至少错误数+接口+状态码） | 真机出问题无可见性，"离线/失败"类问题无法归因 | 8 |
| H2 | 🟠 P1 | **关键漏斗埋点**：登录成功、报单提交成功/失败、汇总查看。轻量即可 | 验证功能真被使用、定位流失 | 8 |

### 模块 I：UI/交互与文案

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| I1 | ✅ 达标 | 回执、常报门店、离线草稿横幅、AI 建议高亮均已实现 | — | — |
| I2 | 🟡 P2 | "我的提交"无记录时空态已有；提交中按钮 disabled 逻辑 OK；`summary` 列表用 `wx:key="index"`（反模式，静态可接受） | 一致性 | 11 |
| I3 | 🟡 P2 | 首屏 `loadStores/loadProducts` 无 loading 态（先空后填），可加骨架/loading | 弱网卡顿感知 | 11 |

### 模块 J：第三方接口联调

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| J1 | 🟢 不在小程序范围 | 回写 ERP（kingdee/yonyou/zhoupu）由 Web 端 `connector-writeback` 负责，小程序不直接调用 | — | — |
| J2 | 🟠 P1 | 若 C1 移动审批纳入首版，则审批→采购申请→回写 ERP 链路需在真机联调（当前小程序未接） | 功能闭环 | 6（随 C1） |

### 模块 K：测试覆盖（🔴 当前仅语法检查）

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| K1 | 🟠 P1 | **真机 E2E**：登录→选店→搜商品→加购→提交→汇总总表可见→（若含审批）审批→Web 端实时可见。至少覆盖 1 条正向 + 1 条失败重试 | 当前无任何真机验证，端到端是否跑通未知 | 9 |
| K2 | 🟡 P2 | 后端补 pytest（提交幂等/越权/汇总过滤/期次窗口校验）；前端关键函数单测（loadProducts 竞态、submit 校验） | 防回归 | 11 |
| K3 | 🟡 P2 | 多角色真机：sales/supervisor/boss 各测一遍权限边界 | 越权/可见性验证 | 11 |

### 模块 L：灰度与回滚方案

| # | 优先级 | 待办 | 影响 | 建议顺序 |
|---|---|---|---|---|
| L1 | 🟠 P1 | **发布策略**：微信支持"体验版 + 审核 + 分阶段发布（灰度%）"，首版建议先 5%~10% 灰度 1~2 天再全量 | 控制故障爆炸半径 | 10 |
| L2 | 🟠 P1 | **后端回滚预案**：forecast_submissions.py/erp_db 改动均为增量（forecast 表 additive），回滚=git 回退+`systemctl restart hergent-erp`；需文档化并确认无破坏性迁移 | 出问题可快速恢复 | 10 |
| L3 | 🟠 P1 | **前端版本回退**：微信保留历史版本，可在后台"回退"到上一审核通过版；需记录每次上传的版本号/commit | 前端问题可秒级回退 | 10 |

---

## 四、建议处理顺序（执行路线图）

1. **A1** 注册真实小程序账号 + 主体认证 + 拿 appid（最长链路，先启动）
2. **A2** 公众平台配置 `https://hergent.cn` 为 request 合法域名（+业务域名）
3. **A3** 起草《隐私保护指引》《用户协议》+ 小程序内首次启动同意弹窗
4. **B1 + A4** 微信开发者工具导入工程→填真实 appid→上传→体验版→提交审核（同步选类目 A4）
5. **E2** 顺手修 `logout` 清 tenantId（发布前小改）
6. **C1/C2 + J2**（需产品拍板移动审批范围）— 若首版只要"录入+查看"，则仅做 C2（我的提交详情/编辑）
7. **D1** 修静默 catch，期次/门店加载失败给明确提示
8. **H1/H2** 加错误上报 + 关键漏斗埋点
9. **K1** 真机 E2E 全流程验证（首版上线前必须）
10. **L1/L2/L3** 灰度策略 + 前后端回滚文档
11. **A5/F/E3/E4/I/K2/K3** 质量与一致性收尾（可灰度后补）

---

## 五、一句话总结

**代码层面已具备上线基础（后端生产就绪、前端优化已改码），当前卡在"微信平台合规 + 发布动作"：touristappid、无隐私协议、未配域名、从未上传发布是 4 个硬性不发版阻塞；功能上移动端审批流与"我的提交"编辑能力缺失（是否首版范围待产品拍板）；监控埋点与真机测试为零，建议补齐后再灰度发布。**
