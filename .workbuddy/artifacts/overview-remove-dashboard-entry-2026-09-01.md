# 移除侧边栏「经营趋势」入口 —— 完成报告（2026-09-01）

## 交付
已移除「经营趋势」导航入口，桌面侧边栏与移动端底部 Tab 同步清理，其余菜单项结构与样式零改动。

## 变更
| 层 | 文件 | 改动 |
|---|---|---|
| 前端 | `hergent-cn-v2/src/components/Shell.vue` | 删除桌面侧边栏 `<router-link to="/dashboard">`（经营趋势）＋ 移动端底部 Tab「趋势」两处入口 |
| 前端 | `src/router/index.js` | **不动**（路由保留，`/dashboard` 直链不 404；默认跳转仍为 `/workbench`） |

## 构建与部署
- `npm run build` ✓（新 Shell chunk `Shell-B_CGeKab.js` 53.89 kB）
- rsync `--delete` → `/opt/hergent-cn-v2` → chown（纯静态，无需重启后端）

## 对齐效果（本地 + 生产双核验）
- 「经营趋势」「趋势」入口：新产物中 **0 处**
- 其余 7 项菜单（经营工作台 / 预报订货管理 / 目标与返利 / 档案管理 / 能力中心 / 定时任务 / 设置）：**全部保留**
- 生产 `index.html` 已引用新构建入口，旧 chunk 已清理

## 需确认事项
1. 侧边栏底部是否留有空白分隔（原「经营趋势」上方有一空行注释分隔）——已同步调整，视觉无变化；
2. 若后续还要彻底移除 `/dashboard` 页面（而非仅入口），说一声即可删路由。
