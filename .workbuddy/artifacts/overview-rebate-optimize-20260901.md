# 返利模块（目标与返利）优化 — 2026-09-01

## 结论先行
用户要求推进"方向1：返利 Rebate.vue 落地"，并明确两点：① 侧边栏入口现叫"目标与返利"；② 先看真实代码，已有功能要优化、不要重复造轮子。

**核实结果：返利模块在 v112/v113 迭代中已生产级完整落地，原记忆里的"P0 改造项"全部就位。** 本次未重造，只做了 2 处真实存在的补完式优化。

## 已确认"已存在、无需重做"的能力（证据）
- 侧边栏入口 `Shell.vue:41` 已为"目标与返利"（符合用户确认）。
- `Rebate.vue`（~2500 行）4-Tab 完整：仪表盘 / 目标规则 / 达成填报 / 年度合同。
- 品牌/单品维度入口卡片（L155-171）、规则详情 Modal（L712+）、计算算式链 Modal（L657+）、试算器 What-if（L600+）均在。
- `statusOf()`（L1371）已覆盖 **未开始/进行中/已结束/停用**，直接消费后端 `status` 字段防漂移。
- 达成填报 Excel 导入干跑预览、行内 300ms 防抖、竞态保护、商品键解析、合同按月分解/计提/结算/反结算、最大余数法分解算法全部就位。
- 后端 `rebate_rules.py`(v113) + `rebate_achievements.py` + `domain/rebate_calc.py` 统一算法内核，返利只走后端计算。

## 本次 2 处真实修复（低风险、补完式）
| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| F1 | `src/components/Shell.vue:65` | 移动端底部 Tab 标签写死"政策"，与桌面侧边栏"目标与返利"不一致 | 改为"目标与返利"，与入口名对齐 |
| F2 | `src/pages/Rebate.vue:135-138` | 维度筛选下拉仅列 品牌/单品，但后端实际支持 5 维度（品类/客户/渠道规则筛不到） | 补齐 品类维度/客户维度/渠道维度 三个选项 |

## 构建与部署
- `npm run build` 成功（Shell-mde43wg4.js 53.9kB / Rebate-Bua72scK.js 93.9kB）。
- rsync 到 `47.113.224.140:/opt/hergent-cn-v2` + chown，hergent.cn 200。
- 纯前端改动，未动后端、无需重启。

## 生产验证（curl 构建产物 grep）
- Shell 构建含 `目标与返利`×2，且**无"政策"** → F1 生效。
- Rebate 构建含 `品类维度/客户维度/渠道维度` → F2 生效。

## 需确认事项
1. 返利模块功能面已完整，**无待补的 P0 改造**。若仍有具体不满意的交互点，请指出页面/截图，我针对性优化而非整体重做。
2. 维度筛选 F2 仅放开"筛选"，创建规则时的维度候选本就来自 `meta.dimensions`（5 维齐全），无需改。
