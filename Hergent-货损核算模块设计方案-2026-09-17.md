# Hergent 货损核算模块 · 前后端完整设计方案

> 日期：2026-09-17（**修订 1**：已并入用户对原 §12 三条答复 —— 见 §1 / §3.5 / §8.3 / §12）
> 范围：`hergent-erp`（FastAPI + SQLite 多租户）+ `hergent-cn-v2`（Vue3）
> 依据：现有代码实读 + **舟谱真实导出文件实读**（4 份样本，见 §2.3）
> 结论先行：需求整体成立。原 3 条待裁决口径**已于 2026-09-17 由用户全部定案**，
> 但由此**引出第 4 条更重要的架构需求**：所有「叫法」必须可配置（§1 🔴4 / §3.5）。

---

## §1 结论先行：3 条已定案 + 1 条新增架构需求

| # | 事项 | 用户定案（2026-09-17 原话） | 设计落地 |
|---|---|---|---|
| ✅ 1 | **货币口径混用**（退货额＝折后**售价**、调拨额只能是**成本价**） | 未反对「统一口径」，但见 🔴2 | `pricing` 全局开关，**所有项一律换算到同一口径**（§8.2） |
| ✅ 2 | **「临期货销售额」抵扣分子** | **按售价** —— 理由：「代表给公司**挽回了多少金额**」 | 抵扣口径＝售价 ⇒ ⚠️ **必须把 `pricing` 也切到 `sale`**，否则**低估**（§8.3 有量化）；并强制加「临期货折扣率」护栏防刷数 |
| ✅ 3 | **「良品仓」在舟谱里叫什么** | **`总仓`**；但「不同经销商叫法不一样，**默认总仓、支持用户自己改名字**」 | 预置 `总仓` 为默认别名 + 别名可配（§3.5） |
| 🔴 4 | **由 ③ 引出的架构需求：一切「叫法」必须可配** | —— | **新增语义角色登记表**（§3.5）：不硬编码任何仓名 / 单据名 |

### 🔴 4 是本轮最重要的增量：用户两句话点破了同一件事

> 原话 ①：「总仓，但**不同的经销商叫法不一样**，可以默认是总仓，但**要支持用户自己改名字**」
> 原话 ②：「我们公司是**自提订单和自提退单**，但还是一样的问题，**别的经销商可能叫销售单/销售退单**」

两句话是同一个模式的两次命中：

| 维度 | 本客户（蒙牛低温奶） | 别的经销商可能叫 | 若硬编码的后果 |
|---|---|---|---|
| 良品仓 | `总仓` | `成品仓` / `正品仓` / `良品仓` | 「良品仓调至X」的**所有分子分母取不到数** |
| 临期仓 | `临期仓` | `不良品仓` / `待处理仓` / `效期仓` | 同上 |
| 门店销售单 | `自提订单` | `销售单` / `销货单` / `出库单` | 门店销售额分母取不到数 |
| 门店退单 | `自提退单` | `销售退单` / `退货单` | 门店退货额分子取不到数 |

⇒ **任何一个硬编码在代码里的舟谱字面值，都是一个「只能卖给一家客户」的暗桩。**
这直接决定 §2 的产品战略：Hergent 是要卖给**多个**经销商的，不是自用系统。
所以本模块从 P0 起就必须是 **配置驱动**，而不是「先硬编码、以后再抽」—— 因为**填数的人是你自己，
你没有第二个经销商的舟谱账号去验证**，硬编码的坑要等客户装上去那天才会炸。

另外三条（重要但可后置）：

- 🟡 **用户说"两类数据"不够**：要支撑他列的公式，至少需要 **4 类** —— 退货单、调拨单、**销售订单（临期仓销售 + 门店销售额）**、报损。缺的两类见 §2.4。
- 🟡 **调拨单里没有"业务员"字段**（只有 `创建人`＝制单人），业务员归属必须走**显式映射表**，不能靠"仓名去掉仓字"猜。
- 🟡 **月度核算 + 每日更新**的语义未定义：一条月记录还是每天一条？未结账的数每天都在变，拿去和上月比会误导。建议 **月账唯一行 + 日快照留痕 + 结账锁定**（§3.4）。

---

## §2 现状勘察

### 2.1 已经有的（不要重造）

| 设施 | 位置 | 本模块怎么用 |
|---|---|---|
| **报单对象字典** | `report_mapping` 表（`erp_db.py:638`）：`counterparty_type IN ('store','customer','self_warehouse')`、`employee_id`、`system_name`、`report_alias`、**`src_wh`/`dst_wh`**、`is_active` | ⭐ **直接复用**：门店白名单（`store`）、业务员仓映射（`src_wh`/`dst_wh`）都放这里，**不建第二份字典** |
| 仓库表 | `warehouses(id, name, is_default)` | ⭐ 只作**本系统内部**的仓库主数据；**舟谱导出的仓名 → 角色**一律走 `loss_role_aliases`（§3.5），**不从本表猜**（本表里没有"良品仓"这个概念） |
| 调拨单 | `transfer_orders`（`from_warehouse_id`/`to_warehouse_id`/`quantity`/`operator_id`） | 手工录入的落点参考；**粒度为单品单行** |
| 退货单 | `return_orders` + `return_order_items`（RMA，含 `reason`/`quality_status`） | 同上 |
| 报损单 | `wastage_orders` / `wastage_order_items`（`total_loss`/`type`/`status`） | ⚠️ tenant_1 **整表 0 行**（从未录入）→ 见 §8.6 |
| 导入两阶段 | `routers/import_router.py`：`/preview`（**只读**）+ `/execute`、`/template/{category}`、`COLUMN_PATTERNS`、`FIELD_LABELS`（唯一一份字段中文名） | ⭐ 新导入**照抄这套契约**，不新造 |
| 字段级留痕 | `db/queries/entity_logs.py`（`log_entity_changes` / `get_entity_changes_by_type`）+ `routers/_field_log.py`（`who` / `safe_log`） | ⭐ 复用（全仓唯一设施，**勿建第二份**） |
| 配方存储 | `workflow_recipes`（key/value JSON），已有 `loss_recipe` | 口径配置**扩展它**，不新建配置表 |
| 现有货损页 | `LossWorkflow.vue`（349 行）+ `routers/loss_workflow.py`（`/api/loss/recipe`、`/run`） | ⚠️ 见 §8.11：**改名区分** |
| RBAC | `server.py` `_PATH_MODULE_MAP`：**`/api/loss` → `stock`**（:314）、`/api/import` → `data`（:455） | ⭐ 新端点挂 `/api/loss/accounting/*` → **自动落 stock 模块，无需新增登记**；否则 fail-closed 403 |

### 2.2 前端现状

- 路由：`/loss` → `LossWorkflow.vue`（`router/index.js:47`），侧栏顶层「货损计算」（`Shell.vue:45`，无分组标题）
- 现有页是**配方驱动**：配 5 项业务参数 → 扫 `inventory` 批次效期 → 算「**预计**货损金额」、按维度分组
- 与本需求的关系：**同源概念、不同算法**。现有页是**存量快照预测**，新页是**期间流水核算**。→ 结论：**新页面 + 老页改名**（§8.11）

### 2.3 舟谱真实导出结构（实测 4 份样本，**这是解析设计的直接依据**）

样本位置：`~/Documents/hergent-erp/舟谱模版/采销管理/`

**① 调拨单明细**（`调拨单明细+20260629154504.xlsx`）

```
r1  标题：调拨订单（已入库）
r2  导出时间:2026-06-29 15:45:03
r3  单据头表头：单据编号 | 单据日期 | 调入仓 | 调出仓 | 调入部门 | 调出部门 | 创建人 | 司机
r4  值        ：DB260629000021 | ... | 刘善涛仓 | 毛辉仓 | — | — | 仲嫚嫚 | —
r6  （空行）
r7  明细表头：商品名称 | 条形码 | 单位 | 订单数量 | 订单数量（混合）| 出库数量 | 入库数量 | 备注
r8  明细值  ：蒙牛优益C原味340ml*24瓶 | （空！）| 件 | 2 | 2件 | 2 | 2 | —
r10 合计     ← 必须跳过
```

| 关键点 | 实测事实 | 设计含义 |
|---|---|---|
| 金额列 | **完全没有**（只有数量） | 金额必须由系统 `数量 × 单价` **折算** |
| `条形码` 列 | **整列为空** | 商品匹配不能只靠条码 → 必须「条码优先 → 名称精确 → 拒行」 |
| 业务员 | **没有这个列**。只有 `创建人`＝仲嫚嫚（制单人） | 业务员归属**必须走映射表**，不能猜 |
| 仓库 | 出现 `刘善涛仓`（调入）、`毛辉仓`（调出） | 仓库名 ≈ `<业务员名>仓`，但**不可据此反推** |
| 单据号 | `DB` 前缀（调拨）| 前缀可判单据类型 |
| 版式 | 单据头块 + 空行 + 明细块 + 合计行，**表头不在第 1 行** | 必须**锚点探测**，不能 `wb.active` 直接当表头 |

**② 退货订单-车销**（`退货订单-车销….xlsx`）—— **明细版**

```
r3  单据头：单据编号 | 单据日期 | 客户 | 业务员 | 部门 | 仓库 | 送货司机 | 备注
r4        ：TD260629000015 | ... | 好想来（谷城店）| 王琴 | 业务 | 王琴仓 | — | —
r5  明细表头：商品名称 | 条形码 | 单位 | 退货价（折后价）| 销售价（小单位）| 订单数量 | 订单数量（混合）| 订单金额 | 退货仓库 | 备注
r6  明细值  ：蒙牛优益C原味100ml*5瓶*10组 | 6934665082313 | 板 | 7.67 | 1.534 | 5 | 5板 | 38.35 | 王琴仓 | —
r8/r9 小计块：订单金额|随单费用|随单返利分摊|随单费用分摊 = 38.35|0|0|0
```

⭐ **`订单金额` = 退货价（折后价）× 数量 = 38.35** → **这是售价口径**（§8.2 的核心证据）
⚠️ **`退货仓库` 的值是 `王琴仓`（业务员仓），不是临期仓** → "退到临期仓"必须**按仓库筛**

**③ 退货订单-自提**（`退货订单-自提.xlsx`）—— **扁平列表版（版式完全不同）**

```
r3  筛选条件：单据状态 | 单据日期
r4          ：全部 | 2026-05-31至2026-06-29        ⭐ 免费的数据完整性证据
r5  表头：单据编号 | 客户 | 业务员 | 仓库 | 状态 | 订单金额 | 创建时间 | 审核时间 | 备注 | 打印数
r6  值  ：TD260629000014 | 美联（保康）店 | 刘小顶 | 临期仓 | 已完成 | 20.88 | ...
r101 总计：17528.02                               ← 必须跳过
```
- **一单据一行，无商品明细列** → 只能算到单据级
- ⭐ **r3/r4 有筛选条件行** → 可以**自动读出用户导出时筛的时间段**，用于期次归属 + 重叠/缺口检测（§5.4）

⭐ **这一份就是用户说的「自提退单」（他公司的门店退货来源）。它比车销版更有用，3 个关键点：**

| 发现 | 实测 | 设计含义 |
|---|---|---|
| ⭐ **自带 `业务员` 列** | r6 = `刘小顶` | **调拨单没有业务员列，但自提退单有** ⇒ 「业务员自售临期货额」按业务员拆分**是可行的**（若自提订单同构），不必额外补映射 |
| 🔴 **有 `状态` 列** | `已完成` / `待审核` / `已取消` | **必须只取 `已完成`** —— 否则 `已取消` 的单据金额也算进来，分子**虚高**。这是 §5.4 的第 4 道闸（此前遗漏） |
| 🔴 **`仓库` 列语义在同名两个版式里相反** | 退货单 r6 `仓库=临期仓` = **退到哪个仓** | 销售单的 `仓库`/`销售仓库` = **从哪个仓发货**。**列名相同、语义相反** ⇒ 绝不可用同一套解析逻辑复用，必须在版式分流之后分别映射（§5.2） |
| 单号前缀可判类型 | 退货 `TD` / 调拨 `DB` | 可做**辅助**校验（不是判据，见 §3.5） |
| ⭐ **单据金额可** `总计` 行会给出 | r101 = `17528.02` | 与逐行求和比对 → **免费的核对数**（不等则说明漏行/多行） |

⚠️ **「自提订单」（门店销售单）版式本轮未实测** —— 上表是「自提**退单**」的结构。
用户答复「我们公司是自提订单和自提退单」⇒ 本模块的**门店销售额 + 临期货销售额**都从这两个导出取。
但**推断的成分必须标出来**：本文档对自提订单的解析设计**基于"与自提退单同构"的假设**（扁平列表 + `客户` + `业务员` + `仓库` + `状态` + `订单金额`）。
⇒ **Phase 0 第一步就是拿一份真「自提订单」导出核对列名**，不要照抄假设（技能 §零.9 已收此条）。

**④ 销售订单-车销**（`销售订单-车销….xlsx`）—— 有 `销售仓库` 列

```
r3 单据头：单据编号|单据日期|客户|业务员|部门|仓库|送货司机|备注
r5 明细表头：商品名称|条形码|单位|折扣|销售价（折后价）|销售价（小单位）|实际售价|订单数量|订单数量（混合）|订单金额|销售仓库|备注
```

**实测枚举值（映射键候选，全量）**

| 维度 | 值 |
|---|---|
| **客户/门店（12 个）** | `美联（保康）店` `美联（卞河店）` `永辉民发店` `永辉吾悦店` `永辉东津店` `美联檀溪店` `多多买菜` `美联民发天地店` `嗨特卖（丹江路）` `美联（南漳涌泉）` `沃尔玛` `美联总仓` |
| **业务员（6 个）** | `刘小顶` `张俊峰` `符号` `仲嫚嫚` `毛辉` `刘善涛` |
| **仓库** | `临期仓` `王琴仓` `毛辉仓` `刘善涛仓`（**无"良品仓"**） |
| 单据状态 | `已完成` `待审核` `已取消` |

⚠️ **客户列里混着多种业态**：`美联总仓`／`多多买菜`／`沃尔玛`／`嗨特卖` 显然不是"直营门店" → 见 §8.7

**通用事实**：4 个文件**均无公式**；日期是**文本** `YYYY-MM-DD HH:mm:ss`；只有一个合并标题行；**画布 200 行但只用十几行**。

### 2.4 用户说的"两类数据"不够 —— 缺的两类

| 公式里的项 | 数据来源 | 用户是否提到 |
|---|---|---|
| 门店退临期仓金额 | 退货订单（筛 `退货仓库`=临期仓） | ✅ 提到了 |
| 业务员仓调临期仓金额 | 调拨单明细（调出仓=X仓，调入仓=临期仓） | ✅ 提到了 |
| 良品仓调至业务员仓金额（**分母**） | 调拨单明细（调出仓=良品仓，调入仓=X仓） | ✅ 提到了 |
| 良品仓直调临期仓金额 | 调拨单明细（调出仓=良品仓，调入仓=临期仓） | ✅ 提到了（同文件） |
| **临期货销售额**（抵扣项） | ✅ **已确认**：**自提订单**（筛 `销售仓库`=临期仓） | 用户答复② |
| **门店本期销售额**（**分母**） | ✅ **已确认**：**自提订单**（按 `客户` 汇总，仅 `已完成`） | 用户答复② |
| **报损金额** | ❌ **仍未定**：舟谱**没有**；Hergent `wastage_orders`（当前 0 行） | 用户未答 → §12 |

→ 需要在页面上做 **3 个上传入口**（**① 自提订单** / **② 自提退单** / **③ 调拨单明细** + 报损手工录入），不是一个通用上传口。

#### ⭐ 「自提订单」一份文件要喂 **3 个桶** —— 必须先列「桶 × 消费方」矩阵

（既有铁律：一份数据拆成多个桶时，**逐桶问"谁读它"**；「能填 + 看得见」≠「被消费」，写入端 / 展示端 / 下游要分别验）

| 桶 | 筛选条件 | 汇总维度 | 消费方（谁读它） |
|---|---|---|---|
| `store_sale` | `状态=已完成`（全部） | 按 `客户` | 门店行**分母**「本期销售额」 |
| `loss_wh_sale` | `状态=已完成` **且** `销售仓库`=临期仓 | 按 `业务员` + 按其他渠道 | 业务员行**抵扣** / 公司行抵扣 |
| `company_sale` | `状态=已完成`（全部） | 全量 | 公司行**分母**「公司销售金额」 |

⇒ 一次解析、派生 3 桶。**风险点**：3 个桶同源但筛选不同，若某桶的筛选条件写错（如忘加 `状态=已完成`），
会出现「分母多了 → 率偏小」而**同屏看不出来**。⇒ 三桶的**行数合计关系**必须做成可断言的不变量：
`company_sale 行数 == store_sale 行数`、`loss_wh_sale 行数 ⊆ store_sale 行数`，导入预览时直接算术自证。

---

## §3 数据模型设计

### 3.1 分层原则

| 层 | 形态 | 理由 |
|---|---|---|
| **L1 事实层** | 长表（明细一行一记录） | 可追溯、可重算、可撤销单批导入；门店/业务员会变，宽表加一列要 `ALTER TABLE` |
| **L2 汇总层** | 月账宽表（期间唯一行）+ 业务员维度行 | 供看板与历史留痕，读得快 |
| **L3 展示层** | **前端一张宽表** | 用户要的"一张表"是**视图**，不是存储（与 `Forecast.vue` 同一范式） |

### 3.2 DDL 草案

```sql
-- ① 导入批次登记（留痕 + 可撤销 + 重叠检测）
CREATE TABLE IF NOT EXISTS loss_import_batch (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    period        TEXT NOT NULL,          -- 'YYYY-MM'
    kind          TEXT NOT NULL,          -- store_return | transfer | sale | wastage
    file_name     TEXT DEFAULT '',
    file_md5      TEXT DEFAULT '',
    declared_from TEXT DEFAULT '',        -- ⭐ 从导出文件筛选条件行读出的时间段
    declared_to   TEXT DEFAULT '',
    row_total     INTEGER DEFAULT 0,
    row_ok        INTEGER DEFAULT 0,
    row_rejected  INTEGER DEFAULT 0,
    reject_detail TEXT DEFAULT '[]',      -- JSON：拒行清单（行号+原因）
    imported_by   TEXT DEFAULT '',
    imported_at   TEXT DEFAULT (datetime('now','localtime')),
    revoked_at    TEXT DEFAULT '',
    revoked_by    TEXT DEFAULT '',
    UNIQUE(period, kind, file_md5)        -- 同文件同期次幂等（重复上传不翻倍）
);

-- ② 货损事件明细（一类事件一行）—— 唯一事实来源
CREATE TABLE IF NOT EXISTS loss_events (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    period        TEXT NOT NULL,
    event_date    TEXT NOT NULL,          -- 单据日期（文本解析后存标准格式）
    kind          TEXT NOT NULL,          -- store_return | transfer | sale | wastage
    event_subtype TEXT NOT NULL,          -- ⭐ 见 §3.3 的子类型表
    doc_no        TEXT DEFAULT '',
    src_wh_id     INTEGER DEFAULT 0,
    src_wh_name   TEXT DEFAULT '',
    dst_wh_id     INTEGER DEFAULT 0,
    dst_wh_name   TEXT DEFAULT '',
    subject_kind  TEXT NOT NULL,          -- store | operator | direct | wastage
    subject_key   TEXT NOT NULL,          -- 稳定的主体键（不用显示名）
    subject_label TEXT DEFAULT '',
    barcode       TEXT DEFAULT '',
    product_id    INTEGER DEFAULT 0,
    product_name  TEXT DEFAULT '',
    spec          TEXT DEFAULT '',
    unit          TEXT DEFAULT '',
    qty           REAL DEFAULT 0,
    unit_price    REAL DEFAULT 0,         -- 按 pricing 口径取的单价
    pricing       TEXT DEFAULT 'cost',    -- cost | sale（冻结当时的口径）
    amount        REAL DEFAULT 0,         -- = qty × unit_price
    amount_source TEXT DEFAULT 'computed',-- doc（单据自带）| computed（系统折算）
    batch_id      INTEGER DEFAULT 0,      -- 溯源到导入批次
    row_seq       INTEGER DEFAULT 0,      -- 文件内行号（溯源）
    UNIQUE(batch_id, doc_no, barcode, row_seq)   -- 幂等去重
);
CREATE INDEX IF NOT EXISTS idx_loss_events_period ON loss_events(period, event_subtype);

-- ③ 手工录入（行级聚合，与明细分表；展示时合并）
CREATE TABLE IF NOT EXISTS loss_manual_inputs (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    period       TEXT NOT NULL,
    subject_kind TEXT NOT NULL,
    subject_key  TEXT NOT NULL,
    col_key      TEXT NOT NULL,            -- ⭐ 走列注册表（单一来源）
    value        REAL,
    note         TEXT DEFAULT '',
    updated_by   TEXT DEFAULT '',
    updated_at   TEXT DEFAULT (datetime('now','localtime')),
    UNIQUE(period, subject_kind, subject_key, col_key)
);

-- ④ 月账（期间唯一行 + 主体维度行）—— 宽表快照
CREATE TABLE IF NOT EXISTS loss_monthly (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    period        TEXT NOT NULL,
    row_kind      TEXT NOT NULL,           -- store | operator | direct | wastage | company
    subject_key   TEXT NOT NULL DEFAULT '',
    subject_label TEXT DEFAULT '',
    -- 分子各项（按 pricing 折算后的金额）
    store_return_amt   REAL DEFAULT 0,     -- 门店退临期仓
    op_to_loss_amt     REAL DEFAULT 0,     -- 业务员仓调临期仓
    op_from_good_amt   REAL DEFAULT 0,     -- 良品仓调至业务员仓（分母）
    op_loss_sale_amt   REAL DEFAULT 0,     -- 业务员自售临期货额（抵扣）
    direct_amt         REAL DEFAULT 0,     -- 良品仓直调临期仓
    wastage_amt        REAL DEFAULT 0,     -- 报损
    loss_wh_sale_amt   REAL DEFAULT 0,     -- 临期仓销售总额（含非业务员渠道）
    -- 分母
    store_sales_amt    REAL DEFAULT 0,     -- 门店销售额
    company_sales_amt  REAL DEFAULT 0,     -- 公司销售额
    -- 结果（⭐ 率必须与两个操作数一起存，否则事后无法解释）
    rate_num      REAL DEFAULT 0,
    rate_den      REAL DEFAULT 0,
    loss_rate     REAL,                    -- NULL = 无分母，不显示率
    data_quality  TEXT DEFAULT '[]',       -- JSON：['缺价2项','未映射仓1个','时间段有缺口']
    computed_at   TEXT DEFAULT '',
    is_closed     INTEGER DEFAULT 0,
    closed_at     TEXT DEFAULT '',
    closed_by     TEXT DEFAULT '',
    UNIQUE(period, row_kind, subject_key)
);

-- ⑤ 日快照（趋势 + 回溯"9月10日当时是多少"）
CREATE TABLE IF NOT EXISTS loss_daily (
    period     TEXT NOT NULL,
    snap_date  TEXT NOT NULL,
    row_kind   TEXT NOT NULL,
    subject_key TEXT NOT NULL DEFAULT '',
    rate_num   REAL DEFAULT 0,
    rate_den   REAL DEFAULT 0,
    loss_rate  REAL,
    computed_at TEXT DEFAULT '',
    PRIMARY KEY(period, snap_date, row_kind, subject_key)
);

-- ⑥ 语义角色（⭐ 本轮新增。一行一角色，**显示名只有这一份**，见 §3.5）
CREATE TABLE IF NOT EXISTS loss_roles (
    role        TEXT PRIMARY KEY,     -- 稳定键、永不变：good_wh|loss_wh|op_wh|store_sale_doc|store_return_doc|transfer_doc|wastage_doc
    role_label  TEXT NOT NULL,        -- ⭐ 用户可改的显示名（'良品仓' 可改成 '总仓' / '成品仓'）
    basis       TEXT DEFAULT '',      -- 该角色的判定基准（给用户看的解释，中文一句话）
    is_required INTEGER DEFAULT 0,    -- 1=必须命中，否则导入 fail-closed 拒收
    is_system   INTEGER DEFAULT 0,    -- 1=系统角色（不可删，可改名）
    sort_no     INTEGER DEFAULT 0,
    updated_by  TEXT DEFAULT '',
    updated_at  TEXT DEFAULT (datetime('now','localtime'))
);

-- ⑦ 角色别名（一行一别名；舟谱里的**实际字面值** → 角色）⭐ 单一来源
CREATE TABLE IF NOT EXISTS loss_role_aliases (
    alias        TEXT PRIMARY KEY,    -- ⭐ 库层硬约束：一个名称只能属于一个角色（防歧义，见 §3.5）
    role         TEXT NOT NULL,       -- 引用 loss_roles.role（SQLite 外键默认关，故只用约定 + 应用层校验）
    source       TEXT DEFAULT 'user', -- builtin（出厂预置）| user（用户手填）| learned（导入时现场指定后沉淀）
    is_active    INTEGER DEFAULT 1,
    hit_count    INTEGER DEFAULT 0,   -- ⭐ 被命中行数累计（反"假旋钮"：用户能自己看到它真在用）
    last_hit_at  TEXT DEFAULT '',
    updated_by   TEXT DEFAULT '',
    updated_at   TEXT DEFAULT (datetime('now','localtime'))
);
CREATE INDEX IF NOT EXISTS idx_loss_role_aliases_role ON loss_role_aliases(role, is_active);
```

**出厂预置种子**（`_ensure_loss_tables()` 里 `INSERT OR IGNORE`，**只喂默认值、不覆盖用户改过的**）：

| role | role_label（默认） | alias（预置，`source='builtin'`） | is_required |
|---|---|---|---|
| `good_wh` | 良品仓 | **`总仓`** ← 用户确认 | 1 |
| `loss_wh` | 临期仓 | `临期仓` | 1 |
| `store_sale_doc` | 门店销售单 | **`自提订单`** ← 用户确认 | 1 |
| `store_return_doc` | 门店退货单 | **`自提退单`**、`退货订单` | 1 |
| `transfer_doc` | 调拨单 | `调拨单明细`、`调拨订单` | 1 |
| `wastage_doc` | 报损单 | （空，舟谱无此数据） | 0 |
| `op_wh` | 业务员仓 | **不预置** —— 由 `report_mapping.src_wh/dst_wh` 动态认领（见 §3.5） | 0 |

⚠️ **`role` 枚举不许在代码里再写第二份**：`loss_roles` 是**权威**，代码里的枚举常量**只用于建表种子**，
运行时（解析 / 展示 / 校验）一律 `SELECT ... FROM loss_roles`。这是既有铁律「第二份拷贝 = 静默漂移」的直接应用。


### 3.3 事件子类型表（`event_subtype`）—— 决定每一项进哪个分子

⭐ **判定条件一律写成「角色」，不写仓名字面值** —— 角色由 `loss_role_aliases` 解析而来（§3.5）。
下表里 `⟨良品仓⟩` 读作「**本租户配置中 `role=good_wh` 的别名所命中的那个仓名**」；对本客户默认 = `总仓`。

| `event_subtype` | 来源 | 判定条件（**角色式，非字面值**） | 计入 |
|---|---|---|---|
| `store_return_to_loss_wh` | 门店退货单 | `退货仓库` ∈ ⟨临期仓⟩ | 门店行分子 / 公司行分子 |
| `op_wh_to_loss_wh` | 调拨单 | `调出仓` ∈ ⟨业务员仓⟩，`调入仓` ∈ ⟨临期仓⟩ | 业务员行分子 / 公司行分子 |
| `good_wh_to_op_wh` | 调拨单 | `调出仓` ∈ ⟨良品仓⟩，`调入仓` ∈ ⟨业务员仓⟩ | **业务员行分母** |
| `good_wh_to_loss_wh` | 调拨单 | `调出仓` ∈ ⟨良品仓⟩，`调入仓` ∈ ⟨临期仓⟩ | **良品仓直调行** / 公司行分子 |
| `loss_wh_sale` | 门店销售单 | `销售仓库` ∈ ⟨临期仓⟩ | 抵扣项（按业务员拆 / 按其他渠道拆） |
| `store_sale` | 门店销售单 | 按 `客户` 汇总（仅 `状态=已完成`） | **门店行分母** |
| `company_sale` | 门店销售单 | 全量汇总（仅 `状态=已完成`） | **公司行分母** |
| `wastage` | Hergent 报损单 / 手工 | — | 报损行 / 公司行分子 |

> ⭐ 关键设计：**划分由「仓库角色组合」决定，不由文件名决定**。用户混在一个文件里传也应能正确归位。

#### 🔴 未认领的仓名必须 fail-closed 暴露，不许静默丢弃

解析完一份文件后，**收集文件里出现过的全部仓名字面值**，逐个查 `loss_role_aliases`：

| 情况 | 处理 |
|---|---|
| 命中某个角色 | 正常归类 |
| **未命中任何角色** | ⚠️ **不静默丢**：进「**未归类**」告警块，**必须带量** —— `未归类仓：王琴仓 · 12 行 · 3,240 元 未纳入本次核算`，并在导入预览页给出「请指定它的角色」下拉（良品仓 / 临期仓 / 业务员仓 / 忽略） |
| 用户现场指定后 | 写 `loss_role_aliases`（`source='learned'`）⇒ **下次自动命中**，不再问 |

判据：**「未纳统量」必须显式暴露**（既有铁律：零值即健康是陷阱 / 降级即抹掉）。
反例：解析器默默跳过认不出的仓名 → 用户只会看到「本月货损突然降了」，**永远查不出原因**。

### 3.4 「月度核算 + 每日更新 + 保留历史」的落地

| 要求 | 落地方式 |
|---|---|
| 月度为核算单位 | `loss_monthly` 用 `period` 做键，**一月一行（每主体）** |
| 支持每日更新 | 重算时 `INSERT OR REPLACE` 覆盖**未结账**月份；同时写 `loss_daily` 快照 |
| 保留历史 | ① 月账：结账后 `is_closed=1` **锁定**；② 日快照：只读留痕；③ 导入批次：可查可撤销 |
| ⚠️ 未结账提示 | 页头固定显示「**本期未结账 · 数据截至 9 月 16 日**」—— 否则用户拿月中的数和上月结账数对比会得出错误结论 |

**结账闸门**：`is_closed=1` 后，任何导入 / 手工改 / 重算一律 409 拒绝，除非先「反结账」（需要 boss 角色 + 留痕）。

---

### 3.5 ⭐「叫法」可配置（本轮新增，直接来自用户答复①②）

**为什么必须做**：用户原话 ——「不同的经销商叫法不一样，默认总仓，但**要支持用户自己改名字**」。
这是本模块**能不能复制到第二个经销商**的分水岭；做不做得好，决定它是一次性交付还是可售产品。

#### 3.5.0 三个概念必须分清（混了就全错）

| 概念 | 存在哪 | 用途 | 谁改 |
|---|---|---|---|
| `role` 语义角色 | `loss_roles.role` | **代码里的稳定键**，永不变 | 谁都别改 |
| `role_label` **显示名** | `loss_roles.role_label` | 界面上怎么称呼它（要说用户的话） | **用户可改** |
| `alias` **别名** | `loss_role_aliases.alias` | **识别用**：舟谱导出里的**实际字面值** | **用户可改** |

🔴 **最容易犯的错：拿「显示名」当「识别依据」。**
用户把显示名改成「总仓」后，若代码拿显示名去匹配文件 —— 文件里恰好写 `总仓` 时侥幸能中，
但换个导出（或带个尾空格 `总仓 `）就全崩，且**只在客户现场崩**。
**判据：识别只读 `alias`，显示只读 `role_label`，两者不得互相兜底。**

#### 3.5.1 为什么不能塞进 `report_mapping`（免得被当成重复建设）

`report_mapping` 的映射目标是**主体**（一个业务员一行，带他的 `src_wh`/`dst_wh`），
而这里要映射的是**仓的物理角色** —— 良品仓 / 临期仓**不属于任何业务员** ⇒ 装不进去。两者互补：

| 映射 | 目标是 | 载体 |
|---|---|---|
| 客户名 → 门店 | 主体 | `report_mapping`（`counterparty_type='store'`）✔ **复用** |
| 仓名 → 业务员 | 主体 | `report_mapping.src_wh/dst_wh` ✔ **复用** |
| **仓名 → 仓的物理角色** | **角色** | ⭐ `loss_role_aliases`（新增，无处可复用） |
| **单据名 → 单据类型** | **角色** | ⭐ `loss_role_aliases`（同上） |

⇒ **`op_wh`（业务员仓）不预置别名**，由 `report_mapping` **动态认领**：
凡出现在 `report_mapping.src_wh/dst_wh` 里的仓名 ⇒ 即业务员仓。
**用户只需在一处登记业务员仓**，不会出现「两份清单互相不一致」这个经典漂移源。

#### 3.5.2 🔴 改名后的「重建路径」—— 不给出这条路，就等于假旋钮

（既有铁律：**新增"开关型"配置必须同时给出「状态 → 重建数据」的路径**。）

用户把别名从 `总仓` 改成 `主仓`（舟谱那边改过名）之后会发生什么：

| 对象 | 行为 | 为什么 |
|---|---|---|
| **未来导入的文件** | 立刻按新别名解析 | 解析读实时表 |
| **历史事件行** `loss_events` | **不回溯复写** —— 存的是 `src_wh_name/dst_wh_name` 的**原文**（事实不该被改写） | ✅ 正因为存了原文，才**可重建** |
| **未结账月份的月账** | 提供「**按当前别名重算本期**」→ 重跑 `event_subtype` 判定 → 覆盖 `loss_monthly` | 这是让别名变更**对历史生效**的唯一正确路径 |
| **已结账月份** | `is_closed=1` ⇒ **不自动回改**；要改须先反结账（boss + 留痕） | 否则上月已公示的数字会静默变化 |

🔴 **这条是硬前提，必须写进 DDL 注释：`loss_events` 必须保存仓名原文** ——
这正是 §3.2 ② 里 `src_wh_name` / `dst_wh_name` 两列存在的理由。
**若只存 `event_subtype`（判定结果）不存原文，别名一改就永久不可重建**，模块退化成一次性的。

#### 3.5.3 「假旋钮」自证：让用户自己看出别名有没有生效

`loss_role_aliases` 带 `hit_count` / `last_hit_at`，界面上直接呈现：

```
叫法映射（本期命中）
  良品仓   ← 别名：总仓            命中 128 行   最后命中 2026-09-16 15:02
  临期仓   ← 别名：临期仓          命中  46 行   最后命中 2026-09-16 15:02
  业务员仓 ← 别名：（来自业务员档案） 命中  92 行
  ⚠️ 未归类：王琴仓 · 12 行 · 3,240 元                    [ 指定角色 ▾ ]
```

判据（既有铁律「不做假旋钮」：配置须 ① 能被观察到 ② 无等价机制 ③ 效果稳定）：
这张「命中计数」同时满足三条 —— 用户改完别名**马上能看到命中行数变化**，不必找我查日志。

#### 3.5.4 ⚠️ 单据类型别名 ≠ 判定依据（必须区分）

用户说「别的经销商可能叫销售单/销售退单」。但**单据类型的最终判定不靠别名字符串，靠表头结构**（§5.2 版式探测）：

- **别名的作用**：① 上传入口 / 类型选择器**用用户的话**；② 文件里有「单据类型」列或文件名匹配到别名时 → **预选**
- **结构的作用**：**最终分流**（有「单据头块 + 明细块」= 调拨/车销版式；扁平列表 + 有 `状态` 列 = 自提版式）

⇒ **为什么不靠别名判定**：别名是**用户输入**，写错/漏配就全盘错位；结构是**文件的客观事实**，不随配置改变。
（同族铁律：任何「从字符串反推语义」的实现，都要先假设它是错的。）

---

## §4 表格结构设计（L3 展示层）

### 4.1 行结构：**分组 + 主体行 + 表尾**

```
┌─ 公司整体（置顶，视觉强化，唯一有"公司综合货损率"的行）
├─ ① 直营门店退货        （row_kind=store，一行一门店）
├─ ② 业务员仓调拨        （row_kind=operator，一行一业务员）
├─ ③ 良品仓直调临期仓    （row_kind=direct，**合计一行 + 可按单据展开**）
├─ ④ 报损                （row_kind=wastage，**合计一行 + 可按单据展开**）
└─ 表尾：合计校验行（= 各类金额加总，用于与公司行**并列核对**，不是替代）
```

> **为什么 ③④ 是单行而不是"一行一单据"**：它们没有"主体"（不是某个门店/业务员的责任），单据数可能很多。默认给合计，需要看明细时展开。

### 4.2 列注册表（**列只由一处派生**，沿用 `MASTER_COL_DEFS` 范式）

| # | 列 | 类型 | 适用行 | 来源 | 显示规则 |
|---|---|---|---|---|---|
| 1 | 主体 | 文本 | 全部 | 映射字典 | 门店名 / 业务员名 / `⟨良品仓⟩→⟨临期仓⟩` / `报损`（角色名走 `role_label` ⇒ **用户改名后这里自动跟着变**） |
| 2 | **分母金额** | 金额 | store / operator | 导入或手工 | ⭐ **列头必须标明是谁的分母**：门店行「本期销售额」，业务员行「良品仓发货额」 |
| 3 | 退货 / 调拨额（分子主项） | 金额 | store / operator / direct | 导入 | `loss_events` 按子类型汇总 |
| 4 | 临期货销售额（抵扣） | 金额 | operator | 导入或手工 | 只对业务员行有意义，其他行灰显 |
| 5 | **货损毛额** | 金额 | store / operator | 计算 | `= #3`（不扣抵扣） |
| 6 | **货损净额** | 金额 | store / operator | 计算 | `= #3 − #4`，**允许负数**（绿 + 注记「临期销售抵扣超过调拨损失」） |
| 7 | **货损毛率** | 百分比 | store / operator | 计算 | `= #5 ÷ #2`，恒 ≥ 0 —— **对照列** |
| 8 | **货损净率** | 百分比 | store / operator | 计算 | `= #6 ÷ #2` —— **默认主指标**，可为负；分母缺失显「**—（缺本期销售额）**」，**绝不显 0.00%** |
| 9 | 临期货折扣率 | 百分比 | operator | 计算 | `= 临期销售均价 ÷ 进货价` ⭐ **售价抵扣口径的防刷护栏**：< 70% 正常；**≥ 95% 标红**「疑似按正价销售，抵扣额含正常利润」 |
| 10 | 数据来源 | 枚举 | 全部 | 派生 | `导入` / `手工` / `混合` |
| 11 | 数据完整度 | 图标 | 全部 | 派生 | 缺价 / 缺分母 / 时间段缺口 / **未归类仓** → 黄色角标 + hover 明细 |

⭐ **为什么「毛率」与「净率」必须两列同在**（正面回应 §8.3）：
用户定案**按售价抵扣**（理由「代表给公司挽回了多少金额」），这会让净率**可能为负**。
如果同屏只有净率，用户看到负数的第一反应是「系统算错了」。两列并列后，用户能自己读出
「毛率 30.8% · 净率 −2.4% ⇒ 是临期销售抵扣把它拉下去的」—— **自证**，而不是来报 bug。

**列注册表落地**：新建 `const LOSS_COL_DEFS = [...]`，`<colgroup>` / `<thead>` / `<tbody>` / 表尾 td **四处共用同一份**（`Forecast.vue` 已验证的范式；改这里必须同步四处）。
⚠️ 沿用 `Forecast.vue` 的既有铁律：**表尾是另一张 table**（靠 `--foot-sl` 跟随）、冻结列偏移走 `colW()`、`table-layout:fixed` 下列宽权威是 `<colgroup>`。

### 4.3 公司整体行的公式（严格按用户给的口径）

```
毛额 = 门店退临期仓额 + 业务员仓调临期仓额 + 良品仓调临期仓额 + 报损额
净额 = 毛额 − 临期仓销售总额
分母 = 公司销售金额（仅 状态=已完成 的单据）
毛率 = 毛额 ÷ 分母 × 100%
净率 = 净额 ÷ 分母 × 100%      ← 用户口径（按售价抵扣）
```
⚠️ 三点必须显式，否则会算错或被误读：
1. **口径绑定**：抵扣按售价 ⇒ **`pricing` 必须同时切到 `sale`**，否则低估（§8.3 有量化：毛利率 20% 时低估约 2 个百分点）
2. **与业务员行的扣减范围不同**：公司行扣**临期仓销售总额**（含临期仓直卖的），业务员行只扣**自售部分**
   → 包含关系必须**可见**：表上给两列 `业务员自售临期货额` + `临期仓其他渠道销售（= 总 − 自售）`
3. **报损为 0 不能当作「无货损」**：`wastage_orders` 当前 0 行 ⇒ 必须显示「**报损未录入**」而非「报损 0 元」（零值即健康是陷阱）

### 4.4 两个视图状态（复用 `Forecast.vue` 范式）

| 状态 | 用途 | 实现 |
|---|---|---|
| **只读汇总表** | 看结果、冻结首列、横向滚动 | 一张 table + 表尾独立 table |
| **编辑态网格** | 手工录入行级聚合值 | 复用现有编辑表范式（含冻结列、`colW()` 拖宽） |

---

## §5 上传解析设计

### 5.1 总体流程（**沿用既有 `/preview` + `/execute` 两阶段契约**）

```
用户上传文件
  → POST /api/loss/accounting/import/preview   （只读，不落业务数据）
      ├─ ① 版式探测（块版 / 扁平版）
      ├─ ② 列映射（表头 → 字段），返回**依据**
      ├─ ③ 主体映射（客户→门店 / 仓→业务员），返回**未命中清单**
      ├─ ④ 时间段读取（从筛选条件行）
      ├─ ⑤ 校验告警（合计行跳过数 / 拒行 / 缺价 / 已存在重复）
      └─ ⑥ **将覆盖 N 个手工填写的值** ← 必须让用户看到
  → 用户确认界面（可改判映射）
  → POST /api/loss/accounting/import/execute   （落库，幂等）
```

### 5.2 版式探测（**实测有 3 套版式，必须先分流**）

| 版式 | 判别特征 | 解析策略 | 样本 |
|---|---|---|---|
| **明细块版** | 存在 `商品名称` 表头行 | 以 `商品名称` 行为锚点：向上找单据头表头行 + 值行；向下读明细直到 `合计` / 空行 | 调拨单明细、退货-车销、销售-车销 |
| **扁平列表版** | 存在 `单据编号`+`客户`+`订单金额` 且**无** `商品名称` | 找**最后一个连续表头行**，往下读；跳过 `总计` | 退货-自提 |
| **筛选条件行** | 存在 `单据日期` 且值形如 `A至B` | **读出时间段**用于完整性校验 | 退货-自提 r3/r4 |

**实现要点（逐条对应实测事实）**：
1. **表头不在第 1 行** → 用 `sheet.iter_rows` 扫前 8 行找锚点，禁止 `ws[1]`
2. **必须跳过 `合计` / `总计` 行** → 否则金额翻倍（技能 §零.1「合计行不可信」同族）
3. **空行分隔块** → 遇全空行时判断是否还有下一块（一个文件可能含多个单据）
4. **日期是文本** → 显式 `strptime('%Y-%m-%d %H:%M:%S')`，失败则该行拒收并报原因
5. **画布 200 行但只用十几行** → 全空行剪枝，别把 190 个空行当数据

### 5.3 列映射与主体映射（**必须做"改判真生效"**）

| 映射 | 锚点 | 兜底策略 | 风险 |
|---|---|---|---|
| 表头 → 字段 | `FIELD_LABELS` **唯一一份中文名**（复用，不新建） | 关键词匹配（做**列级唯一**，防泛词吞并） | 泛词错位（技能 §零.6） |
| 商品 | `条形码` 优先 → `商品名称` **精确** | **未命中 → 拒该行 + 报清单** | ⚠️ **绝不"取第一条"**（张冠李戴） |
| 客户 → 门店 | `report_mapping`（`counterparty_type='store'`） | 未命中 → 进「**未归类**」行 + 告警 | 见 §8.7 |
| **仓 → 业务员** | `report_mapping.src_wh/dst_wh` | 未命中 → **拒该行** + 报清单 | ⚠️ 调拨单没有业务员列，**必须走这张表** |
| ⭐ **仓名 → 仓角色** | `loss_role_aliases`（§3.5）——**唯一来源** | 未命中 → 进「**未归类仓**」+ **带量告警**（不静默丢） | 见 §3.3 末 |
| ⭐ **单据名 → 单据类型** | `loss_role_aliases`（同上）——**仅用于预选 / 文案** | 最终以**版式结构**为准（§5.2） | 别名不是判据（§3.5.4） |
| ⭐ **`状态` 列** | 无（直接取值） | 仅取 `已完成`，其余计入「已过滤」并**回报行数与金额** | 见 §5.4 第 4 闸 |

⭐ **首导必须走「映射确认」界面**（复用 `hergent-import-mapping-confirm` 的技能范式）：显示每列被识别成什么、依据是什么、前几行长什么样，**改判后真的生效**。
⭐ 其中「**仓库角色**」要**单独占一个区块**（它决定事件归到哪个分子），并**当场允许为未归类仓名指定角色** → 写回 `loss_role_aliases`（`source='learned'`）⇒ 下次自动命中。

### 5.4 数据完整性五道闸（**全部免费可得，强烈建议做**）

| 闸 | 做法 | 防的是什么 |
|---|---|---|
| **① 时间段重叠检测** | 从文件筛选条件行读 `declared_from/to`，与同期次已有批次比对 | 用户重复导出同一时间段 → 金额翻倍 |
| **② 时间段缺口告警** | 同 kind 的批次并集 ≠ 整月 → 标黄「10/1–10/5 无数据」 | 用户忘改筛选条件 → 静默漏数 |
| **③ 文件 MD5 幂等** | `UNIQUE(period, kind, file_md5)` | 手滑重复上传同一个文件 |
| 🔴 **④ 单据状态过滤**（本轮新增） | **只取 `状态=已完成`**；`待审核` / `已取消` 计入「已过滤」，并**回报行数 + 金额** | 把已取消的单据算进分子 → 分子**虚高**（§2.3 自提版有此列） |
| 🔴 **⑤ 合计行核对**（本轮新增，零成本） | 逐行求和 vs 文件里的 `总计` / `合计` 行，不等则告警 | 漏行 / 多行 / 跳错块 → 金额**静默错**（§2.3 自提版 `r101 = 17528.02` 就是现成的对照值） |

> 舟谱导出是**人工筛选**的（时间段 / 仓库都要手点），这三个闸能挡住绝大多数静默错数。

### 5.5 金额折算（**本模块最容易出错的一步**）

```
若 amount_source = 'doc'       → 直接用单据自带金额（退货单/销售单有 `订单金额`）
若 amount_source = 'computed'  → amount = qty × unit_price(pricing)
                                  unit_price 取自 products：
                                    cost → purchase_price（进货价，存量有值）
                                    sale → sale_price
                                  ⚠️ 禁止默认 factory_price —— 存量全为 0，
                                     会把调拨金额静默算成 0（比偏大更危险）
```
**必须回报** `price_stat = {total_rows, priced_rows, unpriced_rows, fallback_rows, pricing}` 并推送给用户 —— 沿用既有「显式回退 + 回报」范式。

---

## §6 接口设计

> 全部挂 `/api/loss/accounting/*` → RBAC 前缀 `/api/loss` 已映射 **`stock`** 模块（`server.py:314`），**无需新增登记**；否则 fail-closed 403 `MODULE_NOT_CONFIGURED`。

| 方法 | 路径 | 用途 | 权限 |
|---|---|---|---|
| `GET` | `/api/loss/accounting/periods` | 期次列表（含 `is_closed` / `computed_at` / 数据完整度） | stock.read |
| `GET` | `/api/loss/accounting/summary?period=` | **月度汇总（宽表行）** = 页面的主查询 | stock.read |
| `GET` | `/api/loss/accounting/detail?period=&row_kind=&subject_key=` | 明细钻取（回到 `loss_events`） | stock.read |
| `GET` | `/api/loss/accounting/trend?period=&months=` | 日快照 + 月度趋势 | stock.read |
| `PUT` | `/api/loss/accounting/manual` | 手工录入（行级，多参数**关键字传参**） | stock.update |
| `POST` | `/api/loss/accounting/import/preview` | **只读**解析预览 | stock.read |
| `POST` | `/api/loss/accounting/import/execute` | 落库（幂等） | stock.create |
| `GET` | `/api/loss/accounting/import/batches` | 导入登记列表 | stock.read |
| `POST` | `/api/loss/accounting/import/{batch_id}/revoke` | 撤销某批导入 | stock.delete |
| `POST` | `/api/loss/accounting/recompute` | 重算月账（写月表 + 日快照） | stock.update |
| `POST` | `/api/loss/accounting/close` | 结账（锁定） | stock.update + boss |
| `POST` | `/api/loss/accounting/reopen` | 反结账（**必须留痕**） | **boss only** |
| `GET/PUT` | `/api/loss/accounting/config` | 口径配置（pricing / 门店白名单 / 报损来源） | stock.read / **boss** |
| `GET` | `/api/loss/accounting/health` | 本期数据体检（缺口 / 缺价 / **未归类** / 重复导入 / 已过滤行数） | stock.read |
| ⭐ `GET` | `/api/loss/accounting/roles` | **角色清单**：`role` / `role_label` / `basis` / 别名数 / **命中统计**（§3.5.3） | stock.read |
| ⭐ `PUT` | `/api/loss/accounting/roles/{role}` | **改显示名** `role_label` —— **只影响展示，不影响识别** | **boss** |
| ⭐ `POST` | `/api/loss/accounting/role-aliases` | 新增别名（`alias` → `role`）；**名称已属别的角色 → 409**（库层 `UNIQUE(alias)`） | **boss** |
| ⭐ `DELETE` | `/api/loss/accounting/role-aliases/{alias}` | 删别名；`source='builtin'` **只允许停用不允许删** | **boss** |
| ⭐ `GET` | `/api/loss/accounting/unmapped?period=` | **未归类清单**：名称 + **行数 + 金额** + 样例行 + 出现过的文件 | stock.read |
| ⭐ `POST` | `/api/loss/accounting/unmapped/assign` | 现场指定角色 → 写别名（`source='learned'`）**并触发本期重算** | stock.update |

🔴 **`/unmapped/assign` 必须同时触发重算** —— 只写别名不重算 = 用户指定完角色、数字**一点没变** = 假旋钮（§3.5.2）。
🔴 **`PUT /roles/{role}` 只改 `role_label`，绝不允许传 `alias`** —— 两个字段分开两个端点，从**接口形状**上防止「拿显示名当识别依据」（§3.5.0）。
🔴 **`POST /role-aliases` 的冲突必须 409 硬拒**，不许「后者覆盖前者」—— 冲突意味着用户自己也不确定语义，静默覆盖会让归因不可追溯。

**响应体约定（对齐既有统一信封）**：`{ok, success, data, message}`；`data` 里**必须带** `rate_num` / `rate_den` / `data_quality`，让前端能解释"这个率是怎么来的"。

⚠️ **前端 `api()` 已解开一层信封** ⇒ 调用点直接拿 `data`，**再读一次 `.data` 恒为 `undefined`**（本项目历史高发坑）。

**留痕**：手工录入 / 反结账 / 撤销导入 / 口径配置变更 → 一律走 `routers/_field_log.py::safe_log`，`entity_type='loss_manual_input'` / `'loss_period'`，**留痕在事务提交之后写**。期次用 `ref=period` 以便跨期检索（周期键置首）。

---

## §7 页面交互设计

```
┌──────────────────────────────────────────────────────────────────────┐
│ 货损核算（月度）                       [2026-09 ▾] ⚠️未结账·截至9/16 │ ← 状态必须常显
│ 计价口径：售价（与抵扣口径绑定）▾  ← 全局，影响所有金额，切换需确认   │
├──────────────────────────────────────────────────────────────────────┤
│ [上传数据 ▾] [手工录入] [重算] [结账] [导出] [叫法映射] [数据体检⚠️3] │
│    ├ 门店销售单（自提订单）  ← 门店销售额 + 临期销售额（按仓拆分）     │
│    ├ 门店退货单（自提退单）  ← 门店退临期仓额                          │
│    ├ 调拨单明细              ← 良品/业务员/临期 三仓的 3 种组合        │
│    └ 报损单（手工录入）      ← 舟谱无此数据                            │
├──────────────────────────────────────────────────────────────────────┤
│ 公司整体 销售 200.0万 │ 毛货损 12.2万 │ 净货损 11.1万 │ 净率 5.55%   │
│                                          毛率 6.10%  ⓘ分子/分母构成   │
├──────────────────────────────────────────────────────────────────────┤
│ ① 直营门店退货                        分母=门店销售额                  │
│   门店        │销售额   │退货额 │净额  │货损率      │来源 │完整度      │
│   永辉民发店  │ 15.0万  │ 0.8万 │0.8万  │ 5.33%      │导入 │ ✓          │
│   美联（保康）│  8.0万  │  —   │  —   │—（缺退货额）│手工 │ ⚠️         │
├──────────────────────────────────────────────────────────────────────┤
│ ② 业务员仓调拨                        分母=良品仓发货额               │
│   业务员│发货额 │调临期│临期自售│毛额 │净额 │毛率 │净率  │折扣率│完整度│
│   王琴  │ 20.0万│ 1.2万│ 0.5万  │1.2万│0.7万│6.00%│ 3.50%│ 78%  │ ✓   │
│   毛辉  │ 12.0万│ 0.9万│ 1.1万  │0.9万│-0.2万│7.50%│-1.67%│62%⚠️│ ⚠️  │
│         ↑ 净率出现负数时，**左邻的「毛率」就是现成的对证**：           │
│           是临期销售抵扣拉下去的，不是系统算错                          │
├──────────────────────────────────────────────────────────────────────┤
│ ③ 良品仓直调临期仓   2.0万   （占公司销售额 1.00%）  [展开明细]        │ ← 不给率
│ ④ 报损              0.0万  ⚠️本期无报损记录，是否漏录？  [录入]        │
└──────────────────────────────────────────────────────────────────────┘
```

**关键交互**：

| 交互 | 设计 | 理由 |
|---|---|---|
| 上传 | **先预览弹窗**（版式识别 + 列映射 + 未命中清单 + ⭐**未归类仓·带量** + ⭐**已过滤行数** + **将覆盖 N 个手工值** + 拒行清单）→ 确认才落库 | 防静默错数；延续既有两阶段契约 |
| 手工录入 | 编辑态网格，只让填**聚合值** | 业务老板不会手填 40 行明细 |
| 手工 vs 导入冲突 | 导入默认覆盖手工，但**必须显式提示**；手工改导入值 → 标 `manual_override` + 留痕 | 静默覆盖不可接受 |
| 货损率列 | 分母缺失 → 「**—（缺销售额）**」，**绝不 0.00%** | 「零值即健康」陷阱 |
| 负数货损率 | 绿色 + tooltip「临期货销售覆盖了调拨损失」 | 业务上是好事 |
| 「数据体检」 | 一键列出：时间段缺口 / 缺价商品 / **未归类仓** / 未归类客户 / 重复导入 / **已过滤单据** | 把 §5.4 五道闸做成入口 |
| ⭐ **叫法映射** | 工具栏入口 → 角色清单（显示名可改）+ 别名表 + **命中计数** + 未归类清单 | §3.5.3：让用户**自己**看出别名是否生效 |
| ⭐ **未归类即时指定** | 预览弹窗内直接为「未归类仓名」指定角色 → **写入别名并触发重算** | §3.3 末：不静默丢，且改完**立刻**看到数字变化 |
| ⭐ **改显示名 vs 改别名** | 分两处：显示名只改展示（立即生效）；改别名提示「**需重算本期**」并给一键按钮 | §3.5.0 / §3.5.2（改完不重算 = 假旋钮） |
| ⭐ **计价口径切换** | 二次确认 + 明示「所有历史数字都会变」；**抵扣口径必须与 `pricing` 同口径** | §8.3（否则低估约 2 个百分点） |
| 钻取 | 点金额 → 明细弹窗（单据号 / 商品 / 数量 / 单价 / 来源文件 / 行号） | 可对账 |
| 结账 | 二次确认 + 显示本期关键数；结账后所有写操作禁用 | 财务铁律 |
| 单位 | **存储元，展示万元**（项目统一约定） | 沿用 |

**权限细化**（⚠️ 门店明细含客户名，属敏感项）：`sales` 角色看门店行时**只给汇总、隐藏门店真名**；`boss`/`admin` 全量。新路由 RBAC 前缀已覆盖，但**行级过滤必须在端点内实现**。

---

## §8 我认为不合理的地方（按严重度）

### 🔴 8.1 三个「货损率」分母各不相同，却并列在一张表上

| 率 | 分子 | **分母** |
|---|---|---|
| 门店货损率 | 本期退货额 | **门店销售额**（对下游的终端销售额） |
| 业务员货损率 | 调拨额 − 临期自售 | **良品仓发货额**（对内的配货额） |
| 公司货损率 | 四项相加 | **公司销售额** |

**问题**：三个率**不能相加、不能平均、也不能比大小**。用户看到三个率，第一反应必然是"加起来应该等于公司率"，而这**永远对不上**。

**建议**：
1. **改名**：`门店退货率` / `业务员调拨货损率` / `公司综合货损率` —— 名字里带口径
2. **列头标明分母**，不要让用户猜
3. 公司率行**置顶或置底并注明**「不是上面几行的合计」
4. 加起来能对上的只有**金额**（表尾给金额合计校验行），**不是率**

---

### 🔴 8.2 分子里混了两种计价口径 —— 这是最实质的问题

**实测证据**：
- 舟谱 `退货订单` 的 `订单金额` = `退货价（折后价）× 数量` = 7.67 × 5 = **38.35** ⇒ **售价口径**
- 舟谱 `调拨单明细` **完全没有金额列**，只有数量 ⇒ 金额只能由系统按 `purchase_price`（进货价）折算 ⇒ **成本口径**

**后果**：分子 = 售价口径的退货额 + 成本口径的调拨额 + ... ⇒ **这个"金额"没有任何经济含义**。

**实算（毛利率按 20% 假设）**：

| 项 | 用户口径（混用） | 一致口径（售价或成本，结果相同） |
|---|---|---|
| 门店退临期仓 | 100,000（售价） | 100,000（售价）/ 80,000（成本） |
| 业务员仓调临期仓 | 50,000（成本） | 62,500（售价）/ 50,000（成本） |
| 良品仓调临期仓 | 20,000（成本） | 25,000（售价）/ 20,000（成本） |
| 报损 | 0 | 0 |
| 临期仓销售（扣） | −35,000（售价） | −35,000（售价）/ −28,000（成本） |
| **分子** | **135,000** | **152,500** / **122,000** |
| **分母** | 2,000,000（售价） | 2,000,000 / 1,600,000 |
| **货损率** | **6.75%** | **7.63%** / **7.63%** |

⭐ **注意**：一致口径下无论选售价还是成本，**结果完全一样**（同比缩放）。真正的错误只来自**混用**。
⇒ 混用让货损率**偏低 0.88 个百分点（相对偏低 11.5%）**，且偏差随毛利率变化，**事后无法修正**。

**建议**：`pricing` 做成**全局单一配置**（复用现有 `loss_recipe.pricing`），页面固定位置常显当前口径，切换口径需二次确认（因为所有历史数字都会变）。切换后**已结账月份不回改**（月表里每行冻结了 `pricing`）。

---

### 🟡 8.3 「临期货销售额抵扣」—— **2026-09-17 已定案：按售价**，但必须与 `pricing` 绑定

**用户定案**（原话）：「**按售价**，代表给公司**挽回了多少金额**」。

这个业务语义**清楚且合理** —— 用户要的不是「成本损失」，而是
「**净损失 = 实际损失 − 已挽回回款**」，即「这批临期货最终让公司亏了多少」。
**口径就此定案，不再改**。但**必须同时改另一处**，否则数字是错的：

#### 🔴 抵扣按售价 ⇒ `pricing` 也必须切 `sale`，两者绑定（分子内部不许混币值）

| 组合 | 调拨额 | 抵扣额 | 分母 | 是否自洽 |
|---|---|---|---|---|
| **A 全成本** | 成本 | 成本 | 成本 | ✅ 自洽 |
| **B 全售价** | **售价** | **售价** | **售价** | ✅ 自洽 ← **用户要的「挽回金额」语义落在这里** |
| **C 混用**（成本调拨 + 售价抵扣） | 成本 | 售价 | 成本 | ❌ **低估** |

**组合 C 低估多少 —— 本节唯一的硬结论**：

```
C 与 A 的差 =（售价抵扣 − 成本抵扣）÷ 成本分母 = 临期销售那批货的毛利额 ÷ 成本分母
```
⇒ **多扣掉的正好是「临期销售的毛利」**。一句话记住：**抵扣按售价、调拨按成本 = 白扣一份毛利**。

代入实算（毛利率 20%、临期销售额 12,000、其成本 10,000、成本分母 100,000）：

| | 分子 | 分母 | 货损率 |
|---|---|---|---|
| **A 全成本** | 10,000 − 10,000 = 0 | 100,000 | **0.00%** |
| **C 混用**（用户当前描述的口径） | 10,000 − 12,000 = **−2,000** | 100,000 | **−2.00%** |
| **B 全售价**（语义换算后） | 12,500 − 12,000 = 500 | 125,000 | **0.40%** |

⇒ **C 与 B 相差 2.4 个百分点**；而 A 与 B 只差 0.4 个百分点（差异来自商品毛利结构，属二阶效应）。
**混用造成的偏差，比口径选择本身大 6 倍** —— 这就是必须绑定的理由。

⭐ 反直觉但重要：**A 与 B 在数学上近似等价**（分子分母同比例缩放）。
所以「按售价抵扣 + 全售价折算」**不会**让货损率失真 —— 用户既拿到了「挽回金额」的语义，率也没被扭曲。
**真正会扭曲的只有 C。**

#### 售价口径的必要配套（两条，缺一不可）

1. **必须加「临期货折扣率」列**（§4.2 #9）：`临期销售均价 ÷ 进货价`。
   售价口径**唯一的可刷数路径**是把好货当临期货低价甩卖；但若折扣率**接近 100%**，
   说明它不是止损、而是**正价销售** —— 抵扣掉的是正常利润。
   ⇒ **≥ 95% 标红**并提示。（纯成本口径不需要这列，这是售价口径的专属代价。）
2. **负值绝不 clamp 到 0**：负货损率是有效信号，绿色显示，
   并**同屏给出毛率对照**（§4.2 #7）让用户能自证「是抵扣把它拉下去的」

---

### 🔴 8.4 「良品仓直调临期仓」和「报损」没有分母，给"率"就是造出来的

用户要求这两类单独列出（对的），但如果也给一个"货损率"，那个分母是**凭空造的**。
**建议**：这两行**只给金额 + 占公司销售额的百分比**，不给"率"。若要给率，请先定义分母（候选：`良品仓总入库额` / `良品仓总出库额` / `公司销售额`），**由用户指定**，不要替用户定。

---

### 🟡 8.5 「月度核算 + 每日更新」语义未定义

三个要求叠加会产生歧义：月记录要不要每天新增一条？
**风险**：一个月出现 30 条互相矛盾的"9月货损率"，用户必然拿错。
**建议**：月账唯一行（日更覆盖）+ 日快照留痕（只读）+ **未结账标识** + 结账锁定。见 §3.4。

---

### 🟡 8.6 「报损金额」没有数据来源

- 舟谱的退货/调拨导出里**没有报损数据**（实测确认）
- Hergent 自己的 `wastage_orders` **整表 0 行**（系统从未录入过报损单，2026-09-13 实测）
- ⇒ 公司口径的分子**少一项** → 货损率**系统性偏低**，而用户不会发现

**建议**：① 页面上给报损**独立录入入口**；② 本期报损为 0 但库存有临期批次时，**必须提示**「本期无报损记录，是否漏录」（沿用「三态 + 缺数据不伪装成安全」范式）。

---

### 🟡 8.7 「直营门店」在舟谱里没有标识，客户列混着多种业态

实测客户列 12 个值里，`美联总仓`／`多多买菜`／`沃尔玛`／`嗨特卖（丹江路）` **显然不是"直营门店"**（KA / 电商 / 自有仓）。
**风险**：把 KA 渠道也算进"直营门店退货率"，分母（门店销售额）含义会混乱。
**建议**：门店白名单可配（复用 `report_mapping.counterparty_type`），**未归类的客户进「未归类」行并告警**，不静默计入。

---

### 🟡 8.8 调拨单里没有可靠的「业务员」字段

实测调拨单明细：只有 `创建人`（仲嫚嫚，制单人）、`调入仓`（刘善涛仓）、`调出仓`（毛辉仓）—— **没有"业务员"列**。
**风险**：靠"仓名去掉仓字"猜业务员，遇到**改名 / 同名 / 代操作**必然错，而且**不报错**。
**建议**：建「仓 ↔ 业务员」**显式映射**（放 `report_mapping.src_wh/dst_wh`），未映射的仓名**拒收该行**并列清单。**判据：任何"从字符串反推身份"的实现都要先假设它是错的。**

---

### 🟡 8.9 退货单有**两套版式**，精度不一致

实测：`退货-车销` = 单据头 + 商品明细（可到商品级）；`退货-自提` = 扁平列表（**只有单据金额**）。
**影响**：同一个"门店退货额"，两种来源的**明细可钻取深度不同**。
**建议**：解析器先探版式再分流；报表的「数据来源」列标注该行来自哪套版式，钻取时如实显示"此数据为单据级，无商品明细"。

---

### 🟡 8.10 手工录入与导入回填的优先级未定义

**风险**：用户手工填了 3 小时，一次上传把它静默覆盖。
**建议**：导入预览必须显示「**将覆盖 N 个手工填写的值**」并要求确认；手工改导入值 → 标 `manual_override` + 留痕。

---

### 🟢 8.11 同屏会有两个都叫「货损」的东西

现有 `/loss` 页（配方驱动、扫 `inventory` 批次、算**"预计货损金额"**）与新页（流水口径、算**"货损率"**）**同名不同口径**。用户拿两个数对比就会困惑。
**建议**：① 现有页改名「**临期预警与预计损耗**」；② 两页**共用同一份 `pricing` 配置**（一个产品里不能有两套"货损"的计价口径）；③ 新页在侧栏与现有页相邻。

---

### 🟢 8.12 数据完整性没有兜底

舟谱导出是**人工筛选**（时间段、仓库都要手点），忘改条件 = 数据重复/遗漏，且**系统当前无法察觉**。
**建议（免费可得）**：从导出文件的**筛选条件行**自动读出时间段（实测自提版第 4 行就是 `2026-05-31至2026-06-29`），做**重叠检测**与**缺口告警**。见 §5.4。

---

## §9 优化建议（超出需求的部分）

| 优先级 | 建议 | 价值 |
|---|---|---|
| **P0** | **全局 `pricing` 口径开关 + 页面常显** | 解决 §8.2，不做则数字无意义 |
| **P0** | **导入两阶段 + 映射确认 + 覆盖提示** | 防静默错数，本模块最大的质量风险 |
| **P0** | **率 = 分子/分母 三元组一起存** | 否则历史月账永远解释不清 |
| **P0** | **缺席显示「—」而非 0** | 「零值即健康」陷阱 |
| **P1** | **数据体检入口**（缺口/缺价/未映射/未归类/重复） | 把 5 类静默错误变成可见 |
| **P1** | **结账锁定 + 反结账留痕** | 月度核算的财务底线 |
| **P1** | **日快照趋势图**（公司 + 各业务员月度折线） | 用户真正想看的是趋势 |
| **P1** | **临期货折扣率防刷数** | 见 §8.3 |
| **P2** | **归因提示**：良品仓直调占比 > 阈值 → 「本期货损主要来自**要货/仓储超期**，与业务员无关」 | 把数据变成决策 |
| **P2** | **AI 副驾联动**：副驾能直接回答"王琴这个月货损多少、为什么" | 复用既有 `loss_recipe` → skill 同步链路 |
| **P2** | **对账导出**：按业务员出「货损确认单」（含明细），供业务员签认 | 落到管理动作 |
| **P2** | **月度排行 + 环比** | 给老板看的抓手 |

---

## §10 分期实施建议

> ⚠️ **本轮修订的关键调整：「叫法映射」（§3.5）从"以后再说"提到 P0-1**。
> 理由：它是**唯一一处无法事后补的架构决策** —— 若先按 `总仓` / `自提订单` 硬编码写解析器，
> 后面要改就得同时动「解析 / 事件归类 / 重算 / 展示」四处，而且**你没有第二个租户可以先验证**。
> 宁可 P0 多花一天，也不要交付一个「只能卖给你自己」的解析器。

| 期 | 内容 | 依赖 |
|---|---|---|
| **P0-1** | ⭐ **叫法映射（§3.5）**：`loss_roles` + `loss_role_aliases` + 出厂种子 + **未归类带量告警** + 命中计数 | ✅ 无依赖（已定案） |
| **P0-2** | 数据模型建表（含 `loss_events` 的**仓名原文**两列）+ 口径配置（`pricing` / 门店白名单 / 仓→业务员映射） | P0-1 |
| **P0-3** | 两类主数据导入（**自提退单** + **调拨单明细**）+ 预览 + 执行 + 映射确认（含未归类即时指定） | 实测版式已明确 |
| **P0-4** | 汇总表页面（4 类行 + 公司行 + **毛/净两率** + 手工录入 + 钻取） | 复用 `Forecast.vue` 表格范式 |
| **P0-5** | 数据体检（**五道闸**）+ 留痕 + RBAC | 复用既有设施 |
| **P1** | **门店销售单（自提订单）**导入（门店销售额 + 临期销售额）+ 报损入口 | 🔴 需先拿到**真「自提订单」导出**核对列名（§12-2） |
| **P1** | 日快照趋势 + 结账锁定 + 导出 | |
| **P2** | 副驾联动 + 对账单 + 排行 | |

---

## §11 需要遵守的既有铁律（实施时逐条对照）

1. **金额单位**：存储**元**，录入与展示统一**万元**
2. **率与百分点**：差值＝**个百分点**（减法），比例＝**%**（除法），不可换
3. **同屏数字口径必须同源**；配置里有阈值而代码硬编码另一套 = 口径不一致高发源
4. **第二份拷贝 = 静默漂移** → 列注册表 / `FIELD_LABELS` / 仓映射 都只能有一份
5. **零值即健康是陷阱** → 缺数据必须另立字段回传（三态）
6. **新表/新列三处登记**；`CREATE TABLE IF NOT EXISTS` 不给已存在的表补列 → 新增列须在 `_ensure_*` 里 `PRAGMA table_info` + 幂等 `ALTER TABLE`
7. **新路由必须登记 RBAC**（fail-closed 403）
8. **多参数写接口一律关键字传参**；取不到收件人必须失败且不落库
9. **留痕只记真实变化**；字段集取请求体实际出现的字段；**留痕在事务提交之后写**
10. **前端 `api()` 已解开一层信封** ⇒ 调用点直接拿 `data`
11. **真机断言选择器禁用 `[class*=xx-]`**；`page.evaluate` 传字符串会被当表达式求值
12. **给表格加冻结列**：z-index 全景 / 既有冻结列 `left` 整体右移（走 `colW()`）/ **表尾是另一张 table**
13. ⭐ **「叫法」必须配置驱动**（2026-09-17 v176 同源，本轮新增）：仓名 / 单据名**不许硬编码**；
    识别只读 `alias`、显示只读 `role_label`（**两者不得互相兜底**）；**未认领的名称必须带量告警**（不静默丢）；
    **改了别名必须给「重算本期」路径** —— 且前提是事件表**存原文**，否则永久不可重建

---

## §12 待确认事项

### ✅ 已定案（2026-09-17，原 3 条全部落定）

| # | 问题 | 用户定案 | 落地位置 |
|---|---|---|---|
| 1 | 「良品仓」在舟谱里叫什么 | **`总仓`**；且「不同经销商叫法不一样，**默认总仓 + 支持改名**」 | §1 ✅3 / §3.5 |
| 2 | 门店销售额 / 临期货销售额的来源 | **自提订单**（+ 自提退单） | §2.4 |
| 3 | 临期货销售额抵扣用售价还是成本 | **按售价** —— 理由：「代表给公司**挽回了多少金额**」 | §8.3 |

### 🔴 由定案新引出的 3 个问题（建议尽快回答，其中第 1 条阻塞 P0）

1. **`pricing` 是否一并切到「售价」口径？**（§8.3）
   你选定「按售价抵扣」，**抵扣项是售价**；但调拨额目前仍是**成本**口径 ⇒ 这正是 §8.3 的「组合 C」，
   **会低估约 2 个百分点**（毛利率 20% 时）。
   「按售价抵扣 + 全售价折算」既保住你要的语义、率又**不失真**（与成本口径近似等价），所以建议切过去。
2. **请提供一份真实的「自提订单」导出**（§2.3 末）。本轮只实测过「自提**退单**」，
   自提订单的解析设计目前**基于「与退单同构」的假设** —— 列名必须实测核对，不能照抄假设。
3. **门店销售额只取「自提订单」吗？** 若门店也走车销（`销售订单-车销`），
   只取自提会**漏掉车销部分** ⇒ 门店分母偏小 → 门店货损率**偏高**。

### 🟡 可边做边定（不阻塞开工）

4. 门店白名单如何界定？`美联总仓` / `多多买菜` / `沃尔玛` / `嗨特卖` 是否算「直营门店」？（§8.7）
5. 报损数据的来源？舟谱无此数据，Hergent `wastage_orders` 当前 **0 行** ⇒ 公司口径分子**系统性少一项**
6. 良品仓直调 / 报损行是否要给「率」？若要，分母是什么？（§8.4）
7. 期次归属按**单据日期**还是**上传日期**？（建议单据日期，并允许跨月补录）

### ✅ 已顺手澄清（无需再问）

- **两个「货损」的命名冲突**：本模块是**期间流水核算**（算率），与既有 `LossWorkflow.vue` 的
  **批次效期预测**（扫库存快照算预计损耗）不是同一件事 ⇒ 建议老页更名「**临期预警与预计损耗**」，
  新页叫「**货损核算**」，避免一个产品里出现两个「货损」两套计价口径（§8.11）。
