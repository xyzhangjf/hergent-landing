# Hergent 整合优化方案（hergent.cn 现有产品 × AI 经营副驾新方向）

> 制定日期：2026-08-11 ｜ 状态：**供确认，暂未实现代码**
> 范围：将「当前开发的 AI 经营副驾方向（UI 原型 + 报单流程方案 + 6 工具规划）」与「已部署的 hergent.cn（hergent-erp Web 版）」整合为统一产品。

---

## 0. 一个必须先说清的关键发现（决定整合难度）

本次动手前，我对 hergent.cn 的 Web 后端（`hergent-erp/server/`）做了实查，结果颠覆了"AI 层几乎要从零造"的假设：

**你要的 6 个 AI 工具，底层引擎在生产后端里已经全部存在：**

| 工具 | 现有生产后端已具备 | 位置 |
|---|---|---|
| 预报订单 | 采购申请 + 需求计划 + 智能预测引擎 | `/api/purchase-requisitions`、`/api/demand-plans`、`domain/forecast_engine.py` |
| 算货损 | 临期处理 + 报废 + 效期引擎 | `/api/near-expiry/process`、`/api/inventory/dispose-expired`、`check_expiry` |
| 算工资 | 完整薪酬引擎（社保/公积金/个税/工资条/群发） | `routers/hr.py`（`/payroll/run` 等）、`hermes_core.run_payroll` |
| 对账 | 供应商/客户对账 + 银行流水匹配 | `routers/reconciliation.py`、`match_bank_statement`、`bank_statement_*` |
| 算指标达成 | 销售目标表 + 达成进度 | `/api/sales-targets` |
| 算返利 | 返利合同 + 返利进度报告 | `/api/rebate-contracts`、`/api/reports/rebate-progress` |

**结论：整合不是"重写一遍"，而是「前端形态重构 + 报单流程串联 + 补一个智能审核大脑的合成层」。现有引擎、RBAC、多租户、Hermes 对话全部保留复用，不沉没。**

---

## 1. 两者核心功能与定位对比

| 维度 | hergent.cn 现有产品（已部署） | AI 经营副驾新方向（当前开发） |
|---|---|---|
| **本质定位** | 功能丰富的通用快消 ERP + AI 增强（多角色录入/操作系统） | 以一线懂行为护城河的「老板 AI 经营副驾」（决策/盯盘/预警系统） |
| **导航形态** | 侧边栏分组：经营看板 / 财务 / 人事 / 工具 / AI 团队角色 / 对话 | 经营工作台 / 智能审核大脑 / 6 AI 工具 / 厂家规则设置（轻、聚焦） |
| **视角** | 系统操作视角，多角色（老板/会计/业务/库管） | 老板视角，AI 主动找人、答案进微信 |
| **AI 能力** | 已有 `/api/chat`（Hermes 三级递进：本地CRUD→Hermes工具→AI对话）、20 个 Hermes 工具、技能目录 | 强调「AI 解释账不是算账」、6 工具统一入口、智能审核大脑（三因子合成建议订单） |
| **报单流程** | 分散在各模块（采购申请、销售目标、返利合同各自独立） | 串成一条流水线：预报→三因子审核建议→余额/付款通知→一键模板（厂家下单/舟谱导入） |
| **外部系统** | 无开放 API 对接（蒙牛/舟谱靠人工） | 明确「模板+人工粘贴」，不承诺自动直连（合规/稳定红线） |
| **当前状态** | 生产运行，进销存菜单已隐藏（引擎保留） | 概念原型（UI 原型 HTML + 报单方案文档），未接真实后端 |

**核心差异不在"有没有引擎"，而在：UI 形态、视角聚焦、报单流程是否被串成一条线、以及"智能审核大脑"这个合成价值层是否显式存在。**

---

## 2. 整合目标与预期效果

### 目标
1. **一套产品，一个导航，一个 Hermes 引擎**：把"厚 ERP 增强版"收敛为"AI 经营副驾"，符合你锁定的战略（ERP 入口已隐藏，只做 AI 层）。
2. **复用而不是重造**：6 工具引擎全部复用现有生产后端，把精力压在"形态 + 流程 + 审核大脑"这三件真正差异化的事上。
3. **报单流程端到端可跑**：让"预报→审核→余额→付款→下单→舟谱"在系统内留痕、AI 给建议、人点确认，替代文员 Excel 搬运。

### 预期效果
- 产品从「系统」变为「副驾」：老板打开就是 KPI + 对话 + 待办，不学操作。
- 现有投资（引擎/RBAC/多租户/Hermes）100% 保留，无沉没成本。
- 智能审核大脑成为差异化卖点（竞品 ERP 没有"指标+返利+库存三因子合成建议订单"）。
- 风险可控：前端导航重构不动引擎，可灰度、可注释回退。

---

## 3. 需要保留 / 废弃 / 降级的模块

### ✅ 保留（核心价值，复用）
- **全部后端引擎**：forecast_engine、payroll、reconciliation、near-expiry、rebate、sales-targets、purchase-requisitions。
- **Hermes 对话**（`/api/chat` 三级递进）— 作为副驾对话入口。
- **RBAC / 多租户 / 用户 / API tokens**（现有权限与数据隔离基座）。
- **经营看板 dashboard**（KPI 展示基础，重做成首页）。
- **进销存底层引擎**（已隐藏菜单，AI 工具与报单流依赖其数据，保留）。

### 🔽 降级（不再是主导航，收进"系统/高级"）
- **财务 / 人事 / 工具 三个厚菜单组**：不再是首屏主入口，降级为"高级功能"（会计/HR 等深度角色用）。这避免老板被模块海洋淹没。
- **AI 团队角色切换（大秘/会计/运营主管…）**：保留机制，但默认进入"老板副驾"视角，角色切换收进设置。

### 🗑 废弃 / 冻结（不再投入）
- **桌面端 Electron（desktop-app）**：已 55% 冻结，与 Web 版重复且维护成本高。维持"Web 集中托管"战略。
- **重复的"财务/人事"作为产品主叙事**：不再对外讲"这是 ERP"，改讲"AI 经营副驾"。

### 🆕 新建（真正要写的增量）
1. **经营工作台首页**（KPI 卡 + 对话 + 待办聚合）—— 基于现有 dashboard 重排。
2. **智能审核大脑页面 + 聚合逻辑**（三因子合成"建议订单"）。
3. **报单流水线编排**（预报汇总 → 审核建议 → 余额/付款通知 → 一键模板）。
4. **6 工具统一入口卡片**（前端组合调用现有接口，无需新后端，除审核大脑合成层外）。
5. **厂家与规则设置页**（录入指标/返利/提成——你的领域 IP，复用现有 rebate-contracts / sales-targets 表）。

---

## 4. 技术层面的整合方式

### 4.1 数据层（统一，无需新建 schema）
- 共用现有多租户库（`tenant_1.db` 等）。返利合同表、销售目标表、临期记录、采购申请表、工资表、对账流水表**均已存在**。
- 报单流水线新增的「审核建议 / 付款通知状态」可用轻量新表或挂在现有 purchase_requisitions 扩展字段，不另起炉灶。
- **铁律**：沿用现有 `get_db()`/`get_db_tx()` 事务约定（只读语义、显式 commit），不新建数据访问层。

### 4.2 接口层（复用为主，极少新增）
- **6 工具全部复用现有 REST 路由**，前端直接调：
  - 预报 → `POST /api/purchase-requisitions` + `GET /api/demand-plans`
  - 货损 → `POST /api/near-expiry/process`
  - 工资 → `POST /api/payroll/run` + `GET /api/payroll/payslip/{eid}`
  - 对账 → `routers/reconciliation.py` 的确认接口 + `match_bank_statement`
  - 指标 → `GET/POST /api/sales-targets`
  - 返利 → `GET/POST /api/rebate-contracts` + `GET /api/reports/rebate-progress`
- **唯一建议新增**：`/api/review-brain/advise`（智能审核大脑聚合接口）—— 内部组合调用 sales-targets + rebate + forecast/near-expiry + purchase-requisitions，返回"建议最终订单 + 三因子解释"。**也可纯前端组合调用现有接口实现，不强制新增后端**（降低风险）。
- 对话：复用 `/api/chat`，副驾的"问 AI"直接走它。

### 4.3 页面结构（前端导航重构，Vite 构建）
- 改 `static/app.js` 的 `MENU_GROUPS`（第 541 行区域），将现有"财务/人事/工具"折为"系统（高级）"组。
- 新增主导航组"经营副驾"：
  ```
  经营工作台(dashboard 重排) / 智能审核大脑(新建) /
  预报订单 / 算货损 / 算工资 / 对账 / 指标达成 / 返利测算 / 厂家与规则
  ```
- 进销存隐藏组保持注释（恢复只需取消注释）。
- 视觉沿用现有"苹果化 + 品牌青"规范（`#f5f5f7`/`#06b6d4`/系统色），保证与现有 dashboard 一致。
- 构建部署：`cd static && VITE_BASE=/ npm run build` → rsync 到 `/opt/hergent-erp/static`（沿用既有部署流程）。

---

## 5. 分阶段实施步骤与优先级

### P0 — 导航与形态整合（低风险、高可见，先做）
- **目标**：产品从"ERP 增强版"变为"副驾形态"，老板首屏即对。
- **动作**：重构 `MENU_GROUPS` 为副驾主导航；重排经营工作台首页（KPI 卡 + 对话 + 待办）；6 工具做成一级卡片入口，**直接调现有接口**。
- **不动引擎**，可灰度、可注释回退。
- **验收**：hergent.cn 打开即副驾形态，6 工具点开能出数（复用现有数据）。

### P1 — 智能审核大脑（核心价值高地）
- **目标**：报单流程的价值核心——三因子（指标达成 + 返利 + 库存）合成"建议最终订单"。
- **动作**：建审核大脑页 + 聚合逻辑（优先纯前端组合现有接口；如需再加 `/api/review-brain/advise`）。录入指标/返利规则复用现有表。
- **铁律**：AI 只建议，绝不擅自向厂家下单；定稿必须人点确认。

### P2 — 报单流水线编排
- **目标**：把"预报→审核→余额→付款→下单→舟谱"串成留痕流水线。
- **动作**：预报多人填报汇总（企微/表单）→ 审核建议 → 文员填余额、系统算差额、企微群通知付款 → 到账后生成「厂家下单模板 / 舟谱导入 CSV」（确定性、可复现）→ 人粘贴执行。

### P3 — 数据接入与触达
- **目标**：数据有来源、答案进微信。
- **动作**：舟谱导出 Excel 喂入（最务实数据源）；企微/微信群推送盈亏/临期/回款/待审。
- **外部系统（蒙牛/舟谱）维持"模板+人工粘贴"**，不承诺自动直连——等对方开 API 再升级，否则长期维持。

### 优先级排序
**P0 > P1 > P2 > P3**。P0 立刻可做且风险最低；P1 是差异化卖点需重点投入；P2/P3 依赖前序且涉及外部，后置。

---

## 6. 风险与边界（整合时必须守住）
1. **AI 不下单**：所有定稿动作留人确认按钮（呼应报单流程原则）。
2. **外部系统不自动直连**：蒙牛/舟谱无 API，只生成模板，避免合规与稳定性雷区。
3. **引擎只读复用、不重写**：沿用现有事务/数据访问约定，不在整合中重构底层。
4. **可回退**：导航重构用注释/配置切换，不删现有引擎代码，团队到位可一键恢复进销存。
5. **版本 pin 住 Hermes**：不追其双周大版本，避免非技术者维护断裂。

---

## 7. 待你确认的问题
1. P0 导航重构后，"财务/人事/工具"降级为"系统（高级）"是否接受？还是你希望彻底隐藏（仅 admin 可见）？
2. 智能审核大脑的"建议订单"合成，优先纯前端组合（不加后端）还是允许新增一个聚合接口？（建议先纯前端，最低风险）
3. 报单流水的"预报填报"入口，先用现有 Web 表单，还是优先接企微智能表（你当前实际在用）？
