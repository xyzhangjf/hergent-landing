# v136 配对审批链路修正 — 交付说明

> 日期：2026-09-11 ｜ 后端 `1e546c5`（hergent-erp，`origin/upgrade/v84-international`）｜ 前端 `053dbe3`（hergent-cn-v2，`origin/main`）
> 已部署：后端 `/opt/hergent-erp/hermes_tenants.py`（重启已验证 health 200）；前端 `ConnectCenter-DvYuxOnI.js` / `ConnectCenter-CWyepDS-.css` 落 `/opt/hergent-cn-v2/assets/`

---

## 一、先回答你的原问题：新用户收到那条信息后该怎么做？

企微那边回给你的那句话是 Hermes 的**配对引导**，它在说："我还不知道你是谁，让你老板批一下。"

| 谁 | 做什么 |
|---|---|
| **新同事**（收到 `PAVGNRZ2` 的人） | 把那 8 位码**发给你**（微信/口头都行），然后等 |
| **你**（管理员） | 打开 Hergent → **能力中心 → 「谁能跟 AI 对话」** → 在对应那一行**填他给你的 8 位码** → 点「批准」 |
| 批准之后 | 他再给机器人发一句话就能正常对话了（不用重新配对） |

**重点**：那句 `Ask the bot owner to run: hermes pairing approve wecom PAVGNRZ2` 是 Hermes 给"能上服务器敲命令行的人"看的。**你不需要上服务器** —— Hergent 里那个卡片就是这条命令的图形版。

---

## 二、核查中发现三处硬伤（都已修）

### 🔴 1. 配对目录读错了 —— 后端列表恒为空

Hermes v0.19+ 把配对数据放在 `<HERMES_HOME>/platforms/pairing/`（`PAIRING_DIR = get_hermes_dir("platforms/pairing", "pairing")`，新目录权威，旧目录 `home/pairing/` 是历史遗留）。

旧实现**只读旧目录** → 真实情况是：**你在手机那边等着审批，后台「谁能跟 AI 对话」却一直显示"暂无待审批请求"**。

更糟的是 `ensure_home()` 每次初始租户都建一个空的旧目录，排查时只会更迷惑。

→ 修：两个目录都读、新目录覆盖旧的；`ensure_home` 改建权威目录。

### 🔴 2. Hermes CLI 任何情况都 exit 0 —— 错码会被当成"已批准"

`hermes_cli/pairing.py` 的 `_cmd_approve` 对**成功 / 错码 / 渠道锁定**三种情况都只 `print` 不同文案，**从不 `sys.exit(非0)`**。所以退出码永远是 0。

旧实现 `if rc != 0: 报错` → **永远走不到报错分支**，填错码也会回你"已批准，该用户现在可以跟 AI 对话了"。（这是最坏的一类假成功：你以为批好了，对方却还是没人理。）

→ 修：改为解析 stdout —— `Approved!` / `not found or expired` / `locked out` / `Revoked access`。

### 🔴 3. 前端拿到的"配对码"是假的 —— 还会把整个渠道锁死

待审数据里存的 `code` 其实是 **entry_id**（`secrets.token_hex(8)`，用来当 key 的随机串），真正的配对码**只存加盐 SHA-256**，明文仅出现在对方收到的那条机器人回复里 —— 服务端**根本无法反查**。

旧实现把这个 entry_id 当配对码显示并提交 → 点「批准」必然失败；**而 Hermes 每失败一次就记一次 `_failures`，连错 5 次触发渠道级锁定 1 小时**（`_lockout:wecom`），锁定期间连新码都不再签发 —— 也就是说"多点几次"会把**所有人**都锁在门外。

→ 修：待审条目不再返回任何码，只给"谁在等 + 码还剩几分钟"；配对码由你**向对方索要后手填**。

---

## 三、改完之后长什么样

「能力中心 → 谁能跟 AI 对话」现在是：

```
谁能跟 AI 对话   新同事在聊天软件里给机器人发条消息 → 机器人回他一个 8 位配对码 → 他把码发给你，你填在这里批准   [刷新]
─────────────────────────────────────────────────────────────────────────────
企业微信   新来的业务员    码还剩 59 分钟有效    [填对方收到的 8 位码]  [批准]
─────────────────────────────────────────────────────────────────────────────
[也可以直接粘贴配对码，如 A1B2C3D4]                    [企业微信 ▾]  [批准]     ← 兜底
─────────────────────────────────────────────────────────────────────────────
企业微信   验证用同事                                              已授权  [撤销]
```

细节：
- **行内输入框**：粘 8 位码 → 点「批准」。空着时「批准」是禁用的。
- **码过期**：该行输入框禁用并提示"码已过期，让对方重新发一条消息"。
- **渠道被安全锁定**（连错 5 次）：卡片顶部出橙色横幅，写明还剩几分钟、原因是连续填错 —— 不再让你对着"暂无请求"干瞪眼。
- **手填兜底**：万一待审条目已被网关清理（过期/重启），还能直接粘码 + 选渠道批准。渠道默认落在**你已配置的那个**（不再默认飞书）。

---

## 四、验证记录

| 层 | 验证 | 结果 |
|---|---|---|
| 后端函数 | `runuser -u hergent` 跑端到端自检（带断言） | **E2E_ALL_PASS** |
| ｜ | 错码 → `ok=False`；且**不进** approved | ✅（修复前是 `ok=True`） |
| ｜ | 真码 → `ok=True` 进 approved；revoke 后 approved 为空 | ✅ |
| HTTP API | 错码 | **400** +「配对码不正确或已过期…」 |
| ｜ | 空码 | 400 +「channel / code 不能为空」 |
| ｜ | `GET /pairings` 返回字段 | 只有 `user_name / expires_in_minutes / expired / locked_out` —— **不含码** |
| 真机 UI | 卡片渲染（`mptestsp` 登录，`#/connect`） | 待审行 + 手填兜底 + 已授权行齐全 |
| ｜ | 填**错码**点批准 | 红 toast 报错、**待审行仍在**（不再假成功） |
| ｜ | 填**真码**点批准 | 行消失 + 进已授权「验证用同事 / 已授权」 |
| ｜ | 点「撤销」 | 已授权 1 → 0 |
| ｜ | 「批准」空码禁用 / 渠道默认 `wecom` | ✅ |

截图：`outputs/v136-配对审批-真机截图-20260911.png`
自检脚本：`outputs/v136-配对端到端自检脚本.py`、`outputs/v136-配对自检清理脚本.py`

### 环境还原（已核对）
- 自检数据（`__ui_check__` / `__ui_check2__` / `__p_pairtest__`）**全部清除**，`platforms/pairing/` 现为 `{}` / `[]`。
- `_rate_limits.json` 的 `_failures:wecom` **已复位**（测试中累计到 3，再错 2 次就会锁死你的真实企微渠道）。
- 为做真机验证临时给 `supervisor` 加的 `chat` 权限**已还原**（主库 `erp.db` 与遗留表 `tenant_1.db` 双处均回到 `["data","dashboard"]`，复核 supervisor 访问配对接口重新变回 403）。

---

## 五、需要你知道 / 待办

1. ⚠️ **你之前那条待审（`PAVGNRZ2`）已经过期了** —— 它 09-10 20:22 发起，Hermes 的配对码有效期是 1 小时，已被自动清理。要做一次真实配对，**让对方重新给机器人发一条消息**取新码即可。
2. 现在的配对审批是**手动**的：对方把码发给你、你填进去。这是 Hermes 的设计（码只存哈希，服务端不可能代查）。如果你希望"对方发来就能自动批准"，那需要放宽策略（等于放弃这道人工闸门），不建议。
3. **配对只管"入口准入"，不管"角色"**：被批准的人在手机上跟的是通用智能体，不带 Hergent 的具体角色 —— 这是之前聊过的"入站无角色维度"那个缺口，属另一件事。
4. 后端仓库当前工作区还有其它未提交改动（`ai_insight_llm.py` / `ai_roles.py` / `hermes_core.py` / `forecast.py` / `server.py` / `static/app.js` 等），**不是本次改动**，我没有一并提交 —— 如果是你另一条工作流在写，请留意别丢。
