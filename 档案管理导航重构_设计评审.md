# 档案管理导航重构 — 设计评审与优化建议

> 评估对象：新增「档案管理」入口（含员工档案 / 客户档案），支持手动录入 + Excel 导入 + 连接器同步（畅捷通/金蝶）。
> 评估方式：基于现有代码核对（非凭空设计）。所有结论标注了真实文件/接口依据。
> 结论先行：**方向正确，但方案低估了两块工作量（客户档案是全新模块、员工同步后端尚未支撑），且"同步"应复用已有连接器通道而非新建。**

---

## 0. 现状核对（代码事实，避免拍脑袋）

| 维度 | 现状 | 依据 |
|------|------|------|
| 侧边栏「数据补录」 | **当前侧边栏并无独立「数据补录」入口**；它仅藏在「设置 → 去补录数据」按钮，跳转 `/data-fill` | `Shell.vue:34-49` 仅含 经营/数据/能力/其他 分组；`router/index.js:44` 有 `data-fill` 路由但非主导航 |
| 员工档案 | 已存在：`DataFill.vue` 的「员工档案」Tab + `employeeApi`(`/api/employees` 增改查)，含开小程序账号、分配门店 | `DataFill.vue:11,20,190`、`server.py:4469-4488`、`erp_db.py:4813 employee_create` |
| 客户档案 | **无专属管理页**；数据在 `contacts` 表（`type='customer'`），有 `contact_create/list/update`，且门店分配已用该表 | `erp_db.py:607` 建表、`db/queries/contacts.py`、`forecast_submissions.py:449 all_stores` |
| 连接器同步 | 能力中心已有 畅捷通/金蝶 的「连接 + 同步」，**同步拉取对象 = customers/products/suppliers（及金蝶的 inventory/orders）** | `ConnectCenter.vue:941,1015` 调 `/api/datasources/v2/{chanjet,kingdee}/sync`；`datasources_v2.py:117,197` |
| 连接状态判定 | 前端已用 `/api/datasources/v2/{chanjet,kingdee}/status` 返回 `{connected}` 判定，并缓存 localStorage | `ConnectCenter.vue:899-904,973-978` |
| ⚠️ 员工同步 | **后端连接器同步对象里不含 `employees`** —— 员工目前无法从畅捷通/金蝶同步进来 | `datasources_v2.py` 两处 `objects` 列表均无 employees |
| ⚠️ 员工 Excel 导入 | 无自助 `employees` 导入 category；当前是"整理 Excel 发 AI 导入"的人工流程 | `import_router.py` 仅有 products/contacts/inventory/receivables/sale_orders 等分支，无 employees |

---

## 1. 方案合理性评估

**总体：方向正确。** 把零散的"补录"收敛为「档案管理」一级入口，契合"Hergent 只做 AI 档案/分析层、不造交易系统"的战略，也和"数据经连接器或 Excel 摄入"的既定路线一致。

**需要修正/补强的 4 点：**

1. **客户档案是全新模块，被低估。** 员工档案可"平移"自 `DataFill.vue`，但客户档案没有任何前端管理页（只有底层 `contacts` 表与门店分配逻辑）。需新建列表/新增/编辑/导入全套 UI。
2. **"同步"语义必须明确 = 复用现有连接器，而非新开通道。** 能力中心已经能连畅捷通/金蝶并同步客户/商品。档案管理页的「同步」按钮应直接复用 `/api/datasources/v2/{chanjet,kingdee}/sync`，只是把同步范围收敛到"档案对象"，不要另写一套同步逻辑。
3. **员工同步后端尚不支持。** 用户设想"连接后点同步拉外部档案"，但员工档案目前拉不进来。需后端给连接器补 `employees` 同步对象 + `map_employee`，否则员工页的「同步」是空洞的。短期可先让客户档案同步跑通、员工页同步置为"敬请期待"。
4. **"移除数据补录"要小心。** `DataFill.vue` 里除了员工档案，还有库存效期补录等（货损/工资工作流依赖）。应"改名下沉为二级（如『其他补录』）"，而非整页删除，否则会破坏货损/工资的数据入口。

---

## 2. 模块结构优化建议

**侧边栏（归入现有「数据」分组）：**
```
数据
 ├─ 数据看板
 └─ 档案管理        ← 新增一级入口
      ├─ 员工档案   (/archive/employees)
      └─ 客户档案   (/archive/customers)
```
> 分组逻辑：档案是"基础数据"，放在「数据」下比放在「经营」更贴切；与「能力中心」（连接）形成"连接→档案"的因果关联。

**页面拆分（推荐子路由，而非 Tab 堆叠）：**
- `/archive/employees`：平移 `DataFill.vue` 的「员工档案」Tab（保留开账号/门店分配），后端 `hr_employees` 不变。
- `/archive/customers`：**新建**，基于 `contacts(type='customer')`，提供列表/搜索/新增/编辑 + 导入 + 同步。
- 原 `/data-fill` 保留，改名为「其他补录」（库存效期等），从设置入口可见，避免破坏既有工作流。

**复用组件（避免重复造轮子）：**
- `ArchiveList`：通用表格（搜索 + 分页 + 行操作），员工/客户共用。
- `ImportModal`：复用 `importApi`（`smartParse`/`oneShot`/`template`）。客户用现有 `category='contacts'`；员工需后端新增 `employees` category。
- `SyncButton`：统一封装"探测连接状态 → 触发同步 → 状态反馈"。

**数据归属：** 员工 = `hr_employees`；客户 = `contacts(type='customer')`。两者共用"手动录入 + Excel 导入 + 连接器同步"三件套，结构对称、易维护。

---

## 3. 交互流程优化

**档案管理页统一操作栏：**
```
[+ 新增]  [📥 Excel 导入]  [🔄 同步]   搜索框……
```

**「同步」按钮状态机（进入页面即并行探测）：**
1. 并行调 `chanjet/status` + `kingdee/status`。
2. **两者都未连接** → 按钮置灰并显示"未连接"角标；点击弹「未连接提醒」Modal（说明"请先到 能力中心 连接畅捷通/金蝶"，附一键跳转）。
3. **至少一个已连接** → 按钮可用；点击触发对应连接器同步，范围限定档案对象（`{customers}` 必含；`{employees}` 后端就绪后加入）。
4. **同步中** → 按钮转圈 + 禁用，防重复提交。

**员工档案同步的过渡方案（因后端暂不支持）：**
- 短期：员工页「同步」点击后提示"员工档案同步即将上线，当前请手动录入或用 Excel 导入"；或该按钮在员工页直接隐藏、仅客户页显示。
- 长期：后端补 `employees` 同步对象后，两页同步行为一致。

**Excel 导入：**
- 客户：`category='contacts'`（已支持，含表头智能识别与舟谱映射）。
- 员工：需后端新增 `employees` 导入 category（建议补，达到与客户的体验对称）。

---

## 4. 异常处理优化

| 场景 | 建议处理 |
|------|----------|
| 连接状态探测失败（/status 500/超时） | 不要直接判"未连接"阻断，应显示"状态未知，点此重试"，避免误伤已连接用户 |
| 同步中重复点击 | 按钮 `loading` + `disabled` 防并发；同步接口本身建议幂等（按 `source_id` upsert） |
| 同步部分失败（如拉到客户拉不到员工） | 返回结构化结果 `{imported, updated, skipped, failed:[{type,reason}]}`，前端逐条展示，**不因单条失败整体回滚** |
| 导入重名冲突 | 复用现有 `check_quick_duplicate` + 冲突预览，让用户选 覆盖/跳过/合并（`contacts` 已有 `merge_contacts`） |
| 权限不足 | 非 boss/admin 隐藏「同步」「导入」「删除」等危险操作（后端 `_admin` 已校验，前端再做视觉收敛） |
| 空数据首访 | 显示三入口引导："连接 ERP 一键同步 / 手动新增 / 下载模板导入"，降低新用户茫然感 |

---

## 5. 同步状态反馈优化（重点）

**五态可视化（按钮 + 列表角标）：**
- 灰「未连接」→ 蓝「可同步」→ 转圈「同步中」→ 绿「最近同步成功 · 时间」→ 红「同步失败」。

**同步结果摘要（toast + 详情弹层）：**
> "畅捷通同步完成：新增客户 12、更新 3；员工 0（该来源暂不支持）"

**行级溯源标记：** 每条档案记录带 `source` 字段（`chanjet` / `kingdee` / `manual` / `excel`）+ `last_sync_at`，便于回溯"这条是谁来的、何时同步的"。`contacts` 表已有 `source`/`source_id` 列可直接复用；`hr_employees` 建议补同结构列。

**失败可定位：** 同步失败不全页报错，而是列出失败项（如"客户『XX超市』缺少必填税号，已跳过"），用户可补字段后重同步。

---

## 6. 实施优先级与风险清单

| 优先级 | 任务 | 工作量 | 风险 |
|--------|------|--------|------|
| **P0** | 侧边栏新增「档案管理」，员工档案从 `DataFill` 平移为 `/archive/employees` | 低（前端重组） | 低 |
| **P0** | 新建 `/archive/customers`（contacts CRUD 前端 + 复用导入） | 中 | 中（新模块） |
| **P1** | `SyncButton` 复用 `chanjet/kingdee/status`+`/sync`，收敛同步范围 | 中 | 低 |
| **P1** | 后端给连接器补 `employees` 同步对象 + `map_employee`（否则员工同步空洞） | 中 | 中（需对接 ERP 员工字段） |
| **P2** | 后端新增 `employees` Excel 导入 category，与 `contacts` 对称 | 低 | 低 |
| **P2** | `hr_employees` 补 `source`/`last_sync_at` 列，支撑行级溯源 | 低 | 低 |

**验收建议：** 硬刷新 + 真机 e2e 覆盖四态——①未连接点击弹提醒；②已连接点击跑通同步且列表出现 `source` 标记；③同步中防重复点击；④导入重名走冲突预览。

---

## 7. 给决策者的两条关键提醒

1. **不要新建第二套同步通道。** 能力中心已有畅捷通/金蝶的完整连接与同步能力，档案管理页只做"触发 + 范围收敛 + 状态展示"，复用 `/api/datasources/v2/{chanjet,kingdee}/sync` 即可。
2. **"移除数据补录"实为"改名下沉"。** 侧边栏本无独立「数据补录」项；真正的 `DataFill.vue` 还承载库存效期等补录（货损/工资依赖），应改名为「其他补录」保留，而非整页删除。

---

## 实施状态（2026-08-26 · P0 已落地）

用户认可评审后，已按 P0 完成并生产验证通过：

- **侧边栏**：新增可展开「档案管理」分组，含「员工档案」「客户档案」两个子路由（移动端同步）。
- **员工档案页**（`EmployeeArchive.vue`）：平移自 DataFill 的员工档案（加员工 / 补录底薪 / 开小程序账号 / 分配门店），并补上用户要求的 **Excel 批量导入**（后端新增 `employees` 导入分类，复用 preview/execute/去重管线）。
- **同步按钮**：probe 畅捷通/金蝶 `/status`；未连接 → 弹「尚未连接 ERP」提醒（引导去能力中心）；已连接 → 调 `/sync` 拉取外部档案。员工 API 同步后端未支撑，已在页面标注 P1。
- **客户档案页**（`CustomerArchive.vue`）：P1 占位，含同步按钮（已连接可真实拉客户）+ 未连接提醒，列出 P1 规划能力，无死链。
- **DataFill 收敛**：移除员工 Tab，改名为「库存效期补录」（货损/工资依赖的库存导入保留），不再作为独立"数据补录"入口。
- **验证**：puppeteer 真机全绿——导航、同步提醒、员工 Excel 导入（2 行→成功 2 条、底薪解析正确）均通过，无页面报错；后端 `bash deploy.sh` health=200。

**P1 待办**：①客户档案完整管理页（新增/编辑/导入 UI）；②员工 API 同步（连接器补 `map_employee` + `employees` 同步对象）。
