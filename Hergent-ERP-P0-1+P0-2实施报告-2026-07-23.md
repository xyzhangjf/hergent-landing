# Hergent Web ERP — P0-1 + P0-2 Fail-Closed 实施报告

> 实施日期：2026-07-23 ｜ 实施人：Senior Developer（高级开发工程师）
> 对象：生产运行代码（分支 `upgrade/v84-international`，提交 `f98bcc5`）
> 关联评估：[Hergent-ERP-fail-closed安全评估报告-2026-07-23.md](./Hergent-ERP-fail-closed安全评估报告-2026-07-23.md)

---

## 一、实施内容

### P0-1：全局认证中间件（认证 fail-closed）

在 `server.py` 新增 `auth_required_middleware`：所有非公开 `/api` 路径，若 `_get_user()` 返回 `None`（未登录/无有效令牌），一律 **401** 并带 `error_code: UNAUTHENTICATED`，关闭"端点忘记调 `_auth`"的 fail-open 缺口。

- 复用既有 `_PUBLIC_PATHS`（登录、健康检查、webhook、bot、portal、版本信息等）作为豁免清单，这些路径仍由其端点自身守卫。
- 与 tenant 中间件已有的"未登录放行、交由认证中间件 401"设计意图一致（tenant 中间件注释原本就假设存在全局 auth 中间件）。
- 已核实前端登录流只匿名调用 `/api/auth/login`（公开）与（登录后）`/api/auth/me`，无登录前匿名访问受保护路径，故 P0-1 不会误杀登录页。

### P0-2：RBAC 默认拒绝 + 补全模块映射（权限 fail-closed）

原 `_PATH_MODULE_MAP` 默认模块为 `"dashboard"`（**每个角色都持有**），导致任意未映射路由对全体已登录角色开放 = 越权。改造两点：

1. **补全模块映射**：原映射仅 79 键，覆盖 597 个路由后出现 **225 条非公开未映射路由**（含专属价、对账单、发票、审批流、BI、导入等老板必用功能）。若直接"默认拒绝"会把这 225 条全 403 掉、**直接打挂 ERP**。因此按路由顶级段自动推导模块，补全至 **188 键**，覆盖全部 597 路由。所有分配的模块均为老板（boss）已持有的模块（data/accounts/sales/stock/reports/hr/crm/buying/dashboard），**保证老板永不被锁**。
2. **默认拒绝**：未映射路径的默认模块由 `"dashboard"` 改为 `None → 403`（`error_code: MODULE_NOT_CONFIGURED`）。补全映射后，该分支**仅对真正未知路径触发**（当前注册路由为零），不会误杀任何合法路由。

> 报告原文也写明 P0-2 "需补全合法模块的映射，避免误杀正常功能"——补全这 225 条映射即 P0-2 的真正工作量。

---

## 二、程序化验证（部署前）

重跑路由覆盖审计（扫描全部 597 路由 vs 租户白名单 / RBAC 映射 / 公开前缀）：

```
ROUTE_COUNT: 597
RBAC_KEYS:   188   (原 79 → 补全后)
[B] 租户内但 RBAC 未映射:        0   ← 37 条越权路由已闭合
[UNMAPPED_ALL] 非公开未映射:     0   ← 默认拒绝不会误杀任何合法路由
```

---

## 三、生产实弹冒烟（部署后，commit f98bcc5）

| 测试 | 期望 | 实测 | 结论 |
|------|------|------|------|
| 匿名 `GET /api/dashboard` | 401 | 401 `UNAUTHENTICATED` | ✅ P0-1 网关生效 |
| 匿名 `GET /api/versions` | 200（公开） | 200 | ✅ 公开豁免正确 |
| 登录 `POST /api/auth/login`（错密码） | 到达端点（非网关 401） | 401 `用户名或密码错误` | ✅ 登录流未被误杀 |
| boss `GET /api/print/templates` | 200 | 200（返回模板列表） | ✅ 老板永不被锁 |
| sales `GET /api/print/templates` | 200 | 200 | ✅ 业务员域正常 |
| **sales `GET /api/salary-send/history`** | 403 | **403 `不能访问'hr'模块`** | ✅✅✅ **P0-2 越权封堵生效** |
| boss `GET /api/salary-send/history` | ≠403（rbac 放行） | 500（端点自身 bug，见下） | ✅ rbac 未误锁老板 |
| sales `GET /api/customer-prices` | 200（sales 域） | 200 | ✅ 专属价域正常 |

**核心结论**：P0-1 关闭了匿名访问 fail-open 缺口；P0-2 封堵了"任意已登录角色可越权访问 hr/salary-send/print 等"的缺口（sales 访问发工资已被 403），同时老板与业务员的核心业务域畅通。

---

## 四、残留风险与注意（务必阅读）

1. **⚠️ 非特权角色可能被新收紧（预期内，但需确认）**
   P0-2 把原本对全员开放的 225 条路由按模块收紧。若某角色（sales/driver/accountant）原本靠"dashboard 默认"能访问某模块（如 sales 访问 `bi/*`→reports、访问 `hr/*`/`accounts/*`），现在会 **403**。这是安全收紧的正确行为，但可能与其实际工作流冲突（例如业务员看经营看板若走 `/api/bi/*` 现会被拦）。
   **处置**：如某角色确实需要某模块，只需在 `core.py` 的 `_DEFAULT_PERMS` 给该角色加上对应模块即可（一行），不影响其他角色。请告知哪些角色需要哪些模块，我即时调。

2. **salary-send 端点 500 是既有 bug，与本次无关**
   boss 访问 `/api/salary-send/history` 返回 500：`module 'erp_db' has no attribute 'salary_send_log_list'`。这是 `erp_db.py` 缺失该函数导致的既有缺陷，rbac 已正确放行老板。建议另行修复（不在 P0 范围）。

3. **core.py 的 H1b 改造仍留在本地工作树，未部署**
   本地 `server/core.py` 有一批未提交的 H1b 统一错误信封改造（异常处理器、422 校验、trace-id）。本次为控制部署范围、避免范围蔓延，**未将其纳入本次部署**（生产仍跑旧 core.py）。我的 P0 中间件用内联 `JSONResponse`，不依赖 H1b，配旧 core.py 完全兼容。H1b 需在其自身验证通过后单独部署。

4. **部署备份一致性**
   本次部署前备份出现 `tar: .: file changed as we read it` 警告（活库备份非完全一致快照）。本次只改代码、未改数据，回滚靠 git 历史即可；B1+B2 时期的旧备份 `/tmp/hergent-backup-20260723-164859.tar.gz` 仍可作兜底。

---

## 五、部署方式（已固化为标准流程）

`git archive HEAD server/`（仅代码）→ `scp` → `tar --strip-components=1` 解到 `/tmp/deploy_extract` → 删 `static` 符号链接 → `cp -rf` overlay 到 `/opt/hergent-erp/` → 清 `.pyc` → 改属主 `hergent:hergent` → `systemctl restart hergent-erp`。静态文件（`static/`）本次未变动，保持原样不动。

---

## 六、下一步建议

- 确认各角色（sales/driver/accountant）需要的模块，微调 `_DEFAULT_PERMS`（如有 403 误伤）。
- 修复 `erp_db.salary_send_log_list` 缺失（salary-send 功能 bug）。
- 单独验证并部署 core.py H1b 统一错误信封。
- P0-3（租户隔离默认拒绝，修 235 条 A 类漏隔离）建议在测试环境先行，再上生产。
