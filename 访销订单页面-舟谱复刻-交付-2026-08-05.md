# 访销订单页面（舟谱风格复刻）交付 — v107.47

## 一、对应截图
portal.zhoupudata.com 商家版「创建访销订单」页面，复刻其紧凑布局 + 12 行商品明细表 + 实时金额计算。

## 二、页面结构（自上而下）

```
┌──────────────────────────────────────────────────────────────┐
│ 🗒 创建访销订单            [已可保存] [✓ 创建] [上传附件]      │  ← 顶部 brand-color header
├──────────────────────────────────────────────────────────────┤
│  ┌─ 订单信息区（双列网格，背景色块）─────────────────────────┐ │
│  │ *客户(searchable)    | *业务员(下拉)                      │ │
│  │ *部门(searchable)    | *销售仓库(searchable)               │ │
│  │ 单据日期             | 预提货日期                          │ │
│  │ 送货车辆             | 收货地址                            │ │
│  │ 付款方式             | 是否加急                            │ │
│  │ 欢数(系统) ¥0.00     | 营销类型                            │ │
│  │ 备注 0/500 (跨两列)                                       │ │
│  │ □改订单金额时变金额  □按小单位金额                       │ │
│  └────────────────────────────────────────────────────────┘ │
│  ┌─ 商品明细表（12 行预置，奇行淡蓝底）──────────────────────┐ │
│  │ ☐ + 商品名称   营销类型 条形码 单位 销售价 实际价 数量  │ │
│  │          订单数量(盒份) 订单金额 销售仓库 备注  操作    │ │
│  │ 1 □▼ (搜..)  [-]     abc    件   5.00    5.00   10     │ │
│  │   10       ¥50.00     [仓]   ...     新增 删除           │ │
│  │ 2 ...                                                     │ │
│  │ ...                                                       │ │
│  └────────────────────────────────────────────────────────┘ │
├──────────────────────────────────────────────────────────────┤
│ 3 项明细 · ¥150.00                       [保存] [✓ 提交并审核] │  ← 底部固定操作栏
└──────────────────────────────────────────────────────────────┘
```

## 三、表单字段清单（按截图识别）

| 字段 | 类型 | 必填 | 备注 |
|---|---|---|---|
| 客户 | searchable 选择 | ✅ | 头像/搜索图标 |
| 业务员 | 下拉 | ✅ |  |
| 部门 | searchable 选择 | ✅ | 演示版内置 3 项 |
| 销售仓库 | searchable 选择 | ✅ |  |
| 单据日期 | date |  | 默认今天 |
| 预提货日期 | date |  | 默认今天 |
| 送货车辆 | searchable |  | 演示版空列表 |
| 收货地址 | searchable |  | 演示版空列表 |
| 付款方式 | 下拉（现结/月结/账期/预付） |  |  |
| 是否加急 | 下拉（否/是） |  |  |
| 欢数 | 计算字段 |  | 实时显示总金额 |
| 营销类型 | 下拉 |  |  |
| 备注 | textarea |  | 0/500 字符 |

**商品明细列（与截图一致）**：商品名称*、营销类型、条形码、单位、*销售价(折扣价)、实际价、*订单数量、订单数量(盒份)、订单金额（自动）、销售仓库、备注、操作（新增/删除）

## 四、核心交互

- **数据获取**：客户/员工/仓库/商品 4 接口并行加载（`/contacts?type=customer`、`/employees`、`/warehouses`、`/products`）
- **金额实时计算**：每行输入监听 `oninput/onchange` → `_vsfRecalc()` 重算每行小计 + 底部总金额
- **选商品自动填充**：监听商品 hidden value 变化 → 取列表缓存或 `/products/{id}/detail` → 回填条形码/单位/销售价/实际价
- **行增删**：每行"新增"按钮调 `_vsfAddRow()`；"删除"调 `_vsfDelRow()`（清理 watcher + 重新编号）
- **校验**：客户 + 业务员 + 仓库必填；至少 1 项商品（订单数量>0）

## 五、提交与持久化

- **HTTP**：`POST /api/sale-orders`（已存在）
- **Body**：`{customer_id, items:[{product_id, product_name, quantity, unit_price, amount, barcode, unit}], warehouse_id, operator_id, note, status: 'draft'}`
- **访销特有字段**（加急/付款/货车/地址）：写入 `note` 字段（演示版），后端未持久化独立列
- **提交后**：toast 反馈 → 关闭 modal → 自动刷新销售列表

## 六、入口

- **侧边栏 mega 菜单** → `访销订单` → `showVisitSaleForm()`（替代原 5 tab 分页的 `showSaleForm`）
- 其他"销售开单"入口（+ 销售开单菜单项）仍走原通用流程

## 七、视觉风格

- **统一令牌**：品牌色 `var(--p)` + 成功 `var(--suc)` + 危险 `var(--dan)` + 表面 `var(--bg/bg2/bg3)` + 文字 `var(--t1/t2/t3)` + 边框 `var(--bd)`
- **斑马纹**：商品表奇行 `rgba(var(--p-rgb),.04)` 浅蓝底（如舟谱）
- **圆角**：字段 6px、按钮 8px、modal 10px
- **间距**：信息区 14×16px、字段间距 10×24px、表行 4×6px
- **字体**：表 12px、字段 13px、标题 16px（与全站一致）
- **双主题**：自动适配浅色/深色

## 八、替换/兼容

- **不破坏**：原 `showSaleForm()`（5 tab 分页销售单）保留；其他 +销售开单入口仍可用
- **仅替换**：mega 菜单的"访销订单"入口

## 九、验证

- node --check ✓ / 17 处引用确认 ✓
- 构建 `main-hegqKNgR.js` 1458KB / gzip 358KB ✓
- 部署上线、线上 200 ✓

## 十、无法识别 / 演示说明

- **"欢数"字段**：截图字面是"欢数￥0.00"，UI 严格按"欢数"显示（不能猜"含税合计"等含义），数据按实际价 × 数量合计
- **"是否加急/送货车辆/收货地址"**：后端无独立列，本版写入 `note` 字段以"健=值"形式持久化；后续可扩展后端 schema 加专用字段
- **"部门"选项**：演示版内置 3 项（销售部/市场部/运营部），实际部门接口如存在可替换
- **"快捷键说明"按钮**：演示版 toast 提示，无具体快捷键
- **"上传附件"按钮**：演示版 toast 提示，未实现文件上传
- **舟谱"改订单金额时变金额"/"按小单位金额"**：以 checkbox 形式存在，逻辑 hooks 预留（本版未实现联动）
- **导入/导出、批量操作**：截图未展示，本版未实现
