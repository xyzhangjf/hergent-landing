# 预报订单小程序 · 真机 E2E 测试清单

> 用途：小程序上传发布后，在**真机（iOS / Android 各一台）+ 微信体验版**逐条执行。
> 所有用例基于已落地代码（`forecast-order-miniprogram-20260812T023419087Z/`）。
> 前置：A1 真实 appid 已替换、A2 request 合法域名已配、B1 已上传发布。

---

## 一、发版前必须通过的 P0 用例

| 编号 | 用例 | 前置 | 步骤 | 预期结果 | 关联代码点 |
|---|---|---|---|---|---|
| E1 | 隐私同意拦截 | 清 Storage（`fs_privacy_agreed` 不存在） | 1. 冷启动小程序 2. 观察是否直接进入登录页 | 先弹「隐私保护指引」覆盖层；点击「同意并继续」后才展示登录表单；「暂不同意」保持弹窗、无法登录 | `login.js` showPrivacy、`login.wxml` 覆盖层 |
| E2 | 隐私标记持久化 | E1 已同意 | 杀进程重进 | 不再弹隐私层，直接登录页 | `wx.setStorageSync('fs_privacy_agreed','1')` |
| E3 | 真实账号登录 | A1/A2 完成；有可用账号 | 输入 username/password → 登录 | 成功写入 `fs_token/fs_user`，按 `fs_redirect`（无则 `/pages/fill/fill`）跳转；`wx.reportAnalytics('login')` 触发 | `login.js` login() |
| E4 | 合法域名下接口可达 | 真机联网 | 登录后进入 fill 页 | `open-periods`、`fill-search` 正常返回（非 `request:fail` 域名拦截）；若未配域名则**必定失败**，属 A2 阻塞 | `utils/api.js` request |
| E5 | 填写并提交主流程 | 已登录、有可用期次 | 1. 选门店 2. 搜商品 3. 填数量 4. 提交 | 页面**底部弹「提交结果卡」**（不是 Toast）：含**单号**、件数、门店、期次；`fs_cart` 清空；`wx.reportAnalytics('submit')` 触发 | `fill.js` submit() / `submitResult`、`fill.wxml` `.bottom-dock` |
| E6 | 我的提交-详情展开 | E5 已提交 | mine 页点一条记录 | 展开显示商品明细 + 状态徽标（待审批） | `mine.js` toggleExpand |
| E7 | 我的提交-撤回 | E6，状态为待审批 | 点「撤回」→ 二次确认 | 状态变「已撤回」；后端 `recall` 成功；汇总不再计入 | `mine.js` recall() + `POST /{sid}/recall` |
| E8 | 移动审批-通过 | 用审批角色账号登录 | approval 页→选待审→「通过并生成采购申请」 | 状态变「已通过」；`wx.reportAnalytics('approve')` | `approval.js` doApprove() |
| E9 | 移动审批-驳回 | 审批角色 | approval 页→「驳回」填原因→确认 | 状态变「已驳回」；原因入库；`reportAnalytics('reject')` | `approval.js` doReject() |
| E10 | 汇总页 + 复制 | 已有提交 | summary 页查看；点复制 | 汇总表展示；复制成功 Toast | `summary.js` copySummary |
| E11 | 登出清租户 | E3 已登录 | mine 页登出 | `fs_token/fs_user/fs_tenant_id/fs_period_id/fs_store_id/fs_cart/fs_redirect` 全部清除；`app.globalData.tenantId=''`；reLaunch 登录页 | `mine.js` logout() |
| E12 | 埋点上报 | 公众平台「自定义分析」已开启事件 | 执行 E3/E5/E8 | 后台「自定义分析」能看到 login/submit/approve 事件（未开启则静默 no-op，不报错） | `utils/track.js` |

---

## 二、边界与异常 P1 用例

| 编号 | 用例 | 步骤 | 预期 |
|---|---|---|---|
| B1 | 空购物车提交拦截 | fill 页未加任何商品直接提交 | 不发起请求，Toast「请先添加预报商品」 |
| B2 | 期次为空提示 | 后端无开放期次（或接口异常） | `loadPeriods` 失败仅告警 + 非阻断 toast「暂无可预报期次」，不闪退 |
| B3 | 网络失败 → 草稿 + 错误条（可重试） | 提交时断网 | `api.js` 判为**网络层失败** ⇒ reject 里带 `isNetwork/retryable`；**底部错误条**（不是 Toast）给出原因 + **「重试」**按钮；同时存本地草稿。恢复网络后在 fill 页 `onShow` 自动补传（不再需要手动重进） |
| B4 | 重复提交幂等（v224 起语义变了） | 同一门店+同一期次连续提交两次 | 第二次返回**「已更新这一单」**且**单号与第一次完全相同**；库里**始终只有一条**（不再新增第二条、也不换单号）。⚠️ 改前的预期「返回已存在 / 换单号」已作废 |
| B5 | 非审批角色看不到入口 | 用销售角色登录 | mine 页无「待审预报」卡片；直接访问 approval 页被后端 403 |
| B6 | 撤回后不可再撤回 | E7 后再次点撤回 | 按钮消失或提示「已撤回」 |
| B7 | 已审单不可撤回 | 审批通过的单点撤回 | 后端返回「仅待审批可撤回」错误，前端提示 |
| B8 | 登录过期跳转 | token 失效后任意请求返回 401 | 清本地会话、reLaunch 登录页，不无限报错 |

---

## 三、兼容性与体验 P2 用例

| 编号 | 用例 | 预期 |
|---|---|---|
| C1 | iOS / Android 双端 | 弹窗、列表、复制在各端表现一致 |
| C2 | 微信基础库低版本（≥2.10.0） | 无 `wx.reportAnalytics` 不可用导致的崩溃（track 已做能力检测） |
| C3 | 弱网（开发者工具 Network 限速） | 提交有 loading，超时给明确失败提示而非白屏 |
| C4 | 长商品明细 | 我的提交详情可滚动、不溢出 |

---

## 三之二、v224 专项（一店一期一单 · 期次关闭前可改）2026-09-21

> 业务规则（用户 2026-09-21 拍板）：**一家门店一个期次只能有一单，但在这期期次关闭之前都可以修改。**
> 关键三条要亲手验：① 同店同期次**恒一条**；② 覆盖时**单号不变**；③ 失败时**给得出下一步**。

| 编号 | 用例 | 步骤 | 预期结果 | 关联代码点 |
|---|---|---|---|---|
| D1 | 同店同期次恒一条 | 选门店A 报单 → 弹窗提示后**再报一次**（改数量） | 第二次弹「本期已报过，将更新这一单」，副文案明确「**单号不变，不会多出一条**」；确认后结果卡显示**同一个单号** | `fill.js` dup 弹窗、`submit()` |
| D2 | 单号稳定可对外引用 | D1 后进「我的报单」 | 同店同期次**只有一条**，且单号与结果卡一致（`mine` 列表已显示 `单号 xxx`） | `mine.wxml` `.rec-no` |
| D3 | 换人改同一店 | 甲账号报门店A → 乙账号（也负责门店A）报同一店同期次 | **仍只一条**（改前会真存两条、汇总翻倍）；乙的「我的报单」里**看得到**这一条 | 幂等键 = (store, period)；`/my` 门店范围并集 |
| D4 | 结果卡就是反馈落点 | 提交成功后**不要滚动页面** | 卡片出现在**底部同一位置**（与提交栏/错误条互斥同位置），是**弹出来的**，不需要用户去找 | `fill.wxml` `.bottom-dock` |
| D5 | 连点只发一次 | 快速连点提交 3 次 | 只发一次请求；第二次点击无任何副作用（同步锁在函数第一行置位） | `fill.js` `_submitLock` |
| D6 | 错误分「能不能重试」 | ① 把期次关闭后提交 ② 提交中断网 | ① 错误条给**原因**（「本期已定稿（关闭），不能再报单；如需改动请联系管理员重开本期」）且**不给「重试」**（重试必然再失败）；② 网络故障时**才**出现「重试」+ 存草稿 | `api.js` `httpError(statusCode/isNetwork/retryable)`、`_showSubmitError` |
| D7 | 撤回后重报要计入汇总 | 对某店报单 → 在我的报单里**撤回** → 立刻重新报同一店同期次 | 重新报单**恢复为待审批并计入汇总**（实现里必须把 `status` 复位为 `pending`；不复位会继承「已撤回」而**不计入**，但用户看到的是「提交成功」= 静默失效） | `erp_db.forecast_submission_replace` |
| D8 | 期次不存在 | 让选中的期次失效（或管理员删掉该期次）后再提交 | 提示「**所选报单期次不存在，请下拉刷新后重选**」（与「已定稿」是两句不同的话，可据此判断该做什么） | `routers/forecast_submissions.py` 期次守卫 |
| D9 | 粘贴/带入不越期 | 「一键带入上次报单」或跨期次遗留购物车里的商品，若不在本期清单 | 提交后结果里**逐项说明哪些被跳过**（不是静默丢弃） | v215 报单基准（商品清单优先） |

---

## 三之三、v227 专项（复制生成样单「照往期做本期单」）2026-09-21

> 需求原话（用户）：「报单人能否具备样单体验功能：在小程序中选择一个期次后，直接点击复制生成样单，
> 然后切换到已开放的期次。如果样单内容需要修改，支持直接改单；如果无需修改，则可直接提交。」
>
> **交互定义（三条边界，缺一条这个功能就会说谎）**：
> ① **源** = 该门店在某个**往期**报过的单（含数量），取自**服务端**（不是本机快照 —— 换手机也在）；
> ② **落点** = 当前选中的期次（提交时用的就是它）；"切到已开放的期次再提交"复用的是既有的
>    「切期次保留已填数量」行为，**不新增机制**；
> ③ **样单不落库** —— 它只是一份本地草稿（落库会造出"未开放期次的报单"，与「一店一期一单」
>    和「未在窗口内不许报」两条硬规则直接冲突）。⇒ 生成样单**只发读请求**。
>
> 前置：本机已用开发者工具打开项目；建议先在电脑端造一个 **`order_start` 在未来的 open 期次**，
> 用来验 D16（否则生产上全是"已过"的期次，三态里的「未开始」分支碰不到）。

| 编号 | 用例 | 步骤 | 预期结果 | 关联代码点 |
|---|---|---|---|---|
| D10 | 样单入口常驻可见 | 进入填报页（期次列表非空） | 期次选择器下方有一条「把往期报过的单复制成本期样单，改完直接提交 / 复制生成样单」 | `fill.wxml` `.sample-bar` |
| D11 | 选源面板列的是**该门店报过的**期次 | 点「复制生成样单」 | 弹出面板，按**由近及远**列出该门店报过的期次，每项带「N 项 M 件」；**撤回/驳回的单不出现在面板里**（拿作废数量再报一次是本用例要挡的事） | `fill.js` `genSample()` 里 `status==='recalled'/'rejected'` 的过滤 |
| D12 | 生成样单 = 数量真的落进页面 | 选一个源期次 | 商品行的数量格被填上、底部合计随之变化、出现一条「样单来自「X」· N 项 / M 件」的回执 | `fill.js` `_applySample()`、`syncCart()`、`applyQtyToRows()` |
| D13 | 覆盖必须问一句 | 先手填几项 → 再点「复制生成样单」选源 | 弹「覆盖当前已填内容？」；**点取消 ⇒ 原有内容一项不动**；点覆盖 ⇒ 才被替换 | `fill.js` `_applySample()` 的 `wx.showModal` 分支 |
| D14 | 越界预检不说假话 | ① 本期清单**还没翻完**时生成样单 ② 翻到底后再生成 | ① 显示「提交时系统会核对本期清单，不在的会自动跳过」（**不得**报出具体条数 —— 未加载完就下结论会把"还没翻到"误报成"不在清单"）；② 才显示「N 项不在本期清单，提交时会被自动跳过」 | `fill.js` `_applySample()` 的 `loadedAll` 判据 |
| D15 | 切期次后样单条要消失 | 生成样单 → 切换到期次列表里的另一期 | 样单回执条**消失**（它的越界结论是按上一期的清单算的，留着就是让页面说错话）；**已填数量仍保留**（跨期沿用是既定行为） | `fill.js` `onPeriodChange()` 里 `sampleInfo: null` |
| D16 | 期次窗口三态不能混 | 列表里同时存在「今天在窗口内」「未来才开始」「已过窗口」三种期次 | 未来期显示「**（未开始）**」+ 蓝色提示「X 才开始报单 —— 现在可以先生成样单，开放后再提交」；已过期显示「（已过窗口）」+ **红色**警示；两者**文案与颜色都不同** | `fill.js` `loadPeriods()` 的 `wstate`；`fill.wxml` `.period-warn-future` |
| D17 | 落到"已开放的期次"提交 | 在一个**未开始/已过**的期次上生成样单 → 切到**窗口内**的期次 → 直接提交（不改数量） | 提交成功；**结果卡里的期次名 = 切过去的那个**（不是做样单的那个）；若该店本期已有单，则走「更新这一单 · 单号不变」（同 D1） | `_doSubmit()` 携带的 `period_id`；结果卡 `periodName` |

> v227 专项硬标准：**D11 / D13 / D15 全 P**。
> - **D11** —— 源正确性（含"作废单不能当源"），错了会让用户把已撤回的数量再报一遍；
> - **D13** —— 一键覆盖是这个页面上最容易造成**实际数据损失**的动作，确认框少不得；
> - **D15** —— 切期次后清样单条，防的是"拿上一期的越界结论误导本期操作"。

---

## 四、执行记录表（测试时填写）

| 用例 | 设备 | 微信版本 | 结果(P/F) | 备注 |
|---|---|---|---|---|
| E1-E12 | | | | |
| B1-B8 | | | | |
| C1-C4 | | | | |
| **D1-D9（v224）** | | | | |
| **D10-D17（v227 样单）** | | | | |

> 通过标准：P0 全部 P，且 B1-B8 无 blocker，方可进入灰度。
> v224 专项另有一条硬标准：**D1/D2/D3/D7 全 P** —— 它们是「一店一期一单」的业务规则本身，
> 过了这四条才说明规则真的生效（其余是体验项）。
> v227 样单专项硬标准：**D11/D13/D15 全 P**（源正确性 · 覆盖确认 · 切期次清样单条）。
