<view class="page">
  <view class="card">
    <view class="badge">!</view>
    <view class="title">当前角色暂无报单权限</view>
    <view class="role-line">你的角色：<text class="role">{{roleText}}</text></view>
    <view class="reason">{{reason}}</view>
  </view>

  <view class="card">
    <view class="card-hd">怎么才能用？</view>
    <view class="steps">
      <view class="step"><text class="idx">1</text><text class="txt">把这句提示发给你的管理员（点下面按钮可直接复制）</text></view>
      <view class="step"><text class="idx">2</text><text class="txt">管理员在电脑上打开网页端，进入【员工档案 → 角色权限】</text></view>
      <view class="step"><text class="idx">3</text><text class="txt">给该角色开通「报单数据」，或把你的角色改为业务员 / 主管</text></view>
    </view>
    <button class="btn-ghost" bindtap="copyHint">复制提示发给管理员</button>
    <button class="btn-primary" bindtap="onRecheck" disabled="{{checking}}">
      {{checking ? '检查中…' : '我已开通，重新检查'}}
    </button>
  </view>

  <view class="tip">
    如果管理员确认已开通但这里仍然进不去，请把本页截图发给他 —— 那说明权限配置本身有问题。
  </view>

  <view class="ft">
    <button class="btn-quit" bindtap="onLogout">退出登录</button>
  </view>
</view>
