# 微信小程序注册信息 · 一键复制粘贴包

> 用途：注册小程序时逐个字段复制，不用再翻证照。
> 主体：**湖北省小赫智体数字科技有限公司** ｜ 法人：张俊峰（本人）
> ⚠️ 本文件含生产测试账号密码明文，**属本地文档，勿外传**。

---

## 0. 注册入口与流程（先读这一段）

1. 打开 <https://mp.weixin.qq.com> → 右上角「立即注册」→ 选「小程序」
2. 填**未注册过微信公众平台**的邮箱 → 邮箱激活 → 选主体类型「**企业**」
3. 进入「**主体验证**」→ 选「**法定代表人验证**」→ **你自己的微信扫码**（你就是法人）
4. 验证通过 → 填写下面的主体信息 → 提交
5. 1-3 个工作日后拿到 **AppID**

> 邮箱必须全新：已绑过公众号/小程序的邮箱无法再用。建议专门注册一个。
> 主体验证别选「对公打款」（要 3-5 天且需对公户），法人扫码最快。

---

## 1. 主体信息（注册页要填）— 逐个复制

| 字段 | 填写内容 |
|---|---|
| 主体类型 | 企业 |
| 企业名称 | `湖北省小赫智体数字科技有限公司` |
| 统一社会信用代码 | `91420606MAKF1YPG5Y` |
| 法定代表人姓名 | `张俊峰` |
| 法定代表人身份证号 | （你本人身份证，执照上不显示，需自行填写） |
| 注册资本 | `50` 万元 |
| 企业成立日期 | `2026-05-26` |
| 营业期限 | 长期 |
| 登记机关所在地 | 湖北省襄阳市 |
| 企业住所 | `湖北省襄阳市樊城区王寨街道松鹤路社区创业路5号（原新华玻璃厂院内）` |
| 企业类型 | 有限责任公司（自然人独资） |

**⚠️ 填写纪律**
- 企业名称必须和执照**完全一致**（含"省"字），一个字都不能差，否则工商核验失败。
- 统一社会信用代码建议**复制粘贴**，手输易错。
- 系统会调用工商数据比对，若提示"未查询到企业信息"，通常是成立未满 15 天或工商数据未同步，等几天重试。

---

## 2. 经营范围（照抄执照原文；若后台要求精简，用下面这段）

```
一般项目：人工智能应用软件开发；人工智能基础软件开发；人工智能理论与算法软件开发；计算机系统服务；数据处理与存储支持服务；软件开发；软件销售；信息系统咨询服务；市场营销策划；咨询代理；广告制作；互联网销售（除销售需要许可的商品）；网络技术服务；数字内容制作服务（不含出版发行）。
```

（许可项目：**网络文化经营** —— 与小程序类目无关，**不要去办这个证**，提交时可忽略或不填。）

---

## 3. 管理员信息

| 字段 | 建议 |
|---|---|
| 管理员姓名 | `张俊峰`（法人本人兼管理员，决策最快） |
| 管理员身份证 | 本人 |
| 管理员手机号 | 本人常用手机号（接收审核通知） |
| 管理员微信 | **必须是已实名认证的本人微信**（验证时扫码用） |

---

## 4. 小程序名称 ✅ 已拍板

```
小赫 AI 报单助手
```

- 微信字符数 **16**（汉字算 2），在 4–30 区间 ✅
- 含 **AI** 词 —— 经营范围「人工智能应用软件开发」可背书 ✅
- 不含「蒙牛 / 低温奶 / 乳业 / 食品」✅

**命名纪律**（已满足）
- ❌ 不要出现「蒙牛」等品牌词（触发商标授权审核）
- ❌ 不要出现「低温奶 / 乳业 / 食品」（与经营范围不符，且易被要求食品证）
- ✅ 名称唯一，被占用就加后缀或换候选

**若名称被驳回的备选（按顺序试）**
| 顺序 | 备选名 | 何时用 |
|---|---|---|
| ① | **小赫 AI 报单助手** | 首选（当前） |
| ② | **小赫智体报单助手** | 若"小赫"被判与他人在先商标冲突 → 用公司全名字号，更好证明关联 |
| ③ | **智体报单助手** | 若含"AI"被要求说明 AI 能力 → 去掉 AI 词最稳 |

**两个潜在风险与应对**
- **「小赫」商标风险**：微信名称审核查商标库。若他人在第 9 类（软件）/第 42 类（技术服务）注册了「小赫」，可能被要求提供商标证 → 换备选 ②（用公司全名字号）。
- **「AI」名义风险**：若审核员追问 AI 能力，回复模板：**「本小程序不涉及 AI 生成内容，仅为订货数据填报与审批流程工具，名称中的 AI 指向主体经营范围中的人工智能应用软件开发资质。」**

---

## 5. 小程序简介（≤120 字符，直接复制）

> 微信限制：**4–120 字符**（汉字/字母/标点各算 1 个）。以下均已实测在限内。

### ✅ 推荐版（76 字符）— 直接用这个

```
本企业内部订货预报工具：销售按门店填报商品需求，主管在线审批后自动汇总生成采购计划。仅限企业员工账号登录，不涉及在线支付、对外销售，不收集任何个人信息。
```

### 备选 1 · 精简版（66 字符）

```
企业内部订货预报工具：销售按门店填报商品需求，主管在线审批，自动生成采购汇总。仅限本企业员工账号登录使用，不涉及在线支付与对外销售。
```

### 备选 2 · 合规版（69 字符）— 若被驳回要求改，用这个

```
本企业内部使用的订货预报工具。销售人员按门店填报商品需求数量，主管在线审批并汇总。需企业账号登录，不涉及在线支付、对外销售及个人信息收集。
```

**简介纪律**
- ✅ 首句必须含「**企业内部**」—— 审核员第一眼判断是否需要电商/食品资质就看这个
- ✅ 保留「不涉及在线支付 / 对外销售 / 个人信息收集」三连，直接堵死资质追问
- ❌ 不要在简介里写「低温奶 / 乳业 / 食品」（B 业务版虽有，但会引向食品类目联想，**不推荐**）
- ❌ 不要出现「最」「第一」「领先」等极限词（违反广告法，必被驳回）
- ❌ 不要放电话、网址、微信号

> 关键点都写到了：**企业内部使用 / 不涉及支付 / 不对外销售 / 不收位置** —— 这四句是审核员判断"是否需电商/食品资质"的核心依据。

---

## 6. 服务类目（设置 → 基本设置 → 服务类目）

> ⚠️ 已按微信官方《开放的服务类目》核实：**「工具」大类下没有「效率」这个二级类目**（那是抖音小程序的类目，此前写错）。以下为正确推荐。

### ✅ 首选

```
商业服务 → 企业管理
```

官方适用范围原文：**「适用于提供企业办公工具/办公管理、门店信息管理、展示等服务」**，资质要求：**无**。
→ 我们的小程序正是"企业内部办公工具 + 门店（客户）信息管理 + 填报审批"，逐字命中。

### 备选（后台若找不到上面那个，按此顺序试）

| 顺序 | 类目 | 官方适用范围 | 说明 |
|---|---|---|---|
| ② | **工具 → 办公** | 适用于提供办公工具等服务 | 最通用、审核最稳 |
| ③ | 工具 → 信息查询 | 适用于提供信息查询、**订单功能**等服务 | 我们有预报单，勉强沾边 |

**类目纪律**
- ✅ **一次只加一个类目**，多加会触发更多资质要求
- ❌ **不要选**「食品」（需《食品经营许可证》）
- ❌ **不要选**「电商平台 → 商家自营」（会牵出支付/订单体系审核）
- ❌ 不要选「商业服务 → 公关/推广/市场调查」（与业务不符）
- 📌 微信类目会不定期调整，**以后台实际能搜到的为准**；若以上都没有，搜关键词「企业管理」「办公」定位

---

## 7. 服务器域名（开发 → 开发管理 → 开发设置 → 服务器域名）

**request 合法域名**（必填，否则真机所有接口被拦截）：
```
https://hergent.cn
```

| 类型 | 是否需配 |
|---|---|
| request 合法域名 | ✅ `https://hergent.cn` |
| socket / uploadFile / downloadFile | ❌ 未使用 |
| udp / tcp | ❌ 未使用 |

> 配完**需在微信开发者工具里重新上传并清缓存**才生效。
> 当前 `project.config.json` 里 `urlCheck:false` 是开发期绕过，**提审前必须确认域名已配且校验通过**。

---

## 8. 隐私保护指引（设置 → 服务内容声明 → 隐私保护指引）

按**代码实际调用的 API** 勾选，多填会驳回、漏填也驳回：

| 采集项 | 勾选 | 依据 |
|---|---|---|
| **剪切板** | ✅ 必勾 | `summary.js` 用 `wx.setClipboardData` 复制汇总 |
| 账号信息（自有账号） | ✅ 建议勾 | 自有账号体系登录 |
| 位置 / 相册 / 摄像头 / 手机号 / 蓝牙 / 通讯录 | ❌ 全不勾 | 代码中**根本没调用** |

> 已核查：全项目仅用 19 类基础 API，无任何敏感接口调用。
> 指引提交后需**等待数小时生效**才会在小程序内触发系统弹窗（我们代码有自定义弹窗兜底，两条路都堵住）。

---

## 9. 测试账号（提审时在「测试账号」栏填写，直接复制）

```
销售账号：mptest    密码：Mptest@1   （角色：销售，可填写预报、查看/撤回本人提交）
主管账号：mptestsp  密码：Mpsup@1    （角色：主管，可审批预报、查看汇总总表）
```

**⚠️ 密码长度限制（系统缺陷，别踩）**：后端 `_hpw()` 把 64 位密钥拼在密码后做 bcrypt，占满 72 字节上限 → **任何账号密码最长 8 字符**。这两个已是 8 字符临界值，**不要在后台改成更长的**。

**审核通过后务必禁用**（主库 `erp.db`，改完重启）：
```sql
UPDATE users SET is_active=0 WHERE username IN ('mptest','mptestsp');
```

---

## 10. 审核备注模板（提交审核时粘贴）

```
本小程序为湖北省小赫智体数字科技有限公司内部使用的订货预报工具，不对公众开放。
使用者为本企业销售人员与主管，需使用企业分配账号登录后方可使用。

功能路径：
1. 登录：使用下方测试账号登录（销售账号 / 主管账号各一个）
2. 填报：首页「填报」→ 选择门店 → 搜索商品 → 填写数量 → 提交
3. 我的提交：「我的提交」→ 点击记录展开明细 → 待审批的单可撤回
4. 审批：用主管账号登录 → 「我的提交」→ 审批预报单 → 通过 / 驳回
5. 汇总：用主管账号登录 → 「我的提交」→ 汇总总表 → 按日期查看并复制

说明：
- 本小程序不涉及在线支付、不涉及商品对外销售、不涉及食品经营
- 不收集用户位置、相册、通讯录、手机号等任何敏感信息
- 仅使用剪切板功能（用于复制汇总数据）
- 服务类目：商业服务 → 企业管理
```

---

## 11. 拿到 AppID 后要做的事（把这步交给我）

1. ~~把 AppID 发我 → 我替换 `project.config.json:47` 的 `touristappid`~~ ✅ **已完成**（2026-09-06，无需再做）
2. 微信开发者工具打开目录：
   `/Users/zhangjunfeng/Documents/laozhangai-product/forecast-order-miniprogram-20260812T023419087Z/miniprogram`
3. 点「上传」→ 填版本号（建议 `1.0.0`）+ 备注 → 上传为体验版
4. 手机上加体验成员 → 跑《真机E2E测试清单.md》E1–E12
5. 全绿后提审 → 按《提审打钩清单.html》逐项勾

---

## 12. 时间线预期

| 阶段 | 耗时 | 谁在忙 |
|---|---|---|
| 主体注册 + 法人验证 | 1-3 天 | **平台处理**，你只需点几下 |
| 配域名/类目/隐私指引 | 30 分钟 | 你 |
| 上传体验版 + 真机 E2E | 半天 | 你 |
| 提审 + 审核 | 1-7 天 | **审核员处理** |
| 灰度放量 | 4 天起 | 观察 |

> 80% 的时间是"平台在处理"。提交注册后立刻回去优化 Web 端，零冲突。
> ⚠️ 唯一需避开：部署后端时重启会短暂中断，别在真机测试时段部署。
