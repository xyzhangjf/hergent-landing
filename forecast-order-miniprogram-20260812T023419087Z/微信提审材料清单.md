# 预报订单小程序 · 微信公众平台提审材料清单

> 目标：把「首次提审」拆成可照做的动作，一次过审。
> 前置：代码改动（A3/E2/D1 + 埋点 + 我的提交/撤回 + 移动审批）已落盘，**须由你本人在微信公众平台 + 微信开发者工具操作**。
> 目录：`forecast-order-miniprogram-20260812T023419087Z/`（非 git 仓库）

---

## 0. 前置：主体与资质（先准备材料，别先提审）

| 项 | 要求 | 你的情况 |
|---|---|---|
| 主体类型 | **企业** 或 **个体工商户**（个人主体做不了商业订货类） | 用你的经销商营业执照注册 |
| 营业执照 | 注册主体时上传 | 必备 |
| 对公验证 / 法人验证 | 企业主体需法人扫码或打款验证 | 约 1-3 天 |
| 食品经营许可证 | 若类目涉及食品/生鲜，**大概率需要** | 经销商一般有，提前备好扫描件 |

⚠️ 主体注册 + 验证需要时间，第一步先做这个，别等代码改完才开始。

## 1. 注册真实小程序账号 + 替换 appid

1. 微信公众平台（mp.weixin.qq.com）→ 立即注册 → 小程序 → 邮箱注册 → 选主体类型 → 填营业执照 → 完成验证。
2. 注册完成后：开发管理 → 开发设置 → 复制 **AppID**。
3. ~~打开 `miniprogram/project.config.json`，把 `"appid": "touristappid"` 替换为真实 AppID（`project.config.json:47`）。~~ ✅ **已完成**（2026-09-06，真实 AppID 已替换）。
4. 微信开发者工具重新打开项目目录，确认右上角显示的 AppID 已变。

## 2. 配置服务器域名（request 合法域名）

开发管理 → 开发设置 → 服务器域名 → request 合法域名 → 添加：

```
https://hergent.cn
```

- 必须 **HTTPS**，且域名需已备案（hergent.cn 已备案可用）。
- 只加这一条即可（我们只调 `/api/*`）。
- 配置后**无需改代码**；开发者工具里可勾选「不校验合法域名」继续本地调试，但**真机预览/体验版/线上必须配置**，否则所有接口被拦截。
- ⚠️ 当前 `project.config.json` 里 `urlCheck: false` 只是开发期绕过，上线前不影响（发布时以平台域名配置为准）。

## 3. 类目选择

设置 → 基本设置 → 服务类目 → 添加类目。

**已根据你的证照（湖北省小赫智体数字科技有限公司，统一社会信用代码 91420606MAKF1YPG5Y，经营范围含人工智能应用软件开发/软件开发/信息技术咨询/数据处理/互联网销售）调整推荐顺序。**

> ⚠️ **2026-09-06 更正**：此前推荐的「工具 → 效率」**不存在**（那是抖音小程序类目，我记混了）。已按微信官方《开放的服务类目》文档核实，下表为正确版本。

| 优先级 | 类目路径 | 是否需要资质 | 说明 |
|---|---|---|---|
| ① **首选** | **商业服务 → 企业管理** | **无** | 官方适用范围原文：**「适用于提供企业办公工具/办公管理、门店信息管理、展示等服务」** —— 我们的小程序（内部办公工具 + 门店信息管理 + 填报审批）**逐字命中**，且与你的"软件开发/信息技术咨询"经营范围契合 |
| ② 备选 | 工具 → 办公 | 无 | 官方适用范围「适用于提供办公工具等服务」，最通用最稳 |
| ③ 兜底 | 工具 → 信息查询 | 无 | 「适用于提供信息查询、**订单功能**等服务」，我们有预报单，勉强沾边 |
| ④ 不选 | 电商平台 → 商家自营 | 营业执照 | 经营范围含"互联网销售"理论可用，但会牵出支付/订单体系审核，复杂度高 |
| ⑤ 不选 | 食品 → 食品 | 需《食品经营许可证》 | 你经营范围**无食品相关**，选这个必被拒 |

**微信「工具」大类下真实二级类目**（供后台核对）：记账、投票、日历、天气、备忘录、**办公**、图片处理、计算器、报价/比价、**信息查询**、兑换、预约/报名、健康管理、开票服务/企业代票工具、云手机、发票查询、视频客服、设备管理。

**建议**：先只加 **①商业服务-企业管理**（首选），若被驳回要求补充再换 ②。一次只加一个类目，别多加（多加会触发更多资质要求）。类目会不定期调整，**以后台实际能搜到的为准**。

## 4. 隐私保护指引（关键，逐项勾选）

设置 → 服务内容声明 → 隐私保护指引 → 更新。

**我们实际采集项（已代码核查，无位置/相册/手机号/蓝牙/通讯录）：**

| 隐私指引条目 | 是否勾选 | 依据（代码证据） |
|---|---|---|
| **剪切板** | ✅ **必须勾选** | `summary.js` 用 `wx.setClipboardData` 复制预报汇总；`fill.js` 也可能复制 |
| 用户信息（账号） | ✅ 建议勾选 | `login.js` 收集用户名/密码用于鉴权（自有账号体系，非微信授权） |
| 位置信息 | ❌ 不勾 | 未调用 `wx.getLocation/chooseLocation` |
| 相册/摄像头/麦克风 | ❌ 不勾 | 未调用 `chooseImage/chooseMedia/startRecord` |
| 手机号 / 微信头像昵称 | ❌ 不勾 | 未调用 `getPhoneNumber/getUserProfile` |
| 蓝牙 / 通讯录 / 相册写入 | ❌ 不勾 | 未调用 |

**用途说明填法（可直接复制）：**

- 剪切板：「用于用户一键复制本店预报汇总内容，方便粘贴到微信或表格。」
- 账号信息：「用于销售/督导登录本企业订货系统，验证身份并区分所属门店权限。」

填完点「确定并生成」→ **等待生效（一般几分钟到数小时）**。

⚠️ 重要联动：代码用了 `wx.getPrivacySetting` / `wx.requirePrivacyAuthorize`。这两个官方接口**只有在后台隐私指引生效后才会触发系统弹窗**；若未生效，`getPrivacySetting` 会返回 `needAuthorization=false`，此时我们代码里的**自定义同意弹窗兜底会自动接管**（未同意即拦截登录），所以两条路都堵住了，不会漏。但**后台隐私指引仍必须配**，否则提审必驳回。

## 5. 小程序基本信息

| 项 | 建议填法 |
|---|---|
| 名称 | ✅ **小赫 AI 报单助手**（已拍板。微信字符数 16，在 4–30 区间；含 AI 词由经营范围「人工智能应用软件开发」背书） |
| 简称 | 同上，≤10 字 |
| 简介 | ≤120 字符，用「本企业内部订货预报工具：销售按门店填报商品需求，主管在线审批后自动汇总生成采购计划。仅限企业员工账号登录，不涉及在线支付、对外销售，不收集任何个人信息。」（76 字符，详见 `小程序注册信息-复制粘贴包.md` 第 5 节另有 2 个备选） |
| 头像 | 简洁图标，避免用品牌 Logo（商标风险） |

⚠️ **名称/简介里不要出现「蒙牛」**，除非你已获得品牌授权（含品牌词会触发商标审核驳回）。简介明确写「本企业内部使用」有助于审核理解业务场景。
⚠️ **简介不要写「低温奶 / 乳业 / 食品」** —— 会把审核引向食品类目联想（可能被要求《食品经营许可证》）。写「企业内部订货预报工具」即可。

**名称若被驳回，按顺序换备选**：① 小赫 AI 报单助手（首选）→ ② **小赫智体报单助手**（"小赫"撞在先商标时用公司全名字号）→ ③ **智体报单助手**（"AI"被追问时去掉 AI 词最稳）。
**"AI"被追问的回复模板**：本小程序不涉及 AI 生成内容，仅为订货数据填报与审批流程工具，名称中的 AI 指向主体经营范围中的人工智能应用软件开发资质。

## 6. 提审材料包（提交审核时填写）

### 6.1 测试账号（❗最关键，漏了这个 90% 被驳回）
审核员要用账号登录才能看到功能——**我们的登录是自有账号体系，必须提供真实可登录的账号**。

✅ **已在生产创建完毕并跑通全链路**（2026-08-30 服务端实测）：

| 用途 | 用户名 | 密码 | 角色 | 门店 | 可演示路径 |
|---|---|---|---|---|---|
| 销售填报 | `mptest` | `Mptest@1` | sales | 永诺旗舰店（id=2220） | 登录→选门店→搜商品→填量→提交→我的提交→撤回 |
| 主管审批 | `mptestsp` | `Mpsup@1` | supervisor | 不限 | 审批列表→通过 / 驳回 |

提审时在「测试账号」栏填这两组，并在备注说明各自能走到哪些页面。
⚠️ **审核通过后立即删除或禁用**：`UPDATE users SET is_active=0 WHERE username IN ('mptest','mptestsp')`（主库 erp.db），改完重启服务。

> **密码长度硬限制（重要）**：本系统 `_hpw()` 把 64 位 hex 的 `ERP_SECRET` 拼在密码后再做 bcrypt，而 bcrypt 上限 72 字节 → **所有账号密码实际最长只有 8 个字符**（72−64）。设更长会直接 500。给审核员改密码时务必 ≤8 字符。

### 6.2 功能页面路径（填在审核备注）
```
pages/login/login     登录（含隐私同意）
pages/fill/fill       填写预报（核心）
pages/mine/mine       我的提交（详情展开 + 撤回）
pages/approval/approval 移动审批（通过/驳回）
pages/summary/summary 预报汇总 + 复制
```

### 6.3 补充说明模板（可直接复制粘贴到审核备注）
> 本小程序为乳制品经销商**企业内部订货预报工具**，仅面向本公司销售/督导/主管使用，不对外提供服务、不涉及在线支付与消费者交易。
> 功能：① 销售填写每日商品预报量并提交；② 销售可查看/撤回自己的提交；③ 主管在移动端审批（通过/驳回）；④ 汇总查看与复制。
> 登录账号由企业内部分配，非微信授权登录；不采集位置、相册、手机号等信息；仅使用剪切板用于复制汇总文本（已在隐私保护指引声明）。
> 测试账号：销售 `mptest` / `Mptest@1`（可填写预报、查看/撤回自己的提交）；主管 `mptestsp` / `Mpsup@1`（可查看待审列表并审批通过或驳回）。

## 7. 截图要点（提审上传的页面截图）

准备 3-5 张真实数据截图（不要用空白/测试数据）：

1. **登录页**（含隐私同意弹窗关闭后状态）
2. **填写预报页**（已选门店 + 已加几个商品 + 有数量，显示真实商品名）
3. **我的提交页**（有 1-2 条记录，展开显示了明细 + 状态徽标）
4. **移动审批页**（有 1 条待审，显示门店/提交人/AI 建议量）
5. **汇总页**（有汇总数据）

⚠️ 截图里**不要出现**「touristappid」、测试数据"test/test"、红色报错 toast。

## 8. 常见驳回原因 + 应对

| 驳回原因 | 应对 |
|---|---|
| 「无法登录/测试账号无效」 | 检查账号是否真的能在生产登录；确认绑定的门店有商品数据；审核期间别改密码 |
| 「缺少隐私保护指引」 | 后台配置并等生效；代码已有兜底弹窗 |
| 「类目与内容不符」 | 按驳回意见换类目（若要求工具类目，改「工具 → 办公」；切勿再写「工具-效率」，微信无此类目） |
| 「涉及在线支付但未说明」 | 我们无支付，备注里明确"不涉及在线支付" |
| 「名称含品牌词需授权」 | 去掉「蒙牛」等品牌词 |
| 「服务范围仅限内部不应公开」 | 备注强调"企业内部工具"，并说明账号由企业分配 |

## 9. 提审打钩清单（逐项确认再提交）

- [ ] 主体注册完成（企业/个体工商户），已通过验证
- [ ] 真实 AppID 已替换 `project.config.json:47`
- [ ] request 合法域名已加 `https://hergent.cn`
- [ ] 服务类目已添加（建议：商家自营）
- [ ] 隐私保护指引已配置并**生效**（勾选：剪切板 + 账号信息）
- [ ] 名称/简介已填，**无品牌词**
- [ ] 生产已创建专用测试账号（销售角色 + 主管角色各一）
- [ ] 测试账号能真实登录、门店有商品可搜、能提交成功
- [ ] 3-5 张真实数据截图已备好
- [ ] 审核备注已填（含功能路径 + 补充说明模板 + 测试账号）
- [ ] 代码全部改动已在开发者工具「上传」为体验版，并跑过真机 E2E 清单 E1-E12

## 10. 时间预期

| 阶段 | 时长 |
|---|---|
| 主体注册 + 验证 | 1-3 个工作日 |
| 域名/类目/隐私配置 | 0.5 天（含隐私指引生效等待数小时） |
| 代码上传 + 体验版真机验证 | 0.5 天 |
| 微信审核 | 1-7 天（企业主体一般 1-3 天） |
| 分阶段灰度 | 4 天起（每阶段 ≥24h） |

---

⚠️ **免责说明**：类目与资质要求以微信公众平台当前规则为准，后台会按你选择的类目动态要求材料；若页面提示需要额外资质，以页面为准。本清单基于业务逻辑与实际代码核查（敏感 API 已核实：仅剪切板 + 账号）给出，用于降低驳回概率，不保证一次通过。
