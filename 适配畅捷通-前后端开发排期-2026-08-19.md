# 适配畅捷通生态 · 前后端开发排期（P0/P1）

> 生成日期：2026-08-19
> 结论前置：**后端已有一套可复用的金蝶连接器范式**（`server/kingdee_connector.py` + `server/routers/datasources_v2.py` 的 `/kingdee/*`）。畅捷通与金蝶同属「OAuth + OpenAPI」型 ERP，适配工作量≈**克隆金蝶模式改 API 地址 + 补齐多租户凭据隔离 + 前端暴露连接卡**，而非从零设计。

---

## 0. 现状基线（已验证，避免重复造轮子）

| 已有资产 | 文件 | 复用价值 |
|---|---|---|
| 金蝶 OAuth 连接器 | `server/kingdee_connector.py`（OAuth2 + 自动刷新 + 30+ 业务端点 + `map_product/map_contact` + `kingdee_tokens` 表） | **80% 代码可直接改写出 `chanjet_connector.py`** |
| 数据源路由 v2 | `server/routers/datasources_v2.py`：`/kingdee/connect|callback|status|disconnect|sync` | `/chanjet/*` 端点照抄 |
| 路由注册 | `server.py:690,768` 已 `include_router(datasources_v2_router)` | 无需改注册，加函数即可 |
| Hermes MCP 副驾路径 | `laozhangai-product/hermes-mcp-chanjet/*`（已就绪） | **副驾 Q&A 直读畅捷通现在就能用，P0 验证免后端改造** |
| 前端连接中心 | `hergent-cn-v2/src/pages/ConnectCenter.vue`：连接器 Tab 只有企微 + MCP 卡 | 加「畅捷通」连接卡克隆企微卡 |

**两个金蝶模式的关键缺口（必须补，否则畅捷通多客户不成立）：**
1. `kingdee_tokens` 是**全局主库表**，没有 `tenant_id`。畅捷通是**多客户 SaaS**，必须每租户隔离凭据。
2. 金蝶同步写 `products/contacts` 带 `source='kingdee'`，但前端从未暴露连接 UI；6 引擎能否消费取决于同步是否写入**租户库**（见 T6）。

---

## P0 阶段：接第一个畅捷通客户前必须完成（≈ 8.5–10.5 人天）

> 目标：客户能自己在连接中心绑定畅捷通，副驾能基于其数据问答，且**凭据按租户隔离、写操作零越权**。

### T1 克隆金蝶 → `server/chanjet_connector.py`
- 复制 `kingdee_connector.py`，改写：
  - 认证：畅捷通开放平台 `appKey/appSecret` → 客户 OAuth 授权 → `openToken`（6 天有效、可刷新），替换金蝶的 `client_id/secret` 流程。
  - 业务端点：用畅捷通 OpenAPI（库存/应收/销售单/商品/客户），替换金蝶 `/v3/...` 路径。
  - 字段映射：`map_product` / `map_contact` 改写为畅捷通字段 → Hergent 内部模型（商品名/规格/单位/条码/售价；客户名/电话/地址）。
  - tokens 表名改 `chanjet_tokens`（先保留全局，T3 再改每租户）。
- 估点：**3–4 人天**（AI 可主导，复用率 80%）。
- 验收：单测 `get_valid_token()` 在授权后能拿到有效 token；`map_product` 把一条畅捷通商品正确映射。

### T2 后端 `/chanjet/*` 端点（`server/routers/datasources_v2.py`）
- 克隆 `kingdee_connect|callback|status|disconnect|sync` 五个函数，前缀改 `chanjet`，内部调 `chanjet_connector`。
- 回调 `redirect_uri` 改 `https://erp.hergent.cn/api/datasources/v2/chanjet/callback`。
- RBAC：`_auth(request)` 保留（角色 `data`，与 kingdee 同）。
- 估点：**1.5 人天**。
- 验收：`POST /api/datasources/v2/chanjet/connect` 返回 `auth_url`；`/status` 在授权前后状态正确切换。

### T3 凭据每租户隔离（关键改造，金蝶没做）
- 新建 `datasource_bindings` 表：`id, tenant_id, provider('chanjet'|'kingdee'), encrypted_token, refresh_token, expires_at, org_name, created_at`。
- `chanjet_connector._save_token/_get_db` 改为按当前租户上下文写/读该表；token **加密落库**（用 `server/crypto_utils.py`，复用现有加解密，避免明文存 `openToken`）。
- 同步 `datasources_v2` 的 disconnect/sync 用 `tenant_id` 过滤。
- 估点：**2 人天**（含加密 + 迁移旧全局表逻辑）。
- 验收：租户 A 授权后，租户 B 调 `/chanjet/status` 返回未连接；DB 中 `openToken` 为密文。

### T4 前端「畅捷通」连接卡（`ConnectCenter.vue`）
- 克隆企微卡（行 62–94、260–282 的 `wecom` 对象 + 弹窗），新增 `chanjet` 对象：`去连接`（跳 `/chanjet/connect` 返回的 `auth_url`）/ 已连接状态 / 解绑 / `立即同步` 按钮。
- 调 `POST /api/datasources/v2/chanjet/sync` 手动同步。
- 估点：**1.5 人天**。
- 验收：浏览器走完 OAuth 回调后，卡片显示「已连接 · XXX 公司」；点解绑后状态回退。

### T5 铁律审计：写工具零越权（0.5 人天）
- 确认 Hermes 接畅捷通 MCP 时，`tools.exclude` 排除所有写类工具（改单/删单/付款）。
- 副驾任何「建议下单/改单」走二次确认弹窗（沿用现有「AI 只建议不擅自下单」铁律）。
- 估点：**0.5 人天**（审计级，改动小）。
- 验收：副驾对畅捷通数据只读；生成预报单只出模板不回写。

**P0 出口标准**：1 个真实畅捷通客户走通「连接中心绑定 → 手动同步 → 副驾 Q&A 基于其数据回答」全链路，凭据隔离且无越权写。

---

## P1 阶段：让 6 大引擎吃畅捷通数据（≈ 9 人天）

> 目标：货损/返利/预报/工资/对账/指标 6 引擎能基于畅捷通同步来的数据计算，且快、可离线。

### T6 同步写入租户库（关键）
- 金蝶同步用 `db.get_db()`（受租户上下文影响）；确认畅捷通同步同样写入**当前租户库** `tenant_{id}.db` 的 `products/contacts/inventory/sale_orders/receivables`，而非主库。
- 若 6 引擎直查租户库，此步后引擎即自动可见同步数据（无需改引擎）。
- 估点：**2 人天**。
- 验收：租户 A 同步后，其库存/应收页出现畅捷通数据（`source='chanjet'`）。

### T7 库存/批次/效期映射（领域缺口，Hergent 增值点）
- 畅捷通库存若无 `batch_no/expiry_date`，注入 Hergent 货损计算口径（参考现有 `near-expiry` 引擎逻辑）。
- 临期/货损引擎用映射后的数据算「本周哪些货临期、货损多少」。
- 估点：**2 人天**。
- 验收：临期引擎对同步数据输出正确清单（无 batch/expiry 时有合理兜底口径）。

### T8 定时同步服务（cron，限流感知）
- `cron_tasks` 加「畅捷通增量同步」：用 `modified_after` 拉增量；记录 `last_sync_at`；API 限流（429）退避重试。
- 估点：**1.5 人天**。
- 验收：定时触发后库存页「最后同步 10:32」自动更新；限流时不崩。

### T9 6 引擎适配（若 T6 不够）
- 若某引擎直连主库或硬编码源，统一改为走租户库 / 抽象 provider。
- 估点：**2 人天**（视 T6 结果，可能 0）。
- 验收：6 引擎在畅捷通数据租户下输出与本地库租户一致。

### T10 前端「数据来源感知」
- 库存/应收/销售页加徽标「数据来源：畅捷通 · 最后同步 10:32」+ 手动「立即同步」。
- 副驾对话标注「基于你的畅捷通数据」。
- 估点：**1.5 人天**。
- 验收：老板能一眼看出数从哪来、何时同步。

**P1 出口标准**：6 引擎在畅捷通客户租户下正常算货损/返利/工资，页面标注数据来源，定时同步稳定。

---

## P2 阶段（更后，不阻塞首单）
- 真多租户云 + API 调用计费（按客户计量畅捷通 API 用量）。
- 断网韧性：本地读模型离线可算，恢复后增量补。
- Excel/CSV 降级路径强化（命门：无 ERP 也能跑，保持独立获客）。
- 评估上架畅捷通应用市场做 ISV（借其百万客户 + 渠道）。

---

## 建议执行顺序（关键）
1. **现在就用 `hermes-mcp-chanjet/` 跑通副驾 Q&A**（零后端改造，验证「老板愿为基于畅捷通数据的 AI 副驾付费」）。
2. 并行启动 **T1 + T2**（克隆金蝶，最快出可用后端）。
3. **T3 每租户隔离** 必须在接第二个客户前完成。
4. T4 前端卡与 T1/T2 同批上线。
5. 拿到首单验证后，再投 **P1（T6–T10）** 让 6 引擎吃数据。

## 不变的三条铁律
- 「能连畅捷通」**不是护城河**——价值在连上后的低温奶配方化算法（货损/返利/预报/工资 skill）。
- **绝不丢本地库 / Excel 降级路径**（不被平台绑死、独立获客命门）。
- 副驾**任何写操作必须人点二次确认**。

## 复用清单（避免 AI 重写）
- `server/kingdee_connector.py` → 改写 `chanjet_connector.py`
- `server/routers/datasources_v2.py` → 加 `chanjet_*` 函数
- `server/crypto_utils.py` → 凭据加密
- `hergent-cn-v2/src/pages/ConnectCenter.vue` 企微卡 → 克隆畅捷通卡
- `laozhangai-product/hermes-mcp-chanjet/*` → 副驾 MCP 路径（已就绪）
