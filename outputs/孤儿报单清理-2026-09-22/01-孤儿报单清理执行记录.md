# 孤儿报单清理 · 执行记录（2026-09-22 v243）

> 授权原话：**「直接清理掉吧」**（承接上一轮诊断：`2026-08-22 报单` 是"未被任何期次覆盖"的合成行）
> 目标库：`/opt/hergent-erp/tenant_1.db`（生产，租户 1）
> 执行时间：2026-09-22 17:26–17:28 CST｜**全程未 push，未改任何代码**

---

## 一、结论

「2026-08-22 报单」这行**已从往期预报中消失**，其 20 条报单与 571 条明细已删除，**无残留**、**未误伤任何其它数据**。

| 表 | 删除行数 | 说明 |
|---|---|---|
| `forecast_submissions` | **20** | id 2–21，`period_id=0 / role='导入' / status='pending'`，note 全为 `Excel导入 最新26年8月下单表8.18.xlsx` |
| `forecast_submission_items` | **571** | `submission_id IN 2..21` —— 唯一引用表 |
| `forecast_period_confirm` | **1** | `(2026-08-22, 2026-08-22)`，`confirmed_by='系统回填'`，**与 20 条提交同秒（10:43:02）产生** |

被删数据规模（删除前实测）：**12,709 件 / ¥80,979.96**，全部 `pending`（从未审核）。

---

## 二、为什么判定它可以删

1. **它从来不是"期次"** —— 是 `erp_db.forecast_order_board()` 动态补的**合成行**（`id < 0`）。`forecast_periods` 里没这条记录，所以四个行级按钮的判据 `Number(row.id) > 0` 全部不成立 ⇒ 按钮不是"漏了"，是**没有可作用的对象**。
2. **来源可溯** —— 该行由「未选期次直存」产生：前端用合成期次「今日报单」保存，**报单落库但不写 `forecast_periods`**。看板补行是为了不让这批数据彻底消失。
3. **形态是早期试用产物** —— 20 条全部集中在 **10:43:01–02 一秒内**、note 指向同一个 Excel 文件、`period_id=0`、`status=pending` 从未审核，且它是**全库唯一**一个"有报单无期次覆盖"的日期。
4. **用户确认** —— 上轮已明确告知数据量（20 条 / 12,709 件 / ¥8 万 / 全部 pending）并询问"是真实业务单还是测试单"，用户答复「直接清理掉吧」。

---

## 三、影响面盘点（先盘清才动手）

**按语义列的扫描结果**（有判别力）：

| 检查维度 | 结果 |
|---|---|
| 含 `submission` 相关列的表 | 仅 `forecast_submission_items.submission_id` → 571 行 |
| 含 `order_date` 列的表 | 除 `forecast_submissions`(20) 外**全为 0** |
| 含 `period_start`+`period_end` 的表 | `forecast_audit_decisions` 0 / `forecast_extra_qty` 0 / `period_payments` 0 / `consignment_settlements` 0；**仅 `forecast_period_confirm` 1 行** |
| `forecast_import_products` 的 `period_id=0` | **0 行**（无共享清单桶，删除无连带） |
| 数据库触发器 | 仅 products/contacts 的 FTS 触发器，**与本次无关** |
| `forecast_extra_qty`（加单 1264 行） | 关联键是 `(period_start, period_end, product_id)`，**不用 submission_id**，覆盖 8/22 为 0 行 |

> 🔴 **一条方法论教训（本轮实测）**：我先试了"值级全库扫描"（把 id 2..21 拿去比所有含 `id` 的列），命中 **124 张表** —— 全是噪音。因为 `2..21` 这种小整数在任何自增主键里都天然存在，**值级匹配毫无判别力**。
> **正解：按「语义列名」扫（submission_id / order_date / period_start+period_end），不按值扫。**

---

## 四、先演练、后执行（R4 纪律）

在 `/tmp` 内用 `sqlite3.backup()` 做**生产镜像**，导入**生产代码**，直接调真实后端函数 `erp_db.forecast_order_board()`：

| 时点 | 看板行数 | 8/22 那行 |
|---|---|---|
| 删除前 | 3 | `id=-1`，20 人 / **12,709 件** / ¥80,979.96 |
| 删除后 | 2 | **消失** |

同时断言：**保留的 19 条提交 / 19 条明细逐列不变**，差集**恰好**限于目标三表。

> 演练中我自己的断言写漏一项（`confirm_all` 内容快照键），报出**假 FAIL**；修正后通过。**这不是数据误伤**，但记一条：断言"变化范围"时，内容快照键与行数键要**一起列全**。

---

## 五、生产执行与验证（三层）

**执行**：`BEGIN IMMEDIATE` + 主键精确定位 + `rowcount == (20, 571, 1)` 断言，不符立即回滚。

| 层 | 验证 | 结果 |
|---|---|---|
| **列值** | 三表行数差集 | 39→19 / 590→19 / 8→7，**变化范围恰好限于目标三表** |
| **数据完整性** | 保留行逐列快照比对 | 19 条提交 / 19 条明细**逐列相同** |
| **业务函数** | `erp_db.forecast_order_board()`（真实函数） | 8/22 合成行**消失**，#16/#17 两行**逐字不变** |
| **HTTP 读端** | `GET /api/forecast/order-board`（临时令牌绑 boss，用完即删） | HTTP **200**，看板 **2 行**、无 8/22；`#16` 的 19 条报单体完好 |
| **鉴权对照** | 同一请求不带令牌 | **401** ⇒ 证明不是"根本没人拦" |
| **残留核查** | 8/22 的 submissions / items / confirm | **0 / 0 / 0** |
| **孤儿核查** | 全库"有报单无期次覆盖"的日期 | **空** |
| **未受影响面** | `forecast_periods` 2 / `forecast_import_products` 924 / `forecast_extra_qty` 1264 | **与删除前完全一致** |

---

## 六、回滚（一句话可还原）

**全量备份**：
```
/opt/hergent-erp/backups/tenant_1.db.before-clean-orphan-20260822-20260922-172622.bak   （12.3 MB）
```

**精确回滚脚本**（内含 592 条 INSERT，覆盖三张表全部被删行）：
```
/root/backup_v243_20260922-172622/ROLLBACK.sql
用法：sqlite3 /opt/hergent-erp/tenant_1.db < ROLLBACK.sql
```

**同目录另有**：`baseline.json`（删除前基线快照）、`scripts/`（本轮全部脚本，供复现）。

> 备份文件**保留不删**。

---

## 七、本轮我自己的两个失误（已记）

1. 🔴 **值级扫描无判别力**（124 表噪音）⇒ 影响面必须**按语义列名**扫。
2. 🔴 **断言集合漏项导致假 FAIL**：比较"删除前后差集"时，漏把 `confirm_all`（内容快照键）列入期望集，于是"正确的结果"被判成异常。⇒ 期望集要同时含**行数键 + 内容键**。

---

## 八、遗留与建议

- **其它日期未受影响**：`forecast_submissions` 现存 19 条（全部属于 9/20，也是 `pending`）。这 19 条**有期次覆盖**（#16），是正常数据，**不在本次范围**。
- **这类"未选期次直存"仍会产生新的孤儿行**（这是产品的既有行为，非缺陷）。若要根治，应让"未选期次保存"时**自动落一条真实期次**，而不是等看板补合成行 —— 这属于功能改动，需另立一批。
- 本次**只删数据、未改代码、未部署**。
