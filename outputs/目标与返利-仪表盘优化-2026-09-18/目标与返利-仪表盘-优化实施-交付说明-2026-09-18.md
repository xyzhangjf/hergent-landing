# 「目标与返利 › 仪表盘」优化实施 —— 交付说明

- **日期**：2026-09-18
- **基线**：审查报告 commit `ce2ff5d`（v185，纯审查未改码）
- **本提交**：`c6fa22a` — feat(rebate): 「目标与返利 › 仪表盘」审查落地（v185）
- **改动面**：前端 3 个文件 + 1 个工具脚本，**后端零改动**
- **生产**：hergent.cn（`/opt/hergent-cn-v2`）已部署并真机复测

---

## 一、落地项对照（按 5 条拍板）

| 拍板 | 落地内容 | 落地位置 |
|---|---|---|
| ① 「本月实际返利」取**本月全部填报** | hero 位从「本月预估返利」换成**「本月实际返利」**，口径 = `achievements[].actual_rebate` 合计（与「达成填报」页同源同字段）。原预估返利降为次级卡并改名**「本月应返 · 按当前达成推算」**（自带口径自证）。零填报时显示 `—` + 「本月尚未填报」 | `Rebate.vue` `dashBase.summary` / hero KPI |
| ② 「距目标」位改**距下一档绝对金额** | 行内第 3 格：规则**有分档** → `距下一档 ¥X`；规则为 **on_target**（达标即返）→ `距达标 ¥X`。标签随 `hasTiers` 自证，不再有「距目标」这种与左侧两条数字口径重叠的达成率 | `Rebate.vue` `gapSegLabel` / `gapSegText` |
| ③ 图表**保留 12 月 + 紧凑档切换** | 新增「完整 / 紧凑」密度切换：柱高 `200 → 132`px、刻度 `5 段 → 3 段`、画布 `563 → 427`px。**12 个月份全保留**（不裁尾部空月）。选择持久化到 `localStorage(hergent_achv_density)` | `MonthlyAchvChart.vue` |
| ④ 跨页链接带**品牌参数**一起做 | `goSprint()` 跳转带 `#/forecast?brand=…`（选中品牌才带，未选保持原链接）。`Forecast.vue` 新增承接逻辑：词表对得上则筛 + toast，对不上则**静默降级**（不筛、不弹错） | `Rebate.vue` `goSprint` / `Forecast.vue` watcher |
| ⑤ 口径免责声明**接受现状** | 不补回「非实际到账」说明（改动量已由 R1 净减 3 条 / 55 字） | — |

**R1–R4 一并落地**：

- **R1 删 4 条重复说明**：副标题 `574px → 252px`（只留「上下两张图各用自己的量程，柱高不可跨图比较」）；两条 `sec.note` 随模板与 CSS 一并删除（不留死 CSS）；图例 `4 条 → 3 条`（灰轨道 / 深色段合并为一条）
- **R2 删序号圈**：`.rr-rank` 与风险态变体一并删除（纯装饰，且风险行染红后更像警示标记而非序号）
- **R3** 见上表 ②
- **R4 删死字段**：`dashboardModel.triggered` / `dashboardModel.effectiveRate` / `summary.noData`（另注：`simResult.triggered` 与 `accrueResult[].details[].triggered` 是**另一个对象**，保留）
- **C2** 复用 v154 遗留的 `.mac-seg` / `.seg-btn` 死 CSS —— 此前有名无实，现在名副其实

---

## 二、实测数据（本地 dev 直连真实后端，同口径 A/B）

| 指标 | 改前 | 改后 |
|---|---|---|
| 文本节点 / 字数 | 81 / 455 | **83 / 421**（+2 节点是新增的密度按钮，字数 −34） |
| 说明性文案 | 10 条 / 148 字 | **7 条 / 93 字** |
| 图表副标题宽 | 574px | **252px** |
| KPI 卡 | 4 张 | **6 张**（+实际返利 / +本月应返） |
| 图表卡高 | 563px | **563px**（完整档）/ **427px**（紧凑档，−136） |
| 图例条数 | 4 | **3** |
| 行内第 3 格 | `距目标 ¥900,000` | **`距达标 ¥900,000`** |
| 序号圈 | 有 | **无** |

**紧凑档实测**：`svgH [132,132]`、`tickCount [4,4]`（= 3 段 + 1）、`canvas 332`、`chart 427`；**12 个月份全保留**（1月–12月）；`localStorage` 持久化验证通过（重载后仍 `compact`）。

**跨页品牌四路实测**（A/B/C/D 全绿）：

- A 打桩生效性 → 候选 `['测试品牌']`
- B **命中路** `?brand=测试品牌` → `checked:['测试品牌']`
- C **降级路** `?brand=不存在的品牌XYZ` → `checked:[]`、候选仍在、无报错
- D **混合** `?brand=测试品牌,不存在XYZ` → 只勾中命中的那个

链接侧：Rebate 选中品牌后生成的 hash 为 `#/forecast?brand=%E6%B5%8B%E8%AF%95%E5%93%81%E7%89%8C`（由 B 路证实）；未选品牌时为 `#/forecast`。

---

## 三、构建与部署

- **构建**：`CODEBUDDY_SAFE_DELETE_ENABLED=0 npm run build` → `✓ built in 1.92s`
  - 产物：`Rebate-p3J49zCm.js 177788B`、`Rebate-DrtmXEgO.css 42043B`、`Forecast-CbtW40nR.js 284678B`
- **SFC 定点编译自证**：三文件 template / script 全通过
- **部署前归一化差集**：仅 **3 个文件真变化**（`Forecast-HASH.js +397`、`Rebate-HASH.css −352`、`Rebate-HASH.js +1509`）；其余（含 `index.js` 导出序号位移、共享 chunk 序号位移）归一化后长度差 **+0** = 纯派生噪声
- **部署**：`rsync -a --no-owner --no-group --delete dist/ → /opt/hergent-cn-v2/` + `chown -R hergent:hergent`
- **双侧 md5 逐个相同**：
  - `Rebate-p3J49zCm.js` `4a40e55991d82a87fd1c06e8b7d1a857`
  - `Rebate-DrtmXEgO.css` `ad5341c5abf8b00109d3289c8d1a4400`
  - `index-B791Ppsi.js` `cc3d919e365be3a125d299a665b894f6`
  - `Forecast-CbtW40nR.js` `e01fd37d988786073f15cb305ad4f9f3`
- **生产包特征串**：新串全命中（`本月实际返利`×2、`本月应返`、`按当前达成推算`、`本月全部填报`×2、`距达标`、`距下一档`、`?brand=`、`紧凑`、`灰轨道＝目标位`、`各用自己的量程`、`已按来源页品牌筛选`）；**已删串全为 0**（`本月预估返利`、`rr-rank`、`柱高＝实际销量`、`柱高＝实际返利金额`、`深色段＝超出目标的部分`）
- **生产真机复测**（只读演示租户 tenant 10）：`leafCount 83 / charCount 421`、`chart 563`、`chartSub {h:19, w:252}`、KPI 6 张、图例 3 条、行内「距达标 ¥900,000」、`secs[].note` 全空、`rr-rank` 已消失、紧凑档 `svgH 132` + `ls:'compact'` 重载保持 —— **与本地逐项一致**

---

## 四、未做项与理由

| 项 | 状态 | 理由 |
|---|---|---|
| **R7** 品牌筛选候选取「本年度确有品牌目标」 | **未动** | 现用 `/api/brands?include_inactive=1` 全量档案。改为「本年度确有目标」需后端补聚合口径，且属产品语义变更 —— 不在本轮 5 条拍板范围内 |
| 口径免责声明（「非实际到账」） | **接受现状** | 用户第 5 条明确拍板 |

---

## 五、待确认事项

1. **R7 是否推进**（品牌筛选候选收窄到「本年度确有品牌目标」）
2. 新期次预填窗口是否改「今天 ~ 今天+4」
3. 改期次窗口是否加二次确认
4. 已关闭期次是否单开「仅改名不动窗口」通道
5. `Forecast.vue` 的 8 个在途 hunk（`loadEditGrid` sources 合并 / `buildRowBase` 行底同源 / `.imp-errs` 搬家）是否维持现状不提交

---

## 附：真机验证产物清单

`outputs/目标与返利-仪表盘优化-2026-09-18/真机验证/`

- `A-全页-1560.png` / `B-仪表盘卡-1560.png` / `C-图表-1560.png`
- `D-窄屏-1024.png` / `E-图表-1024.png`
- `card-full-1560.png` / `card-compact-1560.png`
- `chart-full-1560.png` / `chart-compact-1560.png`
- `forecast-unknown-brand.png`（跨页降级路）
- `measure.json`（DOM 客观测量原始数据）
