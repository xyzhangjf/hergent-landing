# 商品目标管理 · 需求梳理与开发计划

> 日期：2026-09-24（D1~D4 口径已由你拍板，当日更新）　｜　状态：**口径已拍板 4/5，剩 D5 显示位置**（见第七节，配了图）
> 编号：落地时起号（当前前端已用到 `v259`，建议 **v260** 起编；起号前必做三连搜）

---

## 一、先说结论（四句话）

1. **你的七条需求全部可做，且你举的三个数字例子（9箱=72包 / 加6箱=48包 / 差3箱×10人=30）与系统里的真实换算 100% 吻合** —— 我拿生产库里的真商品（`蒙牛0蔗糖原味百利包150g*5袋*8包`，id=1449）逐条验算过，一条不差。这说明你对这套业务的口径是清楚的，方案可以直接照你的口径落地。
2. **最大的技术坑不在功能本身，在单位换算**。我已按你的提醒把系统的真实换算摸清了，并**抓到两个会导致静默算错的陷阱**（其中一个是 SQL 层面的，会让"有中单位的商品数"从 53 变成 210）。本方案的所有计算都走**现有唯一实现**，不再写第二份。
3. **功能要新增 3 张表 + 补 1 个缺口**，分期做：P0 先把"目标建档 + 自动分解"和"小程序提示"跑通（能立刻用起来），P1 做加单分配与通知。
4. 🔴 **2026-09-24 你订正的加单口径是对的，而且"之前已经实现的那半"就在系统里** —— Forecast 页顶部「返利冲刺看板」的「建议均单」＝ `(目标 − 达成 − 本期报单) ÷ 期次`（把本期已报**摊平**到剩余每期）；你要的 `(目标 − 达成) ÷ 期次 − 本期报单` 是本期**归位**口径。期次 = 15 时两者**相差 93%** ⇒ **不能复用现成的那个数**，必须新算（见 §5.2）。同时核实到两条会让预填值静默算错的硬事实：**本期报单量不能含加单**（`rowFinalQty` 含加单 ⇒ 循环依赖）、**剩余期次不能数期次表**（实测 `forecast_periods` 只 3 行）。

---

## 二、现状核实：系统里的真实单位换算（本需求的地基）

> 你特别提醒"要看系统里的真实单位换算是什么样的"。查完了，先把结论摆出来。

### 2.1 唯一权威 = 商品档案的三级单位（不是规格串）

`products` 表里管单位的列有 6 个，**真实的语义分工是**：

| 列 | 含义 | 生产 tenant_1 实测（285 个在售商品） |
|---|---|---|
| `unit` | **小单位**名 | 瓶 94 · 杯 49 · 组 39 · 袋 34 · **件 32（脏数据）** · 盒 13 … |
| `medium_unit` + `medium_ratio` | **中单位**名 + 每中单位含几个小单位 | 只有 **53** 个商品真有中单位（如「包」，5袋/包） |
| `large_unit` + `large_ratio` | **大单位**名 + 每大单位含几个小单位 | **209** 个商品有大单位：件 202 · 箱 7 |
| `order_unit` | **报单单位**（人工指定，空则回退 `unit`） | 只有 **30** 个商品填了；其中仅 **1 个**与 `unit` 不同 |

**换算层级**（以你最关心的那个商品为例）：

```
蒙牛0蔗糖原味百利包150g*5袋*8包      id=1449   spec=150g*5袋*8包
  小单位 = 袋
  中单位 = 包   medium_ratio = 5    →  1包 = 5袋
  大单位 = 件   large_ratio  = 40   →  1件 = 40袋 = 8包   ★
```

### 2.2 用你的例子反推验证 —— 完全对上

你举的加单例子：

> 「经理加60箱、刘善涛占10%即加6箱即48包，其原报40包即5箱，最终定稿88包即11箱」

拿上表的真实换算算一遍：

| 你的说法 | 系统换算 | 结果 |
|---|---|---|
| 每箱 8 包 | `large_ratio ÷ medium_ratio` = 40 ÷ 5 | **8 包/箱** ✅ |
| 加 60 箱 × 10% = 6 箱 | 60 × 0.10 | **6 箱** ✅ |
| 6 箱 = 48 包 | 6 × 8 | **48 包** ✅ |
| 原报 40 包 = 5 箱 | 40 ÷ 8 | **5 箱** ✅ |
| 最终 5+6 = 11 箱 = 88 包 | 11 × 8 | **88 包** ✅ |

**五个数全部闭环，误差为零。** 你说的"0糖袋"就是 `蒙牛0蔗糖原味百利包150g*5袋*8包` 这类商品（生产库里 `medium_unit='包'` 的一共 8 个，都是同一规格族）。

> 顺带确认另一处：你说「可报15单」—— 生产库 `rebate_target_rules` 里 **「蒙牛低温2026年目标」(id=10) 的 `arrival_count_override` 正好 = 15**（每 2 天到货一次）。所以"可报单数"这个词不是我猜的，系统里真有这个数。

### 2.3 🔴 两个已发现的静默陷阱（不处理，功能上线就会算错）

#### 陷阱 A：SQL 里 `medium_ratio > 0` 是**假判据**，会多算 157 个商品

生产实测：`medium_ratio` 这一列**存了两种类型**——128 行是数字，**157 行是空字符串 `''`**（不是 0）。而 SQLite 的规则是「文本永远大于数字」，所以：

```sql
SELECT '' > 0;          -- 返回 1（true！）
SELECT COUNT(*) FROM products WHERE medium_ratio > 0;              -- 210  ❌ 假阳性
SELECT COUNT(*) FROM products WHERE CAST(medium_ratio AS REAL) > 0; --  53  ✅ 正确
```

**后果**：任何新写的 SQL 只要用 `medium_ratio > 0` 判断"这商品有没有中单位"，就会把 157 个根本没有中单位的商品也算进去 → 均单目标、加单分配、箱数换算**全部算错，且不报任何错**。

> 好消息：前端已有的 `perCase()` 函数里写的是 `Number(arc.medium_ratio) || 0`，JS 的 `Number('')` 是 0 ⇒ **前端是安全的**。所以这是"新代码别踩"的问题，不是"现有代码坏了"。

**本方案的纪律**：所有单位判据收在**一处**（后端 `db/queries/products.py` 新增一个 `unit_levels_sql()` / `per_case()` 实现），SQL 一律用 `CAST(... AS REAL)`，禁止在业务代码里散写 `medium_ratio > 0`。

#### 陷阱 B：小程序报单框显示的单位，与 Web 主表**不同源**

- Web 主表「单位」列 = `order_unit` 优先、空回退 `unit`（`order_unit_master_sql()`）
- 小程序报单框 = `/api/products/fill-search` 下发的是 `p.unit`，**没带 `order_unit`**

生产影响面：30 个填了 `order_unit` 的商品里，**只有 1 个**与 `unit` 不同（id=1596）⇒ 当前风险很低，**但均单目标必须"按用户填报单位显示"**，所以这个源必须先统一，否则会出现"提示说 72 包、输入框单位写着组"这类看不懂的界面。

### 2.4 结论：本功能必须复用的三个唯一实现

| 要算的东西 | 复用哪份实现 | 位置 |
|---|---|---|
| 每箱含几个「某单位」 | `perCase(spec, unit, arc)` | 前端 `Forecast.vue:3132` |
| 报单单位名 | `order_unit_master_sql(i)` | 后端 `db/queries/products.py:46` |
| 「合计(箱)」 | `rowBoxes(r)` / `boxesOf()` | 前端 `Forecast.vue:3177` |

**新代码只调用它们，不再写第二份换算。** 这是本项目已经踩过多次的铁律（同一语义两份来源必然漂移）。

---

## 三、需求逐条梳理

| # | 你的原话 | 系统落点 | 口径定义 | 缺口 |
|---|---|---|---|---|
| 1 | 创建商品目标时提供自动分解，可自由选承接人，支持手动调整分解比例 | **全新**：Web 端新增「商品目标」页 | 目标是「商品 × 月份」一条；分解按**占比**（你明确：不用同比升降，用占比，合计 100%） | 无商品目标表 |
| 2 | 小程序报单框用浅色字提示均单目标，按填报单位显示 | 小程序 `fill.js` 报单行 | 见 §5.1 公式；"已达成"取**达成填报**数值 | 达成填报缺商品维度的单位列 |
| 3 | 低于均单目标时弹窗确认，选"是"直接提交 | 小程序 `_doSubmit()` 已有确认弹窗模式 | 客户端提示，**不做服务端硬拒** | 无 |
| 4 | 加单列**预填**「均单剩余 − 本期报单合计」（2026-09-24 你订正为**总量口径**），悬停看计算逻辑 | Web 端 `Forecast.vue` 加单列 | 见 §5.2 公式；落点**已定**（§7-D5） | 🔴 预填值必须用**不含加单**的报单量（`rowFinalQty` 含加单 ⇒ 循环依赖）；⚠️ 报单表 `store_id` 39 行里 38 行为 0（对策见 §7-D5） |
| 5 | 加单输入框支持负数 | 前端 `numInput` + 后端 `save-matrix` | **经核实：已支持**（见下） | 仅需真机验证 + 视觉提示 |
| 6 | 保存后按比例自动分配加单/减单到人 | Web 端保存汇总表时触发 | 见 §5.3 公式；**加单总量不变，分配是派生** | `forecast_extra_qty` **没有人员维度** |
| 7 | 定稿后向对应人员发送系统通知说明原因 | `message_center`（现有） | 只在分配量 ≠ 0 时发；定向到人 | 无（但有 2 个已知坑，见 §8） |

### 关于需求 5（负数）—— 已核实**当前就可输入**

| 层 | 实现 | 是否放行负数 |
|---|---|---|
| 输入框 | `type="text" inputmode="numeric"` | ✅ 不拦 `-` |
| 前端解析 | `parseNumInput` → `Number('-5')` = `-5` | ✅ |
| 前端计算 | `rowFinalQty = Math.ceil(rowBoxes-1e-9) + rowExtraQty` | ✅ 直接相加，无 `max(0,…)` 夹断 |
| 后端落库 | `float(r.get("extra_qty") or 0)` | ✅ |

⇒ **需求 5 不需要开发，只需要**：① 真机验证一遍（确认没有我没看到的夹断）；② 给负数加个视觉提示（例如标红 + hover 说明"减单"），否则经理填了负数看不出是故意的还是手误。

---

## 四、数据模型设计

### 4.1 `product_targets` —— 商品目标主表（新建）

```sql
CREATE TABLE IF NOT EXISTS product_targets (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  name              TEXT    NOT NULL DEFAULT '',   -- 如「9月0糖袋目标」
  period_month      TEXT    DEFAULT '',            -- '2026-09'（月度桶，唯一键之一）
  product_id        INTEGER NOT NULL DEFAULT 0,
  product_name      TEXT    DEFAULT '',
  brand             TEXT    DEFAULT '',            -- 冗余，便于按品牌筛/查规则
  target_qty        REAL    NOT NULL DEFAULT 0,    -- 目标数量（**一律折算到大单位=箱**）
  target_unit       TEXT    DEFAULT '件',          -- 下发时的大单位名（件/箱）
  target_amount     REAL    DEFAULT 0,             -- 目标金额（元；按金额下发时用）
  basis             TEXT    DEFAULT 'qty',         -- 下发口径：qty(按箱) / amount(按金额)
  order_count       INTEGER DEFAULT 0,             -- 可报单数**快照**（建目标时的值，仅供审计/排查用）
  status            TEXT    DEFAULT 'active',      -- active / closed
  created_by        TEXT    DEFAULT '',
  created_at        TEXT    DEFAULT (datetime('now','localtime')),
  updated_at        TEXT    DEFAULT (datetime('now','localtime'))
);
CREATE UNIQUE INDEX IF NOT EXISTS uq_product_target_month
  ON product_targets(period_month, product_id);
```

🔴 **`target_qty` 一律折算成箱**（大单位）入库。理由：
- 厂商下发就是箱，加单列、定稿、"最终下单(箱)"全部以箱为口径，不引入第三种量纲；
- 舟谱也是"以大单位计 / 以小单位计"二选一，不要多造一层。

🔴 **可报单数走「实时读取」，不走「读快照」**（D2 已拍板"取用户在系统里设置的数"）：每次算均单目标时**现取** `rebate_target_rules.arrival_count_override`（按商品所属品牌）。`order_count` 那一列只在建目标时写一次、**仅供事后解释**"当时为什么算出来 9 箱"，**绝不参与计算** —— 否则用户在设置里改了次数、界面却不跟着变（"我明明改了怎么还是老数"）。

### 4.2 `product_target_alloc` —— 分解明细（新建）

```sql
CREATE TABLE IF NOT EXISTS product_target_alloc (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  target_id     INTEGER NOT NULL,
  employee_id   INTEGER NOT NULL,
  employee_name TEXT    DEFAULT '',
  ratio         REAL    NOT NULL DEFAULT 0,   -- 占比（百分数，0~100）
  target_qty    REAL    NOT NULL DEFAULT 0,   -- 分解目标（箱）= 主表 target_qty × ratio/100
  sort_no       INTEGER DEFAULT 0,
  created_at    TEXT    DEFAULT (datetime('now','localtime'))
);
CREATE UNIQUE INDEX IF NOT EXISTS uq_pt_alloc ON product_target_alloc(target_id, employee_id);
```

**服务端硬校验**：`Σ ratio` 必须 = 100（容差 0.01），否则 422 拒绝。前端实时显示"当前合计 98%，还差 2%"。

### 4.3 `forecast_extra_alloc` —— 加单/减单分配到人（新建）

```sql
CREATE TABLE IF NOT EXISTS forecast_extra_alloc (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  period_start  TEXT    DEFAULT '',     -- 与 forecast_extra_qty 同键
  period_end    TEXT    DEFAULT '',
  product_id    INTEGER DEFAULT 0,
  employee_id   INTEGER DEFAULT 0,
  employee_name TEXT    DEFAULT '',
  target_id     INTEGER DEFAULT 0,      -- 来源目标（可追溯）
  ratio         REAL    DEFAULT 0,      -- 快照：当时的占比
  total_delta   REAL    DEFAULT 0,      -- 快照：经理加单总量（箱，可负）
  alloc_qty     REAL    DEFAULT 0,      -- 分到这个人（箱，可负）
  reason        TEXT    DEFAULT '',     -- 通知里要说明的原因
  created_at    TEXT    DEFAULT (datetime('now','localtime'))
);
CREATE UNIQUE INDEX IF NOT EXISTS uq_fea ON forecast_extra_alloc(period_start, period_end, product_id, employee_id);
```

**为什么不是改 `forecast_extra_qty` 加人员维度**：
- 那个表的语义是"经理对这个商品的加单总量"，经理视角**只有一个数**，改成按人会让"经理加 60 箱"这件事消失；
- 分配是**派生量**，必须能从总量 + 比例重算出来 ⇒ 单独落表，且**每次保存时与总量同事务重算覆盖**（否则改一次加单、分配不跟着变 = 静默漂移，这是 v229"三档价联动"踩过的同族坑）。

### 4.4 缺口：达成填报 `rebate_achievements` 缺「单位」列

`rebate_achievements` **已经支持商品维度**（`dimension='product'` + `scope_key` + `actual_qty`），但：

| 现状 | 说明 |
|---|---|
| 表结构 | `id / period_month / dimension / scope_key / scope_name / actual_amount / actual_qty / actual_rebate / source / note` |
| 生产数据 | 4 行，**全是 `dimension='brand'`，且 `actual_qty` 全 = 0**（只填了金额） |
| 🔴 缺口 | **`actual_qty` 没有单位列** —— "150" 到底是 150 箱还是 150 包？没人知道 |

**建议补一个 `actual_unit` 列**（vNNN 迁移），并约定 `dimension='product'` 时 `actual_qty` 的单位必须显式记录。**否则需求 2 的公式会因为没有分母单位而算错**。

---

## 五、核心算法（唯一实现，禁止第二份）

### 5.1 均单目标（需求 2）

```
① 已达成(箱)      = 达成填报该商品本月的 actual_qty × 换算到箱      ← D1 已拍板
                     └ 未填 / 空 ⇒ 视为 0（照常算，不退化成"提示不出来"）
② 已发生次数      = 🔴 **不能数期次表**（实测 tenant_1 的 forecast_periods 总共只有 3 行、
                     全是 9 月 09-20~09-24，8 月 0 行 —— 期次表根本没有历史）
                     ⇒ 改按「整月到货日历」数到今天为止已发生的到货日个数
③ 剩余可报期次    = 「本月到货次数」(设置值) − 已发生次数            ← D2 已拍板
                     ├ 取值：rebate_target_rules.arrival_count_override，**按商品所属品牌取**
                     │       生产实测：蒙牛低温 15 ／ 简爱 8（两个品牌不同，不可全表共用一个数）
                     ├ 整月到货日历与配置面板「未来期次预览」**同源**
                     │   （后端 `domain/arrival_schedule.py::compute_arrivals`，别再写第二份）
                     └ 下限 1；为 0 时提示"本月已无可报单数"
④ 均单剩余(箱)    = ( 月目标(箱) − 已达成(箱) ) ÷ 剩余可报期次
                     └ 语义 =「从本期起，每期平均该报多少才能补齐月目标」
⑤ 显示值          = 均单剩余(箱) × perCase(填报单位)               ← 复用唯一实现
```

> 🔴 **② 是本轮新暴露的硬冲突（2026-09-24 实测）**：你设的 `arrival_count_override` 是 **15**，
> 而系统里 9 月的报单期次**只有 3 条**。若按"15 − 期次表里的 3 = 12"算剩余，会得出"9 月还剩 12 期"
> —— 而 9 月只剩 6 天。**必须走整月到货日历**，否则均单会被算小一大截（1254 元 vs 4000+ 元这种量级差）。

**⑤ 的换算表**（拿 id=1449 走一遍，月目标 150 箱、后续可报 15 单、已达成 15 箱）：

| 用户填报单位 | perCase | 显示值 |
|---|---|---|
| 件（大单位） | 1 | 9 件 |
| **包（中单位）** | `large_ratio ÷ medium_ratio` = 40÷5 = **8** | **72 包** ← 与你说的"9箱即72包"完全一致 |
| 袋（小单位） | `large_ratio` = 40 | 360 袋 |

> ⚠️ **关于"150 箱 ÷ 15 单 = 10 箱，而你说'约 9 箱'"** —— 差额来自「已达成」那一项：(150 − 15) ÷ 15 = **9 箱** ✅ 正是你说的数。**公式（先减已达成、再除剩余单数）是你原话里的，我没有改动**；D1/D2 拍板后分子分母都有了确定来源，数字自动对上。

**边界处理**（必须全部处理，否则又是一个"静默失效"）：

| 情况 | 显示 |
|---|---|
| 该商品本期无目标 | 不显示提示（不猜、不填 0 冒充） |
| 换算不出来（档案缺 `large_ratio` 且规格串解析不出） | **不显示数字**，改显示"该商品缺单位换算，无法提示" ← 绝不能静默当 1 |
| 已达成 ≥ 月目标 | "本期目标已达成" |
| 后续可报单数 = 0 | "本期已无可报单数" |

### 5.2 加单列「预填值」（需求 4 —— 2026-09-24 你订正了口径）

> **你的原话**：「web端加单那一列应该预填 **该商品目标减去实际达成除以剩余可报期次减去本期报单合计**，也就是我们之前应该已经实现的**均单剩余减本期报单**」
>
> ✅ **核实结论：你对，我原来的口径要作废。** 下面把「为什么对」「和现行实现的差别」「必须钉死的三条红线」一次讲清。

```
预填值(箱) = ( 月目标(箱) − 已达成(箱) ) ÷ 剩余可报期次  −  本期报单合计(箱)
             └──────────── 均单剩余（= §5.1 ④） ────────┘   └── 本期已报（不含加单）──┘

① 可正可负：正 = 本期还该加这么多；**负 = 本期报超了，该减**（这正是需求 5「支持负数」的用武之地）
② 纯派生量，**不落库**（落库只落经理确认后的最终加单总量 → forecast_extra_qty）
③ 每期重算：剩余期次随日期递减 ⇒ 同一目标在月初要求低、月末要求高（这是"月目标要达标"的必然）
```

#### 拿你的 0糖袋 例子验算（自洽版）

月目标 150 箱、每箱 8 包、月内 15 期，本期还没人报单（**这就是生产实测的现状**：本期 period 18 报单 = 0）：

| 情形 | 均单剩余 | 本期报单合计 | 预填值 | 换算成"包" |
|---|---|---|---|---|
| 达成未填（D1⇒0）、本期未报 | (150−0)÷15 = 10 箱 | 0 | **10 箱** | **80 包** |
| 达成填了 15 箱、本期未报 | (150−15)÷15 = **9 箱** | 0 | **9 箱** | **72 包** ← 与你说的"约9箱即72包"完全一致 |
| 达成填了 15 箱、本期已报 12 箱 | 9 箱 | 12 | **−3 箱**（该减 3 箱） | −24 包 |

> ✅ 第二行印证了你的直觉：**"9 箱"这个数正是因为"已达成 15 箱"那一项**。公式（先减达成、再除剩余期次、再减本期已报）全部是你原话，我只做了核实与去歧义。

#### 与现行「返利冲刺看板」的口径差别（别混用）

系统里**已经有一个**"均单"，在 Forecast 页顶部「返利冲刺看板」（`rebateSprint.perOrder`）。它和你要的**不是同一个数**：

| | 公式 | 语义 | 结论 |
|---|---|---|---|
| 现行冲刺看板 `perOrder` | `(目标 − 达成 − 本期报单) ÷ 期次` | 把本期已报**摊平**到剩余每一期 | ✅ **这个口径本身没错**（"后面每单平均要报多少"），**不要去改它** |
| **你的口径（本次订正）** | `(目标 − 达成) ÷ 期次 − 本期报单` | 本期**归位**到均单 | ✅ 加单预填的**正确**口径 |

🔴 **两者相差 `本期报单 × (1 − 1/期次)`** —— 期次 = 15 时**差 93%**。所以：**现成的 `perOrder` 不能直接拿来当预填值**，必须新算一个派生量。

**为什么你的口径对**：加单格影响的**就是本期这一单**。把本期的超报/少报摊销给未来每一单（现行做法），会让"本期该加多少"这个数失真。

#### 与你原需求 4 举例口径的差别（"各业务员差额合计"）

你上次举例是「某业务员只报 8 箱差 3 箱、10 人各差 3 箱则显示 30」。这是**按人累加**；本次订正成**总量相减**：

| | 按人累加（原举例） | **总量相减（你的订正）** |
|---|---|---|
| 公式 | `Σ_人 max(0, 应报_人 − 已报_人)` | `均单剩余 − 本期报单合计` |
| 有人超报时 | **不抵扣**（超报的白超，缺口被高估） | ✅ **自动抵扣**（多报的顶掉少报的） |
| 要不要按人拆应报量 | 要（还得先定"每人应报多少"，没有依据） | ✅ **不要** |
| 与落库粒度是否一致 | ❌ 不一致 | ✅ 一致（`forecast_extra_qty` 唯一键 = 产品×期次，**本来就没有人员维度**） |

⇒ **总量口径更准、更简单、且与落库粒度天然一致**。按人明细**降级为"悬停解释用"**，不参与计算（见下）。

#### 悬停展示的计算逻辑（你要求的"鼠标悬停时展示"）

```
预填加单 = −50 箱        ← 负数即"本期报多了，该减"
├ 均单剩余 30 箱 = (月目标 150 − 已达成 0) ÷ 剩余可报 5 期
│      ↑ D1：达成未填按 0        ↑ D2：本月到货 15 次 − 已发生 10 次
├ 本期报单合计 80 箱（不含加单）= 本期各家门店实报之和
└ 30 − 80 = −50 箱
   ── 参考（只解释、不参与计算）：各人本期实报
      刘善涛 8 箱 · 张俊峰 8 箱 · …（共 10 人）
```

#### 🔴 实现三条红线（写错任一条就是静默算错）

| # | 红线 | 为什么 |
|---|---|---|
| **1** | **「本期报单合计」必须用"不含加单"的报单量**（`rowBoxes(r)` / 后端 `SUM(quantity)`），**绝不能用 `rowFinalQty`** | 现行 `rowFinalQty = Math.ceil(rowBoxes) + rowExtraQty` —— **它本身含加单**。用它会让预填值自己吃自己（填了加单 ⇒ 报单合计变大 ⇒ 预填值变小 ⇒ …），**形成循环依赖** |
| **2** | **「剩余可报期次」不能数期次表**（见 §5.1 ②） | 实测 `forecast_periods` 只有 3 行、8 月 0 行。数出来 3，实际已发生 10+ 期 ⇒ 均单被放大数倍 |
| **3** | **换算一律走 `perCase` 唯一实现**，并按用户当前填报单位显示 | 三级单位（袋/包/件）混用必错，见 §2 |

### 5.3 加单/减单按比例分配（需求 6）

```
① 经理在「加单(箱)」填一个总量 D（可正可负）
② 对每个承接人 i：  alloc_i = D × ratio_i / 100
②' 🔴 D3 已拍板：**减单夹断，最低到 0**
    alloc_i = max( alloc_i , −该人本期已报箱数 )
    ⇒ 某人只报 5 箱、按比例该扣 6 箱时，**实际只扣 5 箱**（落到 0，不为负）
③ 分到人的填报单位值 = alloc_i × perCase(该人的报单单位)
④ 该人定稿量 = 该人本月已报(箱) + alloc_i        ← ≥ 0，由 ②' 保证
```

⚠️ **夹断引出"要不要重分配"的子问题** —— 详见 §7-D3（我推荐：夹断的部分再分给有余量的人，保证"经理说减 60 就真减 60"；若你要简单版则是夹断后不再分）。

拿你的例子验算（D = +60 箱，刘善涛 ratio = 10%，报单单位 = 包）：

```
alloc = 60 × 10% = 6 箱
显示  = 6 × 8 = 48 包          ✅
原报  40 包 = 5 箱
定稿  5 + 6 = 11 箱 = 88 包     ✅
```

**舍入纪律**（必须写死，否则总和对不上被用户投诉）：
- 分配额**保留 2 位小数**，不取整 —— 取整会导致 `Σ alloc_i ≠ D`；
- 若 `Σ ratio ≠ 100`（历史脏数据），**按比例归一化后再分**，并回写一份归一化后的快照；
- 展示层显示"约 N 箱"，但**入库/通知用精确值**。

---

## 六、开发计划（分期）

### P0 —— 让功能立刻能用（目标建档 + 小程序提示）

| 层 | 改动 | 文件 |
|---|---|---|
| 后端 | 建 `product_targets` / `product_target_alloc` 两张表 + `_safe_migrate('vNNN_…')` | `server/erp_db.py` |
| 后端 | 单位判据唯一实现（`per_case` / `unit_levels_sql`，SQL 用 `CAST(... AS REAL)`） | `server/db/queries/products.py` |
| 后端 | `product_target_create / update / list / get / delete` + 分解校验（Σ=100） | `server/erp_db.py` |
| 后端 | 新增路由 `/api/product-targets`（list/get/create/update/delete/auto-split-ratio） | `server/routers/` 新文件 |
| 后端 | 补 `rebate_achievements.actual_unit` 列（vNNN 迁移） | `server/erp_db.py` |
| 后端 | 均单目标查询：`/api/product-targets/avg-target?period_id=&product_ids=` | 同上 |
| 后端 | 打通 `order_unit` 到 `/api/products/fill-search` | `server/routers/data.py` |
| 前端 | 新页面「商品目标」（列表 + 新建弹窗 + 选人 + 比例表格） | `hergent-cn-v2/src/pages/ProductTarget.vue`（新） |
| 前端 | 三处接线：router / Shell 侧栏 / `api/modules.js` | 见技能 `hergent-cn-v2-add-module` |
| 小程序 | 报单行浅色提示均单目标 | `pages/fill/fill.wxml` + `fill.js` |
| 小程序 | 提交前低于目标的确认弹窗 | `pages/fill/fill.js::_doSubmit` |

**验收**：沙箱租户建目标 → 分解 Σ=100 校验生效 → 小程序看得到提示 → 提示数字与手算一致（用 id=1449 验算 8 包/箱）。

### P1 —— 加单列预填 + 加单分配 + 通知

| 层 | 改动 |
|---|---|
| 后端 | 建 `forecast_extra_alloc` 表 |
| 后端 | `save-matrix` 保存时**同事务**重算分配并覆盖写（幂等） |
| 后端 | 预填值接口：按 (期次×商品) 返回 `{预填值, 均单剩余, 本期报单合计, 剩余期次, 每人实报}` |
| 后端 | 定稿时发通知（`message_send(..., recipients=账号id, event_key=...)`） |
| 前端 | 加单列**预填** + 建议态标记（未确认）+ hover 展示计算过程 |
| 前端 | 负数视觉提示 |
| 前端 | 分配结果可查看（点开看"谁分到多少、为什么"） |

**验收**：经理加 60 箱 → 刘善涛收到"分到 6 箱（48包）"通知 → 分配明细求和 = 60（误差 < 0.01）→ 改加单量再保存，分配跟着变（无漂移）。

### P2 —— 收尾

- 达成填报页支持商品维度录入（含单位）
- 目标达成看板（目标 vs 已报 vs 缺口）
- Excel 导入商品目标
- 目标与实际达成的一致性校验脚本

---

## 七、口径决策项（D1~D5 **全部已定**）

> 2026-09-24 你已拍板 D1~D4，落地口径逐条写在各条下面。**D5 我换了写法 —— 见本节末尾，那里配了图。**

### D1 ✅ 已拍板 —— "已达成"取「达成填报」；**没填时视为 0**（不退化成空）

**你的原话**：「"已达成"取「达成填报」，如果这个数没有填就以均单数量为目标」

**落地口径**：

```
已达成 = rebate_achievements.actual_qty（取 dimension='product' 的行）
         ├─ 有值      → 代入公式
         └─ 未填 / 空 → 视为 0（照常算，不是"提示不出来"）

均单目标 = (月目标 − 已达成) ÷ 剩余可报单数
         = (150 − 0) ÷ 15 = 10 箱   ← 达成未填时，结果就是这个
```

**"以均单数量为目标"这句我理解为**：达成没填 ⇒ 不扣减，直接用 `月目标 ÷ 可报单数` 当均单目标。
⇒ 这样**功能永远不会因为没人填达成而空着** —— 你原本担心的"新的人工动作会退化"这个风险，被这句话直接解决了。

⚠️ **两个实现细节**（我先记下，不用你答）：
1. `rebate_achievements` 现在**没有「单位」列**（见 §4.4），而要减的目标是"箱" ⇒ 填数时必须能换算。我会给它补一列 `unit`。
2. 达成填报页现在**只能按品牌填**（生产 4 行全是 `dimension='brand'`）；要按商品填，得先把商品维度在页面上打开。

### D2 ✅ 已拍板 —— "可报单数"取**系统里设置的那个数**

**你的原话**：「"可报单数"取用户在系统里设置的数」

**落地口径**：系统里这个设置**已经存在**，就是返利规则 → 到货节奏（L2）里的 **「本月到货次数」**：

| 品牌规则 | 字段 | 生产实测值 |
|---|---|---|
| 蒙牛低温2026年目标 | `rebate_target_rules.arrival_count_override` | **15** |
| 简爱9月目标 | 同上 | **8** |

⇒ 正好是你在需求 2 里说的"可报 15 单"，**口径与你心里那把尺完全一致**。

🔴 **两个必须注意的点**（这是核实出来的真实现状，不是猜测）：

1. **它是「品牌维度」的，不是全局一个数** —— 蒙牛 15、简爱 8，两个品牌不一样。所以要按**商品所属品牌**去拿这个值，不能全表共用一个。

2. **它是「整月次数」，公式要的是「剩余单数」** —— 所以：

```
剩余可报单数 = 本月到货次数(设置的数) − 本月已发生的报单期次数
             = 15 − 3 = 12   ← 9月目前的真实值（期次 16/17/18 已发生）
```

**这样"均单目标"才会随月份推进自然变小** —— 月初每单要求低、月末要求高，符合你"除以后续可报单数"的原话。

⚠️ 若某品牌**没设**这个数（`arrival_count_override` 为空）怎么办：回退到按到货周期实时推算（每 2 天一次 ⇒ 从今天到月末还有几单）。**但这是兜底，不是主路径** —— 兜底值会随日期漂移（今天看 9 箱、明天看 10 箱），所以只在该品牌确实没配置时启用，并在提示里标明"（按节奏推算）"。

### D3 ✅ 已拍板 —— 减单**不允许扣到负数，最低到 0**

**你的原话**：「不能减单扣到负数，最低到0」

**落地口径**（加单不变，只给减单加一道下界）：

```
alloc_i = D × ratio_i / 100                       （D 为负时就是减单）
alloc_i = max( alloc_i , −该人本期已报量 )          ← 夹断：扣完不能低于 0
该人定稿量 = 该人已报 + alloc_i  ≥ 0               ← 结果永不为负
```

**举例**：经理减 60 箱，某人占比 10% ⇒ 该扣 6 箱，但他只报了 5 箱 ⇒ **实际只扣 5 箱，落到 0**。

⚠️ **夹断会引出一个我无法替你决定的小问题 —— 要不要"重分配"？**

| | 做法 | 后果 |
|---|---|---|
| **A（我推荐）** | 被夹断的那部分，**再按比例分给还有余量的人**（迭代到分完为止） | 经理说减 60 箱，就**确实减掉 60 箱**；账对得上。代价：算法多一层循环，且"某人的实际扣减比例"会与他名义占比略有出入 —— 所以**通知里显示的是实际扣减数，不是名义比例算出来的数** |
| B（你的字面口径） | 夹断后**不再分** | 经理说减 60，实际可能只减掉 45 箱（有 15 箱夹断了）⇒ **"我说减 60 怎么只减了 45"**，且与"加单总量"那一栏对不上 |

**我推荐 A** —— 因为减单的目的是"把总量压到某个数"，夹断后不分就达不到目的，而且会让加单总量这一栏长期对不上账（后端 `forecast_extra_qty` 存的是总量，两边不一致会变成一个反复被问的问题）。
**如果你要的就是 B（简单、可预期）**，回 `B` 即可，我照做。

### D4 ✅ 已拍板 —— 目标挂「月」，期次**自动认领**

**你的原话**：「目标挂"月"，期次自动认领」

**落地口径**：

```
存法：product_targets.period_month = '2026-09'     ← 只存年月，不存期次 id

认领：某期次 order_start 的年月 == period_month ⇒ 该期次就用这份目标
      （一个月 15 个期次共用同一份目标，不是每个期次各一份）

🔴 均单目标每期**重算、不缓存** —— 因为分子分母都在随月推进变化：
   月初  已达成 0 箱， 剩余 15 单 → (150 − 0) / 15 = 10.0 箱
   报了3单后 已达成 24 箱，剩余 12 单 → (150 − 24) / 12 = 10.5 箱
   ⇒ 同一个月内，越往后每单要求越高（这是"月目标要达标"的必然结果，不是 bug）
```

⚠️ **边界**：跨月的期次（如 8/31 报、9/1 到）**按 `order_start` 归月** —— 与系统里既有的期次归属口径一致，不另立规则。

⚠️ **一个真实风险要先说清**：这个公式里**分子靠人工填（达成填报）、分母靠系统算**。如果达成填报长期没人填（=0 处理），那均单目标会**随剩余单数减少而单调上升**（150/15 → 150/12 → 150/9…），月末可能涨到很高。
⇒ **对策**（我会实现，不需要你操作）：在提示旁标明"已达成：未填报"，并在 Web 端目标页给一个"达成填报待补"的计数提醒。**看到了就去填，没填也不会算错，只是提示的数字偏保守。**

### D5 ✅ 你已订正 —— 加单列**预填**「均单剩余 − 本期报单合计」

**你的原话**：「web端加单那一列应该**预填** 该商品目标减去实际达成除以剩余可报期次减去本期报单合计，也就是我们之前应该已经实现的**均单剩余减本期报单**」

**一句话**：加单格里**直接有一个数**（= §5.2 的预填值），经理看一眼、要么照用要么改。这个数就是"本期还该加多少箱才能让月目标达标"。

| | 现在 | 按你的订正 |
|---|---|---|
| 加单格 | 空的（`placeholder="·"`），凭经验填 | **有数**：如 `10`（箱）或 `−3`（负数=该减） |
| 经理的动作 | 想 → 填 | **看一眼 → 照用 / 改 / 清空** |
| 负数 | 要自己算才知道该减 | 预填值本身是负的，**一眼看出"报超了"** |

#### 🔴 这里有一个必须先定的点：预填值**算不算数**？

加单是**真实写库**的（`forecast_extra_qty`，唯一键 = 产品×期次）。所以"预填"有两种做法，代价差很多：

| | 做法 | 经理不管它会怎样 | 评价 |
|---|---|---|---|
| **A（推荐）** | 预填值**显示为建议态**（浅色底），经理**编辑过**才成为真实值；保存时若仍有"未确认的建议值"，**弹一次确认**："将按建议值给 N 个商品加单，确认？" | 保存前**被拦一次**，不会静默加单 | ✅ 符合你一贯的"AI **只建议、不擅自动手**"铁律（与 §5.3 分配同理） |
| **B（你的字面口径）** | 预填即真实值，经理不改就是它 | **系统自动替他加单**（含自动减单） | 最省事，但"经理没注意到"就会产生一批非本意的加减单，且**事后很难分辨**是他同意的还是系统填的 |

**我推荐 A** —— 预填**照做**（格子里就是有数，你的诉求完全满足），只是给这个数加一道"未确认"标记 + 保存前一次确认。**这不增加操作步骤**（照用时就是多按一次"确认"），但把"系统替他做了决定"这件事变成"他确认过"。

#### 悬停展示（你要求的"鼠标悬停时展示计算逻辑"）

按 §5.2 的**总量口径**展开，并附按人实报**仅供解释**：

```
预填加单 = −50 箱        ← 负数：本期报多了，该减
├ 均单剩余 30 箱 = (月目标 150 − 已达成 0) ÷ 剩余可报 5 期
│      ↑ D1：达成未填按 0        ↑ D2：本月到货 15 次 − 已发生 10 次
├ 本期报单合计 80 箱（不含加单）
└ 30 − 80 = −50 箱
   ── 参考（只解释、不参与计算）：刘善涛 8 箱 · 张俊峰 8 箱 · …（共 10 人）
```

#### ⚠️ 按人明细的数据链（只用于悬停解释，**不再是计算路径**）

原需求 4 的说法是"各业务员差额合计"。按本次订正，**合计值不再按人算**（§5.2 已说明原因），但"悬停看逐人实报"这条数据链仍然要用，我核实过生产上它是通的：

```
① 目标分解（新建表）      员工 A、B、C… + 各自占比
        ↓
② 该员工报了哪些单        report_mapping.employee_id → report_alias（报单别名）
                          ⭐ 这个值就是报单表里的 store_name
        ↓
③ 实报数量               forecast_submissions（store_name / period_id）
                          ⨝ forecast_submission_items（product_id / quantity / unit）
        ↓
④ 折成"箱"               ÷ perCase（判据须用 CAST(...AS REAL)，见 §2.3 陷阱 A）
        ↓
⑤ 逐人实报（悬停参考）    某人本月/本期报了 X 箱
```

**生产实测的印证**（期次 17 的真实数据）：
- `report_mapping` 5 行，其中 `员工6 → report_alias='刘善涛'`（本人仓 / 调拨单）、`员工7 → '东津'`（永辉东津店 / 自提订单）—— **别名就是报单表里的 store_name，两边同源** ✅
- 所以「刘善涛报了多少」= `forecast_submissions where store_name='刘善涛'` 之下 items 的数量和 ✅

🔴 **一个必须先修的隐患**：报单表 `forecast_submissions.store_id` **39 行里 38 行是 0**，只有 `store_name` 有值 ⇒ 目前**只能靠名字匹配**。名字（`report_alias`）是用户在报单配置里自己维护的，两边同源所以能用，但**一旦改名就会断链**（改名后历史报单归不到人）。
⇒ **对策**：本次开发**顺带把 `store_id` 写对**（新报单落库时带上 `counterparty_id`），并对存量做一次名字↔ID 对账，对不上的在目标页给出告警。**这是 P0 的一部分，不是可选项** —— 否则"逐人实报"从第一天起就有一批人算不准。

---

## 八、风险清单（动手前必读）

| # | 风险 | 后果 | 对策 |
|---|---|---|---|
| R1 | `medium_ratio > 0` SQL 假阳性（157 行空串） | 中单位判断多算 157 个商品，**所有换算静默算错** | 判据收口到一处，SQL 一律 `CAST(... AS REAL)` |
| R2 | 小程序与 Web 单位不同源 | 提示说"72包"、输入框写"组" | P0 打通 `order_unit` 到 fill-search |
| R3 | `message_send` 第 4 位是 `sender` 不是 `recipients` | 通知发给**所有人**（工资条踩过，全公司看到实发金额） | 收件人**必须用关键字** `recipients=` |
| R4 | 分配明细与加单总量不同事务 | 改一次加单，分配不跟着变 = 静默漂移 | 同事务重算覆盖；加一致性断言 |
| R5 | `Σ ratio ≠ 100`（历史脏数据 / 手工改） | 分配总和对不上 D | 服务端硬校验 + 归一化兜底 |
| R6 | 新增后端路由被小程序共用 | 破坏性兼容 | 改前核查 11 个共享接口（项目既有铁律） |
| R7 | 目标月份与期次窗口跨月 | 月末的期次算进下月目标 | 明确归属键，写测试断言 |
| R8 | 均单目标"无目标时显示 0" | 用户看到"每单 0 箱"以为系统坏了 | 无目标**不显示**，不填 0 冒充 |
| R9 | 🔴 报单表 `store_id` **39 行里 38 行为 0** | 「各业务员实报量」只能靠**名字**匹配，改名即断链 ⇒ **有人算不准且不报错** | P0 顺带写对 `store_id` + 存量名字↔ID 对账 + 目标页告警（见 §7-D5） |
| R10 | 可报单数**是品牌维度**的（蒙牛 15 / 简爱 8） | 若全表共用一个数，另一品牌的商品目标整列算错 | 按商品所属 `brand` 取；品牌取不到时不猜，走"按节奏推算"兜底并标注 |
| **R11** | 🔴 预填值误用**含加单**的报单量（`rowFinalQty = 报单合计 + 加单`） | 预填值**自己吃自己**：填加单 ⇒ 报单合计变大 ⇒ 预填值变小（**循环依赖**），且零报错 | 一律用 `rowBoxes(r)` / 后端 `SUM(quantity)`；写单测断言"预填值不随加单格变化" |
| **R12** | 🔴 「剩余可报期次」去**数期次表**（实测 `forecast_periods` 只 3 行、8 月 0 行） | 实际已发生 10+ 期却数出 3 ⇒ 剩余被**高估** ⇒ 均单被算小一大截 | 走整月到货日历（`compute_arrivals`）数"今天及以后"的到货日；期次表**只作展示、不作计算** |
| **R13** | 把现行冲刺看板 `perOrder` 直接当归单预填值复用 | 两者相差 `本期报单 × (1 − 1/期次)`，期次=15 时**差 93%** ⇒ 加单量严重偏小 | 新算一个派生量；`perOrder` 口径**保持不动**（它回答的是"后面每单平均要报多少"，本身没错） |
| **R14** | 预填值"未确认即生效" | 经理没注意到 ⇒ 系统替他加了/减了一批单，事后**分不清是他同意的还是系统填的** | 见 §7-D5：建议态标记 + 保存前一次确认 |

---

## 九、舟谱截图对照（逐张）

| 截图 | 舟谱怎么做 | 我们怎么做 | 差异理由 |
|---|---|---|---|
| 1 基本信息配置 | 目标名称 / 起止时间（按月）/ 目标指标（金额、数量等 8 种）/ 拆解方式（到员工/到客户/不拆解）/ 选员工 | 目标名称 / 月份 / 口径（按箱 or 按金额）/ 拆解到员工 | 只保留你要的口径；**不做"拆解到客户"**（本需求只到人） |
| 2 选择员工弹窗 | 可选 ↔ 已选 双栏 + 全部选择 | 同款双栏 | 直接照抄交互 |
| 3 设定目标 | 商品池 + 数量计算方式（大单位/小单位）+ 目标设定参考（同期销量）+ 员工×月表格 + 预设/批量设置 | 商品 + 目标数量 + **承接人×占比**表格 | 🔴 **你的明确要求：不用「同期上升/下降」，改用占比**，合计 100% |
| 4 选择商品弹窗 | 可选 ↔ 已选 双栏 | 同款双栏 | 照抄 |
| 5 规则设定（同比升降 X%） | 「按 2026年8月 商品净销售数量 上升 5%」自动算 | **不采用** —— 改为手工填目标 + 按占比分解 | 你的原话：「不用对比同期上升/下降，我们用占比」 |
| 6 确认目标 | 拆解详情表（每人 150，合计 1200） | 同款确认页，合计必须等于目标 | 照抄 |

---

## 十、开工前 checklist

- [x] ~~你回 D1~D5 五个决策~~ → **D1~D4 已拍板**；**D5 已由你订正为"加单列预填 = 均单剩余 − 本期报单合计"（2026-09-24）**
- [ ] 🔴 **只剩一个待你定**：预填值**算不算数**（建议态 + 保存前确认 / 直接生效）—— 见 §7-D5，回 `A`（推荐）或 `B`
- [ ] 生成「目标管理」小程序与 Web 的字段级测试清单（照项目铁律：改行为必须同步改测试清单）
- [ ] 起号：三连搜（后端 `grep -e "v26X" -e "V26X"` + `memory/` + `tools/`，**两个仓库都要搜**）
- [ ] 确认 `rebate_achievements` 是否要补 `actual_unit` 列
- [ ] 确认本功能是否需要先在小范围验证（按你"先验证再产品化"的一贯节奏）

---

*本方案的所有换算结论均来自 2026-09-24 对生产 `tenant_1.db` 的只读查询，未修改任何生产数据。*
