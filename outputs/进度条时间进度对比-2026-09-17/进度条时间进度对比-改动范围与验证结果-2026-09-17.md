# 进度条「时间进度对比」文案 — 改动范围与验证结果

日期：2026-09-17 ｜ 页面：本期预报 → 返利冲刺看板 → 明细表「进度」列
文件：`hergent-cn-v2/src/pages/Forecast.vue`

---

## 一、交付

| # | 你的要求 | 结果 |
|---|---|---|
| ① | 三种状态文案 | 落后「**落后时间进度 X 个百分点**」／持平「**与时间进度持平**」／超前「**超过时间进度 X 个百分点**」 |
| ② | X 为具体差值、**动态计算准确** | 取**真实差值**（1 位小数、整数不带 `.0`）：实测 4.706 个百分点 → 「4.7」；0.417 → 「0.4」 |
| ③ | 空间充足 → 直接显示 | 1680 / 1440 / 1280 三档均**直接显示、零截断**（实测 `scrollWidth == clientWidth`） |
| ④ | 空间不足 → 悬停提示 | 列被压到放不下时**隐藏文字**（不显示半截），进度条挂 hover 提示，含双方数值 |
| ⑤ | 文案风格统一、清晰可读 | 11.5px（与既有 `.sprint-sum .muted` 同族）；颜色复用 `--dan/--suc/--t3` |

### 文案样式（与进度条同源着色）

| 状态 | 文案 | 颜色 | 与进度条一致 |
|---|---|---|---|
| 超前 | 超过时间进度 4.7 个百分点 | 绿 `--suc` | 进度条绿 ✓ |
| 落后 | 落后时间进度 0.4 个百分点 | 红 `--dan` | 进度条红 ✓ |
| 持平 | 与时间进度持平 | 灰 `--t3` | 进度条绿（按「未落后」处理）|

---

## 二、变更（`Forecast.vue`）

1. **新增 `sprintPaceOf(ach)`** —— 三态判定的**唯一**实现。返回 `{state, pp, cls, text, hover}`；
   前置条件：`ach != null` 且时间进度 `frac > 0`（到货月未开始不做比较，返回 `null` 不渲染）。
2. **`sprintBarClass()` 改为复用 `sprintPaceOf()`** —— 此前着色单独写了一份 `ach >= frac`，
   会让差 0.03 个百分点的一行出现「文案写落后、进度条却判绿」的自相矛盾（同屏口径必须同源）。
3. **`sprintTimeProgress` 增 `pct1`（1 位小数）**，页头「本月时间进度」与决策横幅改用它 ——
   否则文案里的差值（0.4）与页头整数（57%）对不上，用户自己一算就是矛盾的。
   （旧整数版 `pct` 至此**已无任何引用**，保留未删：删它需再走一次「重建 + 部署生产」，
   而收益只是清掉一个死字段，不抵再次触碰生产的风险。已登记为「下次因别的原因重建 `Forecast.vue` 时顺手清理」。）
4. **进度单元格新增 `.sp-pace` + 自适应测量**：
   判据 = `el.scrollWidth <= el.clientWidth`（真实 DOM 尺寸，不用 canvas 估算）；
   放不下时加 `.is-hidden`（`visibility:hidden`，保留占位 → 行高不跳动、显隐不引起表格重排）。
   重测时机：`onMounted` / `window.resize` / `[rebateSprint, rebateSprintOpen, sprintTimeProgress]` 变化。
5. **模板**：`.sp-bar` 挂 `:title="paceHint(s)"`（**仅放不下时**返回提示，避免与可见文字重复）；
   CSS 新增 `.sp-pace` 五条规则（含三个状态色）。
6. **注释修正**：初版 CSS 注释写「`width:100%` ⇒ 文案不会撑宽该列」，实测**撑了 72px**（见下），已改为实测数值。

### 一个需要你知道的布局事实

`nowrap` 文案会被浏览器按 min-content **撑宽该列**：进度列 **168px → 240px**，其他列各缩约 7px。
这**正是文案能直接显示的原因**（撑到刚好放下），所以没有"修"它。
实测四档视口：**其他列均未换行、页面无横向滚动**；1100px 档容器内出现横向滚动，
但那是改前就有的（各列 min-width 合计 795 > 容器 772）。

---

## 三、构建 / 部署

- `npm run build`（`CODEBUDDY_SAFE_DELETE_ENABLED=0`）→ **最终上线产物**
  `assets/Forecast-B8x2rYFo.js`(276733B) / `assets/Forecast-CzJKAYe2.css`(70040B)
- **差集核查**：26 个文件「字节数相同但 md5 不同」→ 归一化（抹平 chunk 名 hash）后**逐字节相同**，
  判定为纯 hash 漂移；**真变化只有 2 项** —— `Forecast.js`(+1239) 与 `Forecast.css`(+449)，即本次改动 ⇒ **零夹带**
- 部署 → `/opt/hergent-cn-v2/`（`rsync --delete`，`legal/` 两侧一致未被删），`chown -R hergent:hergent`
- **两侧一致（53 个文件集合比对，无「仅本地有 / 仅生产有 / 同名不同 md5」）**，关键 5 项：

  | 文件 | md5 |
  |---|---|
  | `assets/Forecast-B8x2rYFo.js` | `63a0997cc0f347fcd311d57ebb77300d` |
  | `assets/Forecast-CzJKAYe2.css` | `9b329b271a96820488dd2dec54b9a51a` |
  | `assets/index-Bwn2NmXH.js` | `5b6ec0cab0e6b01139e27bb9719b5557` |
  | `assets/Shell-bB48Lxj4.js` | `fccf35e42ed8098a84e478f4aab50b1c` |
  | `index.html` | `2e5662fda61dffa4c5188afaf57162ea` |

  ⚠️ 比对方式：`find` 出来的清单**两侧排序不同**，`diff` 会报一堆「偏移」噪音 ⇒ **必须按集合比对**，
  不能逐行 diff（这是本项目已知的跨平台排序坑）。
- 生产此前是 9-16 23:06 的构建（**已含**并行会话的改动）⇒ 本次构建只增不减，未回退任何既有功能

---

## 四、验证结果

| 层 | 工具 | 结果 |
|---|---|---|
| 真机（线上包） | `.workbuddy/tools/forecast-sprint-pace-verify.js`（本轮新建，24 项断言） | **24 / 24 PASS** |
| 纯逻辑（源码真切片） | `paceNum` / `sprintPaceOf` 三态 + 边界 + 同源，35 项 | **35 / 35 PASS** |
| 改动前基线（before） | 同一探针 `MODE=before` | 6 / 6（`.sp-pace` 计数 **0** ⇒ 改动前确无此文案） |
| 回归 v178 | `forecast-brand-dup-verify.js`（条码重复按品牌判定） | **20 / 20 PASS** |
| 回归 v174/v175 | `forecast-errlist-verify.js`（查错清单与标记） | **41 / 41 PASS** |
| 布局副作用 | 4 视口扫描（1680/1440/1280/1100） | 其他列**零换行**、页面**零横向滚动** |

### 关键实证（真机，非估算）

```
页头「本月时间进度：56.7%」
行1 蒙牛低温  目标¥674,000 达成¥413,649 → 61.372%  vs 56.667%  真实差 +4.706pp  → 「超过时间进度 4.7 个百分点」（绿）
行2 简爱      目标¥ 80,000 达成¥ 45,000 → 56.250%  vs 56.667%  真实差 -0.417pp  → 「落后时间进度 0.4 个百分点」（红）
```
- 差值由**表内金额 + 虚线位置**独立复算，不读组件状态 ⇒ 显示值与复算值一致（容差 0.06 内）
- 压窄进度列至 103px：文案**全部隐藏**（0/2 可见）、进度条挂上
  `超过时间进度 4.7 个百分点（达成 61.4% · 时间进度 56.7%）`；复原宽度后可见性回到原状（**自适应可逆**）
- hover 里的「时间进度 56.7%」与页头**同源同值**；文案可见时进度条**不再**挂冗余提示
- 零 console 错误、零 4xx/5xx

### 本轮顺带修好的探针缺陷（与产品无关，但会让回归"假红"）

1. 既有探针假设「进页面选中的期次就有报单记录」—— 实测沙箱当期「9月15日报单9月19日到货」**为空**，
   有数据的是「9月提审期-开放填报」（29 行）。`enterEdit` 在空期次上**不报错、只 toast** ⇒
   网格零行 → 断言 FATAL `no col`，**看起来像功能坏了**。已改为遍历期次挑有数据的那个（reload 后切回同一个）。
2. 虚拟滚动下远处行不可点（`Node is either not clickable or not an Element`）⇒ 样本行限定前 12 行。
3. v178 的 G 段断言「网格行品牌非空数 == 档案商品品牌非空数」是**过强假设**（两者是不同集合：
   网格 158 行 / 档案 285 个），昨天凑巧都 245 才绿 ⇒ 改判据为**网格内非空占比 ≥90%**（实测 156/158 = 98.7%）。

---

## 五、需确认事项

1. **单位用「个百分点」而不是「%」** —— 按你此前定下的规范：两个百分数**相减**的结果单位是百分点，
   写成 % 会被读成比值（经销商拿去对账会出错）。若你确实想要「%」，改一处字符串即可
   （`' 个百分点'` → `'%'`），我可以立即改。
2. **页头「本月时间进度」由整数改成 1 位小数**（57% → **56.7%**）—— 这是为了让页头数字与文案差值**能互相对上**。
   如果你更想保留整数 57%，那文案的差值就得改成整数口径（精度会降）。
3. **持平阈值**：差值 < **0.05 个百分点**视为持平（1 位小数下本就显示不出差异）。
4. 进度列宽了 72px，其他列相应收窄（均未换行）。若你希望进度条本体更窄、把宽度留给文案，可单独调。
