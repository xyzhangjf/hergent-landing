# 经营工作台 · 删除「近 7 天销售趋势」面板 — 交付说明

- 日期：2026-09-18
- 页面：hergent.cn `#/workbench`（`Workbench.vue`，侧栏「经营工作台」）
- 提交：`4966bd7`（`laozhangai-product` main）· 3 files · +311 / −34 · spec `fe-v190-workbench-trend`
- 线上包：`Workbench-BbFK4Eqc.js` 10,352 B / `Workbench-Czs2paaM.css` 3,913 B
- 回滚快照：`/opt/_bak/hergent-cn-v2-v190-before-20260918-205411.tar.gz`（507,303 B）

---

## 一、交付

| 项 | 内容 |
|---|---|
| 面板渲染内容 | ✅ 已删（模板整块 + 6 项 DOM 断言为 0） |
| 数据请求 | ⚠️ **一个都没删** —— 该面板**没有独立请求**（见第三节），删了会打坏 KPI 与「经营趋势」页 |
| 组件 / 配置引用 | ✅ 脚本层 4 个符号 + 样式层 5 条规则全删，零残留 |
| 布局空位 | ✅ 已消除（宽屏 475.3px → 0；判别性对照见第四节） |
| 报错 | ✅ 无未捕获异常；6 个路由冒烟全过 |

## 二、变更

### 1. 模板（`Workbench.vue`）
删除原第 95–104 行的 `trend-card` 整块（注释 + 卡片 + 面板头 + 柱状图容器）。

### 2. 脚本
删除 `trend` / `trendMax` / `trendMaxLabel` / `barHeight`。
**保留** `dashData`（`kpis` 与 `isEmptyTenant` 在用）与 `fmt`（`kpis` 在用）——二者删掉会让 KPI 横条与空账套判据一起坏。

### 3. 样式
删除 `.trend-card` / `.trend-chart` / `.tc-bar-wrap` / `.tc-bar` / `.tc-label`，以及两个 media 断点里的对应项。

> 🔴 `.tc-` 前缀被「今日经营要务」的 `.tc-body` / `.tc-title` / `.tc-text` / `.tc-acts` **共用**，
> 删时必须逐个精确匹配，不能用前缀通配（否则会打掉主块的排版）。

### 4. 布局修正（本轮真正的价值）
原布局 `today-panel{span 8; grid-row:span 2}` + `trend-card{span 4}`：第 1 行由两者拼满，
第 2 行 `today-panel` 独自续占左 8 列、**右侧 4 列靠 trend 顶住**。只删面板不动布局 ⇒ 第 2 行右侧必留空洞。

| 修法 | 说明 |
|---|---|
| `.expiry-card` 加 `grid-row:span 2` | 拉满右侧两行，与 `today-panel` 的跨度对齐 |
| 两卡各加 `:class="{'span-all': 对方不存在}"` | 覆盖**四种显隐组合**，任一卡缺失时留下那张铺满整行 |
| `@media(max-width:1200px)` 下 `.expiry-card` 改整行 | 它原为 `span 3`、与已删的 trend-card 拼一行；网格降为 6 列后只剩自己会右侧空 3 列 |

> ⚠️ 未用 `:has()` —— scoped 样式 + 全局 `.bento` 的组合下可读性差、难排查。
> 当前生产租户 `expiryData` 为空（无近效期预警），实际生效的正是「预警缺失 ⇒ today 铺满」这条分支。

## 三、🔴 为什么「相关数据请求」一个都没删

用户原话要求「删除相关数据请求」，但**正确处置是一个都不删**：

- 该面板**没有独立请求**。它的 `trend` 来自 `/api/dashboard/today-profit` 的**同一个响应**，
  与 KPI 四个数（`sales`/`profit`/`orders`/`payment`）**同源** ——
  后端 `server/routers/dashboard.py:77-102` 一次返回 `{profit, sales, orders, payment, trend}`。
- `trend` 字段**仍被 `Dashboard.vue`（路由 `/dashboard`「经营趋势」页）消费**。

⇒ 删请求会连带打掉 KPI 横条；删后端字段会打掉 `/dashboard`。**前后端均未改动，本次 diff 不含任何 `.py`。**

## 四、验证（三层，全绿）

### 1. 静态
- 模板标签配平：`div 41/41`、`span 10/10`、全标签 `ALL BALANCED`
- `<script setup>` 经 `node --check` 通过
- 残留扫描：`trendMax` / `barHeight` / `tc-bar` / `tc-label` / `trend-chart` 全为 **0**（只剩解释性注释）

### 2. 部署前差集（`dist_normalized_diffcheck.py`）
27 项纯派生噪声 / **真变化仅 `Workbench`（css −450、js +60）**。
`Forecast-HASH.js` 归一化后 289622 vs 289622 **逐字节相等** ⇒ 本轮零夹带、未回滚其他会话产物。

### 3. 真机验收（新增 `.workbuddy/tools/workbench-trend-removal-verify.js`）
**改前 14 passed / 1 failed ⇒ 改后 21 passed / 0 failed**

🔴 判别性对照（**同一点位、同分辨率**）：

| 断言 | 改前 | 改后 |
|---|---|---|
| `today` 右边界 vs `.bento` 右边界 | 1204.7 vs 1680 ⇒ **空 475.3px** | 1680 vs 1680 ⇒ **铺满** |
| `elementFromPoint(1442, 816)` | 命中 `.bento` 本身（`isBentoItself:true`）| 命中 `today-card`（`inToday:true`）|

命中目标从「容器」变成「卡片」比「宽度数字变大」有力得多 —— 但只有在**改前真的留了空**时才测得出来，
所以「先跑一次改前探针」这一步不能跳。

其余：面板零残留 6 项 ✓、无未捕获异常 ✓、KPI 仍 5 项 ✓、AI 晨报仍在其下 ✓；
1100px 下网格降为 6 列且两卡各整行 ✓；`HG_SMOKE=1` 多页冒烟 **6/6**
（`/forecast` `/dashboard` `/rebate` `/loss-accounting` `/archive/products` `/connect`）
—— 因 scopeId 变化导致 chunk 整体换名，必须确认其他页引用未断。

### 4. 双侧 md5（三文件一致）
`b3a7d17f0923454e20ed401c629933b7`（index.html）
`89ccac51ea348472071a7e1b76d04af7`（Workbench js）
`85a5aabd739615580227e3bd93830598`（Workbench css）

## 五、对齐效果

- 面板与其全部引用消失，页面从 5 块卡片变成 4 块（KPI 横条 / 今日经营要务 + 近效期预警 / AI 晨报）。
- **无残留空位、无死 CSS**：`trend-card` 在 JS 与 CSS 中均为 0；`span-all` + `grid-row:span 2` 已生效。
- 空位只存在于宽屏（>1200px）；窄屏（1100px）改前改后截图**逐字节相同**
  （md5 `01ad749622d1f59657233251bec2f5b9`）—— 因为窄屏下 `today-panel` 本就整行。

## 六、需确认事项

1. 🔴 **线上原本已是「半成品」**：并发会话在 20:52:09 部署过一版 `Workbench-BD_QvAxa.js` ——
   模板与脚本**已删**，但 CSS 里**仍留着 5 条死规则**、`.expiry-card` 也没有 `grid-row:span 2`
   ⇒ 真机实测**留了 475.3px 空位**。本提交把它补完整并覆盖之。
2. 🔴 **同名易混页面**：`/dashboard`「经营趋势」页有一个独立 SVG 图，标题为
   「**近 7 天销售 / 毛利趋势**」（`Dashboard.vue`，同样读 `today-profit` 的 `trend`）。
   本轮**保留未动**（用户明确说的是「经营工作台页面」）。若那个也要处理，请再指一句。
3. **号冲突**：`v190` 裸号被并发会话的 `fe-v190-caseprice` 占用，本 spec 用 `-wb-trend` 后缀区分；
   `Workbench.vue` 内注释写的是「v190」，与对方同号但**文件不同** ⇒ 追责按文件区分（同 v187 的处理）。
   改注释会连带改 scopeId（须重构建重部署），本轮从简保留。
4. **可选优化**：`today-profit` 每次要跑 7 次「按天查毛利」的循环；既然工作台已不再用 `trend`，
   可考虑加 `?trend=0` 让工作台跳过它。**本轮未做**（`/dashboard` 仍需该字段，属新增能力而非删除）。

## 七、归档文件

| 文件 | 说明 |
|---|---|
| `01-宽屏1440-改前-today右侧空475px.png` | 改前：today 右边界 1204.7，右侧空 475.3px |
| `02-窄屏1100-改前（与04逐字节相同）.png` | 改前窄屏：本就整行，无空位 |
| `03-宽屏1440-改后-铺满无空位.png` | 改后：today 右边界 1680 = bento 右边界 |
| `04-窄屏1100-改后（与02逐字节相同）.png` | 改后窄屏：与 02 逐字节相同 ⇒ 窄屏不受影响 |
