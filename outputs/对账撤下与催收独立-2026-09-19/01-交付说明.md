# 撤下「三步对账向导」+「催收跟进」独立成页 — 交付说明（2026-09-19）

**编号** v197 ｜ **spec** `fe-v197-recon-retire` ｜ **仓库** laozhangai-product

---

## 一、交付

| 项 | 内容 |
|---|---|
| 撤下 | `/reconciliation` 三步对账向导页面（页面移除，4 处入口全部换掉） |
| 上线 | 新增 `/collections`「催收跟进」独立页 |
| 断链保护 | 旧链接 `#/reconciliation` → 自动重定向 `#/collections` |
| 后端 | **零改动**（`/api/reconciliation/*` 与 `/api/collections/*` 均照旧） |
| 验收 | 真机 **41/41 全绿**；双侧 md5 七项全等 |

**为什么撤**（依据见 `outputs/对账模块存废与AI归属评估-2026-09-19/`）：
界面上的「系统应收余额」与匹配结果里的「系统算出」**取的不是同一个口径**（同一客户同屏差 17.7 万），
且上线以来 39 次访问**全是查看、确认 0 次**（`audit_logs` 0 条）。
页面上唯一真正在用的功能是「催收跟进」（514 次真实动作）—— 这才是被独立出来保住的东西。

**为什么后端接口不删**：重做方案要以它为存量入口做灰度；客户历史对账记录还在库里。

---

## 二、变更

### 新增

- `hergent-cn-v2/src/pages/Collections.vue`（催收跟进独立页）
- `.workbuddy/tools/reconciliation-retire-collections-verify.js`（真机探针，41 条断言）

### 删除

- `hergent-cn-v2/src/pages/Reconciliation.vue`（369 行，整文件移除；git 历史可恢复）

### 修改（4 处入口 + 2 处收尾）

| 文件 | 改动 |
|---|---|
| `src/components/Shell.vue` | 侧栏入口 `#/reconciliation`→`#/collections`，文案「对账工作流」→「催收跟进」，图标换 Lucide `phone`；移动端抽屉同步 |
| `src/components/CommandPalette.vue` | 命令面板同一项替换（`icon: 'audit'` → `'phone'`） |
| `src/pages/Workbench.vue` | 催收待办跳转 path → `/collections`；待办图标「账」→「催」 |
| `src/router/index.js` | 路由替换 + 旧链接 redirect + 撤下原因注释 |
| `src/api/modules.js` | `reconciliationApi` 加注释说明「页面已撤、接口暂留」，并写明该组接口为何不适合承载三类场景 |
| `src/pages/Collections.vue` | 页头业务化（「谁欠我钱 · 欠了多久 · 下一步做什么」）；「未收总额」补 `¥` 单位（原页面只有裸数字） |

**入口枚举过程**：原以为 3 处，实际**4 处** —— 经营工作台「今日待办」里的催收项也指向 `/reconciliation`
（`Workbench.vue` 的 `todoItems`）。若漏掉它，用户从工作台点催收会落到一个已撤下的路由。

---

## 三、构建 / 部署

- **隔离构建**：源码 rsync 到 `/tmp/hg-v197-build` + `node_modules` 软链（不污染共享 `dist/`）
- **部署前差集核查**：归一化后 **20 项纯派生噪声 / 9 项真变化**，其中 5 项净 +0（minify 别名重排）、
  4 项净非 0 全部归属本轮：`Shell` +301 / `Workbench` −3 / `index` +28 / `modules` −274
- **零夹带证明**：对 4 个产物做**中文字符串集合差** —— 新增仅「催收跟进」、删除仅「对账工作流」，
  **没有一个不属于本次改动的字符串**
- **单侧文件**：线上独有的 `Reconciliation-*.js/.css` 已由 `rsync --delete` 清除；新增 `Collections-*.js/.css`
- **备份**：`/opt/hergent-cn-v2-backup-1789782333.tar.gz`（509,527 字节）
- **双侧 md5 七项全等**：

| 文件 | md5 |
|---|---|
| `index.html` | `dacff594d9c4f63c7e73a704bf1b94a6` |
| `Collections-DA7w-P57.js` | `de9fa5d4f851bffceb33c056c3d23328` |
| `Collections-c_HJGy93.css` | `8955ca0a203ec60814dc44dea18fa1b5` |
| `Shell-Ep2C5KKI.js` | `8d37e7236d42467031ae34969ed3bc41` |
| `index-jOXAX6iM.js` | `0d8b8b94b8a46e79ce1afd787e88f84f` |
| `Workbench-D5T6Ltkw.js` | `b1a6ad2e0c4612e5cb6d6b66a10ce9c5` |
| `modules-Du7lIpUK.js` | `30cb4c33309d441d8f8273d3f1cd4125` |

线上入口：`index-C5JGFrvo.js` → **`index-jOXAX6iM.js`**

---

## 四、对齐效果（真机 41/41）

| 组 | 断言 | 关键实测值 |
|---|---|---|
| A 侧栏（桌面） | 5 | 13 项，「催收跟进」在「算工资」之后（**位置不变**，净增 0 项）；无 `/reconciliation`、无「对账工作流」；图标 = Lucide `phone` |
| B 新页 | 11 | 点侧栏 → `#/collections`；标题「催收跟进」；**100 行真实数据 / 400 个动作按钮**；未收总额 `¥1,264,088`；两接口均 200 |
| C 旧链接 | 2 | `#/reconciliation` → `#/collections`，渲染催收页 |
| D 命令面板 | 4 | 列表含「催收跟进」、无「对账工作流」，点击跳转正确 |
| F 工作台待办 | 3 | 「催：永辉民发店 177,014 元 逾期 993 天」→ 跳催收页 |
| E 移动端抽屉（390px） | 4 | 抽屉含 `/collections`、无 `/reconciliation` |
| G 其它页面冒烟 | 7 | forecast / rebate / loss-accounting / payroll / archive / settings / dashboard 全部正常 |
| H 控制台 | 4 | 零 `pageerror`、零非 403 控制台错误、零 404 |

**只读保证**：全程未点「刷新队列」（`POST /api/collections/rebuild`），不做任何写操作。

---

## 五、需确认事项

1. **`sales` 角色点「催收跟进」会看到 403 提示** —— 催收接口属 `accounts` 权限模块，
   `sales` 角色无权限（实测 `403 权限不足：你的角色'sales'不能访问'accounts'模块`）。
   **这是既有闸门、与本轮改动无关**（原对账页调同样两个接口，行为一字未变），
   但侧栏**不按权限隐藏菜单项**，所以销售点进去会看到空页 + 报错提示。
   → 是否要在侧栏按角色隐藏该入口？（本轮未动，属既有设计）
2. **`/api/reconciliation/*` 暂留未删** —— 等重做方案落地后一并处理（见技术方案文档）。
3. 并发会话在途改动（`Shell.vue` 10 个 hunk / `modules.js` 8 个 / `router/index.js` 2 个）
   **未纳入本次提交**，按 scoped 提交纪律逐 hunk 排除。
