# v122 · R5 修复 + 字段增强 交付说明

日期：2026-09-08　方案：**A（品牌页双模式）**　状态：**已部署生产并真机验证通过**

---

## 一、为什么要做（R5 是线上真实隐患）

Step 0 摸底发现：真实租户 tenant_1 的 4 条品牌规则**全是 `period_type=month`**：

| 规则 | 品牌 | 口径 | 目标 | 生效期 |
|---|---|---|---|---|
| 蒙牛低温8月目标 | 蒙牛低温 | month | 100 万 | 8/01–8/31 |
| 简爱8月目标 | 简爱 | month | 8 万 | 8/01–8/31 |
| 蒙牛低温9月目标 | 蒙牛低温 | month | 100 万 | 9/01–9/30 |
| 简爱9月目标 | 简爱 | month | 8 万 | 9/01–9/30 |

即：**你本来就是按月建目标的，不是按年。**

而 v121c 把品牌页锁成「12 个月分解唯一模式」，`save()` 里强制 `period_type='year'`、`target_value = 各月之和`。
于是编辑「蒙牛低温9月目标」时：填 9 月 100 万 → 保存变成**年度 100 万** → **返利基数少算 12 倍**。

这是**线上现已存在**的隐患，本次一并解决。

---

## 二、方案 A 落地：品牌页双模式

品牌目标支持两种模式，按规则自身 `period_type` 回填，**新建默认年度**：

| 模式 | 适用 | 字段 | `period_type` | 落库 |
|---|---|---|---|---|
| **年度（按 12 个月分解）** | 按年谈返利 | 目标年度 + 全年目标 + 全年返利率 + 12 月表 | `year` | `monthly_amounts` / `monthly_rates` 有值 |
| **单期（月，单值）** | 按月定目标 | 目标年度(选填) + 本期目标金额 + 触发方式 + 返利 | 保留原值（默认 `month`） | 月度分解清空 |

**切换行为**：切到单期时不清空 `monthlyRows`（切回年度数据还在，防误触丢数据）；保存时才按当前模式决定写不写月度分解。原「年」口径切单期会自动降为「月」，避免"年口径 + 单值"的矛盾组合。

---

## 三、改动清单

### 后端（hergent-erp）
| 文件 | 改动 |
|---|---|
| `server/erp_db.py` | 租户库迁移元组 +2 列：`target_year INTEGER`、`target_unit TEXT`（幂等，11 个租户库全生效，42→44 列） |
| `server/routers/rebate_rules.py` | `_row_to_dict` 加 2 字段（白名单①） |
| | `validate_rule` 加解析与校验：年度 2000–2100 整数；单位 ≤16 字符；`cleaned` 加 2 字段 |
| | `INSERT` 41→43 列 / `UPDATE` 40→42 项（占位符同步） |
| | ⚠️ `update_rule` **局部更新白名单**补 `target_year` / `target_unit`（白名单③，详见「坑」） |

### 前端（hergent-cn-v2 · `src/pages/Rebate.vue`）
| 项 | 改动 |
|---|---|
| 品牌双模式 | 新增 `brandMode` / `isBrandForm` / `switchBrandMode()`；`monthlyOn` 改为 `品牌 && 年度模式` |
| 模式回填 | `fillMonthlyFromRule()`：有月度分解 或 `period_type==='year'` → 年度；否则单期（**不再强行拆分月/季口径**） |
| 目标年度 | 年度模式必填（默认当前年 2026，保存前校验 2000–2100）；单期模式选填 |
| 数量单位 | 商品/数量度量时显示「数量单位」（箱/件/提） |
| 商品默认度量 | 由 `quantity` 改为 **`amount`**（按你的拍板） |
| 商品选择器 | 接上已有 `productRefs`：商品名候选从 **0 → 12** 个；保存时 `scope_name` → 商品 ID（`resolveProductKey`），不再出现"填了名字但 scope_key 空 = 作用于全部单品" |
| 商品页裁剪 | 商品维度**整块隐藏「到货与报单节奏」**（报单是品牌方排产节点，与单品无关） |
| 品牌页裁剪 | 隐藏「目标度量」选择器（品牌只能金额，选了会被后端拒） |

---

## 四、⚠️ 本轮踩到的第三个白名单（重要）

新增列写了 `_row_to_dict`（读）、`INSERT`/`UPDATE`（写），但**保存后 `target_year` 仍是 None**。

排查到 `update_rule` 里还有一份**局部更新白名单**（L1039 起的 `for k in (...)` 元组）——不在这个元组里的字段，即使 body 传了也会被 `merged` 覆盖回旧值，**静默丢弃**。

至此本项目已有 **三处白名单**，新增列必须全部登记：

1. `_row_to_dict`（读回）
2. `INSERT` / `UPDATE` 的 SQL 列与占位符
3. **`update_rule` 的局部更新字段元组**（本次新增认知）

---

## 五、验证结果（全部真机 Chrome + demo boss，tenant_10）

| # | 场景 | 结果 |
|---|---|---|
| 1 | 编辑月口径规则 #1（蒙牛8月目标） | 自动回填 **单期**模式 ✅；12 月表不显示 ✅；金额 0.0065 万（原 65 元）✅ |
| 2 | 改 100 万后保存 | `period_type` **仍是 month** ✅（旧版会被改成 year）；`target_value=1000000` ✅；`monthly_amounts` 空 ✅ |
| 3 | 编辑年度规则（有 12 月分解） | 回填 **年度**模式 ✅；12 行表格 ✅；目标年度 2026 ✅；全年目标 1250 万 ✅ |
| 4 | 新建品牌目标 | 默认年度模式 ✅；目标年度默认 2026 ✅ |
| 5 | 年度 → 切单期 | 12 月表消失 ✅；「本期目标金额」出现 ✅ |
| 6 | 新建单期规则保存 | 落库 `period=month / tv=500万 / year=2026 / 无月度分解` ✅ |
| 7 | 商品目标页 | 到货报单节奏**已隐藏** ✅；周期口径 / 目标度量保留 ✅；商品名候选 **12 个**（原 0）✅；默认度量 `amount` ✅ |
| 8 | 后端服务 | `active`；health 200；11 个租户库 44 列，`target_year`/`target_unit` 齐备 ✅ |

构建：`Rebate-CXXEBOFn.js` 135.12 kB；前端 `index.html` 200；后端生产文件与本地 md5 一致。

---

## 六、数据影响与清理

- 测试数据「真机验证-12月分解」「真机验证-单期模式」已**物理删除**。
- ⚠️ demo 租户 #1「蒙牛8月总目标」在验证中被我从 **65 元改为 100 万元**（原值 65 元明显是测试残留，不可能是月目标）。如需还原请告知。
- 存量规则**零改写**：仅回填时不拆分、不强制转年度；只有用户主动切换模式或保存才变更。

---

## 七、遗留（下一批）

1. **文件级拆分未做**（`BrandTargetForm.vue` / `ProductTargetForm.vue` + 共用底座）。本轮做的是**字段层裁剪与双模式**，属于拆分前必须先定的语义问题（否则拆完仍不知道品牌页该有哪些字段）。拆分建议下一步进行。
2. **品牌约束仍写在前端 `save()`**（品牌=年度/金额/达成即返/比例）。API 直调可绕过 → 应下沉到 `validate_rule`（规划 Step 4）。
3. 商品维度已隐藏报单节奏区，若将来有商品规则带 `order_*` 配置，编辑保存会清零 —— 摸底确认当前**商品规则 0 条**，无影响。
4. 两仓改动**仍未 commit/push**（连同 v121/v121b/v121c 及历史未提交项）。
