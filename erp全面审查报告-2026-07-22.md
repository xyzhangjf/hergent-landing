# erp.hergent.cn 产品全面审查报告

> **审查对象**：erp.hergent.cn（Web ERP，v90，代码库 `/Users/zhangjunfeng/Documents/hergent-erp/`）
> **审查日期**：2026-07-22
> **审查维度**：产品对标 / 定位匹配 / 前端优化 / 后端优化 / 优先级
> **数据来源**：实地代码审计（index.html、app.js、styles.css、63 个 router、erp_db.py、core.py、db/connection.py、import_router.py）+ 7-19 四路专项审计 + v90 Phase1 安全报告

---

## 〇、TL;DR（给老板的一句话）

**功能覆盖面已经接近用友/金蝶的中小版本，但"像不像一个低温奶经销商专属系统"还差得远。** 当前它是一个「通用 ERP 外壳」，而我们要卖的是「AI 原生、低温奶专属、双模式、五角色」。差距主要在四块：① 行业专属能力（FEFO/临期/专属价）没做成「默认开启、开箱即用」；② 五角色的数据级权限没隔离；③ UI 只有暗色、导航太满、没有浅色默认；④ AI 入口（企业微信 Bot）还没建，AI 目前只是「报表里的图表」不是「替你干活的人」。

---

## 一、产品对标分析（vs 用友 / 金蝶 / SAP）

### 1.1 功能广度对比

| 能力域 | 用友 U8 / 金蝶云星空 | SAP B1 | **Hergent 现状** | 差距判断 |
|---|---|---|---|---|
| 总账/凭证/期末结转 | ✅ 企业级 | ✅ | ✅ 重度（voucher_templates/period_close/consolidation） | 持平 |
| 应收/应付/对账 | ✅ | ✅ | ✅（bi-dashboard/reports） | 持平 |
| 进销存（采/销/存） | ✅ | ✅ | ✅（trade-mega/wms-console） | 持平 |
| 批次/序列号追溯 | ✅ 通用 WMS | ✅ | ⚠️ 有 batch-trace 但**非 FEFO 业务化** | 偏弱 |
| 生产/BOM/MRP | ✅ 强 | ✅ 强 | ❌ 仅 assembly 简单装配 | 不需要（SMB经销） |
| 人力资源/薪资 | ✅ | ⚠️ | ✅（salary-ui/visit-tracking） | 持平 |
| CRM/客户 360 | ⚠️ 需配 | ⚠️ | ✅（customer-360） | **领先** |
| BI 报表/自定义报表 | ✅ | ✅ | ✅（bi-dashboard/report-designer） | 持平 |
| 审批流/BPM | ✅ 企业级 | ✅ | ⚠️ bpm-designer 存在但**未深度集成业务** | 偏弱 |
| 多组织/集团合并 | ✅ 强 | ✅ 强 | ⚠️ 多租户隔离靠分库，非集团架构 | 不需要（SMB） |
| **移动原生 App** | ✅ | ✅ | ❌ 仅 Web 响应式（无 iOS/Android 原生） | 偏弱 |
| **AI 原生（预测/自动录单/主动推送）** | ⚠️ 外挂模块 | ⚠️ 外挂 | ⚠️ forecast-console 存在但**非主交互** | 机会窗口 |

### 1.2 功能缺失 / 薄弱环节（Hergent 相对巨头）

🔴 **（高）行业专属能力没做成「默认能力」**
- 用友/金蝶是「通用工具 + 行业包」。Hergent 要赢，必须把**低温奶经销的专属逻辑**做成开箱即用的默认流程，而不是藏在 88 个模块里。
- 当前 `batch-trace.js` 有批次追溯，但「先到期先出（FEFO）」「临期自动折扣建议」「按终端的 8024 条专属价自动匹配」都还停留在「数据能存」阶段，没有变成「下单时自动触发」的业务规则。

🔴 **（高）AI 是「图表」不是「员工」**
- 巨头（含用友的「AI 助手」）的 AI 都是「问答/图表」形态。Hergent 的差异点必须是「替你干活」——但目前 `forecast-console.js` 只给预测曲线，没有「点确认 → 自动生成蒙牛采购单」的闭环，更没有企业微信 Bot 入口。

🟡 **（中）审批流/BPM 未深度集成业务**
- `bpm-designer.js` 能画流程，但采购单/退款单/调价单是否真的走审批流？从代码看业务端点与 BPM 是两套，未打通。

🟡 **（中）移动原生 App 缺失**
- 经销商的业务员/司机在手机上高频操作（开单、签收、查库存）。Web 响应式可用，但扫码签收、离线开单、推送通知体验不如原生。低温奶场景移动是刚需。

🟢 **（低）生产/MRP 能力**
- 用友/金蝶强，但 Hergent 目标用户是「经销商」不是「工厂」，不需要。这是**主动不做**，不是短板。

### 1.3 对标结论

**不要和巨头拼功能广度（拼不过，也不该拼）。** Hergent 的胜算在「巨头做不好的三件事」：① 低温奶短保冷链的 FEFO/临期/专属价行业逻辑；② AI 原生「替你干活」而非「给你图表」；③ 中小企业用得起的月费 + 微信入口。这三点巨头都是「外挂模块」级别，Hergent 应做成「产品内核」。

---

## 二、产品定位匹配（vs 我方定位与目标用户）

**我方定位**（来自产品方向决策）：蒙牛低温奶经销商的 AI 管理系统，双模式（AI+传统），五角色（业务员/导购/老板/会计/司机），四核心场景（需求预测→采购单 / FEFO+临期预警 / 专属价自动匹配+退货损耗 / 每日盈亏快报）。

| 定位要素 | 当前产品契合度 | 差距 |
|---|---|---|
| 低温奶行业专属 | ❌ 低 | 通用 ERP 外壳，无低温奶术语/流程预设 |
| 双模式（AI+传统） | ⚠️ 部分 | 传统模式成熟；AI 模式仅有 forecast-console，无微信入口、无语音/拍照录单 |
| 五角色权限 | ❌ 低 | 仅 admin/boss/accountant/sales 四角色，无导购/司机；**数据级权限未隔离** |
| 四核心场景 | ⚠️ 部分 | 底层数据能力都在（批次/价格/预测/报表），但**未串成「开箱即用」的场景闭环** |
| 中小企业用得起 | ✅ 高 | 月费定位 + 微信入口方向对，但微信 Bot 未建 |

### 核心判断

**当前产品是「通用 ERP 能跑」，不是「低温奶经销商 AI 系统能用」。** 最大风险是：功能越多，离「低温奶老板一眼就懂」越远。建议把 88 个模块按角色裁剪为「5 个角色各自的主路径」，行业专属逻辑做成默认开关。

---

## 三、前端优化

> 数据：`static/index.html` 108 个 `<script>` 同步标签、`static/js` 合计 2.4MB、`app.js` 7466 行单体、`styles.css` 6148 行、93 个 JS 模块、暗色模式为唯一主题（浅色 token 为死代码）。

### 3.1 加载性能

🔴 **（高）108 个 script 标签全部同步加载，无 defer/async**
- `static/index.html` 内 108 个 `<script src>` 串行阻塞解析，首屏 JS 2.4MB 全量加载。
- **改进**：① 拆分「核心壳（登录/导航/首页）」与「业务模块」，核心壳即时加载，业务模块按路由懒加载；② 引入打包工具（Vite）做 tree-shaking + 代码分割；③ 至少给非首屏 script 加 `defer`。

🔴 **（高）app.js 7466 行单体无构建**
- 改一个按钮样式要在这 7k 行里找位置，无模块热更新、无压缩。
- **改进**：新功能强制走 `js/modules/`（已有铁律），中长期用 Vite 把 93 个模块 + app.js 打包成 2-3 个 chunk。不重写 app.js，但在其顶部只保留启动器和路由分发。

🟡 **（中）无资源预加载 / 缓存策略**
- 静态资源无 hash 指纹，浏览器缓存失效靠手动刷新。
- **改进**：构建产物加 `contenthash`，`Cache-Control` 长缓存 + 版本号刷新。

### 3.2 响应式适配

🟡 **（中）移动端有适配但「Web 响应式」≠「移动体验」**
- 已有 768px 断点 + 底部 tab + 卡片视图，但业务员/司机的高频操作（扫码、手写签名、离线）Web 响应式做不到。
- **改进**：司机/业务员两条线优先做「轻量 PWA」或小程序，而非把 Web 塞进手机。

🟢 **（低）表格在手机上仍是横向滚动**
- 大数据表格移动端体验差。建议移动端用「卡片列表 + 关键字段」替代整表。

### 3.3 UI/UX 一致性

🔴 **（高）只有暗色模式，浅色 token 是死代码**
- `app.js:4220` 注释 `Dark-only theme — light mode removed v75`。经销商在仓库/车上/阳光下用手机，暗色可读性差。
- **改进**：恢复浅色为默认（`variables.css` 的 `:root.light` token 仍在，工作量小），暗色作为可切换选项。

🔴 **（高）导航约 20 个一级入口，认知负荷过高**
- 老板登录看到「总账维度/期间关账/合并报表/收入确认/会计准则」等不相关入口。
- **改进**：按角色动态生成导航（现有 `buildSidebar` 已支持 `perms` 过滤）。老板只看 5 个、业务员只看 3 个、司机只看 2 个。

🟡 **（中）设计令牌未 100% 贯彻**
- `styles.css` 6148 行里仍有散装 magic number 和 inline style（`index.html` 内联 `style=` 残留）。
- **改进**：收口圆角到 3 级（8/12/20px）、字号到 5 级（12/14/16/20/28）；内联 style 全部类化。

🟡 **（中）缺新手引导（Onboarding）**
- 经销商老板第一次打开 ERP 会懵。
- **改进**：首次登录 3 步向导（加商品 → 导客户 → 录第一单），用 `onboarding.js` 模块 + modal。

🟢 **（低）加载态只有 spinner，无骨架屏**
- 列表/报表 loading 用 skeleton 比 spinner 感知更快。

### 3.4 数据展示逻辑

🟡 **（中）报表「全量加载」无虚拟滚动上限**
- 大客户/大订单量时前端渲染可能卡。建议列表超 500 行启用虚拟滚动（app.js 已有虚拟滚动能力，需默认开启）。

🟡 **（中）错误态不统一**
- 部分接口失败前端仍按成功渲染（后端 HTTP 200 + `{"success":false}` 混合）。
- **改进**：见 4.5，前后端统一错误协议。

---

## 四、后端优化

> 数据：63 个 router、约 728 端点、`erp_db.py` 677 函数 / 10633 行、229 张表仅 2 个外键 / 26 个索引、`server/` 裸 `except:` 已降至 18 处（v90 清理）、Pydantic schema 仅 4 个文件（auth/inventory/order/product）。

### 4.1 API 设计

🔴 **（高）728 端点仅 4 个 Pydantic 模型，其余零输入校验**
- 绝大多数端点 `d = await request.json()` 后直接 `d.get("field")`，无类型/必填/长度/范围校验。
- **改进**：① 优先给写操作（create/update）端点补 Pydantic 模型（参照 `schemas/order.py`）；② 引入统一校验中间件，未声明模型的写端点至少做「必填字段 + 类型」基础校验。

🔴 **（高）分页/响应格式三套混用**
- `limit/offset`、`page/limit`、不分页三种分页并存；错误响应 HTTP 200 + `{"success":false}` 混合。
- **改进**：统一为 `page/limit` + 统一响应体 `{"code":0,"data":...,"msg":...}`，HTTP 状态码表征成败。

🟡 **（中）无 API 版本控制**
- 全部 `/api/...` 无 `/api/v1/`。后续破坏性变更无法平滑过渡。
- **改进**：加 `/api/v1/` 前缀，旧路径做 301 兼容。

### 4.2 数据库结构

🔴 **（高）229 张表仅 2 个外键、26 个索引**
- 大量引用 `contacts(id)` 的字段无约束，存在孤儿数据风险；`products.barcode`、`journal_entries.voucher_no`、`receivables.due_date` 等高频检索字段无独立索引。
- **改进**：① 补外键约束（或至少加应用层校验 + 孤儿数据清理任务）；② 对高频 WHERE/ORDER BY/JOIN 字段补索引（先用 `EXPLAIN QUERY PLAN` 定位慢查询）。

🔴 **（高）多租户隔离靠「分库」无 `tenant_id` 列**
- `db/connection.py` 用 contextvars + 每租户一个 `.db` 文件。机制可行，但**业务表无 `tenant_id` 列**，一旦 contextvars 串号或某查询绕过上下文即跨租户泄露。
- **改进**：共享表加 `tenant_id` 列 + 索引，查询层强制注入（双保险），不依赖纯连接隔离。

🟡 **（中）erp_db.py 677 函数巨石文件**
- 所有 schema/CRUD/报表/迁移挤在一个文件，已到「不敢动」临界点。
- **改进**：新函数只写 `db/queries/*.py`（已部分拆），老函数随改随迁，设季度目标拆完。

🟡 **（中）迁移机制 `_safe_migrate` 吞异常**
- `erp_db.py` 的迁移用 `executescript` 多语句，部分成功后标记 failed 但已执行语句不回滚，数据可能不一致。
- **改进**：单条 DDL 逐条执行 + 事务包裹；或全面切到 alembic（当前 alembic 仅 1 个空 stamp）。

### 4.3 权限控制

🔴 **（高）数据级权限未隔离——业务员能看全租户订单**
- `routers/sales.py:70-74`：`operator_id` 是前端传入的参数，后端 `_auth` 只校验登录，不按当前用户强制过滤。业务员 A 可查业务员 B 的客户/订单。
- **改进**：后端查询层自动注入 `operator_id = 当前登录用户.id`（当角色为 sales/业务员时），前端参数仅作覆盖（且需权限校验）。

🔴 **（高）缺导购、司机角色，司机无独立账号**
- `core.py:215-217` 默认角色仅 boss/accountant/sales。司机 `driver_id` 存的是名字字符串非用户 ID。
- **改进**：① 新增 `导购`、`司机` 角色 + 对应权限矩阵；② `driver_id` 改为关联 `users.id`，给司机做独立轻量界面（配送路线 + 扫码签收）。

🟡 **（中）权限仅菜单级，无按钮/字段级**
- 前端 `buildSidebar` 按 `perms` 过滤菜单，但 `navTo` 不校验，知道 page id 可手动进。
- **改进**：路由进入时后端按角色校验（已有 `_check_perm`），前端仅做展示裁剪。

### 4.4 并发处理

🔴 **（高）SQLite 单写锁，多用户并发写瓶颈**
- 当前每租户一个 SQLite 文件。业务员+会计+司机+老板同时操作（开单/记账/签收）会触发 `database is locked`。
- **改进**：生产环境迁移 PostgreSQL（`db/connection.py` 已支持 PG schema 模式），SQLite 仅留本地开发。迁移前先解决 4.2 的 `tenant_id` 列问题。

🟡 **（中）无连接池配置可见性**
- 需确认 PG 模式下的连接池上限与超时，避免高并发耗尽连接。

### 4.5 数据校验 & 输入安全

🔴 **（高）文件上传仅校验扩展名，无 MIME/大小限制**
- `import_router.py:156-157` 只检查 `.xlsx/.xls/.csv` 后缀，`openpyxl.load_workbook` 直接解析未受信任文件 → XXE / zip 炸弹风险。
- **改进**：① 校验 `file.content_type`；② 限制文件大小（如 ≤10MB）；③ 解析前做格式预检（magic number）。

🟡 **（中）XSS 已基本修复但需复查**
- v90 已统一 `esc()` 并加引号转义（23 个模块删除重复定义）。建议加自动化测试防止回归（正则扫描 `innerHTML=` 未过 esc 的拼接）。

### 4.6 日志与异常处理

🟡 **（中）异常已清理但日志非结构化**
- v90 把 148 处裸 except 降到 18 处，但日志是 `logging.warning(f"...")` 文本，无法按租户/请求 ID 检索。
- **改进**：引入 `structlog` 或标准 `logging` + JSON formatter，每条日志带 `tenant_id` + `request_id` + `user_id`，便于排障与审计。

🟡 **（中）无审计日志闭环**
- 关键操作（改价、删单、调账）需留审计轨迹。现有 `audit_logs` 表 + 端点，但业务写入是否全量记录待确认。
- **改进**：对写操作统一加审计装饰器，记录 before/after。

---

## 五、优先级总表（排期用）

| 编号 | 模块 | 优化项 | 优先级 | 工作量 | 建议时机 |
|---|---|---|---|---|---|
| B1 | 后端·权限 | 业务员数据级隔离（sales.py 注入 operator_id） | 🔴 高 | 0.5d | 立即 |
| B2 | 后端·安全 | 文件上传加 MIME+大小校验 | 🔴 高 | 0.5d | 立即 |
| B3 | 后端·DB | 多租户加 tenant_id 列 + 查询注入 | 🔴 高 | 2d | 本周 |
| B4 | 后端·权限 | 新增导购/司机角色 + 司机账号化 | 🔴 高 | 1.5d | 本周 |
| B5 | 后端·并发 | SQLite→PG 迁移评估与路线 | 🔴 高 | 3d | 两周内 |
| B6 | 后端·API | 写端点补 Pydantic 校验 | 🔴 高 | 3d | 持续 |
| B7 | 后端·DB | 补外键 + 高频字段索引 | 🔴 高 | 2d | 本周 |
| F1 | 前端·性能 | 108 script 懒加载/构建拆分 | 🔴 高 | 3d | 两周内 |
| F2 | 前端·UX | 恢复浅色模式为默认 | 🔴 高 | 0.5d | 立即 |
| F3 | 前端·UX | 按角色裁剪导航（≤5 入口） | 🔴 高 | 1d | 本周 |
| P1 | 产品·对标 | FEFO/临期/专属价做成默认业务规则 | 🔴 高 | 5d | 本月 |
| P2 | 产品·AI | 企业微信 Bot + 语音/拍照录单闭环 | 🔴 高 | 5d | 本月 |
| B8 | 后端·API | 统一响应体 + 分页 + 版本前缀 | 🟡 中 | 2d | 下月 |
| B9 | 后端·DB | erp_db.py 拆分 + alembic 接管 | 🟡 中 | 5d | 季度 |
| B10 | 后端·日志 | 结构化日志 + 审计装饰器 | 🟡 中 | 2d | 下月 |
| F4 | 前端·UX | 设计令牌收口（圆角/字号/内联类化） | 🟡 中 | 2d | 下月 |
| F5 | 前端·UX | 新手引导 3 步向导 | 🟡 中 | 1d | 下月 |
| F6 | 前端·移动 | 司机/业务员轻量 PWA/小程序 | 🟡 中 | 5d | 季度 |
| A1 | 对标·BPM | 审批流与业务端点打通 | 🟡 中 | 3d | 季度 |
| B11 | 后端·迁移 | `_safe_migrate` 事务化 / 切 alembic | 🟡 中 | 1d | 本月 |
| F7 | 前端·性能 | 资源 hash + 缓存策略 | 🟢 低 | 0.5d | 有空 |
| F8 | 前端·体验 | 列表骨架屏 | 🟢 低 | 0.5d | 有空 |
| F9 | 前端·移动 | 手机端表格转卡片列表 | 🟢 低 | 1d | 有空 |
| A2 | 对标·生产 | MRP/生产（主动不做，仅记录） | 🟢 低 | — | 不排期 |

---

## 六、一句话行动建议

**先做「不花钱就能赢」的三件事**：① 恢复浅色默认（F2，半天）；② 按角色砍导航（F3，1天）；③ 补业务员数据隔离+文件上传校验（B1+B2，1天）。这三件做完，产品从「通用 ERP 外壳」变成「低温奶老板一眼就懂、敢用的系统」。然后集中火力把 P1（行业专属规则）和 P2（企业微信 AI 入口）做出来——那才是和用友/金蝶拉开差距的地方。

---

*附：本报告与既有文档关系——`UI审查报告-hergent.cn-2026-07-21.md` 审查的是**桌面版**（hergent.cn），非本对象；`Phase1-*.md` 覆盖的是 v90 后端安全（已落地），本报告在其基础上补充了产品对标、定位匹配与前后端工程优化建议。*
