# v108 蓝图优化：厂家返利中心

> 基于《赫金特产品观——给后来者的完全手册》第三十七章 v108 蓝图，结合一线经销商真实使用场景与产品"卖给其他快消/低温奶经销商"的定位，做的可落地优化版。
>
> 相对原蓝图的关键改动，逐条对标 37.2 / 37.4，见下文「§0 优化总纲」。
> 工程铁律沿用手册：app.js 只读、前端改完必须 `cd static && VITE_BASE=/ npm run build` 后 rsync `dist/`、严禁 scp 单文件前端、rsync 绝不带 `*.db`、新页面走 `sidebar-v2.js` 硬编码（不注册 ModuleRegistry，避免折叠组重复入口）、不把演示数据带进生产。

---

## §0 优化总纲（与原蓝图的关键差异 · 我的拍板）

| # | 原蓝图（37.2 / 37.4） | 优化的做法 | 为什么 |
|---|---|---|---|
| 1 | 政策页标"厂家规则"，和我已上线的"财务工具→返利规则"散落两处 | **新建统一「厂家返利中心」领域模块**，把规则配置、数据导入、看板收敛进同一分组；已上线的返利规则并入此中心，删除"财务工具"里的孤立入口 | 避免侧边栏重复入口、用户到处找；也解决 ModuleRegistry 与 sidebar-v2 双机制共存问题 |
| 2 | "上传实际数据"是 modal 表单弹窗 | **做成独立「实际数据导入」页**（非 modal）：Excel 上传 + 手动补录两条通道都常驻 | 导入需要模板下载、文件解析进度、错误报告下载，modal 装不下；手动兜底也要表单 |
| 3 | 主通道 = 舟谱"采购入库单"Excel 导入（强绑定舟谱） | 主通道 = **通用 Excel 模板导入 + 可配置列映射**；内置"舟谱采购入库单"预设，但允许其他经销商自定义映射 | 产品要卖给用不同 ERP 的经销商；且**不等舟谱样例也能先交付**（通用模板立刻可用，舟谱预设后续增强） |
| 4 | 看板用 ECharts（未说明怎么引入） | **ECharts 通过 npm 打包进 Vite bundle**（不依赖 CDN），避免客户无外网时图表挂掉 | 已核实：当前 Vite 工程无任何图表库，必须从零引入；打包进 bundle 最稳 |
| 5 | 纯功能页面 | **注入「AI 副驾解读」层**：每页/每品牌自动生成白话结论 + 行动建议（调用 Hermes 引擎） | 这是产品最核心的差异化卖点，也是"AI 经营副驾"定位的落点；没有它只是又一个舟谱 |
| 6 | 桌面端为主 | 看板核心数字（总达成率、本月预计返利）**响应式**，手机一屏看完 | 一人公司老板常在外，手机看核心数字高频 |
| 7 | 看板"核销状态"未明确视角 | 明确看板是**「厂家返利收入」视角**：环形图 = 本月应返 / 已确认到账 / 待核销 | 经销商最关心"厂家承诺的返利到底到没到"，不是给客户返出去的钱 |

**一句话定位**：把"目标多少、实际多少、返利多少、钱到没到、下一步卖什么"在一个中心里闭环，并且用 AI 替老板把数据读成人话。

---

## §1 信息架构（导航优化）

侧边栏新增 **「厂家返利」** 分组（同级于"经营业务""财务工具"），下挂三个一级页，**写/读分离 + 双向跳转**：

```
侧边栏 · 厂家返利
 ├─ 规则配置      （写：返利政策 + 销售目标，复用已上线 /api/rebate-rules，UI 微调）
 ├─ 实际数据导入  （写：Excel 导入 + 手动补录，双通道）
 └─ 达成&返利看板 （读：ECharts 图表 + AI 解读）
```

- **双向跳转**：规则配置页每行政策 → "📊 看趋势"跳看板并带筛选；看板对比图 → "⚙️ 调政策"跳回规则配置对应行。
- **删除旧入口**：把已上线但孤立的"财务工具 → 返利规则"入口下线（功能并入"规则配置"，不丢能力）。
- 导航走 `sidebar-v2.js` 硬编码（同 ops-workbench 模式），不进 ModuleRegistry。

---

## §2 页面 A：规则配置（写）

复用已上线后端 `/api/rebate-rules/*`（CRUD + validate + simulate + conflicts），UI 在现有 `rebate-rules.js` 基础上微调：

- **返利政策**：品牌/单品维度 × 金额/箱计量 × 阶梯/达成/搭赠结构（已实现，品牌维度锁定"金额"，与导入字段一致，前后不打架）。
- **销售目标**（新增轻量表单，存"目标表"）：品牌/单品 × 月度/年度 × 金额/箱 × 目标值。
  - 蒙牛口径：品牌总目标=按月+按金额，兼容年返；单品=按月，计量金额或箱皆可。
- **搭赠折算**：赠送商品单价 × 箱数 折算成金额，统一进返利总额（原 37.2 决策6）。
- 保存前实时 `/validate` 红字提示；顶部冲突 banner（同维度+周期+作用对象且生效期重叠）。

---

## §3 页面 B：实际数据导入（写 · 友好设计）

**入口**：侧边栏「实际数据导入」独立页（不是 modal）。顶部两个醒目卡片：「📥 下载 Excel 模板」「⬆️ 上传文件」。

### 通道 1：Excel 导入（主）
- **通用模板**（首行带示例、列带注释）：`期间(2026-08)` · `维度(品牌/单品)` · `厂家品牌` · `单品` · `发货箱数` · `发货金额(元)`。
- **列映射可配置**：内置"舟谱采购入库单"预设（拿到真实样例后填）；其他经销商可自定义"A列=期间、B列=金额…"。映射关系存租户配置，不写死。
- 维度选"品牌"时自动禁用"发货箱数"列（品牌只算金额，与规则一致）。
- 上传后**逐行校验**，出错行红字标出，可一键下载「错误报告.xlsx」。

### 通道 2：手动补录（兜底）
- 单条表单（同上 6 字段），用于舟谱缺失时文员手填汇总值。

### 数据规则（原 37.2 决策2）
- **舟谱优先、手动兜底、不叠加、留来源记录**：每条达成记录带 `source` 字段（zhoupu / manual），舟谱数据存在时手动值仅作缺失兜底，覆盖时留覆盖日志。

### 工程复用
- 复用现有 `import-wizard.js` 的 Excel 解析 / 校验 / 错误报告骨架，不重复造。

---

## §4 页面 C：达成&返利看板（读 · ECharts + AI）

### 图表库引入
- `npm i echarts` 装进 Vite 工程，打包进 bundle（不依赖 CDN，客户无外网也不挂）。
- 按需引入（trend/bar/pie/gauge）控制体积。

### 四个核心图（对标原 37.4 第四批，增强）
1. **月度趋势复合图**：实际(柱) vs 目标(折线)，一条品牌/全店一张，看"冲量集中在哪月"。
2. **品牌达成率对比柱状图（可下钻）**：各品牌达成率横排，点某品牌下钻看其月度 + 返利阶梯详情。
3. **核销状态环形图**：本月应返 / 已确认到账 / 待核销（厂家返利收入视角）。
4. **年返单独进度**：年度累计进度条 + 剩余月份（与月返分开，不混进月度条）。

### AI 副驾解读层（核心卖点 · 新增）
- 每页顶部一张**「AI 经营解读」卡片**，调用 Hermes 引擎，根据达成率/返利趋势/阶梯缺口自动生成白话结论 + 行动建议，例如：
  - "蒙牛 8 月达成 92%，差 8 万到下一档（3%→5%），建议下周重点压特仑苏 + 纯甄，可多返约 ¥1,800。"
  - "预计本月厂家返利 ¥3,600，同比 +15%；其中 2 笔共 ¥1,200 已确认到账，其余待核销。"
- 这是卖给其他经销商时最值钱的差异点，必须做。

### 响应式
- 核心数字卡（总达成率、本月预计返利、待核销金额）在手机上一屏看完；图表在窄屏改为纵向堆叠。

### 筛选联动
- 顶部筛选：期间（月/年）、维度（品牌/单品）、计量（金额/箱），四个图 + AI 卡片同步刷新。

---

## §5 数据模型（后端补两张表）

复用已上线 `rebate_rules` 作**政策表**，新增：

| 表 | 字段要点 | 说明 |
|---|---|---|
| `sales_targets_mfr`（目标表，新） | period_type/month_or_year, dimension(brand/product), scope_key, measure(amount/box), target_value, priority | 销售目标，复用 37.2 三维模型 |
| `achievement_data`（达成数据表，新） | source(zhoupu/manual), period, product/scope, boxes, amount, override_log | 补上"实际达成"缺口，即导入页落库处 |
| `rebate_rules`（政策表，已上线） | 见现有契约 | 复用，不重建 |

看板 = 政策表 × 目标表 × 达成表 join 算达成率 + 返利（复用现有 `/simulate` 逻辑扩展到按表汇总）。

> ⚠️ 现有 `sales_targets`（业务员绩效）和 `rebate_contracts`（你给客户的返利）**不要动、不要混**——本中心是"厂家给你的返利（收入）"视角。

---

## §6 落地批次（微调）

| 批次 | 内容 | 相对原蓝图的变化 |
|---|---|---|
| **第三批**（先做） | 后端补 `sales_targets_mfr` + `achievement_data` 两表 + 导入解析落库；前端「厂家返利中心」分组 + 规则配置（并入旧返利规则）+ 实际数据导入页（通用 Excel 模板，不阻塞等舟谱样例） | 把旧孤立入口归并、导入做独立页、先用通用模板交付价值 |
| **第四批** | 数据看板：ECharts 四图 + 筛选联动 + 响应式 | 同原蓝图，明确 ECharts 打包进 bundle |
| **4.5 批** | AI 副驾解读层（接 Hermes + LLM） | 原蓝图缺失，本次新增，建议紧随第四批 |

**不阻塞项**：舟谱"采购入库单"真实字段样例拿到后，再加"舟谱预设映射"；在此之前通用模板已可用。

---

## §7 风险与注意

1. **图表库体积**：ECharts 全量 ~1MB，按需引入控制在 ~300KB；已在 Vite 工程验证打包可行。
2. **演示数据**：蓝图原型是静态演示，落地必须接真实接口，勿把演示数据带进生产（原 37.5 铁律）。
3. **AI 解读依赖 LLM**：需 Hermes 接 DeepSeek/Qwen 等，离线或额度耗尽时降级为"纯图表 + 数字结论"，不报错。
4. **多租户**：新表随租户库（tenant_N.db），沿用现有 `_connect()` 租户上下文，前端直接用 `api()`，无需显式带租户头。
5. **种子/权限**：新页面在 `core.py` `_DEFAULT_PERMS` 为 boss/sales/guide 登记（同 ops-workbench），admin 通配。

---

## §8 给用户的下一步

方案已定。建议下一步直接做**第三批**（后端补两表 + 导入页 + 规则归并），做完你就能在 hergent.cn 录入真实达成数据、立刻看到返利数字。你回个"做"，我就按本蓝图开工，每批做完给你验收。
