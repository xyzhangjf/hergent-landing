# Hergent ERP 全量代码评审报告

- **评审日期**：2026-07-25
- **评审对象**：`/Users/zhangjunfeng/Documents/hergent-erp/`（分支 upgrade/v84-international，HEAD `6c77d76`）
- **代码规模**：后端 Python 约 43,248 行（63 个路由文件、751 条路由、359 条写端点）；前端 JS 约 25,550 行（110 个文件）+ CSS 6,367 行
- **评审方式**：仅静态评审，未修改任何代码；所有结论均附文件+行号证据

---

# 一、项目整体代码评级：**C（能跑、业务闭环完整，但架构改造前有阻断性缺陷）**

| 子项 | 评级 | 一句话评语 |
|---|---|---|
| 业务功能完整度 | A- | 低温奶闭环（预报→采购→批次效期→销售专属价→财务→返利→盈亏推送）真实可用，近期安全加固（P0-1/P0-2/H1b）方向正确 |
| 前端工程化 | D+ | 7,522 行 app.js 直发、5 个备份文件随生产分发、路由/请求/加载三层基础设施"有规范未落地" |
| 后端架构 | C+ | 分层设计存在（connection→queries→erp_db→domain）但被 25 个路由文件直写 SQL 架空；erp_db.py 10,805 行上帝文件 |
| 数据库设计 | D+ | 租户库 schema 漂移 116 张表、金额全 REAL、外键几乎为零、租户库 0 索引 |
| 业务逻辑健壮性 | C | FEFO 排序键自相矛盾、退货事务断裂、过期奶可售等高危缺陷 |
| 安全水位 | C+ | 认证/RBAC 已 fail-closed，但租户隔离 fail-open、单据级 IDOR、真实密钥入 git |

**总评**：这是一个"业务上成功、工程上欠债"的系统。单租户自营使用当前可接受；按规划做 SaaS 多租户 + UI 改版 + AI 集成前，必须先清零第二章的高危项，否则架构改造必然引发严重故障（数据串租户、批次链断裂、金额失真）。

---

# 二、高危风险项（必须优先修复，否则架构改造会出严重故障）

| # | 问题 | 证据 | 风险后果 | 建议 | 工作量 |
|---|---|---|---|---|---|
| G1 | **真实 DeepSeek API Key 已入 git 历史** | 仓库根 `DeepSeek key.docx` 被 git 跟踪，内含有效 key `sk-a978...837a5`；`.gitignore` 未含 `*.db`/`*.docx` | 密钥泄露被刷量扣费；ERP 库文件随时可能误提交泄露全部经营数据 | 立即吊销 key → git filter-repo 清史 → gitignore 补 `*.db`/`*.docx` → 密钥迁入密钥管理器 | 小 |
| G2 | **多租户隔离 fail-open** | `server.py:47-70` `_TENANT_REQUIRED_PREFIXES` 仅 82 前缀，约 235 条路由未覆盖（`/api/credits`、`/api/hr`、`/api/bi`、`/api/import`、`/api/salary-send` 等 19+ 前缀落入 master DB 默认分支 `server.py:123-125`） | 开第二个租户即成跨租户数据通道，SaaS 化的命门 | 改默认 deny：未知 /api 路径一律强制租户上下文；前缀表代码生成+测试兜底 | 中 |
| G3 | **租户库 schema 漂移 116 张表 + 批次效期链断裂** | master 256 表 vs tenant_1 仅 143 表；`sale_orders` 漂移 13 列；`sale_order_items` 租户库无 batch_no/expiry_date；`batch_trace`/`wastage_orders`/`transfer_orders` 租户库缺失；迁移只对 master 执行（`erp_db.py:8527`），`_ensure_tenant_module_tables()` 只硬编码回填 6 表 | FEFO、批次追溯、报损在租户库实际不可用；迁移不同步随租户增多指数恶化 | 建"启动时遍历 tenant_*.db 跑 _migrations diff"的迁移 runner；批次链相关迁移强制推送所有租户库 | 中 |
| G4 | **FEFO 排序键自相矛盾 + 库存不足半提交** | `db/queries/inventory.py:82` fefo_deduct 把无效期批次排最前（`'0000-00-00'`），`:146` batch_out_fefo 却排最后（`'9999-12-31'`）；`:99-108` 扣减已执行后返回 error 但 finally 无条件 commit → 批次已扣订单仍草稿 | 新奶先出、临期滞留报损（生产实测 4 条在库 3 条无效期）；重复发货双倍扣库存 | 统一排序键；先验总量再扣或 error 时 raise 触发回滚 | 小 |
| G5 | **销售退货事务断裂 + 幽灵库存 + 应收永不冲减** | `db/queries/sales.py:206` 调 inventory_adjust 未传 db_conn（独立连接先提交）；`inventory.py:43` 回库不带批次；`sales.py:216` 负 AR 行被 `amount>paid_amount` 过滤（`profit_reporter.py:136`）→ 退货不冲应收 | 财务账目永久错误、退货库存落到随机批次 | 退货全程单事务；回库带批次号；应收余额口径改为 `amount - paid_amount - 退货冲减` | 中 |
| G6 | **金额字段全用 REAL 浮点** | 110 张表金额/单价/成本列 REAL（`sale_orders.total_amount`、`receivables.amount`、`journal_entries` 等）；`queries/sales.py:10/19` total 未 round | 浮点累加误差，财务对账长期必出分钱级差异；AI 集成后数据不可信 | 短期：封装 money 读写层统一 round(2)；中期：改 INTEGER 分 | 大（短期方案：中） |
| G7 | **单据级 IDOR 越权** | `routers/sales.py:156-160` GET /sale-orders/{oid} 无归属校验；deliver(:163)/sign(:170)/batch-deliver/approve 同漏；`db/queries/sales.py:173-178` 无 operator/tenant 过滤；B1 只挡了列表 | 业务员 A 改单号即可看/操作业务员 B 的单据 | db 层统一加归属过滤，路由层校验可见性，推广到 purchase/finance | 中 |
| G8 | **过期批次 50% 折扣继续可售** | `domain/pricing_engine.py:373-381` 已过期批次返回 discount_pct=50；`:353-359` FEFO 候选未排除已过期 | 低温奶食品安全红线，法律风险 | 过期批次禁售仅报损（仅此一处规则修补，不动定价链） | 小 |
| G9 | **预报订货链路整体缺失** | `forecast_order_create/review/confirm`、`rebate_calculate_month` 等 8 个 erp_db 函数不存在（运行时 AttributeError 实测） | 预报→采购闭环断裂，属未完成而非缺陷 | 按既有路由契约补齐实现 | 中 |
| G10 | **datasources v1/v2 同前缀冲突，v2 被静默遮蔽** | `routers/datasources.py:194` 与 `datasources_v2.py:89` 同注册 `POST /api/datasources/kingdee/sync`，先注册者生效 | 金蝶同步行为不确定，排查极难 | 拆分 v2 前缀为 `/api/datasources/v2` | 小 |
| G11 | **api_tokens / bot token 永不过期** | `erp_db.py:1961-1967` 不写 expires_at；`core.py:163-172` 不校验过期 | 泄露的 token 永久有效 | 创建强制 expires_at + 校验；存量 token 设过期批量轮换 | 小 |
| G12 | **RMA 流程顺序错误，库存可翻倍** | `erp_db.py:9489` 先回库+冲应收、`:9496` 才建审批；审批拒绝不回滚；`:9561-9564` 处置 restock 二次回库；退款无防重不写 cash_flow | 重复回库、退款无凭据 | 调整为先审批后执行；处置环节幂等；退款写现金流+防重键 | 中 |

---

# 三、中优改进项（影响体验、性能、维护效率）

## 维度 1：前端 UI & 样式
| 问题 | 证据 | 等级 | 建议 | 工作量 |
|---|---|---|---|---|
| 硬编码泛滥 | inline `style="` 4,036 处；`.style.xxx=` 426 处 + cssText 59 处；JS 硬编码 hex 280 处（expiry-dashboard.js 44、app.js 43）；JS 内联 px 5,631 处 | 高 | 立 lint 门禁：禁 hex/px/inline-style 进 JS（仅对新增代码生效，存量渐进清理） | 大 |
| 组件复用率低 | 无公共表格渲染器（`<table>` 字符串拼接 18+ 处）；modalOpen 之外各模块自建弹窗 300+ 处；validateForm 之外 8 处 alert 校验；空态 `padding:40/60/80px` 复制 170+ 处 | 高 | 抽 Table / EmptyState / ActionGroup / FormField 四个公共渲染器即可消灭大半重复 | 大 |
| 间距不统一 | 内联 padding/margin 组合值 144 种，`padding:10px 14px` 106 次，未走 `--sp-*` 令牌 | 中 | 新组件一律用令牌，存量随 UI 改版顺带清理 | 中 |
| 深色→苹果极简迁移 | 有利：variables.css 双主题令牌完备（100+ 变量），styles.css 仅 205 处 `.dark` 覆盖；障碍：280 处 hex + 4,036 处 inline style 绕过变量；`--text-inverse` 浅深同值是坏味道 | 中 | 三步走：① JS 内联样式→CSS class（占工作量 70%）② 删 dark 覆盖 ③ 令牌值换苹果灰阶。**不要单独做，与组件抽取合并实施** | 大 |

## 维度 2：前端工程结构
| 问题 | 证据 | 等级 | 建议 | 工作量 |
|---|---|---|---|---|
| 备份死文件随生产分发 | `static/app.js.bak`(680KB)、`.offlinebak`、`.serverbak`、`.broken2`、`.deployed` 合计约 2.2MB；7 个模块未被引用（ai-learning-dashboard、forms-zhoupu-gap 等）；`css/themes/` 空目录 | 高 | git 确认后删除，部署流水线加静态目录白名单 | 小 |
| 路由双轨 + 死代码 | `js/core/router.js` HergentRouter 全项目 0 调用；实际路由是 `app.js:4273 navTo()` 20+ 硬编码 if 分支 + `index.html:448` setTimeout 500ms 时序补丁 | 高 | 二选一收敛；建议保留 ModuleRegistry 路线、删 router.js、消灭 setTimeout 补丁 | 中 |
| window 全局污染 | 1,011 个不同 `window.Xxx` 引用；`window.api=` 被赋值 38 次（ai-safety.js 两次改写）；无事件总线；apiGet/apiPost fallback 在 14 个模块逐字复制 | 高 | 单命名空间 `window.Hergent` + 轻量 EventBus，渐进收编 | 大 |
| 裸 fetch 绕过封装 | 77 处裸 fetch（procurement-console.js 8、tasks-page.js 5），绕过统一 loading/错误处理/401；`js/core/api-client.js` 反而是 0 调用死代码 | 中 | 77 处收编到 api()；删 14 份复制 fallback | 中 |
| script 加载混乱 | `index.html:343-456` 共 104 个同步 script 无 defer；版本戳 `?v=201`×50、`?v=202`×2、53 个无戳，`__APP_VER__=202` 与 `_hrt_ver=201` 自相矛盾；vite 配置齐全但从未构建 | 高 | 启用 vite 构建出 hash 文件；过渡期统一 ?v 单常量 | 中 |
| 注释 | app.js 无文件头，注释密度约 7%，无 JSDoc | 低 | 新模块强制块头+函数注释，存量不补 | 小 |

## 维度 3：后端接口 & 代码
| 问题 | 证据 | 等级 | 建议 | 工作量 |
|---|---|---|---|---|
| 290 条写端点无签名级参数校验 | `request.json()` 290 处/54 文件；FastAPI 签名注入 0 处 → OpenAPI 无请求体契约，AI 对接无 schema 可依 | 高 | 以 schemas/ 为载体推进 H1a 收尾，按模块批量补（purchase/crm/hr/forecast/platform/tax 优先） | 大 |
| 路由层直写 SQL 架空分层 | 25/63 路由文件 191 处 `.execute()`（sso.py 30、trade_fair.py 17、portal_api.py 13、mfa.py 13） | 高 | SQL 下沉 db/queries/，优先 sso/mfa/portal_api/trade_fair | 大 |
| 异常吞噬 | `except Exception` 317 处（erp_db.py 151 处含 `:21 return '',''` 静默降级） | 中 | 财务/库存链路禁止空 except，至少记日志+trace_id | 中 |
| `_PUBLIC_PATHS` 过宽 | 13 项含 `/api/backup`（公网可触发的备份属高危）、`/api/portal`、`/api/bot` | 中 | backup 移入鉴权；逐项复核白名单 | 小 |
| RBAC map 误伤 | `/api/ai-learning`、`/api/bank-api`、`/api/tax-filing` 等前缀不在 map → 合法路由撞默认 403；map 键数实测 245 与记录 274 口径不一 | 中 | 补键 + 加"全部路由必有映射"的启动自检测试 | 小 |
| 顶层文件平铺 | server/ 下 40+ 模块平铺（6 个 ai_*、hermes_*、dami.py）；`.db` 与代码同目录 | 低 | 归包 ai/、integrations/、engines/；DB 文件移出代码目录 | 中 |

## 维度 4：数据库结构
| 问题 | 证据 | 等级 | 建议 | 工作量 |
|---|---|---|---|---|
| 租户库 0 索引 + 4 条索引列名写错静默失败 | `db/indexes.py:32-37`：`idx_soi_order_product` 列名 `sale_order_id` 实为 `order_id`；`idx_recv_cust_date` 用 `customer_id` 实为 `contact_id`；`payments` 表不存在；except: pass 吞错 | 高 | 修列名；索引纳入每租户迁移；创建失败必须报错 | 小 |
| 外键几乎为零 | 两库各仅 1 处 FOREIGN KEY；`sale_order_items.order_id`、`receivables.ref_id` 无约束无级联 | 高 | 补 FK+ON DELETE 约束；或加每日对账巡检任务兜底 | 中 |
| 审计字段覆盖率低 | 租户库 143 表：created_at 61、updated_at 4、created_by 1、软删除 0 | 中 | 统一补 created_at/updated_at；明细表至少 created_at | 中 |
| alembic 是摆设 | versions/ 仅 1 个 no-op；env.py 传裸 sqlite3 连接根本无法工作；实际靠 `_safe_migrate` 157 处调用/172 条 ALTER | 中 | 二选一：弃 alembic 完善 _migrations（推荐，成本小），或真接入 | 小 |
| **SaaS 多租户路线结论** | 租户业务表 0 张有 tenant_id 列；`db/queries/*.py` 零 tenant_id 过滤，全靠文件隔离 | — | **维持"每租户一库"路线，不要改共享表+tenant_id**：143 张表加列回填+全量 SQL 改造，漏一处即串租户，当前无 FK、迁移失序，成熟度不足。PG schema-per-tenant 已预留（connection.py:120），是未来正道 | — |

## 维度 5：业务逻辑（中优部分，高危已列第二章）
| 问题 | 证据 | 等级 | 建议 | 工作量 |
|---|---|---|---|---|
| 幂等缺失 | sale_order_create 无幂等键（双击=重复单）；quotation_convert_to_order 无状态检查可重复转单（`queries/sales.py:371-378`） | 中 | 单据号唯一约束 + 客户端幂等键 | 中 |
| GL 分录在发货事务外 | `routers/sales.py:108-119` 失败仅记日志无补偿 | 中 | 加凭证补偿巡检任务 | 中 |
| 每日毛利口径错 | `profit_reporter.py:92` COGS 用 products.purchase_price 现价而非出库批次成本/WAC | 中 | 改用批次成本（数据已在 inventory_logs） | 中 |
| 返利三缺陷 | 阶梯首档未校验门槛（`erp_db.py:7045-7057`）；计提基数不退货冲减（`:7075-7079`）；厂家返利方向可能记反（`:7116` type='ap'） | 中 | 修门槛校验+退货冲减；厘清 rebate_contracts 客户/厂家语义 | 中 |
| 锁库不生效 | `erp_db.py:8075` 建 ATP 锁但 `sales.py:70-77` 发货校验不扣锁定量 | 中 | 发货校验并入锁定量 | 小 |
| 专属价无有效期 + 调价缓存不失效 | `pricing_engine.py:470-474`；`queries/sales.py:221` 未调 invalidate_price_cache | 中 | 专属价加生效/失效期；调价后主动失效缓存 | 小 |
| 停用客户可下单 | `pricing_engine.py:170-174` 不校验 contacts 状态 | 中 | 下单前置校验客户状态 | 小 |

## 维度 6：安全 & 权限（中优部分）
| 问题 | 证据 | 等级 | 建议 | 工作量 |
|---|---|---|---|---|
| B1 行级隔离仅覆盖 sales | purchase.py operator_id 出现 0 次；finance/inventory 无行级过滤 | 中 | 定义角色行级可见性矩阵逐模块落地 | 大 |
| 上传校验不一 | attachment_create 仅扩展名白名单；upload_product_image（server.py:2113-2122）无白名单无大小限制任意 base64 落盘 | 中 | 复用 import_router.py:29 魔术字节校验 | 小 |
| XSS 面 | app.js innerHTML 216 处 vs esc() 204 处；app.js:342 通知原文直拼；CSP 允许 unsafe-inline+unsafe-eval | 中 | 存量替换 esc()/textContent；CSP 渐进收紧 | 大 |
| 审计覆盖不足 | audit_log 调用约 80 处 vs 751 端点；删单/导出/权限变更无审计；register/send-code 无独立限流 | 中 | 中间件级写操作审计 + 敏感端点限流 | 中 |
| 动态标识符 SQL | gdpr.py:42、reports.py:353、erp_db.py:1471 等 8 处表名/列名/天数拼接 | 中 | 一律走枚举白名单 | 小 |

---

# 四、低优优化项（UI 细节、代码整洁度）

| 问题 | 证据 | 建议 | 工作量 |
|---|---|---|---|
| 接口命名小动作后缀混用 | 约 30 处 `/approve`、`/clock-in`、`/generate`、`GET /safety-stock/calculate/{id}`（GET 做计算） | 出一份 URL 风格约定文档，新接口遵守即可，存量不改 | 小 |
| quantity INTEGER 无法称重量级小数 | 11 张表 quantity INTEGER，batch_trace.quantity 却是 REAL | 统一 REAL 或加 unit 字段 | 小 |
| 商品名查不到默认 pid=1 | `queries/sales.py:18`、`purchases.py:25` | 查不到即报错，杜绝静默错单 | 小 |
| 空态文案/留白重复 | 170+ 处空态复制 | 随 EmptyState 组件顺带解决 | 小 |
| `nuke.html` 一次性清缓存页、portal.html 等入口页无版本管理 | static/ 根 | 纳入构建或加注释说明用途 | 小 |
| 注释密度 7% | 全项目 | 新代码强制，存量不补 | 小 |
| `--text-inverse` 浅深主题同值 | variables.css:86/185 | UI 改版时修正 | 小 |
| forecast_engine.py:100 硬编码开发机绝对路径 | domain/forecast_engine.py | 改配置项 | 小 |

---

# 五、后续改造实施顺序（强烈建议严格按此顺序）

```
第 0 步（本周）：G1 密钥吊销清史 + 删备份死文件        ← 半天，止血
第 1 步：修高危（G2~G12）                              ← 约 2~3 周
   ├─ G4/G8/G9 业务逻辑缺陷（FEFO/过期禁售/预报补齐）   ← 先修，涉及钱和食安
   ├─ G5/G12 退货+RMA 事务链
   ├─ G3 租户迁移 runner + 索引修复（数据库基座）
   ├─ G2/G7/G11 租户 fail-closed + IDOR + token 过期  ← SaaS 阻断项
   └─ G10 v2 前缀拆分
第 2 步：工程规整                                      ← 约 2 周，与第1步可部分并行
   ├─ 后端：H1a 收尾（290 端点 Pydantic）+ SQL 下沉
   └─ 前端：vite 构建落地 + 请求层收编 + 路由收敛
第 3 步：UI 改版（苹果极简风）                          ← 约 3~4 周
   └─ 与组件抽取（Table/Modal/EmptyState/FormField）合并做，
      借组件化一次性消化 4,036 处 inline style，不要分两遍改
第 4 步：租户架构                                     ← 约 2 周
   └─ 维持每租户一库；做迁移 runner 强化 + 租户开通/备份/
      销毁工具链 + 跨租户运营后台（只读 UNION 统计）
第 5 步：AI 集成（Kimi K3 数字员工）                    ← 前置依赖第2步
   └─ 依赖：OpenAPI 契约完整（H1a 收尾后才有）、金额可信（G6 短期方案）、
      审计覆盖（AI 操作必须可追责）
第 6 步：移动端 APP
   └─ 复用第2步收编后的 API 层契约；hergent_mobile/ 已有 Flutter 雏形
```

**关键依赖关系**：AI 集成排在工程规整之后，因为 K3 数字员工需要完整的 OpenAPI 请求体契约和可信的金额/审计数据；UI 改版必须在组件抽取时合并做，否则 4,036 处 inline style 要改两遍。

---

# 六、全程禁止修改的核心业务代码范围（红线）

以下代码**只允许修复明确标注的逻辑缺陷，禁止任何形式的重构、改名、结构调整、规则变更**：

| 范围 | 位置 | 允许动的边界 |
|---|---|---|
| 专属价优先级链 | `server/domain/pricing_engine.py:249-303`（专属价 > 量价/分级/标准最低价 > 临期+促销折扣叠加） | 仅允许：G8 过期批次禁售、专属价加生效/失效期、调价缓存失效修复 |
| FEFO 效期升序出批次原则 | `server/db/queries/inventory.py:71 fefo_deduct`、`:142 batch_out_fefo`、`server/domain/batch_tracker.py:216` | 仅允许：G4 排序键统一 + 提交边界修复；禁止改 FIFO/LIFO/指定批次策略 |
| 返利阶梯累进模型 | `server/erp_db.py:7036-7058 accrue_rebate`、`:7096 settle_rebate` | 仅允许：门槛校验、退货冲减、厂家/客户方向厘清；禁止改阶梯结构 |
| 预报订货周期模型 | `server/routers/forecast.py`（period/order_start/order_end/arrival + reviewer_qty 审核量语义） | 仅允许：G9 按既有 API 契约补齐缺失实现；禁止改字段语义 |
| 单据状态机 | 销售 draft→delivered→signed；采购 draft→received（`db/queries/sales.py`、`db/queries/purchases.py`） | 仅允许在状态机内部修事务边界与幂等；禁止增删状态、禁止改状态含义 |
| 采购→入库→应付→WAC 链路 | `db/queries/purchases.py:8/29/36` → `inventory.py:111 batch_in` → `erp_db.py:2806/2868` | 仅允许事务完整性修复；禁止改 WAC 计价算法 |

---

*报告完。评审全程只读，未修改任何代码。*
