# 核查：小程序报单数据 → Web「本期预报」展示链路

**核查时间**：2026-09-12 21:1x
**核查方式**：源码逐段读 + 生产库真数据对账 + 活体 HTTP 接口实测（租户 1）
**核查结论**：**接口层数据链完全打通（已实测命中）；页面层存在 2 个真实断点，会让老板「看不到」，容易被误判成"没同步"。**

---

## 一、核查范围

### 1.1 涉及的数据字段

| 环节 | 表 / 字段 | 作用 |
|---|---|---|
| 提交主体 | `forecast_submissions`：`user_id` `role` `store_id` `store_name` `order_date` `status` `total_qty` `total_amount` `period_id` | 一次报单的抬头 |
| 提交明细 | `forecast_submission_items`：`submission_id` `product_id` `product_name` `spec` `unit` `quantity` `price` `amount` | 逐 SKU 数量 |
| 期次定义 | `forecast_periods`：`order_start` `order_end` `arrival_date` `status` | **日期区间的唯一来源** |
| 商品主档 | `products`：`is_active` `sale_price` `spec` | **决定能不能成行 / 金额是否为 0** |

### 1.2 页面入口与调用链

| 入口 | 前端文件 | 后端接口 | 传参 |
|---|---|---|---|
| 小程序报单 | `pages/fill/fill.js` `submit()` | `POST /api/forecast-submissions` | `{store, items, period_id}` |
| Web「本期预报」→ 汇总表（交叉表） | `Forecast.vue` `loadCrossGrid()` L5660+ | `GET /api/forecast-submissions/summary` | `start=期次.order_start` `end=期次.order_end` |
| Web「本期预报」→ 逐单补录（编辑网格） | `Forecast.vue` `loadEditGrid()` L2120+ | 同上 | 同上 |

调用封装：`hergent-cn-v2/src/api/modules.js` → `forecastApproveApi.summary(date, start, end)`。

### 1.3 展示逻辑（决定性的一步）

`Forecast.vue` L5700+：

```
matrixRows = (prods.items || []).map(pd => { ... sumById[pd.id] 注入数量 ... })
```

- **行** = `productsApi.grid()` 返回的商品主档
- **列** = `summary.all_units`（历史所有出现过的 `store_name`，跨期不丢）
- **单元格** = `sources[].qty` 按 `store` 归集
- ⚠️ 行集合**严格由主档决定**，**不追加**"报单里有、主档里没有"的商品

---

## 二、判断依据（三层证据）

### 证据 1：生产库真数据（tenant_1.db）

小程序提交样本 **id=189**：

```
user_id=2  role=boss  store_id=2220  store_name=永诺旗舰店
order_date=2026-08-30  status=pending  period_id=9  total_qty=4
明细：pid 1196 安慕希黄桃燕麦味 ×1 / pid 1185 新希望… ×1
      pid 1160 *友芝友… ×1 / pid 1161 蒙牛小鲜语绿莎莎… ×1
```

期次 **id=9**：`order_start=2026-08-30`，`order_end=2026-09-15` → **与 `order_date` 完全对齐**（后端写库时 `od = _p.get("order_start")`）。

### 证据 2：复刻 Web 汇总 SQL（手工执行）

```
WHERE s.order_date BETWEEN '2026-08-30' AND '2026-09-15'
  AND s.status NOT IN ('rejected','recalled')
```

→ **命中 4 行**，全部 `sources = 永诺旗舰店`；`all_units` 含 `'永诺旗舰店'`（新客户列会创建）。**数据确实落地了。**

### 证据 3：活体 HTTP 接口（生产，主管账号 mptestsp）

```
POST /api/auth/login {mptestsp / Mpsup@1}  → tenant_id=1, role=supervisor

GET /api/forecast-submissions/summary?start=2026-08-30&end=2026-09-15
→ rows = 4
   *友芝友金嚼鲜酪原风味发酵乳预制杯10g*5杯*8条 | qty 1 | src [('永诺旗舰店',1)]
   蒙牛小鲜语绿莎莎牛乳绿豆奶PET瓶185ml×24瓶    | qty 1 | src [('永诺旗舰店',1)]
   新希望塑杯荔枝白桃味冰淇淋酸奶110g*3杯12组    | qty 1 | src [('永诺旗舰店',1)]
   安慕希黄桃燕麦味                              | qty 1 | src [('永诺旗舰店',1)]
```

**接口层 = 通的。** 且 `status='recalled'` 的 id=187（sales 报的 29 件）被正确排除 —— 撤回语义生效。

---

## 三、🔴 两个真实断点（"落库了但看不见"）

### 断点 1：默认视图的日期是「今天」，不是期次 ⭐ 最可能被误判成"没同步"

`GET /api/forecast/periods` 实测返回：

```
count=4   current=None        ← 没有任何期次被标为"当前"
7/8/9/10 四个期次 status 全部 = closed
```

因为 `current=None`，前端 `curPeriod` 停在 `0`，`loadCrossGrid()` / `loadEditGrid()` 双双落入兜底分支：

```js
p = { id: 0, name: '今日报单', order_start: today, order_end: today }
```

实测后果：

```
GET /summary?date=2026-09-12  →  rows = 0
```

**老板一打开「本期预报」，看到的是一张空表。** 他必须手动去期次下拉框里选「9月提审期-开放填报」，那 4 条才会出现。

**这就是"小程序报的没进来"这个错觉的根源** —— 数据在，只是默认视窗没框住它。

**当前触发条件（2026-09-12 实测）**：4 个期次全部 `closed`，`open-periods` 返回 `[]`，所以小程序现在**连提交都会被 400 拒绝**。也就是说这个断点目前是"活跃"状态。

### 断点 2：停用商品被静默丢弃

`/api/products/grid` → `db.product_list("", "", "", limit=100000)`，而 `product_list` 签名默认 `include_inactive=False`：

```python
where = ["1=1"] if include_inactive else ["is_active=1"]
```

实测：`grid 行数 = 269`（在售），而主档共 **430** 行。

把期次 9 窗口内被报的商品逐一对状态：

| pid | 商品 | is_active | 在 grid？ | 结果 |
|---|---|---|---|---|
| 1160 | *友芝友金嚼鲜酪原风味发酵乳预制杯… | **0 停用** | ❌ | **数量不显示** |
| 1196 | 安慕希黄桃燕麦味 | **0 停用** | ❌ | **数量不显示** |
| 1161 | 蒙牛小鲜语绿莎莎牛乳绿豆奶… | 1 | ✅ | 正常 |
| 1185 | 新希望塑杯荔枝白桃味冰淇淋酸奶… | 1 | ✅ | 正常 |

**小程序报的 4 个 SKU，有 2 个在 Web 上根本不出现。** 而且 `colTotals`（列小计）和 `grand`（总计）都是从 `matrixRows` 算的 → **合计也少算这 2 件**。

编辑网格（逐单补录）同样是 `(prods.items||[]).map(...)`，无兜底追加，问题一致。

---

## 四、附带发现（与本次结论无关，但建议记一笔）

| # | 发现 | 影响 |
|---|---|---|
| 1 | **期次窗口重叠 → 跨期重复计入**。期次 9 窗口 `08-30~09-15` 完整包住期次 10 的 `09-03`。实测重叠检查：`order_date 09-03 → 命中期次 [9, 10]` | 对期次 10 报的单，查看期次 9 时也会出现 → **两处都算，合计翻倍** |
| 2 | **金额恒为 0**。报的 4 个商品 `sale_price` `purchase_price` `dist_price` 全为 `0.0` | Web 汇总表金额列显示 0。**不是同步问题**，是主档没维护价格 |
| 3 | 小程序当前无法报单 | `open-periods` 返回 `[]`（4 期次全 closed），提交会被 400 |
| 4 | 幂等键 = `(user_id, store_id, order_date)`，**不含 `period_id`** | 同一天两个不同期次会互相覆盖；两个期次若 `order_start` 相同则冲突 |

---

## 五、需要进一步确认的环节（决策项）

| # | 问题 | 可选做法 |
|---|---|---|
| 1 | **期次默认选中谁？**（断点 1 的根治） | (a) 后端把"最近的 open 期次，若无则最近创建的期次"作为 `current` (b) 前端记住上次选择 (c) 保持现状，靠人工选 |
| 2 | **停用商品要不要显示？**（断点 2） | (a) 汇总表追加"已停用/已删除"标记行 (b) 报单侧就禁止选停用商品 (c) 保持现状（但合计会偏小） |
| 3 | **跨期归属是否改为按 `period_id` 精确匹配？** | 现在是纯日期区间；改 `period_id` 可根治重叠，但历史"导入"数据 `period_id=0` 需兜底 |
| 4 | **期次 9 是否重开**，以便继续用 mptest 账号跑端到端测试 | 重开 → 小程序立即可报单，可复验全链路 |

---

## 六、一句话结论

> **数据链是通的，Web 接口能拿到小程序报的每一件货。**
> 你看到"没同步"，是两件事叠加：**默认视窗停在"今天"（而报单落在期次窗口里）** ＋ **停用商品没有行可以承载**。
> 前者是产品默认值问题（改 1 处即可），后者是主档生命周期问题（需定策略）。
