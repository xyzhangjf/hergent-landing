# 侧边栏功能重叠分析（经营业务 vs 其他板块）

> 目标：在不改动经营业务板块的前提下，梳理侧边栏中**与经营业务功能重叠/重复**的模块，指明重叠功能名称、对应经营业务入口与具体表现，供后续去重整合参考。
> 分析日期：2026-08-11 ｜ 代码基准：hergent-erp `static/` 当前工作树

---

## 一、经营业务板块的构成与本质

侧边栏「经营业务」组（sidebar-v2.js 硬编码）共 **7 项**：

| 入口 | 实现 | 性质 |
|---|---|---|
| 经营看板 | dashboard 独立页 | 数据展示面板 |
| 档案管理 | `ArchiveHub` 聚合面板（7 类 35 项基础档案） | **聚合面板** |
| 采销管理 | trade 独立模块（销售/采购订单） | 业务操作 |
| 仓配管理 | `OperationsHub` 聚合面板（stock-hub） | **聚合面板** |
| 资金管理 | `OperationsHub` 聚合面板（finance-hub） | **聚合面板** |
| 财务管理 | `OperationsHub` 聚合面板（accounting-hub） | **聚合面板** |
| 数据报表 | `OperationsHub` 聚合面板（reports-hub） | **聚合面板** |

**关键结构性发现**：经营业务 7 项里有 4 项（仓配/资金/财务/报表）本身就是**聚合面板**——它们的下拉子项已直接指向许多独立侧边栏入口所指向的**同一功能、同一 `navTo` 目标**。因此所谓"独立侧边栏入口"很多是聚合面板子项的**第二入口/第三入口**。这是重叠的本质。

---

## 二、重叠清单（按来源板块分类）

### A. 仓库操作组（3 项） → 与经营业务重叠

| 独立入口 | 重叠的经营业务入口 | 具体表现（代码证据） |
|---|---|---|
| **AI采购控制台** (procurement-console) | 采销管理 | 核心产出：`approvePlan()` 调 `/api/procurement/plans/{id}/approve` **自动生成采购单**；而采销管理正是采购订单的创建/管理入口。两者最终都落到采购单，AI 控制台是采购流程的 AI 前置入口，与采销管理采购功能重复。 |
| 同上 | 仓配管理 | 预测总览含"库存紧急度 / 临期告警"（`expiry_alert`），与仓配管理「临期预警表」功能重叠。 |
| **临期看板** (expiry-dashboard) | 仓配管理 →「临期预警表」 | 仓配管理子项动作 `navTo('expiry')`（`operations-hub.js:28`），与临期看板导航目标**完全一致**——同一 expiry 页面，两个入口。 |
| **报损管理** (wastage-console) | 仓配管理 | 报损属仓库库存损益操作；仓配管理聚合面板已含「盘点点写单 / 库存查询 / 库存锁定 / 条码操作 / 订单合并」等仓库作业子项，报损是其中一环，定位重复。数据报表聚合面板也有「盘亏汇总表」(dev) 指向同一概念。 |

### B. 财务工具组（11 项） → 与资金管理 / 财务管理重叠（**最严重**）

财务工具组几乎每一项都能在 资金管理 / 财务管理 聚合面板中找到对应子项：

| 独立入口 | 经营业务对应子项（OperationsHub） | 具体表现 |
|---|---|---|
| **付款批** (paymentrun) | 资金管理 →「付款批」`navTo('paymentrun')` (ops:84) | 完全相同导航目标，**重复入口** |
| **催收管理** (dunning) | 资金管理 →「催收管理」`navTo('dunning')` (ops:87) | 完全相同，**重复入口** |
| **GL维度** (gldimensions) | 财务管理 →「GL 维度」`navTo('gldimensions')` (ops:107) | 完全相同，**重复入口** |
| **税务管理** (taxmanager) | 财务管理 →「税务管理」`navTo('taxmanager')` (ops:109) | 完全相同，**重复入口** |
| **一键报税** (tax-filing) | 财务管理 →「报税」`navTo('taxfiling')` (ops:110) | 同一报税功能，命名不同（一键报税 vs 报税） |
| **凭证模板** (voucher-templates) | 财务管理 →「单据凭证模版」`render_voucher_templates` (ops:95) | 同一凭证模板功能，命名不同 |
| **流水导入** (payment-import) | 资金管理 →「付款导入」`render_payment_import` (ops:85) | 同一流水/付款导入功能 |
| **银企互联** (bank-api) | 资金管理 →「账户管理」`openRefData('bank_accounts')` / 「银流水对账」(ops:78/77) | 均围绕银行账户与流水，概念重叠（银企互联前端暂缺，与账户管理互补） |
| **发票扫码** (invoice-scan) | 财务管理 →「凭证管理」`navTo('ledger')` (ops:94) | 发票扫码→自动凭证，与凭证管理/单据生凭证链路重叠 |
| **工资条群发** (salary-send) | 财务管理「资产管理」+ 设置「员工与部门」 | 薪资发放属财务/HR 交叉范畴，概念重叠（无直接子项但同主题） |
| **五险一金** (insurance-stats) | 同上（薪酬社保） | 与工资条群发同属薪酬社保，概念重叠 |

→ **财务工具 11 项中，至少 7 项与资金管理/财务管理的聚合子项目标完全重合，4 项概念重叠。**

### C. 系统工具组（8 项） → 与经营业务（及设置）重叠

| 独立入口 | 重叠的经营业务/设置入口 | 具体表现 |
|---|---|---|
| **打印模板** (print-templates) | 财务管理 →「打印模板」`navTo('print-templates')` (ops:113) | 完全相同，**重复入口** |
| **智能导入** (smart-import) | 仓配管理 →「智能导入」`render_smart_import` (ops:34) + 档案管理「期初录入」建议用智能导入 | 同一导入工具被**两个聚合面板各挂一次 + 独立侧边栏入口 = 三重入口** |
| **批量打印** (batch-print) | 财务管理 →「打印模板」 | 批量打印凭证账簿依赖打印模板，功能相邻重叠 |
| **电子档案** (document-archive) | 档案管理 (archive-hub) | "电子档案"与"档案管理"字面/概念重复；档案管理已是经营业务 7 项之一（承载 35 项基础数据档案），二者并列易混淆 |
| **BPM流程** (bpm) | 审核中心 (review-queue) | BPM 流程设计与审核中心的审批流/审核任务概念相邻（审批是 BPM 执行端），流程类功能交叉 |
| **租户管理** (tenant-admin) | 设置 →「员工与部门 / 角色权限」 | 多租户隔离与设置的员工/权限管理同属后台管理，与"经营业务"无关但与"设置"重叠 |
| 开发者后台 / Feature Flags | — | 纯开发运维工具，**与经营业务无重叠** |

### D. 扩展功能组（4 项） → 与经营业务重叠

| 独立入口 | 重叠的经营业务入口 | 具体表现 |
|---|---|---|
| **预报订单** (forecast) | 采销管理 | 预报订单（预录入客户意向单）最终转为正式销售单，是销售流程前置，与采销管理销售订单功能重复 |
| **数据源** (datasources) | 数据报表 | 数据源管理（AI 经营数据来源）与数据报表（经营数据分析）同属"数据"主题；数据报表聚合面板含「BI 仪表盘 / 财务报表 / 利润表」等，数据源是这些报表上游，链路重叠 |
| **盈亏快报** (profit-report) | 财务管理 →「利润表」`navTo('profit-report')` (ops:115) **＋ 经营看板** | **确定性完全重复**：财务管理子项「利润表」直接 `navTo('profit-report')` 指向盈亏快报本身；经营看板亦为核心损益展示面板，三者同主题 |
| **专属价查询** (pricing-explain) | 采销管理（订单专属价自动匹配）/ 档案管理 →「价格方案」 | 专属价查询（客户+商品查专属价匹配）与采销管理订单内"专属价自动匹配"是同一套定价规则的两个视图；档案管理「价格方案」也是定价档案，概念重叠 |

---

## 三、重叠严重程度分级

| 级别 | 判定 | 入口举例 |
|---|---|---|
| **🔴 完全重合（同一 navTo）** | 聚合面板子项与独立入口指向同一目标 | 付款批、催收管理、GL维度、税务管理、打印模板、临期看板、盈亏快报（利润表） |
| **🟠 同功能异名** | 同一功能两套命名/两套 UI | 一键报税 vs 报税、凭证模板 vs 单据凭证模版、流水导入 vs 付款导入 |
| **🟡 概念/主题重叠** | 同属一大类，定位交叉 | AI采购控制台↔采销管理采购、报损↔仓配、发票扫码↔凭证管理、数据源↔数据报表、电子档案↔档案管理、BPM↔审核中心、智能导入（三重入口）、预报订单↔采销销售、专属价查询↔采销/档案管理 |
| **⚪ 无重叠** | — | 开发者后台、Feature Flags、消息、通讯录 |

---

## 四、去重整合方向（供后续，本次不实施）

1. **直接收敛（🔴 完全重合）**：仓库操作/财务工具/系统工具/扩展功能中 7 个🔴 项可直接从独立侧边栏移除，功能已在经营业务聚合面板内可达——与 v107.147 删除"合并出库/扫码出入库/精准锁库"同理（那三项当时也仍在 OperationsHub 仓配管理内可用）。
2. **归并命名（🟠）**：保留一个权威入口（建议留在经营业务聚合面板），统一命名，独立侧边栏不再单列。
3. **归类收口（🟡）**：
   - AI采购控制台 → 并入采销管理（采购流程 AI 前置）或仓配管理；
   - 报损管理 / 临期看板 → 归入仓配管理；
   - 数据源 / 盈亏快报 / 专属价查询 / 预报订单 → 归入对应经营业务入口；
   - 电子档案 → 与档案管理合并或明确拆分"数据档案 vs 文档档案"；
   - 发票扫码 / 银企互联 / 工资条 / 五险一金 → 归入财务管理/资金管理；
   - BPM流程 → 与审核中心合并流程类；智能导入 → 仅保留仓配/档案各一处。
4. **顶层收敛**：除经营业务 7 项外，其余入口不再单独占顶层区块，统一降级为经营业务下的可折叠子分组（见《侧边栏整合方案-2026-08-11.md》）。

---

## 五、不受影响说明

- 经营业务 7 项**保持不变**（含 4 个聚合面板的全部子项）。
- 删除独立入口不影响功能：所有🔴/🟠 项在聚合面板内均有对应可达入口（如 付款批 仍在资金管理→付款批）。
- 移除 `ModuleRegistry.register(...)` 调用即可消除独立入口，**无需删除模块文件**，其 render 函数仍被聚合面板调用。

---

*生成方式：基于 `sidebar-v2.js`、`operations-hub.js`、`archive-hub.js` 及全部 `ModuleRegistry.register` 调用逐文件核对，所有重叠均附代码行号证据。*
