# 多租户服务形态落地报告（Step 1）

- 日期：2026-09-01
- 范围：`hergent-erp/server/scheduler.py`（后端主动推送引擎）
- 定位：差距分析报告 `Hergent-行动建议差距分析-2026-09-01.md` 中「建议 2 守住服务形态」的 P0 硬阻塞

---

## 一、背景与根因

差距分析判定「多租户简报未实现」是商业化硬阻塞——代码自注「多租户需另循环」（原 `scheduler.py:662`），意味着**第二家客户起收不到任何简报**。

代码勘察确认真实缺口不止简报一处：

| 原状 | 影响 |
|---|---|
| 简报 `_push_copilot_cards_brief` 只在默认租户上下文聚合 | 第 2 家客户收不到简报 |
| 11 个业务 monitor（低库存/逾期/费用异常/沉睡客户/保质期/合同到期/档案健康/价目表/盈亏快报/采购计划/AI洞察/用户cron）同样无租户遍历 | 第 2 家客户收不到**任何**主动提醒 |
| 推送通道走全局 env webhook（`WECOM_WEBHOOK_URL`） | 多租户会**串到同一个群** |
| `_tenant_ids()` 用 `str.replace` 解析文件名 | 见下方两个真实 bug |

生产实际有 9 个租户库 + 1 个异常命名库，此前列举的缺口全部真实存在。

---

## 二、交付清单

| # | 改动 | 位置 | 状态 |
|---|---|---|---|
| 1 | 简报重构为「当前租户聚合」+「遍历推送」两段：`_build_copilot_brief_lines` / `_push_copilot_brief_current` / `_push_copilot_cards_brief` | scheduler.py:657 / 765 / 785 | ✅ |
| 2 | 新增租户级推送通道 `_push_tenant_channels`：优先读 `ai_channel_configs` 租户凭证，配过渠道则**绝不回落全局**（防串台）；未配才回落全局 env | scheduler.py:717 | ✅ |
| 3 | 简报标题带租户名（`09月01日 经营卡简报 · 测试企业A`），多租户场景可区分 | scheduler.py:765 | ✅ |
| 4 | 新增通用遍历包装器 `_run_per_tenant(fn, label)`，只切上下文、不动 monitor 内部逻辑 | scheduler.py:212 | ✅ |
| 5 | 12 个业务 monitor 套上遍历壳（低库存/逾期/cron/费用异常/沉睡客户/保质期/合同到期/盈亏快报/采购计划/AI洞察/档案健康/价目表） | scheduler.py:30-117 | ✅ |
| 6 | **修复 bug A**：`_tenant_ids()` 改严格正则 `fullmatch` —— 原 `replace("tenant_","")` 把异常命名的 `tenant_tenant_1.db` 误解析成 id=1 | scheduler.py:852 | ✅ |
| 7 | **修复 bug B**：`_tenant_ids()` 排除 `tid<=0` —— `set_tenant_context(0)` 会被当作无租户而回落**主库** | scheduler.py:852 | ✅ |
| 8 | 给无节流的 `_check_low_stock`/`_check_overdue_receivables` 加节流（1800s/3600s） | scheduler.py:29 | ✅ |

**刻意未动**：备份、session 清理、webhook 投递、慢查询等运维类任务（多在主库，套壳反而会破坏现有行为）。

---

## 三、两个被修复的真实 bug（生产实测发现，非推测）

### Bug A：异常命名库导致重复推送
生产存在 `tenant_tenant_1.db`。原解析 `"tenant_tenant_1.db".replace("tenant_","").replace(".db","")` → `"1"` → 与 `tenant_1.db` 撞号。
**后果**：tenant_1（用户真实数据）每次收 **2 条一模一样的简报**。
**验证修复后**：`tenant_1.db` 仅遍历 1 次，新增消息 `new=1`。

### Bug B：tid=0 回落主库
`set_tenant_context(0)` 中 0 被当作「无租户」→ 业务 monitor 在**主库 erp.db** 上执行。
**后果**：往主库写业务消息/数据，属跨租户污染风险。
**验证修复后**：遍历列表不再出现 `erp.db`。

> 两个 bug 同时影响 `_push_forecast_reminder` 与 `_check_channel_health`（共用 `_tenant_ids`），本次一并修复——即**预报催单此前也会重复推送**。

---

## 四、验证结论（生产端到端，隔离租户 9996）

`RESULT ok=21 fail=0`，**零残留**（租户1 测试消息按 rowid 精确回收、隔离库删除、主库测试租户记录清理）。

| 验证项 | 结果 |
|---|---|
| 遍历覆盖全部有效租户 | ✅ `[1,2,3,4,5,7,8,10,9996]` |
| 遍历不误入主库 `erp.db` | ✅ |
| `tenant_1` 只遍历一次（防重复推送） | ✅ count=1 |
| 异常命名库未被误识别 | ✅ |
| 遍历后上下文复位主库 | ✅ |
| 隔离租户收到简报 | ✅ 0 → 1 条 |
| 真实租户 1 收到简报且仅 1 条 | ✅ `09月01日 经营卡简报 · 测试企业A` |
| 租户级渠道优先、不回落全局 env | ✅ `global_called=False, tenant_called=True` |
| 服务健康 | ✅ `active` / `HTTP 200` |

---

## 五、待确认项

### 1. 僵尸测试租户库造成日志噪音（中优先级，非阻塞）
`tenant_2/3/4/7/8` 为历史旧版建库，仅含 `rebate_contracts`，缺 `message_center`/`cron_tasks`/`price_lists`/`erp_contracts`。主库登记名均为「测试企业A/B」「企业A/企业B」，判定为测试残留。
遍历后每分钟约 20 条 `no such table` 日志（fail-closed，功能无害，但不美观且浪费资源）。

三个选项：
- **A 清理**（推荐）：确认无真实数据后删除 `tenant_0.db`、`tenant_2/3/4/7/8.db`、`tenant_tenant_1.db` + 主库对应 tenants 记录
- **B 补齐**：对这些库幂等补建缺失表（保留数据，噪音消失）
- **C 维持现状**：仅噪音，无功能影响

> 注：`tenant_tenant_1.db` 是 10MB 完整库，疑为某次错误命名产生的副本，**切勿直接删**，需先确认与 `tenant_1.db` 的差异再处置。

### 2. 节流间隔变更（低优先级）
低库存预警由「每分钟」改为「每 30 分钟」、逾期应收改为「每 1 小时」。
**理由**：原实现每分钟无条件执行且无去重，会持续重复推送同一条预警（站内消息刷屏），多租户遍历后按租户数放大。
若业务要求更及时，可调回 `scheduler.py:29` 的间隔参数。

---

## 六、结论

- 「服务形态」从**单租户**升级为**真多租户**：简报与 11 类主动提醒现已覆盖全部有效租户，各租户数据隔离、推送走各自渠道不串台。
- 商业化硬阻塞解除：第 2 家客户起可正常接收每日简报与全部主动提醒。
- 连带修掉 2 个此前未被发现的生产 bug（重复推送、主库误写）。
