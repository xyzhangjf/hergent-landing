# Hergent 副驾 UI 规范（2026-08-20）

> 来源：WorkBuddy→Hergent 副驾 UI 借鉴讨论（2026-08-19/20）+ 现有 `hergent-cn-v2` 代码现状核实。
> 目标：把"对话式 AI 经营副驾"的交互规范固化，指导前端 M1–M5 迭代。
> 配套组件 Roadmap 见文末，当前实施进度见 `TaskList`。

---

## 1. 定位与目标用户

### 定位
Hergent 副驾 = **低温奶经销商的 AI 经营副驾**，是一层"坐在畅捷通/金蝶/Excel 之上的对话式决策层"，**不重造 ERP**。它消费后端 6 大引擎（预报/货损/工资/对账/指标/返利）的真实业务数据，用自然语言回答老板的高频经营问题，并把结论沉淀为可保存、可转发、可执行的"经营卡片"。

### 目标用户
- **经销商老板**（核心）：非技术、零术语、大字号、微信优先。高频痛点 = 预报订什么货 / 这个月货损多少 / 谁该催款 / 我能拿多少返利 / 工资算没算对 / 指标达成没。
- **业务员 / 内勤**：报单、补录、对账辅助。

### 设计哲学（一句钉死）
> **AI 只建议，不擅自下单。** 能一句话问清的，绝不甩一张表单；能一屏看懂的，绝不摊一长串数字；要落库的，必须人点"确认"。

---

## 2. 设计原则（6 条）

| # | 原则 | 说明 | 对应 WorkBuddy 借鉴 |
|---|------|------|---------------------|
| P1 | **单入口零选择** | 副驾是统一入口，不堆菜单；能问就不填表 | 对话即主界面 |
| P2 | **对话即界面** | 自然语言为主，结构化卡片为辅 | Chat 为主 |
| P3 | **产物可沉淀** | 每个回答都是可保存/可转发的"经营卡片"（Artifact 范式） | 右侧 Artifact 面板 |
| P4 | **渐进式访谈** | 复杂任务用追问澄清，不一次性甩表单 | 不堆表单的引导 |
| P5 | **进度透明** | 长任务显式分阶段进度（读数据→算→出报告） | 任务进度可视 |
| P6 | **本土化优先** | 微信优先、语音>打字、大字号零术语、一键转发企微群 | 本土化滤镜 |

---

## 3. 视觉令牌（复用 `variables.css`，不另起炉灶）

现有设计体系已对齐苹果风，**禁止新建颜色变量**，全部走令牌：

- **品牌青**：`--p:#06b6d4`（装饰/高亮）、`--p-dark:#0891b2`（主交互白字）、`--p-bg`（青色浅底）、`--p-border`。
- **语义色**：`--suc:#34c759`、`--dan:#ff3b30`、`--war:#ff9f0a`，均配套 `-rgb` 三元组（用于 `rgba(var(--x-rgb),.12)` 浅底徽章）。
- **中性面**：`--bg`(页面白)、`--bg2:#f5f5f7`(卡片底)、`--bg3`(输入底)、`--bg4`(悬浮)；文字 `--t1/#1d1d1f`(主)、`--t2/#6e6e73`(次)、`--t3/#a1a1a6`(提示)；边框 `--bd`、`--border-subtle`。
- **圆角**：`--radius-sm:8 / -md:12 / -lg:16 / -xl:22`。副驾卡片用 `--radius-lg`。
- **阴影**：`--shadow-sm/md/lg`（轻阴影，苹果风）。
- **通用类**：`.btn(.btn-primary/.btn-ghost/.btn-block)`、`.card`、`.badge(.badge-green/-red/-amber/-blue)`、`.tag(.ok/.warn/.bad/.info)`、`.progress`、`.table-wrap/.tbl`。
- **基线**：SF Pro 字体栈、`#f5f5f7` 底、12px+ 圆角、系统色、轻阴影。双主题 `:root`(浅，线上实际跑) + `:root.dark`(深)。新增令牌须确认不被 `:root.dark` 翻转。

---

## 4. 核心范式：ResultCard（经营结果卡）

> 这是副驾 UI 的"货币"。所有 AI 经营结论都以 ResultCard 呈现，而不是一段纯文本。

### 4.1 结构（自上而下）
1. **头部**：场景图标 + 标题 + **场景徽标**（货损/返利/预报/对账/工资/指标）。
2. **摘要行**：一句话结论（AI 建议摘要，老板扫一眼就懂）。
3. **指标网格**：3–4 个 KPI（`label` + `value` + 语义色调/趋势 hint）。
4. **要点列表**：关键发现，每条可带语义色点（好/警/危）。
5. **状态留痕**：`待确认 / 已确认 / 已执行 / 已驳回`（对齐 AdvicePanel 的"AI 只建议"范式）。
6. **操作区**：采纳 / 驳回 / 查看明细 / 转发 / 保存（主操作高亮）。
7. **图表占位**（可选）：迷你图插槽，M1 先留占位，M3 后接真实迷你图。

### 4.2 状态机
```
draft(建议) ──采纳──▶ confirmed(已确认) ──执行──▶ executed(已执行)
    │
    └────驳回────▶ rejected(已驳回)
```
状态变更须经用户显式点击；卡片落库/推送走 AdvicePanel 同款留痕 API。

### 4.3 数据契约（前端 message 模型扩展）
消息模型从 `{role, content}` 扩展为 `{role, content, card?}`。卡片结构：
```ts
type Card = {
  type: 'loss' | 'rebate' | 'forecast' | 'reconcile' | 'payroll' | 'kpi'
  title: string
  summary: string
  metrics: { label: string; value: string; tone?: 'good'|'warn'|'bad'|'neutral'; hint?: string }[]
  points?: { text: string; tone?: 'good'|'warn'|'bad' }[]
  status?: 'draft' | 'confirmed' | 'executed' | 'rejected'
  actions?: { key: string; label: string; primary?: boolean }[]
  chart?: { kind: 'mini'; series: number[] }   // M3 接入
}
```
组件 `ResultCard.vue` 接收 `card` prop，向父级 `emit('action', {key, card})`，由 CopilotDrawer 决定后续（追加用户消息回 Hermes / 调留痕 API）。

---

## 5. 交互模式

- **对话流**：`user` / `assistant` 气泡（已存在）；`assistant` 消息可附带 `card`，卡片渲染在气泡下方。
- **溯源**："参考来源"面板（偏好记忆 + 业务数据，已存在），强化"读真实数据作答"可信度。
- **示例卡**：欢迎页新增「查看示例经营卡」按钮，插入一张 demo 货损卡，让老板**先看懂价值**再开口问。
- **渐进式访谈（M2）**：Hermes 返回澄清问题时，前端渲染引导 chips（如"按客户维度 / 按商品维度"），而非空白追问。
- **进度透明（M3）**：长任务（如"算整月工资"）显式步骤条：读库存 → 算折价 → 出报表。

---

## 6. 组件 Roadmap（里程碑）

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| **M1** | ResultCard 通用组件 + 副驾集成卡片渲染（`m.card`）+ 示例入口 + 构建部署 | ✅ 完成 |
| **M2** | 渐进式访谈引导卡（追问澄清 chips，`m.followups`） | ✅ 完成 |
| **M3** | 任务进度可视化（`m.progress` 步骤条）+ 卡片迷你图 sparkline | ✅ 完成 |
| **M4** | 后端结构化卡片协议**前端适配**（流式解析 ` ```card ` 围栏，零改造消费） | ✅ 完成（前端侧；后端信封待接） |
| **M5** | 微信/语音本土化（卡片转发面板 + Web Speech 语音输入） | ✅ 完成（前端侧；企微直推待后端通道） |

> **M4 / M5 边界说明（2026-08-20 推进后）**：前端已具备"零改造消费"能力——只要后端 Hermes 在回答里输出 ` ```card\n{...}\n``` ` 围栏，副驾会自动渲染经营卡；语音与转发摘要已纯前端可用。**仍待后端/通道补齐**：① Hermes 实际吐 `card` 信封（M4 后端侧）；② 企微机器人直推卡片（M5 通道侧，当前是复制到微信手动发）。

---

## 7. 不要照搬的边界（本土化滤镜）

- ❌ **不照搬 WorkBuddy「专家市场 / 插件目录」心智**：对老板太重，副驾应是"问一句就答"，不是"先去市场挑个专家"。
- ❌ **不堆左侧多层导航菜单**：能用对话解决的，不进菜单。
- ❌ **术语不本地化不上屏**：面向老板禁用 `token / agent / 工作流 / 节点` 等词，一律用"建议 / 帮忙算 / 自动对账"。
- ✅ **可借鉴**：对话+产物双栏、渐进式访谈、进度透明、苹果化视觉一致性、能力卡片化（连接器/专家在对话中"浮现卡片"而非藏菜单）。

---

## 8. 验收标准（沿用项目铁律）
- 每次改动上线前 **必须无头浏览器真机运行时验证**（不仅 `npm run build` 通过）。
- 杜绝 `node --check` 假阳性与浏览器 eval 假登录两类已知 Bug 模式。
- 部署：前端 `npm run build` → `rsync -a --no-owner --no-group --delete dist/ root@47.113.224.140:/opt/hergent-cn-v2/` → `chown -R hergent:hergent`；nginx 纯静态无需 restart。
