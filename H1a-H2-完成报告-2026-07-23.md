# H1a（Pydantic 写接口校验）+ H2（CSP/限流/CORS 安全加固）完成报告

> 日期：2026-07-23 ｜ 对象：生产 ERP `47.113.224.140:8700`（根 `/opt/hergent-erp`）
> 结论：**H1a 与 H2 均已落地并上线验证通过**。

---

## 一、H1a：Pydantic 写接口校验 — ✅ 完成

### 覆盖范围（已部署生产 + commit 进 git）
- **批次 1–3**（提交 `b7f4429` / `5f82c34` / `0f36ff4`）：sales / finance / consignment / subcontracting / assembly / workflow / inventory / purchase / crm / hr / recruitment / forecast / tax / payment_run / salary_send / platform / projects / consolidation / bpm / auth / bi / reconciliation / sso / print_templates / voucher_templates / vehicles / rfq / zhoupu_gap / tasks / bot / mfa / import_router 等。
- **本回合收尾**（提交 `b5c9721`）：唯一真实活跃缺口 `routers/gl_dimensions.py`（挂载名 `gl_dims_router`）修复。

### gl_dimensions 修复要点（`b5c9721`）
- 3 处 `json.loads(request.body.read())` 潜在 **500 bug**（Starlette 中 `request.body` 是协程，无 `.read` 属性 → `AttributeError`）→ 改为 `await request.json()`。
- 3 个 handler 改 `async`；新增零 coercion `_Base` / `GlDimPayload` + `_v()` 校验门（与 H1b 统一错误信封接通）。
- 部署：精确 scp → `chown hergent:hergent` → `IMPORT_OK` 预检通过 → `systemctl restart` → health 200。

### 验证证据（生产实测）
| 检查项 | 结果 |
|---|---|
| 权威生产扫描（已挂载 × 写接口 × 无 Pydantic 信号） | `REMAINING LIVE-GAPS: timeline.py today.py`（二者 POST 无 JSON body，正确排除） |
| `POST /api/auth/register` 空 body | `{"ok":false,"success":false,"data":null,"error":"参数校验失败: company: Field required; phone: Field required; password: Field required","code":422}` ✅ |
| gl_dimensions 修复前/后 | 修复前解析即 500；修复后正确解析并走业务层（不再因解析崩溃）✅ |

### 刻意未覆盖（维持"不碰死代码"决议）
`check` / `doc_ops` / `erp_connect` / `export_ops` / `search_notify` / `disassembly` / `scrap` / `demo_data` 及孤儿 `forecast_router.py` —— 均未 `include_router` 注册，改了不生效。

---

## 二、🔴 新发现：gl_dimensions 是半成品功能（预存在，非本次引入）

经排查，`routers/gl_dimensions.py` 调用的 `db.gl_dimension_*` 系列函数（create / delete / value_create / value_update / value_delete / list / value_list）**在 `erp_db.py` 中全部不存在**（grep `gl_dimension` 0 匹配）。

- 该路由虽已挂载，但 **db 层缺实现**，任意 GET/POST/PUT/DELETE 均 500（`AttributeError: erp_db 无 gl_dimension_*`）。
- 本回合修的是其 **JSON 解析 500 bug + 校验门**；功能本身仍因缺 db 实现而 500。
- **归属**：预存在功能缺口，超出 H1a 校验范围，列为独立待修项。修复需补 `gl_dimensions` / `gl_dimension_values` 的 CRUD 函数（表结构已在 `tenant_1` 存在：`dimension_tags` / `gl_dimensions` / `gl_dimension_values`）。

---

## 三、H2：CSP + 限流 + CORS 白名单 — ✅ 完成且已上线

`/opt/hergent-erp/server.py` 已含三项并验证：

1. **安全响应头中间件**：注入 `Content-Security-Policy`（`default-src 'self'`、`script-src` 含 jsdelivr CDN 白名单、`frame-ancestors 'none'`、`report-uri`）+ `X-Frame-Options: DENY` + `X-Content-Type-Options: nosniff` + HSTS + Permissions-Policy。
2. **slowapi 限流中间件**：`Limiter(default_limits=["300/minute"])`（生产日志 `[RateLimit] Enabled: 300 req/min default`，slowapi 已安装）。
3. **CORS 白名单**：`CORS_ORIGINS` 环境变量驱动（默认 localhost，**非 `*`**）。

### 验证证据
| 检查项 | 结果 |
|---|---|
| `curl -I /api/health` | 响应含 `content-security-policy: default-src 'self'; ...` ✅ |
| 恶意 `Origin: http://evil.example.com` | **无** `access-control-allow-origin` 头（证非通配）✅ |
| `Origin: http://localhost:8700` | 正确回显 ✅ |
| 限流日志 | `[RateLimit] Enabled: 300 req/min default` ✅ |

---

## 四、提交与备份

- 本地 git 提交：`b7f4429` / `5f82c34` / `0f36ff4` / `b5c9721`（全部 `upgrade/v84-international`）。
- GitHub 备份：`git push --force origin upgrade/v84-international` → `876c7df..b5c9721` ✅（历史曾重写，按规约 `--force`）。
- 跟踪文档 `hergent-audit-task-tracking.md` 已校正（H1/H2 标 ✅ + 第七节权威校正）。

---

## 五、结论与后续建议

- **H1（写接口校验）+ H2（安全加固：CSP / 限流 / CORS 白名单）均已完成并上线**，符合"先止血防坑"目标。
- **剩余项（均非阻塞）**：
  1. 死代码路由器（不碰）；
  2. **gl_dimensions db 层 CRUD 半成品**（独立待修，超出 H1a 范围）；
  3. 后续可转 **H3**：DB 连接池 + 幂等迁移（解启动 `duplicate column` 刷屏），或补完 gl_dimensions CRUD。
