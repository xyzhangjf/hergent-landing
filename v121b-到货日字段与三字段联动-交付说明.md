# v121b：新增「到货日」字段 + 报单日 / N / 到货日 三字段双向联动

日期：2026-09-08　状态：**已上线并真机验证通过**　改动范围：**仅前端**（`hergent-cn-v2/src/pages/Rebate.vue`），后端零改动

---

## 一、核心设计决策：到货日不落库

| 项 | 处理 |
|---|---|
| 落库字段 | 只有 `order_lead_days`（N，提前天数）—— 原本就存在，无新增列、无迁移 |
| 到货日 | **派生展示值，不落库** |

理由：到货日若单独存一列，就与「报单日 + N」形成**双源**，改一边必产生矛盾数据。
现在任何时候都只有一个真相：**库里存 N，界面上到货日 = 报单日 + N 实时算出来显示**；用户手改到货日时立即反推成 N 写回。

---

## 二、字段与默认值

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| 首次报单日 `order_first_date` | date（落库） | 空 | 主锚点，绝对日期，跨月连续不重置 |
| 提前天数 `order_lead_days`（N） | int（落库） | 4 | 范围 0~30 |
| **首次到货日**（新增） | date（**派生**，不落库） | = 报单日 + N | 用户可编辑，编辑后立即反推 N |

「即使未手填也必须可见」→ 到货日是 `computed`，报单日与 N 一旦齐全，**立刻自动显示**，无需任何触发动作。

界面位置：① 节奏分区里与 N 同排，左→右即计算链路 ——
`[首次报单日] [报单周期/星期]` → `[提前天数 N] [首次到货日] [最多可提前]`

---

## 三、联动规则（状态流转）

| 操作 | 系统行为 | 实测结果 |
|---|---|---|
| 改报单日 | 按当前 N 重算到货日 | 9/1 + 4 → **2026-09-05** ✅ |
| 改 N | 重算到货日 | N 改 10 → **2026-09-11** ✅ |
| 手改到货日 | 反推 N = 到货日 − 报单日，回填 N | 改 9/15 → N 自动变 **14** ✅ |
| 清空报单日 | 到货日同步清空（N 保留，它不依赖报单日） | 到货 = `""` ✅ |
| 清空 N | 到货日同步清空 + 弹出原因提示 | 到货 = `""`，提示「「提前天数」没填，到货日算不出来 —— 填 0 表示当天到货」 ✅ |
| 清空到货日 | 反推链路失效 → N 同步清空 | N = null ✅ |
| N 非法（负/小数/>30） | 到货日清空，提示具体原因，保存时阻断 | 见下节 |双向依赖清理干净：任一字段空 → 依赖它的值必空，**不残留、不回落默认值**（避免"看着空、实际按 4 天算"）。

---

## 四、校验与边界

| 场景 | 处理 |
|---|---|
| N 非负整数 | `Number.isInteger(n) && 0 ≤ n ≤ 30`；非整数提示「须为整数，不能填 2.5」，负数提示「不能为负（到货日不得早于报单日）」，>30 提示上限 |
| 到货日不得早于报单日 | 手改到货日若 diff < 0 → toast「到货日不能早于报单日（2026-09-20）」，**输入框回弹到原值** |
| diff > 30 | toast「提前天数最多 30 天，当前是 X 天」+ 回弹 |
| 跨月 / 跨年 / 闰年 | 一律用 `Date.UTC` 计算，实测：9/28+4=10/02（跨月）、12/30+4=2027-01-03（跨年）、2024-02-27+2=**2024-02-29**（闰年）、2026-02-27+2=03-01（非闰年） |
| 非法日期（2026-02-30 / 2026-13-01 / 空串 / 乱码） | 解析返回 null → 到货日输出空，不产生脏值 |
| 周末 / 节假日 | **按现有系统规则：不跳过，按自然日直加**。`arrival_schedule.compute_arrival_dates_from_order` 本就无跳过逻辑，界面提示行已写明「按自然日计算，不跳周末/节假日（与后端算法一致）」 |
| 保存时 | 填了报单日但 N 非法 → 阻断并提示（防止存一条"到货日算不出来"的半成品） |
| N 非法/清空时 | 均单请求与 6 期预览**自动回退旧到货口径**（不按 4 天硬算），与界面显示保持一致 |

---

## 五、改动文件

**`hergent-cn-v2/src/pages/Rebate.vue`**（唯一改动文件）

模板：
- ① 节奏区第二行由 `form-grid2` 改 `form-grid3`，新增「首次到货日」输入（`:value` + `@change`，label 带小字「自动算，可改」）
- 提前天数由 `v-model.number` 改为 `:value` + `@input`（为支持"清空"这个独立状态，不再被 `.number` 兜成 0）
- 新增 `leadErr` 告警行；原静态派生说明改为动态：`到货日 = 报单日（2026-09-20） + 2 天 = 2026-09-22（周二）…`

脚本：
- 新增日期工具：`_parseISO` / `_addDaysISO` / `_diffDaysISO` / `_weekdayOf`（UTC 计算，跨月跨年闰年安全）
- 新增 computed：`leadValid` / `leadErr` / `firstArrivalDate` / `arrivalWeekday`
- 新增方法：`onLeadInput`（改/清空 N）、`onArrivalChange`（手改到货日 → 反推 N，异常回弹）
- `loadArrivalPreview` / `loadAutoPeriodPreview`：`order_first_date` 仅在 N 合法时透传，否则回退旧口径；`order_lead_days` 非法时不发
- `save()` 新增一致性校验：报单日已填而 N 非法 → 阻断

---

## 六、验证

1. **纯函数单测 19 例全绿**（跨月/跨年/闰年/非法日期/反推负值/星期）
2. **真机验证**（Chrome + demo boss 登录，hergent.cn 规则编辑弹窗）8 项全绿：
   ```
   0 初始: 报单= N=4 到货=
   A 报单9/1+N4 → 到货=2026-09-05  ✅
   B 改N=10  → 到货=2026-09-11     ✅
   C 手改到货=9/15 → N反推=14      ✅
   D 清空报单日 → 到货="" N保留14   ✅
   E1 报单=9/20 → 到货=2026-10-04  ✅（跨月）
   E2 到货改早于报单 → 被拒回弹    ✅
   F 清空N → 到货="" + 明确告警     ✅
   G 提示行: 到货日 = 报单日（2026-09-20）+ 2 天 = 2026-09-22（周二）… ✅
   ```
3. **后端同源**：`auto-period-preview` 返回 报单 9/9→到货 9/11、9/11→9/13、9/13→9/15，差值恒为 N=2，与前端派生口径一致
4. 构建 `Rebate-UvB6Ceh1.js` 129.53 kB；已 rsync + chown 至 `/opt/hergent-cn-v2/`，`index.html` HTTP 200

---

## 七、遗留 / 注意

- 6 期预览在 demo 租户显示 0 期：该端点按**租户已保存规则**聚合，未保存的报单日不进库，属既有行为（P3 调度未做），非本次回归。
- 周末/节假日跳过**未实现**（现系统规则即不跳过）。若后续要支持「到货日遇周日顺延到周一」，需前后端同步加规则，建议单独立项。
- 前端改动**未 commit**；连同两仓历史未提交项待统一整理。
