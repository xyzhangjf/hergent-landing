# 副驾「角色定位」机制整合到 Web 版 — 评估与设计

> 范围：把桌面版（`desktop-app`）的 AI 团队/角色机制，整合进 Web 主产品（`hergent-cn-v2` 副驾）。
> 结论先行：**建议整合，但必须精选角色集、并服务端化（多租户）**，不能把桌面那套 9 个通用团队原样搬成"通用助手货架"。

---

## 一、评估结论

### 1.1 现状对比

| 维度 | 桌面版 `desktop-app`（已冻结） | Web 副驾 `hergent-cn-v2`（主产品） |
|---|---|---|
| AI 团队 | 有。9 个内置角色（大秘/会计/运营主管/客服助手/程序员/作家/编剧/私教），可自定义 | **无**。直连 Hermes 默认 agent，不注入任何 system prompt |
| 角色存储 | 客户端文件 `roles.json`（每用户一份，非服务端） | 无 |
| 角色实现 | 每个角色 = 一个独立 Hermes agent（`SOUL.md` + `config.yaml` 的 `system_prompt` + 专属 skills/记忆/工作区） | 单一共享 Hermes agent |
| 触发方式 | 左侧头像栏点击切换，存 `localStorage` 上次角色 | — |
| 后端配套 | 无（纯客户端 Electron） | 后端 `server.py` 有一套**旧的** `/api/chat/roles`（仅 dami/accountant/ops 三个，是 ERP 内置旧 chatbot，与桌面不是同一份数据） |

> 注意：Web 副驾当前**不走** `server.py` 的 `/api/chat`，而是 `hermesChat()` 直连 `/hermes/v1/chat/completions`。所以旧 `/api/chat/roles` 是**另一套遗留**，本方案不依赖它，仅作兼容说明（见 §六）。

### 1.2 整合的必要性（3 点）

1. **目标用户高度匹配**：9 个角色里，**运营主管 / 会计 / 客服助手 / 大秘** 对低温奶经销商老板是真实刚需。特别是我们刚装好的 `bank-reconciliation` 对账技能，天然归属「会计」团队。
2. **机制轻量且已验证**：桌面跑通了半年——角色 = `systemPrompt + opening + 可选 skills`，切换只是换 system prompt。**Web 副驾改造成本极低**：`hermesChat` 只需在 messages 前 prepend 一条 system 消息即可，无需另起 Hermes agent。
3. **体验一致性**：用过桌面的用户（包括你自己）会期待 Web 副驾也能"切团队"，否则像是功能回退。

### 1.3 风险与边界（必须守住）

- **不能做成通用 ChatGPT 货架**：战略已收敛为「垂直行业 AI 经营副驾」，护城河是配方化算法 + 行业 know-how，不是通用助手。桌面的 **作家/编剧/私教/程序员** 是消费级通用团队，原样搬进来会稀释定位、且和免费通用助手直接竞争（畅捷通精易版已内置"AI 问数"，这正是我们要避开的方向）。
- **不能做成客户端文件模型**：Web 是多租户 SaaS，角色必须**服务端化、按租户隔离**，老板能给自己团队增删改 AI 团队。

### 1.4 结论

**整合"角色框架"，精选"垂直角色集"**：
- 框架（切换器 + 多租户角色表 + system prompt 注入）全量做；
- 默认角色只内置 **经营副驾（主驾）/ 运营主管 / 会计 / 客服助手 / 大秘** 五个；
- 作家/编剧/私教/程序员列为**可选的"通用助手包"**（P2，默认不开放），由老板在设置里手动开启。

---

## 二、角色数据结构（复用桌面版，服务端化）

### 2.1 桌面版 `DEFAULT_ROLES` 字段（直接复用）

```js
// desktop-app/src/main/roles-data.js
{ name, systemPrompt, opening, builtIn, avatarPreset? }
```

- `name`：显示名（如"运营主管"）
- `systemPrompt`：团队核心指令（注入 Hermes 的 system 消息）
- `opening`：首次进入的欢迎语
- `builtIn`：是否内置（内置不可删，自定义可删）
- `avatarPreset`：头像预设（manager/secretary…），Web 端映射为 emoji/图标

### 2.2 Web 端新增表 `ai_roles`（多租户）

```sql
CREATE TABLE ai_roles (
  id          INTEGER PRIMARY KEY,
  tenant_id   INTEGER NOT NULL,          -- 租户隔离
  role_id     TEXT    NOT NULL,          -- 稳定 key：ops / accountant / cs / dami ...
  name        TEXT    NOT NULL,
  system_prompt TEXT  NOT NULL,
  opening     TEXT,
  avatar      TEXT,                      -- emoji 或图标名（替代 desktop 的 avatarPreset）
  built_in    INTEGER DEFAULT 1,         -- 1=内置种子 0=老板自定义
  is_active   INTEGER DEFAULT 1,         -- 是否对当前租户开放
  sort_order  INTEGER DEFAULT 0,
  UNIQUE(tenant_id, role_id)
);
```

**复用方式**：桌面 `DEFAULT_ROLES` 作为**全局种子**，首次访问时按租户复制进 `ai_roles`（与 `datasource_adapter` 的"默认源=local"同源思路）。桌面字段 1:1 映射，仅把 `avatarPreset` → `avatar`（emoji）以适配 Web 主题。

---

## 三、需要新增/调整的接口（后端 `server.py`）

> 全部走租户上下文（`/api/ai/*` 已在 RBAC 映射到 `chat` 模块），老板可管。

| 方法 | 端点 | 作用 | 说明 |
|---|---|---|---|
| GET | `/api/ai/roles` | 列出当前租户可用角色 | 返回 `ai_roles` 中 `is_active=1`，含 `system_prompt`/`opening` |
| POST | `/api/ai/roles` | 老板新建自定义角色 | `built_in=0`，写入当前租户 |
| PUT | `/api/ai/roles/{role_id}` | 编辑角色（改名/改团队/开关） | 内置角色仅可改 `is_active`，不可删 |
| DELETE | `/api/ai/roles/{role_id}` | 删除自定义角色 | 仅 `built_in=0` |
| POST | `/api/ai/roles/seed` | 手动触发种子初始化 | 幂等，已存在则跳过 |

**聊天端点改造**：副驾聊天目前由前端 `hermesChat` 直连 Hermes。两种落地方式（推荐 A）：

- **方案 A（推荐，零后端依赖）**：前端 `hermesChat(messages, {role})` 在发往前 prepend system 消息：
  ```js
  const sys = [{ role:'system', content: currentRole.system_prompt }];
  fetch('/hermes/v1/chat/completions', { body: JSON.stringify({ model, messages: [...sys, ...messages] }) })
  ```
  后端无需改 chat 路由，角色数据经 `/api/ai/roles` 取。
- 方案 B（经后端中转）：副驾也改走 `server.py /api/chat`，复用其 `role→system_prompt` 三级管线。但那套是旧 chatbot 语义，且与当前卡片抽取/复盘流水线耦合不同，**不推荐**，避免回退已有副驾能力。

> 兼容：旧的 `/api/chat/roles`（ERP 内置 chatbot 的 dami/accountant/ops）**保留不动**，属于另一产品模块；本方案的 `/api/ai/roles` 是副驾专属、多租户、字段更全。两套并存不冲突（见 §六）。

---

## 四、Web 端角色切换与管理的交互逻辑（`hergent-cn-v2`）

### 4.1 切换器（CopilotDrawer 输入框左下角，WorkBuddy 范式）
- 采用与 WorkBuddy 一致的**输入区团队胶囊**：在输入框左下角（「＋」附件按钮左侧）常驻一颗胶囊，显示当前角色头像 + 名称 + 下拉箭头；点击展开角色下拉列表（emoji 头像 + 名 + 一句话定位）。
- **不另起头部横条**：副驾头部已有标题 + 全屏按钮，团队放输入区更贴合"以谁的身份说这句话"的心智，也不挤占头部空间；角色多时下拉永远只占一格，不会像横条那样越加越长。
- 切换即生效：**不重置对话**，仅影响后续消息注入的 system prompt（符合"跟不同同事说话"的心智）。
- 首次进入默认 `经营副驾`（主驾）。
- 状态存 `store.chat.currentRole`（会话级），并 `localStorage` 记住上次角色（对齐桌面 `hermes_last_role`）。

### 4.2 会话级角色状态
- 每条消息发送时携带当前 `currentRole.system_prompt` 作为 system 消息。
- 多轮对话内角色保持一致；切换角色后新消息用新团队，历史消息保留（Hermes 按整段 messages 理解，system 始终置顶）。

### 4.3 角色管理页（老板用）
- 入口：`设置 → AI 角色`（新增页或弹窗）。
- 能力：查看内置 5 角色、开关（如关掉"大秘"）、编辑 `system_prompt`/`opening`/emoji、新建自定义角色（如"采购专员"）。
- 校验：团队字数上限、不可删内置、自定义可删。

### 4.4 前端模块改动清单
| 文件 | 改动 |
|---|---|
| `src/api/client.js` | `hermesChat(messages, { role, onDelta, model })` 支持 prepend system |
| `src/store/index.js` | 增 `chat.currentRole`、角色列表 `chat.roles`；actions：`loadRoles()` / `setRole()` |
| `src/components/CopilotDrawer.vue` | 输入框左下角加团队胶囊 + 下拉（替代头部横条，对齐 WorkBuddy）；`send()` 取 `currentRole.system_prompt` 注入 |
| `src/pages/Settings.vue`（或新 `Roles.vue`） | 角色管理 UI |
| `src/api/aiRoles.js`（新） | 封装 `/api/ai/roles` 的 CRUD |

---

## 五、与桌面版角色配置的兼容策略

1. **不实时同步**：桌面已冻结、且是客户端 `roles.json` 单用户模型，Web 是多租户服务端模型，二者生命周期不同，**不做双向同步**。
2. **数据结构 1:1 复用（单次种子）**：桌面 `DEFAULT_ROLES` 的字段（`name/systemPrompt/opening/builtIn/avatarPreset`）作为 Web `ai_roles` 的种子源，复制时 `avatarPreset → avatar(emoji)`。即"数据契约一致，不共享运行时"。
3. **未来迁移工具（可选 P2）**：写一个 `import_desktop_roles(tenant_id, roles_json)` 脚本，把某台桌面的 `roles.json` 导入成该租户的自定义角色——仅当用户明确要求"把我桌面配的团队搬到 Web"时才用，默认不跑。
4. **团队文案统一维护点**：建议把 `DEFAULT_ROLES` 的"权威副本"放到 Web 后端 seed 里，桌面版冻结后不再改；避免两处漂移。

---

## 六、与现有 `/api/chat`（ERP 旧 chatbot）的关系

- 后端 `server.py` 现有 `/api/chat` + `/api/chat/roles` 是 **ERP 后台内置的旧 AI 助手**（dami/accountant/ops 三角色，带本地 CRUD 三级管线），服务于 `erp.hergent.cn` 的聊天入口。
- Web 副驾（`hergent.cn`）走独立 Hermes 通道，本方案的 `/api/ai/roles` 是副驾专属。
- **两者并存、互不影响**：老板在 ERP 后台看到的"AI 角色"和副驾里的"团队"是两套，本方案不改动旧 `/api/chat`，仅在副驾侧新增。文档/培训需说明此区分，避免混淆。

---

## 七、实施分期

- **P0（核心，建议先做）**：`ai_roles` 表 + 种子（精选 5 角色）+ `GET/POST/PUT/DELETE /api/ai/roles` + 前端输入框团队胶囊（WorkBuddy 范式）+ `hermesChat` 注入 system prompt。行为保真，不改变现有副驾能力。
- **P1**：老板自定义角色（增删改 UI 完整）+ 角色开关 + 会话级记忆（可选：按角色隔离 Hermes 会话）。
- **P2（可选，防稀释）**："通用助手包"开关（作家/编剧/私教/程序员，默认关）；桌面 `roles.json` 导入工具。

---

## 八、风险与验证

- **回退简单**：角色机制是"注入 system prompt"的纯加法，不下线任何现有能力；若不要某角色，置 `is_active=0` 即可。
- **真机验证（沿用项目铁律）**：P0 完成后必须无头浏览器真机验证——切换角色后首条消息确实带新团队（可让"会计"角色输出对账引导验证）、卡片抽取/复盘流水线不受影响、老板自定义角色后刷新仍在。
- **不破坏多租户**：`ai_roles` 全表带 `tenant_id`，所有端点走租户上下文，复用现有隔离。
