# 评估：NeoBrowser 能否解决「舟谱没有开放 API」— 2026-09-07

## 结论（先讲结果）
**能解决"进不去"，解决不了"没有 API"。**

NeoBrowser（Rust 写的 MCP server，驱动本机真实 Chrome、复用真实登录态）补齐的是**登录态与指纹**这一层，
数据获取方式仍然是 **UI 抓取**，不是 API 契约。所以它把问题从「登录墙 + 机器人检测」变成了
「用你自己的账号、自己的浏览器，把手工导出自动化」。

定位：**自用降本（推荐）**；**不作为 Hergent 的标准数据接入能力**。

## 它解决了什么
| 痛点 | NeoBrowser | 说明 |
|---|---|---|
| headless 无 cookie | ✅ | 复用真实 Chrome profile 的登录态 |
| navigator.webdriver / 假 GPU 被识别 | ✅ | 真二进制真指纹，通过 bot.sannysoft |
| 需要反复人工登录 | ✅ | 解密钥匙串里的 cookie 注入自动化浏览器 |
| CAPTCHA | ⚠️ | 主动检测后交回控制权（设计如此），**不保证无人值守** |

## 它解决不了什么（关键）
1. **没有 schema**：页面改版即断，维护是长期成本 → 触碰红线①「运维成本不可失控」。
2. **架构位置不对**：MCP stdio 必须与 agent 同机。Hermes 生产在云服务器（Linux，无桌面 Chrome、无用户登录态），
   所以 NeoBrowser **只能跑在终端用户本机**，成不了云端统一接入层。
3. **合规**：自动化登录第三方 SaaS 取数可能违反舟谱 ToS；封号风险由账号持有人承担。
   若托管客户账号，风险由客户承担 → 触碰红线③「合规禁踩」。
4. **成熟度**：项目 2026-08-18 才发布（MIT，作者 Daniel Perez Pinazo），HN 上有质疑（vibe-coded、
   演示 GIF 疑似泄露密码）；`curl | sh` 属供应链风险。开 `NEOBROWSER_REAL_PROFILE` 等于把整个 Chrome 登录态交给 agent。

## 与现战略的关系
战略是「不造 ERP，只做 AI 层，数据走 Excel 上传 / 官方 Connector」。
NeoBrowser 的价值是**加固既有 Excel 路线**：把「人工登录 → 导出 Excel → 上传」的上游人工环节自动化，
让客户侧几乎无感，而 Hergent 侧仍只接收 Excel——**接入口径不变，无需舟谱配合**。

## 建议动作
- ✅ **自用**：你自己的账号、自己的数据、自己的机器 → 风险最低，省的是实打实的时间。
- ⚠️ **对客户（以后）**：只做「客户侧本地采集器 + 只上传 Excel」，客户书面授权；不托管客户账号、不写进标准卖点。
- ❌ **不做**：服务端爬虫、托管客户账号、把舟谱抓取当成产品核心依赖。

## 动手前必须先回答的三问（30 分钟可行性探针）
1. 舟谱登录是否有验证码 / 短信二次验证？登录态能保持几天？
2. 报表页是否有「导出 Excel」按钮？有 → 抓下载即可；没有 → 只能抄 DOM 表格，成本翻倍且更脆。
3. 每天 1 次的访问频率是否会触发风控（IP 异常、频繁登录告警）？

## 探针执行纪律（若要试）
- 用**独立 profile**（`NEOBROWSER_PROFILE=zhouspu`），**不开** `NEOBROWSER_REAL_PROFILE`，不暴露个人全部登录态。
- 不在生产服务器（47.113.224.140）安装。
- 先 `neobrowser doctor` 自检，再手动登录一次舟谱，观察 3 天稳定性再谈定时任务。
