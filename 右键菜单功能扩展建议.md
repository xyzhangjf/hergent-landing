# 右键菜单（反键）功能扩展建议

> 适用：预报订单汇总表（列表视图可编辑网格）表体。
> 现状右键菜单：`粘贴` / `复制选区` / `插入行(上/下)` / `删除行` / `插入列(左/右)` / `删除列` / `清空单元格` / `撤销` / `重做`。
> 目标：把"反键"从"表格编辑工具"升级为"低温奶预报业务的快捷入口"——这是 Excel 没有、而我们 AI 副驾 + 行业 know-how 能做的地方。

---

## 一、产品特性锚点（为什么反键适合我们）

| 特性 | 含义 | 反键如何放大价值 |
|---|---|---|
| **AI 经营副驾**（Hermes + ResultCard/AdvicePanel） | 已有货损/返利/工资/预报四张只读聚合卡 | 从"看数据→切去问 AI"压缩成"右键商品→一键出建议卡" |
| **低温奶强时效**（短保/临期/货损） | 保质期 1–7 天的商品风险极高 | 右键直接"按安全库存补齐""看批次到期"，防断货/防损耗 |
| **经验依赖重**（老员工凭手感定预报量） | 老板/督导最贵的是经验 | 右键"AI 生成建议量""设为此商品默认值"，把经验沉淀进系统 |
| **多角色协作**（老板/财务/销售/促销/督导/经销商） | 列权限已落地 | 右键"提交审批""@督导""锁定行"，把协作动作收进表体 |
| **模块孤岛**（预报/对账/货损/工资/返利） | 数据同源但页面分散 | 右键"跳转对账/货损/工资"，一键打通业务闭环 |

---

## 二、建议新增右键功能（分五类）

### A. AI 副驾类（我们的护城河，Excel 没有）

| 功能 | 触发位置 | 产品价值 | 难度 | 依赖 |
|---|---|---|---|---|
| **AI 分析此商品** | 右键商品名/行 | 选定商品→调 Hermes 读库存/批次/到期/专属价/历史动销→生成货损·补货·毛利建议卡落入副驾面板 | 中 | Hermes 桥接(已生产) + 副驾面板(已有) |
| **AI 生成下单建议** | 右键数量格/行 | 基于安全库存+在途+历史动销+返利窗口→生成建议量，**填到格需人点确认**（符合"AI 只建议不擅自下单"） | 中 | forecast_engine 已有计算能力 |
| **AI 批量复盘选中行** | 右键选区/多行 | 对选中商品做一段复盘小结（动销偏差/货损归因） | 中 | Hermes + 选区 |

### B. 业务语义类（低温奶/预报专属）

| 功能 | 触发位置 | 产品价值 | 难度 | 依赖 |
|---|---|---|---|---|
| **按安全库存补齐** | 右键数量格 | 一键把该格填到 `safety_stock`，防断货（短保高频痛点） | 低 | 前端读 `rw.safety_stock` |
| **设为此商品默认值** | 右键数量格 | 把当前值存为"商品×客户"默认预报量，下期次自动带出，减少重复录 | 中 | 需后端存默认(DataSourceAdapter)或前端草稿 |
| **复制此商品到下期次** | 右键行 | 跨期次复用上次预报，老客户续订零成本 | 中 | 期次切换逻辑(已有) |
| **查看商品档案** | 右键商品名 | 弹层显示库存/批次数/到期/专属价/近期动销，核对依据 | 低 | 前端已有数据，弹层即可 |

### C. 导航联动类（打通模块孤岛）

| 功能 | 触发位置 | 产品价值 | 难度 | 依赖 |
|---|---|---|---|---|
| **跳转到对账/货损/工资** | 右键行 | 选中商品→打开对应模块并预筛，业务闭环一键达 | 中 | 路由/模块(已有) |
| **导出选中行为 Excel** | 右键选区/行 | 选区→下载 xlsx，给督导/老板离线看 | 低 | xlsx 导出(已有按钮可复用) |
| **表头右键：排序/隐藏/冻结此列** | 右键表头 | 列操作前置到右键，不用进列设置菜单 | 中 | 列模型(已有) |

### D. 协作审批类（多角色）

| 功能 | 触发位置 | 产品价值 | 难度 | 依赖 |
|---|---|---|---|---|
| **提交此行审批** | 右键行 | 标记行状态=待审，触发@督导，和既有审核台打通 | 中 | 审批流(已有舟谱模板/审核台) |
| **锁定此行** | 右键已推送/已审行 | 锁死防误改，配合列权限形成行级防护 | 中 | 行状态字段 |
| **@ 对应角色提醒** | 右键行 | 拉群/发消息提醒督导或老板跟进 | 中 | 企微/飞书连接器(当前 disconnected) |

### E. Excel 式增强（纯前端，低风险）

| 功能 | 触发位置 | 产品价值 | 难度 | 依赖 |
|---|---|---|---|---|
| **选区求和/平均/计数**（状态栏） | 右键选区 | 仿 Excel 底部状态栏，快速核数 | 低 | 纯前端 |
| **复制整行** | 右键行 | 复制该行到下方，续订场景常用 | 低 | 纯前端 |
| **转置粘贴** | 右键单元格 | 行列翻转粘贴，贴错方向时救场 | 中 | 剪贴板 API |
| **插入 N 行** | 右键行 | 批量插入，不用点 N 次 | 低 | 纯前端 |

---

## 三、落地路线（按"价值/难度/依赖"排序）

**P0 — 纯前端、低难高价值，建议立刻做**
1. `按安全库存补齐`（B）— 直击短保断货痛点
2. `查看商品档案` 弹层（B）— 核对依据，只读无风险
3. `导出选中行为 Excel`（C）— 复用已有导出
4. `选区求和/平均/计数` 状态栏（E）— Excel 习惯
5. `复制整行`（E）— 续订高频
6. 表头右键 `隐藏/冻结/排序`（C）— 列操作前置

**P1 — 需后端或副驾桥接**
7. `AI 分析此商品` / `AI 生成下单建议`（A）— 护城河
8. `设为此商品默认值`（B）— 需存默认量
9. `复制此商品到下期次`（B）— 期次逻辑
10. `跳转到对账/货损/工资`（C）— 模块路由
11. `提交此行审批` / `锁定此行`（D）— 行状态

**P2 — 生态/协作（依赖连接器）**
12. `AI 批量复盘选中行`（A）
13. `@ 对应角色提醒`（D）— 依赖企微/飞书连接器(当前 disconnected)
14. `转置粘贴`（E)

---

## 四、红线提醒（与既定战略一致）

- **AI 只建议不擅自下单**：所有 AI 右键项生成的是"建议卡/建议量"，定稿必须人点确认，不静默回写订单。
- **不造 ERP**：右键不做任何新 order 写操作；"提交审批""锁定行"只是状态标记，回写一律走连接器/审核台。
- **配方化差异化**：补齐逻辑、默认量口径应走配方配置，不硬编码，方便每客户差异化。
- **不破坏数据模型**：新增右键项不引入合并单元格、不造公式引擎（用"计算列自动派生"替代）。

---

## 五、我建议的第一步

把 **P0 六项**做成一批（纯前端、零后端依赖、零战略风险），让右键菜单从"编辑工具"变成"业务入口"。其中 `按安全库存补齐` 和 `查看商品档案` 最贴合低温奶场景，用户感知最强。

要我直接按 P0 全做吗？
