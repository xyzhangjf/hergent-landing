# 预报订单导入模版 与 价格渠道配置 设计方案

> 2026-09-14 · 基于代码实读（非记忆），所有断言均附文件:行号
> 关联附件：`价格表20260913.xlsx`、`2026年9月13日到货下单表.xlsx`
>
> **v2 修订说明（重要）**：v1 把「永辉价 / 沃尔玛价 / 美联价」当成三个固定列设计，本质是把
> **单一客户的客户结构写进了产品代码**。别的经销商客户可能是大润发、华润、自家车队，甚至
> 不分渠道 —— 那样这套模版对他们是坏掉的。v2 改为**「价格渠道」可配置模型**：
> 渠道是**数据**，不是列名、不是表字段、不是代码分支。
>
> **用户原话**：「设计功能时要从用户角度出发，而不是从我个人或单一客户的角度考虑。
> 我的客户包括永辉、沃尔玛、美联，但其他客户的情况并不相同，因此不要把价格写死在代码里，
> 应改为让客户可以自行配置。」

---

## 〇、先摆事实：现状核实结果（v2 复核确认）

### 1. 导入模版的真实字段清单

`server/routers/import_router.py:1077` `_TEMPLATE_FIELDS["forecast_cross"]` —— **7 个商品列**：

| 序 | 当前列名 | 必填标记 | 用途 |
|---|---|---|---|
| 1 | 商品条码 | `*` 硬必填 | 识别商品、自动建档的去重口径 |
| 2 | 商品名称 | `☆` 条件必填 | 建档 |
| 3 | 规格 | `☆` 条件必填 | 建档 + 算件数 |
| 4 | 单位 | `☆` 条件必填 | 建档 |
| 5 | 厂价 | `☆` 条件必填 | 算「本期需付款」 |
| 6 | 品牌 | 选填 | 区分共码商品 |
| 7 | 分销价 | 选填 | 出自提单 |
| 8… | *报单对象列* | — | 按「报单配置」动态生成，列头 = 报单人/门店 |

必填标记三态见 `import_router.py:1113-1127`：`*` = 红底白字／`☆` = 黄底**条件**必填／无标记 = 选填。

### 2. 导入识别器的真实字段集

`import_router.py:121-132` `COLUMN_PATTERNS["forecast_cross"]` —— **9 个字段**（比模版多 `rhythm` 订单排期）：

```
barcode → spec → unit → brand → factory → dist → rhythm → price → name
```

⚠️ **dict 顺序 = 字段优先级（先命中者赢）**（`:118-120` 注释）。`price` 字段的关键词含「价格」「单价」
→ **任何带「价格」二字的新列都会被它抢走**。这是"写死列名"路线的必然产物，v2 用动态匹配根治。

### 3. 取价链路（问题根因）

导入侧 `import_router.py:441-443`：

```python
fp_row   = num_cell(row, "factory") or num_cell(row, "price")
dist_row = num_cell(row, "dist")
price    = fp_row or dist_row          # 厂价 → 通用单价 → 分销价
```

生成侧 `routers/forecast.py:601`：

```python
price = _round_price(it.get("price"))   # 直接取上面那个行级单一值
```

**结论：这是行级单值，与报单对象完全无关** → 永辉店、沃尔玛店、美联店当期拿到的单价必然相同。
真实业务要求（附件4 口径实测）：**渠道价按客户身份取**。该规则应做未做。

### 4. 自提模板生成时的硬编码清单（v2 新增核实）

`routers/forecast.py:455-470`：

| 硬编码 | 值 | 行号 | 问题 |
|---|---|---|---|
| `ZT_HEADERS` | 舟谱自提 22 列表头 | `:455` | ✅ **正确保留** —— 这是对接方（舟谱）的格式契约，不是我们的业务配置 |
| `DB_HEADERS` | 舟谱调拨 13 列表头 | `:461` | ✅ 同上 |
| `ZT_SALESMAN` | **`"张俊峰"`** | `:467` | 🔴 多租户下每家的业务员都会写成张俊峰 |
| `ZT_DEPT` | **`"湖北福宝商贸有限公司"`** | `:468` | 🔴 别人家的公司名被写成你的 |
| `ZT_WAREHOUSE` / `DB_SRC_WAREHOUSE` | `"总仓"` | `:469`/`:470` | 🔴 仓库名写死 |
| `_UNIT_HINTS` | 名称关键词→单位推断 | `:473` | 🟡 靠猜；应优先读 `forecast_barcode_units` 配置 |
| 单据号起始 `seq = 21` | `:617` | 🟡 与厂家约定的号段，宜可配 |

⚠️ **必须区分两类"固定"**（这是本设计的核心判断）：
- **对接格式固定 = 对的**。`ZT_HEADERS` 是舟谱规定的列，不能由我们的客户改。
- **业务配置硬编码 = 错的**。业务员、公司名、仓库、渠道清单，每家不同，必须可配。

### 5. 其它已核实事实

| 事实 | 位置 | 影响 |
|---|---|---|
| 厂家编码列**不在**识别器也不在模版 | `COLUMN_PATTERNS` 无对应字段 | 导入附件1 时该列被当"编码列"跳过，值丢弃 |
| `products.product_code`（厂家编码）**列已存在** | `db/queries/products.py:15` 白名单 | 只需加进模版+识别器，无需迁移 |
| `products.yh_code`（永辉编码）**不存在** | 全库 0 命中 | v2 不再加这个列（会重犯写死的错，见 §二） |
| 渠道价表**不存在** | 全库 0 命中 | 系统只存得下 1 个渠道价（`products.dist_price`） |
| `report_mapping.order_template`（"单型"）**只写不读** | UI 有输入框（`ReportMapping.vue:139`），引擎只读 `counterparty_type`（`forecast.py:615`） | **一处已存在的"填了不生效"死配置** —— 前车之鉴 |
| 调拨模板用了配置的 `employee_name` | `forecast.py:651` | 只有调拨用了 |
| **舟谱模板生成的前端入口不存在** | `hergent-cn-v2/src/` 搜 `zhoupu`／`import-templates`／`舟谱` = **0 命中** | 后端引擎是「孤儿能力」，用户够不着 |
| 导入模版**无指纹/版本号**，无表头白名单 | `import_router.py` 全文件 | 任何 Excel 只要表头碰巧含关键词就能导入 |
| `report_mapping.counterparty_id` **存在** | `erp_db.py:614` | 报单对象可指向真实客户 → 渠道绑定的天然挂点 |
| `forecast_config(kind, payload, updated_at)` = 租户级 KV | 厂价闸门已在用（`db/queries/products.py:193`） | 渠道字典可选用同族存储，或独立建表 |

---

## 一、设计原则：什么可配置、什么必须固定

先从"用户视角"划三条线，后面所有设计都由它推导：

| 类别 | 内容 | 判据 | 例子 |
|---|---|---|---|
| **① 必须由客户自己配** | 任何反映"这家经销商怎么做生意"的东西 | 换一家客户就不同 | 渠道清单（叫什么、有几条）、各渠道的价、客户属于哪个渠道、业务员、公司名、默认仓 |
| **② 必须固定（不能配）** | 外部系统的对接契约 / 物理事实 | 换谁来都不变 | 舟谱 22 列格式、报单数量 = 最小销售单位、一箱 = 规格个 |
| **③ 必须唯一（只能有一处）** | 口径计算本身 | 每多一份拷贝就多一个漂移源 | 取价函数、厂价判据、件数换算 |

⚠️ **最容易搞混的是 ① 与 ③**：把 ① 的清单写进 ③ 的代码里 ——
例如 `if 客户名 == "永辉": 取永辉价`。这既不能配（客户加一个渠道要改代码），又不唯一（散落多处）。

**v1 的错误正是把 ① 当成 ② 了**：把"永辉/沃尔玛/美联"当成不变的列名。

---

## 二、数据模型：渠道是数据，不是列名

### 2.1 渠道字典 `price_channels`（租户级）

一条渠道 = **一套外部系统的对接契约** = 一套商品编码 + 一套价格 + 一个下单出口。
把「编码」和「价」放在同一个维度上，是因为它们在业务上本就是一回事
（你的背景描述就写着：「厂商系统商品编码：用于用户到厂家系统下单；永辉系统商品编码：用于用户到永辉系统下单」
—— 编码和价都属于"这个渠道"）。

```sql
CREATE TABLE IF NOT EXISTS price_channels (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  tenant_id    INTEGER DEFAULT 0,
  code         TEXT NOT NULL,          -- 机器码，租户内唯一，如 factory / dist / yh
  name         TEXT NOT NULL,          -- 显示名，如「永辉系统」
  col_prefix   TEXT DEFAULT '',        -- 模版列名前缀，默认取 name 去掉「系统」二字
  kind         TEXT DEFAULT 'retail',  -- factory | distribution | retail（决定默认归属与出单方向）
  price_source TEXT DEFAULT 'matrix',  -- 价从哪读，见 2.4
  code_source  TEXT DEFAULT 'matrix',  -- 商品编码从哪读
  is_default   INTEGER DEFAULT 0,      -- 兜底渠道（通常是分销），租户内至多一条
  sort         INTEGER DEFAULT 100,
  is_active    INTEGER DEFAULT 1,
  created_at   TEXT DEFAULT (datetime('now','localtime'))
);
```

**这条设计直接回答"不要写死"**：
- 你的租户建 4~5 条（厂家系统 / 分销价 / 永辉系统 / 沃尔玛系统 / 美联系统）
- 隔壁老王的租户建 2 条（厂家系统 / 分销价）
- 再一家建 6 条（加了大润发、华润）
- **三条租户共用同一份代码，零分发支**

### 2.2 价格矩阵 `product_channel_prices`（商品 × 渠道）

```sql
CREATE TABLE IF NOT EXISTS product_channel_prices (
  product_id    INTEGER NOT NULL,
  channel_id    INTEGER NOT NULL,
  external_code TEXT DEFAULT '',      -- 该渠道下的商品编码
  price         REAL    DEFAULT 0,    -- 该渠道下的价格
  updated_at    TEXT,
  PRIMARY KEY (product_id, channel_id)
);
```

**为什么用矩阵表而不是给 `products` 加列**：
- 加列 = 每加一个渠道要 `ALTER TABLE` + 改 5 处读代码 → 就是"写死"
- 矩阵表 = 加渠道只是插一行数据，**零 DDL、零代码改动**
- 粒度天然就是「商品 × 渠道」，与 `products.dist_price` 同级

⚠️ **厂家渠道不搬进矩阵表**（防第二份拷贝）：
`products.factory_price` / `products.product_code` 已被 v158 闸门、补价面板、
付款口径全链依赖，**继续作为厂家渠道的权威值**，由 `price_source='products_factory'` 指向它。
**绝不把同一个量存两份** —— 这是本项目反复踩过的坑（`products.factory_price` 加列后
与 `purchase_price` 形成第二份拷贝，269/269 全空即为代价）。

### 2.3 客户 → 渠道绑定

**渠道是"客户自身的属性"，不是"报单配置上的开关"**。分三层，后者覆盖前者：

| 层 | 存哪 | 用途 | 默认 |
|---|---|---|---|
| 1 | 客户/门店档案上的 `channel_id` | 这家店在哪个系统下单 —— 固有属性 | 租户默认渠道 |
| 2 | `report_mapping.channel_id` | 覆盖位（同一客户在不同报单对象下取价不同时的例外） | `0` = 继承层 1 |
| 3 | `price_channels.is_default` | 最终兜底（通常是分销价） | 必有一条 |

⚠️ **绝对不要靠客户名关键词猜**（`"永辉" in 客户名`）。理由：
- 门店名会变、会新开（"永辉东津店"、"永辉吾悦店"…下个月还有新店）
- 客户名可能同时含两个字（"永辉美联合作店"）
- 名字是**人的标签**，渠道是**业务的属性**，两者不等价

### 2.4 三层的唯一解析实现

```python
# server/db/queries/prices.py  ← 唯一实现，前端不得另造一份
def resolve_report_channel(mapping) -> channel_row | None:
    """报单对象 → 渠道。判决顺序（先命中者赢）：
       1) mapping.channel_id > 0          → 用它（显式覆盖）
       2) counterparty_type == self_warehouse → 走调拨，不取价（直接返回 None）
       3) counterparty_id 指向的客户有渠道 → 用客户渠道
       4) 否则                             → is_default 渠道
    """

def resolve_channel_price(product_id, channel) -> (price, source, missing):
    """商品 + 渠道 → 价。按 channel.price_source 分派：
       'products_factory' → products.factory_price
       'products_dist'    → products.dist_price
       'matrix'           → product_channel_prices.price
       统一口径：> 0 才算已录；解析往宽（认 "1,234.5"），异常不抛。
       missing=True 时 price 返回 0，调用方必须显式回退并回报（见 §三）。"""

def resolve_channel_code(product_id, channel) -> (code, source):
    """同上，取该渠道的商品编码。'none' → 返回空字符串。"""
```

`price_source` / `code_source` 用**有限的枚举 token**（`products_factory` / `products_dist` / `matrix` / `none`），
不是原始列名 —— 避免把 SQL 列名当数据存（注入面 + 改 schema 就静默失效）。

---

## 三、取价逻辑：唯一实现 + 显式回退

### 3.1 生成自提模板时

```
对每个 (报单对象 alias, 商品 pid)：
  渠道     = resolve_report_channel(mapping)          # §2.4
  price,src,missing = resolve_channel_price(pid, 渠道)
  if missing:
      回退到 is_default 渠道并计入 fallback 列表（不静默）
  写进 ZT_HEADERS 的 *单价(折后价)
```

**替代掉现在的 `price = _round_price(it.get("price"))`。**

### 3.2 回退必须显式回报（铁律）

🔴 **本项目已重复踩过两次的坑**：降级时把差异抹掉，用户永远看不到。
（例：付款口径里厂价缺失回退销售价、聚合接口把"未纳统量"归零。）

因此 `_build_zhoupu_data` 的返回体必须新增：

```python
"channel_fallback": [
    {"alias": "永辉东津店", "channel": "永辉系统", "fallback_to": "分销价", "items": 3},
    ...
],
"channel_fallback_count": 7,
"channel_missing_cells": 12,
```

前端在生成结果里展示「**7 处渠道价缺失，已按分销价生成，请核对**」——
**绝不静默取一个错价写进下单凭证**。

### 3.3 一个容易做反的细节：价格字段与描述字段的合并语义**相反**

现行「只补空」规则（`db.product_backfill_from_import`）对**描述类**字段（规格/单位/品牌）是**对的**：
导入行不该覆盖人工维护的档案描述。

但对**价格类**字段，同一规则是**错的**：
- 价格表每月更新，新价必须覆盖旧价 —— "只补空"意味着**第一次导入的价永远改不掉**
- 现行的 `products.dist_price` 正好吃了这个亏

**建议**：

| 字段类别 | 合并语义 | 回执 |
|---|---|---|
| 描述类（名称/规格/单位/品牌） | **只补空**，绝不覆盖 | `products_backfilled` |
| 价格类（厂价/渠道价） | **非空即覆盖更新** | `prices_updated` + `prices_unchanged` |

两者**分列回报**，用户能看到"这次更新了 N 个价、N 个没变"。

---

## 四、两个出口都动态化

### 4.1 导入模版：商品区列**按渠道字典动态生成**

```
固定基础列：商品条码* │ 商品名称☆ │ 规格（箱入）☆ │ 单位☆ │ 品牌 │ 厂价☆
动态渠道列：对每条 range(is_active) 渠道生成一组：
             {col_prefix}系统商品编码   {col_prefix}价
```

**三个租户下载到的模版会长得不一样**：

| 租户 | 下载到的商品区列 |
|---|---|
| 你 | 条码 / 名称 / 箱入 / 单位 / 品牌 / 厂价 / 分销价 / 永辉编码 / 永辉价 / 沃尔玛编码 / 沃尔玛价 / 美联编码 / 美联价 |
| 只做永辉的经销商 | 条码 / 名称 / 箱入 / 单位 / 品牌 / 厂价 / 分销价 / 永辉编码 / 永辉价 |
| 不分渠道的经销商 | 条码 / 名称 / 箱入 / 单位 / 品牌 / 厂价 / 分销价 |

**这是"不写死"最直观的体现** —— 没有渠道就不会出现一堆空列。
同时也解决了 v1 那个荒唐的约束：「新价格列必须叫『永辉价』不能叫『永辉价格』」——
那是被 `price` 泛词逼出来的脆弱约定，v2 从根上不需要它。

### 4.2 导入识别器：两阶段匹配（顺带修掉泛词抢列）

`COLUMN_PATTERNS` 现在是一层静态关键词 dict。改为**两阶段**：

```
阶段 1 · 动态精确匹配（优先）
  读租户渠道字典 → 对每条渠道生成精确列名规则：
     "{col_prefix}价" / "{col_prefix}价格" / "{col_prefix}系统商品编码" / "{col_prefix}编码"
  命中 → 直接归到该渠道（不再往下走）

阶段 2 · 静态关键词匹配（兜底）
  现有 barcode → spec → unit → brand → factory → dist → rhythm → price → name
```

**收益**：
- 客户把列写成「永辉价格」还是「永辉价」**都能识别**（精确名先赢，泛词不会抢）
- 新渠道在字典里加一行，识别器自动支持，**不用改代码**
- 顺手把 `price` 这个泛字段的抢列风险降到兜底层

### 4.3 自提模板生成的其它动态化

| 项 | 改成 |
|---|---|
| 单价 | `resolve_channel_price(商品, 报单对象渠道)` |
| 业务员 | 租户配置「单据默认值」（现硬编码 `"张俊峰"`） |
| 部门 / 公司名 | 同上（现硬编码 `"湖北福宝商贸有限公司"`） |
| 仓库 | 同上或指向仓库档案（现硬编码 `"总仓"`） |
| 单位 | 优先 `forecast_barcode_units` 配置，名称推断退化为最后兜底**且要回报推断行数** |
| 单据号起始 | 可配（现 `seq = 21`） |

**保留不动**：`ZT_HEADERS` / `DB_HEADERS` —— 舟谱格式契约，不是业务配置（见 §〇.4 的区分）。

---

## 五、配置面板：新增「渠道字典」页，绑定并入「报单配置」

### 5.1 回答原始问题：要不要新增「导出配置面板」

**不要新增「导出配置面板」** —— 但**要新增「渠道字典」**。两者名字像，性质完全不同：

| | 导出配置面板（❌ 不做） | 渠道字典（✅ 要做） |
|---|---|---|
| 归属 | 挂在"导出模板"这个动作下 | **产品级、跨域复用**（导入模版的动态列也读它） |
| 后果 | 同一份渠道清单出现第二处，改一处忘另一处 | 单一真相源 |
| 将来 | 访销/对账/工资要用同一条渠道，得再抄一遍 | 直接复用 |

**判断依据**：渠道是**导入与导出共用的同一份配置**。
把它挂在"导出"一侧，等于人为制造第二份拷贝 —— 正是本项目反复吃亏的模式。

### 5.2 落在哪两个地方

| 配置 | 位置 | 理由 |
|---|---|---|
| **渠道字典**（增删改 / 启停 / 排序 / 指定默认渠道） | ✅ **新建独立页**「渠道与价格」 | 产品级概念，跨域复用；不是"导出侧"的附属 |
| **商品 × 渠道 价格矩阵** | ✅ 商品档案里（可导出/导回，复用 v158 补厂价面板那套清单机制） | 粒度是"商品"，跟着商品档案走 |
| **客户 → 渠道绑定** | ✅ 并入现有「报单配置」面板（增一列下拉，默认「自动」） | 是报单对象的属性，与"这个对象是谁、归谁"同维度 |
| ❌ 单独一个「导出配置面板」 | 不做 | 见 5.1 |

⚠️ **不要让渠道清单同时出现在两处**。这是本项目最贵的一课
（`products.factory_price` 与 `purchase_price` 的第二份拷贝 → 269/269 全空）。

### 5.3 前车之鉴：不要做第 2 个死配置

`report_mapping.order_template`（"单型"）**至今是填了不生效的死配置**（§〇.5）。
因此渠道配置落地时，**必须同批打通**：

```
能配 → 能存 → 生成时真的读它 → 界面能看到"用了哪条渠道的价"
```

少任何一环都是第 2 个「单型」。**不要先上面板再补引擎。**

### 5.4 与 Hermes agent 的边界（不变）

- **主流程必须确定性**：舟谱模板是**下单凭证**，价错直接出错单。引擎设计原则明写「确定性生成，零 LLM 零 token」（`forecast.py:442`）。
- **agent 退回做例外与对账**：解释"这笔为什么取这个价"、发现异常偏离、一次性核对。
- ❌ **不要交给 agent 决定取哪个价** —— 那不是"灵活"，是把不确定引入了凭证生成。

---

## 六、必须清掉的「单一客户视角」硬编码清单

| # | 硬编码 | 位置 | 改成 | 优先级 |
|---|---|---|---|---|
| 1 | `ZT_SALESMAN = "张俊峰"` | `forecast.py:467` | 租户配置「单据默认值」 | P0 |
| 2 | `ZT_DEPT = "湖北福宝商贸有限公司"` | `forecast.py:468` | 同上 | P0 |
| 3 | `ZT_WAREHOUSE` / `DB_SRC_WAREHOUSE = "总仓"` | `forecast.py:469-470` | 同上或指向仓库档案 | P0 |
| 4 | 渠道价 = 固定列名「永辉价」等 | v1 设计 | 渠道字典动态列 | P0 |
| 5 | 渠道 = 客户名关键词猜 | （未实现，防患） | 客户档案的渠道属性 | P0 |
| 6 | `_UNIT_HINTS` 推断无回报 | `forecast.py:473` | 优先读 `forecast_barcode_units`；推断必回报行数 | P1 |
| 7 | 单据号起始 `seq = 21` | `forecast.py:617` | 可配 | P2 |
| 8 | ❌ **不要清** `ZT_HEADERS` / `DB_HEADERS` | `forecast.py:455/461` | 保留 —— 外部契约≠业务配置 | — |

---

## 七、落地顺序（依赖关系不能颠倒）

| 阶段 | 内容 | 依赖 |
|---|---|---|
| **P0-a** | **模板生成的前端入口** —— 后端引擎完备但前端 0 命中，用户够不着。**没有这一步，后面全部无意义** | 无 |
| **P0-b** | 数据层：`price_channels` + `product_channel_prices` 两表 + 迁移 + 你的租户种 5 条渠道 | 无 |
| **P0-c** | 唯一取价函数 `resolve_channel_price` / `resolve_channel_code` / `resolve_report_channel`（三份唯一实现，同一文件） | P0-b |
| **P0-d** | 导入模版**动态列** + 识别器**两阶段匹配** + 导入写入矩阵（价格覆盖/描述补空，分列回报） | P0-b/c |
| **P1-a** | 客户档案加渠道字段 + 报单配置面板加「取价渠道」下拉（默认自动） | P0-b |
| **P1-b** | 自提模板按渠道取价 + 缺价**显式回报** + 硬编码业务员/公司/仓库清掉 | P0-c |
| **P1-c** | 「渠道与价格」页（字典维护 + 矩阵导出/导回，复用 v158 面板机制） | P0-b |
| **P2** | 模版指纹 + 填写说明动态化 + 单位配置优先 + 单据号可配 | — |

---

## 八、需要你拍板的点

1. **渠道字典存哪**：独立表 `price_channels`（推荐，可增删改、字段多）还是塞进 `forecast_config` KV（省事但难做校验与关联）？
2. **厂家渠道要不要也进矩阵**：我建议**不进**，继续用 `products.factory_price` / `product_code`（防第二份拷贝），渠道记录只持"指针"。同意吗？
3. **价格字段的合并语义**：价格**非空即覆盖**（推荐，价格表每月更新）vs 与描述字段一律「只补空」（现状，会导致价永远改不掉）？
4. **渠道字典页放哪**：新建独立一级入口「渠道与价格」（推荐）vs 塞进现有「能力中心」二级？
5. **「取价渠道」下拉放客户档案还是报单配置**：我建议**两层都放**（客户档案为权威 + 报单配置为覆盖位，默认"继承"），只放一处会有例外配不了。接受吗？

---

## 九、落地状态（2026-09-14 18:55 更新）

### 你拍板的 5 条（原话：「1.独立表；2.同意；3.非空即覆盖；4.先新建独立入口我看一下效果；5.接受」）

| # | 拍板点 | 你的决定 | 落地情况 |
|---|---|---|---|
| 1 | 渠道字典存哪 | **独立表**（不用 KV） | ✅ `price_channels` 表已建 |
| 2 | 厂家渠道是否搬进矩阵 | **同意不搬**（继续用 `products.factory_price` / `product_code`，渠道只持指针） | ✅ 厂家渠道 `price_source=products_factory`，值走 `factory_price_sql()`，无第二份拷贝 |
| 3 | 价格字段合并语义 | **非空即覆盖** | ✅ 已实现并明确「空 = `""/None`，不含 `0`」 |
| 4 | 先看效果 | **先新建独立入口** | ✅ 已上线：`hergent.cn/#/price-channels` |
| 5 | 取价渠道下拉放哪 | **接受两层都放**（客户档案权威 + 报单配置覆盖位） | ✅ 报单配置已加（客户档案侧属 P1-a，未做） |

### 已上线
- **后端** commit `002e24a`（4 文件 +931/−5）：`db/queries/prices.py`（新增，取价唯一实现）、
  `routers/price_channels.py`（新增，7 条路由）、`erp_db.py`（两表 + 迁移 + `report_mapping.channel_id`）、
  `server.py`（RBAC → `data` + include_router）。
- **前端** commit `f0dec38`（5 文件 +641/−4）：`PriceChannels.vue`（新增独立入口）、路由、侧栏（桌面+移动）、
  `priceChannelApi`、报单配置「取价渠道」列 + 下拉。
- **两仓库均未推送**（延续暂缓推送）。

### 验证证据
- 生产**隔离租户**（id 9997）跑真实端点：**47/47 断言全绿**；跑完彻底销毁（用户/绑定/会话/租户/库文件全清，
  零残留）。覆盖：惰性补种 / 零 DDL 新建渠道 / 非空即覆盖 / 三层判决 / 厂价≡进价取值 / 默认渠道与删除保护 /
  越权 401 / **零渠道兜底**。
- **真机** hergent.cn：独立入口 4 张渠道卡 + 渠道价矩阵 + 试算成功（¥105.50 来自「永辉系统」、
  该渠道编码 YH-001）+ 报单配置「取价渠道」列。控制台 **0 错误**、API **0 个 4xx/5xx**。
- **真实租户回归**：`tenant_1`（真实业务）报单对象 `channel_id=0`（＝自动）、渠道按需补种 2 条；
  `tenant_10`（演示）页面正常。数据零改写。

### 生产 E2E 抓出并已修掉的 4 个缺陷（都在本轮新代码里）
1. 非法枚举值被**静默改写 / 静默丢弃**（返 200 但值没变）→ 改抛 400。
2. 渠道**同名不拒**（`code` 自动加后缀 → 能建出两个「永辉系统」）→ 按 `TRIM(name)` 唯一拒。
3. `POST /resolve` 只在**没有** `mapping_id` 时才读 `channel_id` → 两者同传时覆盖被静默丢弃。
4. **零渠道时 `resolve_report_channel` 返回 None** → 价一路传成 0/缺价（＝**静默取错价**）
   → 改为按需补种默认渠道，价源 `products_dist` 与旧口径**逐字一致**。

### 还没做（下一步，优先级不变）
- 🔴 **P0-b：把取价函数接进正式模板生成**。现在取价三件套**只有「试算」这一个调用方**
  （`grep` 全仓仅 `routers/price_channels.py` 引用）→ **真实租户行为本轮未变**。
  接线时唯一闸门就是上面第 4 条。
- **P0-c：清掉「单一客户视角」硬编码清单**（本文第六节）：`routers/forecast.py:455-470` 的
  `ZT_SALESMAN="张俊峰"` / `ZT_DEPT="湖北福宝商贸有限公司"` / `ZT_WAREHOUSE="总仓"` / `seq=21`。
  ⚠️ 必须区分：`ZT_HEADERS` / `DB_HEADERS` 是**舟谱格式契约，保留正确**；业务员/公司名/仓库是**业务配置，硬编码错误**。
- **P1-a：客户/门店档案上的渠道字段**（现在 `contacts.channel_id` 列能自动补，但界面没有入口）。
- ⚠️ 前车之鉴仍在：`report_mapping.order_template`（「单型」）至今是「填了不生效」的死配置 ——
  渠道配置必须在 P0-b 打通「能配→能存→生成时真读→界面能看到用了哪条渠道」，**否则就是第 2 个死配置**。
