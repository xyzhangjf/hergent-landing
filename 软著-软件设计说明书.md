# Hergent 软件设计说明书

## 一、引言

### 1.1 软件概述

Hergent（赫帧）是基于 Electron 框架开发的桌面端 AI 办公助手软件。软件以 Hermes Agent 开源框架为 AI 内核，通过本地 Gateway 网关提供 OpenAI 兼容 API，上层封装为双击即用的桌面应用。后端采用 FastAPI 框架，提供用户认证、积分管理和 AI 请求代理服务。

### 1.2 设计目标

- **易用性**：用户无需技术背景，通过自然语言即可驱动 AI 完成工作
- **安全性**：用户数据本地处理，敏感信息不离开设备
- **可扩展性**：支持自定义角色和技能，灵活适配不同业务场景
- **可靠性**：定时任务稳定执行，引擎故障自动恢复

### 1.3 技术指标

| 指标 | 说明 |
|------|------|
| 首次安装耗时 | < 5 分钟（含 AI 引擎自动下载） |
| 对话响应时间 | 流式输出，首字延迟 < 3 秒 |
| 最大对话轮次 | 60 轮/会话 |
| 内存限制 | 12,000 字符记忆上限 |
| 引擎端口 | 本地 18765 |
| 服务端 API | HTTPS / HTTP |

---

## 二、总体设计

### 2.1 系统架构

系统采用三层架构：

```
┌─────────────────────────────────────────────────┐
│               展示层 (渲染进程)                     │
│  index.html + js/app.js + styles.css             │
│  对话界面 / 任务管理 / 通道配置 / 设置 / 成果浏览   │
└──────────────────┬──────────────────────────────┘
                   │ IPC (contextBridge)
┌──────────────────┴──────────────────────────────┐
│              主进程层 (Electron Main)              │
│  main.js / preload.js                           │
│  IPC Handlers / Gateway 管理 / 引擎引导 / 定时任务 │
│  License 管理 / 更新管理 / 文件系统操作            │
└────────┬──────────────────────────┬──────────────┘
         │                          │
         │ HTTP (localhost:18765)    │ HTTPS
         ▼                          ▼
┌─────────────────────┐  ┌─────────────────────────┐
│  AI 引擎层           │  │   服务端                  │
│  Hermes Agent        │  │   FastAPI (server.py)    │
│  Gateway (本地)      │  │   用户认证 / 积分管理     │
│  DeepSeek API 代理   │  │   SQLite 数据库           │
│  角色工作区隔离       │  │   充值对接                │
└─────────────────────┘  └─────────────────────────┘
```

### 2.2 技术选型

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 客户端框架 | Electron | 39.8.8 | 跨平台桌面应用框架 |
| 前端技术 | HTML5 + CSS3 + ES2020+ | - | 原生 Web 技术，无框架依赖 |
| AI 引擎 | Hermes Agent | v0.14+ | 开源 AI Agent 框架 |
| 编程语言 | Python | 3.11.10 | AI 引擎运行环境 |
| 后端框架 | FastAPI | >=0.100.0 | 异步 Python Web 框架 |
| 服务器 | Uvicorn | >=0.20.0 | ASGI 服务器 |
| 数据库 | SQLite | 3 | WAL 模式，本地单文件 |
| HTTP 客户端 | httpx | >=0.25.0 | 异步 HTTP 请求 |
| 自动更新 | electron-updater | 6.8.3 | 应用更新管理 |
| 打包工具 | electron-builder | 25.1.8 | macOS DMG + Windows NSIS |

### 2.3 运行流程

```
应用启动
  ↓
解析 Hermes CLI 路径
  ↓
引擎是否已安装？
  ├── 否 → 自解压引擎 → 引导安装 Python + Hermes → 写入配置
  └── 是 → 检查引擎配置 → 更新如需要
  ↓
启动 Hermes Gateway (本地 18765 端口)
  ↓
启动 Electron 窗口 → 加载 index.html
  ↓
用户登录 / 注册
  ↓
用户发起对话 → 主进程转发至 Gateway → Gateway 调用 AI API
  ↓
流式返回结果 → 渲染进程逐字显示
```

---

## 三、模块设计

### 3.1 客户端主进程 (main.js)

**职责**：Electron 主进程，管理应用生命周期和所有后端逻辑。

**核心子模块**：

| 模块 | 函数/处理 | 说明 |
|------|-----------|------|
| 窗口管理 | BrowserWindow 创建 | 无边框透明窗口，毛玻璃效果 |
| 引擎管理 | getEngineDir, extractBundledEngine, ensureEngineConfig | AI 引擎自解压、安装、配置 |
| Gateway 管理 | startHermesGateway, restartGateway, isGatewayRunning | Gateway 启停和健康检查 |
| 认证中继 | auth:send-code, auth:verify-code, auth:wechat-url | 将前端认证请求转发到服务端 |
| 对话管理 | hermes:execute, chatViaGateway, _cancelFn | AI 对话执行（Gateway 优先，CLI 兜底） |
| 积分管理 | activation:credits, recharge:request, usage:history | 积分查询、充值、使用记录 |
| 定时任务 | cron:list/create/remove/pause/resume/run | 通过 Hermes CLI 管理 cron 任务 |
| 通道管理 | channels:save/remove/test/pairing-approve | IM 平台通道配置和测试 |
| 角色管理 | roles:list/save/add/delete/update | 角色 CRUD，持久化到 roles.json |
| 文件系统 | file:select, shell:openFolder, chat:export | 文件选择、打开文件夹、对话导出 |
| 主题管理 | theme:get/set | 深浅色主题切换 |
| 自动更新 | update:check/install/quit-and-install | 应用版本更新 |
| License | getDeviceId, loadLicense, saveLicense | 设备标识和许可证管理 |

**IPC 通信**：共 63 个 IPC 通道，其中 handle 模式 59 个（请求-响应），on 模式 4 个（事件推送）。

### 3.2 客户端预加载脚本 (preload.js)

**职责**：在渲染进程和主进程之间建立安全的通信桥梁。

- 通过 `contextBridge.exposeInMainWorld` 暴露两个全局对象
- `window.hermes`：60+ 个 API 方法，覆盖所有功能
- `window.hermes_on`：4 个事件监听器（流式输出、主题变更、Gateway 消息、更新状态）

### 3.3 客户端渲染进程

**index.html (488 行)**：
- 单页面应用 (SPA)，包含 6 个功能页面
- 对话主页、定时任务、通道连接、我的成果、技能、设置
- 模态弹窗：登录、任务创建、引导安装、通道配置、角色编辑、充值

**js/app.js (3414 行)**：
- 约 110 个前端函数
- 认证登录流程（短信验证码 / 微信扫码）
- 聊天系统（消息发送、SSE 流式展示、Markdown 渲染）
- 文件拖拽上传
- 定时任务 UI（创建、列表、暂停、恢复、删除）
- 通道管理 UI（配置保存、连接测试、Gateway 重启）
- 角色管理 UI（增删改查、头像上传裁剪 256x256）
- 主题切换（系统/浅色/深色）
- 积分充值和用量明细
- 引导安装进度展示
- 对话导出

**js/config.js (99 行)**：
- 5 个任务模板
- 9 种执行频率配置
- 4 个通道平台卡片定义
- 5 个通道配置向导
- 8 个内置角色定义（含系统提示词）

**styles.css (3234 行)**：
- CSS 自定义属性主题系统
- 毛玻璃窗口效果
- 侧边栏布局
- 聊天气泡样式
- 模态弹窗系统
- 深色/浅色主题完整适配
- 动画效果（淡入、进度条、侧边栏宽度过渡）

### 3.4 AI 引擎 (Hermes Agent)

基于开源 Hermes Agent 框架（v0.14+），作为独立进程运行在本地：

- **Gateway 模式**：以 HTTP 服务器模式运行在 `localhost:18765`，提供 OpenAI 兼容 API
- **CLI 模式**：作为兜底方案，直接通过命令行执行
- **角色隔离**：每个角色拥有独立的工作区、配置、记忆和会话目录
- **配置管理**：通过 `hermes config set` 命令行管理配置项
- **平台连接**：通过 `gateway_state.json` 管理飞书/企微/钉钉/QQ 的连接状态

### 3.5 服务端 (server.py + auth.py)

**server.py (820 行)**：FastAPI 主应用
- 积分系统：余额查询、扣费、充值、用量记录
- API 代理：将客户端请求转发至 DeepSeek API，自动计费
- 充值对接：支持线上支付回调（面包多支付）和管理员手动充值
- Token 用量追踪和积分扣减（含 CREDIT_MULTIPLIER 加价系数）

**auth.py (213 行)**：认证模块
- 短信验证码：生成、存储（内存，5 分钟 TTL）、腾讯云 SMS 发送
- JWT 管理：手动 HS256 实现，30 天有效期
- 微信 OAuth：授权 URL 生成、回调处理、用户信息获取

### 3.6 落地页 (landing-page)

**index.html (744 行)**：产品介绍页
- 动态版本号加载（从 version.json）
- 8 个 AI 角色展示
- 9 个功能特性卡片
- 7 大维度与传统 Chat AI 对比表
- 6 个实际使用场景演示
- 定价方案和下链引导
- 滚动动画、毛玻璃导航栏

---

## 四、数据库设计

### 4.1 客户端数据存储

| 存储项 | 位置 | 格式 | 说明 |
|--------|------|------|------|
| 通道配置 | channels.json | JSON | IM 平台 App ID/Secret |
| 角色配置 | roles.json | JSON | 内置 + 自定义角色定义 |
| 用户偏好 | theme.json | JSON | 主题选择 |
| 服务端地址 | server-url.json | JSON | 服务端 URL 配置 |
| 设备标识 | license.json | JSON | 设备 ID、激活状态 |
| 对话历史 | localStorage | Web Storage | 按角色分区的对话记录 |
| 积分缓存 | localStorage | Web Storage | 积分余额本地镜像 |
| AI 配置 | config.yaml | YAML | Hermes 引擎配置 |
| AI 记忆 | MEMORY.md | Markdown | 按角色隔离的长期记忆 |
| AI 会话 | sessions/ | 目录 | 按角色隔离的会话持久化 |
| 角色工作区 | agents/<role>/workspace/ | 目录 | 角色专属文件工作区 |

### 4.2 服务端数据库 (SQLite)

**数据库位置**：`~/Library/Application Support/hergent-credits/credits.db`

**数据库模式**：WAL (Write-Ahead Logging)

**表结构**：

| 表名 | 说明 | 主要字段 |
|------|------|----------|
| users | 用户信息 | id, phone, wechat_openid, nickname, avatar, credits, total_recharged, created_at |
| sessions | 登录会话 | id, user_id, token, created_at |
| recharge_log | 充值记录 | id, user_id, amount_yuan, credits, payment_ref, created_at |
| usage_log | 用量记录 | id, user_id, model, tokens_in, tokens_out, cost_fen, credits_used, created_at |

**索引**：
- usage_log(user_id, timestamp)
- recharge_log(user_id, timestamp)
- users(phone)
- users(wechat_openid)
- sessions(token)

---

## 五、接口设计

### 5.1 客户端内部通信

客户端采用 Electron 标准 IPC 机制：

- **渲染进程 → 主进程**：通过 `ipcRenderer.invoke(channel, args)` 发起请求，返回 Promise
- **主进程 → 渲染进程**：通过 `ipcMain.handle(channel, handler)` 注册处理器，返回异步结果
- **事件推送**：通过 `webContents.send(channel, data)` 主动推送事件到渲染进程

### 5.2 客户端与 AI 引擎通信

通过本地 HTTP 协议与 Hermes Gateway 通信：

- **地址**：`http://127.0.0.1:18765`
- **认证**：API Key（本地密钥）
- **接口**：OpenAI 兼容格式 `/v1/chat/completions`
- **流式**：支持 SSE (Server-Sent Events) 流式输出

### 5.3 客户端与服务端通信

通过 HTTP/HTTPS 协议：

**认证接口**：
- `POST /api/auth/send-code` — 发送短信验证码
- `POST /api/auth/verify-code` — 验证短信码，返回 JWT
- `GET /api/auth/wechat/login-url` — 获取微信登录 URL
- `GET /api/auth/wechat/callback` — 微信 OAuth 回调
- `GET /api/auth/me` — 获取当前用户信息
- `POST /api/auth/logout` — 退出登录

**积分接口**：
- `GET /api/credits` — 查询积分余额
- `POST /api/credits/deduct` — 扣除积分
- `GET /api/usage/history` — 用量记录

**充值接口**：
- `GET /api/recharge/tiers` — 获取充值档位
- `POST /api/recharge` — 管理员手动充值
- `GET /api/payment/url` — 生成支付链接
- `POST /api/payment/callback` — 支付回调

**AI 代理接口**：
- `POST /v1/chat/completions` — OpenAI 兼容 API 代理

**健康检查**：
- `GET /health` — 服务健康状态

### 5.4 外部 API 依赖

| 服务 | 用途 | 接口 |
|------|------|------|
| DeepSeek API | 大语言模型推理 | POST /v1/chat/completions |
| 腾讯云 SMS | 短信验证码发送 | SendSms API |
| 微信开放平台 | 微信扫码登录 | OAuth 2.0 授权流程 |
| 面包多支付 | 在线充值支付 | 支付链接生成 + Webhook 回调 |
| GitHub Releases | 应用版本更新 | electron-updater generic provider |

---

## 六、安全设计

### 6.1 数据安全

- **本地优先**：用户对话数据全部存储于本地设备，不上传至服务端
- **设备绑定**：基于设备指纹的 SHA-256 标识，防止身份伪造
- **API 密钥**：AI API 密钥仅存储在服务端，客户端不直接接触
- **本地 Gateway**：AI 引擎以本地 HTTP 服务运行，仅绑定 127.0.0.1

### 6.2 通信安全

- **JWT 认证**：手动实现 HS256 算法，30 天有效期，包含过期验证
- **管理员鉴权**：充值等敏感操作需要 X-Admin-Secret 请求头验证
- **支付回调验证**：支持支付平台 MD5 签名校验，防止伪造回调
- **交易去重**：基于 payment_ref 防止重复扣款

### 6.3 进程安全

- **沙箱隔离**：AI 引擎以子进程方式运行，与主进程隔离
- **异常兜底**：全局 uncaughtException 和 unhandledRejection 捕获
- **错误日志**：内存保留最近 20 条错误，Gateway 独立日志文件
- **超时保护**：CLI 命令 30 秒超时，AI 对话 300 秒超时

---

## 七、部署方案

### 7.1 客户端分发

| 平台 | 格式 | 架构 |
|------|------|------|
| macOS | DMG / ZIP | arm64 + x64 |
| Windows | NSIS 安装包 (setup.exe) | x64 |

通过 GitHub Releases 分发，支持 electron-updater 自动更新。

### 7.2 服务端部署

| 组件 | 方案 |
|------|------|
| Web 服务器 | Uvicorn (ASGI) |
| 反向代理 | Nginx（SSL 终止、WebSocket 升级、缓存关闭、600s 超时） |
| 进程管理 | systemd（自动重启、环境变量注入） |
| SSL 证书 | Let's Encrypt（certbot 自动续期） |
| 运行用户 | www-data |

### 7.3 落地页部署

通过 GitHub Pages 托管，从 main 分支自动构建部署。

---

## 八、源代码清单

详见《Hergent 源代码清单》文档。
