# 舟谱接入探针报告（NeoBrowser 实测）— 2026-09-07

## 一句话结论
**NeoBrowser 这条路在本机上跑不通。** 不是工具不行，是把它跑顺所需的"窗口 + 渲染 + 调试端口 + profile 隔离 + 滑块续期"这一长串条件，在 macOS + 你这个用法场景里叠加成本太高。

**真正能用的方案是「半自动」：你 30 秒/天手动点导出，文件落盘后 Hergent 自动摄入**。零外部依赖、零合规风险、零滑块续期、维护成本几乎为零。

## 安装与基础验证
| 项 | 结果 |
|---|---|
| 版本 | v0.1.7（GitHub 直下，`shasum -a 256` 校验 OK） |
| 安装路径 | `~/.local/bin/neobrowser` |
| `neobrowser doctor` | 全绿：Chrome 152、CDP 连通 |
| 工具数 | 43（navigate/read/screenshot/extract_table/save_cookies 等） |
| 安装方式 | 未走 `curl | sh`，手审脚本后下载 release + sha256 校验 |

## 探针四问实测结果
| # | 问题 | 结果 | 影响 |
|---|---|---|---|
| ① | 登录有无验证码 | **有滑块** | 无法全自动登录，必须人工过一次 |
| ② | 登录态保持多久 | **15 天免登录** | 过一次滑块可撑半个月 |
| ③ | 报表有无导出 | **数据报表里都有导入/导出** | 不用抄 DOM，导出按钮直达文件 |
| ④ | 每天 1 次会否触发风控 | 未实测 | 待验证，但 15 天免登录已是强信号 |

## 三条硬性踩坑（导致 NeoBrowser 方案在本机失败）
1. **默认 headless**：官方原文 *"By default NeoBrowser runs its own headless Chrome under a dedicated profile"*。无可见窗口，必须走 attach 模式。
2. **attach 模式需自起带调试端口的 Chrome**：`--remote-debugging-port=9222 --user-data-dir=独立profile`。但 macOS 下 `nohup` 启动的 Chrome **没有 GUI 上下文**，窗口不出现、CDP 截图全白。
3. **Tab 回收**：NeoBrowser 进程退出时会关闭它自己开的标签页，导致每次新会话拿到的都是空白页，需要重新 navigate。

## 数据接入三条路线再评估（更新版）
| 路线 | 方案 | 本机可行性 | 结论 |
|---|---|---|---|
| ① 官方 API | 舟谱无自助 API | ❌ | 走不通 |
| ② 服务端爬虫 | Linux 无 Chrome 无登录态 | ❌ | 架构不通 |
| ③ NeoBrowser 全自动 | attach 模式 | ⚠️→❌ | 渲染/窗口坑叠加，成本远超收益 |
| ④ **半自动（人导出+自动摄入）** | 你在自己 Chrome 里点导出，Excel 落 `~/Downloads`，Hergent 监听目录 | ✅ | **推荐** |

## 推荐下一步：「半自动 Excel 摄入」
**做法**：Hergent 加一个 `/api/import-folder` 接口 + 一个简单的 CLI/守护，前端"导入"按钮除了"选文件"再加一个"开启文件夹监听"开关——开启后监视某个目录（如 `~/Downloads/zhouspu/`），出现新的 `.xlsx/.csv` 就自动解析入相应模块（订单/库存/销售）。

**你的操作**：每天在自己常用的 Chrome 里手动打开舟谱 → 进数据报表 → 点导出 → 文件自动落到监听目录。**30 秒/天**，其余全自动化。

**收益对比全自动**：
- ✅ 省下 30 秒/天的人工 vs 全自动差的就是这 30 秒
- ✅ 零外部依赖（不装 NeoBrowser、不维护 Chrome profile）
- ✅ 零滑块续期（你登录自己的 Chrome 已习惯）
- ✅ 零 ToS 风险（人在浏览器里操作，自动化只在你授权的本地文件上）
- ✅ 零 UI 改版风险（Hergent 侧只关心 Excel 字段，不关心舟谱页面）
- ✅ 零账号安全风险（不暴露任何登录态给第三方工具）
- ⚠️ 唯一限制：出差/电脑不在身边时不能自动跑——但 15 天免登录已经把"续期频率"压到极致

## 后续动作（若你点头）
1. 停掉正在跑的 NeoBrowser Chrome（无进程退出机制，你 `Cmd+Q` 关掉那个无标题 Chrome 即可，或重启清掉）
2. 设计并实现 `~/Downloads` 目录监听 + 自动摄入（预计 200 行代码，半天工作量）
3. 给"导入"按钮加"开启文件夹监听"开关，配对清单：销售/库存/订单 三类报表对应的字段映射模板

## 文件清理建议
- `~/.local/bin/neobrowser` —— 可保留（供以后有兴趣时再折腾）
- `~/.nb-zhouspu/` —— 独立 Chrome profile，可删
- `~/.neobrowser/` —— NeoBrowser 数据目录，可删
- `/tmp/nbtool.py` —— 探针用临时脚本，可删