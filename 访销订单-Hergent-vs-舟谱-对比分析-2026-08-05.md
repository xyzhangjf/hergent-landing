# 访销订单 Hergent vs 舟谱 对比分析与优化方案

> **截图对照**：
> - 图1 (Hergent)：左侧浅蓝标签 + 绿「自营」chip + 4行5列字段区 + 16行商品明细
> - 图2 (舟谱)：白底简洁 + 4行5列字段区 + 15行商品明细 + 表头右侧复选框区

---

## 一、页面整体布局与视觉设计风格

### 差距分析

| 维度 | Hergent | 舟谱 | 问题 |
|---|---|---|---|
| **主区底色** | 字段区用浅灰容器（`--bg2`） | 纯白底，更清爽 | 浅灰容器稍显"分区切割感"过重 |
| **顶部配色** | 浅蓝标签条 + 品牌蓝按钮 | 蓝(主) + 橙(警示横幅) + 绿(创建按钮) | 创建按钮颜色不一致（Hergent 蓝 / 舟谱绿） |
| **字段框视觉** | 普通灰边输入框 | 必填字段**蓝色边框** + 🔍 放大镜 | 蓝色边框让"必填可搜索"一眼可辨 |
| **label 字号** | 11px 灰 | 略大、更醒目 | Hergent 略小，长字段名（如"销售仓库"）拥挤 |

### 优化方案（落地）
1. **创建按钮改绿色**（`var(--suc)`）：舟谱是绿色主操作按钮，Hergent 当前是品牌蓝——视觉对位差
   - 文件：`static/app.js` 第 5595 行附近 `'+icon('check',14)+' 创建'`
2. **searchable 指示器 🔍 替代 ▼**：在 `.searchable-wrap .dropdown-arrow` 处改图标
   - 文件：`static/styles.css` 第 353 行（searchable 样式）
   - 改法：用 SVG 放大镜路径替换 ▼ 字符；或者保持 ▼，但 Hergent 视觉体系已统一，强行改舟谱风意义不大，**建议保留 ▼，用蓝色边框区分必填**
3. **label 字号 11px → 12px**：更易读，且更接近舟谱

---

## 二、表单字段的设置与排列顺序

### 字段对比清单（14 字段逐项对比）

| # | Hergent | 舟谱 | 差异 |
|---|---|---|---|
| 1 | 客户*（searchable）| 客户*（searchable）| 一致 |
| 2 | 业务员*（select）| 业务员*（select）| 一致 |
| 3 | 部门*（searchable）| 部门*（searchable）| 一致 |
| 4 | 销售仓库*（searchable）| **销售仓库*** 提前到第1行 | **顺序不同** |
| 5 | 单据日期 | 单据日期 | 一致 |
| 6 | 预提货日期 | 预提货日期 | 一致 |
| 7 | **送货司机**（searchable）| ❌ 没有 | **冗余字段** |
| 8 | 送货车辆 | 送货车辆 | 一致 |
| 9 | 收货地址 | 收货地址 | 一致 |
| 10 | 付款方式 | 付款方式 | 一致 |
| 11 | 是否加急 | 是否加急 | 一致 |
| 12 | **欢数** | **欠款 ¥0.00** | **命名不准确** |
| 13 | **营销类型**（订单级）| ❌ 没有（移到商品行）| **位置应下移** |
| 14 | 备注 | 备注 | 一致 |
| — | ❌ | ✅ 自定义（齿轮图标）| **缺失** |

### 顺序对比（核心差异）

**舟谱 4 行布局**：
- 行1：**客户** / **业务员** / **部门** / **销售仓库** / 单据日期 / 预提货日期（4 个核心必填 + 2 日期）
- 行2：欠款¥0.00 / 送货车辆 / 收货地址 / 付款方式 / 是否加急 / 备注

**Hergent 4 行布局（v107.53）**：
- 行1：客户 / 业务员 / 部门 / 单据日期 / 预提货日期（销售仓库被挤到行4）
- 行2：送货司机 / 送货车辆 / 收货地址 / 付款方式 / 是否加急
- 行3：欢数 / 营销类型 / 销售仓库 / 备注

### 优化方案
1. **字段重排**（app.js:5614-5628）：把"销售仓库"提前到行1第4列（核心四必填集中），腾出"欢数/营销类型"位置
2. **字段重命名**："欢数" → **"欠款"**（更准确反映该字段含义）
3. **删除冗余字段**："送货司机"（与送货车辆信息重叠，舟谱不单列）
4. **下移营销类型**："营销类型"从订单级移除，每个商品行独立选（更灵活，符合舟谱模式）
5. **添加自定义字段**：行2 末尾加齿轮图标按钮，点击弹窗让用户增删自定义字段（与舟谱对齐）

---

## 三、操作流程的便捷性与用户交互体验

### 交互细节对比

| 维度 | Hergent | 舟谱 | 问题 |
|---|---|---|---|
| **searchable 下拉指示器** | ▼ 箭头 | 🔍 放大镜 | ▼ 不够直观 |
| **第一行商品选择** | 点击输入框展开下拉 | 商品名单元格内"…"三点按钮 | 舟谱更"表格内嵌"的视觉 |
| **"新增"按钮语义** | 末尾追加新行 | **当前行下方插入** | 舟谱更符合"加一件商品"的直觉 |
| **复选框区位置** | 字段区底部右侧 | **商品明细表上方右侧** | 舟谱位置更合理（与商品表操作相关） |
| **快捷键说明位置** | 字段区底部右侧 | 商品明细表上方右侧 | 舟谱位置更合理 |
| **行号显示** | 无明显行号 | 左侧固定 1-15 行号 | 舟谱便于核对商品行 |
| **底部栏样式** | 「数据次数: 0 项 · 合计: ¥0.00」+ 保存 + 提交 | 「数据次数」+ 保存 + 提交并审核 | 一致，Hergent 多了"项"字样可优化 |

### 优化方案
1. **复选框区 + 快捷键说明迁移**：从字段区底部（5629 行）移至商品明细表上方（与"批量操作/快捷键"对齐舟谱）
2. **"新增"操作改为下方插入**（`_vsfAddRow` 接受可选 rowIndex 参数，默认插在调用行下方）
3. **商品行加行号列**：最左侧 32px 宽度，显示当前行号（与舟谱对齐）
4. **🔍 图标可选升级**：在 `.searchable-wrap` 加可选 `data-icon="search"` 属性，不影响现有 ▼ 默认

---

## 四、功能完整性（关键差距）

### 商品明细表列对比

| 列 | Hergent（13列）| 舟谱（15列）| 差距 |
|---|---|---|---|
| ☑ 复选框 | ✅ | ✅ | 一致 |
| **业务属性** | ❌ | ✅ | **缺失** |
| *商品名称 | ✅ | ✅ | 一致 |
| 营销类型 | ✅（订单级已删后保留）| ✅（商品级）| 位置不同 |
| 条形码 | ✅ | ✅ | 一致 |
| *单位 | ✅ | ✅ | 一致 |
| *销售价（折扣价）| ✅ | ✅ | 一致 |
| 实际价 | ✅ | ✅ | 一致 |
| *订单数量 | ✅ | ✅ | 一致 |
| 订单数量（盒份）| ✅ | ✅ | 一致 |
| *订单金额 | ✅ | ✅ | 一致 |
| 销售仓库 | ✅ | ✅ | 一致 |
| **仓位** | ❌ | ✅ | **缺失** |
| **可用库存** | ❌ | ✅ | **缺失** |
| 备注 | ✅ | ✅ | 一致 |
| 操作 | ✅ 新增/删除 | ✅ 新增/删除 | 一致 |

### 功能层面差距汇总

| 功能 | Hergent | 舟谱 | 优先级 |
|---|---|---|---|
| 业务属性列（商品行）| ❌ | ✅ 必选 | **P0** |
| 仓位列（商品行）| ❌ | ✅ | **P0** |
| 可用库存列（商品行）| ❌ | ✅ 实时显示 | **P0** |
| 自定义字段（订单级，齿轮）| ❌ | ✅ | P1 |
| 上传附件 | 仅 toast | ✅ 实际 | P2（演示版允许） |
| 改订单金额时变金额联动 | 仅复选框无联动 | ✅ 实际 | P1 |
| 按小单位价格联动 | 仅复选框 | ✅ 实际 | P1 |
| 营销类型（商品级）| 订单级 | ✅ 商品级 | **P0**（改位置）|

### 优化方案
1. **商品明细表加 3 列**：业务属性、仓位、可用库存（调整列宽和表头文案）
   - 文件：`static/app.js` 5648 行 `<thead>` 表头与 `<tbody>` 渲染部分
   - **可用库存**：选商品+选仓库后调 `/api/inventory/available?product_id=&warehouse_id=` 实时显示（接口可能不存在，需新建）
   - **仓位**：searchable，调 `/api/warehouse-zones` 列表
   - **业务属性**：固定 select，选项「赠品 / 正常 / 特价 / 促销」
2. **营销类型移到商品行**：订单级"营销类型"字段移除，每个商品行独立 select
3. **自定义字段齿轮**：行2 末尾添加齿轮按钮，点击弹窗管理用户自定义字段（持久化到 localStorage 即可，不需后端）

---

## 五、冗余与缺失清单

### 🔴 冗余内容（建议删除）

| 项 | 位置 | 删除理由 |
|---|---|---|
| 送货司机 | 字段区行2 第1列 | 舟谱无此字段；与"送货车辆"信息重叠，驾驶员可从车辆字段连带 |
| 营销类型（订单级）| 字段区行3 第2列 | 舟谱在商品行更灵活；Hergent 移到商品行后即可删除订单级 |

### 🟢 缺失内容（建议新增）

| 项 | 位置 | 新增方式 |
|---|---|---|
| 业务属性列 | 商品明细表第3列 | select，4 个固定选项 |
| 仓位列 | 商品明细表新加 | searchable + 后端 `/api/warehouse-zones` |
| 可用库存列 | 商品明细表新加 | 只读数字，选仓库+选商品后调接口实时显示 |
| 自定义字段按钮 | 字段区行2 末尾 | 齿轮图标 → 弹窗管理用户自定义字段（localStorage） |
| 行号列 | 商品明细表最左侧 | 显示 1-16 序号 |
| 欠款替代欢数 | 字段区 | 字段重命名 |

---

## 六、优先级建议（落地清单）

### P0 — 必须改（核心对齐）
1. **字段重命名**："欢数" → "欠款"
2. **字段重排**：销售仓库提前到行1第4列（核心四必填集中）
3. **删除冗余字段**：送货司机、订单级营销类型
4. **商品明细表新增 3 列**：业务属性、仓位、可用库存
5. **创建按钮改绿色**：与舟谱主操作色对齐

### P1 — 应该改（提升体验）
6. 复选框区 + 快捷键说明位置迁移（→ 商品明细表上方）
7. "新增"操作改为"当前行下方插入"
8. 商品行加行号列
9. 营销类型下移到商品行（每个商品独立）
10. "改订单金额时变金额 / 按小单位价格" 复选框联动

### P2 — 锦上添花（可选）
11. 自定义字段齿轮按钮
12. 可用库存实时联动仓库+商品
13. 🔍 放大镜图标（可选升级）

---

## 七、预计工作量

| 类别 | 文件 | 改动量 |
|---|---|---|
| 字段区重排+重命名 | app.js (5614-5628) + CSS | ~30 行 |
| 商品明细表加 3 列 | app.js (5648-5690) | ~60 行 |
| 可用库存接口（新）| 后端 inventory.py + 前端调用 | ~40 行 |
| 复选框区位置迁移 | app.js 重排 | ~20 行 |
| "新增"下方插入逻辑 | app.js _vsfAddRow | ~10 行 |
| 行号列 | app.js + CSS | ~15 行 |
| 营销类型下移商品行 | app.js | ~15 行 |
| 自定义字段齿轮 | app.js + CSS | ~40 行 |
| **合计** | — | **~230 行 + 1 新接口** |

---

**说明**：本报告为对比分析，**未修改任何代码**。请确认按上述 P0/P1/P2 优先级分批实施，或调整优先级。确认后我可一次性把 P0 + P1 全部落地（v107.54）。