# v128 · 同年份已存在目标 —— 提示 + 一键编辑

> 用户拍板：**A（提示 + 编辑这条，不自动带出）+ 清（清理 `brandMode` 死代码）**
> 纯前端改动，后端零改动（`GET /api/rebate-rules` 已返回 `target_year`，父页 `rules` 即全量）。

## 一、改前行为（问题）

| 环节 | 位置 | 行为 |
|---|---|---|
| 创建入口 | `Rebate.vue openCreate()` | 只传 `presetDim`，不传年份、不传已有规则 |
| 年份控件 | `MonthlySplitBlock.vue` | 纯 `v-model.number`，无 `@change`、无查询 |
| 表单初始化 | `TargetFormModal.init()` else 分支 | `defaultForm()` + `emptyMonthlyRows()` → 空白 |
| 唯一回填 | `init()` 的 `edit`/`dup` 分支 | `{...r}` + `fillMonthlyFromRule` |
| 判重 | `runPrecheck()` | 只写 `dupWarn`，**月份级**提示，需先填作用对象 |

→ 防重复只靠后端月份锁 409 兜底，用户填完品牌才看到警告，且口径是"月份"不是"年份"。

## 二、改后行为

填完「作用对象」且年份命中已启用规则 → 蓝底提示条（与月份冲突的黄底区分）：

```
2026 年这个对象已经有目标了
「蒙牛低温2026年目标」已填 12 个月，合计 901 万元
同一年份、同一对象建议只保留一条目标，否则返利会被重复计算。要改就改原来这条。
[编辑这条]  [仍然新建]
```

- 命中 1 条 → 单个「编辑这条」；命中多条 → 逐条「编辑「规则名」」
- 点编辑 → 弹窗**不关**，就地切到编辑态（标题/按钮/月表全部回填）
- 「仍然新建」→ 关闭提示；换作用对象 / 年份 / 维度后自动重新生效
- 编辑态不提示（提示的是自己）

## 三、改动清单

| 文件 | 改动 |
|---|---|
| `useRebateTargetForm.js` | 新增 `ruleMonthlySummary(r)`：`monthly_amounts` → `{ filledMonths, months, totalWan }`，无月表回退 `target_value`；兼容 string/object |
| `TargetFormModal.vue` | ① 新 prop `existingRules`；② computed `yearHits`；③ `.dup-warn.year-hit` 提示条 + `goEditExisting` / `dismissYearHits`；④ emit `load-existing`；⑤ **新增 `watch(() => [props.mode, props.rule && props.rule.id])` → `init()`**；⑥ 清 `brandMode` 死代码；⑦ 新增 `.year-hit` / `.yh-act` 样式 |
| `Rebate.vue` | 传 `:existing-rules="rules"`；新增 `onLoadExisting(r)` 就地切编辑；接 `@load-existing` |

### 匹配口径（`yearHits`）

```js
if (editing) return []                    // 编辑/复制不提示
if (!Number(form.target_year)) return []  // 年份未填
if (scope 为空) return []                  // 没填作用对象不打扰
r.is_active === 1
&& r.dimension === form.dimension
&& Number(r.target_year) === y
&& [r.scope_key, r.scope_name] 与 [form.scope_key, form.scope_name] 有交集
```

用**集合相交**而非单向比较：商品维度下已有规则存的是商品 ID、表单里输入的是名称，单向比较会漏。

### 关键坑：弹窗内切模式不会重新初始化

`TargetFormModal` 原本只有 `watch(() => props.open, v => v && init())`。点「编辑这条」时 `open` 始终是 `true` → init 不重跑 → **点了没反应**。
必须补一个 `watch(() => [props.mode, props.rule && props.rule.id], () => props.open && init())`。

## 四、验证

| 场景 | 结果 |
|---|---|
| 创建品牌目标 · 蒙牛低温 | 提示「已填 12 个月，合计 901 万元」（#6/#8 已停用，不列） |
| 创建品牌目标 · 简爱 | 2 条各一行 + 两个编辑按钮（8 月 / 9 月，各 8 万） |
| 创建品牌目标 · 伊利 | 不提示 |
| 点「编辑这条」 | 标题→「编辑品牌目标」、按钮→「保存」、12 格回填 60/50/60/70/80/90/90/95/100/70/76/60、提示条消失 |
| 点「仍然新建」 | 提示消失；改成简爱后重新出现 |
| 创建商品目标 · 填「蒙牛低温」 | 不误报（维度过滤生效）；「目标度量」仍显示 |
| 生产数据 | 未变动：规则 5 条、月份锁 14 行 |

**commit**：前端 `1eacc6a`（hergent-landing main，已推送）。dist 已备份至 `/root/hergent-backup/hergent-cn-v2-bak-*`。
