# 预报订单微信小程序 · MVP 方案（2026-08-12）

> 定位：把"预报订单"从「企业微信智能表格 + Excel 手工搬 + 审核靠经验」升级为
> **多角色手机填报 → 系统算建议量 → 人确认 → 一键出下游模板**。
> 厂家系统 / 舟谱 因无开放 API 仍由文员人工执行，但"算"和"搬运准备"都由系统完成。

---

## 1. MVP 范围

### 做（本期）
- 多角色填报（业务员 / 促销员 / 导购员 / 分销商）
- 智能审核台（指标达成 + 库存 → 系统建议下单量）
- 一键生成「厂家下单粘贴模板」+「舟谱导入模板」
- 企微 / 微信群通知（审核完成、付款差额、到货提醒）

### 不做（本期，因对方无 API 或后续再做）
- 厂家系统自动查余额 / 确认到账 / 下单
- 舟谱自动导入（仍需文员手动导入模板）
- 付款 / 财务 / 到账确认
- 多租户、卖给他人的完整 SaaS 外壳（见第 9 节）

---

## 2. 角色与权限

| 角色 | 能做什么 | 数据范围 |
|------|----------|----------|
| 填报人（4 类） | 填报、存草稿、提交、看自己的预报状态 | 仅自己 |
| 审核人（你 / 同事） | 看全量汇总、调建议量、批准生成订单 | 全量 |
| 文员 | 下载 / 复制模板、执行下游人工 | 已批准订单 |

---

## 3. 页面清单（小程序端）

1. **登录 / 角色识别** — 微信 openid → 映射内部用户 + 角色（或企业微信身份）
2. **预报填报** — 选渠道/客户 → 选商品 → 填数量 → 自动带出专属价/金额 → 存草稿 / 提交
3. **我的预报** — 列表 + 状态（待审 / 已采纳 / 已调整 / 作废）
4. **审核台** — 当期全部预报汇总；每个商品展示「指标达成率 + 库存可销天数 + 系统建议量」；审核人逐条确认 / 微调
5. **订单确认** — 最终订单预览 → 批准 → 触发通知文员 + 生成模板
6. **模板中心**（文员）— 厂家粘贴模板 / 舟谱导入模板，一键复制 CSV 或发文件

> 通知走企微群机器人 webhook，不单独占页面。

---

## 4. 数据模型（字段）

复用现有 Hergent `tenant_1.db` 的商品 / 库存 / 客户数据，新增以下表：

**forecast_orders（预报单头）**
- id, submitter_id, role, channel_id, period(期), status, note, created_at

**forecast_items（预报单行）**
- id, order_id, product_id, qty, unit_price(专属价), amount

**review_results（审核结果）**
- id, product_id, period, suggest_qty(系统建议), final_qty(审核人定),
  target_rate(达成率快照), sellable_days(可销天数快照), rebate_hint, reviewer_id

**targets（厂家指标 / 返利，需你导入）**
- id, manufacturer, product_id(或总), period, total_target, item_target,
  rebate_tiers(JSON: 阶梯量→返利率)

**inventory_snapshot（审核时锁定的库存快照，避免抖动）**
- product_id, on_hand, in_transit, avg_daily_sales, snapshot_at

> 商品主数据、客户/渠道、库存已在库，无需重建——这是复用 Hergent 后端的最大省力点。

---

## 5. 智能审核规则（核心，系数 [待你确认]）

这是把"审核靠经验"变成"系统算"的关键，也是你的护城河。先给结构，具体数字你来定。

### 5.1 库存维度
- 日均销量 `d` = 近 N 天出货量 / N （N 建议 14~30 天，**待确认**）
- 可销天数 = `(当前库存 + 在途) / d`
- 覆盖目标天数 `T` = 提前期(4天) + 到货周期(2天) + 安全库存(建议 3~7 天，**待确认**)
- 库存建议补量 = `max(0, d × T − 当前库存 − 在途)`

### 5.2 指标 / 返利维度
- 单品达成率 `r` = 本期累计销量 / 单品指标
- 总指标达成率 `R` = 总累计 / 总指标
- 返利调节系数 `k`：
  - `r < 阈值_low`（如 0.8，**待确认**）→ 冲量 `k = 1.1~1.3`（**待确认**），提示"距返利阶梯差 X 件"
  - `r ≥ 1.0` → 控量 `k = 0.8~1.0`（**待确认**）
  - 其余 → `k = 1.0`

### 5.3 综合建议
- **建议下单量** = `round( 库存建议补量 × k )`
- 可选约束：≤ 单品指标剩余空间（避免超指标压货），**待确认是否启用**
- 审核台同时展示：达成率、可销天数、建议量、返利提示，由人最终拍板

### 5.4 返利提示（高价值）
- 计算"再订 Δ 件可达下一返利阶梯"，预计多返利 = `Δ × (阶梯返利率差) × 单价`
- 直接对应你审核时最在意的"指标 + 返利"权衡

> 规则以"可配置参数表"实现（不写死），你后续自己调阈值；Hermes 负责把结果用自然语言解释给审核人听（"为什么建议订 120 件"）。

---

## 6. 技术架构建议

```
微信小程序  ──HTTPS──>  Hergent API (FastAPI, 已有)
                              │
              tenant_1.db ◀── 商品/库存/客户/指标（复用，不重建）
                              │
              智能审核引擎（规则表 + Hermes 解释）
                              │
              模板生成（厂家 CSV / 舟谱 CSV）
                              │
              企微群机器人 webhook ──> 文员/付款群
```

- **前端**：微信小程序（原生 或 uni-app）
- **后端**：复用 Hergent FastAPI —— 已有商品/库存/客户数据，省掉数据层重建
- **审核引擎**：可配置规则表 + Hermes 兜底自然语言解释
- **通知**：企业微信群机器人 webhook（你已在用企微）
- **部署**：微信小程序**强制 HTTPS + 备案域名**；用现有服务器加 TLS，或走微信云开发
- **模板**：生成 CSV → 复制到剪贴板 / 发文件给文员

---

## 7. 里程碑与工期（粗估，需开发资源）

| 阶段 | 内容 | 工期 |
|------|------|------|
| P0 | 数据 API 暴露（商品/库存/指标/预报 CRUD） | 2~3 天 |
| P1 | 填报小程序（角色 + 表单 + 草稿/提交） | 1~2 周 |
| P2 | 审核台 + 智能建议引擎 + 微调 | 1~2 周 |
| P3 | 模板生成 + 企微通知 | 3~5 天 |

合计约 **4~6 周**。你非技术，需我辅助 coding 或外援；建议 P0+P2 先跑通"审核引擎逻辑"（价值最大、不依赖前端），再用小程序包外壳。

---

## 8. 需要你先拍板 / 提供的

1. **选型**：微信小程序 vs 企业微信小程序 —— 分销商是否在你企业微信内？（决定用哪种）
2. **资质**：微信小程序需企业主体 + 备案域名 / 或走云开发；appid 与服务器 TLS
3. **规则系数**：第 5 节所有 [待确认] 的阈值与系数（你的经验值 = 护城河）
4. **返利阶梯**：厂家给的返利政策表（总指标 / 单品指标 / 阶梯返利率）

---

## 9. 风险与提示

- 微信小程序需**企业认证 + 类目审核**（电商/订货类目要资质），个人无法发布
- 数据合规：分销商信息、交易数据上云需告知并最小必要
- 复用 Hergent 后端 = 不重建数据层，但需加**小程序鉴权**（openid → 用户映射，RBAC 沿用现有）
- 卖给他人的完整 SaaS（多租户/计费/支持）是另一阶段，本期不做；本期先"自己用起来验证模式"
