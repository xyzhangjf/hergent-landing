# Phase 1 P0 安全漏洞修复报告

**日期**: 2026-07-19  
**版本**: v89 → v90 (Phase 1)  
**执行人**: Senior Developer  

## 执行摘要

按照开发计划 Phase 1 Week 1-2 的任务，完成了全部 8 项 P0 安全漏洞修复。所有修改通过全量测试（163 passed, 0 failed），JS 语法检查全部通过。

## 修复详情

### P0-1 & P0-3: platform.py 30+ 端点补认证

**文件**: `server/routers/platform.py`（重写）  
**问题**: 30+ 个 API 端点完全没有认证检查，包括：
- `GET /api/backup/download` — 任何人可下载完整数据库
- `POST /api/tenants` — 任何人可创建租户
- `GET /api/audit-logs` — 任何人可读取审计日志
- `POST /api/webhooks` — 任何人可创建 webhook（SSRF 风险）
- `POST /api/backup/schedule` — 任何人可修改备份计划

**修复**: 所有端点添加认证：
- 管理类操作（备份、租户管理、审计日志、webhooks、active-sessions）→ `_admin(request)`
- 普通用户操作（通知、标签、自定义字段、收藏等）→ `_auth(request)`

### P0-6: 密码策略 bug 修复

**文件**: `server/core.py` line 47  
**问题**: `if not (has_digit or has_alpha)` — 使用 `or` 导致纯数字密码（"12345678"）通过验证  
**修复**: `or` → `and`，密码必须同时包含数字和字母

### P0-5: 静态文件路径遍历修复

**文件**: `server/server.py`  
**问题**: `/{path:path}` catch-all 路由直接拼接路径，`../../etc/passwd` 可读取任意文件  
**修复**: 添加 `os.path.realpath()` 检查，确保解析后的路径在 STATIC 目录内。同时修复附件下载端点的同类问题。

### P0-4: SQL 注入修复（3处）

**文件**: `server/erp_db.py`  
**问题**: 3 个函数使用 f-string 直接拼接用户参数到 SQL 语句中

| 函数 | 行号 | 注入参数 | 修复方式 |
|------|------|---------|---------|
| `report_trend()` | 530-533 | 日期表达式 `d` | 改为参数化查询 |
| `get_churn_risk()` | 882 | `cutoff` 日期字符串 | 改为 `?` 参数化 |
| `get_expiring_soon()` | 1028 | `days` 整数 | `int()` 强制转换 + 参数化 |

### P0-2: SAML 签名验证

**文件**: `server/routers/sso.py`  
**问题**: `saml_acs()` 用 regex 提取用户身份，完全不验证 SAML 响应签名。攻击者可伪造任意 SAML 响应登录任何用户。  
**修复**: 完全重写 `saml_acs()`：
1. **Fail-closed**: 未配置 IdP 证书时拒绝所有 SAML 登录
2. **签名验证**: 使用 `signxml` 库验证 XML 签名
3. **重放保护**: 追踪已使用的 Response ID，24小时内重复使用即拒绝
4. **时间窗口**: 验证 NotBefore/NotOnOrAfter（±5分钟时钟偏差容忍）
5. 新增依赖: `signxml>=3.1.0`, `lxml>=4.9.0`

### P0-7: esc() 统一安全转义函数

**文件**: `static/js/core/utils.js` + 23 个模块文件 + `static/portal.html`  
**问题**: 全局 `esc()` 不转义引号（`"` `'`），25+ 个模块各自定义不同版本的 `esc()`，大部分都不转义引号 — HTML 属性上下文中的 XSS 根因  
**修复**: 
- 全局 `esc()` 增加引号转义: `.replace(/"/g,"&quot;").replace(/'/g,"&#39;")`
- 删除 23 个模块文件中的重复 `esc()` 定义
- 修复 `portal.html` 中的 `esc()`

### P0-10: Dockerfile 默认密钥移除

**文件**: `Dockerfile`  
**问题**: `ENV ERP_SECRET=change-me-in-production` 提供公开已知的默认密钥  
**修复**: 移除默认值。`core.py` 已有检查 — 未设置 `ERP_SECRET` 时容器启动即失败。

## 测试验证

```
ERP_SECRET=test python3 -m pytest tests/ -v
======================= 163 passed, 28 skipped in 14.90s =======================
```

- 163 个测试全部通过
- 0 个失败
- 28 个跳过（可选依赖未安装）
- 所有 JS 文件语法检查通过

## 修改文件清单

| 文件 | 修改类型 |
|------|---------|
| server/routers/platform.py | 重写（30+ 端点加认证） |
| server/core.py | 密码策略 bug 修复 |
| server/server.py | 路径遍历修复 ×2 |
| server/erp_db.py | SQL 注入修复 ×3 |
| server/routers/sso.py | SAML 签名验证重写 |
| static/js/core/utils.js | esc() 加引号转义 |
| static/portal.html | esc() 加引号转义 |
| static/js/modules/*.js (23个) | 删除重复 esc() 定义 |
| Dockerfile | 移除默认密钥 |
| server/requirements.txt | 新增 signxml, lxml, pyyaml |
| tests/test_auth.py | 适配新密码策略 |
| tests/test_api_auth.py | 适配新密码策略 |

## Phase 1 剩余任务

- [ ] Git 历史清理（移除 .db 文件和 .bak 备份文件）
- [ ] 密钥轮换（SECRET_KEY, SAML 证书等）
- [ ] P1 高风险消除（CSP、token 统一、SSRF 防护、120+ 裸 except:pass、依赖锁定）
- [ ] 工程纪律建立（pre-commit 强化、CI 门槛提高、分支策略）
