# 员工档案 / 账号与组织 / 权限 三模块整合分析

**日期**：2026-08-30
**方法**：先查代码与生产真实数据，再下结论（非凭经验推断）

---

## 〇、先摆事实：现在的数据到底长什么样

### 1. 三张表的物理位置（这是所有决策的前提）

| 实体 | 库 | 关键字段 |
|---|---|---|
| `hr_employees` 员工 | **租户库** `tenant_N.db` | id, employee_no, name, department_id, position, hire_date, id_card, bank_*, social_insurance_*, salary_structure, warehouse_id, is_active |
| `users` 账号 | **主库** `erp.db`（跨租户共享） | id, username, password_hash, display_name, role, is_active, phone, **employee_code**, pinyin, **department_id**, mfa_*, password_changed |
| `departments` 部门 | **租户库** | id, name, parent_id, is_active, manager, cost_center |
| `role_permissions` 角色权限 | **主库和租户库各一份** | role_name, permissions |

> ⚠️ **账号在主库、员工在租户库** —— 物理隔离，数据库层面**根本不可能建外键**，一致性只能靠应用层保证。这是整个问题的根源。

### 2. 生产真实数据（tenant_1）

```
hr_employees（7 人）          users（5 账号）
id=1 EMP001 管理员 dept=0     id=1 管理员      admin      emp_code=''
id=2 EMP002 王老板 dept=0     id=2 张俊峰      boss       emp_code=''
id=3 EMP003 王会计 dept=0     id=4 张记乳品演示 boss       emp_code=''
id=4 EMP004 刘小顶 dept=0     id=5 微信审核-销售 sales     emp_code='9001'
id=5 EMP008 赵仓管 dept=0     id=6 微信审核-主管 supervisor emp_code='9002'
id=6 EMP009 刘善涛 dept=0
id=7 EMP010 张俊峰 dept=0     departments（4 个：总部/销售部/财务部/仓储部，无人使用）
```

**关联字段**：`users.employee_code` ↔ `hr_employees.id`（代码：`staff_account_create` 写 `employee_code=str(employee_id)`）

### 3. 四个必须知道的真相

**① 实际有效关联 = 0 条**
仅有的两个 `employee_code` 是 `9001`/`9002`——那是为小程序提审建的**测试账号**编码，不对应任何员工 id（1–7）。业务上**没有任何一个员工真正关联到账号**。

**② 字段语义错位（命名陷阱）**
`users.employee_code` 存的其实是 **员工 id**（数字字符串），而 `hr_employees.employee_no`（EMP001）才是真正的工号。名字叫 code、存的是 id —— 后来者极易写错，已是事实上的坑。

**③ 「张俊峰」两边都有，却没关联**
员工表 id=7「张俊峰」和账号表 id=2「张俊峰（boss）」指向同一人，但 `employee_code` 为空 → 系统认为是两个人。**这正是你要的「数据重叠」的实证。**

**④ departments 表建了但完全没用**
4 个部门，7 个员工全部 `department_id=0`；而 `users` 侧也有 `department_id` 列（迁移 v4 加的），**从未被任何查询使用**——是个死列。

---

## 一、是否应该整合？——逐项判断依据

### 结论先行：**不做物理合并，但必须做逻辑整合 + 入口收敛**

| 维度 | 现状 | 判断 |
|---|---|---|
| **数据冗余** | 姓名双份（`hr_employees.name` / `users.display_name`）、状态双份（两个 `is_active`）、部门双份（两边都有 `department_id`，一侧还是死列） | **中高**。冗余集中在 3 个字段，但核心 HR 字段（薪资/社保/银行）与认证字段（密码/MFA）**并不重叠**——不是"两份同样的档案" |
| **一致性风险** | ①离职只改员工表，账号照常能登录 ②姓名两处独立可改 ③`role_permissions` 主库/租户库分裂 | **高（这是真问题）**。风险不在"字段重复"，而在"**没有联动**" |
| **维护成本** | 员工页已能开账号、设角色、分门店；设置→权限 又列了一遍账号能改角色 | **中**。两处都能动"人的权限"，改完不知道以谁为准，且行为不一致（员工页停用 ≠ 账号停用） |
| **使用频率差异** | 员工档案：**高频**（新员工入职、每月算薪、调岗、离职）；权限矩阵：**低频**（系统初始化配一次，之后偶尔调）；账号：中频 | **大**。这决定了**不能合成一个页面**——把低频的权限矩阵塞进高频的员工日常页，会互相干扰 |

### 为什么**不能**物理合并成一张表

1. **跨库硬约束**：账号在主库（跨租户），员工在租户库。合并意味着要么把所有账号下放到租户库（**破坏多租户登录体系**，登录时要先知道租户才能查账号，但知道租户又需要先登录——死循环），要么把员工上提到主库（**破坏数据隔离**，薪资/身份证/银行卡属最敏感数据，绝不应跨租户混存）。
2. **生命周期不同**：一个人可以先有账号再补档案，也可以先建档案（算薪用）暂不开账号。强行一对一会卡住业务流程。
3. **你的约束「不得破坏现有功能」**：合并表 = 改登录、改小程序、改薪资、改权限，全链路回归，风险不可控。

### 为什么**必须**逻辑整合

三个模块现在**语义上管的是同一个"人"**，却各自为政：
- 员工页停用 → 人还在，账号还能登（**安全漏洞**）
- 员工页能开账号设角色，设置页也能改角色（**两个入口**）
- 两处显示同一个人的状态可能相反（**显示矛盾**）

---

## 二、数据模型关系（目标态）

```
                    ┌─────────────────┐
                    │  departments    │  租户库（组织架构唯一源）
                    │  id/name/parent │  ← 目前建了没用，需启用
                    └────────┬────────┘
                             │ department_id
                             ▼
   ┌──────────────────────────────────────────┐
   │  hr_employees（租户库）                    │
   │  ★ 人员主数据唯一源（SSOT for 人）          │
   │  id / employee_no(工号) / name / dept /    │
   │  position / hire_date / id_card / bank /   │
   │  社保 / salary_structure / warehouse /     │
   │  is_active(在职状态) / leave_date(新增)     │
   └───────────────┬──────────────────────────┘
                   │ ① 强关联（新增 employee_id 整型列）
                   │    employee_code 保留但只存工号(语义归位)
                   ▼
   ┌──────────────────────────────────────────┐
   │  users（主库 erp.db）                      │
   │  ★ 认证唯一源（SSOT for 账号）              │
   │  id / username / password_hash /           │
   │  display_name / role / is_active /         │
   │  employee_id(新增) / mfa / ...             │
   └───────────────┬──────────────────────────┘
                   │ ② role
                   ▼
   ┌──────────────────────────────────────────┐
   │  role_permissions                         │
   │  ★ 角色权限唯一源（当前主/租户分裂，须统一） │
   │  role_name → permissions[module...]       │
   └──────────────────────────────────────────┘
```

### 唯一数据源（SSOT）划分

| 数据 | 唯一源 | 另一侧的处置 |
|---|---|---|
| 姓名、工号、岗位、部门、入职/离职日期 | **`hr_employees`** | `users.display_name` 改为**只读派生**，创建账号时快照，之后由员工档案单向同步 |
| 薪资、社保、银行账号、薪资结构 | **`hr_employees`** | 账号侧不存 |
| 用户名、密码、MFA、登录态 | **`users`** | 员工侧不存 |
| **在职状态** | **`hr_employees.is_active`**（主） | `users.is_active` 变为**从属**，由联动逻辑自动改写，禁止人工单独改 |
| 角色 | **`users.role`** | 员工档案页可调（写入口），但读以 users 为准 |
| 角色能访问哪些模块 | **`role_permissions`** | 统一到租户库（见下） |
| 门店/报单范围 | **`employee_stores`（租户库）** | 保持 |

### 职责边界（划清楚谁管什么）

| 模块 | 管什么 | **不**管什么 |
|---|---|---|
| **员工档案**（高频主入口） | 人员增删改、薪资社保、在职状态、**开/停账号**、设角色、分门店 | 角色"能访问哪些模块"的定义 |
| **设置 → 权限** | 角色 × 模块权限矩阵 | 具体某个人的账号、具体某个人的角色 |
| **设置 → 账号与组织** | 组织架构（部门树）、当前账号、主题 | 人员档案、成员授权 |

> 一句话：**员工档案管"人"，权限页管"角色能干什么"，组织架构管"部门树"**。三者不再有交集。

---

## 三、🔥 顺带查出的 P0 缺陷（必须一并处理）

### `role_permissions` 主库 / 租户库分裂

生产实测：

| 库 | role_permissions 内容 |
|---|---|
| `erp.db`（主库） | `supervisor → ["data","dashboard"]` |
| `tenant_1.db`（租户库） | `库管 → ["dashboard","stock","data"]` |
| `tenant_tenant_1.db` | 空 |

**问题**：建表走 `_safe_migrate`（主库），但读写走 `get_db()`（**租户上下文**）。导致：
- 权限页保存 → 写入**租户库**
- `reload_perms()` 在请求上下文执行 → 读**租户库** → 主库那份 `supervisor` **丢失**
- 结果：**小程序主管审批所依赖的 supervisor 权限，在老板保存过一次权限后就会失效**（直到重启才可能恢复）

这与之前提到的「supervisor 是我手工插库补的，`_DEFAULT_PERMS` 里没有」是同一个雷，现在有了实锤。

> **建议**：把 `role_permissions` **统一收敛到租户库**（加租户补丁确保每库建表），并把 `supervisor` 写进 `core.py` 的 `_DEFAULT_PERMS`，不再依赖手工插库。

---

## 四、菜单与信息架构调整方案

### 目标结构

```
档案管理
  └─ 员工档案          ← 唯一人员入口（含开账号/停用/设角色/分门店）

设置
  ├─ 账号与组织         ← 组织架构（部门树）+ 当前账号 + 主题
  ├─ 权限              ← 仅「角色 × 模块」矩阵（移除成员授权）
  ├─ AI 配置
  └─ 数据与系统
```

### 变更点

| # | 变更 | 理由 |
|---|---|---|
| M1 | **设置→权限 移除「成员授权」区块** | 与我刚加的入口重复；成员授权归位到员工档案（那里已有开账号/设角色/门店，是最自然的落点） |
| M2 | 员工档案增加**「账号状态」列与「同步」提示** | 一眼看出谁有账号、账号是否异常（有账号但员工已停用） |
| M3 | 启用 **部门树**（目前 4 个部门闲置） | 让组织架构真正可用，否则"账号与组织"形同虚设 |
| M4 | 员工档案「停用」弹窗增加**联动选项**（默认勾选：同时停用账号并回收权限） | 满足你的约束① |

---

## 五、实施路径（分期）

### 复杂度标注
🟢 低（<0.5 天，单文件）｜🟡 中（1–2 天，前后端联动）｜🔴 高（3 天+，跨库/数据结构变更）

---

### **P0 — 先止血（建议立即做）**

| 项 | 内容 | 复杂度 |
|---|---|---|
| **P0-1** | ~~离职联动：员工停用 → 自动停用账号~~ **✅ 已完成** | 🟡 |
| **P0-2** | ~~权限表收敛到租户库 + `supervisor` 写入 `_DEFAULT_PERMS`~~ **✅ 已完成** | 🟡 |
| **P0-3** | ~~补强关联列 `users.employee_id`（整型）~~ **✅ 已完成** | 🟡 |
| **P0-4** | ~~报单配置走"转交"而非停用~~ **✅ 已完成** | 🟡 |

> **✅ P0 四项已全部实现并上线（2026-08-30 晚）**，详见第九节「实施记录」。

> P0-1 实现方式（已落地）：在 `server.py` 的员工停用端点后，跨库调用 `_master_db()` 把关联账号置为 `is_active=0/1`。**修正一处早前判断**：联动代码其实一直存在（原 `UPDATE users ... WHERE employee_code=?`），但它被 `except: pass` 静默吞掉，且只按 `employee_code` 匹配——而该列对真实员工为空（只有测试账号 9001/9002 有值），因此**联动从未真正生效**。现改为优先匹配新的整型 `employee_id`、失败回退 `employee_code`，并返回 `accounts_affected` 供前端反馈。反向恢复用「启用员工」触发。

---

### **P1 — 入口收敛与一致性（1～2 周内）**

| 项 | 内容 | 复杂度 |
|---|---|---|
| **P1-1** | 设置→权限 **移除成员授权**，员工档案承接 | 🟢 |
| **P1-2** | 员工列表补 `account_status` 字段（无账号/正常/**异常：员工停用但账号在用**） | 🟡 |
| **P1-3** | 一次性数据清洗：按姓名+工号把现有 7 员工与 5 账号配对，回填 `employee_id` | 🟡 |
| **P1-4** | **收敛 `employee_stores` 与 `report_mapping` 的"员工→门店"重叠**（见第八节） | 🟡 |

---

### **P2 — 组织与体验（按需）**

| 项 | 内容 | 复杂度 |
|---|---|---|
| **P2-1** | 启用部门树 CRUD，员工可指派部门（现 4 个部门闲置） | 🟡 |
| **P2-2** | 姓名单向同步：`hr_employees.name` 改 → 同步 `users.display_name` | 🟢 |
| **P2-3** | 离职时同时回收门店授权（`employee_stores`）与报单映射 | 🟡 |

---

### **P3 — 远期（暂不建议）**

| 项 | 内容 | 复杂度 |
|---|---|---|
| P3-1 | 账号下放到租户库（真正的租户内闭环） | 🔴 需重做登录体系，**风险高** |
| P3-2 | 引入独立 RBAC 服务 / ABAC | 🔴 当前规模不需要 |

---

## 六、如果选择「不整合」——如何保证一致性

若你决定维持三模块现状，则必须补齐以下机制（否则一致性无保障）：

1. **单向数据流**：`hr_employees` → `users` 单向同步姓名与状态；禁止在账号侧独立修改这两项（UI 上置灰）
2. **双向校验任务**：定时任务扫描 `员工停用但账号活跃` 的异常对，告警并列出（可先只告警不自动处理）
3. **统一操作入口**：任何改角色/开账号/停用的操作，全部走员工档案一个入口，权限页只保留矩阵（即 M1）
4. **关联完整性**：写账号时必须校验 `employee_id` 存在且未被他人占用（唯一约束）
5. **审计日志**：跨库联动操作（离职→停用账号）必须留痕，可追溯谁在何时做了什么

> 坦率讲：**这套"不整合但要一致"的机制，工作量与 P0+P1 相当，且长期维护成本更高**。所以我的推荐仍是走 P0→P1 的轻整合路线。

---

## 七、推荐结论

**推荐方案：逻辑整合 + 入口收敛（P0 → P1），不做物理合并。**

- 保持两张表（跨库约束决定）
- **`hr_employees` 是"人"的唯一源，`users` 是"认证"的唯一源，`role_permissions` 是"权限"的唯一源**
- 员工档案是操作人员的唯一入口；设置→权限只定义角色能力
- 优先做 P0-1（离职联动）和 P0-2（权限表分裂），这两项是**当前实际存在的缺陷**，不是优化

**最小可交付（如只想先解决最痛的）**：P0-1 + P0-2，约 1～2 天，即可满足「离职自动停用账号并回收权限」且修掉权限表分裂。

---

## 八、补充：「预报订单管理 → 报单配置」是否也要整合？

用户追问后补查（2026-08-30）。**结论：不并入权限/账号/员工，但需处理 2 个真问题。**

### 8.1 报单配置是什么

`report_mapping`（租户库）：`employee_id` + `counterparty_type(门店/客户/自有仓)` + `counterparty_id` + `system_name` + `report_alias(列头简称)` + `order_template(单型)` + `src_wh/dst_wh` + `is_active`

即「**哪个员工负责哪个门店/客户，单怎么报（别名/单型/仓流向）**」。

后端 `report_mapping_health()` 会拿 `hr_employees WHERE is_active=1` 比对，输出 `unassigned_employees`（在职但没配报单的员工）→ **设计上它就是"员工"的业务延伸属性**。

### 8.2 为什么不并入权限页（维度正交）

| 维度 | 报单配置 | 角色权限 |
|---|---|---|
| 回答的问题 | 这个人**对哪些门店**、单**怎么报** | 这个角色**能进哪些功能菜单** |
| 性质 | **数据范围 + 业务规则** | **功能权限**（RBAC 另一轴） |
| 离职时 | 要**转交**（门店还得有人报） | 要**回收** |
| 使用者/频次 | 懂业务的人，中频（新开门店/换单型） | 老板/管理员，低频（配一次） |

这是经典 RBAC 的两个正交轴：**功能权限**（能不能做）vs **数据范围**（对哪些数据做）。合并会让"权限"语义膨胀，也会让低频的权限页被高频的报单配置淹没。

### 8.3 它其实已经整合过一次

`Forecast.vue:1421` 注释：「报单配置（**原档案管理独立页，整合为标签页**）」—— 当年已从档案管理搬进预报，方向正确：**配置跟着使用场景走**（配报单时人就在预报页）。

### 8.4 查出的两个真问题

**① 孤儿路由 `/archive/mappings`（✅ 已修复）**
搬走时路由没删。实测：侧边栏无入口、`Archive.vue` 无入口、Forecast 直接 `<ReportMapping />` 嵌组件不走路由 → 全库 `grep archive/mappings` 除路由定义本身零引用，只能手输 URL 打开，且会以"被嵌入"的样子显示（重复页面头）。
**已处理**：删除路由 + 清理随之失效的 `const ReportMapping = ...` 路由 import。生产核对 `archive/mappings`=0、`报单配置`=3、组件仍随 Forecast 打包（`报单简称`=2）——功能完好，仅死链消除。复杂度 🟢。

**② `employee_stores` 与 `report_mapping` 概念重叠（待处理）**
两者都在表达"员工 → 门店"：

| 表 | 结构 | 生产实测 |
|---|---|---|
| `employee_stores` | 仅 2 列（`employee_id, store_id`），纯授权 | **1 行，且是测试数据**（`employee_id=9001`，即小程序提审测试账号编码，非真实员工 id 1–7） |
| `report_mapping` | 11 列，含别名/单型/仓流向 | 2 行，其中 1 行 `employee_id=NULL`（`self_warehouse` 仓到仓调拨，本就不绑人） |

风险：同一个"谁负责哪家店"要维护两处，且可能不一致。**这才是真正该收敛的重叠**，优先级高于"并进权限页"。

### 8.5 ⚠️ 离职语义差异：权限要回收，报单要交接

「离职自动停用账号 + 回收权限」**不能直接套用到报单配置**——若把离职员工的报单配置一并停用，其负责的门店就**没人报单了，业务中断**。

正确做法：停用员工时提示「该员工还有 N 个门店/客户的报单配置，请转交给：[下拉选人]」，批量把 `report_mapping.employee_id` 改指到接任者。

> 已作为 **P0-4** 加入分期表。

### 8.6 处置建议

| 优先级 | 动作 | 复杂度 | 状态 |
|---|---|---|---|
| 立即 | 删除孤儿路由 `/archive/mappings` | 🟢 | ✅ 已完成 |
| **P0-4** | 离职联动时，报单配置走**转交**而非停用 | 🟡 | ✅ 已完成 |
| **P1-4** | 收敛 `employee_stores` 与 `report_mapping` 的"员工→门店"重叠 | 🟡 | 待做 |
| 不做 | 报单配置并入权限页 | — | 维度正交，不推荐 |

---

## 九、实施记录（P0 全套 + M1，2026-08-30 已上线）

### 9.1 后端（`server.py` / `erp_db.py` / `core.py`）

| 项 | 实现 | 验证 |
|---|---|---|
| **P0-3** | `erp_db.init_db()` 加 `_safe_migrate('v110_users_employee_id', 'ALTER TABLE users ADD COLUMN employee_id INTEGER DEFAULT 0')`；并加一次性回填（用 `_migrations` 标记防重跑），把历史 `employee_code` 数字串 `CAST` 进 `employee_id` | 生产：`employee_id` 列已建；9001/9002 回填成功，真实员工（空 code）保持 0 |
| **P0-2** | ①租户补丁循环补 `role_permissions` 建表（原先只在主库建）②`core._DEFAULT_PERMS` 新增 `"supervisor": ["dashboard","data"]` ③加主库→各租户库的一次性配置迁移（`INSERT OR IGNORE`，不覆盖租户自有配置） | 生产：全部 11 个库均有该表；supervisor 已迁入；`tenant_1` 原有的「库管」未被覆盖 |
| **P0-1** | 重写员工停用端点：优先 `WHERE IFNULL(employee_id,0)=? OR employee_code=?` 匹配（无 `employee_id` 列时回退纯 `employee_code`），异常改为打印告警而非静默 pass，返回 `accounts_affected` | 生产实测：停用影响 1 个账号、启用影响 1 个账号，数据已还原 |
| **P0-4** | 新增 `POST /api/employees/{eid}/transfer-mappings`（校验接任者存在且在职、不能是本人，返回 `moved`）与 `GET /api/employees/active`（在职员工简表供下拉）；停用响应回带 `report_mapping_count` | 端点 401（鉴权正常）；RBAC 已确认映射到 `hr`，不会 403 |

### 9.2 前端（`Settings.vue` / `EmployeeArchive.vue`）

| 项 | 实现 |
|---|---|
| **M1** | 设置→权限 **移除成员授权**（工具栏+表格+分页及相关状态 `users/userLoading/roleSavingId/memberQuery/memberPage/filteredMembers/pagedMembers/loadUsers/changeRole/isMe/roleOptions/MEMBER_PAGE_SIZE`），只保留角色×模块矩阵。生产核对 `成员授权`=0、`搜索姓名`=0 |
| **P0-1 UI** | `employeeToggle` 改为返回响应，toast 明确提示「已停用员工，并禁用 N 个登录账号」/「该员工无关联账号」，让承诺与真实结果对得上 |
| **P0-4 UI** | 停用后若 `report_mapping_count>0`，自动弹出「交接报单配置」模态：说明不能作废的原因 + 接任者下拉 + 「确认转交」/「稍后处理」 |

### 9.3 部署与验证

- 后端：外科手术式只 rsync `server.py`/`erp_db.py`/`core.py`（**先 diff 确认仅含本次改动，未夹带用户暂缓推送的内容**），`chown` + 清 `__pycache__` + restart，health 200
- 前端：`npm run build` + rsync + chown
- 生产核对：迁移全生效、新旧端点鉴权正常、产物文案全命中

### 9.4 遗留（P1，未做）

- **P1-2** 员工列表补 `account_status`（暴露"员工停用但账号在用"的异常）—— 目前靠停用时的 toast 提示，无持续可见的体检
- **P1-3** 数据清洗：把 7 名员工与 5 个账号按姓名/工号配对回填 `employee_id`（**目前仍是 0 条有效业务关联**，只有测试账号的 9001/9002）。这一步做完，离职联动才对真实员工生效
- **P1-4** `employee_stores` 与 `report_mapping` 的"员工→门店"重叠收敛

> ⚠️ **重要提示**：P0-1 的联动能力已就绪，但**真实员工目前尚未与账号建立关联**（`employee_id` 全为 0）。要让"离职自动停用账号"对真实员工生效，需先做 P1-3 的数据配对，或在员工档案页为每位员工「开账号」（新开的账号会自动写入 `employee_id`）。
