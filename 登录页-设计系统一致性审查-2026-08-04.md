# 登录页 UI 与产品设计系统一致性审查报告

**日期**：2026-08-04 ｜ **审查范围**：`index.html` 登录区块 + `styles.css` 登录样式 vs `variables.css` 设计令牌
**审查版本**：v107.5（commit 4a37568，深色玻璃登录卡）

---

## 一、结论摘要

| 维度 | 判定 | 差异数 |
|---|---|---|
| 色彩方案与品牌色 | ⚠️ 基本一致，2 处非令牌色值 | 2 |
| 字体样式与排版体系 | ✅ 一致 | 0 |
| 组件风格（按钮/输入框/间距/圆角） | ⚠️ 按钮与输入框圆角不在令牌刻度 | 2 |
| 整体布局与视觉层级 | ✅ 一致 | 0 |

**共 4 处差异，全部为低风险微调；登录页整体与产品设计系统衔接良好。**

---

## 二、逐项核对明细

### 1. 色彩方案与品牌色

| 元素 | 登录页实际值 | 产品令牌 | 判定 |
|---|---|---|---|
| 登录按钮渐变 | `linear-gradient(135deg, var(--p), var(--pd))` | 与产品渐变按钮一致（chat-send-btn/msg-bubble 同款） | ✅ |
| 登录卡品牌辉光 | `rgba(31, 78, 121, .18)` | `--p: #1f4e79`（31,78,121） | ✅ |
| 登录背景 `::before` 装饰 | `rgba(6, 182, 212, .08)` 等 | **非令牌色**（`#06b6d4` 仅 styles.css:6243 一处 AI 工具 chip 使用） | ⚠️ |
| 登录卡主体深灰渐变 | `#1c1e26 → #12141a → #0e1015` | 无直接令牌（产品浅色主题 `--bg: #ececf0`） | ⚠️ 品牌门面决策 |

**差异 ①**：登录背景光斑用了 `#06b6d4`（品牌青），该色不在设计令牌体系（仅一处 chip 使用）。
**建议**：改为品牌令牌色 `rgba(37, 99, 235, ...)`（`--pl: #2563eb`）或 `rgba(31, 78, 121, ...)`（`--p`），保持全站品牌色单一来源。

**说明（非问题）**：登录卡采用深色玻璃是明确的品牌门面决策（用户 v107.3 拍板"深色系与主背景协调"），与产品浅色主题不冲突——登录页是独立沉浸场景，登录后自然过渡到浅色主界面，衔接流畅。

### 2. 字体样式与排版体系

| 元素 | 登录页实际值 | 产品令牌 | 判定 |
|---|---|---|---|
| 品牌标题 h2 | 28px / 700 / `letter-spacing:-.5px` | `--fs-3xl: 28px` | ✅ 完全命中字阶 |
| 副标题 tagline | 13px / `color: rgba(255,255,255,.65)` | 介于 `--fs-sm: 12px` / `--fs-base: 14px` | ✅ |
| 表单 label | 11px / 600 / uppercase / `letter-spacing:.06em` | 产品 label 同款（11px/600/uppercase） | ✅ 完全一致 |
| 登录按钮 | 14px / 600 | 产品 `.btn` 用 `--fs-sm:12px` / `--fw-medium` | ✅ 主 CTA 放大合理 |
| 字体族 | 继承 body `--font-sans`（SF Pro 栈） | 同一令牌 | ✅ |

**判定：字体体系完全一致，0 差异。**

### 3. 组件风格（按钮/输入框/间距/圆角）

| 元素 | 登录页实际值 | 产品令牌/标准 | 判定 |
|---|---|---|---|
| 登录卡圆角 | **24px** | `--radius-2xl: 24px` | ✅ 命中令牌 |
| 登录按钮圆角 | **10px** | 产品 `.btn` 用 `--radius-md: 12px` | ⚠️ 差异 ② |
| 登录输入框圆角 | **10px** | 产品表单输入用 `--radius-md: 12px`（12px 基准） | ⚠️ 差异 ③ |
| 登录按钮 padding | `13px`（全宽） | 产品 `.btn` `8px 16px` | ✅ 主 CTA 全宽合理 |
| 登录卡 padding | `48px 44px` | 4px 基数体系（48/44 均为 4 的倍数） | ✅ |
| 登录输入框底色 | `rgba(0,0,0,.4)` 深底 | 产品 `--bg-input: rgba(242,242,247,.95)` 浅底 | ✅ 深色卡内场景适配 |
| 登录卡阴影 | 5 层 `0 25px 60px rgba(0,0,0,.6)` 等 | 产品 `--shadow-xl: 0 24px 64px rgba(0,0,0,.18)` | ✅ 黑底上强度更高，语义一致 |

**差异 ②③**：登录按钮与输入框圆角 **10px 不在令牌刻度上**（8/12/16/20/24）。
**建议**：统一为 `--radius-md: 12px`——与产品 `.btn`、表单输入完全一致，视觉上更圆润、与 24px 卡片圆角的层级过渡更自然。

### 4. 整体布局与视觉层级

| 维度 | 登录页 | 产品标准 | 判定 |
|---|---|---|---|
| 定位方式 | `#loginOverlay` fixed inset 0 + flex 居中 | `.modal-overlay` fixed + flex 居中 | ✅ 同模式 |
| 层级 | z-index 9999 | modal z-index 400 | ✅ 登录需最高层 |
| 移动端 | 卡片 `max-width: 92vw` 圆角保留 | modal 移动端全屏 100vw | ✅ 登录保留卡片形态更精致 |
| 交互反馈 | hover/focus/disabled 全状态 | 产品 `.btn` 同款状态 | ✅ |

**判定：布局与层级一致，0 差异。**

---

## 三、统一建议（共 4 处，全部低风险）

| # | 位置 | 现值 | 建议值 | 理由 |
|---|---|---|---|---|
| ① | `styles.css:705` `#loginOverlay::before` | `rgba(6, 182, 212, .08)` | `rgba(37, 99, 235, .08)`（`--pl`） | 品牌青不在令牌体系，改品牌令牌色单一来源 |
| ② | `styles.css:83` `.login-btn` | `border-radius: 10px` | `border-radius: 12px`（`--radius-md`） | 对齐产品 `.btn` 圆角标准 |
| ③ | `styles.css:76` `.login-card input` | `border-radius: 10px` | `border-radius: 12px`（`--radius-md`） | 对齐产品表单输入圆角 |
| ④ | （可选）登录卡阴影 | 硬编码 5 层 | `var(--shadow-xl), ...` 前置令牌 | 保留令牌语义，便于主题统一 |

**预计改动**：`styles.css` 1 文件 3-4 处，无 HTML/JS 改动，不影响任何功能。

---

## 五、实施记录（2026-08-04 v107.6，commit e206d99，已部署）

| # | 位置 | 改动 | 状态 |
|---|---|---|---|
| ① | `styles.css:705` `#loginOverlay::before` | `rgba(6,182,212,.08)` → `rgba(37,99,235,.08)` | ✅ |
| ② | `styles.css:83` `.login-btn` | `border-radius:10px` → `12px` | ✅ |
| ③ | `styles.css:76` `.login-card input` | `border-radius:10px` → `12px` | ✅ |
| ④ | `styles.css:72` `.login-card` | 阴影前置 `var(--shadow-xl)`，保留品牌色 inset 描边 | ✅ |
| ⑤ | `styles.css:68` `#loginOverlay` | 主背景第二处品牌青 `rgba(6,182,212,.12)` → `rgba(37,99,235,.12)` | ✅ |

**验证**：登录区 10px 圆角残留 0；登录区品牌青残留 0（仅 styles.css:6577 AI 工具 chip 保留，与登录无关）；线上 `.login-btn` / `.login-card input` 均 `border-radius:12px`；首页/新 CSS 均 HTTP 200。

---

## 四、验证方式

1. 构建 + 部署后强刷登录页：按钮/输入框圆角略增大（10→12px），与全站一致
2. 背景光斑由品牌青变为品牌蓝紫（`--pl`），与 `--p` 品牌色系更统一
3. DevTools 检查 `.login-btn` / `.login-card input` computed border-radius = 12px
