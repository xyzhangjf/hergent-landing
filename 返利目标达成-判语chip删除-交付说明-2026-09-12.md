# 返利目标达成 · 时间进度判语 chip 删除 · 交付说明

日期：2026-09-12　版本：v156

## 一、交付

「目标与返利 › 仪表盘 › 返利目标达成」列表的每行原先挂着一枚圆形判语 chip：`超前 70 个百分点` / `落后 20 个百分点` / `与时间进度持平`。本轮**整块删除**，理由是你给的判据：**进度条已经显示了超前还是落后**。

删除后信息并未丢失，只是换了承载位置：

| 信息 | 原载体 | 现载体 |
|---|---|---|
| 超前 / 落后 | chip 文字 + 条形颜色 | **条形颜色**（绿 = 超前，红 = 落后）+ 上方图例 |
| 差多少个百分点 | chip 数字 | 条形 **hover 提示**（完整句） |
| 达标与否 | 行内「已达标 / 预警 / 推进中」标签 | 不变 |

hover 提示长这样（实测原文）：`达成率 200.0%，本月时间进度 40.0%，超前 160 个百分点`、`达成率 40.0%，与本月时间进度 40.0% 基本持平`。

## 二、变更（`src/pages/Rebate.vue`，共 5 处）

| # | 位置 | 内容 |
|---|---|---|
| 1 | 模板 `.rr-top` 内 | 删 `<span class="rr-pace">` 整块（含 ahead / behind / even 三分支），原注释重写为删除理由 |
| 2 | 模板 `.rr-bar` | 加 `:title="it.paceTitle"` —— 承接完整句，信息不丢 |
| 3 | `<script>` items 组装 | 不再把 `timeVerdict` / `vsTimePct` 透传进 item（模板已无人读）；两者仍作局部变量供判色与成句 |
| 4 | `<script>` `vsTimePct` 注释 | 补记「为何不能用 % 代替个百分点」的口径原因，防止后人再改回去 |
| 5 | 样式 | 删 `.rr-pace` 及 `.rr-pace.ahead/.behind/.even` 共 4 条规则；`.rr-bar` 补 `cursor:help`（原 chip 的 hover 光标随之迁址） |

行内其余元素布局未动：`.rr-ach`（达成率）本就是 `margin-left:auto` 贴右，chip 消失后自动收拢，无需改栅格。

## 三、构建与部署

- 产物：`Rebate-BlMU2bVa.js`（165,978 B）/ `Rebate-Dnl1DAFP.css`（39,917 B）
- 构建：`CODEBUDDY_SAFE_DELETE_ENABLED=0 npm run build` → built in 1.82s，0 警告
- 部署：`rsync -a --no-owner --no-group --delete dist/ root@47.113.224.140:/opt/hergent-cn-v2/` + `chown -R hergent:hergent`
- 双侧 md5：
  - JS `4c37a61981e4d75d395f6799d4392ea0` —— **MATCH_OK**
  - CSS `ae6d59d7f6006bda3946a8c0b96e494c` —— **MATCH_OK**

## 四、对齐效果（真机，生产 hergent.cn）

`demo` 租户 + 临时达成数据（带唯一标记，验完已删），三视口 1560 / 1440 / 1280：

- **29 / 29 PASS**，0 JS 报错、0 非 2xx 请求。
- 三分支全覆盖（原担心只剩一条）：
  - 落后 → 条形 `rgb(255, 59, 48)`，提示「落后 40 个百分点」
  - 持平（达成率 40.0% vs 时间进度 40.0%）→ 条形 `rgb(52, 199, 89)`，提示「基本持平」
  - 超前 → 条形 `rgb(52, 199, 89)`，提示「超前 160 个百分点」
- 删除彻底性：DOM 里 `.rr-pace` 计数 `0`，**CSSOM 里 `.rr-pace` 规则计数也为 `0`**（单验 DOM 会漏掉残留样式，故两处都测）。
- 布局无回归：顶行盒高恒 `24px`（单行），整行 `102 ~ 103.6px`，与改动前 v154/v155 实测区间完全一致。
- 边界补测：切到已结束月份（8 月）—— 两条规则均为 `pace-behind`、时间进度虚线按设计不显示（`tpShown` 要求 0<t<1）；切到未开始月份（10 月）—— 无月度目标故不进排行，走空态，符合口径。

## 五、需确认事项

1. **色觉可读性**：删掉文字后，超前/落后完全依赖绿红识别。已通过 hover 提示 + 图例提供文字路径；若你认为还不够，可在图例行补一句「绿＝超前、红＝落后」。
2. 本地 `Rebate.vue` / `Forecast.vue` / `MonthlyAchvChart.vue` / `BrandFilter.vue` 改动**仍未 commit**（与上一轮在途改动同批）。
3. 先前遗留待决：`mptest`（sales）的 `forecast-submissions` 模块权限、`platform.py` 建用户的初始密码标记语义、`:8700` 是否改绑 `127.0.0.1`。
