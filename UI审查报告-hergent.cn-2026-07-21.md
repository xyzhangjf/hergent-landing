# hergent.cn 视觉与交互全面审查报告

> **审查对象**：hergent.cn 桌面端 AI 员工（v1.0.105）
> **审查依据**：线上实际渲染截图（桌面/移动） + `desktop-app/styles.css`（2000行） + `desktop-app/index.html`（1012行）
> **审查日期**：2026-07-21
> **审查人**：Senior Developer

---

## 一、总体评价

### 做得好的地方（先肯定）

在列问题之前，必须承认这套 UI 的基础**比多数一人公司产品扎实得多**：

| 优点 | 具体表现 |
|---|---|
| ✅ 设计令牌系统完整 | CSS 变量覆盖颜色/圆角/阴影/缓动/时长，暗色模式全覆盖（`:root.dark` 重写全部 30+ 变量），不是散装 magic number |
| ✅ 暗色模式真正可用 | 不是简单 invert，是独立设计的一套深色配色（`#101014` 底、`#e8e8f0` 文字），语义色也分别调整（红/黄/绿都提亮了） |
| ✅ 微交互动效有质感 | `bounceIn`、`welcomeFadeIn`、`msgIn`、`pendingPulse`、`slideDown` 全部用 `var(--ease-spring)` / `var(--ease-smooth)`，曲线选得好 |
| ✅ 安全策略到位 | CSP 头严格限定资源来源、`font-src 'self'`、`media-src 'none'` |
| ✅ 中文字体栈正确 | PingFang SC → Microsoft YaHei → sans-serif，渲染不会掉 |
| ✅ 焦点样式全局定义 | `:focus-visible { outline: 2px solid var(--brand-400) }`，键盘用户能跟踪焦点 |

**但**——这些基础好不等于"视觉体验好"。下面按优先级列出问题。

---

## 二、高优先级问题（直接影响第一印象和核心体验）

### 🔴 H1：首屏 50% 是死灰空白——最致命的视觉事故

**现状**：打开产品后，顶部是欢迎区/引导卡片（占 ~35% 高度），底部是输入栏（~15%），**中间约 50% 的屏幕是一片没有任何内容的灰色空地**。

**为什么致命**：这是用户**第一眼看到的东西**。它传达的潜意识信号是"这个产品没做完"或"坏了"。不管你的引擎多强、功能多深，用户在前 3 秒看到的就是一片空——很多人会直接关掉。

**涉及区域**：
- `.chat-history` 的 `flex: 1` 把剩余空间全部填成空白
- `.chat-empty` 内的 `.welcome-area` + `.home-scene-cards` 内容不够撑满
- 引导遮罩（onboarding-guide 或选择员工卡片）显示时下方也是空的

**怎么改**：消灭死空间。三个方向任选——
1. **让欢迎区撑起来**：场景卡片从 2 列改到 3-4 列，加更多快捷入口（导入舟谱 Excel / 查看今日盈亏 / 临期预警），把空白填满
2. **默认显示最近对话列表**：即使没有对话，也展示"开始一段新对话"的模板卡片（类似 ChatGPT 的空白状态）
3. **把引导做成全屏向导**：如果"选择数字员工"是必做步骤，让它 modal 全屏、带步骤进度条，别让它跟聊天区挤在一起留半截空白

---

### 🔴 H2：侧边栏当前页完全看不出选中状态

**现状**：`.sidebar-item.active` 的样式是 `background: rgba(0,0,0,0.05)` + `font-weight: 600`。在浅色背景上，**5% 透明度黑 = 肉眼几乎看不见**。从截图上完全分不清"定时任务"和"连接手机"哪个是当前页面。

**影响**：用户在侧边栏点来点去后**不知道自己在哪**，产生迷失感。这在桌面 App 里是基本导航故障。

**怎么改**：active 背景至少提到 `rgba(6,182,212,0.08)` ~ `rgba(6,182,212,0.12)`（品牌色的淡底），左侧加一条 `3px brand-500` 的竖条指示器（类似 VS Code / Discord 的侧边栏激活态）。成本极低，效果立竿见影。

---

### 🔴 H3：侧边栏缺少品牌标识

**现状**：侧边栏顶部直接就是角色列表（`#sidebarRoles`），没有 Logo、没有产品名、没有用户头像入口。用户不知道自己用的是哪个产品。

**对比参考**：Notion 左上角有 workspace 图标 + 名称；Slack 有 team icon；Discord 有服务器列表。这些都在左上角建立"我在哪"的锚点。

**怎么改**：侧边栏顶部固定一个区域（48-56px 高），放产品图标 + 产品名称 "Hergent"（或"赫金特"），点击可折叠/展开角色区。这同时解决了 H2（active 状态）的问题——用户至少知道"我在 Hergent 里"。

---

### 🔴 H4："选择一位数字员工"引导卡片的视觉权重失衡

**现状**：从截图看，引导卡片占据了主内容区的黄金位置（顶部居中），但它的视觉处理像一个临时弹窗——白色卡片浮在灰底上、按钮是通用蓝色、下方接大段空白。它不像一个"精心设计的入门仪式"，更像一个"没地方放所以放这里了"的折中。

**而且这个文案有问题**："去随便聊个 AI 角色、聊聊你专属领域、点点头像修个像、我能适配任何任务的部位"——最后半句有歧义风险，且整段文案偏"程序员写给程序员看"，低温奶老板看了会懵。

**怎么改**：
1. 如果这是必经步骤 → 做成 **全屏 wizard（step 1/3 式）**，每步一屏、带进度条、背景用品牌渐变而非死白卡片
2. 如果是可选跳过 → 缩小为右侧悬浮卡片或顶部 banner，别占据主页黄金位

---

## 三、中优先级问题（影响专业感和可用性）

### 🟡 M1：圆角半径混乱，缺统一规范

**现状**：代码里同时存在 7 种不同的圆角值，使用场景没有规律：

| 半径值 | 使用位置 | 出现次数 |
|---|---|---|
| 4px (`radius-xs`) | 小标签、小按钮 | 少 |
| 6px (`radius-sm`) | 频率 chip、设置按钮 | 多 |
| 8px (`radius-md`) | 输入框、modal 按钮 | 很多 |
| 10px | 激活码输入、部分 modal | 中等 |
| 12px (`radius-lg`) | 卡片、面板 | 很多 |
| 14px | 场景卡片 | 1处 |
| 16px (`radius-xl`) | 几乎没用 | 极少 |
| 18px | 消息气泡 | 1处 |
| 20px (`radius-2xl`) | modal、window | 多 |
| 30px | dialog 按钮 | 1处 |
| 9999px (`full`) | pill 标签 | 多 |

**问题**：`8px / 10px / 12px` 三种尺寸用在几乎相同的"卡片/按钮"场景上，看起来不统一。

**怎么改**：收敛到 3 级——`sm: 8px`（小元素）、`md: 12px`（卡片/面板）、`lg: 20px`（modal/对话框）。pill 用 `9999px` 不变。删除 10/14/16/18/30 这些中间值。

---

### 🟡 M2：字号阶梯过密，缺排版节奏

**现状**：出现了 11 种不同的 font-size：

```
10px — 时间戳/标签/提示
11px — 次要信息/徽章
12px — 辅助文字/描述
13px — 正文基准（出现最多）
14px — 按钮文字/标题
14.5px — 聊天消息（仅此一处）
15px — 次标题
16px — 页面标题
18px — 大标题
24px — 图标/引导
28px — 欢迎语
```

**问题**：`13px / 14px / 14.5px / 15px` 四个字号挤在 2px 区间里，肉眼根本分不出来，但代码维护成本四倍。`14.5px` 只用在聊天消息上一处——这是个"一次性 hack"的标志。

**怎么改**：收拢到 Major Scale（12/14/16/20/28）+ Fine Scale（10/11/13）。删掉 14.5、15、18、24 这四个"中间地带"。特别是 `14.5px` 的 chat-msg 直接改成 `14px`。

---

### 🟡 M3：body 级 `user-select: none` 伤害可用性

**现状**（styles.css 第 614 行）：`body { user-select: none; }`

**影响**：用户无法选中任何文字——不能复制 AI 回复的内容、不能选中输入框外的文字。这对一个"AI 助手"产品是反直觉的：用户最常做的事之一就是**复制 AI 给他的答案**。

**怎么改**：只在拖拽手柄（titlebar）、侧边栏、按钮上设 `user-select: none`。`.chat-msg`、`.chat-history` 必须恢复 `user-select: text`。

---

### 🟡 M4：HTML 语义化缺失（可访问性硬伤）

**现状**：整个 HTML 结构全是 `<div>` + class，没有语义标签：

| 应该用的语义标签 | 实际用的 |
|---|---|
| `<nav>` | `<div class="sidebar">` |
| `<main>` | `<div class="right-panel">` |
| `<aside>` | 不存在 |
| `<header>` | `<div class="page-header">` |
| `<button>` | 部分用了 button ✓，但有些 clickable div 没有 |
| `<form>` | 登录/激活/充值表单全是 div |

**影响**：
1. 屏幕阅读器用户无法快速导航（"跳转到导航"/"跳转到主内容"不可用）
2. SEO 不友好（虽然这是桌面 App，但 Electron 也受益于语义结构）
3. 表单不能用 Enter 自动提交、不能用浏览器原生验证

**怎么改**：至少三处——侧边栏包 `<nav>`、右侧面板包 `<main>`、所有 form 包 `<form>`。成本极低，收益是整个可访问性基线提升一档。

---

### 🟡 M5：移动端侧边栏没有响应式方案

**现状**：`.sidebar { width: 190px; min-width: 160px; flex-shrink: 0; }` —— 固定宽度，无 `@media` 断点处理。移动端截图（375px 宽）上，侧边栏吃掉了 **一半屏幕宽度**，右侧主内容区被压缩到 ~185px。

**只有渠道连接卡片做了响应式**（`@media max-width: 900px` 变两列、`max-width: 500px` 变单列），但主体布局没动。

**怎么改**：
- ≤768px：侧边栏变为底部 tab bar（图标+文字，5-6 个 tab），类似移动端微信/钉钉的导航模式
- 或者：侧边栏默认隐藏，通过汉堡菜单呼出 overlay drawer

---

### 🟡 M6：内联样式残留

**现状**：index.html 里有多处 inline style：

```html
<!-- line 40 -->
<button ... style="margin-top:8px;border-top:1px solid var(--border-subtle);padding-top:10px;">
<!-- line 67 -->
<div ... style="display:none;">
<!-- line 129 -->
<span ... style="vertical-align:-2px;">
<!-- line 363 -->
<div ... style="display:none;">
<!-- line 644 -->
<p ... style="font-size:12px;color:var(--text-tertiary);margin:4px 0 0;">
<!-- plus 10+ more -->
```

**影响**：破坏 CSS 类的一致性，后续主题切换/重构时这些会被遗漏。

**怎么改**：全部提取为 class。每个 inline style 最多给一次宽限期，之后必须类化。

---

### 🟡 M7：按钮体系不统一

**现状**：至少有 6 种长得不一样的"按钮"：

| 类名 | 圆角 | 背景 | 字号 |
|---|---|---|---|
| `.btn` | 10px | 渐变 brand | 13px |
| `.btn.secondary` | 10px | gray-200 | 13px |
| `.dialog-btn` | 30px | 渐变 brand | 14px |
| `.gw-btn` | 6px | elevated | 12px |
| `.chn-btn` | 8px | bg-input | 12px |
| `.freq-chip` | 18px | transparent | 12px |
| `.og-chip` | 20px | transparent | 13px |
| `.login-btn` | （未查到） | ？ | ？ |

**问题**：同一个产品里，"确认"操作有时是圆角 10px、有时 30px；"次要按钮"有时是灰底、有时透明边框。用户无法建立"这是一样的操作"的心智模型。

**怎么改**：统一为三级按钮——Primary（brand 渐变）/ Secondary（gray-100 底）/ Ghost（透明+边框），每种只有一个圆角和一个字号。特殊形态（chip/pill）保留但限制在筛选/标签场景。

---

## 四、低优先级问题（锦上添花）

### 🟢 L1：空状态太素

**现状**：`.empty-state` 只有一个可选 svg + 文字（"暂无数据"级别），没有插图/动画/引导动作。

**怎么改**：关键空状态（无对话/无任务/无连接）加轻量插画或品牌角色姿势图（可以用 CSS art 或简单 SVG，不用图片资源）。

### 🟢 L2：加载状态只有 spinner

**现状**：`.loading` = 居中 spinner + "加载中..."文字。没有骨架屏（skeleton）、没有 progressive image reveal。

**怎么改**：任务列表/技能列表/成果列表的 loading 改用 skeleton（灰色条模拟布局），感知速度更快。

### 🟢 L3：键盘快捷键无可见提示

**现状**：代码里有 `keyboard.js`，但 UI 上没有任何快捷键 hint（如 Ctrl+K 显示在搜索框旁边、Ctrl+N 新建任务显示在按钮旁）。

**怎么改**：在对应按钮旁加小字快捷键提示（如 Mac 风格的 `⌘K`），power user 会注意到并加速操作。

### 🟢 L4：滚动条样式仅在 WebKit 生效

**现状**：自定义滚动条用了 `::-webkit-scrollbar`，Firefox 用户会看到系统原生滚动条（通常很粗丑）。

**怎么改**：加上 `scrollbar-width: thin; scrollbar-color: var(--gray-300) transparent;` 作为 Firefox 回退（你在 `* { box-sizing }` 后面加一行就行）。

### 🟢 L5：CSP 的 `connect-src` 含 localhost

```css
connect-src 'self' https://api.hergent.cn https://api.hergent.io 
  http://localhost:8765 http://127.0.0.1:18765 ...
```

localhost 开发端口出现在生产 CSP 里不是安全漏洞（因为用户本地没有这些服务），但会在控制台报无用连接错误。上线前应拆分为开发/生产两套 CSP 配置。

---

## 五、与产品设计原则的冲突点

你的 `产品设计原则.md` 写了 **"不追求 UI 花哨，实用 > 好看"**。我尊重这条原则，但需要指出：**上面列出的问题不是"花哨vs实用"的问题，是"基本可用性"的问题。**

具体来说：
- **H1（50% 死灰空白）** ≠ 花哨，是信息密度太低
- **H2（侧边栏 active 看不见）** ≠ 花哨，是导航反馈缺失
- **H3（无品牌标识）** ≠ 花哨，是产品身份模糊
- **M3（无法复制文字）** ≠ 花哨，是功能受损
- **M4（无语义标签）** ≠ 花哨，是可访问性底线

这些不改，"实用"也无从谈起——用户连"我在哪、这是什么、怎么操作"都搞不清。

而真正的"花哨"我没建议你加（比如 3D 背景、粒子特效、玻璃拟态），因为对 B 端工具确实没必要。

---

## 六、改进路线图建议

| 优先级 | 问题 | 工作量 | 建议时机 |
|---|---|---|---|
| 🔴 P0 | H1 消灭首屏死灰空白 | 0.5 天 | **立即**——这是第一印象 |
| 🔴 P0 | H2 侧边栏 active 可视化 | 0.2 天 | **立即**——3 行 CSS |
| 🔴 P0 | H3 侧边栏加品牌标识 | 0.3 day | **本周** |
| 🔴 P0 | H4 引导卡片重设计或降权 | 1-2 天 | 本周 |
| 🟡 P1 | M1-M7 统一圆角/字号/按钮/语义化 | 2-3 天 | 下周集中做 |
| 🟢 P2 | L1-L5 锦上添花 | 1-2天 | 有空再做 |

**总工作量估计**：P0（~2天）→ P1（~3天）→ P2（~1-2天）。一个人两周内可以全部搞定，而且每项都可以独立交付、独立验收。

---

## 七、一句话总结

**基础架构（设计令牌/暗色/动效/CSP）打得很好，但"首屏体验"和"导航反馈"两个最显眼的地方有硬伤，导致第一印象远低于实际产品质量。先把 H1-H4 这四个高优改掉，产品的"完成感"会直接上一个台阶——不是变花哨，是从"能用但有点糙"变成"这就是个正经产品"。**
