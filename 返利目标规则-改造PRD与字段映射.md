# 返利目标规则（rebate_target_rules）· 改造 PRD 与字段映射表

> 对照源：舟谱「目标管理」5 张截图 + 我们 `hergent-erp` 真实代码。
> - 后端表：`server/erp_db.py` v108 迁移 `rebate_target_rules`（21 字段）
> - 后端路由：`server/routers/rebate_rules.py`（CRUD + /validate + /conflicts + /simulate）
> - 前端：`static/js/modules/rebate-rules.js`（vanilla JS 单页表单 + 列表）
> 产出：字段映射对照 + 分优先级改造项 + 产品边界声明。

---

## 0. 定位边界（先说清，避免照搬错）

| 维度 | 舟谱「目标管理」 | 我们 `rebate_target_rules` |
|---|---|---|
| 本质 | **销售目标考核**（定目标→拆到员工→考核） | **返利/指标达成规则引擎**（定规则→算返利冲刺→副驾建议） |
| 核心字段 | 目标名称/指标/周期/拆解对象/拆分明细 | 维度/度量/目标值/触发条件/返利形式/门槛/阶梯/优先级/冲突 |
| 后端强度 | 偏轻（无返利计算） | **偏重**（返利比例/固定/阶梯/门槛/优先级/冲突检测已全） |
| 前端强度 | 卡片入口/向导/详情页/复制/修改记录 | **偏弱**（单页表单，缺入口卡片/向导/详情/复制/审计） |

**结论**：舟谱值得学的是 **前端交互层与信息架构**，不是后端字段（我们后端已更强）。以下改造聚焦前端，后端仅在少量项需补（复制/审计/状态语义）。

---

## 1. 当前真实表结构（server/erp_db.py v108）

| 字段 | 类型 | 含义 | 前端现状 |
|---|---|---|---|
| id | PK | 主键 | ✓ 列表展示 |
| rule_name | TEXT | 规则名称 | ✓ 表单+列表 |
| dimension | brand/product | 计算维度 | ✓ 下拉+筛选 |
| target_type | amount/quantity | 目标度量 | ✓ 下拉（品牌维锁定 amount） |
| scope_key | TEXT | 作用对象键（品牌名/商品ID） | △ 手工填文本 |
| scope_name | TEXT | 作用对象显示名 | △ 手工填文本 |
| period_type | month/quarter/year/custom | 周期口径 | ✓ 下拉 |
| target_value | REAL | 目标值（元/件） | ✓ 数字输入 |
| trigger_mode | on_target/tiered | 触发条件 | ✓ 下拉 |
| trigger_threshold | REAL | 达成门槛(0~1) | ✓ 输入 |
| tiers_json | TEXT(JSON) | 阶梯设置 | ✓ 动态行 |
| rebate_basis | rate/fixed | 返利形式 | ✓ 下拉 |
| rebate_rate | REAL | 返利比例 | ✓ 输入 |
| rebate_amount | REAL | 固定返利额 | ✓ 输入 |
| effective_start | TEXT(YYYY-MM-DD) | 生效开始 | ✓ date 输入 |
| effective_end | TEXT(YYYY-MM-DD) | 生效结束 | ✓ date 输入 |
| priority | INT | 优先级（大优先） | ✓ 数字输入 |
| is_active | INT(0/1) | 启用 | ✓ 启用/停用 按钮 |
| created_at / updated_at | TEXT | 时间戳 | ✗ 仅后端 |

> 后端已有能力：列出（支持 dimension/scope_key/q/include_inactive 筛选）、创建（带 validate + conflicts 校验）、详情、更新、软删、冲突检测、试算。

---

## 2. 前端当前能力盘点（rebate-rules.js）

**已有**
- 列表：优先级/名称/维度·度量/作用对象/周期/目标值/触发·返利/生效区间/状态/操作。
- 操作：编辑、停用/启用、删除。
- 筛选：名称/作用对象搜索 + 维度下拉。
- 冲突 banner（列出重叠规则对）。
- 返利试算弹窗（simulate）。
- 单页 Modal 表单（含条件显隐：品牌维锁金额、阶梯行、门槛、比例/固定切换）。

**缺失（对比舟谱）**
- ✗ 维度选择卡片首页（业务/品牌/商品三入口）。
- ✗ 三步创建向导（基本信息→设定目标→确认）。
- ✗ 独立「目标详情」页（仅有编辑弹窗，无摘要+作用范围+拆分明细+合计）。
- ✗ 状态语义「进行中 / 已结束」（只有启用/停用，未基于生效期推断）。
- ✗ 复制按钮（后端无快捷复制接口）。
- ✗ 修改记录 / 操作审计（无 audit 表）。
- ✗ 作用对象选择器（品牌/商品/员工弹窗，当前纯文本输入）。
- ✗ 指标口径业务化命名（叫「金额/数量」而非「净销售额/销售数量(大单位)」）。

---

## 3. 字段映射对照表（舟谱 → 我们）

| 舟谱字段/能力 | 我们对应 | 覆盖 | 改造建议 |
|---|---|---|---|
| 目标分类：业务/品牌/商品 | dimension=brand/product（缺 business） | △ | 前端做 2 卡片入口（品牌/商品）；business 维度暂不扩后端，留作远期 |
| 目标名称 | rule_name | ✓ | 保持，加字数提示（如≤30字） |
| 目标起止时间（按月度） | effective_start/end + period_type | ✓ | 列表/详情增加「进行中/已结束」状态推导 |
| 目标指标（7 个业务口径） | target_type(amount/quantity) | △ | 仅 2 个度量；前端**显示名**改为业务语义（金额→下单金额/净销售额，数量→销售数量(大单位)） |
| 目标拆解方式（员工/客户/不拆解） | 无 | ✗ | 返利规则**暂不需要**；若未来加 business 维度再补（见 §5 边界） |
| 选员工 | 无 | ✗ | 同上，不纳入返利规则 |
| 目标值 | target_value | ✓ | 保持，单位跟随度量 |
| 触发条件（达成即返/阶梯） | trigger_mode(on_target/tiered) | ✓ | 已覆盖，优于舟谱（舟谱无阶梯返利） |
| 返利形式（比例/固定） | rebate_basis(rate/fixed) | ✓ | 已覆盖 |
| 状态：进行中/已结束 | is_active 仅启用/停用 | △ | 前端新增派生状态：生效期内且启用=进行中；超生效期=已结束 |
| 详情→拆分明细+合计 | 无独立详情页 | ✗ | 新增详情页：目标摘要 + 作用范围 + 本期达成（接 `/api/rebate-rules` + 副驾冲刺数据）+ 合计 |
| 复制 | 无 | ✗ | 后端加 `POST /api/rebate-rules/{id}/clone`（复制并改周期）；前端加复制按钮 |
| 修改记录 | 无 audit | ✗ | 远期：加 `rebate_rule_audit` 表或复用 updated_at + 版本；前端加「修改记录」入口 |
| 列表筛选（名称/品牌/商品/起止/类型） | 名称+维度 | △ | 加：周期月份、状态、度量类型筛选 |
| 首页卡片入口 | 单页列表 | ✗ | 新增维度选择卡片（品牌目标/商品目标） |
| 三步向导 | 单页表单 | ✗ | 改为 Step1 框架（维度/度量/对象/周期）→ Step2 目标值/门槛/阶梯/返利 → Step3 确认 |

---

## 4. 改造项清单（按优先级）

### P0 — 低成本高感知，纯前端
| # | 借鉴点 | 对应舟谱 | 改动 | 优先级 |
|---|---|---|---|---|
| P0-1 | 维度选择卡片入口 | 图1 | 列表页顶部新增「品牌目标」「商品目标」两张卡片（点击进对应新建表单并预设 dimension）；列表区保留 | ★ |
| P0-2 | 指标口径业务化 | 图5 | 前端 label 映射：amount→「下单金额(元)」、quantity→「销售数量(大单位)」；列表/详情同步 | ★ |
| P0-3 | 状态语义（进行中/已结束） | 图2 | 列表新增派生状态列：生效期内&启用=进行中(绿)，超期=已结束(灰)，停用仍显示停用 | ★ |
| P0-4 | 详情页（摘要+范围+达成+合计） | 图3/4 | 新增目标详情 Modal/页：名称/目标值/周期/作用对象/触发·返利 + 副驾冲刺达成条 + 合计；替代纯编辑弹窗的信息密度 | ★ |

### P1 — 体验进阶，前端为主、后端少量补
| # | 借鉴点 | 对应舟谱 | 改动 | 优先级 |
|---|---|---|---|---|
| P1-1 | 三步创建向导 | 图5 | 表单拆 Step1 框架 / Step2 数值 / Step3 确认；底部「下一步(设定目标)」带目的地文案 | ★★ |
| P1-2 | 列表筛选增强 | 图2 | 加：周期月份、状态、度量类型下拉筛选 | ★★ |
| P1-3 | 作用对象选择器 | 图3/5 | 品牌→选品牌下拉（接商品库 brand 去重）；商品→选商品弹窗（多选 SKU）替代纯文本 | ★★ |
| P1-4 | 首页引导 Banner / 空态 | 图1 | 未配置目标时显示「设置返利目标，让副驾在下单时提示冲刺建议」引导卡 | ★★ |
| P1-5 | 复制按钮 | 图2 | **后端**加 `POST /api/rebate-rules/{id}/clone`（复制并清空 id、生成新周期建议）；**前端**列表加「复制」 | ★★ |

### P2 — 远期 / 视需求
| # | 借鉴点 | 对应舟谱 | 改动 | 优先级 |
|---|---|---|---|---|
| P2-1 | 修改记录 / 审计 | 图2 | 加 `rebate_rule_audit` 表记录变更；前端「修改记录」入口 | ★★★ |
| P2-2 | 业务目标维度 | 图1 | 扩 dimension 加 `business`（总大盘目标，非品牌非商品）；需后端迁移+前端表单 | ★★★ |
| P2-3 | 拆到员工/客户 | 图3/4/5 | 仅当 P2-2 业务目标落地时考虑；返利规则本身不需要 | ★★★ |

---

## 5. 产品边界声明（不照搬项）

1. **不引入「拆解到员工/客户」**：舟谱这是销售考核语义；我们的返利规则作用于「品牌/商品 → 厂家返利」，无需拆到人。强行照搬会污染引擎语义。
2. **不引入拜访客户数/铺货客户数等指标**：这些是考核指标，非返利口径；我们的 `target_type` 保持 amount/quantity 即可。
3. **返利计算字段不删不弱**：trigger_threshold / tiers_json / rebate_rate / rebate_amount / priority / conflicts 是我们的护城河（舟谱无），改造只在前端交互层做加法。
4. **状态语义需兼容现有 is_active**：新增「进行中/已结束」是派生展示，不改动存储；停用仍是最强开关。

---

## 6. 与现有副驾「返利冲刺看板」的衔接

- 看板（Forecast.vue）已读 `/api/rebate-rules` 活跃规则并按本期预报聚合。
- P0-4 详情页的「本期达成」可直接复用看板聚合逻辑（按 dimension/scope_key 匹配本期预报额/量）。
- P0-3 状态语义与看板「本期期次」同源（基于 effective_start/end + 当前期次）。
- 三者（列表状态 / 详情达成 / 看板建议）数据一致，避免多套口径。
