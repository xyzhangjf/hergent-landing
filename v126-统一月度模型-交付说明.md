# v126 统一月度模型 · 交付说明

> 日期：2026-09-09
> 后端 `hergent-erp` `67f17c5`（分支 `upgrade/v84-international`）
> 前端 `hergent-cn-v2` `8d924fd`（分支 `main`）
> 状态：**已部署生产并真机验证**

---

## 一、交付

把品牌目标的「年度 / 单期」两个录入口径**合并为一张 12 行月表**，并支持**按月独立配置阶梯返利**；顺带修掉一个长期隐藏的重大计算 bug。

| 编号 | 交付项 | 位置 | 状态 |
|---|---|---|---|
| 1 | 新增 `monthly_tiers` 月级阶梯列（含迁移 + 三处白名单） | `erp_db.py` / `routers/rebate_rules.py` | ✅ |
| 2 | `monthly_view(rule, ym)` 月度投影函数 | `domain/rebate_period.py` | ✅ |
| 3 | 试算/计提按月取值（修复年度 12 月分解是死代码） | `domain/rebate_calc.py` | ✅ |
| 4 | 单期规则自动归一进月表 | `routers/rebate_rules.py` | ✅ |
| 5 | 品牌维度不再被强制 `on_target`，可用阶梯 | `routers/rebate_rules.py` | ✅ |
| 6 | 前端统一 12 行月表（删年度/单期切换） | `MonthlySplitBlock.vue` / `BrandTargetForm.vue` | ✅ |
| 7 | 按月阶梯编辑 UI + 默认档位回退 | `RebateValueBlock.vue` / `TargetFormModal.vue` | ✅ |
| 8 | 存量数据迁移脚本（已 apply，可回滚） | `v126-统一月度模型-迁移脚本.py` | ✅ |

---

## 二、变更

### 核心口径（用户拍板）

1. **没有年返** —— 不做年度结算返利块。
2. **逐月填** —— 返利率逐月录入；「全年返利率」仅作为一键铺 12 格的工具，不是存储口径。
3. **留年度的** —— 8/9 月重复目标取 #10 年度 15%，退役 #6/#8 单期 10%。

**数据模型**：年度 = 12 格全填；单期 = 只填 1 格。顶层 `target_value / rebate_rate / tiers_json` 降级为「未单独配置月份的回退值」，老数据零影响。

### 后端

- `rebate_target_rules.monthly_tiers`（TEXT）：`{"08":[{"from_pct":80,"to_pct":100,"rate":0.03}], ...}`
- `monthly_view(rule, ym)`：把规则投影到某月，用该月格值覆盖三个顶层字段。
- 注入点 `compute_rule_rebate` 首行 → `/simulate`、`/simulate-batch`、`accrue_rebate` 三条路径同时生效。
- `_normalize_by_dimension` 仅在 `trigger_mode != 'tiered'` 时回落 `on_target`。
- 单期归一化：品牌维度且无 `monthly_amounts` 时按生效期自动铺格（整月 1 格 / 同年跨月均摊 / 跨年跳过），并改 `period_type='year'`。

### 前端

- `MonthlySplitBlock.vue` 重写为统一月表：月份 / 目标金额（万元）/ 返利率（%） 或 **阶梯档位（按月独立配）**。
- 顶部快填：目标年度 · 全年目标（铺 12 格）· 全年返利率（铺 12 格）。
- 阶梯行「配置」→ 行内展开 `RebateValueBlock`，标题 `08 月档位（留空则回退默认档位）`；表单底部另有「默认档位（未单独配置的月份用）」。
- `useRebateTargetForm.js`：新增 `buildTiersPayload` / `tierText` / `fillAnnualRateToMonths`；`emptyMonthlyRows` 每行带 `tiers: []`；`fillMonthlyFromRule` 单期回填到生效月那一格。

---

## 三、修掉的隐藏 Bug（重点）

`compute_rule_rebate` 原先只读**顶层** `target_value`（901 万）→ **年度规则的 12 月分解在试算里完全是死代码**：8 月达成 95 万 / 901 万 ≈ 10%，永远够不到门槛，返利恒为 0。

之前仪表盘看到的「8 月返利 11 万」，实际全部来自单期规则 #6（100 万 × 10%），年度 #10 一分没算。

| 场景 | 修复前 | 修复后 |
|---|---|---|
| #10 年度 8 月（basis 95 万，目标 95 万，15%） | 返利 **0** | 返利 **142,500** |

---

## 四、迁移（生产 tenant_1，已执行）

- 备份：`/root/hergent-backup/tenant_1.db.v126.*`
- #6/#7/#8/#9 归一化进 `monthly_amounts`（各 1 格）
- 停用 #6、#8（让位 #10 的 15%）
- 重建月份锁表 → **14 行**（简爱 8/9 月 2 行 + 蒙牛低温 #10 的 12 行）

回滚：`python3 v126-统一月度模型-迁移脚本.py --rollback`

---

## 五、验证

| 项 | 结果 |
|---|---|
| 后端 health | HTTP 200 |
| `monthly_tiers` 列 | 生产 `tenant_1.db` 已存在 |
| 规则 / 月份锁 | 5 条规则 / 14 行锁；测试规则 id=11 硬删后 0 残留 |
| 月级阶梯 API | 8 月（自有档 80~100%:3%）→ 生效 **3.00%**；3 月（无自有档→默认 90%+:2%）→ **2.00%**；8 月 100% 达成 → **5.00%** |
| 前端构建产物 | 生产 dist 含 `monthly_tiers` / `monthlyTiers` / `add-month-tier` / `默认档位` / `达成即返（按月返利率）` |

**真机 UI**：12 行渲染、年度/单期切换已消失；全年目标 1200 → 12 格各 100；全年利率 15 → 12 格各 15；改 8 月为 12 其余不动；切阶梯后第 3 列变档位列；8 月「配置」填 80/100/0.03 显示 `80~100%:3%`；编辑 #10 回填 12 格（60/50/60/70/80/90/90/95/100/70/76/60）；编辑简爱 9 月只回填 9 月一格（8）；仪表盘重复目标告警消失，剩 1 条「2 个目标预计月底不达标」。

---

## 六、已知限制 / 后续

1. **1–12 月年度数据仍不完整**：目前只有 8/9 月与 #10 有数据，其余月份目标为 0，达成图会显示为空。
2. **按月阶梯只在品牌维度开放**；产品维度仍按顶层档位。
3. **叠加 vs 覆盖**：本次按「覆盖」处理（同品牌同月只留一条）。若日后出现「年度框架 + 月度临时奖」需叠加的业务，需新增 `policy_layer` 开关。
4. P3 六期预览调度器（是否跳过周末）仍待确认。
