# 返利达成排行 · 进度条配色对齐冲刺看板 — 交付说明

**日期**：2026-09-12 · **版本**：v151 · **改动**：纯前端（Rebate.vue + Forecast.vue）
**起因**：返利达成排行的进度条在「超过时间进度」时颜色与预报·返利冲刺看板不一致。

---

## 一、问题定位（先讲结论）

两处**色值本来就相同**（都用 `--suc` #34c759 / `--dan` #ff3b30），不一致的是**「什么状态算绿」**：

| | 返利达成排行（改前） | 冲刺看板 |
|---|---|---|
| 🟢 绿 | 达成 ≥ **100%**（已达标） | 达成 ≥ **时间进度** |
| 🔴 红 | **预计月底不达标**（预警，与时间进度无关） | 达成 < **时间进度** |
| 🔵 青 | 其余（推进中） | 无此态 |

→ 在「超过时间进度（40%）但没到 100%」这个区间，冲刺看板是**绿**，返利排行是**青**（或红）。
另：两处**达成口径不同源** —— 返利排行只算已填报，冲刺看板算「已填报 + 本期预报贡献」。

**选定方向**（用户确认）：返利排行改为**时间进度着色**，与冲刺看板完全同口径；青色「推进中」取消，
「已达标 / 预警 / 推进中」改由**行内标签 + 行边框**表达。

---

## 二、变更清单

| # | 文件 | 位置 | 改动 |
|---|---|---|---|
| 1 | `Rebate.vue` | 计算层 ~L1886 | 新增 `paceCls = timeProgress > 0 ? (ach >= timeProgress ? 'pace-ahead' : 'pace-behind') : ''` |
| 2 | `Rebate.vue` | push ~L1927 | `items` 增加 `paceCls` 字段 |
| 3 | `Rebate.vue` | 模板 L127 | 条形 `:class="it.level"` → `:class="it.paceCls"` |
| 4 | `Rebate.vue` | 图例 L109-111 | 「已达标/预警/推进中」→「超前时间进度 / 落后时间进度」 |
| 5 | `Rebate.vue` | CSS L2879-2880 | `.lg.done/.risk/.ontrack` → `.lg.ahead`(绿) / `.lg.behind`(红) |
| 6 | `Rebate.vue` | CSS L2967-2968 | `.rr-bar-fill.risk/.done` → `.rr-bar-fill.pace-ahead`(绿) / `.pace-behind`(红) |
| 7 | `Forecast.vue` | `sprintBarClass` L5557-5566 | 加「到货月尚未开始（`frac<=0`）→ 返回空走中性青」，与排行口径对齐 |
| 8 | `Forecast.vue` | 注释 | 标注两处自 v151 起同口径 |

**刻意保留不动**（信息不丢）：
- 行内标签三态（`.tag.ok` 已达标 / `.tag.risk` 预警 / 空 推进中）— 仍由 `it.level` 驱动
- 行边框 `.rank-row.risk` / `.rank-row.done`
- 顶部大号达成率数字 `.rr-ach` 的 `val-warn` 变红
- 底部 `rr-vs`（超前/落后时间进度 X 个百分点）与 `rr-warn` 预判文案
- 时间进度虚线标记 `.rr-bar-mark`（`2px dashed var(--war)`，与冲刺看板 `.sp-bar-mark` 完全同款）

---

## 三、构建与部署

```
Rebate-DZARzCyf.js   167,167 B
Forecast-sjfJDNd2.js 247,313 B
Forecast-B60FJo3K.css 62,026 B
Rebate-BNn2zgQ0.css   41,607 B
```

- 构建：`CODEBUDDY_SAFE_DELETE_ENABLED=0 npm run build`（Vite 6，1.73s）
- 部署：`rsync -a --no-owner --no-group --delete dist/ root@47.113.224.140:/opt/hergent-cn-v2/` → `chown -R hergent:hergent`
- **双侧 md5 一致（MATCH_OK）**：

| 文件 | md5 |
|---|---|
| Rebate-DZARzCyf.js | `3e220e7e3becdd0cda566a068258c1f8` |
| Forecast-sjfJDNd2.js | `94d73fa2eba377d62f425b703020d044` |
| Forecast-B60FJo3K.css | `4997341b9eb716a330193ce81f5d16a0` |
| Rebate-BNn2zgQ0.css | `25d3280042e4724c360a767564c061ab` |

---

## 四、真机验证（25/25 PASS）

造数：演示租户 `tenant_10` 的「测试月度品牌目标」（9 月目标 ¥900,000；本月时间进度 **40%**），
临时写入 `rebate_achievements`（`note='verify-v151'`），**验完已删除并回读归零（left=0）**。

| 达成 | 期望 | 实测 class | 实测颜色 | 行内标签 | 结果 |
|---|---|---|---|---|---|
| ¥450,000（50%） | 绿 超前 | `rr-bar-fill pace-ahead` | `rgb(52,199,89)` | 推进中 | ✅ |
| ¥360,000（40%，**= 时间进度**） | 绿 边界 | `rr-bar-fill pace-ahead` | `rgb(52,199,89)` | 推进中 | ✅ |
| ¥200,000（22.2%） | 红 落后 | `rr-bar-fill pace-behind` | `rgb(255,59,48)` | 预警 | ✅ |

边界确认：`ach == timeProgress` 判**绿**（`>=` 而非 `>`），虚线与条同处 40%（`markPct=40`）。
图例三项文案与色值、冲刺看板回归（时间进度 40%、红条）均断言通过。

**证据截图**（`outputs/`）：
- `v151-返利排行-时间进度着色-绿-超前.png`
- `v151-返利排行-时间进度着色-绿-边界持平.png`
- `v151-返利排行-时间进度着色-红-落后.png`
- `v151-对照-冲刺看板-回归-20260912.png`

---

## 五、需确认事项

1. **返利排行只算「已填报」达成，冲刺看板算「已填报 + 本期预报贡献」** —— 同一品牌在两处可能
   连数字都不同（进而颜色不同）。要不要把返利排行也并入预报贡献？（本次未动）
2. **月中填报滞后可能使排行整片偏红** —— 这是「按时间进度着色」的必然结果（冲刺看板已是如此），
   若不希望排行过度告警，可改成「仅当落后 ≥ N 个百分点才红」的容差制。
3. **所选月份尚未开始时**（切到未来月，`timeProgress<=0`）条形保持**中性青**、不判红绿，避免
   「0 达成也判绿」。若希望未来月也强制显色，需要另定规则。
4. 本地 `Rebate.vue` / `Forecast.vue` 改动**未 commit**（延续工作区在途改动同批）。
