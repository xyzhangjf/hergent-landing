# 全站页头工具栏单行化 — 验收清单（v107.26）

> 日期：2026-08-04 · commit `d4dcbb6` · 线上 bundle `main-DDUPA4rJ.js` · 备份 `/tmp/static_backup_20260804_*.tar.gz`
> 目标：商品档案工具栏 3 行 → 1 行；全站逐页审查同类问题并优化。功能零缺失、操作流程不打断、窄屏有降级。

---

## 一、核心：商品档案页（P0-1）

**现状（改前）**：页头 3 行，约 144px 垂直空间
- 第 1 行：搜索框 + 主供应商搜索框
- 第 2 行：7 个下拉筛选（状态/图片/条码/品牌/分类/保质期/标签）+ 查询 + 重置（9 控件，全站最拥挤行）
- 第 3 行：操作工具栏（创建/导入/导出/批量操作/视图切换）

**改后**：1 行（约 44px，节省 ~100px）
- 单行内从左到右：搜索框(flex:2) | 状态 | 品牌 | 分类 | 查询 | 更多筛选▾ | spacer | 创建 | 导入 | 导出 | 批量操作▼ | 视图切换
- **次要筛选收进「更多筛选」下拉**：主供应商 / 商品图片 / 有无条码 / 保质期 / 自定义标签 + 重置按钮。点击展开面板，点击外部自动关闭（新增 `window._toggleMoreFilters`）。**功能零缺失**——下拉选项、onchange 行为与原来完全一致，仅收纳位置变化。
- `clearAdvFilter()` 补 `afSupplier` 重置（此前漏重置主供应商搜索框）。

**验收要点**：
1. 进入「商品档案」→ 页头仅 1 行，搜索+核心筛选+操作按钮同行
2. 点「更多筛选 ▾」→ 面板出现，含主供应商/图片/条码/保质期/标签/重置；点外部关闭
3. 每个下拉筛选生效（如状态=停产、保质期<30天）；重置清空全部含主供应商
4. 批量操作 ▼ 菜单三项（批量编辑/批量导出/批量停用）可用
5. 视图切换按钮在，卡片/表格切换正常
6. 左侧分类树「商品品类」面板不变

---

## 二、全站逐页优化清单（其余 14 处）

| # | 页面 | 位置(文件:行) | 改前 | 改后 | 节省 |
|---|------|--------------|------|------|------|
| 1 | 客户/供应商档案 | app.js renderData 客户分支 | 2 行：搜索+筛选chips / 操作工具栏 | **1 行**：搜索+chips+spacer+创建/导入/导出/视图 | ~40px |
| 2 | 调拨管理 | app.js render_transfer_orders | 2 行：筛选chips / 工具栏(新增调拨+导出) | **1 行**：chips 左 + 操作按钮右 | ~40px |
| 3 | 目标管理 | app.js render_goals | 标题下「+设定目标」单按钮独占一行 | 按钮**并入页标题行**右侧 | ~48px |
| 4 | 员工交账 | app.js renderSettlements | 2 行：HR tab chips / 工具栏(+交账) | **1 行**：chips 左 +「+交账」按钮右 | ~40px |
| 5 | 员工列表 | app.js render_hr | 2 行：标题「员工列表」/ 工具栏(+添加员工) | **1 行**：标题+按钮同行 | ~36px |
| 6 | 会计凭证 | app.js renderJournal 等 | 2 行：标题 / 工具栏(录入凭证+计提折旧) | **1 行**：标题+两按钮同行 | ~36px |
| 7 | 客户关系-排行Top10 | app.js render_crm | 2 行：标题 / 工具栏(返利合同+客户档案) | **1 行**：标题+两按钮同行 | ~36px |
| 8 | 项目管理 | app.js render_projects | 操作行(新建项目+添加任务)独占 + 标题行 | 按钮**并入「项目列表」标题行** | ~44px |
| 9 | 固定资产 | app.js renderAssets | 标题下「+新增资产」按钮独占一行(margin 16px) | 按钮**并入页标题行** | ~48px |
| 10 | 销售单 | app.js render_sales | 2 行：筛选chips(+保存) / 工具栏(新增销售/车销/拣货/导出) | **1 行**：chips 左 + 全部操作按钮右 | ~40px |
| 11 | 采购单/采购退货 | app.js render_buying | 同销售单结构 | **1 行**：chips 左 + 全部操作按钮右 | ~40px |
| 12 | 财务管理 | app.js renderAccounts | 2 行：tab(流水/应收应付) / 工具栏(记账/预收/核销/2对账) | **1 行**：tab 左 + 5 操作按钮右 | ~40px |
| 13 | 市场费用 | app.js render_marketing | 3 行：tab chips / 时间筛选 / 工具栏(费用/预算/促销) | **2 行**：tab chips+操作按钮 1 行 + 时间筛选 1 行 | ~40px |
| 14 | 告警管理 | js/modules/alerting.js | 2 行：筛选chips(margin-top 20px) / 工具栏(+新增规则) | **1 行**：chips 左 +「+新增规则」右；间距 20→14px | ~44px |

## 三、注入兼容（关键，防功能缺失）

- `js/modules/dedup-manager.js`：原注入目标 `.data-toolbar`（已并入单行）→ 新增优先注入 `.data-filter-row`（商品/客户/供应商页单行工具栏），重复检测按钮保留
- `js/modules/customer-hierarchy.js`：原兜底注入 `.tbl-toolbar` → 改为 `.data-filter-row, .data-toolbar, .tbl-toolbar`，「层级视图」按钮保留

## 四、设计规范一致性

- 全部沿用现有控件体系：`.btn btn-sm/btn-xs`、`.filter-chip`、`.data-filter-select`、`.data-search-input`，无新增按钮样式
- 圆角/间距/图标（`icon()` SVG）与全站一致；未引入新令牌
- 窄屏降级：所有合并行 `flex-wrap: wrap`，操作区 `justify-content:flex-end` 可自动换行，不溢出、不裁切
- 合并进标题行的按钮加 `text-transform:none`，避免受 `.section-title` 大写化影响（中文无影响，英文安全）
- 合理保留（未动）：表单页区段布局、dashboard 统计卡、表格行内操作、modal 弹窗、库存/应收应付等本已单行的工具栏

## 五、验收方式

1. 浏览器打开 erp.hergent.cn（**Cmd+Shift+R 强刷**），逐页对照第二节清单 1-14 项
2. 商品档案页重点验收第一节 6 点
3. 检查「重复检测」「层级视图」按钮仍在（客户档案页）
4. 窄屏（浏览器宽度 <1280px）检查各页工具栏自动换行、无横向溢出
