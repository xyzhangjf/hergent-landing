# 品牌目标「首次报单日 + 智能创建/关闭报单表」可行性评估与实施方案

> 评估日期：2026-09-08　评估人：Senior Developer
> 需求来源：目标与返利 › 创建品牌目标 › 到货模式，新增「首次报单日」+「智能创建/关闭报单表」开关
> 代码基线：hergent-erp（分支 upgrade/v84-international）、hergent-cn-v2、forecast-order-miniprogram

---

## 业务事实确认（2026-09-08 用户口述，权威）

> 「我们现在也是蒙牛和简爱在一张报单表上。蒙牛每两天报一次单、每两天到一次货；简爱每周报两次单、到两次货。所以用人工去提醒——这一单不光要报蒙牛，也要报简爱。」
>
> 「报单**只能提前，不能延后**，因为涉及品牌方要排产，过了品牌方的报单日期就只能下期再报了。但提前最多 1–2 天，如果提前太长时间，报单准确性会非常低。」
>
> 「简爱我是**周二/周五报单**，但**其它租户可能不一样，要让用户自己配置**。」「可提前天数按你推荐的 **1 天**。」

> **「我们现在是报单日的先一天晚上就可以报单，第二天 10 点截止报单** —— 因为还要留给经理改单的时间、我付款的时间，款到厂家账上也还要时间，**12:00 点前必须在厂家系统完成下单**，否则就只能走特殊报备，或者就错过报单、只能下个周期再报。」
>
> 「**关单就设在报单日上午 10 点**。如果谁错过了，**要找管理员点击开单才能报**，所以要给管理员留这个**再次开单**的权限。」

这四条事实同时确认了七件事：

1. **报单表不分品牌** —— 一张表报完所有品牌。**方案 1（租户级单序列）得到业务验证，方案 2 作废**（详见 §2.1 R1 改写、§6.1）。
2. **主序列 = 最密品牌的节奏** —— 蒙牛每 2 天是主节奏，报单表按它生成；简爱跟着走。
3. **真正的痛点是「本期该报哪几个品牌」没人提醒** —— 这才是自动化的最大价值，比自动建表本身更值钱。已据此新增 **§4.4 本期应报品牌清单**设计，把人工提醒变成系统算。
4. ⚠️ **报单方向是「提前」不是「延后」** —— 品牌方给的是**报单截止日**，错过即作废（只能等下期），提前太多则数据不准。这条**推翻了 §4.4 初版的「吸附延后」设计**，算法已重写为「提前窗口匹配」（详见 §4.4）。
5. 🔴 **报单节奏必须用户可配，不能hardcode** —— 你的简爱是**周二/周五**，但其它租户各不相同。因此报单排程**必须同时支持两种模式**（按间隔天数 / 按固定星期），且**主序列生成也要支持 weekday 模式**（否则纯 weekday 租户无解）。`E = 1` 天为确认默认值。
6. 🔴 **报单窗口是「跨夜 14 小时」，不是单日** —— 报单日前一天晚上开放、次日上午 10:00 截止。这**推翻了初版 `order_start = order_end = D_k` 的单日窗口设计**，必须引入精确时刻字段（详见 §4.1）。
7. 🔴 **一期报单有三个时点，不是一个** —— `开放(前一日晚)` → `截止(当日 10:00)` → `厂家下单截止(当日 12:00)`。中间那 2 小时是**经理改单 + 付款 + 款到账**的缓冲，**系统不能占用**。且**必须有管理员「重开」通道**给错过的人补报（详见 §4.5）。

> **由此确定的时间语义（后续所有推导的公理）**
>
> | 概念 | 定义 | 方向约束 |
> |---|---|---|
> | **报单截止日 `C_X`** | 品牌方要求的最晚报单日（= 品牌到货日 − `order_lead_days`） | 报单日必须 **≤ `C_X`**，过期作废 |
> | **可提前天数 `E_X`** | 该品牌允许的最大提前量，**业务上限 2 天** | 报单日必须 **≥ `C_X − E_X`** |
> | **有效窗口** | `[C_X − E_X, C_X]`，长度 `E_X + 1` 天 | 主序列报单日 `D_k` 落进窗口才算命中 |
>
> **一期报单的三个时点（2026-09-08 确认，§4.1 展开）**
>
> | 时点 | 时刻 | 含义 | 系统动作 |
> |---|---|---|---|
> | `T_open` | 报单日 **前一日 20:00** | 开放填报 | **自动创建**报单表 |
> | `T_close` | 报单日 **当日 10:00** | 报单截止 | **自动关单** |
> | `T_supplier` | 报单日 **当日 12:00** | 厂家系统下单截止 | **仅提醒**（不自动下单，见红线） |
>
> ⛔ **红线**：12:00 的厂家下单动作**不做自动化** —— 厂家系统无开放接口，且托管客户账号违反 ToS（与「不做服务端爬虫、不托管客户账号」一致）。系统只做**提醒 + 逾期告警**，下单仍由人工在厂家系统完成。

---

## 0. 结论先行

**可行，具备落地条件。** 原架构阻塞项（多品牌是否各自出报单表）已由你的业务现状解除；当前唯一待定的是**品牌清单的匹配参数**（可提前天数默认值与简爱截止日星期），不阻塞开工。

| 维度 | 结论 |
|---|---|
| 算法可行性 | ✅ 可行。`domain/arrival_schedule.py` 已有到货排程引擎，只需新增「到货日 → 报单日」反向推导 |
| 调度可行性 | ✅ 可行。`server/scheduler.py` 已有 60 秒粒度循环 + 多租户遍历 + 单租户异常隔离 |
| 数据可行性 | ⚠️ 需补字段：品牌目标表 +10 列、报单表 +10 列、新建执行台账 1 张表 |
| ~~架构阻塞项~~ | ✅ **已解除**。原担忧「多品牌各自开表会冲突」，你已确认蒙牛和简爱**本来就共用一张表** → 方案 1 成立 |
| 风险等级 | **中**（主要风险：已踩过的「open 期次污染」坑会复发 + **品牌截止日漏匹配**） |
| 建议路径 | **一步到位**：租户级单序列（2–3 天），品牌差异靠 §4.4 清单解决，不再引入品牌维度 |

**⚠️ 补充一个重要发现（2026-09-08 算法验证）**：报单「只能提前不能延后」这条约束，把**「首次报单日」从普通配置项变成了决定性参数**——它不只是序列锚点，它的**相位**直接决定某些品牌的截止日能不能被命中。实测（蒙牛 cadence=2 + 简爱周二/周五）：

| 首次报单日 | 9–10 月简爱漏报次数 |
|---|---|
| 2026-09-01（周二） | **0 次** ✅ |
| 2026-09-02（周三） | 1 次（漏掉 9/1 那期）❌ |

因此「首次报单日」在保存时**必须做模拟 + 相位推荐**，不能让用户随便填（详见 §4.4.7、§7.1）。

**✅ 用你确认的真实参数复算通过**：蒙牛 cadence=2 + 简爱周二/周五 + 可提前 1 天 → **9 月零漏报，最大提前 1 天**。

**已确认采用方案 1（租户级单序列）**，理由与你在 2026-09-07 亲自确认的采购口径完全一致：

> 「要采购也是本期预报里所有门店汇总后一起采购」

同理——**报单表是「一次向厂家下单的收集窗口」，本质是经销商级的，不是品牌级的**。蒙牛每 2 天报单、简爱周二/周五报单，但业务员打开小程序只需要看到**一张**当前报单表，报完所有品牌的货。这与「不按门店拆采购单」是同一个道理，不应按品牌拆报单表。

---

## 1. 现有实现盘点（已回代码核实）

| 资产 | 位置 | 现状 |
|---|---|---|
| 品牌目标表 | `rebate_target_rules`（租户库） | 已含到货六字段 `arrival_mode / arrival_first_dom / arrival_cadence_days / arrival_weekdays / arrival_base_dow / arrival_count_override`；**无「首次报单日」、无提前天数、无自动开关** |
| 报单表（期次） | `forecast_periods`（租户库） | `id, name, order_start, order_end, arrival_date, status, created_at`。**无 brands 清单、无来源规则、无自动标记**（多品牌共用一张表，需靠 `brands` 标注本期该报谁） |
| 到货排程引擎 | `server/domain/arrival_schedule.py`（157 行纯函数） | `compute_arrivals(mode, first_dom, cadence_days, weekdays, year, month)` → 返回本月到货日期列表；**只算到货日，不算报单日** |
| 调度器 | `server/scheduler.py`（1135 行） | daemon 线程 `_loop()`，`time.sleep(60)` → **1 分钟粒度**；`_run_per_tenant()` 遍历全部租户，单租户异常不影响其他；`start()` 有进程内单例保护（仅 `server.py __main__` 调用，单进程） |
| 库内定时任务 | `cron_tasks` 表 + `erp_db.cron_get_due()` | 按 `run_time == 当前 HH:MM` 匹配；freq 仅 daily / **weekly 硬编码周一** / **monthly 硬编码 1 号** / custom（仅校验分+时）。**无法表达「指定某月某日执行一次」** |
| 业务常量 | `server/routers/forecast_audit.py:30-31` | `DEFAULT_LEAD_DAYS = 4`（提前 4 天下单）、`DEFAULT_ARRIVAL_CYCLE = 2`（每 2 天到货）→ **报单日 = 到货日 − 4** |
| 创建/关单接口 | `server/routers/forecast.py:78/86` | `POST /periods`（`forecast_period_create`，双写旧列兼容 v94 前旧表）、`POST /periods/{pid}/close`（`forecast_period_close` 仅改 status） |
| RBAC | `server/server.py:311 _PATH_MODULE_MAP` | `/api/forecast`→data、`/api/rebate-rules`→sales **均已登记**，新增子路径自动继承，无需再登记 |

---

## 2. 可行性结论与主要风险点

### 2.1 风险点清单（你点名的 4 个 + 我额外识别出 7 个）

#### R1【已解除·原阻塞】多品牌 vs 无品牌维度的报单表

`rebate_target_rules` 按品牌存到货配置（`scope_key` = 品牌名），但 `forecast_periods` **没有 brand 字段**，是租户级全局表。原以为这是阻塞项——若蒙牛（每 2 天）和简爱（周二/周五报单）各自开启自动创建，会产出两套报单表并存，导致小程序 `forecast_open_periods()` 与 Web `forecast_period_current()` 选中哪个不确定。

**2026-09-08 用户确认：现状就是蒙牛 + 简爱共用一张报单表，靠人工提醒"。** 因此：

- ✅ **方案 1（租户级单序列）成立，方案 2（品牌隔离）作废** —— 与「经销商为单位下单」口径一致
- ⚠️ 风险**转化**为：**一张表上如何标出「本期该报哪几个品牌」**。若不做，业务员仍然要靠记忆判断这期有没有简爱 —— **自动化只做了一半，痛点没解决**
- ⚠️ 新增约束：`auto_period_enabled = 1` 的规则**必须能读到同租户下其他品牌的到货配置**，才能算出品牌清单

**解法**：见 §4.4「本期应报品牌清单」——把品牌节奏**吸附**到主序列报单日上，落库 `forecast_periods.brands`，并在创建时推送提醒。

#### R2【高】跨周期推算断裂 —— 现有「月内日」语义不适合做序列

现有 `arrival_first_dom` 存的是**月内日（1–31）**，`compute_arrivals()` **每月从该日重新起算，不跨月累加**。

举例：`first_dom = 31, cadence = 2`
- 8 月 → `[8/31]`（1 次）
- 9 月 → `[9/1, 9/3, ... 9/29]`（15 次）

**8/31 → 9/1 只隔 1 天，不是 2 天** —— 跨月断裂。这个行为对「按月算均单」无害（现有引擎已接受），但用来驱动「报单表序列」会导致期次间隔忽长忽短、甚至重叠。

**解法**：新增的「首次报单日」存**绝对日期锚点**（`2026-09-01`），序列按 `锚点 + k × 周期` 连续累加、跨月不重置。用 `timedelta` 自然处理月末/闰年。

**副作用**：与现有 `arrival_first_dom`（月内日）形成两套口径，必须在 UI 写清楚——
- `arrival_first_dom`（月内日）→ 仍用于**均单试算**（按月窗口，与返利目标周期对齐）
- `order_first_date`（绝对日期）→ 用于**报单表序列**创建/关单

#### R3【高】重复创建或漏创建

- **重复**：scheduler 每分钟跑一次；`_should_run()` 的节流状态 `_monitors_run` 存在**内存**里，**服务重启即清空** → 重启后当天可能重复执行 → 重复建表
- **漏**：服务停机（部署/崩溃）跨过触发点 → **永久漏掉，无任何补跑机制**（`cron_get_due()` 只匹配当前 HH:MM，不做追赶）

**解法**：新建持久化执行台账表 `forecast_period_jobs`，把「计划」与「执行」分离（详见 §4、§6）。

#### R4【✅ 已消除】关单时点早于创建时点

> **2026-09-08 更新**：你确认的真实时间规则（**前一日 20:00 开放 / 当日 10:00 截止**）**天然消除了本风险**。

原担忧基于「前一天 12:00 创建、当天 12:00 关单」的假设，此时 `cadence=1` 会出现两个时点相等。改用真实时点后：

| 场景 | 计算 | 结果 |
|---|---|---|
| cadence = 1（每天报单） | `T_close(k) = D_k 10:00`，`T_open(k+1) = (D_{k+1} − 1) 20:00 = D_k 20:00` | ✅ **相差 10 小时**，绝不冲突 |
| 任意 cadence ≥ 1 | `T_close(k)` 恒在 `D_k` 上午，`T_open(k+1)` 恒在 `D_{k+1} − 1` 晚上 | ✅ 天然串行，无需人工干预 |
| lead_days 很大（如 10 天） | 到货日 `A_k = D_k + 10` 可能晚于 `D_{k+2}` | 到货日在途交叉属**业务常态**，不算错误，但 UI 需提示 |
| 首次到货日 = 1 号 | 报单日 = 上月 27/28 号 | 首次报单日跨到上月，序列锚点必须允许落在过去 |

**结论**：开放与截止**分属不同时段**（晚 vs 上午），间隔 ≥ 10 小时，本风险**降级为"仅需在配置保存时做一次 `T_close < T_supplier` 校验"即可**。

**保留的解法（防御性）**：生成 job 时仍校验 `T_open < T_close < T_supplier`；极端自定义配置（如把开放设成 09:00、截止设成 08:00）下 → close 提前 1 分钟 + `skipped` 记录原因。

#### R5【高·已踩过】自动产出大量 open 期次，污染默认期次选取

2026-09-07 刚修过一个 bug：`forecast_period_current()` 原用 `ORDER BY id DESC`，选中了「id 最大但窗口已过」的期次，导致你在 Web 端看不到小程序刚报的数据。

**自动创建会批量产出期次**，一旦关单失败就会堆积 open 期次 → **同一个 bug 换条路复发**。

**解法**：① 自动关单的可靠性优先级高于自动创建（关单失败要告警）；② 调度内加一条「卫生检查」：每天早上扫描超过 N 天仍未关闭的自动期次，强制关闭并通知。

#### R6【低】时间与历法边界

- `datetime.now()` 取服务器本地时间；日期以 `YYYY-MM-DD` 字符串比较 → 统一用 date 对象运算，勿字符串比较
- 2 月闰年、31 号遇小月：用 `timedelta` 进位（`8/31 + 2天 = 9/2`），不走「每月固定日」语义
- 服务器时区变更 / 夏令时：中国无 DST，风险低，但 `auto_open_time` / `auto_close_time` / `supplier_deadline_time` 存字符串需固定 `HH:MM` 格式
- **跨夜窗口的日期归属**：`T_open` 落在 `D_k − 1` 晚上，若该日期跨月/跨年（如 D_k = 1 号 → 开放日是上月最后一天），job 的 `period_key`、`due_at` 必须用 **datetime 运算**而非字符串拼接，否则 12/31 → 01/01 会算错

#### R7【中】手动/自动期次共存

生产上现已有手工创建的期次（id=8 `08-28`、id=9 `08-30~09-15`、id=10 `09-03`）。自动创建的会与它们并存甚至重叠。

**解法**：`forecast_periods` 加 `auto_created` / `source_rule_id` 标记，查询与展示时可区分；开启开关时提供「并入已有期次 / 从下一期开始」两种选择（详见 §5）。

#### R8【高·设计已改】品牌匹配方向错误 —— 不能延后，只能提前

**初版设计（已废弃）**：把慢节奏品牌（简爱）的报单日「吸附」到**之后**最近的主报单日，即**延后归集**。

**为什么错**：你的口径是「过了品牌方报单日期就只能下期再报」——延后 = 作废，不是"晚一天报"，是**这一期直接报不了**。同时「提前太久准确性低」也排除了往前随便吸附。

**改后的正确模型（§4.4）**：品牌方给的是**截止日 `C_X`**，报单日必须落在窗口 `[C_X − E_X, C_X]` 内：

- `D_k > C_X` → **已过期，本期不能报**（不是延后归集）→ 记为漏报，告警
- `D_k < C_X − E_X` → 太早，报单不准 → 不归本期，等更晚的期次
- 窗口内多个 `D_k` 命中 → **取最大的**（最靠近截止日，提前量最小，准确性最高）

#### R9【高】漏报 —— 品牌截止日没有任何报单表可用

这是 R8 改模型后浮现的**新核心风险**：若主序列太稀疏，某品牌的截止日可能整个滑过窗口，导致**这一期该品牌彻底没得报**。

**已用真实节奏量化验证**（9–10 月简爱周二/周五，共 18 个截止日）：

| 主序列 cadence | 可提前 E=0 | E=1 | E=2 |
|---|---|---|---|
| **2**（蒙牛现状） | ❌ 漏 9 次 | ✅ **0 漏** | ✅ 0 漏 |
| 3 | ❌ 漏 12 | ❌ 漏 6 | ✅ 0 漏 |
| 4 | ❌ 漏 13 | ❌ 漏 9 | ❌ 漏 5 |
| 5 | ❌ 漏 14 | ❌ 漏 11 | ❌ 漏 7 |

→ 得出**硬性充分条件**：**`cadence_main ≤ min(E_X) + 1`** 时零漏报（窗口长 `E+1` 天，间隔不超过它则必然命中）。

**你的现状**（cadence=2、E=1 已确认）：`2 ≤ 1 + 1` ✅ **满足，零漏报**。注意 E=2 时容错更大（cadence 到 3 仍零漏报），但会让系统提前 2 天建表、数据偏旧 —— 已按你的选择取 **E=1**。

**四层缓解**：
1. **配置校验前置**：保存时若 `cadence_main > min(E)+1` → **阻断开启**并给出具体日期清单
2. **相位寻优**：自动遍历 `D₀ ~ D₀ + cadence_main − 1` 各相位，推荐漏报最少的首次报单日（实测能避免 1 次/月漏报）
3. **每日扫描**：已过截止日但仍未归集的品牌 → 告警「简爱 9/5 截止日已过，本期未报，将顺延至下期」
4. **人工补开**：提供「为本期临时补开一张表」入口，`auto_created=0` 标记，不污染自动序列

#### R10【高】管理员重开后，又被自动关单立即关掉

**场景**：业务员 10:30 发现漏报 → 管理员 10:35 重开 → 调度器下一分钟扫到该期次的 `close` job 仍是 `pending` 且 `due_at`（10:00）已过 → **立刻又关掉**，重开形同虚设。

**缓解**（三重）：
1. 重开动作**同步把对应的 close job 置 `skipped`**，原因写明「管理员重开，自动关单让位」
2. close job 执行前检查 `reopen_count > 0 AND last_reopen_at > job.created_at` → 直接 skipped（双保险，防 job 被重新生成）
3. 若管理员指定了新截止时间 → 生成 `period_key = 报单日#reopen{N}` 的新 close job（加后缀避开唯一索引冲突）

#### R11【中】跨夜窗口导致相邻期次「日期」重叠（cadence=1）

**场景**：窗口改为 `order_start = D_k − 1` 后，`cadence = 1` 时：

```
期次 k−1：order_start = 8/31，order_end = 9/1     （窗口 8/31 20:00 ~ 9/1 10:00）
期次 k  ：order_start = 9/1， order_end = 9/2     （窗口 9/1 20:00 ~ 9/2 10:00）
                       ↑ 9/1 同属两期
```

- **时刻口径**：✅ 不重叠（k−1 期 9/1 10:00 已关，k 期 9/1 20:00 才开，中间空 10 小时）
- **日期口径**：❌ 重叠 —— `forecast_period_current()` 用 `order_start <= today <= order_end` 会在 9/1 命中两条

**这正是 2026-09-07 刚修过的 bug（默认期次选错导致看不到数据）换条路复发。**

**缓解**：
1. `forecast_period_current()` 改用**时刻**判定：取 `open_at <= now <= close_at` 中 `close_at` 最小者；无则回退"最近已截止的一期"
2. 配置保存时对 `cadence = 1` 显式提示「每日报单模式下相邻期次日期会重叠，已按精确时刻判定，无需处理」
3. 你的现状 `cadence = 2` ✅ 不受影响

### 2.2 风险矩阵

| 风险 | 概率 | 影响 | 等级 |  mitigation |
|---|---|---|---|---|
| R1 多品牌冲突 | — | — | ✅ **已解除** | 业务确认共用一张表 → 方案 1；残留风险转为 R8 |
| R2 跨周期断裂 | 高 | 中 | 🟠 | 绝对日期锚点 + timedelta 连续累加 |
| R3 重复/漏创建 | 中 | 高 | 🔴 | 持久化 job 台账 + 唯一索引 + due_at 补偿扫描 |
| R4 时序冲突 | 中 | 中 | ✅ **已消除** | 真实时点（前一日 20:00 开 / 当日 10:00 关）间隔 10h，天然串行；仅保留配置校验做防御 |
| R5 open 期次污染 | 中 | 高 | 🔴 | 关单优先 + 每日卫生检查强制关闭 |
| R6 历法边界 | 低 | 低 | 🟡 | date 对象运算 + 单测覆盖 |
| R7 手动自动共存 | 高 | 中 | 🟠 | 标记字段 + 开启时的并入选择 |
| ~~R8 吸附延后~~ | — | — | ✅ **设计已废** | 业务口径「只能提前不能延后」→ 重写为下方 R8 |
| **R8 匹配方向错误** | 高 | 高 | 🔴 | 改为**提前窗口匹配**：`D_k ∈ [C_X − E_X, C_X]`，窗口内取最大（提前最少）；`D_k > C_X` 判作废不归集 |
| **R9 品牌漏报** | 中 | 高 | 🔴 | ① 校验 `cadence_main ≤ min(E)+1`（当前 2≤2 ✅）；② 相位寻优推荐首次报单日；③ 每日扫过期未归集告警；④ 人工补开入口 |
| **R10 重开后又被自动关** | 高 | 高 | 🔴 | 重开即置 close job `skipped` + 执行前查 `last_reopen_at` 双保险 + 新截止时间用 `#reopenN` 后缀避开唯一索引 |
| **R11 跨夜窗口日期重叠** | 中 | 中 | 🟠 | `forecast_period_current()` 改用 `open_at/close_at` **时刻**判定（cadence=1 才会触发，你 cadence=2 不受影响） |

---

## 3. 数据字段与存储结构

### 3.1 三方关联关系

```
rebate_target_rules（品牌目标·配置源）
    │  id  ──────────┐
    │  scope_key     │  source_rule_id（溯源，多对一）
    │  order_first_date / order_lead_days / order_max_early_days / auto_*（新增）
    │                │
    │                ▼
    │        forecast_periods（报单表·执行结果）
    │            id, order_start, order_end, arrival_date
    │            source_rule_id, auto_created, brands, seq_no（新增）
    │                ▲
    │                │  period_id（执行回填）
    │                │
    └────────►  forecast_period_jobs（定时任务·执行台账）【新建】
                    rule_id, period_key, job_type, due_at
                    status, period_id, attempts, last_error
```

**关联规则**：
- 一条品牌目标规则（`rebate_target_rules.id`）→ 一个报单日 → 一个期次 → 两条 job（create / close）
- `forecast_periods.source_rule_id` 指向来源规则，`seq_no` 记录是第几期（幂等键之一）
- `forecast_period_jobs` 是**唯一驱动源**：调度器只扫这张表，不直接扫规则表

### 3.2 `rebate_target_rules` 新增 10 列

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `order_mode` | TEXT | `'interval'` | **报单排程模式**：`interval`=按间隔天数、`weekday`=按固定星期。**租户可配**（简爱=weekday） |
| `order_cadence_days` | INTEGER | `2` | `interval` 模式的间隔天数（蒙牛=2） |
| `order_weekdays` | TEXT | `''` | `weekday` 模式的报单星期，逗号分隔 `0=周一…6=周日`。**简爱 = `1,4`（周二/周五）** |
| `order_first_date` | TEXT | `''` | **首次报单日**（绝对日期 `YYYY-MM-DD`，序列锚点）。**相位决定漏报率**，保存时做寻优（§4.4.7） |
| `order_lead_days` | INTEGER | `4` | 报单日到到货日的提前天数（默认取 `DEFAULT_LEAD_DAYS=4`）。**到货日由此派生，不单独配置** |
| `order_max_early_days` | INTEGER | **`1`** | **该品牌最多可提前几天报单**（§4.4 窗口下界）。**本次确认默认 1**；业务上限 2；**下限由校验保证 ≥ `cadence_main − 1`** |
| `auto_period_enabled` | INTEGER | `0` | **智能开关**（0=手动，1=自动）。同一租户**最多一条为 1** |
| `auto_open_time` | TEXT | **`'20:00'`** | **自动开放时刻**（`HH:MM`），执行于报单日**前一日**（你的现状：前一天晚上就能报） |
| `auto_close_time` | TEXT | **`'10:00'`** | **自动关单时刻**（`HH:MM`），执行于报单日**当天上午**（你的现状：10 点截止） |
| `supplier_deadline_time` | TEXT | **`'12:00'`** | **厂家系统下单截止时刻**（`HH:MM`），执行于报单日当天。**仅推送提醒 + 逾期告警，不自动下单**（红线：厂家系统无开放接口、不托管客户账号） |

> ⚠️ **语义约定**：以上 `order_*` 全部是**报单**语义（用户直接配"我周几报单"），与既有 `arrival_*`（**到货**语义）区分。
> 到货日 = 报单日 + `order_lead_days`，**派生展示**，避免两处配置打架。老数据迁移见 §4.4.1。

> ⚠️ 迁移方式：走 `_safe_migrate()`（幂等，`ALTER TABLE ADD COLUMN` + duplicate column 容错），与现有 v116/v118/v119 同款写法。

### 3.3 `forecast_periods` 新增 10 列

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `source_rule_id` | INTEGER | `0` | 来源规则 ID（0 = 手工创建） |
| `auto_created` | INTEGER | `0` | 是否自动创建（仅标记，**不锁权限**，仍可手动关闭/编辑） |
| `brands` | TEXT | `''` | **本期应报品牌清单**（逗号分隔，如 `蒙牛,简爱`）。由 §4.4 匹配算法算出，**用于提醒与筛选，不作为隔离维度** |
| `seq_no` | INTEGER | `0` | 期次序号（第几期），与 `source_rule_id` 组成业务幂等键 |
| **`open_at`** | TEXT | `''` | **精确开放时刻**（`YYYY-MM-DD HH:MM`）= `T_open`。跨夜窗口下 `order_start` 只有日期、表达不了"晚上 20:00 才开"，必须存时刻 |
| **`close_at`** | TEXT | `''` | **精确截止时刻** = `T_close`。用于判定"现在还能不能报"（§4.3 R11） |
| **`reopen_count`** | INTEGER | `0` | 被管理员重开次数（0 = 从未重开） |
| **`last_reopen_at`** | TEXT | `''` | 最近一次重开时刻。自动关单 job 据此让位（R10） |
| **`last_reopen_by`** | INTEGER | `0` | 最近一次重开的操作人 `user_id` |
| **`overdue_flag`** | INTEGER | `0` | 重开时是否已过厂家下单截止（12:00）。1 = **补报需特殊报备**，UI 标红 |

> `order_start / order_end` **保持不变**（仍写 `D_k − 1` / `D_k`），供既有汇总查询、Web 交叉表、小程序期次选取继续使用；
> `open_at / close_at` 是**新增的精确时刻层**，只用于"当前能否填报"的判定。两者并存，不做破坏性改造。
>
> 📌 **可选增强（不计入本次 10 列）**：`supplier_ordered_at TEXT`（经理在厂家系统完成下单后手工点确认的时刻）。
> 有它就能把 §4.1 的 12:00 告警从"无条件提醒"升级为"**未确认才告警**"，避免已下单还天天告警。
> 建议 P5 前端一并做掉（一个按钮的事），也可留到上线后按需补。

### 3.4 新建 `forecast_period_jobs`（执行台账）

```sql
CREATE TABLE IF NOT EXISTS forecast_period_jobs (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    rule_id      INTEGER NOT NULL,          -- 来源规则
    period_key   TEXT NOT NULL,             -- 报单日 YYYY-MM-DD
    job_type     TEXT NOT NULL,             -- 'open' | 'close' | 'remind'
                                            --   open   = 前一日晚创建报单表
                                            --   close  = 当日上午关单
                                            --   remind = 当日中午厂家下单截止提醒（仅通知，不自动下单）
    due_at       TEXT NOT NULL,             -- 计划执行时刻 YYYY-MM-DD HH:MM
    status       TEXT NOT NULL DEFAULT 'pending',  -- pending|done|failed|skipped|expired
    period_id    INTEGER DEFAULT 0,         -- create 成功后回填；close 指向目标期次
    attempts     INTEGER DEFAULT 0,         -- 重试次数
    last_error   TEXT DEFAULT '',
    executed_at  TEXT DEFAULT '',
    created_at   TEXT DEFAULT (datetime('now','localtime'))
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_fpj_unique
    ON forecast_period_jobs(rule_id, period_key, job_type);   -- ★ 幂等的根本保证
CREATE INDEX IF NOT EXISTS idx_fpj_due
    ON forecast_period_jobs(status, due_at);
```

**为什么必须这张表**（`cron_tasks` 无法复用）：
- `cron_get_due()` 只匹配 **当前 HH:MM**，服务停机就永久漏掉，无法补偿
- 无法表达「某月某日执行一次」（weekly 硬编码周一、monthly 硬编码 1 号）
- 没有执行结果回写、没有重试计数

---

## 4. 触发逻辑与时间计算规则

### 4.1 核心公式

```
锚点      D₀        = order_first_date                    （首次报单日，绝对日期）

主序列（取「最密品牌」的报单日集合，模式随该品牌）：
  interval 模式 → D_k = D₀ + k × order_cadence_days       （k = 0,1,2,… 跨月连续）
  weekday  模式 → D   = { d ≥ D₀ | weekday(d) ∈ order_weekdays }
                      例：简爱 order_weekdays = "1,4" → 每周二、周五

第 k 期到货日 A_k   = D_k + order_lead_days                （派生，不单独配置）

三个时点（★ 2026-09-08 按你的真实流程确认，已取代初版「单日 12:00」设计）：
  开放时刻   T_open(k)     = (D_k − 1 天) @ auto_open_time      默认 20:00   ← 前一日晚上
  截止时刻   T_close(k)    =  D_k         @ auto_close_time     默认 10:00   ← 当日上午
  厂家下单截止 T_supplier(k)=  D_k         @ supplier_deadline_time 默认 12:00 ← 当日中午（仅提醒）

报单窗口    order_start = D_k − 1 天，order_end = D_k          （★ 跨夜两日）
精确窗口    open_at = T_open(k)，close_at = T_close(k)          （★ 新增时刻字段）
期次名      {brands} {M}月{D}日报单 · {AM}月{AD}日到货
```

**★ 为什么窗口从「单日」改成「跨夜」**：你的实际流程是**前一天晚上就开始报**，不是当天才开。按初版单日窗口，业务员晚上打开小程序会看到"本期未开放"，与现状不符。

实测你的节奏（cadence=2，首次报单日 9/1，lead=4）：

| 期次 | 开放（前一日 20:00） | 截止（当日 10:00） | 厂家下单截止（12:00） | 到货 |
|---|---|---|---|---|
| 第 1 期 | 08/31 20:00 | 09/01 10:00 | 09/01 12:00 | 09/05 |
| 第 2 期 | 09/02 20:00 | 09/03 10:00 | 09/03 12:00 | 09/07 |
| 第 3 期 | 09/04 20:00 | 09/05 10:00 | 09/05 12:00 | 09/09 |

> **填报窗口 14 小时**（跨夜）→ **改单/付款/到账窗口 2 小时**（10:00→12:00，留给经理和你）。
> 三个时点严格递增 `T_open < T_close < T_supplier` ✅，已用代码验证。

**⛔ 12:00 只提醒、不自动下单**：厂家系统无开放接口，且托管客户账号违反 ToS（与「不做服务端爬虫、不托管客户账号」红线一致）。系统在该时点做两件事：① 推送「请在 12:00 前完成厂家系统下单」；② 若该期次 `supplier_ordered_at` 为空 → 告警「本期已过厂家下单截止，需特殊报备或顺延下期」。

**为什么 `order_start/order_end` 仍需保留**：小程序提交时落库的 `order_date` 恒为 `order_start`（后端 `create_submission` 强制），现有汇总查询、Web 交叉表、小程序期次选取**全部依赖这两个日期列**。因此采用 **日期列兼容 + 时刻列精确**的双轨：`order_start/order_end` 承载跨夜两日供既有逻辑使用，`open_at/close_at` 承载精确时点供"当前能否填报"判定使用。

**到货排程（均单）与报单序列（建表）两套口径并存**：
- 均单试算：继续用 `arrival_first_dom`（月内日）按月窗口，与返利目标周期对齐 —— **不动**
- 报单表序列：用 `order_first_date`（绝对日期）连续累加 —— **新增**

### 4.2 计划（Planning）与执行（Execution）分离

**计划阶段**（每次调度先跑，幂等）：

```
1. 读 auto_period_enabled = 1 的规则（租户内最多一条）
2. 从 max(今天, D₀) 起，向后推导 N 天（建议 30 天）内的报单日序列 {D_k}
3. 对每个 D_k 生成 3 条 job，INSERT OR IGNORE（靠唯一索引幂等）
   - open    @ (D_k − 1天) @ auto_open_time            ← 前一日晚 20:00
   - close   @ D_k          @ auto_close_time           ← 当日上午 10:00
   - remind  @ D_k          @ supplier_deadline_time    ← 当日中午 12:00（仅提醒，不自动下单）
4. 生成前校验 T_open < T_close < T_supplier，违反 → status='skipped' + 记录原因
5. 同分钟冲突时（极端配置）：close 的 due_at 提前 1 分钟，remind 顺延 1 分钟
```

**执行阶段**（每分钟）：

```
扫 status='pending' AND due_at <= now() 的 job（★ 用 <= 而非 ==，天然补偿停机）
  → 同 due_at 的按 open → close → remind 顺序执行（串行）
  → open：查 forecast_periods 是否已存在 (source_rule_id, seq_no)
       存在 → skipped（幂等双保险）
       不存在 → forecast_period_create(order_start = D_k − 1 天, order_end = D_k,
                                       open_at = T_open, close_at = T_close)
                → 回填 period_id → done → 推通知「本期需报：蒙牛、简爱」
  → close：目标期次不存在 或 已 closed → skipped
       期次被管理员重开过（reopen_count > 0 且 last_reopen_at > 本 job 生成时间）→ skipped
         ★ 防止刚重开就被自动关单（见 §4.5 R10）
       否则 → forecast_period_close() → done
  → remind：仅推送「请在 12:00 前完成厂家系统下单」；
       若 supplier_ordered_at 为空 → 追加逾期告警
  → 异常：attempts += 1，<3 保持 pending（下一分钟重试），>=3 标 failed + 站内通知
```

### 4.3 六个边界的处理

| 边界 | 处理 |
|---|---|
| **周期与报单日冲突** | 生成 job 时校验 `T_open < T_close < T_supplier`；相等/逆序 → close 提前 1 分钟；仍冲突 → `skipped` + 原因。配置保存时校验 `auto_open_time` 属**前一日**、`auto_close_time` 与 `supplier_deadline_time` 属**当日**，天然跨日故不会误判；仍需校验 `close < supplier` |
| **跨周期推算** | 绝对锚点 + timedelta 连续累加，跨月不重置。若锚点在上月（如到货日=1号 → 报单日=上月27号），序列从首个 `D_k >= 今天` 开始建，不追溯补建 |
| **重复创建** | ① 唯一索引 `(rule_id, period_key, job_type)`；② 执行前再查 `forecast_periods(source_rule_id, seq_no)` 双重保险 |
| **关单早于创建** | close job 执行前若 `period_id = 0`（创建未完成）→ 保持 `pending` 顺延，不 skipped；超过 48h 标 `expired` + 告警 |
| **★ 跨夜窗口导致相邻期次日期重叠**（R11） | 窗口改为 `order_start = D_k − 1` 后，`cadence = 1` 时会出现 `order_start(k) == order_end(k−1)`（同一天归属两期）。实测：**时刻上不重叠**（k−1 期 10:00 已关 < k 期 20:00 才开），但**日期口径会重叠**。解法：`forecast_period_current()` 改用 `open_at/close_at` **时刻**判定（取「已开放且未截止」中 `close_at` 最小者）；保存配置时对 `cadence = 1` 提示风险 |
| **★ 管理员重开后又被自动关单**（R10） | 重开时把该期次对应的 `close` job 置 `skipped` 并记录原因「管理员重开，自动关单已让位」；如需再次自动关，由管理员在重开弹窗指定新截止时间 → 生成一条 `period_key = 报单日 + '#reopen' + N` 的新 close job（避开唯一索引冲突） |

### 4.4 本期应报品牌清单（替代人工提醒的核心算法）

**问题**：一张报单表上要报多个品牌，但各品牌节奏不同（蒙牛每 2 天、简爱周二/周五）。现在靠人记着「这期还得报简爱」——漏报全凭运气。

**约束（2026-09-08 你的口径，算法的公理）**：
- 报单**只能提前、不能延后** —— 过了品牌方报单日即作废，只能等下期
- 提前**最多 1–2 天** —— 提前太久，销量预测准确性会非常低

#### 4.4.1 配置锚点：以「报单日」为准，到货日由它派生

现有 `arrival_*` 一组字段是**到货**语义（`arrival_first_dom` 首次到货日、`arrival_weekdays` 到货星期）。若沿用它们，你要报「周二/周五报单」就得填「周四/周日到货」再让系统减 `lead_days` 反推——**反直觉且极易填错**。

**决定**：新增的一组字段采用**报单语义**，用户直接配「我周几报单」：

| 模式 | 用户配什么 | 你的实例 |
|---|---|---|
| `interval` 按间隔天数 | 首次报单日 + 间隔 N 天 | 蒙牛：9/1 起每 2 天 |
| `weekday` 按固定星期 | 每周哪几天报单（多选） | 简爱：**周二、周五** |

到货日 = 报单日 + `order_lead_days`（**派生展示，不单独配置**），避免两处配置打架。

> 迁移：老数据已填 `arrival_*` 的，提供一次性「从到货配置反推报单排程」按钮（`C = 到货日 − lead_days`），反推结果**必须弹窗让用户确认**再落库，不静默改写。

#### 4.4.2 时间语义

| 符号 | 含义 | 来源 |
|---|---|---|
| `C_X` | 品牌 X 的**报单截止日**（品牌方排产节点） | 用户配置的**报单排程**（见 §4.4.1） |
| `E_X` | 品牌 X 的**可提前天数** | `order_max_early_days`，**本次确认为 1** |
| `D_k` | 第 k 期报单表的报单日 | 主序列：interval → `D₀ + k × cadence`；weekday → 每周指定星期 |
| `W_X` | 品牌 X 的**有效窗口** `[C_X − E_X, C_X]` | 长度 `E_X + 1` 天（含端点） |

#### 4.4.3 匹配算法（提前窗口匹配 —— 不是延后吸附）

```
对每个品牌 X 的每个截止日 C：
    W = [C − E_X, C]
    H = { D_k | D_k ∈ W }          # 落在窗口内的主序列报单日

    if H 为空:
        → 漏报（R9），告警「X 的 9/5 截止日无可用报单表，将顺延下期」
    else:
        D* = max(H)                 # 取最靠近截止日的 → 提前量最小 → 准确性最高
        把品牌 X 归入 D* 那一期的 brands
```

**四个判定分支**：

| 条件 | 判定 | 处理 |
|---|---|---|
| `D_k ∈ [C − E, C]` 且是其中最大者 | ✅ **命中** | 归入该期 `brands` |
| 同一个 `C` 有多个 `D_k` 命中 | ✅ 取**最大**的 | 提前天数最少，数据最准 |
| `D_k > C` | ❌ **已过期** | **不归集**（延后即作废），记漏报告警 |
| `D_k < C − E` | ⏸ **太早** | 不归本期，留待更晚的期次 |

> ⚠️ 与初版「吸附」设计的**根本区别**：初版把 `D_{k-1} < d ≤ D_k` 的截止日**往后**归集（延后 1 天），
> 这在你的业务里等于**让品牌错过截止日**，是错误方向。新算法只往**前**取，绝不越界到 `C` 之后。

#### 4.4.4 用你的真实节奏验证（蒙牛每 2 天 + 简爱周二/周五，E=1）

主序列（蒙牛，首次报单日 9/1，cadence=2）：`9/1, 9/3, 9/5, 9/7, 9/9, 9/11, …`

| 简爱截止日 | 窗口 `[C−1, C]` | 命中报单日 | 提前 | 该期 brands |
|---|---|---|---|---|
| 9/1（周二） | 8/31 – 9/1 | 9/1 | 0 天 | 蒙牛 + 简爱 |
| 9/4（周五） | 9/3 – 9/4 | 9/3 | **1 天** | 蒙牛 + 简爱 |
| 9/8（周二） | 9/7 – 9/8 | 9/7 | **1 天** | 蒙牛 + 简爱 |
| 9/11（周五） | 9/10 – 9/11 | 9/11 | 0 天 | 蒙牛 + 简爱 |
| 9/15（周二） | 9/14 – 9/15 | 9/15 | 0 天 | 蒙牛 + 简爱 |
| 9/18（周五） | 9/17 – 9/18 | 9/17 | **1 天** | 蒙牛 + 简爱 |
| 9/22（周二） | 9/21 – 9/22 | 9/21 | **1 天** | 蒙牛 + 简爱 |
| 9/25（周五） | 9/24 – 9/25 | 9/25 | 0 天 | 蒙牛 + 简爱 |
| 9/29（周二） | 9/28 – 9/29 | 9/29 | 0 天 | 蒙牛 + 简爱 |

**9 月简爱零漏报，最大提前 1 天** —— 完全落在「提前 1–2 天」的准确区间内。

#### 4.4.5 主序列为什么也必须支持 weekday 模式

**场景**：某租户**没有** interval 品牌，只有 weekday 品牌（例如只有简爱，周二/周五报单）。若主序列只认 interval，这台租户就跑不起来。

**规则**：主序列 = **最密品牌的报单日集合**，模式跟随该品牌。

| 模式 | 等效间隔 | 说明 |
|---|---|---|
| `interval` | `order_cadence_days` | 天然均匀 |
| `weekday` | `7 ÷ len(order_weekdays)` | 周二/周五 → 3.5 天 |

取等效间隔最小者为主序列。你的实例：蒙牛 2 < 简爱 3.5 → 主序列 = 蒙牛 interval ✅

**极端验证**（租户只有周二/周五品牌，主序列即 weekday 集合）：

```
主序列 = 9/1周二, 9/4周五, 9/8周二, 9/11周五, 9/15周二, 9/18周五, …
```
该品牌自身截止日与主序列完全重合 → **漏报 0 次** ✅（已实测）。结论：**主序列生成必须 mode-agnostic，窗口匹配算法无需改动**（它只对一个「日期集合」做匹配）。

#### 4.4.6 零漏报的充分条件（写进配置校验）

> ##### `cadence_main ≤ min(E_X) + 1`

窗口长 `E+1` 天，主序列间隔不超过它 → 任意截止日必被某个 `D_k` 命中。

实测（9–10 月共 18 个简爱周二/周五截止日）：

| cadence_main | E=0 | E=1 | E=2 |
|---|---|---|---|
| **2**（你的现状） | ❌ 漏 9 次 | ✅ **0 漏** | ✅ 0 漏 |
| 3 | ❌ 漏 12 | ❌ 漏 6 | ✅ 0 漏 |
| 4 | ❌ 漏 13 | ❌ 漏 9 | ❌ 漏 5 |
| 5 | ❌ 漏 14 | ❌ 漏 11 | ❌ 漏 7 |

→ 实测完全印证规则：**只有 `cadence ≤ E + 1` 的格子才是 0 漏**（E=1 时仅 cad=2 合格；E=2 时 cad=2/3 合格）。

**你的现状 `2 ≤ 1 + 1` ✅ 满足，E=1 已确认采用** —— 不必用满 2 天（提前越少越准）。

> **推论（校验硬规则）**：`E_X` 不能小于 `cadence_main − 1`。
> 若把某品牌设成「完全不允许提前」（`E=0`）而主序列是 2 天 → **必然漏掉约一半的报单机会**，保存时直接阻断。

#### 4.4.7 相位寻优 —— 首次报单日不是随便填的

`D₀` 的**相位**直接决定命中率。同样 cadence=2，实测：

| 首次报单日 | 9–10 月简爱漏报 |
|---|---|
| 2026-09-01（周二） | **0 次** ✅ |
| 2026-09-02（周三） | 1 次（漏掉 9/1 那期）❌ |

**保存时自动执行**：遍历 `D₀` 起连续 `cadence_main` 个相位 → 各模拟未来 90 天 → 取漏报最少者。若优于用户填的值，提示「建议改为 9/1（周二），可避免 1 次漏报」。用户坚持原值可强制保存，但需二次确认。

> 注：漏 9/1 是因为主序列起点 9/2 已晚于该截止日，**序列起点之前的截止日天然无法命中**。故相位寻优还要额外校验：**首次报单日不得晚于同租户最早的品牌截止日**，否则提示「建议提前到 9/1 或更早」。

#### 4.4.8 落库与触达

1. 创建报单表时写入 `brands`，期次名自动带上：`9月7日报单 · 蒙牛+简爱`
2. 创建同时推站内 + 企业微信通知：「本期报单表已开，需报：**蒙牛、简爱**（简爱本周第 2 次，提前 1 天，9/8 到货）」
3. Web 端期次行显示品牌标签 + 提前天数；小程序报单页顶部「本期需报：蒙牛、简爱」
4. 每日扫描：截止日已过但未归集的品牌 → 告警「简爱 9/5 已过截止日，本期未报，将顺延下期」
5. 兜底：手动模式下 `brands` 计算仍可用 —— 建表时点「按节奏填充品牌」即可

**这份清单是当前人工提醒的直接替代物，也是本次改造 ROI 最高的部分。**
---

### 4.5 管理员「再次开单」（重开）—— 给错过的人留的补报通道

> 你的原话：「如果谁错过了，要找管理员点击开单才能报，所以要给管理员留这个再次开单的权限。」

#### 4.5.1 业务语义

自动关单（报单日 10:00）**不是铁板一块** —— 业务员漏报是常态，必须留人工补救口。重开的本质是：**把已截止的期次重新打开，允许指定的人补报**，代价是本期的厂家下单大概率已过 12:00 → **需走特殊报备**。

| 阶段 | 状态 | 谁能报 |
|---|---|---|
| T_open ~ T_close（前一日 20:00 → 当日 10:00） | `open` | 所有人 |
| T_close 之后 | `closed` | 无人 |
| **管理员重开后** | `open`（`reopen_count ≥ 1`） | 所有人（但已过厂家截止，见下） |

#### 4.5.2 权限设计

```
POST /api/forecast/periods/{pid}/reopen
  body: { "reason": "...", "close_at": "2026-09-03 15:00"（可选，新的截止时间） }

鉴权：role ∈ {admin, boss}          ← 只给管理员/老板，不给 supervisor/员工
     （与已下线的审批端点角色集一致：admin/boss/accountant/supervisor，但重开口子更敏感，
       收敛到 admin/boss 两级，避免督导随意放人补报冲乱汇总）
RBAC：/api/forecast 已登记 → data 模块，新增子路径自动继承（server.py _PATH_MODULE_MAP）
```

**为什么不给 supervisor**：重开会改变本期汇总数字，而经理（你）可能已按 10:00 的数字向厂家付过款。补报进来的量属于"计划外"，只有老板能拍板。

#### 4.5.3 重开时系统做什么

```
1. 校验：期次存在 且 status = 'closed' 且 调用者 role ∈ {admin, boss}
2. 作废自动关单：把该期次的 close job 置 skipped，原因「管理员重开」
3. 更新期次：
   status          = 'open'
   close_at        = 管理员指定时间；未指定则默认「当前时间 + 2 小时」
   reopen_count    += 1
   last_reopen_at  = now()
   last_reopen_by  = 当前 user_id
   overdue_flag    = (now() > T_supplier) ? 1 : 0      ★ 是否已过厂家下单截止
4. 若管理员指定了新 close_at → 生成一条新 close job（period_key 加 #reopenN 后缀，避开唯一索引）
5. 记审计：谁、何时、为什么重开（写 audit_log，与现有 log_workflow_advice 同款）
6. 通知：站内 + 企微推给经理「XX 期次已被 {管理员} 重开，原因：{reason}，
         {'⚠️ 已过 12:00 厂家下单截止，补报部分需特殊报备' if overdue_flag else '仍在厂家截止前，请尽快下单'}」
```

#### 4.5.4 关键设计决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 重开后是否再自动关？ | **可选**。默认不再自动关（交给管理员手动关）；管理员可指定新截止时间 | 补报场景不可预测，自动再关容易二次漏人 |
| 重开次数上限 | **不硬限制**，但 `reopen_count ≥ 3` 时告警 | 频繁重开说明节奏配置有问题，应调配置而非反复开单 |
| 重开后是否影响汇总？ | **是，立即计入** | 汇总只排除 `rejected/recalled`，`status` 回到 open 即计入 —— 与既有口径一致，无需改 SQL |
| 已过 12:00 的重开 | **允许，但打 `overdue_flag = 1`** 并在 UI 标红「需特殊报备」 | 你的原话：错过就只能走特殊报备或等下期。系统如实标记，不假装正常 |
| 幂等 | 重开是**人为动作**，不做幂等去重（同一期次可多次重开），但**每次都记审计** | 可追溯 |

#### 4.5.5 前端落点

- `Forecast.vue` 期次行：已截止的期次出现「**重新开单**」按钮（仅 admin/boss 可见）
- 点击弹出：原因（必填）+ 新的截止时间（可选，默认 +2 小时）
- 期次行显示徽标：`已重开 N 次`；若 `overdue_flag = 1` 追加红色 `需特殊报备`
- 小程序侧：重开后业务员刷新即可看到该期次恢复可报（顶部提示「本期已重新开放，请尽快补报」）

---

## 5. 数据来源与依赖链路

| 依赖项 | 来源 | 现状 | 兜底策略 |
|---|---|---|---|
| 到货周期 | `rebate_target_rules.arrival_cadence_days` | ✅ 已有 | 为空取 `DEFAULT_ARRIVAL_CYCLE = 2` |
| ~到货模式（间隔/星期）~ | ~~`arrival_mode` / `arrival_weekdays`~~ | ⚠️ **语义改用报单侧** | 已由新增的 `order_mode` / `order_weekdays` 取代（§4.4.1）。**两种模式都必须支持**：主序列与品牌截止日均需支持 `weekday`，否则周报两次的品牌算不出来 |
| **报单星期（可配）** | `order_weekdays`（新增） | ❌ **缺失** | **必须做成 UI 星期多选器**。用户明确要求「其它租户可能不一样，要让用户自己配置」，简爱 = 周二、周五 |
| 首次到货日 | `arrival_first_dom`（月内日） | ⚠️ 语义不同 | 新增 `order_first_date` 绝对锚点；为空时用 `首次到货日 − lead_days` 反推并提示用户确认 |
| 提前天数 | `forecast_audit.DEFAULT_LEAD_DAYS = 4`（硬编码） | ⚠️ 未落库 | 新增 `order_lead_days` 列，默认写入 4 |
| **每品牌可提前天数 `E_X`** | 无（新增） | ❌ **缺失** | 新增 `order_max_early_days`，默认 2，业务上限 2；**下限 `cadence_main − 1` 由校验强制** |
| **每品牌报单截止日 `C_X`** | 由 `到货日 − order_lead_days` 反推 | ⚠️ 需推导 | 纯计算，无需存储；`order_weekdays` 模式（简爱周二/周五）**必须支持**，否则算不出截止日 |
| 各品牌到货节奏 | 同租户下**所有** `is_active=1` 且配了到货排程的规则 | ✅ 已有 | 至少要有一条；只有一条时 `brands` 恒为该品牌 |
| 品牌名 | `scope_key` / `scope_name` | ✅ 已有 | 参与 §4.4 计算后写入 `forecast_periods.brands` |
| 期次名 | 手工输入 | ⚠️ 自动需规则 | `{brand} {M}月{D}日报单 · {AM}月{AD}日到货` |
| 租户遍历 | `scheduler._tenant_ids()` + `set_tenant_context()` | ✅ 已有 | 直接复用 `_run_per_tenant()` |
| 通知通道 | `server/notify/` / 站内通知 | ✅ 已有 | 失败告警复用 |
| ~~品牌隔离维度~~ | ~~`forecast_periods.brand`~~ | ✅ **不需要** | 已确认多品牌共用一张表，改为冗余 `brands` 清单字段（不做隔离） |
| 通知通道（品牌提醒） | `server/notify/` + 站内通知 | ✅ 已有 | 复用；失败不影响建表 |

**现有数据能否支撑**：**算法与调度层完全能支撑；数据层缺 3 类（报单日锚点、提前天数落库、执行台账），需按 §3 补齐。**

品牌清单（§4.4）**不需要新增数据源**——同租户下各品牌的 `arrival_*` + `order_lead_days` 已足够，属于「用现有数据算出新价值」，是本方案性价比最高的一块。

---

## 6. 手动模式与自动模式兼容方案

### 6.1 方案选择（✅ 已确认为方案 1）

> **2026-09-08 更新：方案 2 已作废。** 用户确认「蒙牛和简爱现在就是在一张报单表上」，报单表不分品牌是**业务现状而非妥协**。方案 1 由「推荐」升为「已确认」。

| | **方案 1 ✅ 已确认 租户级单序列** | ~~方案 2 品牌级多序列（已作废）~~ |
|---|---|---|
| 报单表 | 租户级唯一序列，不分品牌 | `forecast_periods` 加品牌隔离维度，各品牌独立出表 |
| 开关位置 | 挂在其中一条规则上（租户内唯一） | 每个品牌各自开关 |
| 改动面 | 品牌目标 +10 列、报单表 +10 列、新建 1 表、调度 +1 monitor、接口 +3 端点、前端 +1 组表单 + 重开按钮 | 方案 1 全部 + Web 交叉表、小程序期次选取、汇总口径、审核台全部要按品牌分流 |
| 工作量 | 约 2–3 天 | 约 1.5–2 周，回归面大 |
| 与既有口径 | ✅ 与「经销商为单位一起采购」自洽 | ❌ 与「经销商为单位」口径相悖，等于按品牌拆单 |
| 推荐度 | ★★★★★ **已选定** | —（作废） |

**结论：方案 1。** 报单表的本质是「一次向厂家下单的收集窗口」——你向厂家下单是经销商对厂家，不是蒙牛一个单、简爱一个单；业务员打开小程序只需一张表报完所有品牌，这跟不按门店拆采购单是同一个道理。2026-09-08 已由业务现状直接验证。

**多品牌节奏差异不靠拆表解决，靠 §4.4「本期应报品牌清单」解决**——一张表照常开，但系统明确告诉你这一期要报蒙牛、还是蒙牛加简爱。

### 6.2 开关中途切换的处理

| 场景 | 行为 |
|---|---|
| **关 → 开** | ① 不追溯历史，只从 `max(今天, D₀)` 之后推导（避免批量补建过期期次）<br>② 若当天已有 open 期次 → 弹窗二选一：**「并入自动管理」**（回填 `source_rule_id`/`seq_no`，后续由自动关单接管）或 **「从下一期开始」**（该期次保持手动）<br>③ 校验租户内是否已有其他规则开启 → 自动将其置 0 并提示 |
| **开 → 关** | ① 所有 `pending` job 置 `skipped`（不删除，留审计）<br>② 已创建的期次**保留不动**，转为手动管理（不自动关闭、不自动删除） |
| **配置修改**（改周期/报单日/时刻） | ① 重新推导序列<br>② 已生成的 `pending` job 按新序列重算 `due_at`（`done` 的不动）<br>③ **已创建的期次不删改**，仅影响后续未生成的期次 |
| **规则删除 / 停用** | 同「开 → 关」，并提示已创建的 N 个期次将保留 |
| **自动期次手动干预** | 自动创建的期次**允许手动关闭/编辑**（`auto_created` 仅标记不锁权限）；手动关闭后 close job 执行时发现已 closed → `skipped` |
| **自动创建失败** | 不阻塞手动创建，UI 提示「自动创建失败（原因），可手动创建本期报单表」 |

---

## 7. 实施步骤

| Phase | 内容 | 产出 | 验证 |
|---|---|---|---|
| **P0 决策** | 你拍板方案 1 / 方案 2 | 书面确认 | — |
| **P1 数据层** | `_safe_migrate()` 加 20 列（`rebate_target_rules` +10、`forecast_periods` +10）+ 建 `forecast_period_jobs` 表及 2 个索引 | 迁移脚本 | 本地建库 + 重复执行两次验证幂等 |
| **P2 算法层** | `arrival_schedule.py` 新增 ① `compute_order_dates(first_date, cadence, lead_days, horizon_days)` → `[{order_date, arrival_date, seq}]`；② `compute_brand_cutoffs(rule, horizon_days)` → 每品牌截止日 `[C]`（**支持 interval 与 weekday 两种模式**）；③ `match_brands_to_periods(main_dates, brand_cutoffs)` → `[{order_date, brands[], early_days}]`（§4.4 **提前窗口匹配**）；④ `suggest_first_date(...)` 相位寻优 | 纯函数 | **单测覆盖**：跨月（8/31→9/2）、闰年 2/29、月末 31→小月、cadence=1/31 边界；**用真实节奏断言**（蒙牛 2 天 + 简爱周二/周五覆盖 9 月全月 → 断言漏报 = 0、最大提前 = 1 天）；**反例断言**（cadence=3/E=1 → 必须检出 6 次漏报并阻断；cadence=4/E=2 → 必须检出 5 次） |
| **P3 调度层** | `scheduler.py` 新增 `_plan_forecast_periods()` + `_exec_forecast_period_jobs()`，注册进 `_loop()`（60s 节流）。**每期生成 3 条 job**：`open` / `close` / `remind`（§4.2） | monitor | 沙箱模拟：连续 90 天序列 → 断言 job 数 = 期次数 × 3、无重叠、无遗漏、`T_open < T_close < T_supplier` 恒成立 |
| **P4 接口层** | `rebate_rules.py`：保存时校验（见 §7.1，含 **`cadence_main ≤ min(E)+1`** 与**相位寻优推荐**）；新增 `GET /auto-period/preview`（预览未来 6 期，含每期 brands 与提前天数） | 2 个端点 | RBAC 已登记 `/api/rebate-rules`→sales，带真实登录态测 |
| **P4b 重开接口** | `forecast.py` 新增 `POST /periods/{pid}/reopen`（**仅 admin/boss**）+ `erp_db.forecast_period_reopen()`：作废 close job → 置 open → 写 `reopen_count/at/by/overdue_flag` → 审计 → 通知（§4.5） | 1 端点 + 1 函数 | 单测：① 重开后对应 close job 变 `skipped`；② 模拟调度器下一分钟**不会**再把该期次关掉；③ 非 admin/boss 调用返回 403 |
| **P4c 时刻判定改造** | `erp_db.forecast_period_current()` 改用 `open_at/close_at` **时刻**判定（防 R11 跨夜日期重叠）；老期次无时刻值 → 回退原日期口径 | 1 函数 | 回归：生产现有 3 个期次（id=8/9/10）选取结果与改造前一致（09-07 刚修过的 bug 不能再犯） |
| **P5 前端** | ① `Rebate.vue` 到货模式区改为**报单排程区**：模式切换 `interval / weekday` + **星期多选器**（周一至周日，简爱勾周二/周五）+ 首次报单日 (date) + 提前天数 (number，默认 1) + 「智能创建/关闭报单表」(switch) + 时间配置 + **未来 6 期预览表**（含每期 brands 与提前天数）+ **相位建议提示条**；② `Forecast.vue` 期次行显示品牌标签 + 提前天数 + **开放/截止时刻**（如 `08/31 20:00 ~ 09/01 10:00`）+ **「重新开单」按钮**（仅 admin/boss 可见，弹窗填原因 + 可选新截止时间）+ `已重开 N 次` / `需特殊报备` 徽标；③ 小程序报单页顶部「本期需报：蒙牛、简爱」+ 重开后的「本期已重新开放，请尽快补报」提示 | UI | 构建通过 + 真机冒烟（重点：切到 weekday 能勾周二/周五；预览表 brands 与 §4.4.4 实测表逐行一致；**非 admin 账号看不到「重新开单」按钮**） |
| **P6 灰度** | 先上线「只生成 job 不执行」（`status` 落 `pending` 但执行器关闭），观察 3 天 job 生成是否正确 | 观察 | 人工核对 job 台账 |
| **P7 全量** | 打开执行器，你手动关闭 id=8/10 两个过期 open 期次 | 上线 | 端到端：等一个完整周期验证创建 + 关单 |

### 7.1 异常处理与幂等保证

**配置校验（保存时阻断，非运行时才发现）**
- `arrival_cadence_days` ∈ [1, 31]；`order_lead_days` ∈ [0, 30]；`order_max_early_days` ∈ [0, 2]（业务上限）
- `order_first_date` 非空且 `YYYY-MM-DD` 合法
- `auto_open_time` / `auto_close_time` / `supplier_deadline_time` 格式 `HH:MM` 合法
- 🟠 **时点顺序**：`auto_close_time < supplier_deadline_time`（二者同属报单日当天，直接比）；
  `auto_open_time` 属**前一日**，跨日比较须 `+1 天` 后再比 → 保证 `T_open < T_close < T_supplier`
  （你的默认值 20:00 / 10:00 / 12:00 经代码验证 ✅ 严格递增）
- 🟠 **重开期限**：`periods/{pid}/reopen` 仅对 `status = 'closed'` 生效；`overdue_flag` 由系统判定，不接受前端传入
- 租户内 `auto_period_enabled = 1` 最多一条
- 🔴 **零漏报硬规则**：`cadence_main ≤ min(order_max_early_days) + 1`，否则阻断并列出会漏报的具体日期（§4.4.6）
- 🔴 **提前量下限**：`order_max_early_days ≥ cadence_main − 1`，否则「不允许提前」与主序列周期矛盾，必然漏报
- 🟠 **相位推荐（不阻断）**：模拟未来 90 天，若换一个首次报单日可减少漏报 → 提示建议值，用户可坚持原值（二次确认）

**任务调度失败补偿**
- 扫描条件用 `due_at <= now()`（不是 `==` 当前分钟）→ **服务停机 3 小时重启后，错过的 job 仍会执行**
- 每个 job 最多重试 3 次（`attempts`），超次 → `failed` + 站内通知
- job 逾期超过 **48 小时** → 标 `expired` 并告警，**不静默补建**（避免补出一个早已过期的报单表）

**幂等三道锁**
1. 唯一索引 `(rule_id, period_key, job_type)` + `INSERT OR IGNORE`
2. 执行前查 `forecast_periods(source_rule_id, seq_no)` 双重保险
3. close job 执行前校验目标期次状态，已 closed → `skipped`

**Fail-safe**
- 连续 3 期 create 失败 → **自动把 `auto_period_enabled` 置 0** 并通知，避免持续产出脏数据
- 每天早班加一条卫生检查：扫描 `auto_created=1 AND status='open' AND order_start < 今天-N天` 的期次 → 强制关闭 + 通知（防 R5 复发）

**前端兜底**
- 所有自动相关操作失败均有 toast 明确原因（参照 2026-09-07 修「提交无反应」的教训：**新分支一律加外层 catch + toast**）

---

## 8. 需要你拍板的事项

1. ~~**方案 1 还是方案 2？**~~ ✅ **已确认：方案 1**（你已确认蒙牛和简爱共用一张报单表）
2. ~~**延后容忍度**~~ ✅ **问题作废**：你已明确「只能提前不能延后」，原吸附设计废弃，算法改为提前窗口匹配（§4.4.3）
3. ~~**简爱的报单截止日是哪几天？**~~ ✅ **已确认：周二、周五**，且**必须做成可配置**（其它租户各不相同）→ 已落地为 `order_mode` + `order_weekdays` + UI 星期多选器
4. ~~**`order_max_early_days` 默认取 1 还是 2？**~~ ✅ **已确认：1**。实测你的 cadence=2 满足 `2 ≤ 1+1`，9 月零漏报且最大只提前 1 天，报单最准
5. ~~**weekday 模式要不要支持自动建表？**~~ ✅ **已确认：必须支持，且主序列也要支持**（§4.4.5）—— 否则「只有周报品牌」的租户跑不起来
6. ~~**老数据的 `arrival_*`（到货排程）怎么处理？**~~ ✅ **已确认：按建议** —— 保留字段不动，提供一次性「从到货配置反推报单排程」按钮，反推结果**弹窗让用户确认**再落库，**不静默改写**
7. ~~**报单窗口用单日还是跨夜？**~~ ✅ **已确认：跨夜** —— 报单日**前一日 20:00 开放**、**当日 10:00 截止**，`order_start = D_k − 1`、`order_end = D_k`（§4.1）。初版单日设计作废
8. ~~**自动关单时刻？**~~ ✅ **已确认：报单日上午 10:00**（`auto_close_time = '10:00'`）
9. ~~**12:00 厂家下单截止要不要自动化？**~~ ✅ **已确认：不自动化，只提醒** —— 厂家系统无开放接口 + 不托管客户账号（红线）。落地为 `remind` job + 逾期告警（§4.1）
10. ~~**错过的人怎么办？**~~ ✅ **已确认：管理员可「再次开单」** —— 新增 `POST /periods/{pid}/reopen`（仅 admin/boss），重开后打 `overdue_flag` 标记「需特殊报备」（§4.5）
11. 🟠 **自动关单的兜底天数 N**（建议 3 天：超过 3 天未关闭的自动期次强制关闭并通知）—— **仅影响人工未干预的异常期次，重开过的期次不受此规则强制关闭**
12. 🟠 **开放时刻默认 20:00 是否合适？** 你说的是「前一天晚上」，我按 20:00 取值。若实际是 18:00 或 21:00，改配置即可（已做成可配）

---

> **✅ 至此所有阻塞性决策已闭环，可以开工。** 剩余 11、12 两项为参数级默认值，不阻塞开发，可在 P5 前端联调时一并确认。

---

## 9. 实施进度（2026-09-08 开工，分两批）

### 9.1 本批已完成：配置 + 预览闭环（P1 / P2 / P4 / P5）

**刻意不接调度器（P3）**：调度器会写 `forecast_periods`（生产数据）。先让算法在预览里被你核对一遍，
确认节奏算对再让它动期次表 —— 否则就是 R5「open 期次污染」那个坑换个入口复发。

| Phase | 内容 | 落地位置 | 状态 |
|---|---|---|---|
| **P1 数据层** | `rebate_target_rules` +10 列（两处迁移：tenant patch 元组 + `v121_*` 脚本） | `erp_db.py:653-665`、`erp_db.py:1110-1129` | ✅ |
| **P2 算法层** | 4 个纯函数：`effective_interval` / `compute_order_dates` / `match_brands_to_periods` / `build_period_plan` + `suggest_first_date`（相位寻优） | `domain/arrival_schedule.py` | ✅ |
| **P4 接口层** | 保存校验（含**开关租户内唯一**、三时点顺序、可提前上限、日期/时刻格式）；`GET /auto-period-preview` | `routers/rebate_rules.py` | ✅ |
| **P4a 白名单** | `_row_to_dict` 补 10 字段 —— 它是**白名单函数**，不补前端编辑时读不回已存配置 | `routers/rebate_rules.py:236-256` | ✅ |
| **P5 前端** | 「智能报单表」配置面板：手动/自动 + 报单模式(间隔/星期) + 首次报单日 + 提前天数 + 可提前天数 + 三个时刻(time) + 未来 6 期预览表 + 漏报告警 + 相位建议「采纳」 | `hergent-cn-v2/src/pages/Rebate.vue` | ✅ |

### 9.2 本批实测结论（真实节奏，非推演）

配置：蒙牛 interval 2 天（首日 9/1）+ 简爱 weekday 周二/周五 + 可提前 1 天 + 开放 20:00 / 关单 10:00 / 厂家截止 12:00

- **主序列 = 蒙牛**（等效间隔 2.0 < 简爱 3.5）✅
- **填报窗口 14 小时**（9/8 20:00 → 9/9 10:00），关单到厂家截止留 **2 小时** ✅
- **零漏报**（horizon 内），简爱提前 0~1 天，与 §4.4.4 实测表逐行吻合
- **校验拦截实测 8/8 通过**：重复开启开关 / 关单晚于厂家截止 / 可提前超上限 2 / 日期 2026-02-30 / weekday 不选星期 / 时刻 25:70 …
- **反例生效**：`E=0` 检出 9 次漏报、`cadence=3 & E=1` 检出 6 次，与 §4.4.6 矩阵一致 → 证明校验不是摆设

### 9.3 过程中修掉的两个自身 bug

1. **校验误报**：`min(E)` 把主序列品牌自己算进去了。主序列报单日就是它的截止日（early 恒 0），
   算进 `min(E)` 会让「蒙牛 E=0」必然触发「必有品牌漏报」的假告警 → 改为**只统计非主序列品牌**。
2. **相位寻优只往后找**：截止日落在序列起点**之前**时（9/2 起的序列命中不了 9/1 的简爱），
   往后挪永远救不回来 → 改为**双向遍历** `[-6,+6]`，实测能推荐 9/2 → 9/1（1 漏报 → 0 漏报）。

### 9.4 剩余（下一批）

| Phase | 内容 | 依赖 |
|---|---|---|
| **P3 调度层** | `scheduler.py` 新增计划/执行两个 monitor + 建 `forecast_period_jobs` 台账（唯一索引幂等 + `due_at <= now` 补偿停机） | 本批预览被你核对通过 |
| **P4b 重开** | `POST /api/forecast/periods/{pid}/reopen`（仅 admin/boss）+ `reopen_count/last_reopen_at/overdue_flag` | P3 |
| **P4c 时刻判定** | `forecast_period_current()` 改用 `open_at/close_at` 时刻判定（R11：跨夜窗口致日期重叠） | P3 |
| **P6 数据层补** | `forecast_periods` +10 列、`arrival_* → order_*` 一次性反推迁移按钮 | P3 |
| **P7 触达** | 建表通知（站内 + 企微）、Web 期次行品牌标签、小程序顶部「本期需报：X、Y」 | P3 |

---

## 附：关键代码位置索引

| 用途 | 文件:行 |
|---|---|
| 品牌目标表建表 | `server/erp_db.py:520`（租户）、`:995`（主库） |
| 到货字段迁移 | `server/erp_db.py:1078-1094`（v116/v118/v119） |
| 报单表建表 | `server/erp_db.py:928` |
| 创建期次 | `server/erp_db.py:13812` `forecast_period_create` |
| 关闭期次 | `server/erp_db.py:13948` `forecast_period_close` |
| 默认期次选取 | `server/erp_db.py` `forecast_period_current`（2026-09-07 已修为「今天在窗口内」优先） |
| 到货排程引擎 | `server/domain/arrival_schedule.py` |
| 提前期常量 | `server/routers/forecast_audit.py:30-31` |
| 调度主循环 | `server/scheduler.py:25` `_loop()` |
| 多租户遍历 | `server/scheduler.py:228` `_run_per_tenant()` |
| 节流函数 | `server/scheduler.py:167` `_should_run()` |
| cron 现有实现 | `server/erp_db.py:13513` `cron_get_due()` |
| 期次接口 | `server/routers/forecast.py:78/86` |
| RBAC 映射 | `server/server.py:311`（`/api/forecast`→data、`/api/rebate-rules`→sales） |
| 前端表单 | `hergent-cn-v2/src/pages/Rebate.vue:653-680`（到货模式区块） |
