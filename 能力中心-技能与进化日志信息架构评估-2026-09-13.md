# 能力中心 ·「技能」与「进化日志」信息架构评估

> 基于源码 `src/pages/ConnectCenter.vue`（能力中心 = `ConnectCenter.vue`，含 连接器 / 专家 / 技能 / 进化日志 四个 tab）

## 一、现状核查（关键事实）

| 元素 | 「技能」tab | 「进化日志」tab | 是否重复 |
|---|---|---|---|
| **工作流**（封装经营活、一键跑通 + 管理） | ✅ 有（行 285-316） | ❌ 无 | 不重复 |
| **技能库**（AI 技能清单，复用同一份 `aiSkills`） | ✅ 有（「AI 技能库」，行 232-283） | ✅ 有（「技能库（AI 的行业经验）」，行 26-38） | **重复** |

**核查结论：**
- 字面「工作流」**仅出现在「技能」tab**，进化日志 tab 源码中并无「工作流」入口。
- **真正重复的是「技能库」**：同一份数据在「技能」tab 与「进化日志」tab 各画了一遍。两张卡片都是"图标 + 中文名 + 能干嘛"的网格样式，非技术用户极易将「技能库」与「工作流」混为一体，这大概率就是观察到的"重复展示"。
- 若你在**线上**确实看到「工作流」出现在进化日志，则属旧版部署未同步（源码当前状态并非如此），可截图定位后精准清理。

## 二、两页定位对比

| 维度 | 「技能」tab | 「进化日志」tab |
|---|---|---|
| **核心定位** | 能力目录 / 能力货架（"AI 现在能干什么"） | 透明化 / 信任背书（"AI 在持续变聪明"） |
| **交互性质** | **主动、可操作**（点工作流→跑流程、开关工作流） | **被动、只读**（看统计、看进化记录） |
| **目标用户** | 老板 / 业务员，想"用起来" | 老板 / 客户，想"确认它在进化"（含产品演示） |
| **主要场景** | 浏览可用能力、开通停用工作流、了解 AI 能干嘛 | 看最近改了什么、给客户演示"自进化"差异化 |

## 三、工作流归属分析

工作流 = 把高频经营活封装、一键跑通的可操作能力，本质是"能力货架"上的商品。

**应归属「技能」tab，理由：**
1. 它与「AI 技能库」同属"能力清单"，且需要"去使用 / 联系开通"的动作，与「技能」tab 的**可操作**定位一致。
2. 「进化日志」tab 是只读的"变化记录"，放一个要点击运行的入口，**定位冲突**——用户来这里是看"变了什么"，不是"去跑一个流程"。
3. 源码事实也印证：工作流本就只在「技能」tab，无需移动。

**结论：工作流只留在「技能」tab，不进「进化日志」。**

## 四、真正要修的重复：技能库（两页都展示）

**根因**：「进化日志」tab 把完整技能库又画了一遍，但本页定位是"变化记录"，不是"完整目录"。

**建议方案（推荐 A）：**

> **A（推荐）：进化日志移除内嵌技能库，改为引导跳转**
> - 进化日志只保留：**统计** + **进化记录**。
> - 在统计区加一行轻引导：「已沉淀 N 个行业技能 · 去技能页查看 →」（点击切到「技能」tab）。
> - 效果：两页职责彻底分离——技能页＝可操作能力目录，进化日志＝只读透明化记录；重复展示消除，且进化日志更聚焦"信任故事"。

> **B（不推荐）：若坚持在进化日志展示技能，则只展示"本页变化涉及的技能"**
> - 即只列近期进化新增 / 改动的技能，而非全量目录。需后端按进化记录回查技能，成本高且仍可能与技能页重叠。

## 五、最终建议（一句话版）

- **工作流**：仅保留在「技能」tab（本就在那，勿移到进化日志）。
- **技能库**：从「进化日志」tab 移除全量展示，改为「去技能页 →」引导跳转（方案 A）。
- 这样两页各司其职，重复展示消失，信息架构清晰。

## 六、可立即执行项

- 是否按**方案 A** 改 `ConnectCenter.vue`：① 删除进化日志 tab 内 `ev-skills` 区块（行 26-38）；② 在统计区加引导跳转文案（带 `tab='skill'` 切换）；③ 同步移除 `skillMeta` 在该处的引用。
- 若你观察到的"重复展示"是别处（命令面板 / 旧版部署 / 其他页面），请截图或指出具体位置，我再精准定位。
