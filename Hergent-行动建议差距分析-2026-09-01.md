# Hergent 行动建议 × 代码现状差距分析

> 日期：2026-09-01　|　方法：逐条阅读真实代码（本地 `/Users/zhangjunfeng/Documents/hergent-erp/server` + 生产 `root@47.113.224.140`）
> 对照基准：《Hergent-未来路径备忘-2026-09-01.md》第六章"行动建议（按价值排序）"

---

## 一、对照表

| # | 建议项 | 当前状态 | 代码证据（文件:行 / 关键位置） | 待办工作 | 优先级 |
|---|---|---|---|---|---|
| **1** | **加厚配方层**（货损/返利/预报/工资 从 MVP → 可演示行业深度） | **部分满足** | 后端 4 套配方引擎齐备：货损 `routers/loss_workflow.py:23-30` DEFAULT_RECIPE（threshold_days/pricing/near_loss_pct/expired_coefficient）+ `:175/:192` GET·PUT /recipe + `workflow_recipes` 表（`:42`）；工资 `routers/payroll_workflow.py:21,45,82`（commission_rate/base_salary/performance/bonus/allowance/tax_standard_deduction）；预报 `routers/forecast_config.py:64,:71` GET·PUT /recipe + `:346` P10-7 配方行业模板库 + `:961` P16-8 配方市场；返利 `erp_db.py` 规则引擎（rebate_target_rules 阶梯/on_target/brand 维度 + scale_type/rounding_mode + `rebate_tier_versions` 版本链）。行业配方包 4 个在生产：`/root/.hermes/skills/hergent-milk-{expiry,forecast,commission,rebate}/SKILL.md`（43/38/37/38 行，含 FEFO、过期系数、分级处置、阶梯返利等真实 know-how） | ①**配方参数 ↔ Skill 提示词口径同步**：Skill 内口径为静态文本（如 expiry 写"默认 7 天/成本价/1.0"），改后端配方后 AI 说法与计算结果会漂移；4 个 Skill 中仅 expiry、commission 提及"配方"，rebate、forecast 完全未引用。需改造成模板变量注入 `{{threshold_days}}` 等，改配方时同步渲染。②**配方模板库/市场 从 forecast 扩展到其余 3 个**（当前仅 `forecast_config.py:346/:961` 有）。③**本地 Skill 同步**：本地 `~/.hermes/skills/` 只有 3 个通用包（architecture/collections/erp-tools），无 4 个行业包，开发机无法调试 | **高** |
| **2** | **守住服务形态**（每日简报 / 微信推送 / AI 主动提醒） | **部分满足** | 调度与主动提醒齐备：`scheduler.py:104-106` 每天 8:05 副驾经营卡简报 `_push_copilot_cards_brief()`（定义 `:657`，聚合货损/工资/返利三卡）；`:107-109` 9:00/16:00 预报催单；`:111-113` 每周一档案健康检查；`:212` 低库存、`:230` 逾期应收、`:265` 费用异常、`:282` 沉睡客户、`:248` 用户 cron 任务、`:300` 晨报 `_generate_morning_report`。推送通道：`notify/wecom_notify.py:63 send_wecom`（webhook+重试+日志+历史）、`feishu_notify.py` | ①**多租户简报未实现**（商业化阻塞）：`scheduler.py:662` 代码自注"按默认租户上下文聚合（单租户/demo 行为，**多租户需另循环**）"，当前简报只能服务单一默认租户。需遍历租户 + 各自推送目标。②**微信/小程序订阅消息推送缺失**：全库仅 `routers/forecast_submissions.py:163` 一处 `ai_source="miniprogram:forecast"` 标记，无真实订阅消息下发（企微 webhook 是群机器人，触达不到个人微信）。经销商主战场在微信，这是触达缺口 | **高** |
| **3** | **数据接入普适化**（Excel/CSV + 舟谱模板 + 金蝶/用友连接器） | **部分满足** | Excel/CSV 摄入真实可用：`routers/import_router.py:442` preview、`:530` execute、`:900` progress、`:918/:952` template、`:1156` smart-recognize、`:1168` smart-parse、`:1274` one-shot、`:1128` 供应商对账单。舟谱模板真实（逆向自真实样本）：`routers/forecast.py:390` 注释"逆向自用户真实的舟谱导入模板（2026-08 样本）"、`:671` 确定性生成、`:723` POST /zhoupu-template。金蝶连接器真实：`kingdee_connector.py:66-153` OAuth 授权/换票/刷新/存储、`:190` KingdeeClient（products/customers/orders/inventory/vouchers 等 13 类对象 + 分页）、`:316/:332` 字段映射。畅捷通：`chanjet_connector.py` + 探路 PoC `laozhangai-product/hermes-mcp-chanjet/`（README+2 config+119 行 setup） | ①**解耦层仍是桩（架构债）**：`datasource_adapter.py:122-130` `_PlannedAdapter` 所有方法 `raise NotImplementedError("尚未启用（规划中）")`；`:148/:153` KingdeeAdapter、ExcelUploadAdapter 均继承该桩；`:167` `_ENABLED={"local":True,"kingdee":False,"excel":False}`。即"不造 ERP、只做 AI 层"的 DataSourceAdapter 抽象已固化，但 excel/kingdee 两条通道**未接线**——实际数据仍走 import_router 直写本地表，违背解耦初衷。需复用现有 import_router + kingdee_connector 实现两个 Adapter 并启用。②**用友连接器未实现**：全库仅 `datasource_adapter.py:11` 注释提及"金蝶/用友连接器直读（规划中）"，无 `yonyou_connector.py`，需从零构建且须先做可行性探路（用友开放平台文档/授权模式未知，参考舟谱教训：无公开自助 API 时连接器路线可能不成立） | **中**（Excel/金蝶接线）<br>**低**（用友，需先探路） |
| **4** | **可选：公开 MCP server**（如"低温奶货损分析"，放豆包/WorkBuddy 技能市场获客） | **未实现** | 仅有**内部** MCP server 先例：生产 `/opt/hergent-mcp-spreadsheet/server.py`（表格对账，Hermes 内部调用；本地副本 `workspace-tmp/hergent-mcp-spreadsheet-server.py`，193 行）。后端侧只有调用方 `spreadsheet_query.py`、`routers/chat_attachment.py` 提及 MCP。**无对外公开的 MCP server 包**，`hermes-mcp-chanjet` 仅为 2026-08-19 技术探路产物（非生产可用） | 可复用 spreadsheet MCP 范式（stdio server + tool 注册）做一个对外 MCP server，暴露 1-2 个轻量只读能力（如货损测算）。需解决：鉴权/多租户隔离、能力边界（**只暴露计算口径，不暴露配方全文，防 know-how 泄露**）、以及"工具化无收入"的定位认知——仅作获客 | **低** |

---

## 二、最关键的前置工作（3 点）

1. **多租户简报循环（P0，商业化硬阻塞）**——当前 8:05 简报按默认租户聚合，代码已自曝"多租户需另循环"。不修，第二个客户就推不出简报，服务形态无法变现。
2. **配方参数 ↔ Skill 口径同步（P0，护城河质量）**——后端配方与 AI 提示词是两套独立文本，改配方后 AI 会说一套、算一套。这是"行业深度"能否被信任的根基。
3. **DataSourceAdapter 去桩（P1，架构正确性）**——excel/kingdee 两通道为 NotImplementedError，实际仍直写本地表；不接线则"只做 AI 层、不造 ERP"的战略在代码层面尚未成立，多客户接入每家都要改代码。

---

## 三、建议执行顺序

| 步骤 | 事项 | 范围与依赖 | 是否从零构建 | 阻塞/风险 |
|---|---|---|---|---|
| **Step 1** | 多租户简报循环 | 改 `scheduler.py:_push_copilot_cards_brief`（`:657`）加租户遍历；依赖 `datasource_store`/租户上下文切换 + 各租户 webhook 配置 | 否（改造） | 推送风暴与限流；需为每租户配置推送目标，配置缺失租户要 fail-closed 跳过 |
| **Step 2** | 配方口径同步 | 4 个 Skill 提示词模板化（参数占位符注入）；依赖后端 recipe 读取接口（已具备 GET /recipe） | 否（改造） | 提示词改写可能影响既有回答质量，须回归验证；Skill 在生产机，改动需走部署流程 |
| **Step 3** | DataSourceAdapter 去桩 | 复用 `import_router.py`（Excel）+ `kingdee_connector.py`（金蝶）实现 ExcelUploadAdapter/KingdeeAdapter，置 `_ENABLED=True` | 否（接线，非从零） | 现有 import 直写本地表逻辑要并存，改造需保证幂等与回滚；误改会波及已上线的导入功能 |
| **Step 4** | 用友连接器 | 需先可行性探路（开放平台/授权模式），再决定是否实现 | **是**（且可能不可行） | 参考舟谱教训：无公开自助 API 时连接器路线不成立，建议先探路再投入 |
| **Step 5** | 对外公开 MCP server | 复用 `/opt/hergent-mcp-spreadsheet/server.py` 范式，暴露 1-2 个只读计算能力 | 否（复制范式） | 安全/鉴权/多租户隔离；**暴露配方全文 = 泄露 know-how**，须只暴露计算不暴露规则；且工具化本身无收入，仅获客 |

**一句话**：先修「多租户简报」解锁变现，再修「口径同步」夯实护城河，随后「Adapter 去桩」让架构名副其实；用友与对外 MCP 后置，均需先探路或明确其非收入定位。
