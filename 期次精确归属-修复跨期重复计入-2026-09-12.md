# 期次精确归属 — 修复跨期重复计入（2026-09-12）

> 用户决策 **3 做**：汇总口径从「纯日期区间」改为「期次号优先 + 日期兜底」。

## 一、问题

汇总查询（交叉表、编辑网格、定稿审核）只按 `order_date BETWEEN start AND end` 统计。
而**期次窗口可以重叠**：

| 期次 | 窗口 |
|---|---|
| 9「9月提审期-开放填报」 | 2026-08-30 ~ 2026-09-15 |
| 10「9月3日报单9月8日到货」 | 2026-09-03 |

9/3 同时落在两个窗口 → 那天报的单在**两个期次视图各算一遍**，数字看着是对的，只是被放大。
用户以后建「整月期 + 单日期次」是常态操作，这个错很难自己发现。

## 二、改法与依据

复用仓库里**已有的同款口径**（`routers/forecast_config.py::accuracy` 的 A7 双口径），保持多处统计一致：

```sql
(s.period_id = ? OR (s.period_id = 0 AND s.order_date BETWEEN ? AND ?))
```

- **新数据**（有期次号）→ 按 `period_id` 精确命中，不跨期
- **历史数据**（`period_id=0`，tenant_1 共 60 条 Excel 导入）→ 回退日期窗口，不丢
- **不传 `period_id`** → 行为与改动前**完全一致**（向后兼容）

### 改动清单（5 处）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `erp_db.py::forecast_submission_summary` | 新增 `period_id=0` 参数；双口径条件；只给 `period_id` 时自动补期次窗口 |
| 2 | `routers/forecast_submissions.py::/summary` | 新增 `period_id: int = 0` 查询参数并透传 |
| 3 | `routers/forecast_audit.py::/audit-period` | 读 `body.period_id` 并透传（定稿流与汇总表同口径） |
| 4 | `api/modules.js::forecastApproveApi.summary` | 新增第 4 参 `periodId`，拼 `&period_id=` |
| 5 | `pages/Forecast.vue`（5 处 summary + 1 处 auditPeriod） | 传 `p.id / base.id / pp.id / yoy.id` |

`all_units`（客户列名册）、`forecast_audit_decisions` / `forecast_extra_qty`（按 `period_start/period_end` 日期键）**未动** —— 定稿数据键不变。

## 三、🔴 顺带修掉一个"侥幸正确"的隐患

`summary_params = list(date_params) + decision_params` —— 但 SQL 里 **`decision_join` 的 ON 条件在 `FROM/JOIN` 段，位于 `WHERE` 之前**，所以参数顺序应当是 decision 在前。

改之前两者值恰好都是 `[start, end]`，顺序写反也侥幸正确；`date_params` 变 3 个后错位暴露 → 带 `period_id` 查询返回 **0 行**（活体测试抓到）。已修正为 `list(decision_params) + list(date_params)` 并加注释。

> 结论：这个接口的日期查询此前一直靠"参数值相同"掩盖着顺序 bug，属隐性脆弱点。

## 四、验证证据

### 1）对照实验（生产库副本，零风险）

构造一笔 `qty=7、period_id=10、order_date=2026-09-03` 的重叠报单：

| 口径 | 期次 9 | 期次 10 | 合计 |
|---|---|---|---|
| 旧（纯日期） | 11 | 7 | **18（重复 7）** |
| 新（双口径） | **4** | 7 | **11（准确）** |

用**完整 SQL**（含 `decision_join` + 3 参数 `date_cond`，即真实拼接顺序）复跑 → `PASS 无重复计入 + 归属正确`。

### 2）活体接口（tenant 1，主管账号）

| 调用方式 | rows | 总件数 |
|---|---|---|
| 不带 `period_id`（旧行为） | 4 | 4 |
| 带 `period_id=9` | 4 | 4 |
| 只传 `period_id=9`（后端自补窗口） | 4 | 4 |

当前无重叠数据，三者应一致 —— 实测一致 ✅。

### 3）定稿流 + 前端

- `POST /api/forecast-audit/audit-period` 带 `period_id` → `success: true`（demo boss 账号；`supervisor` 返回 403 属既有 stock 模块权限，与本次无关）
- 前端构建 `Forecast-Ck9cJS7j.js` / `modules-DlXuIktL.js`，产物含 `r.push("period_id="+i)`；已 rsync 上线，无旧 chunk 残留
- 浏览器冒烟：`hergent.cn` demo 登录进 `#/forecast` 正常渲染、**0 console 错误**
- 后端 `health HTTP 200`

## 五、部署记录

- 后端：`scp` 具名文件到 `/opt/hergent-erp/` 根（**未用 `deploy.sh`**，其 `--delete` 会删 Hermes 运行时目录）→ `chown` → `systemctl restart` → 落点 `grep` 验证（`summary_params` 修正行在位、无残留 `server/` 目录）
- 前端：`npm run build` → `rsync -a --delete dist/` → `chown -R hergent:hergent`

## 六、遗留

1. **首页卡片 `datasource_adapter.py` 的「本期待报 TOP3」**仍按 `order_date BETWEEN` —— 用途不同（首页趋势卡，按日分组），未改；若将来出现重叠期次的 TOP3 偏差，按同款双口径修。
2. `save-matrix` 的期次解析按 `order_start+order_end+status='open'` 匹配，**窗口完全相同的两个期次**会取 id 最大者 —— 可接受，未改。
3. 幂等键 `(user_id, store_id, order_date)` 不含 `period_id`（同 `order_start` 的两期次会互相覆盖）—— 本次未动。
