# 全站硬编码主题色同类问题排查与修复报告

**日期**：2026-08-04 ｜ **版本**：v107.9（commit 5f39b96，bundle main-DDgJZa2x.js）
**问题类型**：组件硬编码浅色主题值（`white`/`#fff`/`#f5f5f7`/`#fafafa`/`gray-*`），深色主题下翻车——与 v107.8 修复的 `.pf-*` 同类

---

## 一、排查范围（全站）

| 扫描对象 | 范围 | 结果 |
|---|---|---|
| `styles.css` | 全部 159 处 `var(--black)/var(--white)/gray-*/rgba(white)` | 多数为品牌按钮/遮罩/打印等**合理用法**，已逐一甄别 |
| `js/modules/*.js` | 全部模块 inline style 硬编码色 | **发现 2 个问题模块** |
| `app.js` | inline style 白底 | 全部为打印/分享/二维码/签名画布场景（**合理保留**） |
| `index.html` / `mobile.html` | 内联样式 | 无同类问题 |

## 二、发现并修复的问题（2 个模块，19 处）

### 1. expiry-dashboard.js — 效期红绿灯看板（8 处）

| # | 位置 | 原值 | 修复值 | 问题 |
|---|---|---|---|---|
| 1 | `buildSummaryCard` 卡片 | `background:white` | `var(--bg2)` | 深色主题白块 |
| 2 | `buildMetricCard` 卡片 | `background:white` | `var(--bg2)` | 同上 |
| 3 | 两处表头 | `background:var(--white)` | `var(--bg3)` | 深色主题白表头 |
| 4 | 两处行分隔 | `border-bottom:1px solid var(--white)` | `var(--bd2)` | 深色主题白线 |
| 5 | 临期预警条 | `#fef3c7` 硬编码浅黄 | `var(--pb)` | 深色主题刺眼亮黄块 |
| 6 | 筛选按钮（静态+动态 2 处） | `#f3f4f6`/`#6b7280` | `var(--bg3)`/`var(--t2)` | 深色主题灰底白字 |
| 7 | 空态/标题/建议文字 | `gray-400/500/700` | `var(--t3)`/`var(--t1)` | 深色主题对比度低 |
| 8 | 标签文字 | `var(--gray-500)` | `var(--t3)` | 同上 |

**保留合理**：数字语义色 `#dc2626`（红色数值）/`#6b7280`（正常值灰）——数值本身颜色，双主题语义一致。

### 2. module-compare.js — 模块对比（11 处样式注入）

| # | 元素 | 原值 | 修复值 |
|---|---|---|---|
| 1 | `.mc-wrap` 容器 | `#f5f5f7`/`#1d1d1f` | `var(--bg)`/`var(--t1)` |
| 2 | `.mc-badge` 徽章 | `#fff` | `var(--bg3)` |
| 3 | `.mc-table` 表格 | `#fff` | `var(--bg3)` |
| 4 | 表头/首列背景 | `#fafafa` | `var(--bg4)` |
| 5 | 表头/首列文字 | `#86868b` | `var(--t3)` |
| 6 | 副标题/徽章小字 | `#6e6e73` | `var(--t2)` |
| 7 | 总结文字 | `#333` | `var(--t1)` |
| 8 | 边框 | `#e5e5e7`/`#f2f2f4`/`#eee` | `var(--bd)`/`var(--bd2)` |

**保留合理**：品牌强调色 `#06b6d4`（品牌青）/`#10b981`（绿）——双主题下均清晰可见。

## 三、合理保留清单（非问题，不修复）

| 位置 | 场景 | 理由 |
|---|---|---|
| `app.js:1465` 配送单打印 | 打印 | 打印必须白底 |
| `app.js:2941` showPrintPreview | 打印预览 | 同上 |
| `app.js:3462` 二维码弹窗 | 扫码 | 二维码需白底 |
| `app.js:6650` 签名画布 | 手写签名 | 画布需白底 |
| `styles.css` 品牌按钮/渐变头像 | 品牌色底白字 | 双主题正确 |

## 四、验证结果

| 验证项 | 结果 |
|---|---|
| `node --check`（2 个模块） | ✅ 通过 |
| expiry-dashboard 硬编码残留 | ✅ 0 |
| module-compare 硬编码残留 | ✅ 0（仅注释提及设计说明） |
| Vite 构建 | ✅ main-DDgJZa2x.js |
| 线上部署 | ✅ 首页 200 + 新 bundle 200 |
| 线上 bundle 含 token 化逻辑 | ✅ 确认 |

## 五、回归说明

- **行为一致性**：修复后效期看板、模块对比在浅色/深色主题下表现一致（卡片/表头/按钮融入页面，文字对比度 WCAG AA 4.5:1+）
- **无功能影响**：仅样式令牌替换，无逻辑改动
- 验证方式：进入效期看板/模块对比页 → `applyTheme('dark')` 切换 → 检查卡片/表格/文字可读性
