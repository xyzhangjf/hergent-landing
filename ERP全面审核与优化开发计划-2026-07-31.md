# Hergent ERP 全面审核与优化开发计划

> 2026-07-31 | 审核基线：commit `5c04e20`（v90.1 H4.3 + 图标收编 P0）| 审核人：WorkBuddy（高级开发专家）
> 定位前提：蒙牛低温奶经销商自用验证 → 商业化卖给同类经销商；Web + 手机端 + 小程序三端

---

## 〇、现状盘点（基于代码实查，非估算）

| 维度 | 现状 |
|---|---|
| 后端 | FastAPI 单体，63 个 router，751 条 API，274 个 RBAC 映射键；`server.py` 入口 + `erp_db.py`（~万行）+ `domain/` 领域引擎（forecast/pricing/fefo/pnl/procurement/wastage） |
| 前端 Web | 原生 JS IIFE 96 模块 + Vite 6 构建；设计令牌单一源 `variables.css`（苹果化完成）；统一反馈层 `feedback.js`；统一图标层 `icons.js`（今日落地） |
| 手机端 | `mobile.html` 独立 bundle（业务员外勤：拜访/拍照/录单/路线）；另有 Flutter 工程 `hergent_mobile/`（12 dart 文件，早期） |
| 小程序 | 未启动；`portal.html` 客户自助下单门户（Web 版，独立无 ApiClient） |
| 数据层 | SQLite：主库 `erp.db`（认证/租户注册）+ 每租户一库 `tenant_{id}.db`（物理文件隔离）；~210 张表 |
| 租户路由 | `X-Tenant-Id` 头 + contextvar → 切换租户 DB 文件句柄；已有租户硬化测试（无 X-Tenant-Id 即 401） |
| AI | DeepSeek 对话 + `domain/forecast_engine.py`（已包装 Holt/双指数/Holt-Winters 三算法，method=auto）+ 4 场景（预测→采购 / FEFO / 专属价 / 每日盈亏推送） |
| 真实数据 | tenant_1：580 订单 / 15,539 明细 / ¥214 万 / 430 商品 / 716 联系人 / 8,024 专属价 |
| 安全 | P0-1 全局认证 + P0-2 RBAC 默认拒绝 + B1 行级隔离 + H1b 统一错误信封；321 tests |
| 文档 | OpenAPI **生产关闭**（`docs_url=None if ENV=="production"`） |

**一句话判断**：业务闭环已经完成度 ~96%，但"商业化 readiness"大约只有 40%——缺的不是功能，而是**可交付形态**（多租户开通流程、小程序下单端、订阅计费）和**两个行业硬壁垒**（冷链温控数据、效期驱动的智能决策）。

---

## 总体立场（先讲三句实话）

1. **现在不要微服务化**。你只有一个后端开发者（AI 辅助），751 条 API、单租户日订单几十笔——微服务是纯负资产。正确路线是**模块化单体**（边界划清、接口内化），商业化到 20+ 租户且有真痛点时再拆。下面第 2 节给的是"划边界"方案，不是"拆服务"方案。
2. **三端不要统一技术栈，要统一 API 和设计令牌**。小程序是**获客武器**（给客户下单用），不是把 ERP 搬进去。Flutter 工程建议**停掉**——一个人维护三端原生是灾难，手机端用 PWA/现有 mobile.html 足够。
3. **AI 的壁垒不在 LLM，在数据闭环**。DeepSeek 谁都能接；别人抄不走的是"预测→执行→回收→修正"的飞轮，而这个飞轮你现在只转了半圈（有预测、有 Holt-Winters，但**没有预测准确率的回收表**）。

---

## 一、前端：三端统一组件化架构与跨端适配

### 现状问题
- Web 端 96 个 IIFE 模块无组件抽象，同一张" stat-card / 表格 / 弹窗"在几十个文件里重复拼字符串；
- mobile.html 是"另写的简化页"，与 Web 无组件复用，只有令牌复用；
- portal.html 完全独立（无 ApiClient、无令牌），小程序要重写一遍的话就是第三套。

### 目标架构：一套 API 客户端 + 一套设计令牌 + 两层组件

```
shared/
  tokens.css          ← 现有 variables.css 原样复用（已是单一源）
  api-client.js       ← 现有 core/api-client.js，抽成无框架依赖包
  icons.js            ← 今日落地，直接复用
web/     现有 96 模块（不动，逐步收编）
mobile/  mobile.html（保留外勤场景，复用 shared）
mp/      小程序（新建，只承载"客户下单 + 订单跟踪 + 对账单"三个场景）
```

### 组件复用策略（务实版）
1. **不引入 Vue/React 重写 Web 端**。96 个模块重写是 2-3 个月纯风险，商业化窗口等不起。
2. **收编式组件化**：把最高频的 6 个 UI 模式抽成 `core/components.js`（与 feedback/icons 同层）：
   `StatCard` / `DataTable(分页+排序+空态)` / `Modal` / `FormField` / `EmptyState` / `SectionTitle`。
   新模块强制使用；旧模块**用到才改**（不批量重写）。预期 6 个月自然覆盖 60%。
3. **三端复用的是"三样东西"而非组件**：① 设计令牌（小程序 wxss 直接抄变量值生成）；② API 客户端逻辑（小程序用 `wx.request` 包一层同签名 `ApiClient.get/post`）；③ 图标（icons.js 的 PATHS 可直接转小程序 image/svg）。
4. **各端差异化定位**（关键，别贪）：

| 端 | 用户 | 场景 | 差异化处理 |
|---|---|---|---|
| Web | 老板/财务/仓管 | 全功能 | 不动，继续苹果化 |
| 手机端 (mobile.html) | 业务员外勤 | 拜访签到/拍照/录单/今日清单 | 保留独立 bundle；补 PWA manifest 已有；加离线草稿（localStorage 队列，offline-sync.js 已有雏形） |
| 小程序 | **你的客户（门店老板）** | 下单/查订单/对账/专属价 | 全新建，只做 4 个页面：商品（带专属价）、购物车、订单列表、对账单。**不做 ERP** |

### 落地计划
- P0-1（1 周）：`core/components.js` 六组件 + 2 个新模块试点（采购控制台、订货会）
- P0-2（2 周）：小程序 MVP（Taro 或原生二选一——**建议原生**，页面只有 4 个，Taro 的构建复杂度不值；复用 portal.html 的 `/portal/*` API 族）
- P1（2 周）：手机端离线草稿 + 拍照压缩上传链路优化

---

## 二、后端：领域边界划分与可扩展数据模型（模块化单体）

### 现状问题
- `erp_db.py` 近万行是"上帝文件"，63 个 router 直接调它，领域之间可互相越界读表；
- 领域引擎（`domain/`）已经开始分，但 router 层没有强制约束；
- 没有"领域事件"概念——销售出库 → 库存扣减 → 盈亏重算 → 企微推送是硬编码串联，加一个新动作要改 N 处。

### 服务边界（现在划边界，不拆进程）

| 领域模块 | 核心实体 | 拥有的表（示例） | 对外接口（领域内服务函数） |
|---|---|---|---|
| catalog 商品域 | Product/Unit/Barcode/Category | products, product_units, barcodes | `catalog.get_product(id)`, `catalog.resolve_unit()` |
| inventory 库存域 | Stock/Batch/Warehouse/VehicleStock | inventory, batches, warehouses | `inventory.allocate_fefo()`, `inventory.adjust()` |
| sales 销售域 | SaleOrder/Item/CustomerPrice/Return | sale_orders, sale_order_items, customer_prices | `sales.create_order()`, `sales.price_for(customer,product)` |
| purchase 采购域 | PurchaseOrder/Forecast | purchase_orders, forecast_periods | `purchase.suggest()` |
| finance 财务域 | Payment/AR/AP/Voucher/Period | payments, vouchers, periods | `finance.post()`, `finance.pnl_daily()` |
| crm 客户域 | Contact/Visit/Route | contacts, visits | `crm.priority_today()` |
| ai 智能域 | Prediction/Advice/Backtest | ai_*(新建,见第三节) | `ai.forecast_sku()`, `ai.reorder_advice()` |
| tenant 租户域 | Tenant/User/Role/Permission | erp.db 内全部 | `tenant.provision()`（**商业化关键，当前缺失**） |

**约束规则（写进 CLAUDE.md/开发铁律）**：
1. router 只允许调本领域 `domain/xxx_engine.py` 的服务函数，禁止跨域直接读表；
2. 跨域协作走**显式服务调用**（`sales` 需要库存 → 调 `inventory.allocate_fefo()`，不许 SELECT inventory）；
3. 跨域副作用走**领域事件**（内存事件总线即可）：`sale_confirmed` → 订阅者：库存扣减、盈亏重算、企微通知。新增动作 = 加订阅者，不改主流程。
4. `erp_db.py` 只进不出：新代码禁止往里加函数，存量函数按域逐步搬迁到 `domain/`（用到才搬，同前端组件化策略）。

### 拆分触发条件（写下来，避免将来拍脑袋）
满足任一即拆：① 单租户日均订单 >500 且某域慢查询占比 >30%；② 租户数 >20 且需要按域独立扩缩容；③ 招到第二个后端。第一个拆的永远是 **ai 域**（计算密集、无状态、天然可独立）。

### 领域数据模型扩展（低温奶特性补强）
现有批次/FEFO 已在，缺的是**效期驱动的全链路字段**：
- `products` 补：`shelf_life_days`（已有 expiry_days）、`storage_temp_min/max`（冷藏 2-6℃）、`delivery_window_hours`（出库后可售时长）；
- `batches` 补：`production_date`、`remaining_days()` 计算列、`temp_log_status`（正常/超标/未知）；
- `sale_order_items` 补：`allocated_batch_id`（当前可能只有 batch_no 冗余，需确认约束）——这是**批次级追溯**的根基，也是温控数据闭环的挂载点。

---

## 三、AI × 业务深度融合（核心壁垒）

### 现状
- 有：Holt-Winters 三算法（forecast_engine）、采购建议、每日盈亏推送、DeepSeek 对话。
- 缺（致命）：**预测准确率回收**。现在预测发出去就结束了，没人知道准不准——飞轮转不起来。

### 3.1 数据闭环（最先做，P0）

```
预测生成 → 落库 ai_predictions(sku, period, predicted_qty, model_ver, created_at)
         → 到期后回填 actual_qty（定时任务每日跑）
         → 计算 mape/bias 落库 ai_backtest
         → 按 SKU 分桶：准(±15%)/偏高/偏低 → 自动调参（alpha/beta 网格搜索每月一次）
         → 报表页「预测准确率」给老板看（这是销售时的演示杀手锏）
```

新建 3 张表（tenant 库内）：`ai_predictions` / `ai_backtest` / `ai_advice_log`（建议被采纳/忽略的行为记录——LLM 建议的反馈信号）。

### 3.2 分场景模型选型（实话：90% 场景不需要 ML）

| 场景 | 方案 | 理由 |
|---|---|---|
| SKU 日销预测 | **Holt-Winters（已有）+ 分客户分层**：先按"客户×SKU"预测再汇总，比整店预测准得多 | 低温奶强周期（周几效应明显），HW 的季节性正好吃这个；数据量（580 单）不支持 ML |
| 自动补货建议 | **再订货点公式，不预测**：`建议量 = 日均销量 × (到货周期 + 安全天数) − 在库 − 在途`，安全天数按商品保质期分档（7 天奶：2 天；21 天：3 天） | 补货要的是可解释，老板敢信 |
| **效期感知补货**（行业独有） | 补货上限 = `min(补货量, 保质期内可销量)`；临期批次未清完时**降档补货**并推送"先清临期"建议 | 这是低温奶经销商的第一痛点（损耗），别人没有 |
| 销售趋势/异常 | 7 日移动平均 + 同比上周同日；偏离 >2σ 推送异常（客户流失预警已有 churn_risk 雏形） | 简单统计足够，LLM 负责"说人话"解释 |
| 温控风险 | **第一批不要做 IoT**。先做"人工录入 + 车辆出车/回车时间戳"推算暴露时长，超阈值标黄批次 | 硬件是下一年的事；先把数据模型和字段占住 |
| LLM 定位 | 解释层 + 交互层：把统计结果翻译成"人话建议"，把自然语言转成结构化查询（已有 hermes_core 工具调用） | LLM 不做数值预测（幻觉风险），只做解释和调度 |

### 3.3 落地计划
- P0-3（1 周）：3 张表 + 回填任务 + 准确率报表页
- P0-4（1 周）：再订货点引擎替换/增强现有 `predict_reorder`，接入效期分档
- P1（2 周）："客户×SKU"分层预测；LLM 解释层接预测结果
- P2（1 月）：温控暴露时长模型 + 批次温度状态字段

---

## 四、多租户与 RBAC

### 现状
- **已是物理隔离**（每租户一个 SQLite 文件）+ X-Tenant-Id 路由 + 硬化测试——这条路线 2026-07-25 评审已拍板，**是对的，继续**。
- RBAC 默认拒绝 + 274 映射键 + B1 行级隔离已实弹。

### 逻辑 vs 物理的权衡结论
- **经销商 SaaS 的正确答案就是物理文件隔离**：单租户数据量小（万级行）、客户要"数据是我的"的安全感、备份/导出/删库按文件操作即可、出问题互不影响。
- 代价是：跨租户统计难、schema 迁移要跑 N 个库、连接管理复杂。前两个你现在不痛，第三个用连接池+LRU（connection.py 已有雏形）即可。
- **演进路径**：SQLite 每租户一文件 →（租户 >50）PostgreSQL **schema-per-tenant**（同一实例，迁移和连接管理大幅简化，仍是物理隔离语义）→（>500）再考虑 shared schema + tenant_id。写 DB 访问层时就按"schema 可切换"抽象，换 PG 时业务零改动。

### 商业化必须补的两块（当前缺失）
1. **租户开通流水线 `tenant.provision()`**：注册 → 创建 tenant_N.db → 跑 `_safe_migrate` 全套建表 → 种子数据（默认角色/科目/演示商品可选）→ 发账号。现在是手工的，商业化必须一键。配套：套餐字段（free/pro/enterprise 已有）、`server/connectors_store.json` 已见计费雏形。
2. **RBAC 行业角色模板**：预置四角色——老板（全权限）/ 业务员（销售+CRM+自己业绩，行级已有）/ 仓管（库存+批次+采购）/ 财务（凭证+应收付+报表）。新租户开通时自动种入，客户 5 分钟能用。

### 落地计划
- P0-5（3 天）：角色模板种子 + 现有用户映射
- P1（1 周）：`tenant.provision()` + 开通页 + 冒烟测试（新租户建库→登录→下单一单）

---

## 五、数据库"分库分表"

### 实话
你已经分完了——**分片键就是 tenant_id，路由就是 X-Tenant-Id → tenant_{id}.db**。15,539 行明细谈库内分表没有任何意义。

### 真正要做的是三件小事
1. **库内索引审计**：按真实慢查询补索引（`sale_order_items(order_id, product_id)`、`batches(product_id, expiry_date)`、`customer_prices(customer_id, product_id)` 必须确认存在）——比任何分表都值钱。
2. **迁移框架**：`_safe_migrate` 已有，补一个"全租户批量迁移"脚本（商业化后 50 个库手跑会死人）：`tools/migrate-all-tenants.py`，逐库执行 + 失败隔离 + 报告。
3. **归档策略**（低温奶数据特性）：订单明细 2 年后 95% 不再查询——`sale_orders` 加分区视图 `v_orders_recent`（近 12 月），老数据可导出 SQLite 归档文件单独存放。这比分表实用一百倍。

### 水平扩展路径（写死，免得将来纠结）
```
单 SQLite 文件/租户（现在）
  → PG schema/租户（~50 租户，PG 单实例足够）
  → PG 多实例按租户哈希分库（>500 租户，大概率永远到不了）
```

---

## 六、RESTful API 规范、版本与文档

### 现状问题
- 751 条路由命名风格混居（`/products` vs `/api/search` vs `/forecast/orders/{id}`）；
- OpenAPI 生产关闭——对接小程序和将来第三方时等于裸奔；
- 分页参数不统一（有的 `limit/offset`，有的 `page/page_size`）；
- 错误信封已统一（H1b，✅ 这是底子）。

### 规范（定下来，老接口不动、新接口强制）

| 项 | 约定 |
|---|---|
| 前缀 | 内部：`/api/*`（现状维持）；**对外（小程序/三方）：`/api/v1/*`** 新路由族，首批发门户下单 8-10 条 |
| 命名 | 小写复数资源 + 子资源 `/api/v1/orders/{id}/items`；动作型用动词后缀 `/api/v1/orders/{id}:confirm`（或保持 POST 子资源，二选一写死） |
| 分页 | 统一 `page` + `page_size`（默认 20，上限 200），响应 `{items, total, page, page_size}`；移动端长列表用 `cursor`（订单流） |
| 错误码 | 沿用 H1b 信封 `{success:false, error:{code, message, detail}}`；code 三段式 `域.错误.具体`（如 `sales.order.not_found`），HTTP 状态码语义化不变 |
| 写操作 | POST 支持 `Idempotency-Key` 头（下单防重——手机端弱网双击必须），订单创建先落地这个 |
| 版本策略 | URL 主版本（v1）；v1 内只加不改；破坏性变更升 v2，旧版至少保活 6 个月 |
| 文档 | 生产重开 OpenAPI 但**挂认证**（`docs_url="/docs"` + 管理员 session 中间件），另导出 `openapi.json` 到仓库，`/api/v1` 族用 annotation 写全 example，直接生成小程序端 TS 类型（openapi-typescript） |

### 落地计划
- P0-6（2 天）：规范文档落 `docs/api-conventions.md` + OpenAPI 认证重开
- P0-2 配套：小程序 8-10 条 `/api/v1/portal/*` 按新规范实现（含 Idempotency-Key）

---

## 七、我追加的两项（你没问但更值钱）

### 7.1 冷链温控数据模型——行业真壁垒
低温奶经销商的死法只有两种：效期损耗、冷链断链。现在系统有 FEFO，**没有温度**。最小可行三步：
1. 批次表加 `temp_status` + 车辆加 `out_at/back_at`（出回车时间，已有装车/回车流程，只是没存时间）；
2. 每日按"暴露时长 = 回车 − 出车"对比 `delivery_window_hours`，超标批次标黄 + 当日盈亏单列"冷链损耗预估"；
3. 销售时对客户讲："别的 ERP 管账，我们管你的奶会不会坏在路上。"——这就是差异化定价权。
硬件温度记录仪（蓝牙温感）是 P2 以后的事，数据模型先占住。

### 7.2 发布管线——一个人更需要
- 现状：本地 6+ commit 未推远程（历史重写 delicate）、手工 rsync 部署、无 staging 流程强制。
- 最小三件：① 解决 GitHub 历史问题恢复推送（这是**单点灾难**——本地盘坏了 6 个 commit 就没了）；② `staging` 目录常态化（构建 `VITE_BASE=/staging/` → rsync staging → 验收 → 切生产，脚本化 `tools/deploy.sh`）；③ 生产验收脚本 `tests/validate_production.py` 加进部署后自动跑（已有，串起来）。

---

## 八、总体路线图

### Phase 0 — 商业化前夜（4-6 周，自用打磨 + 交付能力）
| 周 | 事项 | 验收 |
|---|---|---|
| W1 | `core/components.js` 六组件 + 2 试点 | 新模块零手写 stat-card |
| W1-2 | AI 数据闭环（3 表 + 回填 + 准确率页） | 老板能看到「上周预测准确率 82%」 |
| W2 | 效期感知再订货点 | 临期未清时补货建议自动降档 |
| W3 | RBAC 角色模板 + API 规范文档 + OpenAPI 认证重开 | 新用户 5 分钟配好权限 |
| W3-5 | **小程序 MVP**（商品/购物车/订单/对账 4 页 + `/api/v1/portal/*` 8-10 条） | 3 个真实门店客户用小程序下单 |
| W6 | tenant.provision() + deploy.sh + GitHub 历史清理推送 | 新租户 10 分钟开通 |

### Phase 1 — 首批付费验证（2-3 月，3-5 家经销商）
分层预测（客户×SKU）、LLM 解释层、手机端离线草稿、全租户批量迁移脚本、计费闭环（credits 已有雏形）。

### Phase 2 — 规模化（3-6 月，10+ 租户）
PG schema-per-tenant 评估与迁移、温控暴露模型、领域事件总线成型、ai 域首个独立服务拆分（若触发条件达成）。

---

## 九、风险与取舍（丑话说前面）

| 风险 | 等级 | 对策 |
|---|---|---|
| 小程序审核（食品经营类目资质） | 中 | 先用你的主体资质过审；客户仅下单不涉及支付（线下结算现状吻合） |
| 每租户一文件的运维成本 | 中 | W6 前必须有 migrate-all-tenants + 备份已覆盖（每日 6 库已跑） |
| 组件化/领域边界"只定规矩不执行" | 高 | 规矩写进 CLAUDE.md 铁律 + code review  checklist；新代码强制，老代码用到才改 |
| 预测准确率公开后"打脸" | 低 | 恰是迭代动力；页面加"模型版本"标识，每月调参可见改进 |
| GitHub 历史问题拖延 | 高 | 这是当前**唯一的数据安全隐患**，本周内处理掉 |

---

> 本计划全部基于今日实查代码（forecast_engine/connection.py/server.py/tests）。执行顺序有意把"AI 数据闭环"排在"小程序"之前——因为前者是你的护城河，后者只是渠道；但商业化现金流角度两者可并行。
