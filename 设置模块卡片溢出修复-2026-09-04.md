# 设置模块「文字溢出卡片」修复报告

日期：2026-09-04 ｜ 影响页面：`#/settings`（侧边栏「设置」）｜ 改动文件：`hergent-cn-v2/src/pages/Settings.vue`（仅前端样式，无后端改动）

---

## 一、交付

修复「设置」模块中**账号与组织 / AI 配置 / 数据与系统**三个页面文字、输入框紧贴并压出卡片边界的问题，并补齐该页两处长期缺失的样式定义。

| 页面 | 修复前 | 修复后 |
|---|---|---|
| 账号与组织 | 文字、按钮贴卡片边缘 | `padding:18px 20px`，无溢出 |
| AI 配置 | 输入框 `width:100%` 顶破卡片 | 输入框可伸缩，无溢出 |
| 数据与系统 | 长段落撑破卡片 | 段落可换行收缩，无溢出 |
| 权限 | 正常（未受影响） | 保持原样（toolbar `14px 16px`） |

## 二、变更

| # | 问题 | 修复 |
|---|---|---|
| 1 | 卡片无内距 | `.card{padding:18px 20px}`；`.card.toolbar{padding:14px 16px}`（高特异性，保住权限页原有间距） |
| 2 | `.page-sub` 全站无定义，本页却在使用 | 补 `font-size:12px;color:var(--t3)`（与各页面统一口径） |
| 3 | `.btn-mini` 本页未定义 → 权限页「恢复默认」是浏览器裸按钮 | 补边框/圆角/字号样式（该样式此前仅存在于 `Rebate.vue` 的 scoped 中） |
| 4 | 输入框顶破卡片 | `.set-row>.input{flex:1 1 240px;width:auto;min-width:0}`（原 `width:100%` 在 flex 行内不收缩） |
| 5 | 长段落 / 长记忆文本溢出 | `.set-row` 加 `flex-wrap:wrap`；`.set-desc` 加 `min-width:0`；`.panel-hd` 允许换行；`.mem-text` 加 `min-width:0;word-break:break-word` |

## 三、构建

- `npm run build` 通过 → `Settings-CI5StJf4.js` / `Settings-DQWJj0rh.css`
- rsync 部署至 `47.113.224.140:/opt/hergent-cn-v2/`
- 哈希比对：js `87a0d21b3df830ac1dc384eb78b09d78`、css `f49b0adaa7cb987aabd250d5541c69e7` → **MATCH_OK**

## 四、对齐效果

**运行时实测**（demo 登录 `#/settings`，逐页签 DOM 直查）：

| 页签 | 卡片 padding | 横向溢出 |
|---|---|---|
| 账号与组织 | `18px 20px` | 无 |
| AI 配置 | `18px 20px` ×2 卡 | 无 |
| 数据与系统 | `18px 20px` | 无 |
| 权限 | toolbar `14px 16px` / 内容卡 `18px 20px` | 无 |

**全站复查**（确认无第二处同类问题）：

- 静态扫描全部页面 style 块 → **NO_RISK，所有 `.card` 用法均有内距兜底**
- 运行时遍历 12 个路由 → **全部 `zeroPad=0`**
- 两处 `overflowX=1` 定性为**假阳性 / 无影响**：
  - `forecast` `.cross-card`：内部 `.table-wrap.cross-viewport` 是 `overflow-x:auto` 的设计内滚动容器，页面级 `docSW=1280 = docCW` 无溢出
  - `connect` `.cc-card`：仅差 10px，所有直接子元素宽度均等于内容区（281），页面级不可见，**未改动**

**类名完整性扫描**：`Settings.vue` 静态 `class` 全部有 CSS 定义（此前缺失的 `.page-sub`、`.btn-mini` 已补齐）。

## 五、内距全局收敛（第二轮，已完成）

经确认后把内距收敛进全局 `.card`，从根上杜绝此类问题。

**关键认知纠正**：此前判断「全局加 padding 会叠加成双重 padding」是**错的**。CSS padding 是**覆盖**不是叠加——页面 scoped 选择器 `.cc-card[data-v-x]` 特异性 (0,2,0) 高于全局 `.card` (0,1,0)，已自带内距的卡片**保持原值不变**。

**全局值取 18px**（基线统计 33 张卡片：18px ×18、16px ×8、20px ×2、特例 ×5）。

| 改动 | 文件 |
|---|---|
| `.card{padding:18px}`（附覆盖语义注释） | `src/styles/variables.css`「通用卡片」 |
| 删除上一轮手工加的 `.card{padding:18px 20px}`、`.card.toolbar{padding:14px 16px}` | `src/pages/Settings.vue` |

无需为 `.card.toolbar` 加特例——`.toolbar` 只在各页面 scoped 定义，特异性更高，不受全局影响。

**验证（先采基线 → 改造 → 再采数据 → 逐项 diff）**：

| 项目 | 结果 |
|---|---|
| 全站卡片 diff（18 路由 / 33 张） | **32 张零变化**，仅 settings `18px,20px` → `18px,18px`（主动清理的重复定义） |
| 权限页 `.card toolbar` | 保持 `14px,16px` ✓ |
| 裸 `.card` 探针注入 | `padding=18px 18px 18px 18px` ✓ 兜底生效 |
| 12 路由 zeroPad | 全部 0 |
| 页面级横向溢出 | `docSW=1280 = docCW`，无 |
| 部署 | js/css/index 三文件 md5 **MATCH_OK** |

设置页四个子页签复检：均为 `18px` 且 `Xover=no`。

## 六、需确认事项

1. **值统一（未做，待你确认）**：目前仍有 16px ×8、20px ×2 的卡片未收敛到 18px。统一会让这 10 张卡片的视觉发生微调，属视觉改造，需你拍板后再动。

2. **`connect` 页面 `.cc-card` 的 10px 内部溢出**未处理（页面级不可见、不影响显示）。若你在该卡片上看到异常，指出具体是哪张，我再针对性处理。

---

## 附：排查方法论（已沉淀）

判断「卡片溢出」不能只看 `.card` 层的 `scrollWidth`：

1. 向上查找是否存在 `overflow-x:auto` 的滚动祖先 —— 有则是设计内滚动，非 bug
2. 以 `documentElement.scrollWidth vs clientWidth` 作为页面级最终判据
3. 静态扫描 class 时，必须排除 `:class="{...}"` 动态绑定，否则会把 JS 比较值（如 `tab === 'account'` 的 `'account'`）误判为类名

**做全局样式收敛的正确姿势**（本次实践验证有效）：

4. **CSS padding 是覆盖不是叠加** —— 页面 scoped 选择器特异性高于全局，改全局不会影响已自带内距的元素。判断风险时不要想当然说「会叠加双重间距」
5. **基线 → 改造 → 再采 → diff**，而不是凭直觉判断影响面。遍历路由采集 computed 值存文件，改造后重采逐项对比，差异一目了然
6. **注入探针验证兜底**：动态创建一个裸 `<div class="card">` 读 computed padding 再移除，直接证明「以后裸用也不漏」
7. 全局值应取自**基线统计的众数**，而非拍脑袋定
