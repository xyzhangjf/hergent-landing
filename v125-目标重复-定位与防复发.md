# v125 · 同品牌同月目标重复创建 —— 定位、处理与防复发

> 现象：蒙牛低温 2026-09 存在两条目标 —— 一条来自「单期」入口，一条来自「年度」入口。
> 结论：**不是两条数据分属不同周期口径所以需要并存，而是判重口径写错了**（把 `period_type` 当成了判重维度）。
> 状态：代码已修复并部署生产；存量数据 2 组重复已定位，**清理动作待你一句话确认**（脚本已备好，可回滚）。

---

## 一、为什么两个入口能重复创建

### 1. 根因：冲突检测把 `period_type` 写进了匹配键

`server/routers/rebate_rules.py` 旧 `detect_conflicts()`：

```python
WHERE is_active=1 AND dimension=? AND target_type=? AND period_type=? AND scope_key=?
#                                                    ↑ 单期(month) ≠ 年度(year) → 两条互相"看不见"
```

两条规则对比：

| | id=8（单期入口） | id=10（年度入口） |
|---|---|---|
| dimension / target_type / scope_key | brand / amount / 蒙牛低温 | **完全相同** |
| period_type | `month` | `year` ← 唯一差异 |
| 生效期 | 2026-09-01 ~ 2026-09-30 | 空（＝不限，全年） |
| 目标 | 100 万 | 全年 901 万，其中 **09 = 100 万** |
| 返利率 | 10% | 15% |
| 创建时间 | 2026-09-03 10:19 | 2026-09-09 20:37 |

`period_type` 不同 → SQL 直接不匹配 → 后端返回 201 创建成功，**没有任何提示**。
年度规则生效期为空（＝不限）这一事实也从未被纳入比较。

### 2. 三个入口都没拦住

| 防线 | 状态 |
|---|---|
| 后端 409 冲突检测 | ❌ 被 `period_type` 挡住（根因） |
| 前端保存前预检 | ❌ 不存在，只有"填完 → 被 409 打回" |
| 数据库唯一约束 | ❌ 完全没有，只有应用层软校验（并发/脚本可绕过） |

### 3. 后果：目标、达成、返利三处同时翻倍

按月份聚合（生产 tenant_1 实测）：

| 月份 | 品牌 | 目标（现） | 达成（现） | 达成率 | 实际构成 |
|---|---|---|---|---|---|
| 2026-08 | 蒙牛低温 | **195 万** | **176 万** | 90.3% | #6 单期 100 万 + #10 年度 95 万 |
| 2026-09 | 蒙牛低温 | **200 万** | **11 万** | 5.5% | #8 单期 100 万 + #10 年度 100 万 |

- 达成也是按规则逐条求和的（`useMonthlyAchv.buildSimItems` 一条规则一对 target/achv），所以分子分母**同倍放大**，达成率看着"正常"，**绝对金额全是双倍**。
- 返利最危险：试算按规则独立计算再求和。若某月达成 100 万 →
  `#8 单期 100 万×10% = 10 万` + `#10 年度 100 万×15% = 15 万` = **25 万**，正确应为 **15 万**。

---

## 二、保留哪一条 + 安全处理步骤

### 建议：保留年度 #10，软停用单期 #6（8月）与 #8（9月）

| 方案 | 评价 |
|---|---|
| **A. 保留年度 #10，停用 #6/#8（推荐）** | 年度含 12 个月分解与月返利率，是完整口径；停用＝可逆；历史仍可查 |
| B. 保留单期 #6/#8，删除年度 #10 | 年度是 9/9 新建的最完整数据，且 8/9 月值与单期一致，删了要重填 12 个月 |
| C. 合并成一条 | 二者返利率不同（10% vs 15%）且年度按年月各异，强行合并会丢口径；不做 |

⚠️ **停用后口径变化（需你确认）**：8 月目标由 **100 万 → 95 万**（年度 8 月值），
8/9 月返利率由 **10% → 15%**。若 8/9 月仍应按 100 万 / 10% 考核，请改选方案 B 或先改年度规则再停用。

### 影响面核查（已查，风险低）

| 关联 | 是否绑定 rule_id | 结论 |
|---|---|---|
| 达成填报 `rebate_achievements` | ❌ 按 `period_month + dimension + scope_key` | **不受影响**，填报数据原样保留 |
| 阶梯版本 `rebate_tier_versions` | ✅ 但 #6/#8/#10 引用数 = **0** | 无影响 |
| 返利试算 `rebate_calculations` | ✅ 引用数 = **0** | 无影响 |
| 计提 `rebate_accruals` | 绑 `contract_id`（且 `rebate_contracts` 0 行孤儿） | 无影响 |
| 报单期次 `forecast_periods` | ❌ 不绑规则 | 无影响 |
| 组合促销 / 告警历史 | ✅ 引用数 = 0 | 无影响 |

### 处理步骤（脚本已备好，默认 dry-run）

```bash
# 1) 预演（不动数据，只打印将要做什么）
scp v125-清理重复目标-脚本.py root@47.113.224.140:/tmp/ && ssh root@47.113.224.140 'python3 /tmp/v125-清理重复目标-脚本.py'

# 2) 确认无误后执行
ssh root@47.113.224.140 'python3 /tmp/v125-清理重复目标-脚本.py --apply'

# 3) 回滚（如需）
ssh root@47.113.224.140 'python3 /tmp/v125-清理重复目标-脚本.py --rollback'
```

脚本做了 5 件事（全部在一个事务里，失败即回滚）：

1. **备份** `tenant_1.db` → `/root/hergent-backup/tenant_1.db.bak-<时间戳>`
2. **软停用** `UPDATE rebate_target_rules SET is_active=0 WHERE id IN (6,8)`（**不物理删除**）
3. **释放月份锁** `DELETE FROM rebate_rule_month_lock WHERE rule_id IN (6,8)`
4. **把 8/9 月锁归还年度 #10**（回填全年 12 个月），保证年度规则是唯一占用者
5. 打印前后对比 + 回滚 SQL

回滚：`UPDATE rebate_target_rules SET is_active=1 WHERE id IN (6,8)` + 重跑锁回填（重启服务或 `--rollback`）。

---

## 三、防复发：已实施的改造（已部署生产）

### 后端（hergent-erp）

| 改动 | 位置 | 说明 |
|---|---|---|
| 判重口径改为「覆盖月份交集」 | `domain/rebate_period.py`（新） + `rebate_rules.detect_conflicts()` | 不再比较 `period_type`；把规则展开成 `{'2026-09', ...}` 再取交集。年度规则生效期空＝全年 12 个月，因此必然与同品牌任意单期规则冲突 |
| 月份覆盖计算下沉 domain 层 | `domain/rebate_period.py` | 原放路由模块会与 `erp_db` 循环 import（迁移回填时报 `partially initialized module`） |
| 保存前预检接口 | `POST /api/rebate-rules/precheck` | 不落库，返回冲突规则 + 冲突月份；编辑时排除自身 |
| 409 提示带月份 | `create_rule` / `update_rule` | 报文改为「该品牌在这些月份已有启用中的目标：2026-08、2026-09」并列出冲突规则的口径（年度/单期） |
| **DB 级唯一约束** | `rebate_rule_month_lock` 表 + `UNIQUE(dimension, target_type, scope_key, ym)` | 应用层拦不住的并发/脚本，由 SQLite 兜底；`IntegrityError → 409` |
| 锁的生命周期 | 新增 `sync_month_lock()` / `release_month_lock()` | create/update 时重算；**停用/删除即释放**（软删可回滚）；存量由迁移 `INSERT OR IGNORE` 回填（先到先得，重复静默跳过，不影响启动） |
| 冲突清单接口升级 | `GET /api/rebate-rules/conflicts` | 同样按月份判重，返回 `months` 字段 |

### 前端（hergent-cn-v2）

| 改动 | 位置 | 说明 |
|---|---|---|
| 表单内实时预检提示 | `TargetFormModal.vue` | 品牌 / 生效期 / 目标年度 / 年度⇄单期切换 / 月度金额变化时 debounce 400ms 调 `/precheck`，黄色提示条列出「哪条规则占用了哪几个月」；**年度与单期两个入口共用同一逻辑** |
| 409 冲突弹窗 | `Rebate.vue` | 新增「口径」「冲突月份」两列，文案改为「同一品牌同一月份只能有一条目标，否则返利会被重复计算」 |
| 异常预警区 | `Rebate.vue` | 由「生效期重叠冲突」改为「N 组目标在同一品牌同一月份重复（返利会被重复计算）」并列出规则对 + 月份 |

### 验证结果（生产实机）

```
precheck 蒙牛低温 2026-09 单期  → ok=false, conflicts=[(#8 单期, 2026-09), (#10 年度, 2026-09)]
precheck 蒙牛低温 2026-10 单期  → ok=false, conflicts=[(#10 年度, 2026-10)]   ← 年度覆盖全年，同样正确拦截
precheck 简爱（无重复品牌）      → ok=true,  conflicts=[]
POST 重复创建 2026-09           → 409 + 冲突月份
POST 不冲突 2027-03             → 201（随后 hard delete 清理，锁表无残留）
GET  /conflicts                 → 2 组：#6×#10 (2026-08)、#8×#10 (2026-09)
表单 UI：年度入口选中品牌即提示；单期入口填 9 月生效期后提示
仪表盘预警区：「2 组目标在同一品牌同一月份重复…」+ 规则对 + 月份
```

---

## 四、需你确认

1. **是否按方案 A 执行存量清理**（停用 #6/#8，保留年度 #10）？回答「执行」即可，我会跑上面脚本并复核。
2. 若执行：8 月目标 100 万 → 95 万、8/9 月返利率 10% → 15%，这是 annual 规则里你填的值，确认无误吗？
3. 若你其实只想留单期（年度 #10 只是 9/9 的测试数据），改走方案 B —— 直接删 #10，代码不用动。

---

## 附：本次改动文件

后端：`server/domain/rebate_period.py`（新）、`server/routers/rebate_rules.py`、`server/erp_db.py`
前端：`src/components/rebate/TargetFormModal.vue`、`src/pages/Rebate.vue`
