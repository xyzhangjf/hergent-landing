# 商品档案列表 — 双套重复实现审查报告

> 审查日期：2026-08-03 ｜ 审查对象：`static/app.js` + `static/js/modules/product-archive-enhance.js`（前端），`server/routers/data.py` + `server/routers/portal_api.py`（后端）
> 结论：**前端确实存在两套商品列表实现，且已发生代码漂移，其中一套存在错位覆盖 bug 风险。**

---

## 一、结论速览

| 层 | 是否存在重复 | 说明 |
|---|---|---|
| 前端列表渲染 | **✅ 是（核心问题）** | `app.js` 主力渲染 + `product-archive-enhance.js` 覆盖层，两套 tbody 行渲染、两套表头排序 |
| 前端表单 | ❌ 否 | `product-form.js` 是被 app.js 委托的**唯一**商品表单实现（`showDataForm` → `window._openProductForm`），职责单一，非重复 |
| 后端列表 API | ⚠️ 部分重叠 | `/api/products`（主系统）与 `/api/portal/{token}/products`（移动端门户）字段口径不同但用途不同，**不建议删**，需统一字段口径 |
| 后端 CRUD | ❌ 否 | `GET /api/products` 仅注册于 `data.py`，server.py 无重复注册 |

---

## 二、前端两套实现的位置与职责

### A 套 — 主力实现（保留对象）
**文件：`static/app.js`，函数 `renderDataList()`（4917–5006 行）**

| 子功能 | 位置 | 能力 |
|---|---|---|
| 数据获取 | 4921 行 | `/api/products?keyword&limit&offset` + 客户端高级过滤（类别/品牌/状态/标签/有图/有条码/保质期） |
| 排序 | 4930 行 + `turnSort()`（241–244）+ `sortTable()`（234） | 表头内联 `onclick="turnSort('name')"`，`_sortCol/_sortDir` 状态 |
| 表头渲染 | 4960–4976 | `_colVis` 列配置（10 列可开关）、`sortHdr` 排序箭头、列设置下拉 |
| 表体渲染 | 4977–5001 | checkbox、序号、**商品编码**、名称（内联可编辑）、助记名、类别（带标签）、品牌（可编辑）、单位（多单位换算）、条码、规格（可编辑）、进价（可编辑）、售价（可编辑）、库存、状态、**操作列（编辑/复制/···菜单）** |
| 其它 | 4934–4957、5004–5006 | 卡片视图、tfoot 合计（进价/售价/库存）、分页、行选择、密度切换、键盘导航 |

### B 套 — 增强/覆盖层（冗余对象）
**文件：`static/js/modules/product-archive-enhance.js`（216 行，main.js:37 真实加载）**

| 子功能 | 位置 | 行为 |
|---|---|---|
| 包装 | `hookRenderDataList()` 71–207 | 替换 `window.renderDataList`，先调 `_orig()` 再"增强" |
| 增强计算 | `enhanceProductRow()` 9–68 | 库存颜色/安全库存/毛利%/效期/批发价/状态徽章/供应商 HTML（计算逻辑本身有价值） |
| 行注入 | 124–153 | 用**列索引**（stockIdx）定位单元格后 innerHTML 覆盖（⚠️ 列序假设与 A 套不一致，见下） |
| 第二套 tbody 渲染 | `refreshProductTableBody()` 168–203 | **完整重写整表**：thumb 图片 + 毛利 + 批发价 + 效期 + 供应商 + 状态徽章列；但**丢失**序号/商品编码/内联编辑/**操作列** |
| 第二套排序 | 84–121 | 给 th 再 `addEventListener('click')`，与 A 套 `onclick="turnSort"` **双触发** |

---

## 三、功能差异对比

| 能力 | A 套（app.js） | B 套（enhance） |
|---|---|---|
| 序号列 | ✅ | ❌ 丢失 |
| 商品编码列 | ✅ | ❌ 丢失 |
| 名称内联编辑（editable-cell） | ✅ | ❌ 丢失（只读 strong） |
| 品牌/规格/进价/售价内联编辑 | ✅ | ❌ 丢失 |
| **操作列（编辑/复制/···菜单）** | ✅ | ❌ **整列丢失** |
| 多单位换算显示 | ✅ | ❌ 简化 |
| 类别标签 pills | ✅ | ❌ 丢失 |
| 缩略图列（thumb） | ❌（仅卡片视图有） | ✅ |
| 库存颜色 + 安全库存 | ❌ | ✅ |
| 毛利率 | ❌ | ✅ |
| 批发价列 | ❌ | ✅ |
| 效期列（expiry_days） | ❌ | ✅ |
| 供应商列 | ❌ | ✅ |
| 状态徽章（正常/低库存/断货/停用） | ❌（仅文字状态） | ✅ |
| 表头排序 | ✅ turnSort | ✅（与前者重复，双触发） |
| 分页 / 卡片视图 / tfoot 合计 / 键盘导航 | ✅ | ❌（refresh 后全部失效） |

> **核心结论：B 套独有的"增强列"（毛利/批发价/效期/供应商/徽章）是产品想要的；但 B 套若真正生效会摧毁 A 套的编辑、操作列等核心能力。两者是"增强想覆盖主力"的关系，而非互补。**

---

## 四、漂移证据（git 历史）

- `product-archive-enhance.js` 自 `67216c2`（initial commit）后**仅一次改动**：`5c04e20`（图标收编，7-31）。
- `app.js` 商品列表自 `94e8682`（7-30）起**重构 10+ 次**：新增商品编码列、序号列、操作列文字化、移除统计卡、紧凑化（`af44c0d`/`a85d2e8`/`7cc4826`/`6ee1894`/`696e7e7` 等）。
- **B 套的列序假设从未随 A 套更新** → 两套已漂移，B 套的列索引定位必然错位。

### 实锤：列错位推演（默认全列可见）
```
A 套实际列序: checkbox, 序号, product_code, name, mnemonic, category, brand, unit, barcode, spec, 进价, 售价, stock, 状态, 操作
B 套假设列序: checkbox, thumb, name, mnemonic, barcode, spec, unit, brand, category, stock
B 套 stockIdx=2 → 命中 A 套 cols[3] = 「name」
```
→ **B 套的行注入会把「商品名称」列覆盖成"库存数字 + 状态徽章"**（node 推演已复现：`cols[3]` = `name`）。

### 次生问题
1. **排序双触发**：点击表头 → A 套 `turnSort`（async 重渲染）与 B 套 click handler（`list.sort` + `refreshProductTableBody` 同步重渲染）都执行；`refreshProductTableBody` 若抢先可见，操作列/编辑/序号/编码全部消失。
2. **表头/表体列数不对齐**：A 套 thead 无"序号"th，tbody 却有序号 td（4961 vs 4979），列配置下拉与列序关系脆弱，放大了 B 套索引定位的风险。

---

## 五、后端说明（不算重复，但需统一口径）

| API | 文件 | 用途 | 字段口径 |
|---|---|---|---|
| `GET /api/products` | `server/routers/data.py:33` | 主系统列表（分页+过滤+keyword） | `sale_price/purchase_price` 等全字段 |
| `GET /api/portal/{token}/products` | `server/routers/portal_api.py:116` | 移动端客户自助下单（token 认证，LIMIT 100 无分页） | `price/cost` 精简字段 |

- 两者功能重叠但**用途不同**（内部管理 vs 客户下单门户），属于开发计划 E 泳道（小程序 MVP）的正常产物，**不建议删除**。
- 建议：统一价格字段命名（`price/cost` → `sale_price/purchase_price` 映射），避免后续数据消费方踩坑。

---

## 六、建议方案（推荐顺序）

### 方案一（推荐）：保留 A 套，B 套降级为"纯计算层"
1. **保留** `enhanceProductRow()` 的 6 项计算逻辑（库存颜色/毛利%/效期/批发价/状态徽章/供应商）。
2. **删除** B 套的：`refreshProductTableBody()`（第二套 tbody 渲染）、表头 addEventListener 排序（84–121）、列索引行注入（124–153）。
3. **改写注入方式**：不再用列索引，改为在 A 套渲染**之后**按 `data-field` 定位单元格注入（A 套已有 `data-field="name"/"brand"/"spec"/"purchase_price"/"sale_price"` 语义锚点），或直接并入 A 套一次性渲染。
4. 若要展示毛利/批发价/效期/供应商/缩略图等增强列 → **在 A 套 renderDataList 中新增对应列**（受 `_colVis` 控制），这才是"一个功能一个入口"的正道。

### 方案二（最小修复，30 分钟）
若短期不想重构：在 `hookRenderDataList` 内**禁用行注入与 refreshProductTableBody**（注释 124–153 与 84–121 两段），仅保留 `enhanceProductRow` 计算供后续并入 A 套。立即消除错位覆盖与双排序。

### 验证方式
- 打开商品档案页（DevTools `Cmd+Option+J`），检查名称列 HTML 是否混入库存数字/徽章 → 若混入，说明行注入已生效（bug 实锤），按方案二先止血。
- 点击任意表头排序后检查：操作列是否仍在、名称列是否为"库存+徽章"。

---

## 七、行动清单

- [x] P0（止血）：禁用 B 套行注入与 refreshProductTableBody，消除名称列错位覆盖与双排序 — **2026-08-03 已完成**（方案一重构，见下方「重构记录」）
- [x] P1：`refreshProductTableBody` 删除；enhance 模块瘦身为纯增强层 — **2026-08-03 已完成**
- [x] P1：行注入改为表头语义映射（`turnSort('field')` 解析），默认/隐藏列配置均精确命中 stock 列（node 模拟验证通过）
- [ ] P1：毛利/批发价/效期/供应商/状态徽章作为 `_colVis` 受控列并入 A 套 `renderDataList`（`enhanceProductRow` 计算已保留待复用）
- [ ] P2：统一 portal/products 字段口径（price/cost → sale_price/purchase_price）
- [ ] P2：修复 thead 缺"序号"th、thead 多"状态"th 的列对齐问题（注：重构后 `map.status` 会误映射到 tbody 操作列，当前未使用，留待 P2 修复）

## 重构记录（2026-08-03，方案一）

**变更文件**：`static/js/modules/product-archive-enhance.js`（216 行 → 全量重写）
- 删除：`refreshProductTableBody()`（第二套 tbody 渲染，曾丢序号/编码/内联编辑/操作列）
- 删除：表头 `addEventListener('click')` 第二套排序（与 `turnSort()` 双触发）
- 删除：硬编码列索引行注入（stockIdx=2 曾错位覆盖「商品名称」列）
- 保留：`enhanceProductRow()` 六项计算逻辑（库存颜色/毛利%/效期/批发价/状态徽章/供应商，供未来 _colVis 增强列复用）
- 新增：`buildFieldMap()` 表头语义映射（解析 `turnSort('field')` → 字段→列索引），注入改为仅对 stock 列做着色 + 安全库存 + 状态徽章
- 排序职责完全回归 A 套 `turnSort()`（唯一实现）
- 验证：`node --check` 通过；node 模拟默认/隐藏列两种配置下 stock 均精确命中；`VITE_BASE=/ npm run build` 构建通过

**已部署生产**（2026-08-03 21:06，bundle `main-DzM5KO9d.js`）

---

## 附：首次进入档案页布局错乱修复（2026-08-03 21:20，commit 481d9af，bundle `main-BNQBKvsI.js`）

**症状**：点击"商品档案"第一次与第二次界面不同。
**根因**：所有入口（archive-hub / trade-mega / sidebar / operations-hub / 全局搜索）均为 `navTo('data') + setTimeout(switchDataTab,200)` 模式 —— `navTo` 时 `_dataTab` 仍是旧值，`renderData()` 用旧 tab 渲染布局；200ms 后 `switchDataTab` 只改 `_dataTab` + `renderDataList(1)` **只换数据不重建布局** → 第一次出现「旧 tab 布局 + 新 tab 数据」错乱，第二次 `_dataTab` 已修正才正常。
**修复（5 处）**：
- `switchDataTab`：`renderDataList(1)` → `renderData()`（内部已含 updateDataTabs + renderDataList，一处修复覆盖全部入口）
- 全局搜索 879/880/1077/1084 行：手动 `_dataTab=xxx + renderDataList` → `switchDataTab(xxx)`（先重建正确布局再设搜索词）
**验证**：node --check ✓；grep 无残留手动 `_dataTab` 入口 ✓；构建 ✓；线上 index 引用 `main-BNQBKvsI.js`，首页/新 bundle 均 200 ✓；备份 `/tmp/static-backup-20260803-211922.tgz`

**待用户确认**：是否部署到生产（构建产物在 `static/dist/`，需 rsync 至服务器并重启）

---

**审查人**：UI Designer ｜ **状态**：待用户确认方案后实施
