# 创建目标页面拆分规划方案（品牌目标 / 商品目标）

> 版本：v122 规划稿 · 2026-09-08
> 现状基线：`hergent-cn-v2/src/pages/Rebate.vue`（3694 行，含 1 个共用「新建/编辑规则」弹窗，第 522–769 行）
> 后端基线：`hergent-erp/server/routers/rebate_rules.py`（`validate_rule` L317、`_row_to_dict` L187）、`server/erp_db.py`（`rebate_target_rules` 建表 L520 / 迁移 L626-665）
> 目标：把 1 个共用弹窗拆成 2 个独立页面（品牌目标创建页 / 商品目标创建页），**只出方案与执行清单，不含代码实现**。

---

## 0. 先说结论（决策摘要）

| 决策 | 结论 |
|---|---|
| 拆分形态 | **组件级拆分**（两个独立表单组件 + 共用底座），**不做两个独立路由**。理由见 §6.1 |
| 数据模型 | **不加表、不改主键**，继续共用 `rebate_target_rules` 单表；差异靠 `dimension` 字段区分 |
| 最大风险 | 不是页面，是**存量商品规则上残留的报单/自动化配置**。商品页若不提交这些字段，保存时会把它们静默清空 —— 见 §5.2 |
| 建议新增字段 | `target_year`（品牌页，**必填**）、`target_unit`（商品数量页，**建议**） |
| 建议下沉后端 | 现在「品牌 = 12 月分解 / 金额 / 达成即返 / 比例」这套强制规则写在**前端 save()** 里（L1821-1840），API 直调可绕过 → 应下沉到 `validate_rule` |

> **Step 0 摸底已完成（2026-09-08 22:15，只读）** → 见 `v122-Step0-数据摸底报告.md`
> 结论：R1/R2/R4 = 0（无商品规则、无残留自动化配置），商品默认改 amount 零影响；`target_year`/`target_unit` 11 个库全缺列需加迁移。
> **但发现 R5（高优先级）**：存量 7 条品牌规则中 **5 条是月口径单期目标**（蒙牛/简爱 8月、9月 各一条），与「12 个月分解唯一模式」+ `save()` 强制 `period_type='year'` 直接冲突 —— 编辑存量月目标会把「9 月 100 万」变成「年度 100 万」。**处理方式待拍板，见 §9.5**。

---

## 1. 现有共用页面字段全量梳理（逐字段处置）

当前弹窗字段按渲染区域分为 6 组。下表「处置」列即本次动作。

### 1.1 基础信息区

| # | 字段 | 落库列 | 品牌页处置 | 商品页处置 | 说明 |
|---|---|---|---|---|---|
| 1 | 规则名称 | `rule_name` | **保留**（改名占位） | **保留**（改名占位） | 品牌页占位改「如 蒙牛低温2026年度目标」；商品页改「如 特仑苏9月目标」 |
| 2 | 计算维度 | `dimension` | **移除 UI**（固定 `brand`） | **移除 UI**（固定 `product`） | 入口按钮已区分，页内再选一次是纯噪音。落库值由页面固定写入 |
| 3 | 目标度量 | `target_type` | **移除 UI**（固定 `amount`） | **保留** | 后端硬约束：品牌维度不能选 `quantity`（`validate_rule` L341）。商品页保留 amount/quantity 二选 |
| 4 | 作用对象 | `scope_name` / `scope_key` | **保留 + 收紧为必填** | **保留 + 改造为商品选择器** | 品牌页：datalist 候选来自品牌档案，不在档案即红字拦截（现有逻辑保留）<br>商品页：现在是**自由文本框**（`scopeDatalist` 对 product 返回空数组，L1716-1720），用户只能手填商品 ID —— 这是当前最明显的体验缺陷，改为下拉搜索，选中后自动回填 `scope_key=id`、`scope_name=name` |
| 5 | 生效开始 / 生效结束 | `effective_start` / `effective_end` | **保留** | **保留** | 完全共用，无差异 |

### 1.2 目标值区（当前两套并存，是拆分的核心）

| # | 字段 | 落库列 | 品牌页处置 | 商品页处置 | 说明 |
|---|---|---|---|---|---|
| 6 | 周期口径 | `period_type` | **移除**（固定 `year`） | **保留** | v121c 已对品牌页隐藏；拆分后彻底从品牌页删除，不再靠 `v-if` 走同一段模板 |
| 7 | 目标金额（单值，万元） | `target_value` | **移除**（由月合计派生） | **保留**（amount 口径时） | 品牌页的 `target_value` 改为**只读派生**= 12 个月之和，不再有独立输入框 |
| 8 | 目标数量（单值） | `target_value` | **移除** | **保留**（quantity 口径时） | 同上 |
| 9 | 全年目标（万元，派生） | 派生 → `monthly_amounts` | **保留** | **移除** | 写 = 均分 12 个月（余数补 12 月）；读 = 各月合计 |
| 10 | 全年返利率（%，派生） | 派生 → `monthly_rates` | **保留** | **移除** | 写 = 全月统一；读 = 12 月一致才显示、不一致显示占位「各月不同」 |
| 11 | 12 个月分解表 | `monthly_amounts` / `monthly_rates` | **保留（默认铺开，页面核心）** | **移除** | 后端硬约束：这两个字段**仅品牌维度允许**，商品维度传了直接 400（`validate_rule` L507-510） |

### 1.3 返利计算区

| # | 字段 | 落库列 | 品牌页处置 | 商品页处置 | 说明 |
|---|---|---|---|---|---|
| 12 | 触发方式 | `trigger_mode` | **移除 UI**（固定 `on_target`，改为说明行） | **保留**（达成即返 / 阶梯返利） | 月度返利 = 各月比例各自生效，与「按全年达成率分档」的阶梯天然互斥 |
| 13 | 计法 | `scale_type` | **移除** | **保留**（仅 tiered 时显示） | 累进 / 全量按档 |
| 14 | 返利形式 | `rebate_basis` | **移除**（固定 `rate`） | **保留**（比例 / 固定金额） | 品牌页由月度返利率承载 |
| 15 | 返利值 | `rebate_rate` / `rebate_amount` | **移除** | **保留** | 品牌页顶层 `rebate_rate` 保留落库（取最早非零月兜底），但不入 UI |
| 16 | 阶梯档位 | `tiers_json` | **移除** | **保留** | 品牌页保存时显式写空串 |
| 17 | 达成门槛 | `trigger_threshold` | **移除**（固定 1） | **由隐藏改为露出（选填）** | 现有字段，目前两页都没入口。商品页露出价值大：「达成 80% 即返」是常见政策。默认 1.0 |

### 1.4 到货与报单节奏区（整块，v121 合并后的 arrival-block，L663-753）

| # | 字段 | 落库列 | 品牌页处置 | 商品页处置 | 说明 |
|---|---|---|---|---|---|
| 18 | 报单模式 | `order_mode` | **保留** | **移除** | 报单是**品牌方排产节点**（蒙牛 / 简爱各自的节奏），与单品无关 |
| 19 | 首次报单日 | `order_first_date` | **保留** | **移除** | 同上 |
| 20 | 报单周期 / 报单星期 | `order_cadence_days` / `order_weekdays` | **保留** | **移除** | 同上 |
| 21 | 提前天数 N | `order_lead_days` | **保留** | **移除** | 同上 |
| 22 | 首次到货日（派生） | — | **保留** | **移除** | 同上 |
| 23 | 最多可提前 | `order_max_early_days` | **保留** | **移除** | 同上 |
| 24 | 智能创建/关闭报单表 | `auto_period_enabled` | **保留** | **移除** | 后端「租户内唯一」约束（L579-590）：商品规则若也开着会互相顶掉 |
| 25 | 三个时点 | `auto_open_time` / `auto_close_time` / `supplier_deadline_time` | **保留** | **移除** | 同上 |
| 26 | 本月到货次数（覆盖） | `arrival_count_override` | **保留** | **移除** | 均单口径依附于品牌目标，商品级无意义 |
| 27 | 6 期预览表 | 只读（`/auto-period-preview`） | **保留** | **移除** | 同上 |
| 28 | 均单（只读派生） | 只读（`/arrival-preview`） | **保留** | **移除** | 同上 |

> **关键判断**：这一整块 11 个字段（24–28 含派生）在商品页**全部冗余**。现在它们对商品目标也照常渲染，用户会困惑「我给一个单品建目标，为什么要填报单日」。这是本次拆分收益最大的一块。

### 1.5 其他

| # | 字段 | 落库列 | 品牌页处置 | 商品页处置 | 说明 |
|---|---|---|---|---|---|
| 29 | 优先级 | `priority` | **保留** | **保留** | 共用 |
| 30 | 启用 | `is_active` | **保留** | **保留** | 共用 |
| 31 | 归属合同 | `contract_id` | 不入弹窗（维持现状，在合同模块绑定） | 同左 | — |

### 1.6 处置汇总

| 处置类型 | 字段数 | 品牌页 | 商品页 |
|---|---|---|---|
| 两页共用保留 | 6 | 规则名称、作用对象、生效起止、优先级、启用 | 同左 |
| 品牌页独有 | 18 | 全年目标、全年返利率、12 月表、节奏区 11 项、目标度量(amount 固定)、触发方式说明 | — |
| 商品页独有 | 8 | — | 周期口径、目标金额/数量、触发方式、计法、返利形式、返利值、阶梯、达成门槛 |
| 两页都移除 | 2 | 计算维度（固定写入） | 计算维度（固定写入） |

---

## 2. 拆分后两个页面的字段清单

### 2.1 品牌目标创建页

**标题**：创建品牌目标（编辑时：编辑品牌目标）
**副标题**：按 12 个月分解 —— 填全年自动铺满，也可只改单月

| 序 | 分区 | 字段 | 必填 | 校验规则 | 默认值 | 落库 |
|---|---|---|---|---|---|---|
| 1 | 基础 | **目标年度** 🆕 | **必填** | 整数 2000–2100 | 当前年 | `target_year`（新增列） |
| 2 | 基础 | 品牌 | **必填** | 必须命中品牌档案（datalist 校验） | 空 | `scope_name` / `scope_key`（同名） |
| 3 | 基础 | 规则名称 | 必填 | 非空，≤64 字；为空时按「{品牌}{年度}年度目标」自动生成 | 自动生成 | `rule_name` |
| 4 | 目标 | 全年目标（万元） | 必填（等价于至少 1 个月有金额） | >0，≤1,000,000；写入即均分 12 个月 | 空 | → `monthly_amounts` |
| 5 | 目标 | 全年返利率（%） | 必填（等价于至少 1 个月有利率） | 0–100，最多 1 位小数 | 空 | → `monthly_rates` |
| 6 | 目标 | 12 月表（月份/金额万元/返利率%） | 选填（微调） | 金额 ≥0；利率 0–100；留空 = 该月无目标 / 不触发 | 全空 | 同上 |
| 7 | 目标 | 年度合计（只读） | — | = 各月之和 | — | `target_value`（元） |
| 8 | 目标 | 触发方式（说明行，非输入） | — | 固定「达成即返（按月返利率）」 | — | `trigger_mode='on_target'` |
| 9 | 节奏 | 报单模式 | 选填 | 按间隔天数 / 按固定星期 | `interval` | `order_mode` |
| 10 | 节奏 | 首次报单日 | 选填；开自动建表时**必填** | 合法 YYYY-MM-DD（拦 2/30） | 空 | `order_first_date` |
| 11 | 节奏 | 报单周期(天) | 条件必填（interval 模式） | 1–31 整数 | 2 | `order_cadence_days` |
| 12 | 节奏 | 报单星期 | 条件必填（weekday 模式） | 至少 1 个 | 空 | `order_weekdays` |
| 13 | 节奏 | 提前天数 N（天到货） | 选填 | 0–30 整数 | 4 | `order_lead_days` |
| 14 | 节奏 | 首次到货日（派生，可改） | 选填 | 不得早于报单日；手改则反推 N | 报单日+N | 不落库 |
| 15 | 节奏 | 最多可提前（天报单） | 选填 | 0–2 | 1 | `order_max_early_days` |
| 16 | 节奏 | 本月到货次数（覆盖） | 选填 | ≥0 整数；空 = 系统算 | 空 | `arrival_count_override` |
| 17 | 节奏 | 均单（只读） | — | — | — | 不落库 |
| 18 | 自动化 | 智能创建/关闭报单表 | 选填 | 租户内唯一 | 手动建表(0) | `auto_period_enabled` |
| 19 | 自动化 | 开放填报 | 选填 | HH:MM | 20:00 | `auto_open_time` |
| 20 | 自动化 | 自动关单 | 选填 | HH:MM，须早于厂家截止 | 10:00 | `auto_close_time` |
| 21 | 自动化 | 厂家下单截止 | 选填 | HH:MM | 12:00 | `supplier_deadline_time` |
| 22 | 自动化 | 未来 6 期预览（只读） | — | — | — | 不落库 |
| 23 | 基础 | 生效开始 / 生效结束 | 选填 | YYYY-MM-DD；开始 ≤ 结束 | 空 | `effective_start/end` |
| 24 | 基础 | 优先级 | 选填 | 整数 | 0 | `priority` |
| 25 | 基础 | 启用 | 选填 | 0/1 | 1 | `is_active` |

**固定写入（不入 UI）**：`dimension='brand'`、`target_type='amount'`、`period_type='year'`、`trigger_mode='on_target'`、`rebate_basis='rate'`、`trigger_threshold=1`、`tiers_json=''`、`rebate_amount=0`、`monthly_*` 有值时 `rebate_rate` 取最早非零月兜底。

### 2.2 商品目标创建页

**标题**：创建商品目标（编辑时：编辑商品目标）

| 序 | 分区 | 字段 | 必填 | 校验规则 | 默认值 | 落库 |
|---|---|---|---|---|---|---|
| 1 | 基础 | **商品** 🔧 | 必填 | 下拉搜索选中；必须能解析出数字 `scope_key` | 空 | `scope_key`(id) / `scope_name` |
| 2 | 基础 | 规则名称 | 必填 | 非空；为空时按「{商品名}{周期}目标」自动生成 | 自动生成 | `rule_name` |
| 3 | 目标 | 目标度量 | 必填 | 下单金额 / 销售数量 | 销售数量 | `target_type` |
| 4 | 目标 | 周期口径 | 必填 | month / quarter / year / custom | month | `period_type` |
| 5 | 目标 | 目标金额（万元） | 条件必填（amount） | >0 | 空 | `target_value`（×10000 存元） |
| 6 | 目标 | 目标数量 | 条件必填（quantity） | >0 | 空 | `target_value` |
| 7 | 目标 | **单位** 🆕 | 条件必填（quantity） | 箱 / 件 / 提（可扩展枚举） | 箱 | `target_unit`（新增列） |
| 8 | 返利 | 触发方式 | 必填 | 达成即返 / 阶梯返利 | 达成即返 | `trigger_mode` |
| 9 | 返利 | 达成门槛 | 选填（on_target 时） | (0,1]，如 0.8 = 达成 80% 即返 | 1.0 | `trigger_threshold` |
| 10 | 返利 | 计法 | 条件必填（tiered） | 全量按档 / 累进 | 全量按档 | `scale_type` |
| 11 | 返利 | 返利形式 | 必填 | 比例 / 固定金额 | 比例 | `rebate_basis` |
| 12 | 返利 | 返利值 | 条件必填（非 tiered） | 比例 (0,1]；固定金额 >0 | 空 | `rebate_rate` / `rebate_amount` |
| 13 | 返利 | 阶梯档位表 | 条件必填（tiered） | 至少 1 档；from < to；末档 to=0 表示无上限 | 空 | `tiers_json` |
| 14 | 基础 | 生效开始 / 生效结束 | 选填 | 日期合法 + 区间合法 | 空 | `effective_start/end` |
| 15 | 基础 | 优先级 | 选填 | 整数 | 0 | `priority` |
| 16 | 基础 | 启用 | 选填 | 0/1 | 1 | `is_active` |

**固定写入（不入 UI）**：`dimension='product'`、`monthly_amounts={}`、`monthly_rates={}`、全部 `order_*` / `auto_*` 归零或置空（见 §5.2 风险）。

---

## 3. 两页差异点对照

| 维度 | 品牌目标页 | 商品目标页 |
|---|---|---|
| **页面结构** | 3 分区：基础 → **12 个月分解（核心，默认铺开）** → 到货与报单节奏 | 3 分区：基础 → 目标值（单值） → 返利计算 |
| **目标值形态** | 双层（金额层 + 返利层）× 12 个月；年度总额 = 各月之和（派生，只读） | 单值 + 周期口径 |
| **返利形态** | 只有一种：按月返利率（本月 10%、下月 8%） | 达成即返（比例/固定） 或 阶梯返利（按达成率分档 + 计法） |
| **交互流程** | 一次性填全年 → 自动铺满 → 单月微调（只改 1 个月不影响其他月）→ 看 6 期报单预览 | 选商品 → 定口径 → 填目标值 → 配返利 →（tiered 时）加档位 |
| **是否有异步预览** | 有 2 个：均单（`/arrival-preview`）、6 期报单表（`/auto-period-preview`），均带 300ms 防抖 | 无（纯同步表单） |
| **提交前置校验** | 至少 1 月有金额、至少 1 月有返利率；N 与到货日一致；三时点顺序 | 商品必须能解析 `scope_key`；tiered 至少 1 档；返利值区间 |
| **提交时强制覆写** | `period_type='year'`、`trigger_mode='on_target'`、`rebate_basis='rate'`、`tiers_json=''` | `monthly_*={}`、`order_*`/`auto_*` 归零 |
| **数据模型** | 同一张 `rebate_target_rules`，`dimension='brand'`；额外用 2 个 JSON 列存月度分解 | 同一张表，`dimension='product'`；用 `target_value` + `tiers_json` |
| **冲突/唯一约束** | 受「租户内 `auto_period_enabled` 唯一」约束 | 不受该约束（页面已移除该字段） |
| **作用对象校验** | 品牌名必须在品牌档案（`brandOptions`） | 商品必须能解析出数字 ID（后端查 `products` 表） |

---

## 4. 新增 / 调整 / 清除清单

### 4.1 建议新增

| 字段 | 页面 | 类型 | 必填 | 理由 | 后端动作 |
|---|---|---|---|---|---|
| `target_year` | 品牌 | INTEGER | **必填** | 12 个月分解但没地方写「是哪一年」，目前只能靠生效期推断 —— 这是当前最实际的缺口 | `erp_db.py` 两处加列（tenant patch 元组 + `_safe_migrate`）；`validate_rule` 校验 2000–2100；`_row_to_dict` 补白名单 |
| `target_unit` | 商品（quantity 时） | TEXT | 条件必填 | 数量目标没有单位等于没有口径（箱？件？提？），算返利和看板都会歧义 | 同上；枚举 `('箱','件','提','其他')` |
| 商品选择器 | 商品 | UI 组件 | 必填 | 现在只能手填商品 ID（L1716-1720 对 product 返回空 datalist） | 复用 `/api/products?limit=5000`（已在 `ProductArchive.vue` 用过）；后端 `validate_rule` 增加「按 name 反查 id 回填」的容错 |

### 4.2 需要调整

| 项 | 现状 | 调整为 |
|---|---|---|
| `trigger_threshold` | 两页都无入口，恒 1.0 | 商品页露出（选填），品牌页固定 1 不入 UI |
| 规则名称 | 必手填 | 两页都支持「留空自动生成」（品牌：`{品牌}{年}年度目标`；商品：`{商品名}{周期}目标`），减少必填项 |
| 品牌字段 | 选填（空 = 全部品牌） | 品牌页改为**必填**（品牌目标不指向具体品牌没有意义）；商品页同理 |
| 「品牌维度强制约束」 | 写在前端 `save()` L1821-1840 | **下沉到后端 `validate_rule`**，按 `dimension` 分支强制，防止 API 直调绕过 |

### 4.3 需要清除的冗余

**品牌页清除**（模板 + `form` 默认值 + 提交逻辑三处都要清）：
`dimension` 下拉、`target_type` 下拉、`period_type` 下拉、单值「目标金额/目标数量」、`trigger_mode` 下拉（改说明行）、`scale_type` 下拉、`rebate_basis` 下拉、`rebate_rate`/`rebate_amount` 输入、`tiers_json` 阶梯编辑器。

**商品页清除**：
到货与报单节奏整块（`order_mode`/`order_first_date`/`order_cadence_days`/`order_weekdays`/`order_lead_days`/`order_max_early_days`/`auto_period_enabled`/`auto_open_time`/`auto_close_time`/`supplier_deadline_time`）、`arrival_count_override`、均单与 6 期预览、12 个月分解表、全年目标/全年返利率。

**代码级清除**：
- `isBrandMonthly` / `monthlyOn` 两个 computed（L1249-1251）—— 拆分后不再需要运行时判断，删除
- 模板中所有 `v-if="isBrandMonthly"` / `v-else` / `v-if="!monthlyOn"` 分支（L556、L596、L623、L636）—— 由组件边界替代
- `targetWan` 在品牌页的残留引用（L1256/L1285/L1817）—— 品牌页改由 `monthlySumWan` 唯一承载

---

## 5. 实施步骤

### Step 0 · 数据摸底（**必须先做，10 分钟，决定后续风险等级**）

在生产库（每个租户库）跑统计，回答 4 个问题：

1. `dimension='product'` 且 `auto_period_enabled=1` 的规则有几条？→ **若 >0，Step 3 必须前先处理**（见 §5.2）
2. `dimension='product'` 且 `order_first_date != ''` 或 `monthly_amounts != ''` 的几条？
3. `dimension='brand'` 且 `monthly_amounts=''` 且 `period_type != 'year'` 的几条？→ 这批是「老口径品牌规则」，编辑时会触发 `legacyNotice`
4. `scope_key` 为非数字字符串的 `product` 规则几条？→ 决定商品选择器回填策略

产出：一份 markdown 统计表 + 1 条 UPDATE 脚本（把商品规则上的 `auto_period_enabled` 清零、把残留 `order_*` 置空），**备份后执行**。

### Step 1 · 纯结构重构（不改任何行为，必须能零差异回归）

新建目录 `src/components/rebate/`：

| 文件 | 职责 |
|---|---|
| `BrandTargetForm.vue` | 品牌目标表单（本文 §2.1 全部字段） |
| `ProductTargetForm.vue` | 商品目标表单（§2.2） |
| `TargetFormBase.vue` | 两页共用底座：规则名称、作用对象、生效起止、优先级、启用、底部按钮 + 校验提示 |
| `MonthlySplitBlock.vue` | 12 个月分解（品牌页独占）：全年目标/全年返利率/12 月表/合计/清空 |
| `ArrivalRhythmBlock.vue` | 到货与报单节奏（品牌页独占）：① 节奏 ② 到货产出 ③ 报单自动化 + 预览 |
| `RebateValueBlock.vue` | 返利计算（商品页独占）：触发方式/门槛/计法/形式/值/阶梯表 |
| `useRebateTargetForm.js` | composable：`form` 默认值工厂、`save()`、`buildMonthlyPayloads()`、`fillMonthlyFromRule()`、日期联动（`_addDaysISO` 等）、`toast` |

Rebate.vue 退化为宿主：列表 + 筛选 + 按 `form.dimension` 渲染 `BrandTargetForm` 或 `ProductTargetForm`。
**验收**：构建通过 + 真机回归（品牌创建、商品创建、编辑、复制、试算）行为与拆分前完全一致。

> ⚠️ 抽组件时共用依赖（`api`、`toast`、`rebateMeta`、`brandOptions`、`defaultCadence`、`conflicts` 刷新）**一律走 props / emits，禁止用全局单例**；`Rebate.vue` 现在的作用域变量不能直接被子组件访问。

### Step 2 · 品牌页裁剪（在 Step 1 基础上删字段）

- 删 1.6 表中「品牌页移除」的 10 个字段
- 加 `target_year` 输入（默认当前年，位于首行）
- 规则名称支持留空自动生成
- **保留** `legacyNotice` 提示（老口径品牌规则不静默拆分）

### Step 3 · 商品页裁剪（**依赖 Step 0 结论**）

- 删整块节奏区（11 个字段 + 2 个预览）
- 「作用对象」改造为商品下拉搜索（选中回填 `scope_key`=id、`scope_name`=name）
- 加 `target_unit`（quantity 时）
- 露出 `trigger_threshold`
- 提交时显式写空：`monthly_amounts={}`、`monthly_rates={}`，`order_*`/`auto_*` 归零

### Step 4 · 后端约束下沉与字段迁移

| 文件 | 改动 |
|---|---|
| `server/routers/rebate_rules.py` | ① `validate_rule` 按 `dimension` 分支：品牌强制 `target_type='amount'`/`period_type='year'`/`trigger_mode='on_target'`/`rebate_basis='rate'`；商品强制 `monthly_*` 为空、`order_*`/`auto_*` 归零 ② 新增 `target_year` / `target_unit` 校验 ③ 商品 `scope_key` 按 name 反查回填的容错 ④ `_row_to_dict` **白名单补新列**（不加前端读不回） |
| `server/erp_db.py` | `target_year` / `target_unit` 两处迁移（tenant patch 元组 L681 附近 + `_safe_migrate` 脚本区），幂等 |
| `server/domain/arrival_schedule.py` | 预计不动；若 `target_year` 参与排程计算则需按年过滤（待定） |

### Step 5 · 兼容与灰度

- 加 feature flag（如 `formV2`）：出问题一键回退旧弹窗
- 存量回填脚本：`target_year` 从 `effective_start` 年份推断；`target_unit` 留空（不臆测）
- 部署顺序：**先后端**（加列 + 校验放宽到兼容旧前端）→ **再前端** → 灰度 1 个租户 → 全量

---

## 6. 取舍说明与注意事项

### 6.1 为什么不做两个独立路由页

| 方案 | 改动 | 评价 |
|---|---|---|
| **A. 组件级拆分**（推荐） | 中 | 两个页面在用户感知上完全独立（不同标题、不同字段、不同流程），但共享父页的列表刷新、合同上下文、`rebateMeta`、冲突检测。**改动可控、可灰度、可回退** |
| B. 两个独立路由（`#/rebate/brand-target`、`#/rebate/product-target`） | 大 | 需把弹窗内所有依赖（试算器、合同、meta、品牌档案、toast）迁到路由页并重新加载；列表页与表单页的状态同步要重做。**收益（URL 可分享 / 可深链）目前不成立** —— 这是低频的配置动作，不是日常浏览页 |
| C. 保持现状 + `v-if` | 0 | 已经证明会持续劣化：`isBrandMonthly` / `monthlyOn` 这类判断会越堆越多，且商品页被迫渲染 11 个无关字段 |

**结论：选 A。** 若未来出现「商品目标也需要按 12 个月分解」或「需要深链分享配置」，再升级到 B。

### 6.2 兼容存量数据的 6 个必须注意

1. **⚠️ 最高风险：商品规则上的残留自动化配置。** 现在商品规则也能开 `auto_period_enabled`。商品页删掉该字段后，编辑保存会把它们**静默清零** —— 而后端「租户内唯一」是靠这张表查的，清零可能导致智能建表无声停摆。Step 0 必须先查、先备份、先处理，并在 Step 3 提交逻辑里对「检测到存量非空值」给出二次确认弹窗。
2. **`_row_to_dict` 是白名单**（L187-259，注释已明说）。任何新增字段不加进去，前端编辑时读不回、被默认值覆盖 → 表现为「保存成功了但再打开是空的」。
3. **`openEdit` 用 `{...r}` 全量浅拷贝**（L1767）。未展示的字段会被原样带回提交 —— 这正是「提交时显式清空」而非「不传」的原因。
4. **老品牌规则的口径迁移不能静默**。现有 `fillMonthlyFromRule` 只对 `period_type==='year'` 自动均分，月/季口径走 `legacyNotice` 留空提示。拆分后这条保护必须原样保留，**且保存前加一次确认**（用户在未注意提示时点保存，等于把月目标改成了年目标口径）。
5. **品牌维度不能选 quantity** 是后端硬约束（L341）。商品页保留该下拉没问题，但若未来加了「商品金额目标」，注意 `target_value` 单位仍是元。
6. **多租户**：所有迁移在 `erp_db.py` 的 tenant patch 与 `_safe_migrate` 两处都要加，且必须幂等（老租户库可能已跑过部分迁移）。

### 6.3 复用共用逻辑时

- **不要复制粘贴 `save()`**。两页的 `save()` 有 60% 相同（校验、组装、请求、刷新），抽到 `useRebateTargetForm.js`，差异部分用「提交前钩子」注入（品牌页注入月度组装 + 强制覆写，商品页注入 tiers 组装 + 节奏清零）。
- **`loadArrivalPreview` 的 300ms 防抖与 watch 依赖**（L1690-1698）随节奏区一起搬进 `ArrivalRhythmBlock.vue`，不要留在父页 —— 否则商品页也会触发无意义的请求。
- **日期联动三函数**（`_parseISO` / `_addDaysISO` / `_diffDaysISO`，v121b 已单测 19 例）原样搬入 composable，迁移后**重跑单测**。
- **CSS**：`.arrival-block` / `.ap-*` / `.monthly-block` / `.dt-tier` 等样式目前内联在 Rebate.vue 的 `<style>`（L3373 起），随组件一起拆出或统一提到 `src/styles/`，避免两个组件各引一份。

---

## 7. 涉及文件清单

**前端**（`hergent-cn-v2/`）
- `src/pages/Rebate.vue` ← 主要改造对象（522–769 弹窗、1236–1350 状态、1699–1900 逻辑、3373+ 样式）
- 新增 `src/components/rebate/BrandTargetForm.vue`
- 新增 `src/components/rebate/ProductTargetForm.vue`
- 新增 `src/components/rebate/TargetFormBase.vue`
- 新增 `src/components/rebate/MonthlySplitBlock.vue`
- 新增 `src/components/rebate/ArrivalRhythmBlock.vue`
- 新增 `src/components/rebate/RebateValueBlock.vue`
- 新增 `src/composables/useRebateTargetForm.js`（或 `src/components/rebate/useRebateTargetForm.js`）

**后端**（`hergent-erp/`）
- `server/routers/rebate_rules.py` — `validate_rule` / `_row_to_dict` / `create_rule` / `update_rule`
- `server/erp_db.py` — 建表 L520、tenant patch L626-665、L681 索引区、`_safe_migrate` L1010 起
- `server/domain/arrival_schedule.py` — 按需（若 `target_year` 参与排程）

**部署**
- 前端：`npm run build` → `mv dist /tmp/...` → `rsync -a --no-owner --no-group --delete dist/ root@47.113.224.140:/opt/hergent-cn-v2/` → `chown -R hergent:hergent`
- 后端：`./deploy.sh`（**注意**：该脚本 rsync 整个工作区，会带上 3789 行历史未提交改动，这是当前生产的实际运行版本，符合预期）
- 验证：服务 `active` + health 200（重启后 **sleep 6** 再探活）+ 真机 Chrome 跑两页各一轮创建/编辑

---

## 8. 验收清单（拆分完成后逐条打勾）

- [ ] 品牌页：无「周期口径 / 目标度量 / 触发方式下拉 / 返利形式 / 返利值 / 阶梯」
- [ ] 品牌页：12 个月区默认铺开；全年 1200 → 12×100；只改 3 月=150 → 其他月仍 100、合计 1250
- [ ] 品牌页：报单日 9/9 + N=2 → 到货 9/11；手改到货 9/15 → N 反推 6；清空报单日 → 到货清空
- [ ] 商品页：无整个「到货与报单节奏」区块、无 12 个月表
- [ ] 商品页：商品下拉能搜索并回填 ID；quantity 时出现单位选择
- [ ] 商品页：tiered 模式至少 1 档否则拦住；on_target 时返利率 (0,1]
- [ ] 两页：规则名称留空能自动生成
- [ ] 落库核对：品牌 `period_type=year`、`trigger_mode=on_target`、`target_value`=月合计；商品 `monthly_amounts=''`、`order_*` 归零
- [ ] 存量规则编辑：老口径品牌规则出现 `legacyNotice` 且保存前二次确认
- [ ] 单测重跑：日期联动 19 例
- [ ] 回滚验证：feature flag 关掉能回到旧弹窗

---

## 9. 待你拍板的 5 件事（1–4 已定）

1. ✅ **`target_year` 加不加？** → **加**（品牌页必填，存量取 `effective_start` 年份回填）
2. ✅ **`target_unit` 加不加？** → **加**（商品 quantity 时；存量留空不臆测）
3. ✅ **商品页「目标度量」默认？** → **amount（下单金额）**；存量商品规则 0 条，零影响
4. ✅ **Step 0 先跑？** → **已跑完**，见 `v122-Step0-数据摸底报告.md`
5. ⬜ **R5：存量月口径品牌规则怎么办？**（🔴 唯一未决项，阻塞 Step 2）

   | 选项 | 做法 | 评价 |
   |---|---|---|
   | **A（推荐）** | 品牌页支持「年度（12 月分解）」/「单期（月，单值）」两模式，按 `period_type` 回填，新建默认年度 | 贴合实际用法（真实租户 4 条全是月口径）；存量零风险；把 4 个技术选项收敛为 2 个业务选项，不违背"去掉周期口径"初衷 |
   | B | 只留 12 月分解；存量月规则编辑时强制二次确认 + 预填 | 维持单模式，但每次编辑存量都要确认，且转换后口径确实变了 |
   | C | 只留 12 月分解；存量月规则设为只读 | 零误改，但存量 5 条变死数据 |
