# hergent.cn AI 层 · 优化设计（对标 WorkBuddy）

> 日期：2026-07-22
> 基线：Hermes 内核；web 版现状 = 2 栏布局（左 `sidebar` 含 `chatHistoryList`+`sidebarRoles`、中 `right-panel`），多屏 Home/DataHub/Forecast/Reports/Tasks/Settings/Skills/Channels；手机端连接已实现、待验证。
> 关联文档：`AI层核心功能模块与UI设计梳理.md`（模块/优先级）、`AI层核心功能模块与UI设计梳理.md` 的 MVP 路线。

---

## 0. 设计基调重申（重要纠偏）

此前梳理建议过"玻璃拟态 / 磁性 hover / Three.js 粒子"的装饰性 premium 方向；本轮明确要求**对标 WorkBuddy、简洁专业、避免花哨**。据此调整设计语言：

- 从"装饰优先"转向**"信息清晰优先"**。
- 保留既有 design tokens（圆角 8/12/20、字号体系、cyan 品牌色）作为技术底座，但**改变应用方式**：平面表面、克制单一强调色、充足留白、清晰层级——不再用渐变铺满 / 粒子背景 / 强磁吸位移。
- 理由：B 端经营决策工具，用户要的是"一眼看懂、快速行动"，不是视觉炫技。WorkBuddy / Claude / Linear 等一线产品的共性就是**克制的专业感**。

---

## 1. 视觉风格对标（简洁专业）

对照 WorkBuddy 的设计原则，落到我们的 token 体系：

- **平面化**：卡片用 `1px` 细边框 + 极浅灰底（如 `#F7F8FA`），不用玻璃模糊 / 重投影。
- **单一强调色**：cyan 仅用于"主操作 / 激活态 / 关键数据"，不滥用；导航、文本以中性灰阶为主。
- **中性灰阶文本**：标题 `#1A1A1A`、正文 `#3A3A3A`、辅助 `#8A8A8A`（沿用 `--text-*`）。
- **统一栅格**：间距用 8 的倍数；圆角 / 字号沿用已收敛的令牌。
- **图标**：线性 icon（`stroke:1.5`），不用实心彩色大图标堆叠。
- **暗色模式**：确认 `:root.dark` 全部变量重写到位，保持同套克制语言。
- **去除清单**：渐变按钮（改纯色 / 描边）、粒子背景、强磁吸位移（保留轻微 hover 反馈即可）。

---

## 2. AI 回复可视化（核心缺口）

**现状**：web 版几乎无图表（仅 1 处 canvas），结构化结果（毛利 / 异常价 / 预测）以纯文本长段呈现，信息密度低、难扫读。
**目标**：AI 回复中的结构化数据自动渲染为"可视化卡片"，文本保留用于解读。

**渲染策略（CSP 安全、零外部依赖）**：图表用 **inline SVG 自绘**（前端/服务端按"结构化结果 JSON → 卡片模板"渲染），避免引外部 CDN（web 版 CSP 在 nginx 层，外链图表库有风险）。如确需 richer 图表，自托管轻量库（精简 ECharts 包）而非 cdn。

**组件规范**：

| 组件 | 内容 | 交互 |
|------|------|------|
| 盈亏快报卡 | KPI 三连（今日毛利 / 毛利率 / 环比 ▲▼）+ 近 7 日毛利 mini 面积图（inline SVG sparkline） | 可展开明细 |
| 专属价异常卡 | 可排序表格，异常行高亮（红/橙左边框），顶部异常计数徽标 | 行内"查看"→ 右栏明细 |
| 预测采购卡 | 表格（商品 / 建议量 / 置信区间 ±）+ 历史销量 sparkline 缩略 | 底部"生成采购单"主按钮 |
| 效期预警卡 | 水平 expiry 时间条（剩余天数色阶：>7 绿 / 3–7 黄 / <3 红），点状批次 | 点批次看明细 |
| 通用表格/列表卡 | 分页、排序、导出 CSV | 导出物进入右栏 |

**交互细节**：卡片可"展开/收起"；关键卡可"固定到右栏"（与 §3 联动）；表格行 hover 高亮；执行类操作给 toast + 可撤销。

---

## 3. 右栏交付成果面板（对标 WorkBuddy Artifacts）

**现状**：2 栏（左 sidebar + 中 main），无"成果/产物"专属区；AI 生成物混在对话流，难二次查看 / 下载。
**设计**：新增**右栏 Artifact Panel**（默认窄栏 ~320px，可拖宽 / 收起）。

**3.1 内容**：当前对话产生的交付物列表——文件（CSV / 报告 MD / 采购单）、图表快照、可固化的卡片副本。

**3.2 交互**：
- 每条交付物 = 一行（图标 + 名称 + 时间 + 操作：打开 / 下载 / 复制 / 固定）。
- 点"打开"→ 右栏内嵌预览（HTML / 表格 / 图片），不跳走；大文件全屏。
- 对话中生成的卡片可一键"发送到右栏"固化。
- 打开历史对话（§4）时，右栏自动载入该对话的交付物。

**3.3 响应式**：≤1024px 右栏收为底部抽屉；≤768px（已有汉堡菜单）右栏隐藏，交付物走全屏页。

**3.4 与可视化（§2）协同**：AI 回复里的图表卡，其"源数据 / 可导出版"落在右栏。

**布局总览（3 栏）**：
`[左] 导航 + 角色切换 + 对话历史  |  [中] 对话 / 业务屏  |  [右] 交付成果面板`

---

## 4. 角色架构下是否需要历史对话查看（评估）

**结论：需要，且应做成"按角色隔离 + 可搜索 + 关联交付物"的历史，而非简单堆砌。**

**现有基础**：sidebar 已有 `chatHistoryList`（对话历史列表雏形）+ `sidebarRoles`（角色切换）——即从"有"升级到"好"。

**理由（结合低温奶经销商场景）**：
1. **经营连续性**：经销商每天产生决策记录（某日采购单、月末盈亏快报、某客户调价），事后必查（"上旬那张进货单多少箱"）——无历史 = 每次重写上下文。
2. **审计与合规**：AI 生成的采购单 / 调价 / 报损是**经营凭证**，需可追溯（谁、何时、基于什么数据）；财务场景尤甚。
3. **多角色隔离**：大秘 / 会计 / 订单文员 / 运营主管各有独立工作流，历史应**按角色分桶**，避免混淆；同时提供"跨角色全局搜索"。
4. **与右栏协同（§3）**：历史对话项点开 → 右栏载入该对话的交付物，形成"对话—成果"一体回溯。
5. **弥补记忆局限**：M9 长期记忆是"抽象偏好"，历史是"具体事件"，二者互补，不能互相替代。

**注意点**：
- 历史受 M10 权限管控（不同员工可见范围不同）。
- 提供搜索 + 时间/角色筛选 + 归档，避免无限列表。
- 空态引导（首次无历史时的提示）。

**反向考量**：有人会犹豫——"AI 员工"隐喻让人以为员工"自己记得"。但那是记忆（M9）职责，人类用户侧的"回顾"仍需 UI，故不冲突、应并存。

---

## 5. 全面对标 WorkBuddy：关键优化点清单

### 功能
- **[高] 右栏交付物面板（§3）**——最大缺口。
- **[高] AI 回复可视化（§2）**——结构化数据图表化。
- **[高] 历史对话升级（§4）**——按角色隔离 + 搜索 + 关联交付物。
- **[中] 多模型 / 思考模式可见性**：现有仅模型选择（DeepSeek V4 Flash），增加"推理过程"可展开（对标 WorkBuddy thinking），提升可信度。
- **[中] 全局命令面板（⌘K）/ 对话内搜索**——当前无。
- **[中] 交付物版本与导出**——报告 / 采购单可导出 PDF/CSV 并留版本。
- **[低] 模板 / 快捷指令**——常用问法固化（"今日快报""本周进货"）。

### 交互
- **[高] 布局重构为 3 栏**（左导航 / 中对话 / 右成果），解决当前 sidebar 把导航 + 角色 + 历史混在一起的拥挤。
- **[高] 工具调用可视化**：AI 调 API（查 8024 价 / 跑预测）时，对话流内显示"工具卡"（进行中 / 完成 / 结果），让用户看见"AI 在干活"（MVP 设计已提，需落地）。
- **[中] 流式输出 + 打字机**，已有基础上加"停止生成"按钮。
- **[中] 空状态（L1）/ 骨架屏（L2）/ 快捷键 hint（L3）**——补齐体验细节。
- **[中] 移动端**：右栏 → 底部抽屉；汉堡菜单已具备，需联调。

### 体验 / 视觉
- **[高] 视觉去花哨（§1）**——去渐变 / 粒子 / 强磁吸，转平面克制。
- **[中] 一致性**：web 与桌面分叉（技术债），确立单一源码或明确双构建，避免双线样式漂移。
- **[中] 暗色模式打磨**：确认 tokens 全覆盖、无遗漏硬编码色。
- **[中] 首次引导**：角色卡下加"试试这样说"示例提示（对标 WorkBuddy 建议提问），降低冷启动。
- **[中] 操作反馈**：执行类动作（生成采购单 / 调价）给明确确认 + 撤销入口。
- **[低] 加载性能**：首屏 <1.5s，图表用 inline SVG 轻量。

### 本轮优先级建议（结合既有 P0/P1）
- **先做（成本可控、体感最强）**：① 视觉去花哨 + 3 栏布局骨架；② 右栏交付物面板；③ AI 回复可视化基础组件（盈亏快报卡 + 专属价异常表）。
- **其次**：历史对话升级、工具调用卡、thinking 可见。
- **手机端连接**（已实现待验证）优先做端到端验证，再谈 deep 优化。

---

## 6. 落地建议 / 下一步

1. **出可点击 HTML 原型**（用现有 token、平面克制风）验证 3 栏布局 + 右栏 + 可视化卡，再开发——避免布局返工。
2. 先改 **web 变体**（`/var/www/hergent-landing/`），保留 CSP / 移动布局 / web 图标（延续上次移植原则：移植而非覆盖）。
3. 可视化优先用 **inline SVG 自绘组件**，规避 CSP / CDN 风险。
4. 手机端连接先完成验证闭环，再纳入右栏/可视化的移动适配。
