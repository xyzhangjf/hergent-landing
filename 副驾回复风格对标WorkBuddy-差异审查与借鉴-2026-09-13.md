# 副驾回复风格对标 WorkBuddy —— 差异审查与可借鉴做法

> 日期：2026-09-13
> 前期文档：`Hergent-副驾回复格式优化分析-对标WorkBuddy.md`（2026-08-31，已落地协议注入 + markdown 渲染 + ```cards 意图围栏）
> 本文定位：上一份解决"**有没有**格式层"，本文用两侧真实回复做量化对照，回答"格式层**管住没有**、还差哪几维"。

---

## 0. 结论先行

**协议已经被证明有效——但只覆盖了一个维度。**

2026-09-11 14:40，我们把「卡片字段规范」钉进协议。证据显示它的效果是**一分钟内生效**：14:32 和 14:38 的两张预报卡还在自造英文键（`daily_avg/suggest`、`daily_sales/days_supply`），14:41 的下一张就稳定输出了协议规定的中文键（`品名/日均/库存/建议`）。**没有重训、没有微调、没有改代码——只改提示词文本。**

这说明：**模型是能被契约管住的。** 那么现在的问题不是"协议没用"，而是——

| 一句话 | 证据 |
|---|---|
| 协议只钉了「卡片字段」 | 骨架 / 篇幅 / 加粗 / 表格 / 术语白名单 **均无量化要求** |
| 因此这些维度仍在自由发挥 | 6 条真实回复：篇幅 165→1266 字（**差 7.7 倍**）、加粗 1→21 处（**差 21 倍**） |
| 该用的能力闲置 | 前端早支持表格渲染（`utils/md.js:80` 输出 `md-table`），**6 条真实回复 0 条用表格** |
| 该禁的没禁净 | 给经销商老板的回复里直接怼出 shell 命令 `hermes pairing approve wecom PAVGNRZ2` |
| 无校验兜底 | 卡片字段已规范，但**没有任何校验**；`ResultCard.vue:149` 第 5 列起静默丢弃，报错为零 |

WorkBuddy 的回复不只是"排版好看"。它的本质差异是：**把一条回复当成交付物**——固定骨架、表格承载、证据可追溯、未决项单列、收尾给动作。这套东西**不需要升级 Hermes、不需要改架构**，全部落在提示词协议 + 一层轻校验上——而"改提示词就能见效"已由 09-11 那次修复亲自证明。

---

## 1. 取证说明（两侧样本来源）

### 副驾侧：6 条**真实生产回复**（非造数）

取自生产 `tenant_1.db` / `tenant_10.db` 的 `chat_sessions` 表，时间集中在 2026-09-11：

| # | 租户 | 用户问题 | 时刻 | 字数 | 段落 | 加粗 | 表格 | 出卡 | 追问 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | t1 | （设备未配对自动回复） | 09:26 | 165 | 4 | 1 | ✗ | ✗ | ✗ |
| 2 | t1 | 今天该订什么货？ | 14:32 | 1266 | 18 | 11 | ✗ | ✓ | ✓ |
| 3 | t1 | 算一下这个月货损 | 15:37 | 968 | 22 | 3 | ✗ | ✓ | ✗ |
| 4 | t10 | 今天该订什么货？ | 11:24 | 506 | 10 | 21 | ✗ | ✗ | ✗ |
| 5 | t10 | 今天该订什么货？ | 14:38 | 455 | 7 | 11 | ✗ | ✓ | ✓ |
| 6 | t10 | 今天该订什么货？ | 14:41 | 264 | 3 | 1 | ✗ | ✓ | ✓ |

> 附带价值：**同一个问题「今天该订什么货？」在 3 小时内被问了 3 次**，三次形态差异很大（506 / 455 / 264 字；一次无卡两次有卡）——这本身就是"无契约"的直接证据。

### 协议现状（已核实到生产）

| 项 | 状态 |
|---|---|
| `server/ai_roles.py` 与 `server/hermes_core.py` 各一份 `_REPLY_FORMAT_PROTOCOL` | 本地各 **1718 字符、逐行完全相同**（无分叉） |
| 生产 `/opt/hergent-erp/{ai_roles,hermes_core}.py` | 均含「items 的键名用中文」，落地时间 **2026-09-11 14:40** |
| 生产 `/root/.hermes/SOUL.md` | 同步含该条，修改时间 **2026-09-11 14:39** |

### WorkBuddy 侧
本会话（2026-09-12 ~ 09-13）中 WorkBuddy 针对开发/产品问题的实际回复，覆盖返利仪表盘打磨、能力中心去重、提交推送、达成填报口径修复等多个任务。

---

## 2. 四维差异对照

### 2.1 回复格式

| | WorkBuddy | 副驾 |
|---|---|---|
| 章节骨架 | **稳定复用**：交付 / 变更 / 构建 / 对齐效果 / 需确认事项 | **每篇自创**：有的「口径先说清 → 依据 → 下一步」，有的只有三段，有的纯列表 |
| 表格 | 高频：变更清单、验收对照、方案比选 | **0 / 6 条**（前端 `md-table` 能力闲置） |
| 加粗 | 稀疏，只标"老板要记住的数" | 1~21 处浮动；#4 在 506 字里加粗 21 次（**平均每 24 字一粗**），#3 在 968 字里只加粗 3 次 |
| 长度 | 受控，最高信号优先 | 165~1266 字，**差 7.7 倍**；同协议同问题也不同（264 / 455 / 506） |

**关键点**：副驾不是"不会排版"——#2 那条已经有「口径先说清 / 依据 / 下一步建议」的雏形，结构清晰。问题是**这个好结构没被固化成契约**，所以下一篇可能又变成散文。WorkBuddy 的价值在于**结构恒定**：看第二遍时，已知去哪里找结论、去哪里找未决项。

### 2.2 表达方式

| | WorkBuddy | 副驾 |
|---|---|---|
| 措辞 | 面向"懂业务不懂技术的人"：全中文单位（"70 个百分点"而非 pp） | 基本到位，但**有漏网**：#1 直接给出 shell 命令 `hermes pairing approve wecom PAVGNRZ2` 让老板去敲 |
| 不确定性 | **单列一节**「需确认事项」，一条条写清 | 揉进正文："先提醒两个数据口径，别照着账面猛下"——信息在，但**容易被扫读略过** |
| 数据溯源 | 带 commit hash / md5 / 文件路径 / PASS 计数，可复核 | ✅ **做得好**：#3 明写"数据来源：报损单表 + 库存快照"、#2 明写"库存仅覆盖临期仓" |
| 口径披露 | 有，但不如副驾细 | ✅ **副驾强项**："这个 0 是'没录'不是'真没有'"、"销量流水停在 6/15，日均是按 6/9–6/15 推的"——**比多数人类分析师还严谨，务必保持** |

**判断**：表达方式上副驾的**语义质量不输**，输在**呈现纪律**——同样的结论，WorkBuddy 会把它放进固定位置并加粗，副驾会把它埋进第二段第 3 行。

### 2.3 结构组织（含一次已完成的修复）

**正面结论：卡片字段这条已经修好了，而且修法值得推广。**

时间线（全部可复核）：

| 时刻 | 事件 |
|---|---|
| 09-11 14:32 | t1 预报卡：`items:[{name,daily_avg,stock,suggest}]` + 顶层 `data_date/reorder_date/arrival_date` —— 正是后来被点名禁止的英文键 |
| 09-11 14:38 | t10 预报卡：`items:[{name,stock,daily_sales,days_supply,suggest_qty,level}]` + `as_of/cycle_days` —— 又一套全新写法 |
| 09-11 14:39 / 14:40 | SOUL.md 更新；后端两文件部署「items 的键名用中文」等字段规范 |
| 09-11 14:41 | t10 预报卡：`items:[{品名,日均,库存,建议}]` —— **完全合规** |

**同一天、同一个模型、同一个卡型：从"三套写法"到"稳定一套"，只靠改提示词，一分钟见效。**

**但仍有未闭合的部分：前端至今没有校验。**

- `ResultCard.vue:132` 维护着一张 **20 条别名映射表**（`daily_avg/avg/daily/daily_sales` → 全映射到「日均」），这是"漂移曾经是常态"的历史疤痕，也是当前唯一的兜底；
- `ResultCard.vue:141` `DETAIL_KEYS = ['items','rows','detail','list']`——连数组字段名都要猜四个；
- `ResultCard.vue:149` `keys.slice(0, 4)`——**只取前 4 个字段，第 5 个起静默丢弃**；未登记的新键会**以英文原名直接显示给老板**（`ITEM_LABELS[k] || k`）；
- 全程**无校验、无告警、无降级**——异常卡片不报错，只是"显示得不对"，属**静默劣化**。

WorkBuddy 在这层做法不同：它的结构化产物是**工具落盘的文件**（.md/.html/.xlsx），有 schema、有校验、有回读验证（双侧 md5、真机断言）。**"让模型手写 JSON"与"让工具产出工件"是两种可靠性等级**——协议能降低漂移概率，但不能消灭它，所以仍需要最后一层校验。

### 2.4 交互风格

| | WorkBuddy | 副驾 |
|---|---|---|
| 决策 | **选项化**：A/B/C 方案 + 明确推荐 + 理由，等一个短词（"要"/"A"） | 已有 `clarify` 机制（#2/#5/#6 都带了），选项质量不错，但协议**未约束**"必带标签、必带推荐" |
| 收尾动作 | 恒定："要不要我……" —— 把球踢回用户，指向**立即可执行**的一步 | ✅ 有雏形：#3 收在"要不要我先把'效期补录'记成待办？"——**这条已完全达标，应固化为范式** |
| 自主性边界 | 明说"我只看不动手"级别 | ✅ 更明确："AI 只建议，下单还是你点头""这轮我只看不动手"——**做得好，保持** |
| 节奏 | 长任务分阶段自陈（"先取证 → 再改 → 再验"） | 无阶段自陈，一问一答 |

---

## 3. 值得借鉴的具体做法（按优先级）

> 统一思路：**沿用 09-11 已被验证有效的手法——把要求写量化、钉进协议——把覆盖面从"卡片字段"扩到其余维度。**

### P0 · 改提示词协议即可，零风险、即时生效

**① 把"结论先行"升级为「固定回复骨架」**
现在只要求"第一句给答案"，没有章节要求。建议固定为：

```
【结论】一句话答案（老板扫一眼就懂）
【依据】数据 + 口径说明（数据从哪来、截止到哪天、有没有缺口）
【下一步】可执行动作，指向本次对话内立刻能做的事
【需要你确认】不确定项 / 需老板拍板项，一条一行（没有就省略此节）
```

收益：老板第二遍看时知道去哪找什么；也天然治"信息埋没"。

**② 给「加粗」和「长度」定硬指标**（沿用 09-11 钉字段的成功手法）
- 加粗：**一条回复 ≤ 5 处**，只加粗"老板要记住的数字/结论"。
- 长度：**正文 ≤ 400 字**；超了就是该出卡了（结构化移到卡里，正文只留结论 + 口径 + 下一步）。
- 依据：实测 165→1266 字、1→21 处加粗，说明"克制""简洁"这类形容词管不住模型；**"只用这几个字段名"这种量化表述才管得住**（09-11 已证明）。

**③ 对比类结论必须用表格**
凡「≥2 个对象 × ≥2 个指标」（品牌对比、月份对比、品项对比、方案比选），**一律用 markdown 表格**。前端 `utils/md.js` 已完整支持（`<table class="md-table">`），这是**零成本激活的闲置能力**——6 条真实回复 0 条用，纯浪费。

**④ 「不确定就明说」升级为独立小节「需要你确认」**
副驾现在的口径披露写得很好，但混在正文里。单列成节后：老板可以只看这一节做决策，且"要不要追问"变得显性。参考 WorkBuddy 的"需确认事项"。

**⑤ 术语禁令扩到「命令 / 路径 / 英文键 / 内部标识」**
现状禁令只列了 `token / agent / 工作流 / 节点 / 大模型` 五个词，结果漏出 `hermes pairing approve wecom PAVGNRZ2`。建议改为**白名单反向表述**："只出现老板听得懂的业务词；凡出现英文标识、代码、命令、路径、字段名，一律改写为业务语言或省略。"——**这正是 09-11 那次修复的同类思路：列举式黑名单必漏，规则式白名单才严密。**

**⑥ 决策必带选项 + 推荐**
把已有的 `clarify` 机制写进协议：需要老板拍板时给 2~3 个**带标签**的选项（如现在的「就按现有库存算 / 先核对总仓库存 / 按最近销量口径重算」——这几条质量已经很好），并**在正文里给出推荐和理由**。

### P1 · 治本：补上"协议管不到"的最后一层

**⑦ 卡片加"校验 + 显式降级"**（协议已管住字段名，但仍需兜住模型偶发失手）
- 对 `card` JSON 做**字段白名单校验**（`type/title/summary/metrics/points/items/source/status` + items 中文键）；
- 把已有的 `ITEM_LABELS` 别名表**提级为显式契约**（登记进协议，而不是只在前端偷偷兜）；
- 校验不过时**不静默丢弃**：降级为纯文本渲染 + 记一条日志（现在是第 5 列起直接消失，老板永远不知道少了东西）。

**⑧ 协议单一来源**
两份 `_REPLY_FORMAT_PROTOCOL` 手抄同步，靠注释互相提醒——这是结构性隐患（2026-09-11 已踩过一次）。改为从一个共享模块 `from reply_protocol import REPLY_FORMAT_PROTOCOL`，物理上不可能分叉。**本次已核对两份当前逐行相同，但"相同"靠的是人工纪律，不是机制。**

**⑨ 回复质量可观测**
建议落库轻量指标：卡片合规率、平均回复长度、加粗密度、平均段落数。没有度量，协议是否生效只能靠人肉抽查——09-11 那次"违规→合规"的转变，目前除了翻聊天记录无从验证。

### P2 · 交互层
**⑩ 阶段自陈**：长任务（订货测算/对账）回复里显式标注阶段（"先确认口径 → 再出清单"），降低"AI 在干什么"的不确定感。

---

## 4. 明确**不要改**的三处（副驾的既有优势）

对标不是单向砍齐，以下三条副驾做得比 WorkBuddy 的通用回复更到位：

1. **口径披露的严谨度** —— "这个 0 是'没录'不是'真没有'"、"日均是按 6/9–6/15 推的，不是最近 7 天"。这是**低温奶经销商的真实风险点**（数据不全却照下结论会压货压钱），必须保留并强化，不能被"简洁"砍掉。
2. **数据来源标注** —— 每条结论都带来源与截止日期，直接建立可信度。
3. **自主性边界表述** —— "AI 只建议，下单还是你点头"，与老板的心理预期完全对齐。

> 一句话原则：**该严的地方更严（口径/来源），该简的地方更简（排版/篇幅）。**

---

## 5. 落地顺序建议

| 档 | 动作 | 风险 | 生效 |
|---|---|---|---|
| P0-①~⑥ | 改 `_REPLY_FORMAT_PROTOCOL`（两份同改），追加骨架 / 量化指标 / 表格 / 确认节 / 术语白名单 / 选项约束 | 零（纯提示词） | 部署后端即时生效；SOUL.md 需重启 gateway |
| P1-⑦ | 卡片白名单校验 + 别名显式化 + 降级日志 | 低（前端渲染层） | 构建部署 |
| P1-⑧ | 协议常量化到共享模块 | 低 | 部署后端 |
| P1-⑨ | 回复质量指标落库 | 中 | 需要定表 |
| P2-⑩ | 阶段自陈 | 零 | 提示词 |

> 参照 09-11 的经验：P0 全部是改文本，**部署后下次对话即生效**，无需重启 Hermes、无需动模型。

---

## 附录 · 协议增补草案（可直接并入 `_REPLY_FORMAT_PROTOCOL`）

```text
# 回复骨架（固定顺序，老板扫读靠它）
【结论】一句话答案，必须先说结论，不铺垫。
【依据】关键数字 + 口径说明（数据来源、截止日期、缺口）。对比类结论用 markdown 表格。
【下一步】可执行动作，指向本次对话内立刻能做的事。
【需要你确认】不确定项 / 需老板拍板项，一条一行；没有就不写这一节。

# 篇幅与加粗（硬指标）
- 正文不超过 400 字；超过说明该出经营卡了，把明细移进卡里。
- 加粗不超过 5 处，只加粗老板要记住的数字或结论。
- 单段不超过 3 行。

# 对比必用表格
- 两个以上对象比两个以上指标（品牌/月份/品项/方案），一律输出 markdown 表格，不要写成散文。

# 用词白名单（反向表述）
- 只出现老板听得懂的业务词。凡英文标识、命令、代码、文件路径、字段名，
  一律改写为业务语言或直接省略——不要教老板敲命令。

# 需要拍板时
- 给 2~3 个带标签的选项，并在正文里说明你推荐哪个、为什么。
```
