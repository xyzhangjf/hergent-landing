# 小程序「保活 / 长时间不退出」调研报告

> 调研日期：2026-09-21 ｜ 场景：自研订货小程序（企业内部，销售/主管少量人使用）
> 结论先行：**微信小程序在架构上不支持「后台保活」，这不是技术难题，是平台设计。**
> 正确的目标不是「不让它退出」，而是「退出再回来，用户感觉不到」。

---

## 一、三条结论

1. **「保活」这条路在微信小程序里不存在。** 官方明写：进入后台后客户端只维持一段时间（目前 **5 分钟**）就被微信主动销毁。任何宣称能突破的做法，实质是伪造平台白名单权限，**过审即被驳回、上线后被封禁**。
2. **业界主流不是「保活」，而是「状态韧性」。** 大厂小程序的通行做法是把「长时间运行」这件事拆成两半：**要持续的放到服务端常驻**，**要保存的落到本地/服务端草稿**，端上只负责「快速恢复到原样」。
3. **你的场景不需要保活。** 你是 20 人以内的内部订货工具，「切出去接个电话回来发现填的单没了」才是真痛点 —— 这件事用**草稿 + 免重登**可以 100% 解决，且零平台风险。

---

## 二、平台硬约束（先看清天花板在哪）

### 2.1 微信小程序的生命周期与硬限制

| 阶段 | 触发 | 平台行为 | 能否干预 |
|---|---|---|---|
| 前台 | 用户正在用 | 正常执行 | — |
| 后台（前 5 秒） | 点胶囊退出 / 切桌面 / 切微信聊天 | 代码仍在跑，但**网络请求若未完成会返回 `fail interrupted`** | 只能「抓紧存」 |
| 挂起 | 进后台约 5 秒后 | **JS 线程被暂停**，定时器/心跳/长连接全部停摆 | ❌ 不可拦截 |
| 销毁（冷启动） | 后台累计约 **5 分钟** | 容器回收，再次打开 = 全新启动，data 清空 | ❌ 不可拦截 |
| 提前销毁 | iOS：5 秒内收到 ≥2 次内存告警 | 微信主动销毁并提示用户 | ❌ |

> 关于「挂起 5 秒 / 销毁 5 分钟」的具体阈值，不同渠道说法有出入（也有「挂起 30 分钟才销毁」的说法），**但结论一致：不可依赖**。设计时按「随时可能被销毁」来假设。

**其他与保活相关的硬限制：**

| 限制项 | 数值 | 影响 |
|---|---|---|
| WebSocket 连接数 | 同一小程序最多 **5 条** | 不能给每个用户/业务开一条长连接 |
| WebSocket 后台行为 | 进后台约 5 秒被**中断** | 长连接**不能**用来保活 |
| WebSocket 空闲超时 | 实测 30–120 秒无数据即断 | 必须自建应用层心跳（25 秒左右） |
| 网络请求并发 | `request`+`uploadFile`+`downloadFile` 合计 ≤ **10** | 需自建请求队列 |
| 本地缓存上限 | **10 MB** / 小程序 | 不能把整份商品档案塞进去 |
| 消息推送 | 一次性订阅：点一次只能发**一条**；长期订阅**仅政务/医疗/交通/金融/教育**开放 | 工具类小程序**拿不到**长期订阅 |

### 2.2 同类平台横向对比

| 平台 | 后台存活 | 官方后台能力白名单 | 推送召回 | 备注 |
|---|---|---|---|---|
| **微信小程序** | 约 5 分钟后销毁 | `requiredBackgroundModes: audio / location` | 订阅消息（工具类只有一次性） | 最严，白名单类目门槛最高 |
| **支付宝小程序** | 官方文档直接出现「保活期间（5 分钟）」 | 同构模型 | 生活号 / 服务消息 | 同样是「临时会话」模型 |
| **抖音 / 百度等小程序** | 同构（临时会话 + 后台回收） | 各有白名单 | 各自消息通道 | 模型一致 |
| **企业微信自建应用** | H5 同样会被挂起 | — | **应用消息，无类目限制、不需订阅** | ⭐ 对内部场景最友好 |
| **原生 App / 桌面端** | 可真后台常驻（受系统省电策略约束） | 后台任务 / 前台服务 | 系统推送 | 唯一能真「常驻」的形态 |

**一句话**：所有「小程序」都共享同一套设计哲学 —— **用完即走**。想突破它，只能换承载形态，不能在它内部突破。

---

## 三、业界四条主流思路

### 思路 A：官方白名单后台运行（唯一「合法保活」）

**做法**：在 `app.json` 声明 `requiredBackgroundModes`，让小程序在指定能力激活时不进入挂起。

| 能力 | 声明 | 门槛 |
|---|---|---|
| 后台音频 | `["audio"]` | 需**音乐 / 在线教育**等类目；且必须用 `BackgroundAudioManager`，正式版**还需过审** |
| 后台定位 | `["location"]` | 更严：国内主体仅 **12 个开放类目**，需资质证明 + 业务场景流程图 + 录屏 |

**适用场景**：音乐播放器、有声书、导航、运动轨迹、配送追踪。
**对你的适用性**：❌ **不可用**。订货工具与音频/定位无关，申请会被以「**过度申请权限**」驳回；即便伪造音频循环保活，也会带来耗电、审核驳回、上线后被封三重风险。
**优点**：唯一被平台认可的后台持续运行。**缺点**：绑定类目，绝大多数业务拿不到。

### 思路 B：状态韧性（⭐ 业界真正的主流）

「不是不退出，是退出了也看不出来。」三级防御：

| 层级 | 机制 | 覆盖的场景 |
|---|---|---|
| 内存级 | 全局 `Map` 缓存页面状态，`onHide` 存、`onShow` 取 | 5 分钟内切回（热启动） |
| 存储级 | `onHide` 写 `wx.setStorageSync`（草稿/筛选/滚动位置） | 容器被销毁后重进（冷启动） |
| 服务端级 | 草稿同时回写服务端，`onLaunch` 拉取比对 | 换手机、清缓存、多人同一账号 |

**关键 API**：`wx.onAppShow / onAppHide`、`wx.setStorageSync`、`wx.getEnterOptionsSync`。
**反面教材**（高频踩坑）：只在 `onShow` 里 `setData` 恢复 —— 冷启动时 `data` 已是空对象，**无源可溯**；或在 `onUnload` 里清空表单状态 —— 加剧状态断层。

**适用场景**：表单类、录入类、长流程类应用（**正是你的场景**）。
**优点**：零平台风险、零审核、见效快。**缺点**：需要认真设计「草稿的归属与冲突」（同一人两台设备、同一期次多端编辑）。

### 思路 C：服务端常驻 + 推送召回

**做法**：把「需要长时间运行」的事情**全部搬出端上**。

- **服务端常驻**：后端 cron / 定时触发器（Hergent 侧已有的调度机制）负责轮询、算价、汇总、到期提醒 —— 与端上是否活着完全无关。
- **推送召回**：需要用户回来时，用消息把他拉回来。

| 召回通道 | 可用性 | 门槛 |
|---|---|---|
| 小程序订阅消息（一次性） | 可用 | 每次都要用户**主动点授权**；「总是拒绝并记住」后永久静默 |
| 小程序长期订阅 | ❌ 拿不到 | 仅政务民生/医疗/交通/金融/教育 |
| **企业微信应用消息** | ⭐ **可用** | 自建应用**无类目限制、无需订阅**；你们已接企业微信 |
| 公众号模板消息 / 服务通知 | 可用 | 需关注公众号 |
| 短信 | 可用 | 按条计费 |

**适用场景**：所有「用户不在线时也要发生的业务」—— 订单到货提醒、报单截止提醒、审批待办。
**优点**：真正意义上的「长时间运行」。**缺点**：推送是召回，不是保活；用户不点就仍在外面。

### 思路 D：换承载形态（真需要「后台常驻」时）

| 形态 | 后台常驻 | 备注 |
|---|---|---|
| 小程序 | ❌ | 5 分钟回收 |
| 企业微信自建应用（H5） | ❌ 同样挂起 | 但**推送能力无类目限制** ⇒ 内部场景性价比最高 |
| PWA / H5 | 部分（需 Service Worker + 桌面图标） | 后台能力仍受浏览器限制 |
| 原生 App / 桌面端 | ✅ | Android 前台服务、iOS 后台任务；你们已有冻结中的 desktop-app |

**对你的适用性**：**现阶段不建议**。你们已经有 `desktop-app`（冻结），为 20 人内部工具再维护一个 App，运维成本远超收益（这直接撞你的运维红线）。

---

## 四、方案对照总表

| 思路 | 适用场景 | 实现要点 | 平台限制 | 优点 | 缺点 | 对你可行 |
|---|---|---|---|---|---|---|
| A 白名单后台运行 | 音乐/导航/配送 | `requiredBackgroundModes` + 指定 API | **类目绑定 + 审核** | 唯一合法后台运行 | 订货类目拿不到 | ❌ |
| B 状态韧性 | 表单/录入/长流程 | `onHide` 存草稿 → `onShow`/`onLaunch` 恢复 | 无 | **零风险、当天可上线** | 要做冲突设计 | ✅ **首选** |
| C 服务端常驻 + 召回 | 需事后触达 | 后端 cron + 企业微信应用消息 | 订阅消息额度受限 | 真「长时间运行」 | 需用户点击才回来 | ✅ **次选** |
| D 换承载形态 | 真需后台常驻 | App / 桌面端 | 系统省电策略 | 能力最强 | 成本最高 | ⛔ 不建议 |

---

## 五、给「订货小程序」的落地建议

### 痛点重述

你的真实需求极可能是这三件事（而不是「后台常驻」）：

1. 销售在客户店里填单，**切出去接电话/看微信/拍照**，回来发现**填的全没了**；
2. 隔了一段时间重进，**又要重新登录**；
3. 想「一直开着」，是因为**每次都从零开始太费事**。

这三件事全部属于**思路 B**，与保活无关。

### P0 —— 草稿韧性（做完即解决 90% 体验问题）

| 动作 | 落点 |
|---|---|
| `onHide` 落草稿 | 小程序端：把当前报单的 `{ 期次, 客户, 商品行, 数量 }` 写 `wx.setStorageSync`，只存必要字段（**注意 10MB 上限，别存整份商品档案**） |
| `onShow` / `onLaunch` 探测草稿 | 有草稿且未提交 → 弹「上次有未提交的草稿，是否恢复？」（**不要静默覆盖**，用户可能故意放弃） |
| 草稿带时间戳与期次 | 超过合理时长（如 12 小时）或跨期次则提示而不是直接恢复 |
| 提交成功后清草稿 | 避免「上次草稿」反复骚扰 |

### P1 —— 免重登

- 登录态有效期调长 + `onShow` 时静默校验续期（而不是等用户操作时才 401 弹登录）。
- 明确「失效」与「过期」两种回执，分别给不同文案（现在的 401 提示应当能区分）。

### P2 —— 推送召回（用你们已有的企业微信）

- 报单截止提醒、到货提醒、审批待办走**企业微信应用消息** —— **无类目限制、不需订阅**，对内部场景是最优通道。
- 小程序订阅消息只作补充（且要设计成「在用户本来就在点的按钮上顺手勾选」，一次请求最多带 3 个模板）。
- 需要「与端上是否活着无关」的定时任务，一律放后端 cron，**不要依赖小程序端定时器**。

### P3 ——（可选）端上体验小优化

- 引导用户「添加到我的小程序 / 添加到桌面」，提高入口效率（属于召回优化，不是保活）。
- `wx.setKeepScreenOn` 只在用户「正在录入」时开启，离开即关（省电，也避免被误认为后台常驻）。

---

## 六、注意事项与红线

### 🔴 不要做的事

1. **不要伪造类目申请后台音频/定位。** 与业务无关的后台权限，微信审核会以「**过度申请权限**」驳回；你们现在走的是「工具→效率」类目，**不要为了保活动它**。
2. **不要用静默音频循环保活。** 需类目 + 耗电 + 用户投诉，且后台音频本身会被系统省电策略回收（Android 尤其明显）。
3. **不要指望 WebSocket 心跳保活。** 平台在 JS 层就挂起了进程，心跳包发不出去；长连接的正确用法是「**前台维护、后台优雅断开、回前台立即重连**」。
4. **不要诱导/强制订阅消息。** 违反运营规范，用户投诉累积会导致账号消息能力受限；且用户勾了「总是拒绝并记住」后**永久静默**，不可挽回。
5. **不要混淆「用户上滑关闭」与「切后台」。** 上滑关闭是用户主动销毁意图；此时恢复草稿要更谨慎（避免「我都关了你还弹」）。

### ⚠️ 容易忽略的细节

- **冷启动时版本是异步更新的** —— 不要假设「一直开着就是最新版」，关键变更要么用 `wx.getUpdateManager` 强制更新，要么服务端做兼容。
- **iOS 内存告警会直接杀进程** —— 大表格、大图片要主动释放；`wx.onMemoryWarning` 里清理缓存。
- **本地缓存 10MB 是硬上限** —— 草稿要瘦身存储（只存 ID + 数量，不存商品名/规格）。
- **一次订阅只能发一条** —— 召回节奏要按「一次性」设计，不能按「能反复推」设计（这会在立项阶段就埋雷）。
- **企业微信应用消息虽然方便，但仍受企业微信自身的频率与合规约束** —— 只发与业务强相关的通知，不要当营销通道。
- 提供**运营调研问卷**时不要承诺「后台一直运行」—— 这是平台不保证的能力。

### ✅ 验收口径建议

- 真机测：填单 → 切微信聊天 → 锁屏 >5 分钟 → 回来 → **数据仍在**（这是「保活」需求的正确验收方式）。
- 真机测：杀进程后重进 → 草稿可恢复。
- 双设备同账号编辑同一期次 → 冲突提示明确（不静默覆盖）。

---

## 七、参考来源

- 微信官方《运行机制》（后台 5 分钟回收、iOS 内存告警销毁）：同 `developers.weixin.qq.com/miniprogram/dev/framework/runtime/operating-mechanism.html` 的镜像文档
- 腾讯云超级应用服务 · 运行机制（同上口径）
- 微信官方 `wx.getBackgroundAudioManager` 文档（`requiredBackgroundModes: ["audio"]`，正式版需审核）
- 微信开放社区 · 地理位置接口申请条件（12 个开放类目 + `requiredBackgroundModes: ["location"]`）
- 微信官方 · 小程序模板消息能力调整通知（一次性订阅 / 长期订阅仅向政务民生、医疗、交通、金融、教育开放）
- 微信开放社区 · 点击重新进入小程序与数据保持（`onHide` 存 + `onAppShow` 恢复）
- 支付宝小程序《运行机制》与 scheme 文档（出现「保活期间（5 分钟）」同一模型描述）
- 微信小程序 WebSocket 生命周期实践（后台约 5 秒断连、连接数上限 5、25 秒心跳 + 优雅重连）
- 微信小程序页面状态三级缓存实践（内存 Map → Storage → 架构级 Hook）
