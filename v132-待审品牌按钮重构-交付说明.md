# v132 · 待审品牌「忽略」→「不是品牌」+ 二次确认

**状态**：已上线（前端 commit `f8ee720`，已 push origin/main，已 rsync 到生产）
**改动文件**：`hergent-cn-v2/src/pages/BrandArchive.vue`（1 文件，+48 / -3）

---

## 为什么改

v130 P1 时你在 42 秒内处理了 12 条待审品牌，其中 **11 条走了「忽略」**。事后核对发现「忽略」的实际语义是**"这条不是品牌、不建档"**，它**不会改动任何商品数据** —— 但这个含义在按钮上完全读不出来，而且**没有二次确认、没有撤销入口**，一旦批量误点代价很高。

本次就是把这个坑堵上。

---

## 改了什么

| # | 位置 | 前 | 后 |
|---|---|---|---|
| 1 | 按钮文案 | `忽略` | **`不是品牌`**（hover 提示"只从待审队列移除，不改动任何商品数据"） |
| 2 | 点击行为 | 立即执行 | 弹**二次确认弹窗**，需再点「确认不是品牌」 |
| 3 | 确认后提示 | "已忽略" | "已标记为「不是品牌」（未改动任何商品数据）" |
| 4 | 顺修布局 | 说明型弹窗被两列 grid 挤变形 | 单列变体 `.ba-body-stack` + 加宽 580px |

### 确认弹窗正文（关键在把语义讲清楚）

> 确认把「赠品」（命中 43 个商品）标记为**不是品牌**？
> - 这条会从待审队列移除，不再反复提示。
> - 它**不会**改动任何商品的品牌字段 —— 关联商品的品牌名仍是「赠品」。
> - 若要让这些商品彻底从「本期预报」的品牌筛选里消失，请到**商品档案**把它们**停用**（停用可逆，不影响历史订单与统计）。
>
> 还拿不准的话，先点「取消」，留着以后再决定。

第 2、3 条是这次修复的核心 —— 把「档案不建档 ≠ 预报不出现」这个两套数据源的机制，直接写在你要做决定的地方。

### 第 4 项为什么要顺手改

`BrandArchive.vue` 的 `.ba-modal-body` 原本是**给表单用的两列 grid**（`.ba-field` 左列 + 右列，宽备注通栏）。说明型弹窗只有 `<p>`/`<ul>`，塞进两列 grid 会被压成左右两栏、句子断得很碎（第一版截图可见）。加了单列变体后正常；表单弹窗仍保持两列。

---

## 验证记录（真机，非肉眼）

插了一条自检待审条目（命中 7 个商品）走完整链路：

| 步骤 | 结果 |
|---|---|
| 点「不是品牌」 | 只弹验证框，**后端队列仍为 1**（证明不再直接执行） |
| 点「取消」 | 弹窗关闭，队列仍为 1（证明取消真的取消了） |
| 点「确认不是品牌」 | toast「已标记为「不是品牌」（未改动任何商品数据）」+ 列表 1→0 + 空态 |
| 数据核对 | `brand_pending` 自检行已清除；`PENDING=0`；`brands=4`；**`products.brand` 17 组与改动前逐组一致** |

最后一条是关键的**反向证据**：整个 UI 只是个门面，没有任何一行商品数据被触碰。

**自检残留已全部清理，生产无脏数据。**

---

## 需确认事项

1. **待审队列现在是空的** —— 你上次的 11 条已标记为「不是品牌」、1 条建了「新希望」品牌。若之后商品保存 / Excel 导入触发入队，新条目会再出现。
2. **本次 push 顺带带上了同会话的两个「连接手机」提交**（`beeb7bb`、`6456076`）。它们此前已构建进生产 dist、只是没进 git 远端，这次一并推了。
3. **版本号已纠正**：前端仓库 `v131` 已被「连接手机」占用，本次改动记为 **v132**。
4. **未做**（如需可继续）：
   - 已处理条目的「撤销 / 重新入队」入口（当前只能改数据库状态恢复）
   - 品牌档案停用/改名后不回写 `products.brand` 的老问题
   - 商品档案选品牌仍是自由文本输入、无归一（脏值「赠品」就是这么来的）
