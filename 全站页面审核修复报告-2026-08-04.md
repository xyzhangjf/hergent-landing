# 全站页面审核与共性问题修复报告

**日期**：2026-08-04 ｜ **版本**：v107.11（commit 66e5a7c，bundle main-BAg_P1LH.js）
**审核范围**：全站 36 个页面容器 + 全部扩展模块

---

## 一、页面清单（审核覆盖）

| 类别 | 页面 |
|---|---|
| **核心业务** | 经营看板(dashboard)、商品/客户/供应商档案(data)、采销管理(trade/sales/buying)、仓库管理(stock)、收付款(accounts)、客户关系(crm)、数据报表(reports)、员工管理(hr) |
| **财务** | 总账凭证(ledger)、财务管理(finance)、会计科目(gaap)、合并报表(consolidation)、期间结转(periodclose)、工资(salary)、发票(einvoice) |
| **AI/智能** | AI 对话(chat)、AI 时间线(ai-timeline)、AI 今日(today)、AI 技能(skills) |
| **工具/扩展** | 定时任务(tasks)、渠道(channels)、我的成果(results)、设置(settings)、客户360(contacts-v2)、效期看板(expiry)、质量(quality)、RMA(rma)、WMS(wms)、IFRS15(ifrs15)、计费(billing) |
| **弹窗页** | 商品编辑(product-form)、报表设计器(report-designer)、参考数据(refData 系列)、全局搜索 |

## 二、扫描的共性问题模式（7 类）

| 类别 | 扫描方式 | 发现数 | 处理 |
|---|---|---|---|
| A. 硬编码主题色残留 | `grep var(--black)/var(--white)/gray-*`（styles.css 组件类） | 0 | ✅ 无残留（v107.8/107.9 已修） |
| B. 模块白底残留 | `grep background:white/#fff/#f5f5f7`（modules） | 0 | ✅ 无残留 |
| C. 页内重复标题/返回按钮 | `grep ← 返回/页内 h2 与 tab 重复` | 0 | ✅ 无残留（v106.9/107.0/107.10 已修） |
| **D. 响应式适配** | `grep width:Npx;flex-shrink:0` 固定侧栏 + 无 flex-wrap/min-width | **4** | 🔧 **本次修复** |
| E. 敏感文案 | `grep admin123/测试账号/password` | 0 | ✅ 无残留（v107.1 已修） |
| F. 布局错位 | `grep position:absolute 固定宽/固定高 overflow` | 0 | ✅ 弹窗滚动容器均正确 |
| G. 占位文案/交互 | `grep 开发中/TODO/console.log` | 若干 | ✅ 判定为收敛策略/诊断信息，保留 |

## 三、修复详情（D 类响应式，4 个模块）

### 问题描述
4 个模块使用**固定宽度侧栏 + 横向 flex 布局**，在移动端（<768px）会导致侧栏挤压、内容溢出，与全站响应式标准不一致。

### 统一修复方案：`flex-wrap:wrap` + `min-width` 自适应（窄屏自动堆叠）

| # | 模块 | 页面 | 侧栏宽度 | 修复 |
|---|---|---|---|---|
| 1 | `accounting-standards.js` | 会计准则 | 240px | flex 容器加 `flex-wrap:wrap`；侧栏 `min-width:160px`；内容 `min-width:280px` |
| 2 | `consolidation-ui.js` | 合并报表 | 280px | 同上 + 操作按钮行 `flex-wrap:wrap` |
| 3 | `contacts-v2.js` | 客户360 | 300px | 注入 `[data-mobile]` 模式：列表面板全宽 + `max-height:45vh` + 下边框；`_c3Open` 按 `innerWidth<=768` 自动加标记 |
| 4 | `report-designer.js` | 报表设计器弹窗 | 200px | flex 容器 `flex-wrap:wrap`；字段面板 `min-width:140px`；内容 `min-width:260px` |

### 效果
- **桌面端**：布局不变（侧栏 + 内容横排）
- **移动端**：侧栏与内容自动上下堆叠，无挤压/溢出
- 与全站其它页面（参考数据页、档案页等）响应式行为一致

## 四、验证结果

| 验证项 | 结果 |
|---|---|
| `node --check`（4 个修改模块） | ✅ 全部通过 |
| Vite 构建 | ✅ main-BAg_P1LH.js |
| 线上部署 | ✅ 首页 200 + 新 bundle 200 |
| 线上 bundle 含响应式修复代码 | ✅ 18 处（data-mobile/flex-wrap） |
| 备份 | /tmp/static-backup-20260804-0945* |

## 五、判定保留项（非问题，不修改）

| 项 | 判定理由 |
|---|---|
| `console.log` 6 处（contacts-v2/expiry-dashboard/offline-sync/payment-run/pricing-helper/product-form） | 模块版本加载横幅 + 功能性调试输出（离线重放/支付数据），生产诊断有价值 |
| "开发中"占位（archive-hub/operations-hub 未实现项） | 符合"一个功能一个入口"收敛策略，点击有明确 toast 反馈 |
| product-form AI 按钮/搜图/视频占位 | 功能迭代占位，点击有 toast，非死链 |

## 六、可复用检查标准（防止同类问题再发生）

### 1. 新页面/模块开发检查清单
- [ ] 颜色全部使用主题令牌（`var(--bg2/3/4)`、`var(--t1/2/3)`、`var(--bd)`、`var(--p)`），禁止硬编码 `white/black/gray-*/#hex`
- [ ] 侧栏布局必须含 `flex-wrap:wrap` + 侧栏 `min-width` + 内容 `min-width`，或提供移动端堆叠模式
- [ ] 弹窗/表单页必须由顶部 tab 标识当前位置，页内不重复标题/返回按钮
- [ ] 登录/表单页不得出现测试账号明文

### 2. 快速扫描命令（每次改动后执行）
```bash
# 硬编码色残留
grep -nE "background:(white|#fff|#f5f5f7|#fafafa)" static/js/modules/*.js
# 固定侧栏无响应式兜底
grep -rlnE "width:[0-9]{2,3}px;flex-shrink:0" static/js/modules/*.js | xargs grep -L "flex-wrap\|min-width\|data-mobile"
# 敏感信息
grep -rn "admin123\|测试账号" static/ --include="*.js" --include="*.html"
```

### 3. 主题令牌规范（记忆锚点）
- 背景：`--bg2`（卡片）`--bg3`（输入/内嵌）`--bg4`（hover/表头）
- 文字：`--t1`（主）`--t2`（次）`--t3`（提示/弱化）
- 边框：`--bd`（主）`--bd2`（细）
- 品牌：`--p`（主色）`--pl`（亮）`--pd`（暗）
