# 网格内搜索支持条码（Web 端 Forecast.vue）— 2026-09-07

## 交付
| 项 | 内容 |
|---|---|
| 需求 | 预报交叉表/网格 Ctrl+F 搜索框也支持条码（含后 4 位），与小程序、后端搜索口径一致 |
| 改动文件 | `hergent-cn-v2/src/pages/Forecast.vue`（1 处模板 + 4 处逻辑） |
| 构建产物 | `dist/assets/Forecast-CHGKXxNV.js`（245.61 kB） |
| 部署 | rsync → `/opt/hergent-cn-v2/`，chown hergent:hergent，index chunk 已引用新 hash |

## 变更明细
1. 新增统一匹配函数 `rowMatchText(r, f)`（约 10 行）：
   - `name` 子串命中
   - `product_code`（厂家编码）子串命中
   - `barcode` 子串命中 **或** `f.length >= 3` 时末尾匹配（后 4 位场景）
   - 空关键词直接放行（保持原行为）
2. 复用该函数的 4 个位置（原均只匹配 `r.name`）：
   - `rowVisible()` L1595 —— 只读交叉表行过滤
   - `rowShown()` L3593 —— 编辑网格行过滤
   - `filteredRowsForExport()` L3903 —— Excel 导出受筛选影响
   - `findHits` L4417 —— Ctrl+F 回车跳下一匹配
3. L33 搜索框 placeholder → 「搜索商品名 / 条码（后 4 位也行）…」，aria-label 同步。

## 验证
- 构建：`npm run build` ✓ 2.06s（dist 已先备份到 /tmp）
- 生产文件校验：服务器 `Forecast-CHGKXxNV.js` 存在、`index-mmLGCJEm.js` 引用新 hash、新 placeholder 文案已上线
- 逻辑测试（生产真实数据 tenant_1.db 400 条在售商品，SQL 复刻 rowMatchText）：**7/7 PASS**
  - `2988`（后 4 位）→ 3 条，含 6923061002988 ✓
  - 完整条码 `6923644201203` → 精确 1 条 ✓
  - `莎莎` → 1 条 ✓；`蒙牛` → 160 条 ✓；`zzzz` → 0 条 ✓
  - `20`（2 位，不触发末尾匹配）→ 101 条（仅子串）✓；空 → 全量 400 ✓
- 浏览器冒烟：demo 登录 → `#/forecast` 正常渲染，输入关键词无 console 错误

## 未做（按用户选择：2. 不需要）
- 断点①：Web「改单」写入的 `forecast_extra_qty` 回流小程序 —— 本次不修，保持现状。

## 待用户手动
- 小程序端改动仍需在微信开发者工具点「编译」生效。
