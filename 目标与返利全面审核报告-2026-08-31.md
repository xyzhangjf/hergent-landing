# 「目标与返利」模块全面审核报告

- 审核日期：2026-08-31
- 审核范围：前端 `hergent-cn-v2/src/pages/Rebate.vue`（1713 行）+ `src/api/{client.js,modules.js}`；后端 `server/erp_db.py` rebate 核心函数、`server/routers/{rebate_rules.py,rebate_achievements.py,crm.py}`、`server/domain/rebate_calc.py`、`server/server.py` rebate 路由与 RBAC
- 审核方法：静态代码走查 + 跨层契约比对 + 疑似点验证
- 严重级别：**P0** = 确定可复现的功能性错误；**P1** = 影响业务正确性或明显体验缺陷，建议尽快；**P2** = 体验 / 规范 / 性能优化

---

## 〇、审核结论速览

| 级别 | 数量 | 代表问题 |
|---|---|---|
| P0 | 1 | 试算功能前后端契约完全错位，必现失败 |
| P1 | 14 | RBAC 模块归属分裂、删除/修改合同无级联、季度/年度规则无达成填报入口、编辑老合同重算目标缺提示、AI 返利卡忽略阶梯等 |
| P2 | 16 | 硬编码色、emoji/SVG 混用、竞态、未聚焦等体验与规范项 |

---

## 一、功能逻辑完整性 / 业务预期

### R1【P0】试算功能契约错位，点击「计算」必然失败
- **现象**：规则 Tab 点「试算」→ 选规则、填实际值 → 点「计算」→ 结果区空白或显示 `¥undefined`。
- **根因（两端契约完全对不上）**：
  - 前端 `Rebate.vue:803` `runSimulate()` 发送 `{ rule_id, actual_value }`；
  - 后端 `rebate_rules.py:488-508` `/simulate` 读取 `{ dimension, scope_key, period_type, achieved_amount, achieved_qty }` → `rule_id`/`actual_value` 被整体忽略，默认 `dimension="brand"`、`period_type="month"`、`achieved_amount=0` → 永远返回 `total_rebate=0`；
  - 前端 `simResult` 读取 `rebate_amount / effective_rate / achievement / note`，后端返回 `{ total_rebate, results[] }`（每条含 `rebate/achievement_pct/detail`）→ 字段名全部对不上。
- **建议**：把 `/simulate` 改为「按规则 ID 试算」——`{ rule_id, actual_value }`，后端取规则后按 `target_type` 决定用金额还是数量，走 `compute_rule_rebate` 返回 `{ rebate_amount, effective_rate, achievement, note }`。理由：前端交互本来就是「选一条规则填一个值」，按 ID 试算最简单且能消除两端字段错位；改前端去适配旧的「五字段」接口会破坏简洁交互。
- **顺手修**：切规则时清空 `simResult`，避免旧结果残留误导。

### R2【P1】季度 / 年度 / 自定义周期规则无法录达成，永远 0 达成
- **现象**：规则支持 `period_type=quarter/year/custom`，但「达成填报」月份选择器为 `input type=month`（YYYY-MM）；后端 `_norm_month` 只接受 `YYYY-MM/YYYY/MM / YYYYMM / 年月`，对 `2026-Q3` 解析失败 → 季度规则对应达成永远查不到，计提、仪表盘对季度规则恒为 0。
- **建议**：①② 按需二选一——①「达成填报」月份选择器支持季/年键（按规则周期自动切换输入格式）；② 在创建/编辑规则时对 `quarter/year/custom` 提示「当前达成填报仅支持月口径，季度/年度规则需通过 Excel 导入或后端写入」，并扩展 `_norm_month` 支持 `YYYY-Qn`。理由：口径断裂会让用户配置的季度规则在报表里永远"不达标"，属隐性数据错误。
- **同时**：`period_key_for` 的 `quarter → '2026-Q3'`、`year → '2026'` 在 `rebate_achievements.period_month` 里没有写入路径，需统一补齐。

### R3【P1】新录合同永远「销售口径」，无法录采购类
- **现象**：录入弹窗没有 `contract_type` 字段，`create_rebate_contract` 也不传该列 → 新建合同永远落在默认值 `revenue`（销售口径）。而「年度合同」卡片对 `purchase` 有「采购口径返利」文案、本条业务的真实场景（厂家给你的返利）恰恰以采购口径为主。
- **建议**：录入/编辑弹窗增加「合同类型」下拉（销售口径 / 采购口径），POST/PUT 时透传 `contract_type`；后端 `create_rebate_contract` 增参。

### R4【P1】删除合同无级联，孤儿数据 + 已结算合同可被删
- **现象**：`delete_rebate_contract` 仅删除合同行；`rebate_contract_months`、`rebate_accruals` 残留；若合同已结算（`settled=1` 且有应收单 `receivable_id`），删除后应收单仍指向不存在的合同，对账断裂。
- **建议**：① 已结算合同拒绝删除并提示「请先走财务冲销」；② 删除时在同一事务内清理 `rebate_contract_months`、`rebate_accruals`；③ `rebate_target_rules.contract_id` 指向被删合同的规则回置 0 或一并删除（需产品确认）。

### R5【P1】删除规则实为软删除，无恢复/启用入口
- **现象**：`delete_rule` 默认 `is_active=0` 软删除，但前端交互是「删除 + 确认」→ 提示「已删除」→ 规则从列表消失。用户会误以为物理删除；且没有「停用/启用」切换，想恢复旧规则只能通过 API。
- **建议**：操作列改为「编辑 / 停用·启用 / 删除」三态；「删除」做二次确认并明确「物理删除（含历史达成关联將失效）」；停用走软删，启用走 `POST/PUT is_active=1`。理由：与「已停用」筛选配合才能形成完整生命周期管理。

### R6【P1】编辑老合同（无月度分解）首次填月份会静默重算全年目标
- **现象**：老合同（如 108 万、无月度行）编辑时只填 1 月 10 万 → `hasPositive=true` → PUT 全 12 月（1月10万其余 0）→ 后端回写 `target_amount=10万`，全年 108 万被静默替换。`keepOldTarget` 只覆盖「全部清空」场景，不覆盖「部分填充」。
- **建议**：编辑进入时若检测「合同有 target 但无月度分解（`GET /months` 全空）」，弹窗内提示「首次设置月度目标将按 12 个月之和**重算**全年目标（当前 108 万将被替换）」，确认后才允许保存。理由：防止用户只想补 1 月目标却误伤全年。

### R7【P1】结算不可逆：无「反结算 / 冲销」路径
- **建议**：增加反结算接口（作废应收单 → 合同 `settled=0`、`settled_amount=0`、清空 `receivable_id`、月度行 `receipt_id=0` 回退），并加审计日志。理由：财务场景误操作必须有回滚通道，否则只能手工改库。

### R8【P1】AI 返利卡金额忽略阶梯
- **现象**：`server.py:1322` `ai_rebate_card` 用 `achieved × rebate_pct / 100` 线性计算，忽略 `tiers_json`；合同配了阶梯时，副驾「返利进度核算」卡片金额错误。
- **建议**：改用现成的 `calc_rebate_amount(contract, achieved)`（已收敛到 `rebate_calc`），保持一致。

### R9【P1】`get_rebate_progress` 达成口径偏小
- **现象**：`erp_db.py:2515` 有计提时才用 `SUM(achieved_amount)`，未计提月份漏算（「达成填报」里已填但没点「计提」的月份不计入）→ AI 返利卡 / 报表进度偏小。
- **建议**：统一走 `_contract_year_achieved`（当年全口径），有计提单据时再叠加说明或取 max。

### R10【P2】UI 上无「按合同筛选规则」入口
- 后端 `list_rules` 已支持 `contract_id` 过滤，前端未暴露；规则归属合同后只能在年度合同里看 `rule_count`，无法从规则列表反向查看某合同下所有规则。建议规则列表加「归属合同」下拉。

### R11【P2】规则无「复制」功能
- 多 SKU、同结构目标需重复录入 10+ 字段。建议列表加「复制」按钮（复制并清空名称/生效期）。

### R12【P2】合同无搜索/筛选
- 合同增多后只能滚动；建议加名称/年度搜索框。

---

## 二、前端交互体验与页面布局

### R13【P1】录入年度合同弹窗表单过长，保存按钮不可见
- 12 个月网格 + 汇总条后，底部「保存」需滚动才能点，且无「表单过长」分步或吸底按钮。建议：① 月度网格加「折叠/展开」；② 弹窗底部按钮栏 `position: sticky; bottom: 0` 吸底（配背景色防遮挡）。
- 补充：月度网格 4 列在 320px 宽屏单格过窄（`cf-mo-grid` 无响应式折列），建议加 `@media(max-width:480px){ 2 列 }`。

### R14【P1】达成填报行内编辑无防抖、无保存反馈、失败不还原
- `@change` 立即发请求，连续改多个单元格会产生一串无必要请求；保存中无行级 loading；失败仅全局 toast，行内值不回滚。建议：① 300ms 防抖；② 保存中该行输入框禁用；③ 失败后恢复原值并把新值放入「重试」提示。

### R15【P2】仪表盘切月份无竞态保护
- 快速切换统计月份会并发多个 `loadAchievements`，后返回的旧请求可能覆盖新月份数据。建议请求序号或 `AbortController`。

### R16【P2】「达成填报」与「仪表盘」月份互不联动
- 两处各自维护 `achvMonth / dashMonth`，切 Tab 不共享。建议统一为一个月份状态或切 Tab 时同步。

### R17【P2】试算弹窗体验细节
- ① 切规则不自动带出该规则的目标值/单位做提示；② 旧结果未清空（已并入 R1）；③ 试算入口在规则列表右上角，与「目标规则」上下文割裂（可接受，标注即可）。

### R18【P2】编辑弹窗无自动聚焦、回车不提交
- 建议打开弹窗后 `nextTick` 聚焦首个字段；表单内按 Enter 触发保存（需防误触：多行文本除外）。

### R19【P2】空状态无快捷操作
- 仪表盘空态提示「去目标规则创建」但没有可点入口。建议空态文案内嵌跳转按钮。

### R20【P2】删除/结算用原生 `confirm()`
- 与全站弹窗体系（Modal）风格不一致。建议统一为自定义确认弹窗（如结算这类影响财务的要带「输入金额」二次确认更安全）。

---

## 三、UI 样式与视觉规范

### R21【P1】硬编码色与 CSS 变量混用，暗黑主题风险
- `.tag.warn { color:#b45309 }`、`.rr-vs.behind { color:#b45309 }`、`.dt-note { color:#b45309 }` 三处硬编码琥珀色；其余主色用 `var(--p)/var(--war)/var(--dan)`。若项目支持暗黑主题，硬编码色对比度会失配。
- **建议**：统一使用 `var(--war)` / `rgba(var(--war-rgb), …)`，并全模块过一遍暗黑对比。

### R22【P2】图标风格混排
- 仪表盘用 emoji（📅/⏱/⚠），其余按钮用 SVG。建议统一为同一套 SVG 图标（emoji 在 Windows/暗黑渲染不一致）。

### R23【P2】标签色体系不统一
- `.tag.info/.ok/.risk` 用 CSS 变量，`.tag.warn` 硬编码；建议抽成统一的 `.tag.{tone}` 规范，全站复用。

### R24【P2】数字列对齐不一致
- 达成填报的金额/数量 input 右对齐（`.num-input`），但规则列表「目标值」「返利」列虽标 `class="num"` 未看到明确右对齐样式。建议统一 `.num{ text-align:right; font-variant-numeric:tabular-nums }`。

### R25【P2】长金额溢出 KPI 卡
- 「本月预估返利」金额超过 8 位数时可能溢出卡片。建议 `kpi-num` 用 `font-size: clamp()` 或自动缩写（≥1 亿显示「1.2亿」）。

### R26【P2】无打印样式
- 财务/管理层可能打印仪表盘或月度构成表。建议为 `.dash-card / .cc-mtable` 加 `@media print` 基础排版。

### R27【P2】标题区风格不统一
- 仪表盘用 `.dash-hd b`（16px），达成填报用 `.panel-hd b`（字号未显式设），年度合同用 `.dash-hd`。建议统一主标题字号/间距规范。

---

## 四、后端接口性能、健壮性与边界情况

### R28【P1】N+1 查询
- `get_rebate_summary` 对每个合同调用 `_contract_rules`（一次 SQL）+ `_contract_year_achieved`（遍历 12 个月 × 规则，每次内部又可能全表 `_achievement_index`）→ 合同数 N、规则数 M 时约 N×M×12 次内存运算 + N 次查询。
- **建议**：① 一次 `SELECT * FROM rebate_target_rules WHERE contract_id IN (...) AND is_active=1` 取全部规则再分组；② `_achievement_index` 在函数入口构建一次并向下传（目前 summary 已做，但 overview/accrue 各自构建，建议加个请求级缓存或按 `(period_month, dimension, scope_key)` 建索引表）。

### R29【P1】合同 CRUD 无输入校验
- `create_rebate_contract / update_rebate_contract` 对 `year / target_amount / rebate_pct / contact_id` 零校验：负数目标、非法年份（`abc`）、`rebate_pct=999` 均可入库。
- **建议**：加轻量校验（`year` 4 位数字；`target_amount>=0`；`0<=rebate_pct<=100`；`contact_id` 存在性或 0），与 `rebate_rules.validate_rule` 风格一致。

### R30【P1】`set_rebate_contract_months` 无月份格式校验
- 传 `2026-13`、`abc` 也能 upsert 成功；且可传任意数量行（不要求 12 个全量）。
- **建议**：① 每行 `month` 必须匹配 `YYYY-(0[1-9]|1[0-2])`；② 保存时按合同年份强制补齐/清理该年份 12 行（先删后插，事务内），避免孤儿月份（见 R6/R31）。

### R31【P1】修改合同年份后旧年份月度行成孤儿
- `update_rebate_contract` 只更新合同字段，不处理 `rebate_contract_months`；用户把 2026 改成 2027 后保存 → 新 12 月行写入，2026 的 12 行残留 → `GET /months` 按新 year 查不到旧行但库里数据脏。
- **建议**：合同年份变更时，同步把该合同的月度行年份迁移或清理（同一事务）。

### R32【P1】结算无幂等/并发保护（窗口期）
- `settle_rebate` 虽先查 `settled=1` 拦截，但两步之间（查询→插入应收单→置 settled）无事务锁；并发双击或双请求可能生成两张应收单。
- **建议**：`SELECT ... FOR UPDATE`（SQLite 下用 `BEGIN IMMEDIATE`）或加「进行中」互斥标记；前端按钮点击后立即禁用。

### R33【P2】`accrue_rebate` 重复计提覆盖语义
- 未结算月份的重复计提会 UPDATE 覆盖 `accrued_rebate`（合理），但前端按钮文案「计提」不区分「首次/重提」，建议改为「计提 / 重提」，并在重提时二次确认。

### R34【P2】Excel 导入异常无整体回滚
- `import_achievements` 逐行 upsert 后统一 `commit`；若中间某行触发非预期异常，`except` 分支只 `skipped` 计数不抛 500（当前逻辑健壮），但整表 import 无「成功 n 行、失败 m 行」的明细预览。建议导入前先干跑校验预览，再确认导入。

### R35【P2】客户端 20s 超时对 10MB Excel 偏紧
- `api()` 默认 `timeout=20000`；10MB xlsx 在低配服务器解析 + upsert 可能超 20s。建议导入接口显式传 `timeout=60000`。

### R36【P2】前端两步提交（建合同 → 写月度）无原子性
- POST 合同成功但 PUT months 失败 → 合同存在且 target=0、无月度行，用户看到"保存失败"但合同已创建，重试会再建一份。
- **建议**：后端提供 `POST /api/rebate-contracts` 支持一次提交 `{ months: [...] }` 原子写入；或前端在 PUT 失败时提示「合同已创建，可在列表中展开月度构成补填」并提供自动跳转。

### R37【P2】路由重复定义（server.py 与 crm.py）
- `GET/POST /api/rebate-contracts` 在 `server.py:1926` 与 `crm.py:434` 各定义一份（实际生效为 server.py 晚注册者）；PUT/DELETE 只存在于 server.py。双份维护易漂移。
- **建议**：删除 crm.py 中的重复定义，统一收敛到 server.py（或把年度合同相关路由整体迁到 `rebate_rules.py` 同模块）。

---

## 五、前后端数据一致性

### R38【P1】RBAC 模块归属分裂，同一页面跨两个权限域
- `_PATH_MODULE_MAP`：`/api/rebate-contracts`、`/api/rebate-summary` → `sales`；`/api/rebate-rules`、`/api/rebate-achievements` → `stock`。
- 前端「目标与返利」页面同时调这四个接口：角色只有 sales（或只有 stock）时，页面一半 200、一半 403 → 页面功能残缺且无提示。
- **建议**：四个前缀统一归到一个权限域（建议新增 `rebate` 模块或统一归 `sales`），并在前端对 403 做模块级空态提示。

### R39【P1】达成行与规则的匹配键不一致（product 维度按名称导入时）
- 规则 `scope_key=商品ID`；Excel 导入未匹配到档案时 `_resolve_product_key` 返回原文（名称/条码）→ 达成行 key 与规则 key 不同 → `buildAchvRows` 不合并，用户看到「达成数据另起一行」，仪表盘也算不到该规则。
- **建议**：前端构建行时，product 维度先按「名称/条码 → 商品 ID」做一次映射补齐（可复用品牌档案接口）；导入侧保持「未匹配按原文 + 警告」但把警告提升为「该行将不会关联任何目标规则」。

### R40【P1】前端镜像算法与后端 `rebate_calc` 双份实现
- `Rebate.vue` 的 `computeRebateFront`（1033-1054）与 `domain/rebate_calc.compute_rule_rebate` 各写一份。当前逻辑一致，但今后任何一边改动（如阶梯命中、fixed 分支）都会漂移。
- **建议**：短期在两侧文件头部互加注释引用「修改必须同步」；中期可让仪表盘直接复用后端 `POST /rebate-rules/simulate`（按规则 ID 批量试算当月各规则）替代前端镜像。

### R41【P1】`statusOf` 用客户端时钟判「未开始/已结束」
- 前端 `new Date()` 判断生效状态，后端 `_effective_contains` 用服务端日期；跨时区/客户端改时钟时显示不一致。建议：后端在规则列表返回 `status: 'upcoming'|'active'|'expired'` 字段，前端直接消费。

### R42【P2】`expected_rebate`（卡片）与 `est_rebate`（月度表）语义易混
- `get_rebate_summary` 的 `expected_rebate` 按「全年达成」用同一阶梯函数计算；`overview` 的 `est_rebate` 按「当月达成」计算。两者都叫「预计返利」，用户易误读。建议前端加 tooltip 区分「全年预计（按当年已达成口径）/ 当月试算」。

### R43【P2】`trigger_threshold` 边界
- 后端校验 `(0,1]`，但前端详情显示 `*100%`；若绕过 API 直插 `threshold>1`，展示与判断错乱。建议列表/详情对 `threshold` 超出 `(0,1]` 的数据做服务端补丁告警或展示侧 clamp。

### R44【P2】`rebate_pct` 双口径混用风险
- 合同 `rebate_pct`=百分点（3=3%），规则 `rebate_rate`=小数（0.03=3%）。前端 `tierPctText` 已做分支，但创建/编辑接口对 `rebate_pct` 无范围校验（若误填 0.03 会显示 0.03%）。建议后端对 `rebate_pct` 加 `(0,100]` 校验 + 前端 placeholder 强化「百分点，如 3 表示 3%」。

### R45【P2】`saveAchv` 对 `0` 的判定
- `if (!row.achId && !row.achAmount && !row.achQty) return` —— 用户想保存「达成 0」时被拦截（0 为 falsy）。业务上一般不会保存 0 达成，但语义上应允许显式记 0。建议改为 `row.achAmount == null && row.achQty == null` 判定。

---

## 六、问题索引表

| ID | 模块 | 级别 | 一句话 |
|---|---|---|---|
| R1 | 目标规则 | P0 | 试算前后端契约错位，必现失败 |
| R2 | 达成填报 | P1 | 季/年/自定义周期规则无达成录入路径 |
| R3 | 年度合同 | P1 | 新合同永远销售口径，无法录采购返利 |
| R4 | 年度合同 | P1 | 删除合同无级联，已结算合同可被删 |
| R5 | 目标规则 | P1 | 软删除冒充物理删除，无恢复入口 |
| R6 | 年度合同 | P1 | 编辑老合同首次填月份静默重算全年目标 |
| R7 | 年度合同 | P1 | 结算不可逆，无冲销路径 |
| R8 | 通用/AI | P1 | AI 返利卡忽略合同阶梯 |
| R9 | 通用/报表 | P1 | get_rebate_progress 有计提时漏算未计提月份 |
| R10-R12 | 目标规则/年度合同 | P2 | 缺合同筛选、规则复制、合同搜索 |
| R13-R14 | 交互 | P1 | 弹窗过长保存不可见；行内编辑无防抖 |
| R15-R20 | 交互 | P2 | 竞态、月份不联动、试算细节、聚焦、空态、confirm |
| R21 | UI | P1 | 硬编码琥珀色与 CSS 变量混用 |
| R22-R27 | UI | P2 | emoji/SVG 混排、标签色、数字对齐、KPI 溢出、打印、标题风格 |
| R28 | 后端 | P1 | N+1 查询，合同多时性能差 |
| R29-R30 | 后端 | P1 | 合同/月份无输入校验 |
| R31-R32 | 后端 | P1 | 改年份孤儿月行；结算无并发保护 |
| R33-R37 | 后端 | P2 | 重提语义、导入预览、超时、两步提交、路由重复 |
| R38 | 一致性 | P1 | RBAC 模块归属分裂（sales/stock） |
| R39-R41 | 一致性 | P1 | 行匹配键不一致；前端镜像算法；客户端时钟判状态 |
| R42-R45 | 一致性 | P2 | 双「预计返利」语义、threshold 边界、双口径、0 值判定 |

---

## 附录：审核覆盖清单

- 前端：Rebate.vue 全文件（模板/脚本/样式）、api/client.js、api/modules.js rebateApi
- 后端：rebate_rules.py、rebate_achievements.py、rebate_calc.py、erp_db.py rebate 函数族、server.py rebate 路由 + RBAC + ai/rebate-card、crm.py rebate 重复路由
- 数据库：rebate_contracts / rebate_target_rules / rebate_achievements / rebate_contract_months / rebate_accruals 表结构与索引
- 未覆盖：生产数据抽样核对（本文档为静态代码审计，建议按 R 清单在沙箱用真实登录态逐项复现验证）
