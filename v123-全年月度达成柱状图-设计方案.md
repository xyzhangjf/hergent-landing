# 全年月度达成柱状图 · 设计方案（结论式）

> 版本：v123 方案稿 ｜ 日期：2026-09-09 ｜ 范围：Rebate.vue「仪表盘」Tab
> 本文只给结论与字段映射，**不含实现代码**。
> 所有数据结论均基于生产库实测（tenant_1 = 真实租户永诺旗舰店，tenant_10 = demo）。

---

## 0. 结论先行（5 条）

| # | 问题 | 结论 |
|---|---|---|
| 1 | 放哪 | 仪表盘 Tab **KPI 卡片下方、异常预警区上方**，12 栅格满宽，独立卡片。理由见 §1.1 |
| 2 | 销量/返利双单位 | **四柱同屏 + 双 Y 轴**（左=销量、右=返利），**不做**双轴混读的默认值误导；另配「达成率视图」一键切换为百分比同轴。理由见 §1.4 |
| 3 | 品牌筛选单/多选 | **多选 + 可搜索 + 默认全选（即"全部品牌"）**。单选与多选实现成本相同，且"全部=合计"是多选的一个特例。达成率按**加权**（合计达成÷合计目标），非算术平均 |
| 4 | 是否联动其他模块 | **联动**图表本体 + 异常预警 + 条形排行；**不联动** KPI 卡（KPI 是"我这个月总共能拿多少返利"，被筛选改数会误读），KPI 卡加一行灰字说明 |
| 5 | 返利达成要不要补数据入口 | **一期不补**。现有 `POST /api/rebate-rules/simulate-batch` 可精确推算，口径与仪表盘现有"本月预估返利"一致，零新增维护成本。**二期**若要"实际到手返利（计提/核销/发放）口径"，才必须补入口，方案见 §3.4 |

---

## 1. 位置与 UI

### 1.1 布局（插入点）

仪表盘 Tab 现有顺序（自上而下）：

```
.dash-card
  ├─ .dash-hd        标题 + 统计月份（type=month）
  ├─ .dash-kpi       4 个 KPI（本月预估返利 / 预警规则 / 已达标 / 平均达成率）
  ├─ .dash-time      本月时间进度参照
  ├─ .dash-legend    图例
  ├─ .dash-nodata    空数据提示
  ├─ .dash-anom      异常预警区          ← 新增图表插在它【上面】
  └─ .dash-rank      条形排行（按风险升序）
```

**新图表插在 `.dash-legend` 之后、`.dash-anom` 之前。**

理由：
- KPI 是"本月一个数"（点），柱状图是"全年 12 个月"（线），点 → 线 → 问题，符合阅读动线；
- 图表下方的预警区与排行是"看图发现问题后的定位动作"，被图表引出，放在下面最顺；
- 放在 KPI 上方会与"统计月份"控件冲突（图表自带年份控件，两个月份选择器上下相邻会让人分不清哪个管哪块）。

### 1.2 栅格与尺寸

| 项 | 桌面（≥1200px） | 平板（768–1199） | 窄屏（<768） |
|---|---|---|---|
| 占位 | 12 / 12 满宽 | 12 / 12 满宽 | 12 / 12 满宽 |
| 图表高 | 300px（含 X 轴标签 24px） | 280px | 240px |
| 卡片上/下间距 | 18px / 18px | 16px | 12px |
| 卡片内边距 | 16px 18px | 14px | 12px |

图表区内部横向划分：左侧 Y1 轴标 56px、绘图区自适应、右侧 Y2 轴标 64px。

### 1.3 图表元素

**X 轴**：1–12 月，固定 12 个刻度（`01`…`12`，中文环境显示 `1月`…`12月`）。
- 刻度值恒定，不随筛选变化 —— 保证"全年"语义稳定，缺失月份留空而不是压缩 X 轴。
- 当年已过去的月份正常显示；**当前月**刻度下方加一个 2px 圆点标记；未来月份若已有目标值则显示目标柱、达成柱为空。

**每月 4 根并排柱**（顺序固定，不可拖拽重排）：

| 序 | 柱 | 语义 | 填充 | 描边 | 十六进制 |
|---|---|---|---|---|---|
| 1 | 销量目标 | 计划 | 浅灰蓝 40% | 灰蓝实线 1px | `#cbd5e1` / 描边 `#94a3b8` |
| 2 | 销量达成 | 实际 | **品牌青实色** | 无 | `#06b6d4` |
| 3 | 返利目标 | 计划 | 浅琥珀 40% | 琥珀实线 1px | `#fde68a` / 描边 `#fcd34d` |
| 4 | 返利达成 | 实际 | **琥珀实色** | 无 | `#f59e0b` |

配色规则：**"计划 = 浅底 + 描边，实际 = 实色"**，同色系成对。这与页面现有条形排行（`.rr-bar-fill` 的 done/risk/ontrack）和 KPI 语义一致，不引入新色板。

柱宽：每组 4 根，组内间距 2px，组间间距 = 组宽的 40%。

**达成率的视觉编码**：不在柱子上做颜色变化（会与系列色冲突），改为**在达成柱顶部外挂一个状态点**：
- 达成率 ≥ 100%：绿色小圆点 `#16a34a`
- 80% ≤ 达成率 < 100%：不标点（正常）
- 达成率 < 80%：红色小圆点 `#dc2626`

> 注：中国区习惯里"红=涨"，但此处红色仅作**风险警示**而非涨跌，与现有 `.val-warn` 一致，不冲突。

**图例**：图表右上角，4 项横排 + 1 个"切换视图"分段控件。图例项**可点击**（点一次隐藏该系列，再点恢复），便于临时只看两根。

### 1.4 双单位处理（本方案最关键的一处决策）

**问题**：销量可能是金额（万元）也可能是数量（件），返利恒为金额；且量级差约 10 倍（目标 100 万 vs 返利 10 万）。同轴下返利柱几乎不可见。

**方案：主视图 = 四柱同屏 + 双 Y 轴**

| 轴 | 绑定系列 | 单位 | 说明 |
|---|---|---|---|
| 左 Y1 | 销量目标 / 销量达成 | 万元（金额度量）或 件（数量度量） | 轴标题必须带单位，如「销量（万元）」 |
| 右 Y2 | 返利目标 / 返利达成 | 元 | 轴标题「返利（元）」 |

双轴的已知误读风险，**用三件事对冲**：
1. 两根 Y 轴**零基线严格对齐**，且 Y2 量程按 Y1 量程 × 全局返利率中位数自动推算，避免人为拉伸；
2. 轴标题强制带单位 + 柱顶标注数值（12 月 × 4 = 48 个数字会太挤，改为**仅标注"达成"两根柱**的数值，"目标"柱靠悬浮查看）；
3. 图例下方固定一行灰字提示：`左右轴不同量级，柱高不可跨色系直接比较；看结构请切「达成率视图」`。

**辅助视图：达成率视图（一键切换）**
- 每月 2 根柱：销量达成率%、返利达成率%，**同一 Y 轴 0–120%**，100% 处画一条虚线。
- 这个视图彻底消除量级差，是给老板看"哪个月掉队"的主视图。

**建议默认值**：首次进入默认**四柱同屏**（符合"看绝对值"的直觉），但把切换控件做得足够显眼；用户切过一次后记住选择（localStorage）。

**跨度量混合的处理**（当前 tenant_1 全是 `amount`，风险暂未爆发，但必须先定规则）：
- 若某月同时存在 `target_type=amount` 与 `quantity` 的生效规则，**默认只聚合与"多数规则度量"一致的那部分**，并在图表下方提示：`本月有 N 条数量口径规则未计入（当前视图：金额）`；
- 不允许同一根柱混加金额与件数 —— 这是硬红线。

### 1.5 悬浮提示（Tooltip）

鼠标悬停在**月份组**上（不是单根柱，避免 4 根柱太窄难点中），弹出一个卡片：

```
2026年8月                          ← 标题
─────────────────────────────
销量目标      ¥1,000,000   （蒙牛低温）
销量达成      ¥880,000     达成率 88.0%  ⚠
返利目标      ¥100,000     （10.0% 档）
返利达成      ¥88,000      达成率 88.0%  ⚠
─────────────────────────────
距目标差额    ¥120,000
影响返利      ¥12,000
```

要点：
- **四项数值 + 两项达成率**全部给出（用户要求）；
- 达成率 < 100% 时数值右侧加 ⚠ 图标；
- 单品牌筛选时，底部追加"距目标差额 / 影响返利"两行（聚合视图下这两行无意义，隐藏）；
- 金额用 `¥` + 千分位，超过 1 万时用「万」缩写并保留 hover 全量（复用现有 `fmtShort`）。

### 1.6 空数据 / 加载态

| 状态 | 表现 |
|---|---|
| 首次加载 | 图表区骨架屏：12 组 × 4 根灰色占位柱，高度随机 30–70%，1.2s 呼吸动画（复用现有 `.skel-line`） |
| 全年无任何目标 | 整卡替换为现有 `.state-empty` 样式 + 按钮「去创建品牌目标」→ 跳 `mainTab='rules'` |
| 有目标但无达成 | 正常渲染目标柱，达成柱高度 0，X 轴下方灰字：`灰色月份尚未填报达成，去「达成填报」补录` |
| 筛选后无数据 | 图表区居中提示：`「XX」品牌 2026 年暂无目标数据`，右侧给「清除筛选」按钮 |
| 请求失败 | 图表区提示 + 「重试」按钮，**不阻塞**下方预警区与排行（它们用的是已加载的 rules） |

### 1.7 窄屏适配

<768px 时 48 根柱必然挤压，两个备选：

| 方案 | 做法 | 评价 |
|---|---|---|
| A 横向滚动 | 图表容器 `overflow-x:auto`，绘图区 `min-width:720px`，底部提示「← 左右滑动 →」 | ✅ **采用** |
| B 变形 | 降为两柱、目标改折线 | ❌ 数据结构变了，用户要的四柱语义丢失 |

采用 A，理由：数据结构不变、实现零成本、老板在手机上也只看 2–3 个月，滑动可接受。同时窄屏下**默认切到达成率视图**（2 根柱，一屏装得下），并把 Tooltip 改为点击触发（避免移动端 hover 不可用）。

---

## 2. 筛选交互

### 2.1 控件形态

放在图表卡片头部右侧，与左侧标题「全年月度达成」同行：

```
[统计年份 ▾ 2026]   [品牌 ▾ 全部品牌(3) ▾ 可搜索·多选]   [四柱同屏 | 达成率]
```

- **统计年份**：下拉，取 `rebate_target_rules.target_year` 的去重值 ∪ 当前年 ∪ 生效期涉及年份。默认当前年。
- **品牌筛选**：多选下拉 + 搜索框，直接复用 `Forecast.vue` 已有的 `brandSel` 实现（多选 + `.filter-chip` 已选标签 + 一键清除），数据源 `/api/brands?include_inactive=1`（现有接口，3 行档案）。

### 2.2 单选取多选 —— 结论：**多选**

| 维度 | 说明 |
|---|---|
| **默认态** | 全部品牌勾选，标签显示「全部品牌」，等价于用户要的"全部品牌" |
| **未选/全选** | 聚合展示**全部品牌四项指标合计**：目标=Σ、达成=Σ、返利率按加权 |
| **选定 1 个** | 只展示该品牌，Tooltip 追加"距目标差额 / 影响返利" |
| **选定 N 个** | 展示所选品牌的合计（业务场景：老板常把「蒙牛低温 + 蒙牛鲜奶」当一个盘子看） |

选多选而非单选的理由：
1. 实现成本与单选**完全相同**（都是数组，只是 UI 上限制勾选数）；
2. 单选满足不了"蒙牛低温+蒙牛鲜奶"这类真实诉求，而这类诉求在低温奶经销商里是常态；
3. "全部品牌"就是多选的全选态，不需要单独写分支。

### 2.3 聚合口径（重点，避免歧义）

| 指标 | 聚合方式 |
|---|---|
| 销量目标 | 各品牌**直接求和**（同度量才可加） |
| 销量达成 | 各品牌直接求和 |
| **销量达成率** | **加权**：Σ达成 ÷ Σ目标。**不是**各品牌达成率的算术平均 |
| 返利目标 | 各品牌直接求和 |
| 返利达成 | 各品牌直接求和（由各自规则独立试算后再加，避免跨品牌误用返利率） |
| 返利达成率 | 加权：Σ返利达成 ÷ Σ返利目标 |

**为什么强调加权**：蒙牛低温 100 万达成 88%、简爱 8 万达成 112%，算术平均 = 100%（假象达标），加权 = 89.8%（真实）。**必须用加权**，并在图例区小字标注「达成率 = 合计达成 ÷ 合计目标」。

**跨度量红线**：金额口径规则与数量口径规则**不可相加**。若筛选结果里两种都有，只显示与多数规则一致的那一组，并提示被排除的规则数。

### 2.4 联动范围

| 模块 | 是否联动 | 理由 |
|---|---|---|
| 全年柱状图本体 | ✅ 是 | 就是它自己的筛选器 |
| 异常预警区（`.dash-anom`） | ✅ 是 | 预警也应该聚焦到所选品牌，否则筛了品牌还看到别的品牌的预警，很割裂 |
| 条形排行（`.dash-rank`） | ✅ 是 | 排行本就按规则逐条列出，按品牌过滤是符合直觉的 |
| **KPI 卡（`.dash-kpi`）** | ❌ **否** | KPI 回答"我这个月总共能拿多少返利"，是全盘数字。被筛选悄悄改掉会导致老板误判整体盘子。卡片下方加灰字：`本卡始终统计全部品牌，不受上方筛选影响` |
| 规则列表 / 入口卡片（下方工具栏区域） | ❌ 否 | 属于「目标与返利」管理区，与仪表盘分析区职责不同 |
| 其他 Tab（达成填报 / 返利结算） | ❌ 否 | 跨 Tab 隐式联动是最大的误读来源 |

---

## 3. 数据来源与补齐（字段级）

### 3.1 字段级数据映射表

| 柱 | 数据源 | 表 / 接口 | 关键字段 | 统计口径 | 单位 |
|---|---|---|---|---|---|
| **销量目标** | 主 | `rebate_target_rules`（租户库） | `monthly_amounts` (JSON `{"08":1100000,"09":900000}`)、`target_value`、`period_type`、`target_year`、`effective_start/end`、`dimension`、`scope_key/name`、`target_type`、`priority`、`is_active` | ① 年度规则（`period_type='year'`）→ 取 `monthly_amounts['MM']`；<br>② 单期月规则（`period_type='month'`）→ 取 `target_value`，按 `effective_start` 所在月归位（可跨多月则按天占比分摊或整月计入，见待确认 #4）；<br>③ 同一 (月, dimension, scope_key) 命中多条生效规则 → 取 `priority` 最大者，并在 Tooltip 标注"存在 N 条重叠规则" | 元（存）→ 万元（显） |
| **销量达成** | 主 | `rebate_achievements`（租户库） | `period_month` (`'2026-08'`)、`dimension`、`scope_key`、`scope_name`、`actual_amount`、`actual_qty`、`source` | 按 `period_month` 精确匹配月份；`scope_key` 与规则 `scope_key` 对齐（product 维度需先经 `resolveProductKey` 名称→ID，现有逻辑已实现）；`target_type='amount'` 取 `actual_amount`，`'quantity'` 取 `actual_qty` | 元 / 件 |
| **销量达成** | 备（切换源） | `sale_order_items` JOIN `sale_orders` JOIN `products` | `so.order_date`（取 `substr(1,7)`）、`so.status`、`soi.amount`、`soi.quantity`、`p.brand` | 按 `order_date` 月份归集；`status` 排除 `cancelled`/`rejected`/`draft`（tenant_1 现存 580 单全部为 `signed`，暂无脏数据）；按 `p.brand` 分组 | 元 / 件 |
| **返利目标** | 推算 | 由销量目标 × 返利率得出 | `monthly_rates` (JSON) 或 `rebate_rate`、`trigger_mode`、`tiers_json`、`rebate_basis` | = 该月**满额（100%）时应返金额**：<br>· `rebate_basis='rate'` → 月目标 × 月返利率（`monthly_rates['MM']`，缺省回退 `rebate_rate`）；<br>· `trigger_mode='tiered'` → 取 100% 档位对应返利；<br>· `rebate_basis='fixed'` → `rebate_amount` | 元 |
| **返利达成** | 推算 | `POST /api/rebate-rules/simulate-batch` | 入参 `items:[{rule_id, basis_value}]` + `ref_date`（月末日）；出参 `results[].rebate` | **后端唯一算法**（`domain/rebate_calc.compute_rule_rebate`），前端绝不镜像。<br>单次上限 500 条，全年 12 月 × 品牌数 ≤ 60，一次请求足够。<br>`basis_value` = 该月销量达成；`ref_date` = 该月最后一天（用于阶梯版本 `rebate_tier_versions` 按生效期锁定口径） | 元 |

### 3.2 取数流程（一次渲染的请求数）

```
1. GET  /api/rebate-rules?include_inactive=1          （已有，页面启动时已加载 → 复用 rules.value）
2. GET  /api/rebate-achievements?month=YYYY-MM   ×12  （已有接口，按月查；或新增批量参数一次拉全年）
3. GET  /api/brands?include_inactive=1                （已有，页面启动时已加载 → 复用 brandList.value）
4. POST /api/rebate-rules/simulate-batch              一次 48–60 条试算
```

> **优化建议**：第 2 步 12 次串行请求会拖慢首屏。建议给 `GET /api/rebate-achievements` 增加 `from_month` / `to_month` 范围参数（或 `year=2026`），一次拉全年。**这是本方案唯一需要新增的后端能力**，改动量约 10 行。

### 3.3 「返利达成」数据入口分析（重点）

#### 现状实测

| 表 | 生产行数（tenant_1） | 能否支撑「返利达成」 |
|---|---|---|
| `rebate_accruals`（计提） | 2 行 | ⚠️ **不能**。字段有 `month` / `achieved_amount` / `accrued_rebate`（2026-08: 97万 → 计提 10800），**但绑定的是 `contract_id` 而非 `rule_id`**，且 `rebate_contracts` 表 **0 行**（这两条是孤儿数据）。无品牌/维度字段，**无法按月 × 品牌聚合** |
| `rebate_claims`（核销/兑付） | 0 行 | ❌ 功能未启用 |
| `rebate_contract_months`（合同月目标） | 12 行 | ❌ 绑合同，非规则；`rebate_contracts` 为空，同样孤儿 |
| `rebate_achievements`（达成填报） | 4 行 | ⚠️ 只有 `actual_amount` / `actual_qty`，**没有返利金额字段** |

#### 判断：能否由现有数据推算？→ **能**

```
返利达成 = f(月销量达成, 该月生效规则, 阶梯版本)
         = POST /simulate-batch { rule_id, basis_value=月达成, ref_date=月末 }
```

- 算法已在后端 `domain/rebate_calc` 落地，且是当前仪表盘"本月预估返利"的**同一套算法**，口径天然一致；
- 阶梯、计法（graduated/full）、舍入（`scale_type`/`rounding_mode`/`rounding_digits`）全部由后端处理，前端不碰；
- 支持 `ref_date` → 自动命中 `rebate_tier_versions` 的历史版本，保证"8 月按 8 月的阶梯口径算"，与「版本」按钮管理的口径一致。

#### 结论：**一期不补数据入口**

| 维度 | 说明 |
|---|---|
| **结论** | **不需要**新增返利达成录入入口 |
| **理由 1** | 可精确推算，且推算口径 = 现有仪表盘口径，不存在"两套返利数字"的风险 |
| **理由 2** | 补入口 = 让老板每月多录一次数。经销商一人公司，任何新增录入都是负担，且录入值与推算值不一致时反而产生信任危机 |
| **理由 3** | 现有 `rebate_accruals` 是孤儿数据（合同表空），说明"实际计提"这条链路在业务上尚未真正跑通，此时强推录入只会产出垃圾数据 |
| **代价** | 柱状图的「返利达成」语义是**预估应返**，不是**实际到手**。UI 上必须诚实标注：图表副标题写 `返利为按规则预估应返，非实际到账` |

#### 二期：什么情况下必须补？

当业务提出以下任一诉求时，才需要补：

| 触发条件 | 需要的数据 | 建议入口 |
|---|---|---|
| 要看"实际计提了多少"（财务视角） | 月度实际计提金额 | 「返利结算」Tab 增加月度计提录入表 |
| 要看"核销/到账了多少"（现金视角） | 月度实际核销金额 + 核销日期 | 启用 `rebate_claims`，补 `month` 字段 |
| 要对比"预估 vs 实际"的差异 | 上述两者 + 推算值 | 图表加第 5、6 根虚线柱或改折线叠加 |

**若二期要补，方案如下**：

| 项 | 内容 |
|---|---|
| **补在哪** | 优先复用 `rebate_accruals`，**新增三列**：`rule_id INTEGER DEFAULT 0`、`dimension TEXT DEFAULT ''`、`scope_key TEXT DEFAULT ''`（保留现有 `month` / `achieved_amount` / `accrued_rebate`）。不建议新建表 |
| **录入方式** | ①「返利结算」Tab 月度表格手工录入；② Excel 导入（复用 `rebate_achievements` 已有的 `/import` 模板机制，含 `_map_headers` 表头映射） |
| **责任方** | 经销商老板或文员（一人公司即用户本人） |
| **更新频率** | **月度**，次月 5 日前补录上月；与现有「达成填报」同节奏 |
| **校验规则** | ① 同一 (rule_id, month) 唯一，重复录入走 upsert 不报错；<br>② `accrued_rebate` 与 `simulate-batch` 推算值偏差 **> 10% 且 > 1000 元**时，前端黄色提示"与预估差异较大，请复核"，**不阻断保存**；<br>③ `achieved_amount` 与 `rebate_achievements.actual_amount` 不一致时提示，允许覆盖（实际计提基数可能与填报达成不同）；<br>④ 金额 ≥ 0、月份格式 `YYYY-MM` 硬校验 |
| **不做什么** | 不做自动核销、不做与财务凭证的对接（超出 AI 层职责，也不该由非技术创始人扛） |

### 3.4 四项指标单位与币种

| 指标 | 存储单位 | 展示单位 | 币种 |
|---|---|---|---|
| 销量目标 / 达成（金额度量） | 元（REAL） | 万元（≥1万时），保留 1 位小数 | CNY `¥` |
| 销量目标 / 达成（数量度量） | 件（REAL） | 件，整数 | — |
| 返利目标 / 达成 | 元（REAL） | 元（<1万）/ 万元（≥1万） | CNY `¥` |

> 与项目既定口径一致：**存储单位元，录入与展示统一「万元」**。

---

## 4. 待确认问题清单

按阻塞程度排序，**前 3 条不定无法开工**。

| # | 问题 | 为什么关键 | 我的建议 |
|---|---|---|---|
| **1** | **销量达成取哪个源？** `rebate_achievements`（人工填报，tenant_1 有 2026-08/09 共 4 行）与 `sale_order_items`（销售单，tenant_1 有 2026-04/05/06 共 580 单）**在真实租户里完全不重叠** | 取错源 = 全年 12 月里 10 个月是空的，图表等于废掉 | **主=填报，备=销售单**，图例区提供「达成来源」切换。但请确认业务真相：销售单只有 Q2 是因为**还没导入**，还是**已弃用**？ |
| **2** | 现有 4 条规则**全是 `period_type=month`、`monthly_amounts` 为空**（蒙牛低温/简爱的 8 月、9 月） | 全年视图只有 8、9 两个月有目标柱，其余 10 个月空白 | 建议用户先补建 1–12 月的年度规则（v122 已支持 12 月分解），否则图表上线即"半空"。**要不要我顺带把年度规则建好？** |
| **3** | `products.brand` 与 `brands` 档案**口径不一致**：商品表有 `蒙牛低温/蒙牛鲜奶/蒙牛/蒙牛常温/蒙牛奶酪/君乐宝/简爱/新希望/友芝友/赠品/空`，档案表只有 `蒙牛低温/蒙牛鲜奶/简爱` | 若走销售单聚合，"蒙牛常温""蒙牛奶酪""蒙牛"会被算成独立品牌或漏掉 | 若第 1 条选销售单源，**必须先做品牌归一化映射**（`蒙牛*` → `蒙牛低温` 或按需建子品牌）。这是一次性配置工作，约 10 分钟 |
| 4 | 单期月规则跨月生效时（如 `effective_start=2026-08-15`、`end=2026-09-14`）的 100 万目标，**整月计入 8 月还是按天分摊到 8/9 月？** | 直接影响两个月的柱高 | 建议**按自然月归位**：目标落在 `effective_start` 所在月。理由：返利结算本来就是按月对账，分摊会让数字出现"半个月的零头"，反而看不懂 |
| 5 | 同一 (月, 品牌) 存在多条生效规则时的取值（现有 `/conflicts` 已能检出重叠） | 决定柱高取哪条 | 取 `priority` 最大者；`priority` 相同取 `id` 最大者。Tooltip 标注"存在 N 条重叠规则，已取优先级最高" |
| 6 | 「返利目标」是取**满额 100% 应返**还是**当前达成对应档位的返利**？ | 前者是"标杆线"，后者是"当前档位天花板"，语义完全不同 | 取**满额 100% 应返**（与"目标"的语义一致：目标就是"做到 100% 能拿多少"） |
| 7 | 图表默认视图：四柱同屏 还是 达成率视图？ | 影响第一眼看到的形态 | 桌面默认**四柱同屏**（符合"看绝对值"直觉），窄屏默认**达成率视图**（一屏装得下）。用户切换后记住选择 |
| 8 | 是否需要给 `GET /api/rebate-achievements` 加 `year` / 月份范围参数？ | 现状要发 12 次请求拉全年，首屏会卡 | **建议加**（后端约 10 行）。或退而求其次：12 个请求并发而非串行，也能接受 |

---

## 5. 技术落点备注（给实现阶段，不影响方案结论）

| 项 | 结论 |
|---|---|
| **图表库** | 项目当前依赖只有 `vue / vue-router / pinia / xlsx`，**无任何图表库**。建议**纯 SVG 自绘**（48 根矩形 + 坐标轴 + Tooltip，约 200 行），**不引入 ECharts**。理由：图表形态固定、无缩放平移等交互需求，引入 ECharts 会让 Rebate chunk 增加约 330KB(gzip)，且违背"轻量 AI 层"定位 |
| **组件归属** | 新建 `src/components/rebate/MonthlyAchvChart.vue`，与 v122 已拆分的 `components/rebate/` 同目录；数据组装逻辑放 `useRebateTargetForm.js` 同级的 `useMonthlyAchv.js`（纯函数，可单测） |
| **算法归属** | 返利一律走 `POST /api/rebate-rules/simulate-batch`，**前端不写任何返利计算**（v113 已确立的纪律） |
| **与现有代码的关系** | 复用 `rules.value` / `brandList.value` / `achievements.value`（页面已加载），新增的只有"按年聚合"与"批量试算"两步 |

---

## 6. 一页速览

```
┌─────────────────────────────────────────────────────────────┐
│ 仪表盘  [目标与返利] [达成填报] [返利结算]                      │
├─────────────────────────────────────────────────────────────┤
│ 本月预估返利 ¥xx  │ 预警 x │ 已达标 x/x │ 平均达成率 xx%      │  ← KPI（不联动筛选）
│ 本月时间进度 ………                                              │
├─────────────────────────────────────────────────────────────┤
│ 全年月度达成            [2026▾] [全部品牌(3)▾] [四柱│达成率] │  ← 新增
│  销量(万元)                                    返利(元)      │
│  │ ░░██ ░░██ ░░██ ░░██ …  12 组 × 4 柱                │      │
│  └──1月──2月──3月──────────────────●9月────────12月          │
│  ▪销量目标 ▪销量达成 ▪返利目标 ▪返利达成                       │
│  达成率 = 合计达成 ÷ 合计目标 · 返利为预估应返非实际到账        │
├─────────────────────────────────────────────────────────────┤
│ ⚠ 异常预警（联动筛选）                                        │
│ ① 蒙牛低温 88.0% … 条形排行（联动筛选）                       │
└─────────────────────────────────────────────────────────────┘
```
