# 全站"修改记录"按钮补齐 — 页面清单与说明（2026-08-04, v107.42）

## 一、全站档案类页面梳理

| 页面 | 入口 | 实体表 | 修改记录 | 说明 |
|---|---|---|---|---|
| **商品档案** | 侧边栏「档案管理」/ data tab | products | ✅ 已有（v107.41） | 表格行 ··· 菜单 + 卡片视图 |
| **客户档案** | data tab → 客户 | contacts(type=customer) | ✅ **本次新增** | 表格行 + 卡片视图 |
| **供应商档案** | data tab → 供应商 | contacts(type=supplier) | ✅ **本次新增** | 表格行 + 卡片视图 |
| 品牌/分类/单位/仓库等字典 | 「档案管理」openRefData | 各字典表 | ⚠️ 未纳入 | 见下方说明 |
| 员工档案 | 设置 → 员工管理 | hr_employees | ⚠️ 未纳入 | 见下方说明 |

## 二、判断标准

与已实现的商品档案对齐：**独立档案实体 + 有增删改操作 + 修改影响业务数据** 的页面应具备"修改记录"。
商品/客户/供应商是 data tab 三大核心档案，同级别同权重 —— 客户/供应商本次补齐。

## 三、本次新增（2 页面 / 3 处按钮入口）

| 页面 | 按钮位置 | 操作 |
|---|---|---|
| 客户档案 | 表格行操作区（row-actions，编辑按钮右侧）+ 卡片视图操作区 | 🕐 图标，title「修改记录」 |
| 供应商档案 | 表格行操作区（row-actions，编辑按钮右侧）+ 卡片视图操作区 | 🕐 图标，title「修改记录」 |

点击弹窗（与商品档案**同一组件** `_renderChangeLogModal`，样式/交互/列定义完全一致）：
时间（倒序）｜操作人｜动作（创建/修改）｜修改字段｜修改前 → 修改后

## 四、实现要点

- **通用日志表** `entity_change_logs`（entity_type + entity_id，客户/供应商共用 entity_type='contact'），
  写/查时幂等建表（租户库懒建，同商品日志教训）
- **埋点**：创建记「创建」；编辑逐字段 diff（仅真实变化字段），**排除 PII 敏感字段**
  （phone/phone2/email/wechat/bank_account/tax_id/boss_phone —— 加密存储且防日志泄密），
  空→空跳过，bool 归一化，日志异常不影响主流程
- **路由**：`GET /api/contacts/{cid}/changes`（RBAC contacts 模块保护）
- **操作人**：当前登录用户 display_name

## 五、验证

- node --check ✓ / py_compile ×4 ✓
- 冒烟（创建→改名→改账期→改价格等级→改信用额度）：diff 值正确、phone 零泄漏、倒序正确 ✓
- 线上：首页/bundle/health 200，`/api/contacts/1/changes` 匿名 401（已注册受保护）✓
- 生产租户库 `entity_change_logs` 表已建 ✓

## 六、未纳入页面与原因（诚实说明）

1. **字典类（品牌/分类/单位/仓库/科目/账户等 openRefData 系列）**：后端 `/api/ref-data` 只有
   GET/POST（创建），**编辑走各字典专属接口且分散**（如 PUT /api/brands/{id} 等），无统一埋点面。
   若要覆盖需逐个字典改专属 CRUD —— 建议后续做"字典统一编辑路由"时一并接入（改造后一次埋点全覆盖）。
2. **员工档案**：后端 `employee_create` 存在但**无 `employee_update` / PUT /api/employees 编辑接口**
   （员工编辑目前走前端表单，无独立后端更新路由）—— 待员工编辑后端就绪后接入。

> 数据起点说明：与商品档案一致，修改记录**自本版本起生效**；存量客户/供应商此前无字段级日志，
> 弹窗显示"暂无修改记录"直到下一次编辑，这是数据历史的真实起点。
