# v121c：创建品牌目标 —— 只保留「按 12 个月分解」一种录入模式

日期：2026-09-08　状态：**已上线并真机验证通过**　改动范围：**仅前端**（`hergent-cn-v2/src/pages/Rebate.vue`），后端零改动

---

## 一、改了什么（结论先行）

| 项 | 改前 | 改后 |
|---|---|---|
| 周期口径（月/季/年/自定义） | 品牌目标也显示，用户困惑 | **品牌目标不再显示**（保存时固定 `period_type='year'`）；仅商品目标等非品牌场景保留 |
| 独立「目标金额」框 | 与 12 个月分解并存，两处都能填 | **删除**；年度总额 = 12 个月之和（单一真相） |
| 12 个月分解 | 要勾选 checkbox 才展开 | **默认铺开**，无开关 |
| 录入方式 | 只能逐月填 | **全年一次性录入** + **单月微调** 并存 |
| 触发方式 | 与上述字段混在同一网格 | **并入 12 个月分解区**，整块统一呈现 |

---

## 二、为什么周期口径只对品牌目标删掉（重要）

后端硬约束（已回代码核实）：
- `rebate_rules.py:507-510`：**`monthly_amounts` / `monthly_rates` 只允许品牌维度**，非品牌传了会 400；
- `rebate_rules.py:341`：**品牌维度的目标只能按金额**，不能选数量；
- `rebate_rules.py:518-523`：有月度分解时 `period_type` 强制为 `year`（非 year/custom 才改）。

所以「12 个月分解」只能在品牌+金额下成立。而**「创建商品目标」共用同一个弹窗**，若把周期口径和目标值全局删掉，商品目标就无法录入了。
→ 处理：`isBrandMonthly = 品牌维度 && 金额度量` 时走新的 12 个月区；其余（商品/数量）仍走原来的「周期口径 + 目标金额/目标数量」。

---

## 三、新的「按 12 个月分解」区结构

```
┌ 按 12 个月分解（金额层 + 月度返利）          默认铺开，无开关 ┐
│ ① 一次性录全年：[全年目标 万元]  [全年返利率 %]                │
│ ② 单月微调：   月份 / 目标金额(万元) / 返利率(%)  12 行表格     │
│               已填 N 个月 · 合计 X 万元            [清空]      │
│ ③ 触发方式：   达成即返（按月返利率）+ 一句话说明（虚线分隔）    │
└────────────────────────────────────────────────────┘
```

**两种录入方式**
- **全年**：`全年目标` 填 1200 → 自动均分 12 个月（每月 100，余数补 12 月）；`全年返利率` 填 10 → 12 个月统一 10%。
- **单月**：只改某个月的金额或返利率，**其他月份不受影响**；改完 `全年目标` 框自动回读为新的月合计（因为年度总额 = 各月之和）。

**关于「触发方式」为什么变成固定说明**
月度分解模式下，保存逻辑原本就强制 `trigger_mode='on_target' / rebate_basis='rate'`（原来「阶梯返利」选项也是 `:disabled="monthlyOn"`）。既然现在只剩这一种模式，就**不再摆一个永远禁用的下拉**（那正是"容易困惑"的来源），改为一行固定说明：达成即返（按月返利率），并讲清阶梯为什么不并存。提交给后端的 `trigger_mode` 仍是 `on_target`，契约不变。

---

## 四、数据契约（与后端一致，未改字段名）

| 前端 | 后端字段 | 值 |
|---|---|---|
| 各月目标金额（万元） | `monthly_amounts` | `{"01":1000000,...}` 单位**元**，只含有值的月 |
| 各月返利率（%） | `monthly_rates` | `{"01":0.1,...}` 单位**小数** |
| 年度总额 | `target_value` | = 各月之和（元） |
| 周期口径 | `period_type` | 固定 `year` |
| 触发方式 | `trigger_mode` / `rebate_basis` | `on_target` / `rate`；`tiers_json=''` |
| 顶层返利率兜底 | `rebate_rate` | 取最早一个非零月 |

## 五、校验与异常

- 品牌模式**必须至少 1 个月有金额**、**至少 1 个月有返利率**，否则前端直接拦住并提示（后端会对空返利率 400，不让用户撞墙）。
- 老规则回显：
  - 有 `monthly_amounts` → 原样回填 12 个月；
  - 无月度分解但 `period_type='year'` → 按原年框均分预填，**值不丢**；
  - 无月度分解且是月/季口径 → **不擅自拆分**（拆了会改口径），留空并黄条提示原值，让用户按月重填。

## 六、代码改动（仅 `Rebate.vue`）

- 新增 computed：`isBrandMonthly`、`monthlySumWan`、`annualTargetWan`(get=月合计 / set=均分12月)、`annualRatePct`(get=12月一致时显示 / set=全月统一)、`monthlyFilledCount`、`annualRatePlaceholder`、`legacyNotice`
- `monthlyOn` 由 `ref(false)` 改为 **computed**（= `isBrandMonthly`）；删除 `onMonthlyToggle`、`setAllMonthlyRate` 与 checkbox
- `splitAnnualToMonths(v)` 支持传参，且**只重算金额、不动返利率**
- `fillMonthlyFromRule` 去掉 `monthlyOn` 赋值，新增老规则口径保护
- `save()`：品牌模式 `target_value = 月合计`，并加"至少一个月金额/返利率"校验
- `loadArrivalPreview` / watch：均单改用 `monthlySumWan` 驱动

## 七、验证（真机，Chrome + demo boss）

| 项 | 结果 |
|---|---|
| 周期口径 / 独立目标金额 已消失 | ✅ |
| 12 个月区默认铺开（12 行） | ✅ |
| 全年 1200 → 12×100，回读 1200 | ✅ |
| 只改 3 月=150 → 其他月仍 100，全年框自动 1250 | ✅ |
| 全年返利率 10 → 12 月均 10% | ✅ |
| 保存落库 | `period_type=year`、`trigger_mode=on_target`、`target_value=12,500,000`、`monthly_amounts` 12 个月（03月=150万）、`monthly_rates` 12 个月 0.1 ✅ |
| 商品目标流程未受影响 | 周期口径 / 目标数量 / 触发方式 仍在 ✅ |

测试数据（规则「真机验证-12月分解」）已删除。构建 `Rebate-Dm3xj3jn.js` 131.47 kB，已 rsync 上线，`index.html` 200。

## 八、遗留

- 前端改动**未 commit**（连同 v121/v121b 及历史未提交项）。
- 「查看规则」详情弹窗仍显示周期口径（只读回显，品牌规则存的是 year，无矛盾），未改。
