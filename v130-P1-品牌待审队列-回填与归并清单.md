# v130 P1 · 品牌待审队列：回填与归并清单

日期：2026-09-10 ｜ 租户：tenant_1（永诺旗舰店）
后端 commit：`833c750`（P0，已部署）

---

## 一、P1 实际做了什么

### 1. 认知纠偏：待审 UI 早就存在

`hergent-cn-v2/src/pages/BrandArchive.vue` 第 56–89 行是完整的「待审品牌」卡片，
第 242 行 `resolve()` 已实现 `create / merge / dismiss` 三操作。

P0 之前它一直显示"待审队列为空"，**不是因为功能没做，而是因为后端表不存在、接口 500**。
所以 P1 不含任何前端开发，只有两件事：**把数据灌进去** + **验证真的能用**。

### 2. 返利安全性核查（归并前必须确认）

| 检查项 | 结果 |
|---|---|
| `rebate_rules` 行数（全部 11 个库） | **全为 0** —— 返利目标实际落在 `rebate_target_rules` |
| `rebate_target_rules`（tenant_1） | 5 条，`scope_key` = `蒙牛低温` / `简爱` |
| `rebate_achievements`（tenant_1） | 4 条，`scope_key` = `蒙牛低温` / `简爱` |
| `rebate_rule_month_lock`（tenant_1） | 14 条，全部命中 `蒙牛低温` / `简爱` |

**结论：现有返利规则只引用了已建品牌，归并任何脏值都不会破坏返利计算。**

### 3. 回填（唯一写生产动作）

`products.brand` 去重 16 组，3 组已建品牌 → 余 13 组写入 `brand_pending`，`ref_count` = 该品牌下的商品数。

| # | 待审品牌 | 命中商品数 | 来源 |
|---|---|---|---|
| 1 | 赠品 | 43 | backfill |
| 2 | 君乐宝 | 37 | backfill |
| 3 | 蒙牛常温 | 25 | backfill |
| 4 | 新希望 | 19 | backfill |
| 5 | 友芝友 | 18 | backfill |
| 6 | 蒙牛奶酪 | 15 | backfill |
| 7 | 蒙牛乳饮 | 6 | backfill |
| 8 | 妙可蓝多 | 5 | backfill |
| 9 | 妙奇 | 5 | backfill |
| 10 | 旭培 | 4 | backfill |
| 11 | 光明 | 1 | backfill |
| 12 | 测试品牌 | 1 | backfill（已忽略 ✓） |
| 13 | 蒙牛 | 1 | backfill |

### 4. 真机验证（agent-browser 0.27.0 + 系统 Chrome）

- `#/archive/brands` 渲染：品牌主档 **3 行** + 待审 **13 条**，无报错、无空态。
- 每条元素齐全：归一名称输入框 + 「新建为品牌」+ 「合并到已有品牌」下拉（3 个候选）+ 「合并」+ 「忽略」。
- **真实点击「忽略」**（测试品牌）→ toast 成功 + 列表 13 → 12 + 刷新后与后端一致。
- **merge 分支**用零业务影响自检条目验证 → `{success: true, canonical: '蒙牛低温'}`，品牌数不变，队列复位；自检数据已从库中删除。

---

## 二、待你拍板的 12 条归并映射（业务决策）

| 待审值 | 商品数 | 建议动作 | 理由 |
|---|---|---|---|
| 蒙牛 | 1 | **合并 → 蒙牛低温** | 同一主体的简写写法 |
| 蒙牛常温 | 25 | 新建品牌 | 常温线与低温线分开管更合理（返利/目标按品牌算） |
| 蒙牛奶酪 | 15 | 新建品牌 | 不同品类 |
| 蒙牛乳饮 | 6 | 新建品牌 | 不同品类 |
| 君乐宝 | 37 | 新建品牌 | 独立品牌 |
| 新希望 | 19 | 新建品牌 | 独立品牌 |
| 友芝友 | 18 | 新建品牌 | 独立品牌（本地乳企） |
| 妙可蓝多 | 5 | 新建品牌 | 独立品牌 |
| 妙奇 | 5 | **需你确认** | 是"妙可蓝多"的简写，还是另一个品牌？ |
| 旭培 | 4 | **需你确认** | 品牌还是经销商/门店名？ |
| 光明 | 1 | 新建品牌 | 独立品牌 |
| 赠品 | 43 | **忽略** | 业务占位符，不是品牌（见下方注意） |

### ⚠️ 关于「忽略」的一个重要语义

点「忽略」**只会把这条待审记录标记为已处理，不会改动 `products.brand`**。
也就是说 43 个商品的 `brand` 字段仍然是"赠品"，它**依然会出现在「本期预报」的品牌筛选下拉里**。

如果你希望「赠品」不再出现在品牌筛选里，需要额外做数据订正（属 P2/P3 范畴）：
建议把这类占位值统一置空或改为专门的标记字段，而不是当作品牌。

---

## 三、归并后的预期结果

- 品牌主档：**3 → 12 条**（新增 9 个品牌 + 蒙牛常温/蒙牛奶酪/蒙牛乳饮；减去合并不新增的「蒙牛」）
- 待审队列：**12 → 0 条**
- 「蒙牛」名下 1 个商品并入「蒙牛低温」→ 该品牌商品数 132 → 133

---

## 四、操作方式

**方式 A（推荐，你自己点）**：打开「档案管理 → 品牌档案」，在「待审品牌」卡片逐条处理。每条右侧可直接填归一名称或选合并目标。

**方式 B（我批量执行）**：你确认上面映射表后，我一次性调接口完成全部 12 条归并，之后你只需核对结果。
