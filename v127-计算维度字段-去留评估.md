# 「计算维度」是否保留在创建界面 · 评估与结论

> 日期：2026-09-10 ｜ 结论：**移除界面控件，改为入口/记录固定值，后端锁定不可改（方案 C）**
> 评估范围：品牌目标 / 商品目标创建与编辑弹窗（`TargetFormModal`）

---

## 一、结论先行

**建议：方案 C —— 前端移除「计算维度」下拉，维度由入口按钮（新建）或原记录（编辑）决定；后端在 update 时锁定 dimension 不可改。**

| 方案 | 做法 | 评价 |
|---|---|---|
| A 保留 | 维持现状 | ❌ 冗余 + **存在静默数据丢失风险** |
| B 完全移除 | 连数据存储/枚举一起删 | ❌ 不可行，dimension 是计算主键 |
| **C 改为固定值（推荐）** | 界面不暴露，后端锁定 | ✅ 零业务损失，顺带堵住数据丢失 |

---

## 二、角度 1：该字段的实际作用，是否还有「用户手动多选」场景

`dimension` **不是一个用户偏好项，而是一个计算主键**。它的下游依赖有 5 处：

| # | 依赖点 | 位置 | 作用 |
|---|---|---|---|
| 1 | 作用对象语义 | `_normalize_by_dimension` | brand → scope_key 存**品牌名**；product → 存**商品 ID** |
| 2 | 月度分解开关 | `_normalize_by_dimension` | 只有 brand 允许 `monthly_amounts/rates/tiers`，product 一律清空 |
| 3 | 月份锁唯一键 | `rebate_rule_month_lock UNIQUE(dimension, target_type, scope_key, ym)` | v125 判重的核心键 |
| 4 | 达成匹配键 | `rebate_achievements` / `Rebate.vue:1849` | `${dimension}::${scope_key}` 定位填报记录 |
| 5 | 预报冲刺匹配 | `Forecast.vue:5445` | brand 按商品品牌匹配，product 按 product_id 匹配 |

**是否存在需要用户手动切换维度的场景？→ 不存在。**

- 业务上「一条目标」从创建那刻起维度就确定了：厂家给的是品牌框架协议 or 单品冲量政策，不会中途变。
- 界面上两个入口卡片（品牌目标 / 商品目标）已经把这件事说清楚了（"按品牌设置…" / "按核心单品(SKU)…"）。
- 用户想改维度 = 这是另一条政策，应当**新建一条**，而不是把旧规则改过去。

---

## 三、角度 2：拆分后是否已变成冗余项 / 固定值

**已经变成固定值，而且暴露它有实际危害。**

证据链：

1. **下拉里实际只有 2 个选项**：后端 `DIMENSIONS` 有 5 个值（brand/product/category/customer/channel），但前端 `Rebate.vue:1115` 硬过滤 `d.value === 'brand' || d.value === 'product'` → 用户永远看不到另外 3 个。所谓「多维度选择」在 UI 层早已名存实亡。
2. **新建时 100% 冗余**：`openCreate('brand')` / `openCreate('product')` 已经传 `preset-dim`，下拉只是把这个已定值再显示一遍。
3. **编辑时是「伪装成可编辑的不可变属性」，且会丢数据**（这是最关键的发现）：

   > 编辑一条品牌规则 → 把「计算维度」改成单品 → 保存 → 后端 `_normalize_by_dimension` 走到非品牌分支，执行
   > `monthly_amounts = ""` / `monthly_rates = ""` / `monthly_tiers = ""`
   > → **12 个月的目标分解被静默清空**，报单节奏也一并归零。而且因为前端 `monthlyOn` 只在 brand 时为 true，payload 里根本不带月表，后端是用 existing 值合并后又清掉的，用户**完全无感知**。

4. **生产数据 100% 是 brand**（已核实）：tenant_1 5 条 + tenant_10 2 条，全部 `dimension='brand'`；`rebate_achievements` 4 条、月份锁 14 行也全是 brand。零条 product / category / customer / channel。

---

## 四、角度 3：保留 vs 移除的影响面

| 维度 | 保留（现状） | 移除界面控件（方案 C） |
|---|---|---|
| 前端交互 | 多一个下拉；品牌页与「目标度量」挤在同一 `form-grid2`，商品页才露出度量 | 删掉一个控件；`目标度量` 改为商品页独占整行，布局更干净 |
| 后端校验 | 需继续容忍「API 传了页面不允许的组合」→ `_normalize_by_dimension` 兜底 | update 显式锁定 dimension，兜底逻辑只需管 create |
| 数据存储 | 无变化 | **无变化**，`dimension` 列保留（它是计算主键，删不得） |
| 历史数据兼容 | 无影响 | **无影响** —— 只读消费（列表筛选 / 详情展示 / 达成匹配 / Forecast 冲刺）全部读 `r.dimension`，不经过这个控件 |
| 风险 | ⚠️ 编辑时跨维度切换 → 月度分解静默清空 | ✅ 风险消失（要换维度只能新建） |

**需要强调：移除的是「界面控件」，不是「字段」。** 下列全部保持原样：
`rebate_target_rules.dimension` 列、月份锁唯一键、`/meta` 的 dimensions 枚举、列表页「全部维度」筛选、详情页「计算维度」展示、达成填报、Forecast 冲刺匹配、后端 5 个枚举值（category/customer/channel 作为未来扩展位保留）。

---

## 五、改动范围

### 前端（2 个文件）

| 文件 | 改动 |
|---|---|
| `src/components/rebate/TargetFormModal.vue` | ① 删除 L15–20 的「计算维度」`<div class="form-row">`+`<select>`；② 把 `form-grid2` 改为 `v-if="!isBrandForm"` 只包「目标度量」，商品页独占整行（避免品牌页留下空网格）；③ 删除 props 里的 `dimensionOptions`（L162） |
| `src/pages/Rebate.vue` | ① 移除 `:dimension-options="dimensionOptions"` 传参（L548）；② 删除 `dimensionOptions` computed（L1109–1116，删后成为死代码） |

> `form.dimension` 字段本身**保留** —— `isBrandForm`、作用对象解析、`save()` 的 payload 都依赖它，只是不再由用户填写。

### 后端（1 个文件，1 处）

| 文件 | 改动 |
|---|---|
| `server/routers/rebate_rules.py` | `update_rule()`：合并前显式 `body.pop("dimension", None)`，并从局部更新白名单元组（L1299）移除 `"dimension"` → 维度不可改，需换维度请新建。**必须加注释**，否则会踩本项目已知坑（未登记白名单 = 静默丢弃，后人看不懂是故意的还是漏了） |

> `create` 路径不动：仍接受 `dimension`，仍由入口按钮决定。

---

## 六、需要你拍板的一点

编辑品牌/商品目标时，如果确实需要「把这条改成另一个维度」，方案 C 下只能新建。是否需要我在弹窗里加一句提示（如标题旁小字「维度不可修改，需新建」）？—— 默认**不加**（保持极简），你说要再加。

---

# 实施记录（2026-09-10 已上线）

## 交付

| 端 | commit | 改动 |
|---|---|---|
| 前端 | `16c0a71`（hergent-landing / main） | `TargetFormModal.vue`：删「计算维度」select；原 `form-grid2` 整块撤掉，「目标度量」提为 `v-if="!isBrandForm"` 独占整行；删 `dimensionOptions` prop。`Rebate.vue`：删 `:dimension-options` 传参与 `dimensionOptions` computed |
| 后端 | `5b1e829`（hergent-erp / upgrade/v84-international） | `update_rule`：`body.pop("dimension", None)` + 从局部更新白名单移除 → 维度锁定不可改 |

`form.dimension` 字段本身保留（`isBrandForm` / 作用对象解析 / save payload 依赖它），只是不再由用户填写。

## 验证

| 项 | 结果 |
|---|---|
| 后端 health | HTTP 200（首次探测 000 是重启未完成，复检 200） |
| 生产代码 | `/opt/hergent-erp/routers/rebate_rules.py` 含 v127 注释与 `body.pop("dimension"` |
| 维度锁定（API） | 临时规则 id=11（2027-05，月表 12 万 / 5%）→ `PUT {"dimension":"product"}` → **dimension 仍 brand，monthly_amounts/rates 完好**（修复前会被清空） |
| 现场清理 | id=11 `DELETE?hard=1`，2027-05 锁释放，锁总数回到 14 |
| dist 复核 | `dimensionOptions` 字面量已消失；「计算维度」仅剩详情页 `.dt-l` 展示（有意为之） |
| 真机 · 创建品牌目标 | 标题「创建品牌目标」；无「计算维度」；无「目标度量」（品牌固定金额） |
| 真机 · 创建商品目标 | 标题「创建商品目标」；无「计算维度」；**有**「目标度量」 |
| 真机 · 编辑 #10 | 标题「编辑品牌目标」；12 格回填 60/50/60/70/80/90/90/95/100/70/76/60；保存后 API 复核 `dimension=brand`、`monthly_amounts` 12 个月完好 |

## 顺带发现（非本次引入，未处理）

规则 #10（蒙牛低温2026年目标）的 `monthly_rates` 只有 `{"10": 0.15}`，其余月份靠顶层 `rebate_rate = 0.15` 回退 —— **计算正确**（8 月试算仍是 15%），但月表里只有 10 月那一格显示 15%，视觉不一致。要修的话在编辑弹窗用「全年返利率」重铺一次即可。
