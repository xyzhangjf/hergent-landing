# 全站设计风格统一审计与优化清单（2026-08-05, v107.44）

## 一、审计方法
- **设计体系基线**：`css/variables.css` 令牌（品牌主色 `--p:#1f4e79`/亮蓝 `--pl`/深蓝 `--pd`，语义色 `--suc/--dan/--war`，文字 `--t1/--t2/--t3`，表面 `--bg2/--bg3`）+ 组件类体系（btn 系列 / filter-chip / modal / card / data-*）
- **扫描**：全站 19 个 JS 文件含硬编码 hex 色；其中 7 个文件的 **style 属性内**存在 49 处偏离体系的老色（#333/#666 文字色、#2563eb 老蓝、#10b981/#ef4444/#f59e0b 状态色等）

## 二、页面风格状态清单

| 页面 | 设计体系采用状态 | 本轮处置 |
|---|---|---|
| 全局框架（侧边栏/顶栏/设置/布局） | ✅ 已采用（令牌+组件） | 无需改动 |
| 商品/客户/供应商档案 | ✅ 已采用（v107.26 单行化 + v107.43 品牌色链接） | 本轮 app.js 内联色统一 |
| 经营看板（unified-dashboard） | ⚠️ 部分偏离（涨跌色硬编码） | ✅ 已统一 |
| 审核中心（review-queue） | ⚠️ **偏离最重**（批准/驳回按钮、状态徽章硬编码） | ✅ 已统一 16 处 |
| AI 预测准确性（ai-accuracy） | ⚠️ 偏离（红黄绿状态色） | ✅ 已统一 9 处 |
| 冷链看板（cold-chain-zone） | ⚠️ 偏离（温度状态色） | ✅ 已统一 5 处 |
| 客户管理 v2（customers-v2） | ⚠️ 偏离 | ✅ 已统一 3 处 |
| 付款导入（payment-import） | ⚠️ 偏离（主色） | ✅ 已统一 1 处 |
| 其它 12 文件 | ✅ 硬编码为图表/canvas 语义色（合理保留） | 无需改动 |

## 三、统一映射规则（49 处）

| 老色 | → 令牌 | 语义 |
|---|---|---|
| #333 / #1f2937 | var(--t1) | 文字主色 |
| #666 / #374151 | var(--t2) | 文字次级 |
| #999 / #888 / #6b7280 / #9ca3af | var(--t3) | 文字弱化 |
| #2563eb / #3b82f6 / #1677ff / #1890ff | var(--pl) | 亮蓝（主色系） |
| #007bff / #0d1b2a | var(--p) | 品牌主色 |
| #10b981 / #52c41a | var(--suc) | 成功绿 |
| #ef4444 | var(--dan) | 危险红 |
| #f59e0b / #faad14 | var(--war) | 警示橙 |
| #e5e7eb / #ccc | var(--bd) | 边框 |
| #f3f4f6 | var(--bg3) | 表面 |

**替换范围**：仅 style="..." 属性值内（HTML 生效层），canvas/图表颜色数组**不动**（需具体色值）；
同一属性内多个 hex 按整个属性值整体替换（v107.37 贪婪漏配教训）。

## 四、可访问性与响应式基座确认（无需改动，已验证）

- **焦点可见性**：`*:focus-visible` 全局焦点环（var(--p)）+ `.btn:focus-visible` 专用环 ✓（WCAG 2.4.7）
- **响应式**：侧边栏 ≤1024px 折叠为图标栏、移动端底部导航 ✓
- **主题适配**：替换后的 var() 在浅色/深色双主题自动适配 ✓

## 五、验证
- node --check ×7 ✓ / style 属性内 legacy 残留 **0** ✓
- 构建 `main-BRh5jv0y.js` ✓ / 线上 200 ✓ / 双主题视觉回归（浅色为主）✓

## 六、后续建议（非本轮）
- 图表色（canvas 数组）如需深色主题适配，可引入主题变量注入层（图表库级改造，风险大，暂缓）
- 少量 rgba(239,68,68,.x) 半透明底可渐进收敛到 --pb 语义（低优先）
