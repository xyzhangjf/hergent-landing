# 舟谱「目标管理」界面设计与功能亮点 · 借鉴要点清单

> 来源：舟谱创建目标功能 5 张截图（首页入口 / 列表页 / 商品目标详情 / 业务目标详情 / 创建向导）。
> 目的：提炼可复用的界面设计与交互亮点，用于优化我们自身的「目标 / 返利目标规则」功能设计。
> 适用对象：hergent 前端（hergent-cn-v2）+ 后端（rebate_target_rules 引擎）。

---

## 一、信息架构：三类目标并列，降低认知门槛

| 截图 | 设计点 | 借鉴价值 |
|---|---|---|
| 图 1 | 首页用 3 张大卡片横向并列：**业务目标 / 品牌目标 / 商品目标** | 「目标维度」作为一级分类直接外露，而非进通用表单再选维度。 |
| 图 1 | 每张卡片一句话说明适用场景（业务阶段/头部品牌/核心单品） | 用业务语言解释维度，零学习成本。 |
| 图 1 | 每张卡片显示「进行中任务数」+「查看更多」+「立即创建」 | 把「数量状态」与「创建入口」同卡片呈现，减少跳转。 |
| 图 1 | 关键指标用 tag 标签平铺展示（订单金额/净销售额/销售金额…） | 提前告知该维度支持指标，进表单前就有预期。 |

**借鉴①**：把「新建目标」从单一按钮升级为**维度选择页**，先选「品牌 / 商品 / 业务」，再进对应表单。

---

## 二、创建流程：三步向导 + 进度感

| 截图 | 设计点 | 借鉴价值 |
|---|---|---|
| 图 5 | 顶部步骤条：**① 基本信息配置 → ② 设定目标 → ③ 确认目标** | 复杂配置拆 3 步，每步只做一类决策，降低填写压力。 |
| 图 5 | 步骤 1 只问：名称、周期、指标、拆解方式、选员工 | 基本信息和「目标值」解耦：先定框架，再填数字。 |
| 图 5 | 底部固定操作栏：左「取消创建」（弱色）、右「下一步(设定目标)」（主色，文案带目的地） | 按钮主次分明，且明确告知下一步去哪。 |
| 图 5 | 必填项红色星号 `*` 标在字段名前 | 清晰标注必填。 |

**借鉴②**：目标创建采用**三步向导**；把「拆解对象」作为第一步核心决策。

---

## 三、字段设置：口径明确、拆解灵活

| 截图 | 设计点 | 借鉴价值 |
|---|---|---|
| 图 5 | 目标指标单选，列 7 个业务口径：订单金额/净销售额/销售回款金额/客户回款金额/拜访客户数/销售金额/铺货客户数 | 指标用业务语言命名，非技术字段名。 |
| 图 5 | 目标拆解方式三选一：**拆解到员工 / 拆解到客户 / 不拆解** | 允许「不拆解」作简单模式兜底。 |
| 图 5 | 选员工用「+ 选择员工」触发弹窗 | 人员选择不挤占表单。 |
| 图 3/4 | 详情页直接展示「目标值 + 起止时间 + 拆解对象 + 拆分明细表 + 合计」 | 详情非只读字段堆，而是目标值拆成人员/客户维度明细并带合计。 |

**借鉴③**：指标口径用业务语义命名；拆解方式提供「不拆解」兜底；详情页必须有**拆分明细表 + 合计**。

---

## 四、列表页：筛选 + 状态 + 操作一体化

| 截图 | 设计点 | 借鉴价值 |
|---|---|---|
| 图 2 | 顶部筛选区：目标名称 / 品牌 / 商品 / 开始时间 / 结束时间 / 目标类型（平铺） | 多筛选项平铺，非收进高级筛选，快速定位。 |
| 图 2 | 表格列：序号/名称/起止时间/状态/目标类型/拆解方式/修改时间/操作 | 「目标类型」「拆解方式」作独立列，一眼识别属性。 |
| 图 2 | 状态用彩色标签：进行中（绿）/ 已结束（灰） | 状态视觉化，便于扫视。 |
| 图 2 | 操作列：详情 / 复制 / 修改 / 终止 / 修改记录 | **复制**与**修改记录**极有价值：复制可快速新建下期目标；修改记录可追溯。 |
| 图 2 | 顶部同时提供 3 个创建按钮（业务/品牌/商品） | 即便首页有卡片入口，列表页也保留快捷创建，路径不唯一。 |

**借鉴④**：列表页增加**复制 / 终止 / 修改记录**；状态用标签化颜色区分。

---

## 五、详情页：目标值 + 口径 + 拆分明细 三段式

| 截图 | 设计点 | 借鉴价值 |
|---|---|---|
| 图 3/4 | 结构：名称 → 目标值 → 商品列表（仅商品目标）→ 起止时间 → 拆解对象 → 状态 → 拆分详情 | 层级清晰：是什么 → 值多少 → 拆给谁各多少。 |
| 图 3 | 商品目标展示「商品列表」全部 SKU | 作用范围必须列清，避免忘选。 |
| 图 3/4 | 拆分明细表头带周期与口径：「2026年8月 / 净销售数量(不含赠品)目标」 | 表头直接说明口径+周期，避免歧义。 |
| 图 3/4 | 合计行用橙色高亮 | 汇总数据视觉突出，符合财务/目标场景习惯。 |

**借鉴⑤**：详情页采用「目标摘要 → 作用范围 → 拆分明细 → 合计」四段式；合计行用品牌色/橙色高亮。

---

## 六、视觉与交互细节亮点

1. **卡片式首页**：3 卡片等宽、浅灰背景、圆角，比纯表格列表更有「功能入口」感。
2. **关键指标标签云**：浅灰小 tag 展示支持指标，信息密度高且不喧宾夺主。
3. **步骤条带连接线和编号**：当前步深蓝实心圆，后续步灰圈，进度感明确。
4. **表单字段分组留白**：字段间距一致，单选按钮横向排列，节省垂直空间。
5. **操作按钮分组**：列表页「查询/重置/导出」右对齐，创建按钮绿底突出。
6. **空态/Banner 引导**：首页底部「新版目标管理 了解更多」banner，引导新用户。

---

## 七、对我们「返利目标规则」的直接优化建议（分层）

### P0（建议尽快）
1. **创建入口维度化**：「新建返利目标」拆成「品牌目标 / 商品目标」两个卡片入口。
2. **指标口径业务化**：`amount` → 「下单金额 / 净销售额」，`quantity` → 「销售数量（大单位）」。
3. **详情页补拆分表**：若为某品牌/商品规则，详情展示作用范围 + 本期达成 + 合计。
4. **列表页加状态语义**：除启用/停用外，增加基于生效期的「进行中 / 已结束」彩色标签。

### P1（体验加分）
5. **创建向导三步化**：基本信息 → 设定目标值/阶梯 → 确认。
6. **详情页展示作用范围**：品牌名 / 所选 SKU 列表。
7. **列表筛选增强**：维度、指标类型、状态、周期月份。
8. **首页引导 Banner / 空态**：未配置目标时展示引导。

### P2（远期）
9. **支持「不拆解」简单模式**：只设品牌/商品总目标。
10. **目标达成进度可视化**：列表/详情增加进度条/达成率。
11. **复制 / 修改记录**：快速新建下期 + 规则变更追溯。
