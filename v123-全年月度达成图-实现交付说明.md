# v123 全年月度达成柱状图 —— 实现交付说明

日期：2026-09-09
范围：目标与返利 › 仪表盘 Tab
依赖：后端 v123（已部署）+ 前端 v123（已部署）

---

## 一、交付

| 项 | 内容 | 状态 |
|---|---|---|
| 图表组件 | `src/components/rebate/MonthlyAchvChart.vue`（纯 SVG，零图表库） | ✅ |
| 逻辑层 | `src/components/rebate/useMonthlyAchv.js`（纯函数，可单测） | ✅ |
| 页面接入 | `Rebate.vue` 仪表盘 Tab：KPI 卡下方、异常预警区上方 | ✅ |
| 后端接口 | 达成查询支持 `year`、试算支持 item 级 `ref_date` | ✅ |
| 真机验证 | tenant_1（真实数据）+ demo 租户（稀疏数据） | ✅ |

**实现要点**
- **位置**：12 栏满宽，KPI 卡（本月一个数）→ 图表（全年 12 个月）→ 预警/排行（定位问题）。
- **四柱同屏 + 双 Y 轴**：左轴销量（万元）、右轴返利（万元），零基线严格对齐，轴标题强制带单位。
- **达成率视图**：一屏看 12 个月百分比（月 2 柱），窄屏默认走这个视图，横向滚动保 720px。
- **品牌多选**：候选 = 品牌档案 ∪ 规则 `scope_name`，避免自由文本品牌（如「蒙牛低温」）漏项。
- **联动边界**：筛选联动异常预警区 + 条形排行；**KPI 卡不联动**（始终全量），并加了一行说明小字。
- **请求数**：全年达成 1 次 + 返利试算 1 次 = 2 次（未优化前是 12 + 12）。

---

## 二、变更

### 后端（`hergent-erp`，commit `75fb45e`）
1. `server/routers/rebate_achievements.py`
   - `GET /api/rebate-achievements` 新增 `year` 参数：一次返回全年 12 个月。
   - 不传 `year` 时行为不变（仍取单月），**向后兼容小程序与既有调用**。
2. `server/routers/rebate_rules.py`
   - `POST /api/rebate-rules/simulate-batch`：`items[]` 支持 **item 级 `ref_date`**，覆盖顶层 `ref_date`。
   - 返利按各月月末日期试算，12 个月合并为 1 次请求。

### 前端（`laozhangai-product`，commit `9e4483f`）
1. 新增 `src/components/rebate/useMonthlyAchv.js`
   - `buildYearMatrix()`：按年聚合 12 个月四项指标；年度规则取 `monthly_amounts`，单期月规则按生效期归位；跨度量自动排除。
   - `buildSimItems()` / `applySimResults()`：试算项组装与结果回填，**按品牌拆分达成值**（初版未拆分导致跨品牌重复计算，已修）。
   - `rate()` 加权达成率、`niceMax()` 轴上限、`monthEndISO()` 月末（闰年正确）。
2. 新增 `src/components/rebate/MonthlyAchvChart.vue`（451 行）
   - 12 组 × 4 柱、双 Y 轴、达成率视图、Tooltip（四项数值 + 两项达成率）、图例可点灭、骨架屏、空态、当前月圆点。
3. `src/pages/Rebate.vue`
   - 插入图表区（KPI 下、预警上）。
   - `dashItemsFiltered`：筛选后的排行数据；`anomalies` 改用它。
   - `chartBrandList`：品牌候选合并（档案 ∪ 规则 scope_name）。
   - **`mainTab` 定义上移至文件前部** —— 修复顶层 `watch` 求值触发的 TDZ（详见 §五）。
   - KPI 卡下加说明：「以上 KPI 始终统计全部品牌，不随下方图表的品牌筛选变化」。

---

## 三、构建与部署

| 项 | 结果 |
|---|---|
| 前端构建 | `✓ built`；Rebate chunk 143.99 → **161.48 kB**（gzip 48.75 kB），**无新增依赖** |
| 前端部署 | `rsync dist/` → `/opt/hergent-cn-v2/`，`Rebate-hCLIWr-Z.js` 已上线 |
| 后端部署 | `rsync server/` → `/opt/hergent-erp/`，重启后 `active` / `health=200` |
| Git | 后端 `feb0f13 → 75fb45e`；前端 `d1bc2d2 → 9e4483f` |

---

## 四、对齐效果（真机验证，tenant_1 真实数据）

| # | 验证项 | 结果 |
|---|---|---|
| 1 | 图表渲染 | 48 根柱（12 月 × 4）+ 12 hover 区，12 个月轴标签齐 |
| 2 | 8 月数值 | 销量目标 108 万、达成 97 万（**89.8%**）；返利目标 11.0 万、达成 1.1 万（**9.9%**） |
| 3 | 门槛逻辑 | 蒙牛 88% 未达 100% 门槛 → 不返利；简爱 112.5% → 返 1.08 万。与规则配置一致 ✅ |
| 4 | 品牌筛选 | 选「蒙牛低温」→ 8 月目标柱变满高（100 万）、排行 2 → 1 行、预警只剩该品牌 |
| 5 | KPI 不联动 | 筛选前后 KPI 文本**完全一致**（¥0 / 2 预警 / 0÷2 达标 / 8.1%） |
| 6 | 达成率视图 | 每月 2 柱，轴刻度 150%，100% 处有参考线 |
| 7 | Tooltip | 「2026 年 8 月 · 销量目标 ¥108.0万 · 销量达成 ¥97.0万 89.8% · 返利目标 ¥11.0万 · 返利达成 ¥1.1万 9.9%」 |
| 8 | 品牌候选 | 蒙牛低温 / 蒙牛鲜奶 / 简爱（档案与规则名合并），支持搜索 |
| 9 | demo 租户 | 稀疏数据正常渲染，无空态误判、无报错 |

---

## 五、踩到的坑（已写进项目记忆）

1. **TDZ：`<script setup>` 顶层 `watch` 数组里直接写 ref 名**
   `watch([a, b, mainTab], fn)` 在注册时就会求值数组元素；若 `mainTab` 定义在下方 → `Cannot access 'mainTab' before initialization`。
   修法：全部改成 getter（`() => mainTab.value`），并把 `mainTab` 定义上移。
2. **vite dev 缓存会制造假象**：改完源码后必须 `rm -rf node_modules/.vite` 并重启，否则一直看到旧编译结果，会把排查带偏（本次误判了 3 轮）。
3. **试算达成档必须用「该品牌达成值」，不能用「月总达成」**：初版用月总达成给每条规则试算，8 月返利算出 19.4 万（正确 9.88 万），跨品牌重复计算。已按 `byBrand` 拆分修复。
4. **npm build 的 `emptyDir` 在 dev server 运行时会失败**（`rmSync` 权限）：先 `pkill -f vite` 再构建。

---

## 六、需确认事项

1. **目标只覆盖 8、9 两个月** —— 全年视图其余 10 个月为 0（按你的口径「没填就是 0」）。要补 1–7 / 10–12 月的目标，我可以直接建年度 12 月分解规则。
2. **「返利达成」仍为预估应返**（由 `simulate-batch` 推算），UI 已标注「非实际到账」。若要财务口径的实际计提/核销，需补数据入口（方案见设计方案 §3.3）—— 一期不做。
3. **门槛 100% 的反差**：8 月销量达成 89.8%，返利达成仅 9.9%。这是规则配置（`trigger=on_target, threshold=1`）的真实结果，不是 bug。若想让「接近达标」也部分返利，应改规则为阶梯触发，而非改图表。
4. **P3 报单调度** 仍在等你确认 6 期预览节奏（周末是否排单等 5 项），未接调度器。
