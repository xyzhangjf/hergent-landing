# v130 · 「品牌档案 3 个 / 预报品牌筛选 9 个」不一致 —— 排查结论

**结论一句话**：这不是缓存、不是写死枚举、也不是权限过滤——**两个模块读的是两套完全不同的数据源，设计上就不要求相等**。但顺着这条线查下去，挖出 **3 个真缺陷**，其中一个是会静默丢数据的（待审队列在 tenant_1 从来没建过表）。

---

## 一、代码与数据定位

### 1. 两个入口各自的数据源

| 模块 | 数据源 | 代码位置 |
|---|---|---|
| 档案管理 → 品牌档案 | **数据库表 `brands`**（规范层，人工维护） | 前端 `BrandArchive.vue:161` → `GET /api/brands?include_inactive=1` → 后端 `routers/purchase.py:130` → `erp_db.get_brands()` |
| 本期预报 → 品牌筛选 | **不是 brands 表**，是「当期预报行涉及的商品」的 `products.brand` **自由文本去重** | `Forecast.vue:2642 brandCandidates` ← `rowBrand()` L2641 ← `prodMeta`（`productsApi.grid()` 返回的商品档案 brand，填充于 L5637）或行上 `r.brand` |

```js
const brandCandidates = computed(() => {
  const s = new Set()
  cross.value.rows.forEach(r => { const b = rowBrand(r); if (b) s.add(b) })
  return [...s].sort((a, b) => a.localeCompare(b, 'zh'))
})
```

**关键**：候选集随**所选期次**变化——本期有货的品牌才列出来。所以"档案 3 个 / 筛选 9 个"这个数字差本身**不是 bug**。

### 2. 逐条排除你的怀疑项

| 怀疑点 | 实测结论 |
|---|---|
| 下拉是否取自独立数据源/数据字典 | ✅ **是**（差异根因）：筛选取商品档案自由文本，档案取 brands 表 |
| 是否读了缓存/历史数据 | ❌ 无缓存，每次 `loadCross()` 重算 |
| 是否未过滤停用品牌 | ⚠️ **反过来的问题**：预报侧根本没有"停用"概念（只看商品文本）；而档案侧三处前端调用**全都带了 `include_inactive=1`**，停用品牌照样出现在商品档案、返利品牌下拉、预报编辑网格里——"停用"只是加了个灰标签，形同虚设 |
| 权限/状态过滤差异 | ❌ 两侧都无租户外的权限过滤 |
| 前端是否写死枚举 | ❌ 全站无硬编码品牌数组（`/api/brands` 消费点只有 4 处：Rebate、BrandArchive、ProductArchive、Forecast 编辑网格） |

### 3. 生产实测数据（tenant_1）

```
brands 表：3 条 —— 蒙牛低温 / 蒙牛鲜奶 / 简爱（is_active 全 1）

products.brand 去重（共 17 组，非空 16 个）：
蒙牛低温132 | (空)57 | 赠品43 | 君乐宝37 | 蒙牛鲜奶33 | 简爱28 | 蒙牛常温25
新希望19 | 友芝友18 | 蒙牛奶酪15 | 蒙牛乳饮6 | 妙可蓝多5 | 妙奇5 | 旭培4
光明1 | 测试品牌1 | 蒙牛1

预报行（submission_items ∪ audit_decisions ∪ extra_qty）涉及品牌：11 个非空
（蒙牛低温/蒙牛鲜奶/简爱/友芝友/赠品/君乐宝/蒙牛奶酪/蒙牛乳饮/妙奇/蒙牛常温/新希望）
最近有数据的一期（"9月提审期-开放填报"）= 5 个
```

→ **3 vs 9/11/16** 全部对得上：档案是"你想规范的"，筛选是"货上实际写的"。

---

## 二、顺带挖出的 3 个真缺陷

### 缺陷 1（严重）：`brand_pending` 表在 tenant_1 从来没建出来，待审机制静默失效

- 后端设计了归一闭环：写入商品品牌时 `track_brand()`（`erp_db.py:7656`）→ 归一 + 若不在 brands 表就 `add_brand_pending()` 进待审队列 → 品牌档案「待审品牌」里人工确认 → `resolve_brand_pending()` 归并（`UPDATE products SET brand=? WHERE brand=?`）。调用点：`routers/data.py:353/359`（web 录入/改品牌）、`routers/import_router.py:1369`（导入）。
- **但**：`tenant_1.db` 里 **没有 `brand_pending` 表**；`_migrations` 里也**查不到 `v28_brand_pending` 这条记录**（相邻的 `v27_brands_credit_code`、`v28_bin_locations` 都 ok）。而 `erp.db`、`tenant_10.db`、`tenant_11.db` **都有**这张表 → 新库正常，老库 tenant_1 漏了。
- `track_brand()` 内部是 `try: ... except Exception: pass`（注释写着"登记失败不影响主写入流程"）→ 每次 `add_brand_pending` 抛 `no such table: brand_pending` 都被**静默吞掉**。
- **直接后果（已实测）**：`GET /api/brands/pending` 返回 **HTTP 500 Internal server error** → 品牌档案「待审品牌」区在这个租户上永远取不到数据。
- **连锁后果**：所有手填/导入产生的脏品牌（赠品、测试品牌、蒙牛、蒙牛常温…）**永远不会进待审队列**，品牌档案就只能靠手工补——这就是为什么档案停在 3 条。

### 缺陷 2（中）：停用 / 改名不联动商品

- `brands` 表停用（`toggle_brand`）或改名（`save_brand_edit`）**都不会回写 `products.brand`**；只有走待审归并（`resolve_brand_pending` 的 merge/create）才做 `UPDATE products SET brand=?`。
- 于是：档案里停用某品牌 → 商品侧文本照旧 → 预报筛选里照样出现；档案里改名 → 商品侧仍是旧名 → 两边彻底脱节。

### 缺陷 3（中）：商品品牌是纯自由文本，无校验、无别名归一

- `ProductArchive.vue:121`（新增）和 `:49`（行内编辑）都是 `<input list="pa-brand-list" placeholder="可手填或选已有">` —— datalist 只是提示，**手填任意值都放行**。
- `normalize_brand()` 只做去空白/全角转半角/折叠空格，**不做同义词归一** → 「蒙牛」和「蒙牛低温」是两个独立值，各挂着 1 个和 132 个商品。
- 脏值已进生产：`赠品`（43 个商品）、`测试品牌`（1）、`蒙牛`（1）。

---

## 三、影响面评估

| 环节 | 是否受影响 | 说明 |
|---|---|---|
| **订单 / 预报关联** | ✅ 基本安全 | 预报行、订单只存 `product_id`，品牌是经商品关联出来的**派生属性**，无外键 → 品牌档案缺失不会导致关联断裂 |
| **品牌筛选 + 复制报单** | ⚠️ 口径风险 | 按文本精确匹配。勾「蒙牛低温」只筛到 132 个，另 1 个「蒙牛」筛不到——**漏复制** |
| **统计口径（按品牌分组/小计）** | ⚠️ **会裂开** | `Forecast.vue:1656` 分组 key 直接用 brand 文本 → 「蒙牛低温」132 与「蒙牛」1 是两组，「赠品」43 也混进品牌维度 |
| **返利目标 / 月度锁** | 🔴 **能算错钱** | `rebate_target_rules.scope_key` 存品牌名文本，月份锁唯一键是 `(dimension, target_type, scope_key, ym)`（v125）→ 「蒙牛低温」和「蒙牛」**互不冲突，可各建一条同月目标**，达成各算各的，返利重复计提 |
| **下拉回填** | ⚠️ 候选不全 | Rebate(`1063`)/Forecast 编辑网格(`1973`)/ProductArchive(`231`) 三处候选都来自 brands 表（3 个），商品实际有 16 个值 → 回填/筛选会漏项 |
| **新增预报时的品牌校验** | ⚠️ 无校验 | 自由文本直接落库，只靠 `track_brand` 入队（现已失效，见缺陷 1） |
| **停用品牌是否仍可用** | ⚠️ 是 | 三处前端都传 `include_inactive=1`，停用品牌照常出现在候选里 |

---

## 四、哪些是正常设计，哪些是缺陷

**正常设计（不建议改）**
1. 预报品牌筛选只列「当期实际有货的品牌」——有意设计，避免列出一堆本期没货的品牌。**数量不等于档案数是正常的**。
2. 品牌档案定位为"规范层、不强制外键"（页面 tag 原话：「商品 brand 为轻量文本，这里做规范层，不强制外键」）——两边不必严格相等。
3. 未匹配品牌走「待审」而非自动建档——决策②，合理。

**缺陷（建议修）**
1. 🔴 `brand_pending` 在 tenant_1 缺失 + 异常被静默吞 + `/api/brands/pending` 500。
2. 🟠 档案停用/改名不联动 `products.brand`。
3. 🟠 商品品牌无校验、无同义词归一，脏值已入生产。
4. 🟡 前端三处 `include_inactive=1` 让"停用"失效。

---

## 五、需要核查的位置清单

**数据表**：`brands`（3 条）、`products.brand`（16 个非空值）、`brand_pending`（**tenant_1 缺失**）、`_migrations`（无 v28_brand_pending 记录）

**接口**：
- `GET /api/brands` — `routers/purchase.py:130` → `erp_db.get_brands()` L7590
- `GET /api/brands/pending` — **500**，缺表
- `POST /api/brands/pending/{id}/resolve` — 归并逻辑本身 OK，但队列没数据
- `productsApi.grid()` / `PUT /api/products/{id}` / `POST /api/products` — 品牌写入口（`routers/data.py:353/359`）

**前端**：
- `Forecast.vue:2641-2647`（brandCandidates / rowBrand）、`:5637`（prodMeta 填充）、`:1656`（按品牌分组）、`:1973`（编辑网格品牌候选）
- `ProductArchive.vue:49/121/231/261-269`（自由文本录入）
- `BrandArchive.vue:161/170`（档案 + 待审）
- `Rebate.vue:1063`（返利品牌候选）

**后端**：
- `erp_db.py:7656 track_brand`（`except: pass` 静默）、`7601 add_brand_pending`、`7590 get_brands`、`10191 _safe_migrate("v28_brand_pending")`
- `routers/data.py:353/359`、`routers/import_router.py:1369`

---

## 六、建议修复顺序（未实施，等你拍板）

| 优先级 | 动作 | 代价 |
|---|---|---|
| **P0** | 给 tenant_1 补建 `brand_pending` 表（一条 CREATE TABLE + 记 `_migrations`），并**去掉 `track_brand` 的 `except: pass`**（至少打日志）→ 让待审队列活过来，500 消失 | 极小，可脚本一次性 |
| **P1** | 一次性归一：把 16 个品牌值按人工确认表归并（赠品→是否算品牌？蒙牛→蒙牛低温，测试品牌→清掉）。可复用 `resolve_brand_pending` 的 merge 逻辑 | 需你确认映射表 |
| **P2** | 商品品牌录入改为「可选可手填，手填时提示将进入待审」；候选改读 brands 表且过滤停用 | 前端小改 |
| **P3** | 档案停用/改名联动回写 `products.brand`（或至少弹确认提示） | 中 |

**最省事的第一步**：只做 P0（建表 + 去静默），待审队列自己就会开始攒数据，你再决定怎么归并。
