# 能力中心 · 进化日志页技能库重复展示修复 - 交付说明（2026-09-13）

> 背景：评估发现「技能库」在「技能」tab 与「进化日志」tab 两处完整重复展示（同一份 `aiSkills`），非技术视角下易与「工作流」混成"重复展示"。按方案 A 落地：进化日志移除全量技能库，改为引导跳转。

## 交付
- **进化日志 tab**：删除内嵌的完整技能库卡片网格（`ev-skills` 区块），原位置改为一条「去技能页查看 →」引导跳转条。
- **工作流**：维持仅在「技能」tab（本就在那，未移动）。
- 两页职责分离：技能页 = 可操作能力目录（技能库 + 工作流）；进化日志 = 只读透明化记录（统计 + 进化记录 + 引导跳转）。

## 变更（`src/pages/ConnectCenter.vue`）
| # | 位置 | 内容 |
|---|---|---|
| 1 | 模板 · 进化日志 tab | 删除 `ev-skills` 区块（原 `aiSkills` 全量技能卡片） |
| 2 | 模板 · 进化日志 tab | 新增 `.ev-jump` 引导条：`已沉淀 N 个行业技能 · 去技能页查看 →`，`@click="tab='skill'"` 切换 |
| 3 | 计数来源 | `evSkills.total \|\| aiSkills.length \|\| 0`（优先进化接口返回，避免依赖技能页先加载） |
| 4 | 样式 | 删死 CSS：`.ev-skills / .ev-sk-grid / .ev-sk / .ev-sk-ic / .ev-sk-name / .ev-sk-sub` 共 6 条 |
| 5 | 样式 | 保留 `.ev-sk-hd`（被「进化记录」标题复用），新增 `.ev-jump` 4 条样式 |

## 构建
`ConnectCenter-B7arLo-F.js` / `ConnectCenter-Crij8I0P.css` / `index-C_2wmoF-.css` → rsync + chown → 双侧 md5 **MATCH_OK**
（`3b5ba74…` / `2b2e7f3e…` / `f5b046b4…`）。

## 真机验证（生产 https://hergent.cn）
- ✅ 进化日志 tab：`.ev-skills` 容器 = 0、`.ev-sk` 卡片 = 0（技能库已彻底移除）
- ✅ 引导跳转 `.ev-jump` 存在；点击后正确切到「技能」tab，`.ev-jump` 消失
- ✅ 技能 tab 结构完好：区块标题含「AI 技能库」「工作流」、`.wf-grid` 容器在 —— 改动未影响技能页
- ✅ Vue 运行时报错 = 0

## 说明 / 已知依赖
- 引导条计数来自 `/api/ai/evolution` 的 `skills.total`。本次真机该接口在 demo 租户返回 400（Hermes 未连通，同类 `/api/ai/*`、`/api/datasources/*` 均 400），故计数显示 0；**点击跳转仍正常**。该 400 为环境/鉴权问题，与本次改动无关；旧版「行业技能」统计在同环境下亦为 0，无回归。
- 删 CSS 前已 grep 确认：`.ev-sk-hd` 仍被「进化记录」标题复用 → 保留；其余 6 条仅用于已删区块 → 删除无残留引用。

## 待确认
- 本地 `ConnectCenter.vue` 改动尚未 commit（延续近期在途改动同批）。
