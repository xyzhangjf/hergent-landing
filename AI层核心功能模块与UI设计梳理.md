# hergent.cn AI 层 · 核心功能模块与 UI 设计梳理

> 文档日期：2026-07-22
> 基线决策：2026-07-19 产品重新定位（从「通用 AI ERP」→「低温奶/冷链食品经销商 AI 管理系统」）
> 说明：本文档取代 `通用版产品规划*.md` / `产品优化方案_v2.md` 中的「通用个人助理」框架，是 hergent.cn AI 层后续开发的路线图。

---

## 0. 产品定位锚点（决策基线）

- **一句话定位**：不是"功能更多的 ERP"，是"让你少干活的 AI 员工"。
- **目标客群**：低温奶 / 冷链食品经销商（首客 = 你自己；潜在 = 蒙牛 / 伊利 / 光明经销商网络、区域经销商会议）。
- **三层智能引擎**（系统架构主干）：
  - **感知层**：自动采集（舟谱 / 管家婆 Excel 导入、金蝶 / 用友 / 畅捷通 API 直连、手动补录）→ 统一数据底座
  - **智能层**：AI 决策（预测 / 匹配 / 归因 / 生成）→ 728 个现有 API = Agent 工具箱
  - **行动层**：自动执行 + 推送（微信 / 企微）→ 用户全程在微信 / Web 完成，**不打开系统**
- **技术选型**：LLM = DeepSeek；主入口 = 企业微信 Bot（Web 为辅）。
- **差异化核心**：竞品（舟谱 / 管家婆 / 金蝶）在做"功能更多的 ERP"；我们做"少干活的 AI 员工"——同样的 728 个 API，我们让它**自动跑**，而不是让人点。

---

## 1. AI 层核心能力（能力域）

把"AI 能干什么"拆成 6 个底层能力域。功能模块建立在这些能力之上。

| # | 能力域 | 说明 | 支撑模块 |
|---|--------|------|----------|
| **C1** | 智能对话 Conversational Agent | 多轮对话、数字员工人格切换、上下文管理、澄清反问 | M2 |
| **C2** | 语义理解 / 意图识别 NLU | 自然语言 → 结构化意图 → API 动作；实体抽取（商品 / 客户 / 时间段 / 数量） | M2 + 各业务模块 |
| **C3** | 内容生成 Generation | 报告 / 推送文案 / 对话话术 / 异常解读，统一品牌语气 | M6、M3/M4/M5 解读 |
| **C4** | 数据洞察 Insight | 异常检测、趋势预测、归因分析、对比基线 | M3 / M4 / M5 |
| **C5** | 任务编排 / 执行 Orchestration | 多步自动化、定时触发、人工确认节点、幂等 | M7 |
| **C6** | 知识 / 记忆 Memory | 用户偏好、业务规则、历史上下文长期记忆、跨会话 | M9 |

> 低温奶场景的特殊性：C2 必须理解"临期 / 效期 / 批次 / 专属价 / 退货损耗"等**行业实体**；C4 的预测必须考虑**促销节律 + 天气 + 节假日**对低温奶销量的非线性影响。

---

## 2. 功能模块清单（10 个模块）

按"数据 → 对话 → 业务场景 → 触达 → 黏性"分层组织。

### 感知层（基础）
- **M1 数据接入与治理**
  - 舟谱 / 管家婆 Excel 导入、金蝶 / 用友 / 畅捷通 API 直连、手动补录
  - 数据校验、**异常价检测入口**（为 M5 供数）、字段映射模板
  - 现状：Web 版 Excel 导入已上线；API 直连框架在

### 智能层 · 入口
- **M2 智能对话中枢**
  - 数字员工路由（大秘 / 会计 / 订单文员 / 运营主管）、上下文管理、工具调用可视化、澄清反问
  - 现状：角色卡片已在 Web 首页

### 智能层 · 价值核心（4 大场景）
- **M3 AI 需求预测 → 自动采购单**
  - Holt-Winters 预测 + 净需求 + 供应商匹配 + MOQ；生成采购建议，企微审批
  - 现状：Web ERP 侧 `forecast_engine` / `procurement_planner` 已建（⚠️ 但生产库 `sale_order_items=0`，无法回测）
- **M4 批次效期 FEFO + 临期预警**
  - 先进先出效期管理、临期分级预警、损耗预防
  - 现状：Web ERP 侧 `batch_tracker` 已建
- **M5 专属价自动匹配 + 退货损耗追踪**
  - 8024 个专属价自动匹配、退货 / 报损单、GL 联动、撤销冲回
  - 现状：Web ERP 侧 `pricing_engine` / `wastage_tracker` 已建 ★**这是差异化护城河**
- **M6 每日盈亏快报 → 推送**
  - 自动算毛利、生成快报、微信 / 企微推送
  - 现状：`profit_reporter` + `wecom_notify` 已建

### 行动层（触达与黏性）
- **M7 自动化 / 定时任务**
  - 定时跑预测、定时推送、条件触发（如临期自动预警）
  - 现状：Web 版"定时任务"页已在
- **M8 多端连接**
  - 企业微信（主入口 Bot）、飞书、钉钉、QQ
  - 现状：Web 版"连接手机"已在；**企微 Bot 主入口需独立实现**（定位说主入口是企微）
- **M9 知识库 / 记忆 / 个性化**
  - 业务规则学习、用户偏好记忆、跨会话上下文
  - 现状：记忆模块雏形在（`memEditor`）
- **M10 团队与权限**
  - 多员工角色、操作审计、数据隔离
  - 现状：Web ERP 侧权限体系在；Web AI 层需对齐

---

## 3. 模块关联与优先级

### 3.1 依赖关系

```
                ┌─────────────────────────────────────────┐
                │            M1 数据接入与治理              │  ← 感知层（一切基础）
                └─────────────────────────────────────────┘
                   │        │        │         │
                   ▼        ▼        ▼         ▼
              M3 预测    M4 效期   M5 专属价   M6 盈亏快报
              采购单      预警     匹配/损耗    推送
                   ▲        ▲        ▲         ▲
                   │        │        │         │
        M2 对话中枢 ──── 路由 / 意图识别 ────► 上述 4 场景（用户入口）
                   │
        M7 自动化 ──触发──► M3 / M4 / M6（定时执行）
        M8 多端  ──触达──► M6 推送 / M2 对话（企微 Bot 主入口）
        M9 记忆  ──增强──► M2 / M3 / M5（个性化）
        M10 权限 ──管控──► 全部模块
```

### 3.2 优先级矩阵（价值 ↑ / 实现难度 →）

| 优先级 | 模块 | 理由 |
|--------|------|------|
| **P0** | M1 数据接入（补齐异常价检测） | 无数据 = AI 瞎；Web 版已部分具备，补齐即可 |
| **P0** | M2 对话中枢 | 用户入口，已在，需强化工具卡可视化 |
| **P0** | M5 专属价自动匹配 | **最强差异化 + 真实痛点（8024 价）**，护城河 |
| **P0** | M6 盈亏快报推送 | 最快见效、感知最强，"每天看到赚多少" |
| **P1** | M3 需求预测采购 | 高价值，但依赖 M1 数据完整（当前 `sale_order_items=0` 是 blocker） |
| **P1** | M4 效期预警 | 依赖批次数据录入 |
| **P1** | M8 企微 Bot 主入口 | 定位里说主入口是企微，需尽快打通让"不打开系统"成立 |
| **P2** | M7 高级自动化编排 | 基础定时已在，进阶编排后做 |
| **P2** | M9 知识记忆深化 | 黏性增强 |
| **P2** | M10 团队权限 | 多员工场景，规模化时做 |

> ⚠️ **关键依赖阻塞**：M3 / M4 的引擎代码已写，但**生产库 `sale_order_items = 0 行`**（517 订单无明细），预测 / 效期无法回测。M1 必须先把订单明细导入打通，否则 M3 / M4 是空中楼阁。

### 3.3 推荐 MVP 组合（v1 价值闭环）

**M1（异常价检测）+ M2 + M5（专属价匹配）+ M6（盈亏快报）+ M8（企微 Bot 只读推送）**

这 5 个能在"不打开系统"的前提下，让用户每天看到 **"今天赚多少、哪个价异常、哪个客户价要调"**——这是最小的"AI 员工"体感，且每个模块都有部分现状可复用，落地成本最低。

---

## 4. UI 设计方案

### 4.1 设计语言（延续现有，统一令牌）

沿用已建立的 design tokens（Web 版已部署 M1 / M2 / M4 / M7 收敛）：

- **圆角三级**：`8px(sm)` / `12px(md)` / `20px(lg)` + `pill`
- **字号体系**：Major `12 / 14 / 16 / 20 / 28` + Fine `10 / 11 / 13`
- **品牌色**：cyan 梯度 `--brand-50~700`
- **暗色模式**：`:root.dark` 重写全部变量（Web 版需确认暗色是否就位）
- **质感**：玻璃拟态（`.luxury-glass`）、磁性 hover、60fps 过渡
- **三级按钮**：Primary（渐变）/ Secondary（灰底）/ Ghost（透明描边）

> 冷链 / 低温奶品类联想：可在 cyan 科技感基础上引入**冰蓝 / 霜白**冷色调强化品类识别，但保持令牌体系不变。

### 4.2 关键页面与布局

**① 工作台首页（Web 落地页 / App 首页）**
- 顶部栏：用户头像 · 积分 · 模型选择（DeepSeek V4 Flash）
- 角色卡区：4 个数字员工（大秘 / 会计 / 订单文员 / 运营主管）磁吸卡片，点击进入对应对话
- 今日洞察条：盈亏快报摘要 + 异常价提醒 + 临期预警计数（M6 / M5 / M4 的浓缩入口）
- 快捷操作：上传 Excel · 查看采购建议 · 连接企微

**② 对话界面（核心交互）**
- 顶 / 侧：数字员工切换（人格切换）
- 中部：对话流，AI 回复中**内联展示工具调用卡**（"正在查 8024 个专属价…" / "已生成采购单 #123"），让用户看见"AI 在干活"
- 底部：输入框（支持附件 / Excel 拖入 → 触发 M1）
- 关键交互：用户说"明天该进多少货" → AI 调 M3 → 返回采购建议卡（含确认 / 微调按钮）→ 用户点确认 → M7 执行

**③ 数据接入中心**
- 三通道：Excel 导入（拖拽区 + 模板下载）、API 直连（金蝶 / 用友 / 畅捷通 授权卡）、手动录
- 导入后展示：数据质量报告（异常价、缺失字段）→ 直接链到 M5

**④ 业务场景看板（4 个）**
- 预测采购看板 / 效期预警看板 / 专属价看板 / 盈亏快报看板
- 每个看板 = 数据可视化 + AI 解读卡 + 执行按钮

**⑤ 自动化 / 定时任务**：列表 + 新建（触发条件 / 执行动作 / 通知渠道）
**⑥ 设置 / 连接**：模型、积分、企微 / 飞书 / 钉钉 / QQ 连接状态、记忆管理

### 4.3 交互流程（用户操作路径示例）

**路径 A：微信里"一句话出采购单"（定位主路径）**
企微 Bot 收到"帮我看下周进货" → M2 识别意图 + 时间 → M3 跑预测 → 返回采购建议卡片（企微）→ 用户点"生成采购单" → M7 执行 + 企微审批推送 → 完成。**全程不打开 Web。**

**路径 B：Web 上传 Excel 查异常价**
Web 首页拖入舟谱导出 Excel → M1 解析 → 异常价检测 → M5 匹配 → 看板高亮异常客户 / 商品 → 用户确认调价 → 推送下游。

### 4.4 视觉风格建议
- 延续 cyan 科技感 + 玻璃拟态；冷链品类可加冰蓝 / 霜白冷调
- 数据密度高的看板用**卡片网格 + 微图表（mini sparkline）**，避免大屏空旷
- AI 对话区用**"打字机 + 工具卡展开"动画**，强化"AI 在干活"的感知（这是差异化体验）
- 移动端：Web 版已自带 `@media(max-width:768px)` 汉堡菜单；桌面版底部 tab bar 已做，Web 需对齐

### 4.5 待补设计项（基于之前 UI 审查 L1–L5）
空状态插画、骨架屏（替代 spinner）、快捷键 hint、跨浏览器滚动条、CSP 收紧。

---

## 5. 落地建议 / 下一步

1. **先打通 M1 数据缺口**（导入 `sale_order_items`），解锁 M3 / M4 回测——这是当前最大 blocker。
2. **MVP 按 P0 推进**，企微 Bot（M8）作为主入口尽快打通，让"不打开系统"成立。
3. **UI 优先做三屏**：工作台首页 + 对话工具卡 + 异常价看板，形成可演示闭环。
4. **技术债**：Web 版与桌面版已分叉为两套变体（2026-07-21 确认），建议确立"Web 为主、桌面壳复用"或合并源码，避免双线维护。
5. **可选项**：我可基于本方案产出**可点击 HTML 原型**（用现有设计令牌），让你直观验收布局与交互。

---

> 附：与既有文档关系
> - `通用版产品规划*.md`、`产品优化方案_v2.md`：2026-05-09「通用个人助理」路线，**已被本 repositioning 取代**，仅作历史参考。
> - Web ERP 侧引擎（`server/domain/*`、`server/notify/*`）：M3–M6 的算法已落地，本 AI 层 Web 版需对接其 API 并补齐数据。
