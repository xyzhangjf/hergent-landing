# Hergent 产品交接文档

> **维护人**：张俊锋（老板 / PM，非技术，全程靠 AI 协作开发）
> **最后更新**：2026-08-21
> **适用对象**：未来接手的技术合伙人 / 运维 / 支持工程师
> **定位**：本文件是 Hergent 项目的**单一事实源（single source of truth）**。任何部署、架构、铁律以本文件 + 代码为准，不与口头/聊天记录冲突时以本文件为准。

---

## 0. 一句话背景

Hergent 起步是老板自己用的「低温奶经销商经营工具」，目标演化为**卖给其他快消 / 低温奶经销商的 SaaS**。核心资产是**一线经销商的懂行经验（domain expertise）**，不是代码。代码是商品、是风险。

**战略收敛（2026-08-20 老板拍板，最重要的一条）**：
- **不自研 ERP**。ERP 层交给畅捷通 / 用友 / 金蝶。
- Hergent **只做垂直行业的 AI 应用层**：数据要么用户自己上传（Excel/CSV），要么从用友/金蝶处抓取。
- **护城河** = 低温奶配方化算法（货损 / 返利 / 预报）+ 副驾交互形态，不靠通用「AI 问数」。
- 保留 Excel/CSV 降级路径（「赎身券」），绝不把客户锁死在单一数据源。

---

## 1. 系统总览与架构

### 1.1 三大代码库

| 代码库 | 路径（本地） | 角色 | 生产状态 |
|---|---|---|---|
| **hergent-cn-v2** | `/Users/zhangjunfeng/Documents/laozhangai-product/hergent-cn-v2` | AI 经营副驾前端（Vue3 SFC + Vite） | 部署 `/opt/hergent-cn-v2/`，域名 `hergent.cn` |
| **hergent-erp** | `/Users/zhangjunfeng/Documents/hergent-erp/` | 后端 FastAPI（`server.py` + `routers/*` + `erp_db.py`）+ ERP 后台前端（原生 JS + Vite） | 后端 `server.py:8700`；ERP 后台 `erp.hergent.cn` |
| **Hermes 引擎** | laozhangai-product/engines/hermes/ | 内嵌 AI 大脑（v0.18.2 本机 / v0.19.0 生产） | 生产 `:18765`，Bearer `hergent-prod-gateway-key-2026` |

> desktop-app（Electron 桌面端）**已冻结**，非主产品，勿投入。

### 1.2 数据流（解耦后目标架构，2026-08-21 落地骨架）

```
用户数据（用友/金蝶 API 或 Excel/CSV 上传）
        │
        ▼
DataSourceAdapter 数据适配层（server/datasource_adapter.py）
   ├─ LocalErpAdapter      （默认，读本地 tenant SQLite，行为不变）
   ├─ KingdeeAdapter       （规划中，读金蝶/用友 API）
   └─ ExcelUploadAdapter   （规划中，读用户上传快照表）
        │
        ▼
配方化引擎（护城河，6 个已在生产后端）
   预报 / 货损 / 工资 / 对账 / 指标 / 返利
        │
        ▼
AI 经营副驾（CopilotDrawer.vue：左聊天 + 右产物）
   货损卡 / 工资卡 / 返利卡 / 预报卡 / 经营简报
```

**关键解耦点**：副驾 4 张经营卡**不再直读本地表**，统一经 `get_business_adapter(tenant)` 读适配层。当前默认仍走 `LocalErpAdapter`（行为完全保真），激活 Kingdee/Excel 适配器后即可脱离本地订单表。

### 1.3 多租户隔离

- 物理分库：`tenant_<id>.db` 独立文件（非同一库分表）。
- `check_user_tenant(user_id, tenant_id)` 走 `user_tenants` 表判定；中间件每请求校验，越界 403。
- ⚠️ 已修复：`admin` 角色**不再**无条件跨租户放行（2026-08-20 堵掉理论越权）。

---

## 2. 技术栈

- **后端**：FastAPI + SQLite（多租户物理分库）+ 原生 JS（ERP 后台）
- **副驾前端**：Vue3 + Vite + Pinia + hash 路由（双主题 light/dark）
- **ERP 后台前端**：原生 JS（`static/` 是 Vite 工程，构建产物 `static/dist/`）
- **AI 引擎**：Hermes（模型无关，当前 deepseek-v4-flash）
- **部署**：nginx（静态）+ systemd（后端）+ rsync 同步
- **运行时**：生产 Python 3（见部署真相）；本地推荐用 managed runtime

---

## 3. 部署真相（唯一权威，改前必读）

### 3.1 后端（hergent-erp）

```
# 1) 本地改完，语法校验
cd /Users/zhangjunfeng/Documents/hergent-erp/server
python3 -m py_compile *.py        # 至少编译 server.py / erp_db.py / 改动的 router

# 2) flatten rsync 到生产（绝带 *.db/*.db-wal/*.db-shm！）
rsync -a --no-owner --no-group --exclude=static --exclude=__pycache__ \
      --exclude='*.pyc' --exclude='*.db' --exclude='*.db-wal' --exclude='*.db-shm' \
      server/ root@47.113.224.140:/opt/hergent-erp/

# 3) 权限 + 清缓存 + 重启
ssh root@47.113.224.140 "chown -R hergent:hergent /opt/hergent-erp && \
      rm -rf /opt/hergent-erp/__pycache__ && systemctl restart hergent-erp"

# 4) 健康检查
curl -s --noproxy '*' http://127.0.0.1:8700/api/health
```

- **SSH**：`ssh -i ~/.ssh/id_ed25519 root@47.113.224.140`
- **新后端路由两步**：`server.py` 的 `_PATH_MODULE_MAP` 加 `prefix→module` + handler `async def`。
- **生产路径**：`/opt/hergent-erp/`，主库 `erp.db`，端口 `8700`。
- **域名**：`hergent.cn` 的 `/api/` → 8700；`erp.hergent.cn` 同源；`/hermes/` → 18765。

### 3.2 副驾前端（hergent-cn-v2）

```
npm run build
rsync -a --no-owner --no-group --delete dist/ root@47.113.224.140:/opt/hergent-cn-v2/
# nginx root=/opt/hergent-cn-v2，纯静态，无需 restart
```

### 3.3 ⚠️ 运维卫生铁律（2026-08-21 事故后新增）

**每次重启 / 部署后端，必须确认 8700 只有一个进程、无孤儿，并 `nginx -s reload` 清 keepalive。**

> 事故还原：生产曾并存两个 `server.py` 进程（一个 8/2 手动起的孤儿，旧代码未映射新 RBAC 路径 → 返回 403）。nginx keepalive 把部分请求路由到旧进程，导致副驾首屏零星 403。
> 修复：`systemctl stop` → **按精确 PID** `kill -9 <pid>` 孤儿（⚠️ 禁止 `pkill -f server.py`，会误杀 ssh 自身会话）→ `systemctl start` → `nginx -s reload`。
> 验证：`ss -ltnp | grep 8700` 应只有一个 PID；boss token 轮询 `dashboard` 应稳定 200。

⚠️ **致命补充（2026-08-21 复盘）**：生产有 **3 个独立的 `server.py` 系服务**，cmdline 完全相同，只能靠 `cwd`/端口区分，**绝不能凭 cmdline 见 `server.py` 就杀**：

| 服务 | systemd 单元 | 用户 | 目录(cwd) | 端口 | 作用 |
|---|---|---|---|---|---|
| **ERP（真身）** | `hergent-erp.service` | `hergent` | `/opt/hergent-erp` | **8700** | 副驾+ERP 后端，唯一要守护的 |
| Hermes/积分服务 | `hergent-server.service` | `root` | `/opt/hergent/server` | **8765** | 旧的 Hermes/积分服务（2026-07-22 遗留，重启策略 `Restart=always`） |
| 管理后台 | `hergent-admin.service` | `root` | — | 8766 | 管理后台 |

> 误杀教训：本轮部署时一度把 8765 的 Hermes 进程当成「ERP 孤儿」想 kill（其 cmdline 也是 `python3 server.py`）——幸先查 `ls -l /proc/<pid>/cwd` + `ss -ltnp` 确认它绑 8765、cwd 是 `/opt/hergent/server`，才没误杀副驾大脑。
> **正确排查顺序**：① `ss -ltnp | grep 8700` 看是否唯一 ERP PID；② 若有多余，**先 `ls -l /proc/<pid>/cwd`** 确认是 `/opt/hergent-erp` 再杀；③ 端口 8765/8766 的进程一律不动（除非老板明确要下线 `hergent-server.service`）。

---

## 4. 设计铁律（must-follow，违反即返工）

1. **AI 只建议，不擅自下单**。定稿必须人点（货损/工资/返利/预报审核均如此）。
2. **蒙牛 / 舟谱无开放 API**。只生成粘贴模板 / 导入 CSV，不承诺自动直连。
3. **Hermes 版本 pin 住**，不追双周大版。
4. **6 引擎已在生产后端**：预报 / 货损 / 工资 / 对账 / 指标 / 返利。

### Web ERP 开发铁律（v90.1）

1. `app.js`（~7000 行）**只读不写**，新功能 → `js/modules/xxx.js`（IIFE）。
2. `db/connection.py` 是**唯一 DB 真理源**；`get_db()` 的 `finally` 含 rollback——写完须显式 `commit` 或 `get_db_tx()`。
3. `get_db()` 受租户上下文影响；**全局表须直连主库** `sqlite3.connect(DB_PATH)`。
4. **禁 `sed` 批量改**，须 `Read` + `Edit` 逐处。改完 `node --check` + `py_compile`。
5. 密钥 **fail-closed**：缺失即报错，不静默降级。勿直读 `/root/.hermes`（周期重置 700）。

### 前端规范（Vue3 + Vite + Pinia + hash 路由）

- 设计令牌：`variables.css` 双主题（`:root` 浅 / `+:root.dark`）。品牌青 `--p=#06b6d4`；语义 `--suc/--dan/--war`；圆角 `--radius-*`。通用 `.btn/.card/.badge/.tag/.progress/.table-wrap`。
- 副驾骨架：`CopilotDrawer.vue` 左聊天 + 右产物双栏；`ResultCard.vue` 渲染结构化卡（compact 压缩模式供右栏瓦片）。
- 验收标准：**每次改动上线前必须无头浏览器真机运行时验证**；杜绝 `node --check` 假阳性与浏览器 eval 假登录两类已知 Bug。

---

## 5. 功能进度清单

### 已落地（生产可用）

- **副驾 4 卡 + 经营简报**：货损 / 工资 / 返利卡（只读聚合）→ `ResultCard`；每日 8:05 经营卡简报推送（企微/飞书，未配 webhook 自动 no-op）。
- **预报交叉表视图**：`Forecast.vue` list/cross tabs；冻结表头/首列；行=商品、列=动态客户；计算列（合计/件数/最终下单/单价/金额）。
- **Excel 导入 forecast_cross**：`import_router.py` 识别「商品身份列 + 动态客户列」，逐客户列生成 `forecast_submission_create`（幂等）。
- **定时催单**：每天 9:00 / 16:00 按租户遍历提醒；`POST /api/forecast-submissions/remind` 手动端点。
- **副驾预报卡**：`GET /api/ai/forecast-card`（未报单元 warn + 近7日报单趋势 sparkline + source 溯源）。
- **周期级 AI 审核台（P1-1）**：`routers/forecast_audit.py` 整周期批量审核 + 厂家返利冲量提示；`POST /audit-period/adopt` 定稿落库（幂等，AI 留痕，**绝不自动下单**）。
- **余额 & 付款闭环（P1-2）**：`supplier_balances` + `period_payments`；订单金额 − 厂家余额 = 差额，差额>0 自动推付款提醒。
- **一键舟谱模板（P1-3）**：`POST /api/forecast/zhoupu-template` 确定性生成 CSV（utf-8 BOM）。
- **结构化拆字段（P1-4）**：products 加 `ordering_entity`/`arrival_lead_days`；导入正则解析「红桶（恒滋下单）」「+3到货」回写。
- **返利冲档测算（P2-1）**：`rebate_gap_projection`（采购额口径 + 阶梯解析 → 缺口/收益毛估）。
- **数据适配层（2026-08-21）**：`server/datasource_adapter.py`，见 §1.2。

### 待办（按优先级）

- **P0 收尾**：单品级厂家指标表；交叉表最终下单列并入 decisions；`T10` 数据来源徽标。
- **Phase 2 解耦**：激活 `KingdeeAdapter`（读 `datasource_store` 租户凭据 → 金蝶/用友 API）；激活 `ExcelUploadAdapter`（读上传快照表）；把 `_push_copilot_cards_brief` 调度也收口到适配层。
- **P2-1 数据问题**：tenant_1 返利合同 `contact_id=1` 无效引用（真实蒙牛拆成 2912/2920/2921/2923/2910 多主体），需老板确认总指标绑定口径后修合同。

---

## 6. 已知坑 / 已修复事故

| 事故 | 现象 | 根因 | 修复 |
|---|---|---|---|
| **孤儿进程 403（2026-08-21）** | 副驾首屏 8 接口零星 403 | 生产双 `server.py` 进程，旧孤儿未映射新 RBAC 路径 | 精确 PID 杀孤儿 → restart → `nginx -s reload`（见 §3.3） |
| **ERP 401 竞态（2026-08-20）** | boss 登录后极快跳回登录页 | 前端初始化并发拉 dashboard，预登录 401 异步迟到冲掉会话 | `api-client.js` 加 `hadToken` 守卫；`app.js` 忽略 `_noauth` |
| **admin 跨租户越权（2026-08-20）** | 理论漏洞 | `check_user_tenant` 对 admin 无条件放行 | 删放行；cross-join 改绑 tenant_1 |
| **真实表导入陷阱（2026-08-20）** | 垃圾行 | Excel 计算列（合计/件数/下单金额/永辉代码/商品编码'3'）被当客户列 | `import_router.py` 加 `_CROSS_SKIP_KEYWORDS` + 纯数字表头跳过 + `identity_like` 排除 |
| **summary 坍缩（2026-08-20）** | 不同商品同桶 | `sources` 按 `(product_id,unit)` 分组，`product_id=0` 坍缩 | 分组键加 `product_name/spec` |
| **detail 500（2026-08-20）** | 接口 500 | 漏选 `i.spec` | 补选 |

> 浏览器控制台 `COMPUTE_PAGE_FINGERPRINT` / `content.js` 报错 —— 来自某**浏览器插件**（隐私/指纹防护类），与系统无关，可忽略。

---

## 7. 生产数据缺口（需老板人工补录）

- 7 名员工 `salary_structure` 为空 → 喂入 payroll 工作流前须补。
- 428 个 SKU 中 **54 行**库存缺 `batch_no` / `expiry_date` → 喂入 loss 工作流前须补。
- tenant_1 返利合同 `contact_id` 无效引用（见 §5 P2-1）。

---

## 8. 密钥与凭据管理（⚠️ 高风险）

- **`企业微信API.docx` 含明文凭据**：GitHub PAT ×2 / 阿里云 AK ×2 / LLM key / 企微 Secret。
- **强烈建议**迁移到密钥管理器（如 1Password / Vault），勿长期明文留存。
- 生产 Hermes Bearer：`hergent-prod-gateway-key-2026`（配置于 `config.yaml` 或 systemd env）。
- 生产 SSH：`~/.ssh/id_ed25519`（root@47.113.224.140）。

---

## 9. 给技术合伙人的上手路线

1. 读本文 §0–§4，建立战略与架构心智模型。
2. 本地拉 `hergent-erp`（分支 `upgrade/v84-international`）+ `hergent-cn-v2`，按 §3 跑通本地。
3. 重点理解 `server/datasource_adapter.py`（解耦核心）与 6 个配方化引擎（护城河）。
4. 部署严格走 §3，部署后必做 §3.3 运维卫生检查。
5. 任何「AI 自动下单 / 跨租户 / 直连蒙牛舟谱」类需求，先回看 §4 设计铁律，违者勿做。

---

*本文件随每次重大变更更新。新增事故 / 铁律 / 部署变更请直接 append 对应章节并改顶部「最后更新」日期。*
