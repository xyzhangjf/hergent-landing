# 全站内联事件传参问题排查修复清单（v107.37）

> 日期：2026-08-04 · commit `5c84374` · 线上 bundle `main-XwkHKETa.js`
> 背景：v107.34 修复商品列表"复制"SyntaxError（`esc` 的 `&#39;` 被 HTML 解码成裸引号导致内联 JS 字符串炸裂），本次对**全站同类问题**做逐页逐模块收尾排查。

## 一、排查范围与结论

| 扫描维度 | 结果 |
|---|---|
| 事件属性（onclick/onchange/onkeyup/oninput/onmouse\* 等双引号+单引号）内 `esc(` 残留 | **修复前 7 处 → 清零** |
| `href="javascript:"` 内联传参 | 0 处 ✓ |
| 右键菜单/动态 action 字符串内 `esc(` 拼接 | 0 处 ✓（v107.34 已手动修） |
| 非事件属性 `esc(`（HTML 文本/普通属性上下文，应保持 esc） | 594 处 ✓ 正确无需改 |
| `copyText` 可选参数 el 缺失类（v107.35 同类） | 已核实全部调用点安全 ✓ |

## 二、v107.34 盲区复盘（根因）

v107.34 的替换脚本用 `onclick="[^"]*?esc(` **非贪婪前缀匹配**，存在两个盲区：
1. **同一事件属性内的第 2+ 个 `esc(` 全漏**——非贪婪匹配 + 替换不重叠，每个属性只处理到第一个 `esc(`
2. **只覆盖了 `onclick`**——`onchange`/`onkeyup` 等其它事件属性漏网

典型漏网点：`editBrand(..., \''+jsEsc(b.name)+'\', \''+esc(b.manufacturer)+...` —— 第一个参数已换，后续参数仍是 `esc`。

## 三、本轮修复清单（6 文件 +8 处）

| # | 文件 | 行号 | 位置 | 修复内容 |
|---|------|------|------|---------|
| 1 | app.js | 1928 | 品牌管理 `editBrand`（8 参数） | 后续参数 `esc` → `jsEsc`（+2 处） |
| 2 | app.js | 2015 | 单位管理 `showEditUnitForm`（7 参数） | 后续参数 `esc` → `jsEsc` |
| 3 | js/modules/unified-dashboard.js | 78 | 经营看板今日行动按钮 | `esc` → `jsEsc` |
| 4 | js/modules/new-features.js | 164 | 新功能语音确认订单 | `esc` → `jsEsc` |
| 5 | js/modules/report-designer.js | 46 | 报表设计器字段添加（2 处） | `esc` → `jsEsc` |
| 6 | js/modules/tenant-admin.js | 56 | 租户管理编辑弹窗 | `esc` → `jsEsc` |
| 7 | js/modules/review-queue.js | 316 | 审核中心信任级别 `onchange`（onclick 外漏网） | `esc` → `jsEsc` |

修复方式：按**整个事件属性值**为单位贪婪匹配，内部**所有** `esc(` → `jsEsc(`（`jsEsc` 内的 esc 无单词边界不会误伤）。

## 四、验证结果

- 事件属性内 `esc(` 残留：**0 处** ✓（复扫全站）
- `node --check` 8 个改动文件：全部通过 ✓
- 构建成功（main + mobile）✓ → 部署 ✓ → 线上 200 ✓

## 五、设计说明（为什么这样分层）

- **`esc()`**：HTML 上下文转义（文本节点/普通属性值），输出 `&amp; &lt; &gt; &quot; &#39;`——全站 594 处保留
- **`jsEsc()`**：内联事件属性里**将被当 JS 源码执行**的参数转义——先转 JS 危险字符（`\` `'` `\r` `\n`）再 HTML 实体化；修复后浏览器解码 → JS 解析全链路安全
- 判别标准：**值最终进入 JS 字符串字面量**（事件属性/动态 action）→ jsEsc；**只进入 HTML 内容** → esc

## 六、遗留建议

- 事件属性内的内联传参本质是"字符串拼代码"，未来新代码建议优先用 `data-*` 属性 + 事件委托（如 `data-copy` 模式），从根上消除此类问题
- 可在 CI/提交前加一条静态检查：事件属性内出现 `esc(` 即告警
