# v233 · 「报单单位」修复说明（2026-09-21）

> 一句话：**系统里一个商品其实有两个「单位」**（档案单位 / 报单单位），一直挤在同一格；
> 现在把它拆成两格，并且**改单位时数量会一起换**（以前只换个标签、数字不动 —— 这正是
> 你 9 月报过的那个「条/件」问题）。后端已提交（`80d8f92`），本次随同部署。

## 一、问题是什么（为什么「单位」会打架）

一个商品在系统里有两个不同的「单位」概念，以前**共用一列**：

| | 什么意思 | 谁在用它 |
|---|---|---|
| **档案单位** | 舟谱里的「**小单位**」（最细的那个） | 档案对账、价格档位 |
| **报单单位** | 你实际**下单**用的那个单位（可以是「组」这种中单位） | 主表「单位」列、舟谱模板「`*单位`」列 |

两者挤在一格 ⇒ 谁写进去就按谁的算，**这就是「条 → 件」问题的根**。

举你报过的那个例子（真实数据）：

> 商品 `160红枣5连杯`，规格填的是 `8`，档案里的单位是「**件**」，而报单明细里写的是「**条**」。

系统按档案显示「件」，舟谱要的是「条」⇒ **量纲差 8 倍**。

**再看一层**：档案里有 61 个商品的单位写着「件」，但**它们的大单位换算、大单位名都是空的**。
不是数据撞车，是**当初从舟谱导档案时这些商品压根没录过小单位**，导入时落了默认值「件」——
而「件」在舟谱里是**大单位**。⇒ 同一列里并排放着两种语义。

## 二、改了什么

| | 改之前 | 改之后 |
|---|---|---|
| 「单位」有几格 | 1 格（两种语义混用） | **2 格**：档案单位 + **报单单位** |
| 报单单位填哪 | 没地方填（只能在别处的条码表里维护） | **商品档案 → 报单单位**（留空 = 跟随左边的「单位」） |
| 主表「单位」列取自 | 档案单位，可能被条码表覆盖 | **统一取「报单单位」**（留空则回退档案单位） |
| 舟谱模板「`*单位`」取自 | 条码表 → 明细 → 猜名称 | **同一个「报单单位」**（与主表**同源**，不会再各说各话） |
| 🔴 改单位时「`*数量`」 | **不动**（只换标签） | **按档案换算一起换**；换不出来就**保持原样并告警** |

第三行与第四行是这个改动的关键：**主表和舟谱模板从此看同一个值**，不会再出现「主表说件、
舟谱要条」这种对不上的情况。

最后一行是安全阀：**宁可不动，也不写一张量纲可能错的单**。
换算不出来时（该商品没录中/大单位换算、规格串里也定位不到单位），系统会
**保留你填的单位和数量原样**，并在下载时的告警里**点名是哪几行**，让你去档案里补。

## 三、这次数据上动了什么（上线前已算清，不是事后才知道）

**回填来源**：全部来自你**自己维护过**的那张「条码 → 单位」表（115 行），
**没有一条是系统猜的**。

| | 数量 |
|---|---:|
| 命中并回填的**商品** | **29 个** |
| 其中会让**主表「单位」列变化**的行 | **22 行**（件 → 条 / 组 / 板 / 包 / 提 / 瓶） |

另有一个候选来源（「报单明细里填的单位和档案不一致」的商品）**实测 0 条**：
它们全被一道「**信任档案**」的闸门挡下了。举个真实的反例说明为什么必须有这道闸门：

> 商品 `id=1316`：档案=**盒**、大单位=**件**、换算=**12**，而某条报单明细里填了「件」。
> 舟谱的规则是「填了大单位就必须填大单位换算」⇒ 有换算说明**档案是认真录过的**。
> **不加闸门就会把对的改成错的。**

## 四、会不会影响已经导出的模板？

对 `tenant_1` 现有 107 行报单明细做了**逐行对比**（跑的是线上同一份代码）：

| 分类 | 行数 |
|---|---:|
| 完全一致（单位、数量都不变） | **105** |
| 单位变化 | 0 |
| 只有数量变化 | 0 |
| 🔴 「换算不出来 ⇒ 保持原样 + 告警」 | **2** |

那 **2 行**是 `CD杯（16连）` 与 `CD杯8杯`：明细填「件」、档案是「组」、又没有换算、
规格是纯数字（定位不到单位）⇒ 系统**换不出来**，于是**保持你填的「件 + 数量」不动**并告警。

⚠️ **这 2 行在改之前是错的**：旧版本会输出「`组 / 1`」——**标签换成「组」了，数量还是「1」**，
这正是「只换标签不换数」的老毛病。**所以这 2 行变了，是修对了，不是修坏了。**

⇒ **需要你确认的一件事**：这 2 个商品的单位到底该按哪个算？
如果你说按「组」，我就在商品档案里给它补上换算（补完它会自动按「组」换算数量）。

## 五、你要做什么

1. **核对那 29 个商品**的「报单单位」是否填对了（在「商品档案」页新增了一列「报单单位」，
   显示「跟随」= 没单独指定，跟随左边的「单位」）。
2. **看到下载告警里点名的那几行**（形如「🔴 有 N 行报单单位与明细单位不一致、但换算比缺失…」）
   ⇒ 去「商品档案」把该商品的中/大单位换算或规格补齐。
3. 以后**新指定**报单单位一律在「商品档案 → 报单单位」里改；
   旧的「条码 → 单位」表**保留可用但别再往里加**（两份数据会漂移）。

## 六、风险与回滚

- **风险**：主表「单位」列有 22 行会变（件 → 条/组/板/…）——这是**目的本身**，不是副作用。
- **回滚**：回填脚本会输出**回滚 SQL**，并留一份**在线备份**；要退回随时可用。
- **不做的事**：没有删任何表、没有改任何价格、没有动报单数量本身。
