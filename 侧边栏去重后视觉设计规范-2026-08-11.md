# 侧边栏去重后 · 视觉设计与布局规范

> 配套文档：《侧边栏整合方案-2026-08-11.md》《侧边栏重叠分析-2026-08-11.md》
> 目的：详细描述去重功能落地后侧边栏的视觉设计、布局结构、状态样式与响应式表现，作为实施依据。
> 风格基准：完全复用现有 `styles.css` / `variables.css` 的令牌与类名（已逐条核对线上真实值），**不引入新的设计语言**。
> 真实令牌（浅色生产主题，`:root`）：`--p:#1f4e79`（主色/选中左条）、`--pd:#16375a`（选中文字）、`--brand-50:#eff4ff`（选中底色）、`--bg3:#fbfbfd`、`--bd:rgba(0,0,0,.10)`、`--t1:#000/.80→ rgba(0,0,0,1)`、`--t2:rgba(0,0,0,.80)`、`--t3:rgba(0,0,0,.65)`、`--surface:#fff`、`--sw:208px`、`--sw-min:56px`、`--text-base:14px`。

---

## 一、去重后的条目清单（哪些合并/隐藏，哪些保留）

### 1.1 被合并 / 隐藏的独立入口（功能在「经营业务」聚合面板内仍可达，独立入口移除）
按《重叠分析》🔴 完全重合 / 🟠 同功能异名项，移除其独立 `ModuleRegistry.register` 调用，仅保留经营业务面板内的同名子项：

| 被移除的独立入口 | 合并进的经营业务面板子项 | 入口去向 |
|---|---|---|
| 付款批 | 资金管理 →「付款批」`navTo('paymentrun')` | 面板内可达 |
| 催收管理 | 资金管理 →「催收管理」`navTo('dunning')` | 面板内可达 |
| GL维度 | 财务管理 →「GL 维度」`navTo('gldimensions')` | 面板内可达 |
| 税务管理 | 财务管理 →「税务管理」`navTo('taxmanager')` | 面板内可达 |
| 一键报税 | 财务管理 →「报税」`navTo('taxfiling')` | 面板内可达（命名归一） |
| 凭证模板 | 财务管理 →「单据凭证模版」`render_voucher_templates` | 面板内可达（命名归一） |
| 流水导入 | 资金管理 →「付款导入」`render_payment_import` | 面板内可达（命名归一） |
| 打印模板 | 财务管理 →「打印模板」`navTo('print-templates')` | 面板内可达 |
| 智能导入 | 仓配管理 →「智能导入」`render_smart_import`（+ 档案管理期初录入复用） | 面板内可达（移除三重入口中的独立项） |
| 临期看板 | 仓配管理 →「临期预警表」`navTo('expiry')` | 面板内可达（同一 expiry 页） |
| 盈亏快报 | 财务管理 →「利润表」`navTo('profit-report')` | 面板内可达（确定性完全重复） |

> 共 **11 项**独立入口被隐藏；对应 render 函数不删，仍由聚合面板调用。

### 1.2 保留为「可折叠子分组」的残余条目（🟡 概念重叠 / 无重叠，不宜直接合并）
这些功能虽有主题重叠，但形态/入口独立，保留在经营业务伞形下的折叠分组中，默认折叠：

- **仓库作业**（2）：AI采购控制台、报损管理
- **财务中心**（4）：银企互联、发票扫码、工资条群发、五险一金
- **系统工具**（6）：批量打印、电子档案、BPM流程、租户管理、开发者后台、Feature Flags
- **扩展功能**（3）：预报订单、数据源、专属价查询

### 1.3 始终保留的顶层项（不变）
- 协同工具（无标签行）：消息、通讯录、审核中心
- 经营业务（7，扁平常驻）：经营看板、档案管理、采销管理、仓配管理、资金管理、财务管理、数据报表
- 管理：设置

### 1.4 去重前后数量对比
| 维度 | 去重前 | 去重后 |
|---|---|---|
| 顶层分区标题 | 6（消息中心/经营业务/管理/仓库操作/财务工具/系统工具/扩展功能） | **2 主区 + 4 折叠分组**（经营业务 + 仓库作业/财务中心/系统工具/扩展功能 + 管理） |
| 可见菜单项总数 | 37 | **26**（3 协同 + 7 经营 + 15 折叠 + 1 设置） |
| 默认展开项 | 经营业务 7 + 各折叠组 | 经营业务 7（+ 协同 3 + 设置）常驻；4 个折叠组默认收起 → 初始仅 ~11 项 + 5 个分组头 |

---

## 二、整体布局结构（桌面端竖向分区）

```
┌─────────────────────────┐  ← #sidebar  width:var(--sw)=208px
│ [icon] 消息              │     背景 var(--surface)  #fff
│ [icon] 通讯录            │     无分组标签（协同工具行）
│ [icon] 审核中心          │
│ ─────────────────────── │  ← .sb-divider  margin:8px 14px; bg:var(--bd)
│ 经营业务                 │  ← .sb-label  10px 大写灰 #t3 间距1.5px
│ [icon] 经营看板          │
│ [icon] 档案管理          │
│ [icon] 采销管理          │     7 项扁平常驻（hover→聚合面板）
│ [icon] 仓配管理   ▸      │     （仓配/资金/财务/报表 hover 弹出 OperationsHub）
│ [icon] 资金管理   ▸      │
│ [icon] 财务管理   ▸      │
│ [icon] 数据报表   ▸      │
│ ─────────────────────── │
│ ▾ 仓库作业               │  ← 折叠分组头 .sb-label + ▼箭头
│   [icon] AI采购控制台    │     （默认收起，点击 ▾ 展开）
│   [icon] 报损管理        │
│ ▾ 财务中心               │
│   [icon] 银企互联        │
│   [icon] 发票扫码        │
│   [icon] 工资条群发      │
│   [icon] 五险一金        │
│ ▾ 系统工具               │
│   [icon] 批量打印 …      │     （6 项）
│ ▾ 扩展功能               │
│   [icon] 预报订单 …      │     （3 项）
│ ─────────────────────── │
│ 管理                     │
│ [icon] 设置              │
└─────────────────────────┘
```

**排列顺序规则**：协同工具（消息→通讯录→审核中心）→ 经营业务 7 项（看板→档案→采销→仓配→资金→财务→报表，沿用现有顺序）→ 四个折叠分组（仓库作业→财务中心→系统工具→扩展功能，按业务相关度排序）→ 管理（设置）。分组内按现有 `ModuleRegistry` 注册顺序。

---

## 三、七个维度视觉规范

### 3.1 菜单项数量与排列顺序
- 常驻可见：3 协同 + 7 经营 + 1 设置 + 5 分组头 = **16 个元素**（不含折叠组内项）。
- 折叠组内共 15 项，默认隐藏，点击分组头展开。
- 顺序见 §二，严格沿用现有条目顺序，不重排业务语义。

### 3.2 分组 / 分类方式
- **两层模型**：分区标签（`.sb-label`）+ 扁平项列表（容器内 `.sb-item`），**不做多级树缩进**。
- 两类分区：
  1. **常驻区**（无折叠）：经营业务（7 项始终展开）；协同工具行（无标签，直接 3 项）；管理（设置）。
  2. **折叠区**（`.sb-group` + `sb-group-collapsed`）：仓库作业 / 财务中心 / 系统工具 / 扩展功能，默认全部收起，点击 `.sb-label` 切换。
- 折叠交互沿用现有 `module-registry.js` 逻辑：点击 header → `toggle('sb-group-collapsed')` → 箭头 `▼`/`▶` 互换（`grp-arrow`）。**默认折叠**由 `sidebar-v2.js` 的 `_rebuildSidebar` 后处理统一加 `sb-group-collapsed`（现有行为，保留）。

### 3.3 选中 / 未选中状态样式（直接复用 styles.css:137-148）
| 状态 | 背景 | 文字 | 图标 | 左指示条 | 字重 |
|---|---|---|---|---|---|
| 默认 `.sb-item` | 透明 | `var(--t2)` 黑80% | svg opacity .5 | 无 | 450 |
| Hover | `var(--bg3)` #fbfbfd | `var(--t1)` | — | 无 | —（轻微 `translateX(2px)`） |
| **选中 `.sb-item.active`** | `var(--brand-50)` #eff4ff | `var(--pd)` #16375a | svg opacity **1** | `::before` 3px 高亮条 `var(--p)` #1f4e79，入场动画 `indicatorIn .25s` | **600** |
| 深色 `:root.dark` 选中 | `var(--p)` #1f4e79 | `#fff` | `#fff` | `#fff` | 600 |

- 选中态由 `navTo` / `_v2Nav` 统一清除其他 `.active` 再给当前项加（现有机制，去重后分组项也走同一 `navTo(m.id)`，自动高亮）。

### 3.4 图标与文字呈现形式
- **图标**：内联 SVG，`currentColor` 描边（与文字同色，随状态变色）。硬编码项用 `si()` 包裹 **18×18**（sidebar-v2.js:11）；注册项用 **16×16**（module-registry.js:143）。**规范建议统一为 18×18** 以保持视觉一致。
- **文字**：`<span>` 包裹，`white-space:nowrap`，字号 `var(--text-base)=14px`，行高 `var(--leading-normal)=1.5`。
- **图标+文字布局**：`.sb-item` 为 `display:flex; align-items:center; gap:10px`，图标 `flex-shrink:0`。
- **徽标**：审核中心带 `#reviewBadge`（`.sb-badge`，右上角计数，默认 `display:none`），沿用不变。

### 3.5 层级缩进规则
- 当前为**扁平两级**：分组头与项下标左内边距一致（均为 `10px`），层级仅靠 `.sb-label`（大写灰小字 + 箭头）区分，**无缩进阶梯**。
- **规范建议（可选增强）**：折叠组内项相对分组头增加 **8–12px 左缩进**（`padding-left:18–22px`），让"头→项"的从属关系更直观；若不增缩进亦完全符合现有风格（保持现状即可）。
- 经营业务 7 项与协同/管理项同级，均顶格（左 10px）。

### 3.6 空白区域与分隔线处理
- **分隔线 `.sb-divider`**：`margin:8px 14px; height:1px; background:var(--bd)`。用于三大主区间隔离：协同工具 ↔ 经营业务、经营业务 ↔ 折叠分组区、折叠分组区 ↔ 管理。**折叠分组之间不用分隔线**，靠 `.sb-label` 的 `margin-top:4px` + 上间距（`.sb-label` padding `14px 10px 6px`）自然留白。
- **留白**：分组标签上方留 `14px` 呼吸空间；项与项间距由 `padding:9px 10px` + `border-radius:8px` 形成卡片感；hover 时 `translateX(2px)` 微动反馈。
- **空状态**：若某折叠分组无可见项（理论不会），则该分组头连同容器整体不渲染（沿用 `module-registry.js` 的 `if(!mods||!mods.length) return`）。

### 3.7 响应式适配（桌面 / 移动）

**现有规则（styles.css:732，`@media(max-width:1024px)`）**：侧边栏宽塌缩为 `var(--sw-min)=56px` icon rail；`.sb-item span`、`.sb-label`、`.sb-group`、`.sb-divider` 全部 `display:none!important`；`.sb-item` 居中仅显图标。

**⚠️ 现存问题（去重后须修复）**：当前 ≤1024px 直接 `display:none` 隐藏 `.sb-group`，意味着**折叠分组内的残余模块在移动端完全不可达**；且经营业务的 仓配/资金/财务/报表 依赖 **hover 弹出** OperationsHub 面板，触屏无 hover → 不可用。

**去重后响应式规格**：
| 断点 | 侧边栏形态 | 主区（经营业务7/协同/设置） | 折叠分组（仓库作业等15项） | 聚合面板（OperationsHub） |
|---|---|---|---|---|
| **≥1025px 桌面** | 208px 常驻抽屉 | 全显标签+图标 | 头可见、默认收起、点击展开 | hover 弹出玻璃面板（现有） |
| **768–1024px 平板** | 56px icon rail | 仅图标（标签隐藏），点击触发 | **改为点击手风琴**：分组头图标可见，点击就地展开子项（覆盖 `sb-group{display:none}` 为 `display:block`） | 改为**点击展开**（禁用 hover，绑定 tap 打开内联面板/全屏 sheet） |
| **≤767px 手机** | 默认隐藏，汉堡键（`topBar` 左）唤出 **overlay 抽屉**（复用 208px DOM） | 抽屉内全显 | 抽屉内同桌面手风琴 | 点击打开全屏 sheet |

**移动端适配要点**：
1. 取消 `.sb-group{display:none!important}` 的硬隐藏，改为"默认折叠 + 点击展开"的 accordion，保证 15 个残余模块在窄屏可达。
2. OperationsHub 的 `mouseover` 委托在触屏改为 `click`/`touchstart` 触发，展开为全屏或半屏 sheet（避免 hover 失效）。
3. 56px rail 下，分组头以"分组首图标 + 数量角标"呈现（如 📦2），点击展开；或统一收到汉堡抽屉，rail 只留经营业务 7 图标。
4. 深色主题令牌由 `:root.dark` 自动翻转（bg/surface/t/bd），无需额外适配。

---

## 四、与现有界面一致性核对（实施 Checklist）

- [ ] 所有项复用 `.sb-item` / `.sb-label` / `.sb-divider` / `.sb-group` / `.grp-arrow` 类，不新增样式类。
- [ ] 颜色全部走令牌：`--p / --pd / --brand-50 / --bg3 / --bd / --t1-3 / --surface`，深浅主题自动适配。
- [ ] 圆角 `8px`、gap `10px`、字号 `14px`、选中左条 `3px`、hover `translateX(2px)` 与现有完全一致。
- [ ] 折叠交互沿用 `module-registry.js` 的 `sb-group-collapsed` 切换 + 箭头互换。
- [ ] 默认折叠行为沿用 `sidebar-v2.js` 的 `_rebuildSidebar` 后处理（给 registry 组统一加 collapsed）。
- [ ] 选中高亮沿用 `navTo`/`_v2Nav` 的 `.active` 统一清除+赋值。
- [ ] 新增的 4 个折叠分组头用同一 `.sb-label` 渲染（含 `data-mod-registry=header`），仅分组名不同。

---

## 五、DOM 结构示意（实施可直接套用）

```html
<!-- 协同工具行（无标签） -->
<button class="sb-item sb-sub-item" data-role="dami">[svg]消息</button>
<button class="sb-item sb-sub-item" data-mod="contacts">[svg]通讯录</button>
<button class="sb-item sb-sub-item" data-mod="review-queue" id="reviewBadgeHost">[svg]审核中心<span id="reviewBadge" class="sb-badge">0</span></button>

<div class="sb-divider"></div>

<!-- 经营业务（常驻，沿用现有 7 项） -->
<div class="sb-label">经营业务</div>
<button class="sb-item sb-sub-item" data-mod="dashboard">[svg]经营看板</button>
... 档案管理 / 采销管理 / 仓配管理 / 资金管理 / 财务管理 / 数据报表 ...

<div class="sb-divider"></div>

<!-- 折叠分组：由 module-registry 渲染，默认 sb-group-collapsed -->
<div class="sb-label" data-mod-registry="header" style="cursor:pointer;display:flex;align-items:center;gap:4px">
  <span class="grp-arrow">▼</span>仓库作业
</div>
<div class="sb-group sb-group-collapsed" data-mod-registry="group">
  <button class="sb-item" data-mod="procurement-console">[svg]AI采购控制台</button>
  <button class="sb-item" data-mod="wastage-console">[svg]报损管理</button>
</div>
<!-- 财务中心 / 系统工具 / 扩展功能 同上结构 -->

<div class="sb-divider"></div>

<div class="sb-label">管理</div>
<button class="sb-item sb-sub-item" data-mod="settings">[svg]设置</button>
```

---

*本规范完全基于现有 `styles.css:132-162 / 732`、`variables.css:12-30/70-118/147-283`、`sidebar-v2.js`、`module-registry.js:95-164` 的真实类名与令牌撰写，去重后的条目映射来自《侧边栏重叠分析》。*
