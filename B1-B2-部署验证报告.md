# B1 + B2 安全快赢 — 部署与实弹验证报告

**日期**：2026-07-23 ｜ **负责人**：Senior Developer (高级开发工程师) ｜ **状态**：✅ 生产已上线 + 实弹验证全绿

> 承接审查报告「不花钱就能赢」三件套（F2 浅色主题 ✅ / F3 角色导航裁剪 ✅）的最后两块：B1 业务员数据隔离、B2 文件上传安全。

---

## 一、结论

B1 与 B2 均已部署到生产（47.113.224.140:8700）并**端到端实弹验证通过**：
- **B1（业务员数据级隔离）**：业务员登录后只能看到/操作自己经手的订单，他人订单（含真实 190 单）零泄漏。
- **B2（文件上传安全）**：伪装文件、坏扩展名、超大文件全部被拒，合法文件正常放行。
- 顺带：F3 后端权限修正（core.py 的 guide/driver）也因本次部署修正**一并真正上线**（此前因部署拓扑坑从未生效）。

---

## 二、B1 业务员数据级隔离

**改了什么**（`routers/sales.py`）
- `list_sale`（GET /sale-orders）：对 `role=sales` 的用户，**强制** `operator_id = display_name`（与库里 517 历史单 + RMA 的存储约定一致，不用数字 id——否则业务员会看不见自己的历史单）。
- 创建类接口（`create_sale` / `create_quotation` / `convert_quotation` / `create_return`）：用 `_operator_for_role(u, client_operator_id)`，服务端身份始终赢，**业务员无法伪造把订单归属到别人名下**（仅 supervisor 可用客户端值）。

**实弹验证**（生产真实打 API）
- 以业务员 user 4（李业务, sales）身份，在真实运营库 `tenant_1.db` 插两张测试单：
  - A：`operator_id='李业务'`（本人）→ **返回可见** ✅
  - B：`operator_id='仲嫚嫚'`（他人）→ **被隐藏** ✅
  - 库中仲嫚嫚真实 190 单等 → **零泄漏**（返回 total=1，仅 A）
- 对照：若 B1 未生效，会返回全部 ~517 单（含泄漏）。现在只返回自己的 1 单 → 隔离确凿生效。

---

## 三、B2 文件上传安全

**改了什么**（`routers/import_router.py`）
- 新增 `_validate_import_file(filename, content_type, content)`：
  - 扩展名白名单（.xlsx / .xls / .csv）
  - 大小上限 10MB
  - 文件头魔术字节校验（xlsx=`PK\x03\x04` / xls=`D0CF11E0` / csv 禁 NUL 字节）
  - 可疑 content_type 仅告警不阻断
- 接入 `preview_import`（POST /api/import/preview）与 `execute_import`（POST /api/import/execute）。原实现只查扩展名、不查内容，存在 XXE / zip 炸弹风险。

**实弹验证**（生产真实打 API）
| 用例 | 结果 |
|---|---|
| 伪装 xlsx（扩展名 xlsx，内容非 Excel） | 400「文件头不匹配 Excel 格式，可能被伪装」✅ |
| 伪装 csv（含 NUL 字节的二进制） | 400「文件内容异常，疑似非 CSV 文本文件」✅ |
| 超大文件 11MB | 400「文件过大（上限 10MB，当前约 11MB）」✅ |
| 合法真实 xlsx | 200 返回预览数据 ✅ |

---

## 四、部署过程中的关键坑（已解决，已写入长期记忆）

1. **`server/static` 符号链接冲突**：仓库里 `server/static` 是指向 `../static` 的符号链接。用 `git archive --strip-components=1` 部署时，它会生成根级 `static` 死循环符号链接，与真实 `static/` 目录撞车（直接解报 `tar: static: Cannot open: File exists`，或 `cp` 拒绝用符号链接覆盖真实目录）。
   - ✅ 解决：extract 后删掉坏 `static` 符号链接 + 顶层溅射文件，`cp` 代码到根；**静态单独用 `git archive HEAD static/` 干净灌入**。
2. **死代码子目录 `/opt/hergent-erp/server/`**：早期无 `--strip-components=1` 的部署把文件解到了这个子目录，运行进程从不加载。已删除。
3. **陈旧 `.pyc`** 掩盖代码更新 → 部署后统一 `find -name '*.pyc' -delete`。
4. **🚨 多租户 DB 拓扑（重大发现）**：业务查询按租户切到独立 `tenant_{id}.db`（如 `tenant_1.db` 才是真实运营库，含仲嫚嫚 190 单），主库 `erp.db` 仅存认证数据（users/api_tokens/user_tenants）。冒烟插/查业务数据**必须连对库**，否则看到 0 行误判功能失效。

**最终正确部署流程**（已验证全绿，已写入 MEMORY.md）：
```
git archive HEAD | tar xzf - -C /tmp/deploy_extract --strip-components=1
rm -f /tmp/deploy_extract/static  # + 删顶层溅射的 app.js/css/js 等
cp -rf /tmp/deploy_extract/. /opt/hergent-erp/
git archive HEAD static/ | tar xf - -C /opt/hergent-erp/   # 单独灌静态
chown -R hergent:hergent /opt/hergent-erp
find /opt/hergent-erp -name '*.pyc' -delete
rm -rf /opt/hergent-erp/server
systemctl restart hergent-erp
```

---

## 五、后续建议（可选）
- B1/B2 已覆盖核心路径；如需更全面，可把 `_operator_for_role` 模式推广到其他写接口（退货/报价等已覆盖，其余读接口无需）。
- 生产库 `sale_order_items` 仍为 0 行（517 订单无明细），影响定价/FEFO/预测回测与盈亏快报——属数据缺口，需从客户端导入订单明细。
- 多租户中间件当前为 fail-open（异常时静默放行），如需更强隔离可做 fail-closed 评估。
