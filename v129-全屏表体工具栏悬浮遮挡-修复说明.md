# v129 · 全屏表体下「导出 / 复制报单 / 品牌」悬浮遮挡 —— 定位与修复

**结论**：根因是层叠上下文里的 z-index 打架，不是 DOM 挂载位置、不是 transform/overflow。**已修复并部署**（commit `06c3a01`，单文件 `Forecast.vue`，3 处改动）。

---

## 一、代码定位

| 环节 | 位置 | 内容 |
|---|---|---|
| 全屏容器 | `Forecast.vue:399 / 532` | `<div class="grid-area" :class="{ 'is-fs': gridFullscreen }">` —— 汇总表与编辑表**共用同一个全屏容器** |
| 全屏样式 | `Forecast.vue:6148` | `.grid-area.is-fs{position:fixed;inset:0;z-index:1000;background:var(--bg);padding:12px;display:flex;flex-direction:column;gap:10px}` |
| 触发 | `Forecast.vue:4453` | `toggleGridFullscreen()`（按钮 `.grid-fs-btn`，Esc 亦可退出） |
| 工具栏 | `Forecast.vue:9-119` | `<div class="card toolbar">`，**在 `.grid-area` 之外、是其兄弟节点** |
| 出问题的三个按钮 | `Forecast.vue:38 / 53 / 84` | 「导出」「复制报单」「品牌」都被 `<div class="tb-pop">` 包着（因为各自带一个 Teleport 下拉面板） |
| 罪魁 | `Forecast.vue:6165` | `.tb-pop{position:relative;display:inline-flex;z-index:1120}` |

### 排查结论（逐条对应你的疑问）

1. **DOM 挂载位置**：没问题。全屏容器 `.grid-area` 与工具栏 `.card.toolbar` 是兄弟，工具栏本来就在表体外。按钮"脱离容器"只是**视觉上被抬到了全屏层之上**，DOM 没动过。
2. **层叠上下文**：`.card` 全局样式（`variables.css:217`）只有 `transition:box-shadow,border-color`，**没有 transform / filter / will-change / opacity**；`.page` 也无。**两者同属根层叠上下文，直接比 z-index** → 1120 赢。
3. **overflow**：`.toolbar` 上没有 `overflow:auto`（当年下拉面板被裁切，是靠 `Teleport to="body"` 绕开的，`.tb-pop-panel` 已是 `position:fixed`）。所以不是裁切问题。
4. **样式作用域**：`.is-fs` 的规则只作用于 `.grid-area.is-fs` 及其后代，**没有污染工具栏**。工具栏是用自己原有的 `.tb-pop{z-index:1120}` 反过来压过了全屏层。

### 真机实测（修复前）

在 `elementFromPoint(按钮中心)` 上做命中测试：

```
新建期次    COVERED   导入        COVERED
导出        VISIBLE   复制报单    VISIBLE
改单        COVERED   AI智能建议  COVERED
品牌        VISIBLE
```

**只有 `.tb-pop` 包裹的三个按钮 VISIBLE** —— 与 `.tb-pop{z-index:1120}` 完全吻合，实锤。三个按钮 rect 落在 `y=188`，而全屏层占据 `0..633`，即正好压在表头/数据行上。

---

## 二、修复方案（最小改动）

不是把全屏层抬到 1120 以上 —— 那会连带压住 `.imp-modal`(990)、`.al-modal`(990) 等弹窗，影响面大。反过来**只在该全屏时把工具栏弹层容器降回普通层级**，影响面最小。

| # | 文件 / 位置 | 改动 |
|---|---|---|
| 1 | `Forecast.vue:3` | `<div class="page" :class="{ 'grid-fs-on': gridFullscreen }">` |
| 2 | `Forecast.vue:6173`（样式区） | 新增 `.page.grid-fs-on .tb-pop{z-index:auto}` —— `auto` 落在层 0，低于全屏层 1000，于是按钮随工具栏一起被覆盖 |
| 3 | `Forecast.vue:4453` | `toggleGridFullscreen()` 末尾加 `_closePopPanes('')` —— 进出全屏先收起下拉 |

**第 3 点的必要性**：下拉面板 `.tb-pop-panel` 是 `position:fixed;z-index:1101` 且 `Teleport to="body"`，透明遮罩 `.pop-overlay` 是 `fixed;z-index:1100`，两者都高于全屏层 1000。若不收起，会留下一个浮在表体上的孤儿弹层。

**为什么不动 `.tb-pop` 的常态值**：1120 是为了"弹层开着时可直接点别的触发按钮做互斥切换"（高于遮罩 1100）。改成全局降值会破坏普通模式的交互；用 `.grid-fs-on` 限定作用域后，**退出全屏自动恢复**。

---

## 三、验证

| 项 | 结果 |
|---|---|
| 全屏下 7 个工具栏按钮命中测试 | **全部 COVERED**（修复前导出/复制报单/品牌为 VISIBLE） |
| 全屏层 | `z-index:1000 / position:fixed / 0,0,1280×633` 不变 |
| 根节点 | 全屏时 `class="page grid-fs-on"`，退出后回到 `class="page"` |
| 退出全屏后 `.tb-pop` | 计算值回到 **1120** |
| 普通模式互斥切换 | 「导出」开着 → 直接点「品牌」→ 命中工具栏(hit=true)，面板切到 `brand-pop` ✓ |
| dist 复核 | `Forecast-*.js` 含 `grid-fs-on`；`Forecast-*.css` 含 `.grid-fs-on .tb-pop` ✓ |

dist 已先 `cp -a` 备份到 `/root/hergent-backup/hergent-cn-v2-bak-*` 再 rsync。

---

## 四、影响面与备选

- **影响面**：仅本期预报页（`.page` 是本页根节点），汇总表与编辑表两处 `grid-area` 同时受益；不触碰后端、不动其它页面。
- **未采纳 A**：把 `.grid-area.is-fs` 抬到 1121 —— 会连带压住导入弹窗(990)、提醒弹窗(990)、`.col-menu` 等，回归风险大。
- **未采纳 B**：全屏时让整个工具栏浮在表上（给 `.toolbar` 加定位 + z）—— 改动更大，且"全屏"语义下工具栏本就该让位，页面内已有自己的缩放条与退出按钮。

## 五、顺带说明

真机调试时 `mptest` 登录返回 `require_password_change: true`，注入 token 会被踢回 `#/login`；改用 `POST /api/auth/demo-login`（demo 租户、只读）可正常进入 `#/forecast` 做 DOM 验证。
