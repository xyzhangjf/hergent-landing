# 报单映射配置 — 实现规划与优化建议

## 一、三个场景的本质
都归为两类问题：
1. **命名/格式差**：同一现实对象在三套体系里的名字不同
   - 系统主数据：门店全称（永辉东津店）
   - 报单汇总表：列头简称（东津）
   - 小程序报单：员工实际选择/录入的对象
2. **关系复杂度**：一个人要对多个对象、用多种单型报单（刘小顶既要报自己的调拨单，又要报美联保康的订单）。

**核心结论**：不要把简称/模板挂到员工或客户主数据上，抽一个独立的「报单映射」关系实体，所有乱象都在这一层消化。

---

## 二、数据模型

### 主数据层（保持干净，不带简称/模板）
- `employees`：已含 `is_active`；建议加 `warehouse_id`（个人仓）
- `contacts`（客户）：`store_status` 已存在
- `stores`（门店主数据）：canonical 全称 + 可选 `owner_customer_id`
- `warehouses`（仓库）：总仓、员工个人仓

### 新增核心实体：`report_mapping`（报单映射）
| 字段 | 说明 |
|---|---|
| tenant_id | 租户 |
| employee_id | 报单员（谁报） |
| counterparty_type | store / customer / self_warehouse |
| counterparty_id | 对应门店 / 客户 / 员工本人仓 |
| system_name | 系统全称（冗余存，便于核对，如 永辉东津店） |
| report_alias | 报单表简称/列头（东津） |
| order_template | 调拨单 / 自提订单 / 访销单…（引用现有 order config） |
| src_wh / dst_wh | 调拨用：总仓 → 员工仓 |
| (无列位置字段) | 落列一律按 report_alias 匹配列头，不用物理列位置 |

刘小顶因此天然有两条映射：
- (刘小顶, self_warehouse=刘小顶仓, 调拨单, 总仓→刘小顶仓)
- (刘小顶, customer=美联保康, 自提订单/访销单)

---

## 三、三场景落地

**场景1（全称↔简称）**：`report_mapping` 一行里 `system_name='永辉东津店'`、`report_alias='东津'`。小程序按 system_name 选择，落库/汇总时按 report_alias 写列。**主数据不改名**，系统保持规范。

**场景2（刘小顶=内部员工+调拨）**：`counterparty_type=self_warehouse` + `order_template=调拨单` + `src_wh=总仓, dst_wh=刘小顶仓`。他本人不是"客户"，是员工+有个人仓。

**场景3（黄家伟=分销商+自提+开方）**：他是 `contacts`（store_status 正常），显式开小程序账号（`users.is_active=1, role=distributor`），`report_mapping.order_template=自提订单`。开权限做成一个显式开关，避免误开。

---

## 四、用户配置流程（优化后，把原 3-5 步合并）

1. 自注册 → 建公司（tenant）
2. 建员工 / 建客户（主数据）
3. 建门店主数据（全称）
4. **建报单映射**（一次配齐：选 员工 × 对象 × 单型 × 简称 × 仓库/列）—— 简称与模板唯一入口
5. （必做）Excel 批量导入映射（几百家店靠手点不现实，复用现有 Excel 摄入通道）

**小程序端 UX**：员工登录后只看到"我被分配的对象"下拉，模板/简称系统已定，员工不碰列名 → 简称 mismatch 永远到不了用户手里。

---

## 五、优化建议

1. **简称治理**：`report_alias` 在租户内对 (template) 建唯一约束；加「映射体检」页：未映射门店 / 别名冲突 / 无分配员工。
2. **别名冲突**：东津可能重名 → 建议 `(alias + owner_customer)` 唯一；按 alias 落列时若冲突**报错而非静默覆盖**。
3. **模板配方化**：`order_template` 引用现有 `showOrderForm(config)` / `showTransferOrderForm`，不加新代码（契合配方化 MVP 架构）。
4. **Excel 批量导入映射**：经销商门店多，必须支持模板导入，复用现有 Excel 摄入。
5. **落列策略（按 alias 路由，不用列位置）**：汇总表是咱系统自有的表，列头=report_alias（如"东津"）。落数时按 alias 匹配列头，**不依赖物理列位置**。这样：
   - **隐藏列** = 纯展示层，不影响数据路由：数据仍落到该 alias 列（只是不显示）；不会因隐藏导致后面列"错位填错"。
   - **增/减报单门店** = 增/减一条映射 + 一列，其他门店配置完全不受影响（这正是弃用列位置的核心原因——位置会因插入/删除而整体平移）。
   - 唯一风险是 alias 重名（两家店都叫"东津"）→ 租户内 alias 唯一约束 + 映射体检拦截。
6. **权限**：员工只能报自己映射内的对象（防越权/错单）；分销商开方需显式开关。
7. **审计溯源**：每次报单落列记录 `mapping_id`，出问题时可溯源"这条东津单走了哪条映射"。

---

## 六、已确认决策（可进入实现）

① **门店归属**：门店可挂客户下，也可独立（owner_customer_id 可选）；mapping 一律精确到"店"这一层。✅
② **汇总表方向 + 落列**：报单汇总表是咱系统自有表，列头 = report_alias。落数**按 alias 匹配列头，绝不按物理列位置**——因此：
   - 隐藏某列 = 纯展示层，不影响数据路由（数据仍落到该 alias 列，只是不显示）；
   - 增减报单门店 = 增/减一条映射 + 一列，其他门店配置不受影响（这正是弃用列位置的原因）；
   - "停止报某店"请用"停用映射/停用门店"，不要只靠隐藏，避免路由语义不清。✅
③ **单型可自定义**：`order_template` 放在每条 mapping 上（关系层），可自定义、可改，复用现有订单配置，不加新代码。✅

### UI 决策（已确认）
④ **员工报单粒度**：小程序端一单只报一个对象（单选下拉，不支持一次提交多对象）。✅
⑤ **别名冲突处理**：保存映射时，租户内 `report_alias` 重名**直接拦截**（不让存）；"映射体检"页列出冲突项引导修改。✅
⑥ **Excel 批量导入**：提供导入模板（字段见下方），导入后逐行校验并反馈成功/失败。✅

### Excel 批量导入模板字段
| 列 | 说明 | 必填 | 备注 |
|---|---|---|---|
| 员工 | 报单员姓名或工号 | 是 | 需与员工主数据匹配 |
| 对象类型 | store / customer / self_warehouse | 是 | 中文"门店/客户/本人仓"也可 |
| 对象全称 | 系统主数据全称 | 是 | 如 永辉东津店 / 美联保康 / 刘小顶仓 |
| 简称(列头) | report_alias | 是 | 租户内唯一，即汇总表列头 |
| 单型 | 调拨单 / 自提订单 / 访销单… | 是 | 引用现有 order config |
| 源仓 | 调拨单时填 | 否 | 总仓等 |
| 目标仓 | 调拨单时填 | 否 | 员工个人仓等 |

---

## 七、实现落地（2026-08-26）

已全量上线生产（hergent.cn / erp.hergent.cn）。

- **P0 数据层 + 后端**：`report_mapping` 表（tenant 库自动回填；门店/客户=contacts，个人仓=warehouses）；接口 `GET/POST /api/report-mappings`、`PUT /api/report-mappings/{id}`、`POST /api/report-mappings/{id}/toggle`、`GET /api/report-mappings/health`、`GET /api/report-mappings/refs`（下拉数据）、`GET /api/report-mappings/mine`（小程序按登录用户取映射）、`POST /api/report-mappings/import`（Excel 逐行校验）。别名唯一靠应用层校验（保存时租户内重名拦截）。
- **P1 管理端**：`hergent-cn-v2/src/pages/ReportMapping.vue` —— 映射列表 + 新建/编辑抽屉（员工×对象类型×全称×简称×单型×调拨仓库）+ 映射体检红标（未配置/冲突/未分配）+ Excel 导入弹窗 + 停用确认。侧边栏「档案管理」分组新增「报单映射」入口。
- **P2 小程序**：仓库 `forecast-order-miniprogram` 不在本工作区，无法改其 UI；后端已备 `/api/report-mappings/mine` 供小程序下拉调用（单型只读）。小程序侧接驳需在其仓库实现。
- **P3 落列引擎**：`resolve_alias_to_contact(alias)` 把简称解析回系统全称/对应客户；`import_router._execute_forecast_cross` 落列时 alias-first（列头"东津"→"永辉东津店"+contacts.id）。小程序报单提交链路（`forecast_submission_create`）的 `mapping_id` 接驳需在其仓库 + 真机验证。

**仍未闭环**：①线上真机 e2e（需登录态）；②客户档案「修改+停用」P1 待做；③小程序仓库回归工作区后补 P2 UI 与 P3 提交链路接驳。
