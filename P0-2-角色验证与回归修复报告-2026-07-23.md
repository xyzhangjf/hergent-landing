# P0-2 角色 RBAC 验证与回归修复报告（2026-07-23）

> 配套完整清单：**P0-2-角色RBAC受阻清单-2026-07-23.md**（每个角色逐路由受阻明细，由生产部署代码实算）

## 一、做了什么

按你的指示执行 P0-2 角色验证：脚本直接 `import` 生产根 `/opt/hergent-erp/server.py`，复用**同一份** `_PATH_MODULE_MAP`、从生产库加载的 `ROLE_PERMS`、同一个 `core._check_perm`，逐路由复刻中间件判定，实算出每个角色会被 403 的端点清单。

**验证中暴露一个我必须立刻修的严重回归**：P0-2 上线后，有 **166 条业务路由在 `_PATH_MODULE_MAP` 中完全没有映射** → 中间件对未映射路径返回 `403 (MODULE_NOT_CONFIGURED)`，**对所有角色包括老板一律 403**。即老板自己被锁在 166 个真实功能之外（如 `/api/prepay-refunds`）。这是我之前 P0-2 映射补全的遗漏（此前那版审计脚本少统计了，误导了我）。

## 二、回归修复（P0-2.1，commit `a602f23`）

- 把 166 条按语义映射到老板已持有的功能模块：财务类→`accounts`、销售类→`sales`、采购类→`buying`、库存/批次→`stock`、客户→`crm`、主数据/合并→`data`、驾驶舱/利润表→`reports`。
- 纯管理类（RBAC 配置 `/api/role-permissions`、强制登出 `/api/force-logout`、通知渠道、消息模板等）映射到 `hr` 模块——经核对 `hr` **当前只有老板持有**，等于"仅老板可访问"，且不改动 `core.py`（避免把未验证的 H1b 一起带上线）。
- 同步把原有映射到 `settings`（老板不持有）的 `/api/bpm`、`/api/backup`、`/api/alerts`、`/api/webhooks`、`/api/feature-flags` 改为 `hr`，让老板能进。

**修复后重跑审计：老板受阻路由 = 0，admin = 0。** 其余角色的受阻均是**设计内的安全收紧**（见下）。

## 三、各角色最终状态（生产实算）

| 角色 | 持有模块 | 受阻模块（设计内收紧） | 备注 |
|------|----------|------------------------|------|
| **boss** | 全部 15 模块 | **无** | 回归已修复 ✓ |
| **admin** | 全部(`*`) | 无 | ✓ |
| **accountant** | dashboard, accounts, reports, marketing | buying, sales, stock, crm, data, hr, chat, projects, goals, tasks | 财务角色，合理 |
| **sales** | dashboard, sales, buying, stock, crm, data | accounts, reports, hr, chat, projects, goals, tasks | 业务员，合理 |
| **guide** | dashboard, sales, buying, stock, crm | **data**（产品主数据/合并/导出丢失）, accounts, reports, hr… | ⚠️ 导购通常需产品主数据，需你确认是否给 `data` |
| **driver** | dashboard, stock | 其余几乎全部 | 司机，合理 |
| **user** | （无权限行） | **全部 946 条** | ⚠️ 该账号完全锁死，疑似遗留/测试账号 |
| **warehouse** | （代码里未用） | 全部 | 实际仓管角色名是 `库管`（持有 dashboard/stock/data），工作正常；`warehouse` 是死名字 |

## 四、实弹冒烟（生产）

| 测试 | 结果 | 说明 |
|------|------|------|
| 匿名 `/api/dashboard` | **401** | P0-1 网关仍在 ✓ |
| 老板 `/api/prepay-refunds` | 500（原 403） | RBAC 已放行，500 是端点自身 bug（见下） |
| 老板 `/api/bpm/definitions` | 500（原 403） | RBAC 已放行，500 是端点自身 bug |
| 老板 `/api/accounting-periods` | **200** | 完全正常 ✓ |
| 业务员 `/api/sale-orders` | **200** | 域内正常 ✓ |
| 业务员 `/api/salary-send/history` | **403** | hr 模块，越权封堵 ✓ |
| 业务员 `/api/bi/pivot` | **403** | reports 模块，收紧 ✓ |

## 五、残留端点 500（与 RBAC 无关，独立 bug）

`/api/prepay-refunds`、`/api/bpm/definitions`、`/api/salary-send/history` 在 RBAC 放行后返回 500——这是端点处理函数自身的缺陷（疑似 `erp_db` 缺对应查询函数，与之前 salary-send 暴露的 `salary_send_log_list` 缺失同类）。**不影响安全，但功能不可用**，建议单独修。

## 六、需要你拍板的事项

1. **guide 是否需要 `data` 模块**（产品主数据/合并/导出）——当前被收紧紧缺，导购大概率需要。
2. **accountant 是否需 `buying`/`stock`**（采购/库存查看）——目前财务角色看不到采购与库存。
3. **`user` 账号**（完全锁死）是遗留还是真实需要？若是真实员工，应分配角色。
4. 上述 500 端点是否要排入下一轮修复。

## 七、部署说明

- 提交 `a602f23`（仅 `server.py`，未动 `core.py` 的 H1b）。
- 本次部署发现 `git archive --strip-components=1` 方式偶发未覆盖生产根 `server.py`（疑似提取目录状态异常），改用**直接 scp 本地 `server.py` 覆盖** + 清 pyc + 重启，已验证 `MAP_SIZE=274`、新键生效。
- 全量备份在 `/tmp/hergent-backup-*.tar.gz`。
