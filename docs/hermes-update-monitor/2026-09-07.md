# Hermes Agent 官方更新监控报告 — 2026-09-07

> 只读分析，未修改/部署任何生产文件，未重启服务。基线：生产 = v0.19.0（2026-07-20），评估基准 = 已评估的官方 v2026.8.27（v0.20.6，报告 docs/hermes-v0.20.6-compat-report.md）与 v2026.8.31（v0.21.0，2026-09-06 已静态核查）。

---

## ① 官方最新状态

| 来源 | 最新版本 | 发布时间 | 说明 |
|---|---|---|---|
| GitHub Releases | **v2026.8.31 = Hermes Agent v0.21.0「Pantheon」** | 2026-08-31 | 与上次评估（09-06）时完全相同，**一周内无新 tag** |
| PyPI | 0.19.0 | 2026-07-20 | 持续滞后（自 0.19.0 后未再发版），不作为升级依据 |

**自 v2026.8.27 以来唯一新增** = v0.21.0（Pantheon），其 Release Notes 要点：
- 新能力：Bot Mode 桌面「机器人社会」群聊、`hermes peer` agent 间 P2P 私信、cron 任务记忆化与去重、子 agent 实时操控（delegate_task 250 迭代/10 并发）、MCP 指挥中心（健康检查/用量/`hermes://` 深链）、agent 驱动桌面内置浏览器、6 个新 provider（含 Tencent TokenPlan）。
- 安全修复：受保护指令文件（AGENTS.md/skills/memory）写入须审批；密钥泄露深度脱敏（终端错误/.env/检查点/ACP 日志）；Windows 破坏性命令纳入审批。
- 架构事实：**引入全新 gateway/ 运行时**（默认端口 8642、API_SERVER_KEY 鉴权、进程监管）——相对 v0.20.6 架构变化更大，而非更小。
- 回退未发布：Model Council、DCP context 引擎、WS-only 网关服务器。

> ⚠️ 注意：上述能力 **90% 面向桌面客户端/多机器人形态**，与 hergent 的 headless HTTP API 服务形态无关。

---

## ② 与 hergent 生产 v0.19.0 的差距

- hergent 生产 = **v0.19.0（2026-07-20）**；官方最新 = v0.21.0（2026-08-31）。中间隔 v0.20.x 系列全部补丁 + v0.21.0。
- 官方版本跨度描述均为「桌面功能、Bot Mode、provider 扩充、MCP 目录扩充、OS 集成、安全加固」——**无任何一项是 hergent 用到的 headless HTTP gateway 能力修复或服务端契约回归**。
- hergent 近期 AI 优化约 90% 走自有 AI loop（server/hermes_core.py 直连 DeepSeek），与 Hermes 版本解耦；本轮无新增因素改变该判断。

---

## ③ 5 个耦合点兼容性核查（基于本地克隆 /tmp/hermes-231-src = tag v2026.8.31，grep 静态复核，2026-09-07）

| # | 耦合点 | 复核结果 | 兼容性 |
|---|---|---|---|
| ① | 前端 SSE `POST /hermes/v1/chat/completions` | gateway/platforms/api_server.py + gateway/run.py 仍提供 chat/completions；v0.21.0 属全新 gateway 运行时（默认端口 8642、API_SERVER_KEY 鉴权） | 语义在、形态变：需改代理目标/鉴权头，18765 直连形态不复存在 |
| ② | `server/routers/ai_skills.py:26` 调 `GET /v1/skills` | v0.21.0 在 api_server.py:2235 恢复 `GET /v1/skills`（相对 v0.20.6 纯 `/api/skills` 系逆转），返回 {object,data[]} | 路径兼容（新运行时下），但仍受 gateway 鉴权/端口变化牵制 |
| ③ | `server/routers/cron_tasks.py:13` 硬编码 `sudo .../hermes_cron_bridge.py` | **全仓零命中，bridge 脚本仍未回归** | **断裂依旧：升级 = hergent 生产 cron 定时任务全挂** |
| ④ | 生产 `~/.hermes/config.yaml`（model/platforms.api_server/cors_origins/mcp_servers.spreadsheet stdio） | `cors_origins` 改为从 `extra.get("cors_origins", env API_SERVER_CORS_ORIGINS)` 读取（api_server.py:1521）；mcp_servers 顶层键仍被消费（tools.mcp_tool / include_default_mcp_servers） | 旧 config 顶层 cors_origins/api_server 结构需迁移到新 schema + 环境变量；spreadsheet MCP stdio 配置本身仍可保留 |
| ⑤ | 生产 `~/.hermes/SOUL.md` 副驾格式协议（```card/```cards 围栏） | gateway/run.py:1440 上下文文件层仍做 AGENTS.md / SOUL.md discovery + read | 引擎按原样读取注入，兼容 |

**净结论：v0.21.0 相对上次评估（09-06）零变化；5 个耦合点中 ③ 是硬断裂（cron 全挂），①④ 是形态/配置迁移成本，②⑤ 兼容。升级成本与破坏面 = 与 09-06 评估完全一致，无任何新增缓解因素。**

---

## ④ 结论与建议

### 结论：**A 无需升级（维持生产 v0.19.0）**

理由：
1. **官方一周内无新发布**：最新仍为 2026-08-31 的 v0.21.0，与 2026-09-06 已完整静态评估的版本完全相同，无新增修复/能力/安全公告需要重新评估。
2. **v0.21.0 无 hergent 需要的服务端能力**：新特性 90% 为桌面端 Bot Mode/多机器人/provider 扩充，对 headless HTTP 网关服务增益≈0。
3. **升级会破坏现有契约**：③ cron bridge 硬断裂（定时任务全挂）、①④ 需迁移新 gateway 运行时（端口 8642/API_SERVER_KEY/新 config schema），属破坏性升级，与既有评估一致。
4. **hergent 与 Hermes 版本已实质解耦**：自有 AI loop（hermes_core.py 直连 DeepSeek）承载绝大多数 AI 优化，不构成升级的紧迫性。

**升级触发条件（维持上次定义，未变）**：官方发布 headless/server 形态的正式发行说明、修复 cron bridge / config 迁移 / 18765 兼容，或出现波及 v0.19.0 暴露面的严重安全公告。

---

## ⑤ 最小改动清单（仅作建议，不执行）

若未来判定升级（B/C），最小改动路径：
1. **cron**：在 server/routers/cron_tasks.py 移除对 `hermes_cron_bridge.py` 的硬编码调用，改为调用 v0.21.0 gateway cron HTTP API（或先自建等价 bridge 脚本并 pin 在仓库内）。
2. **gateway 接入**：nginx /hermes/ 反代目标从 18765 改指 gateway 新端口（默认 8642），并注入 API_SERVER_KEY 鉴权；或保留旧 18765 形态则需验证 v0.21.0 是否仍提供兼容监听。
3. **config 迁移**：生产 ~/.hermes/config.yaml 按新 schema 迁移 cors_origins → extra/API_SERVER_CORS_ORIGINS 环境变量；mcp_servers.spreadsheet stdio 保留验证。
4. **回归验证**：前端 SSE 副驾对话、/v1/skills 列表、cron 定时任务、MCP spreadsheet 四路全量 E2E。

**风险提示**：v0.21.0 引入全新 gateway 运行时与进程监管，属跨架构升级；staging 验证周期建议 ≥ 2 周，覆盖 4 路回归 + 7×24 稳定性观察后再议生产。

---

*生成方式：只读自动化监控（GitHub Releases API + PyPI JSON + 本地克隆静态 grep 复核），未触碰生产环境。环境备注：本机代理 127.0.0.1:63932 失效，GitHub 直连可用。*
