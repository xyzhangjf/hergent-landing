# Hermes Agent 官方更新监控报告（2026-09-06）

> 生成时间：2026-09-06 07:00（GMT+8）｜监控基线：生产 Hermes v0.19.0（2026-07-20，/usr/local/lib/hermes-agent/，systemd hermes-gateway.service，端口 18765）
> 方法：GitHub Releases API + PyPI JSON + 浅克隆 v2026.8.31 源码静态核查 5 个耦合点（全程只读，未改动生产任何文件）
> 上次评估（2026-08-31）对象为 v0.20.6（v2026.8.27），结论「维持 v0.19.0」；本次为官方发布新 tag 后的复核。

---

## ① 官方最新状态

| 数据源 | 最新版本 | 发布时间 | 备注 |
|---|---|---|---|
| **GitHub Releases** | **v2026.8.31 = Hermes Agent v0.21.0**「The Pantheon Release」 | 2026-08-31T19:29:49Z | ⚠️ **晚于已评估的 v2026.8.27（v0.20.6）**，属功能大版本（v0.20.1~v0.20.6 均为快速补丁线，完整说明随 v0.21.0 交付） |
| PyPI hermes-agent | 0.19.0（wheel/sdist 2026-07-20 上传） | 2026-07-20 | 严重滞后于 GitHub tag，**不作为版本依据**（与生产版一致） |

近 5 条 Releases（按时间倒序）：
1. **v2026.8.31 / v0.21.0**（Pantheon）：Bot Mode 桌面多机器人、`hermes peer` 跨代理私信、**Cron 任务记忆化**（continuity=true 跨次运行带记忆、监控模式无变化跳过 LLM）、实时操控子代理（delegate_task 中途转向/停止）、**MCP 指挥中心**（拖入导入/后台健康检查/hermes:// 深链）、CLI 增强波（Ctrl+P、/model 筛选、/status、全局紧急停止）、代理驱动桌面浏览器、6 个新模型提供商；**安全补丁**：agent 指令文件（AGENTS.md/skills/memory）写入审批防提示注入、密钥擦除缺口封堵、插件安装 Tier-1 扫描、Blender MCP 目录条目因上游泄露移除；回退项：Model Council、DCP、WS-only 网关服务器（#96118 回退，FastAPI 仍在桌面启动路径）。
2. **v2026.8.27 / v0.20.6**：真实配置文件浏览、桌面浏览器独立 OS 窗口、远程 MCP 目录 50+ 厂商服务器、web_search/web_extract TTL 缓存、tool_search 词干提取、OS 密钥链加密、cron 持久事件确认；新模型 GLM-5.3-Flash 等。
3. **v2026.8.19 / v0.20.5**：Bot Mode 群组线程、无密钥 Web 搜索（5 厂商轮换）、CLI 打磨、cron 作业持久记忆与 per-job reasoning effort。
4. **v2026.8.18 / v0.20.4**：桌面玻璃表面、SESSIONS|BOTS 标签侧边栏、NVIDIA SkillEvaluator 咨询扫描、cron 媒体发送加固、SessionDB 修复。
5. **v2026.8.16.2 / v0.20.3**：MCP 2.x SDK 迁移、捆绑 Bot Mode 插件、Python 运行时所有权加固、cron 调度器自愈（EMFILE 恢复）。

> 判断：v0.20.6 只是 v0.21.0 大版本发布前的补丁快照；**v0.21.0 才是真正的"下一个形态"**，且引入了全新的 `gateway/` 运行时（`python -m gateway.run` / `cli.py --gateway`），与 v0.19.0 已不是同一套进程模型。

---

## ② 与 hergent 生产 v0.19.0 的差距

- 版本跳变：v0.19.0 → v0.21.0，跨 v0.20 全系列（≥2 个大版本、约 8 个 tag、数千 commits/PR）。
- 架构形态：v0.19.0 为单进程 `hermes serve`（nginx→18765）；v0.20.x 起出现 `hermes_cli/proxy/` 代理层；**v0.21.0 新增 `gateway/` 完整运行时**，api_server 变成 OpenAI 兼容端点提供者（`gateway/platforms/api_server.py`，aiohttp，默认端口 **8642**，认证 `API_SERVER_KEY`，端点含 /v1/chat/completions、/v1/responses、/v1/skills、/api/jobs、/api/cron/fire），并有 dashboard_procs、control_socket、systemd_notify 等进程监管模块——**部署形态/端口/认证体系均可能变化**。
- 新能力 90% 面向桌面/多机器人/消费者向（Bot Mode、peer、MCP hub UI、桌面浏览器），对 hergent 这类「HTTP 网关消费端」直接增益有限；唯一对 hergent 有潜在价值的是 **cron 记忆化**（每日简报类任务可受益），但前提是 cron 桥接重写完成。

---

## ③ 5 个耦合点兼容性核查（v2026.8.31 源码静态）

| # | 耦合点 | v0.21.0（v2026.8.31）源码证据 | 判定 |
|---|---|---|---|
| ① | 前端 SSE `POST /hermes/v1/chat/completions` | 两条候选路径：a) `hermes_cli/proxy/server.py:241` 仍 `add_route("*","/v1/{tail:.*}")` 捕获转发；b) `gateway/platforms/api_server.py:2247` 原生 `("POST","/v1/chat/completions", _handle_chat_completions)`（OpenAI 格式 SSE，含 /v1/responses）。**但** v0.21.0 api_server 默认端口 8642、认证为 `API_SERVER_KEY`/`extra.key`，与生产 18765 + HERMES_API_KEY 形态不对应 | ⚠️ **语义兼容 / 形态待核**（取决于走哪套运行时与端口/认证配置） |
| ② | `ai_skills.py:26` GET `/v1/skills` | **v0.21.0 恢复了 /v1/skills**（相对 v0.20.6 纯 /api/skills 的逆转）：`gateway/platforms/api_server.py:2235` 路由 + `:4065 _handle_skills` 返回 `{"object":"list","data":[{name,description,category,...}]}`——结构与 hergent `data.get("data")` 期望匹配。但同样挂在 gateway api_server（8642/API_SERVER_KEY）形态下；dashboard 形态（hermes_cli/web_routers/skills.py:430）仍是 `/api/skills` | ⚠️ **新 gateway 形态下兼容**（比 v0.20.6 进步），但依赖部署形态与认证键 |
| ③ | `cron_tasks.py:13` `sudo .../hermes_cron_bridge.py` | 全仓 `find/grep hermes_cron_bridge` **零结果——脚本未回归**。cron 全部走 HTTP API：`hermes_cli/web_routers/cron.py` `/api/cron/jobs` CRUD + `/api/cron/fire`；gateway api_server 另有 `/api/jobs` CRUD + `/api/cron/fire`。**hergent 子进程桥接若升上来=定时任务全挂**，需整段重写为 HTTP 调用 | ❌ **断裂（同 v0.20.6）** |
| ④ | 生产 `~/.hermes/config.yaml`（model / platforms.api_server / cors_origins / mcp_servers.spreadsheet stdio） | a) `mcp_servers` 顶层键**仍被读取**（config.py 白名单 2228/2603-2631/5478-5483，dict 规范化保留）→ spreadsheet MCP stdio 大概率兼容；b) `cors_origins` 键仍在（`gateway/platforms/api_server.py:1522` `extra.get("cors_origins", env API_SERVER_CORS_ORIGINS)`），但**移入 platforms.api_server.extra 或改走环境变量**；c) api_server 端口/密钥改为 `extra.port` / `extra.key` 或 `API_SERVER_PORT`/`API_SERVER_KEY`（service_manager.py:655；api_server.py:266 DEFAULT_PORT=8642）；d) config_migrations.py 仍在，`SUPPORT_FLOOR_VERSION=12` | ⚠️ **需迁移验证**（cors_origins/api_server 结构漂移；mcp_servers 兼容；自动迁移系统存在但覆盖面需实测） |
| ⑤ | 生产 `~/.hermes/SOUL.md` 副驾格式协议 | `gateway/run.py` 多处把 SOUL.md 纳入 context-file tier（AGENTS.md/SOUL.md discovery+read，:1440/:5955/:21953）；`hermes_cli/config.py:917` 仍在 HERMES_HOME seed/读取 SOUL.md。路径 `~/.hermes/` 未变 | ✅ **兼容**（引擎仍按原样读取注入） |

> 核查范围：`/tmp/hermes-231-src`（浅克隆 v2026.8.31，265MB / 4881 个 .py），grep 覆盖 gateway/、hermes_cli/、cron/、tools/。本机代理 127.0.0.1:63932 已失效，GitHub 直连可用。

---

## ④ 结论与建议

### 结论：**A —— 无需升级，维持生产 v0.19.0**

理由（逐条）：
1. **官方虽发布新 tag（v2026.8.31），但无 hergent 需要的修复/能力**：v0.21.0 核心卖点（Bot Mode 桌面、peer 私信、MCP hub UI、桌面浏览器、模型目录扩增）全部面向桌面/消费者向形态；hergent 90% AI 已走自有 `hermes_core.py` 直连 DeepSeek，与 Hermes 进程解耦，这些能力对当前业务直接增益≈0。唯一潜在受益点 cron 记忆化被第 3 条抵消。
2. **升级会破坏契约**：耦合点③ cron bridge 依旧不存在（v0.20.6 已断、v0.21.0 未回归），升级即定时任务（每日简报 8:05 等）全挂；耦合点④ config schema 继续漂移（api_server 端口/密钥/cors_origins 挪位），需手改 + 迁移验证。
3. **架构变化比 v0.20.6 评估时更大而非更小**：v0.21.0 新增整套 `gateway/` 运行时（默认端口 8642、API_SERVER_KEY 认证、进程监管模块），nginx→18765 反代与 systemd 单元均需重核对；"升级"是运行时迁移工程，不是版本替换。参考 v0.20.6 评估：生产机无外网 pip install + Mac 构建 venv 不可移植的部署硬阻塞依旧存在。
4. 唯一正面信号（v0.21.0 gateway api_server 恢复了 `/v1/skills`、原生 `/v1/chat/completions`，OpenAI 兼容面更完整）仅在新 gateway 部署形态下成立，不足以抵消 cron/config/进程模型三重破坏。

与 2026-08-31 评估一致：**收益/风险严重失衡，维持 v0.19.0。**

---

## ⑤ 观测建议（非升级清单；若未来真要动，最小动作与风险提示）

本次结论为 A，不列完整升级清单，仅记录后续监控/决策要点：

- [ ] **继续按双周节奏跑本监控**：Hermes 桌面向发版节奏快（~2-3 天/补丁），但 v0.21.0 后未见面向 HTTP 网关端的稳定化大版本；若官方发布"server/gateway 稳定化 + cron HTTP API 文档化"类版本，值得重评。
- [ ] **若未来评估 cron 记忆化收益**（每日简报带上下文）：先独立评估把 hergent cron 桥接从 subprocess 改为 HTTP（/api/cron/jobs 或 /api/jobs）的成本，再谈版本升级；此改造本身与版本解耦、可先行。
- [ ] **升级触发条件建议定为**：官方出现 (a) 面向 headless/server 部署的正式发行说明，或 (b) 修复 hergent 已踩坑（cron bridge 回归、config 迁移覆盖 cors_origins/api_server、18765 兼容），或 (c) 严重安全公告影响当前 v0.19.0 暴露面——三者任一出现再评估，届时结论预计为 C（先 staging 验证）。
- [ ] 生产安全关注点（与版本无关）：当前暴露面 18765 + HERMES_API_KEY 认证，注意密钥轮换与 nginx 层访问控制即可。

---
*报告由自动化监控任务生成（只读，未修改/部署生产任何文件、未重启服务）。源码克隆留在 /tmp/hermes-231-src 供复核（/tmp 可能被清，需要时重新浅克隆）。*
