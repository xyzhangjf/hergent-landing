# v122 · Step 0 生产数据摸底报告

> 时间：2026-09-08 22:15 · 方式：**只读**（`sqlite3.connect('file:...?mode=ro')`），未写入、未修改任何数据
> 范围：`/opt/hergent-erp/tenant_*.db`（已排除 `incident_20260812/`、`backups/` 下的同名副本）
> 结论：**主风险全部为 0，可以动工**；但发现 1 个高优先级口径冲突（R5），需你拍板

---

## 一、风险指标总表

| 编号 | 指标 | 结果 | 等级 |
|---|---|---|---|
| **R1** | 商品规则残留 `auto_period_enabled=1` | **0** | ✅ 无风险 |
| **R2** | 商品规则残留报单排程 / 月度分解配置 | **0** | ✅ 无风险 |
| **R3** | 老口径品牌规则（无月度分解 且 非 year） | **5 条** | ⚠️ 需处理 |
| **R4** | 商品规则 `scope_key` 非数字（无法回填选择器） | **0** | ✅ 无风险 |
| **R5** | 🆕 存量品牌规则按「月」建、与「12 个月分解唯一模式」冲突 | **7 条中 5 条是月口径** | 🔴 **高，需拍板** |
| — | `target_year` / `target_unit` 列是否存在 | **全部缺失** | 需加列 |
| — | 商品规则总数 | **0** | 默认度量改 amount **零影响** |
| — | 已配「首次报单日」/「智能建表」的规则 | **0 / 0** | v121 节奏功能尚未实际启用 |

**→ 原计划里最担心的「商品页删字段导致智能建表静默停摆」风险不存在**：生产上根本没有商品规则。

---

## 二、库级分布

| 租户库 | 规则数 | 维度 | period_type | 备注 |
|---|---|---|---|---|
| `tenant_1.db` | 4 | brand ×4 | month ×4 | **真实业务数据**（永诺旗舰店） |
| `tenant_10.db` | 3 | brand ×3 | month ×1, year ×2 | 演示/测试数据 |
| `tenant_0/2/3/4/5/7/8/11.db`、`tenant_tenant_1.db` | 0 | — | — | 空库，但**列已齐全（42 列）**，迁移都跑过了 |

**迁移完整性**：11 个库全部 42 列，`monthly_amounts` / `order_*` / `auto_*` / `scale_type` / `contract_id` 等关键列**无缺失**。
→ 新增 `target_year`、`target_unit` 时同样要在**两处**加（tenant patch 元组 + `_safe_migrate`），且必须幂等，保证这 11 个库都覆盖到。

---

## 三、R3 明细：这 5 条到底是什么

| 库 | id | 规则名称 | 品牌 | period | 目标值 | 返利率 | 生效期 |
|---|---|---|---|---|---|---|---|
| tenant_1 | #6 | 蒙牛低温8月目标 | 蒙牛低温 | month | 100 万 | 10% | 2026-08-01 ~ 08-31 |
| tenant_1 | #7 | 简爱8月目标 | 简爱 | month | 8 万 | 12% | 2026-08-01 ~ 08-31 |
| tenant_1 | #8 | 蒙牛低温9月目标 | 蒙牛低温 | month | 100 万 | 10% | 2026-09-01 ~ 09-30 |
| tenant_1 | #9 | 简爱9月目标 | 简爱 | month | 8 万 | 12% | 2026-09-01 ~ 09-30 |
| tenant_10 | #1 | 蒙牛8月总目标 | — | month | 65（元，测试脏值） | 100% | 2026-08-01 ~ 08-31 |

**关键解读**：tenant_1 的 4 条**不是"年目标被拆错了"，而是本来就是按月建的规则** —— 每个月一条、金额都是当月的。

---

## 四、🔴 R5：由此暴露的口径冲突（必须拍板）

v121c 把品牌目标页锁成了「**按 12 个月分解**唯一模式」，且 `save()` 强制：

```js
f.period_type = 'year'
f.target_value = 各月之和
```

那么现在去编辑「**蒙牛低温9月目标**」（100 万 / 月口径）会发生什么：

| 步骤 | 结果 |
|---|---|
| 打开编辑 | 弹出 12 个月空表 + 黄条 `legacyNotice`（"原来是月目标 100 万，未自动拆分"） |
| 用户没细看，填了 9 月 = 100 万 | 其他月留空 |
| 保存 | `period_type` 被改成 **year**，`target_value` = **100 万** |
| 后果 | 「9 月目标 100 万」变成「**年度**目标 100 万拆到 9 月」→ 返利基数从 100 万/月 变成 100 万/年，**返利算少 12 倍**，且冲刺看板口径全变 |

`legacyNotice` 只是提示，**不阻断保存**。这是当前线上就存在的隐患，与本次页面拆分无关，但拆分时必须一并解决，否则两个页面拆完、隐患照旧。

---

## 五、处理方式（三选一，需你拍板）

| 选项 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **A（推荐）** | 品牌页支持**两种模式**：「年度（12 个月分解）」/「单期（月/季，单值）」。按 `period_type` 回填；新建**默认年度** | 贴合真实用法（你现在的规则就是按月建的）；存量规则编辑零风险 | 相当于把「周期口径」请回来了 —— 但**只保留 年/月 两个值**，措辞改成业务语言（"年度目标" / "单月目标"），不再是 4 个技术选项 |
| **B** | 品牌页只保留 12 个月分解；存量月口径规则编辑时**强制二次确认**（"这会把 9 月 100 万改成年度 100 万，确认吗"），并预填「该月金额放进对应月份、其余留空」 | 维持你要的"只有一种模式" | 每次编辑存量规则都要点一次确认；且转换后口径确实变了，用户容易点错 |
| **C** | 品牌页只保留 12 个月分解；**存量月口径规则设为只读**（只能停用、不能编辑，要改就新建） | 零误改风险 | 存量 5 条规则变成"死数据"，用户想改 9 月目标只能新建一条，体验差 |

**我的判断**：选 **A**。数据摆在这儿 —— 真实租户（tenant_1）建的 4 条品牌目标**全是月口径**，说明"按月定目标"是实际用法；"按年谈返利"是另一种场景。把两者都支持、新建默认年度，既不违背你"去掉让人困惑的周期口径"的初衷（4 个技术选项 → 2 个业务选项），又不会把存量数据改坏。

---

## 六、对原规划的影响（确认项已定）

你已拍板的 3 项，摸底结论如下：

| 你的决定 | 摸底验证 | 影响 |
|---|---|---|
| 1. 加 `target_year` | 11 个库全部缺列 | 需加列；存量回填建议取 `effective_start` 年份（tenant_1 的 4 条 → 2026） |
| 2. 加 `target_unit` | 11 个库全部缺列 | 需加列；存量全留空（不臆测单位） |
| 3. 商品目标默认按金额 | **存量商品规则 = 0 条** | **零影响**，改默认值不会动到任何数据 |
| 4. 先跑 Step 0 | ✅ 已跑完 | 主风险全 0，可动工 |

**新增待定**：第 5 项 —— R5 选 A / B / C。

---

## 七、下一步

R5 一定，即可按原规划的 Step 1（纯结构重构）开工。Step 0 的所有统计脚本保留在 `/tmp/step0_audit.py`、`/tmp/step0_b.py`，后续可作为回归基线重跑。
