# 报单业务流程分析 · Hergent.cn 实现方案
（2026-08-11，基于用户口述的低温奶经销商报单流程）

## 一、现状流程复盘（6 个环节）

| 环节 | 谁做 | 现状做法 | 主要痛点 |
|---|---|---|---|
| 1 预报订单 | 业务员/促销/导购/分销商 | 企业微信智能表格填写 | 多人填写、格式不一、靠文员导出 Excel 再粘贴汇总 |
| 2 审核 | 审核同事 | 综合「指标达成 + 库存」凭经验定单 | 依赖个人经验，返利/指标算不清，容易错失返利档 |
| 3 确认回传 | 文员 | 收到审核后执行下单动作 | 信息在多人之间靠文件传来传去 |
| 4 余额与付款 | 文员 | 查厂家余额 → 差额通知群 → 付款 → 确认到账 | 差额手工算、通知手工发、到账靠人盯 |
| 5 厂家下单 | 文员 | 把订单粘贴进厂家系统 | 厂家系统无 API，纯手工复制粘贴 |
| 6 舟谱入账 | 文员 | Hermes 生成导入模板 → 手工导入舟谱 → 打印 | 模板靠 AI 即兴生成，非确定性 |

当前节奏：每 2 天到货、提前 4 天下单，报单主要依赖员工经验。

## 二、可自动化 vs 必须手工的边界（先说清楚，免得幻想全自动）

hergent.cn 是 Web 版、以 Hermes agent 为引擎的 **AI 层**，不是 ERP、也不是厂家/舟谱的系统。

- **蒙牛厂家系统**：无开放 API → hergent.cn **不能直接查询余额、不能直接下单**。能做的是：跟踪余额数字、生成粘贴模板、提醒。
- **舟谱**：无开放 API → hergent.cn **不能直接导入**。能做的是：一键生成规范导入 CSV、提醒导入。
- 这两道「外部墙」决定了 ⑤⑥ 环节永远是「半自动准备 + 人工执行」。这不是产品缺陷，是客观限制（前面多轮已共识）。

所以 hergent.cn 的价值定位：**把两道墙「周围的一切」做顺、做准、做智能**——采集、算账、生成模板、提醒、推送，让人只做最后的「粘贴/确认」动作。

## 三、各环节功能实现方案

### ① 预报订单采集（半自动）
- **现状替代**：保留企微智能表格作为一线入口（一线人不必换习惯），hergent.cn 提供「导入智能表 / 对接企微」能力；或直接在 hergent.cn 内建轻量表单（业务员/促销/导购/分销商分角色填报）。
- **聚合**：多人预报自动汇总成一张「预报订单总表」，按SKU合并数量，标注来源人。
- **引擎抓手**：汇总结果直接喂给已有的 `forecast_engine`（智能需求预测 + 效期感知再订货点），为 ② 提供输入。
- **交互**：Hermes 可在企微群问「今天谁还没报 XX 店的预报？」并 @ 未报人。

### ② 智能审核 · 核心大脑（AI 自动化）—— 这是整个流程的价值高地
把审核同事「凭经验拍脑袋」的三类判断，全部变成可计算、可解释的输出：

- **算指标达成（待建引擎）**
  - 录入：厂家「总指标」+「各单品指标」（合同期一次性录入，是你的 IP）。
  - 输入：实际销量（从舟谱导出喂入，或 hergent 轻量记录）。
  - 输出：总达成率、各单品达成率、距指标还差多少。
- **算返利（待建引擎）—— 最值钱的一块**
  - 录入：返利条款（阶梯返利 / 搭赠 / 单品达成额外返利等，来自合同）。
  - 输出：当前所处返利档、**「再订 X 箱单品 Y 即可解锁下一档，预计多返 ¥R，扣除压货成本后净赚 ¥N」**。
  - 这块就是你的行业懂行，别人抄不走，应写成可复用 Skill。
- **算库存（已有引擎 `check_expiry` / FEFO）**
  - 效期感知：临期 SKU 不压货、缺货 SKU 必补；给出每个 SKU 的「建议订量下限/上限」。
- **合成建议最终订单**
  - 三因子加权 → 生成一份「建议最终订单」+ 一段大白话解释（为什么这么订：缺口多少、为返利补多少、受效期压多少）。
  - 取代「依赖员工经验」，且不代替人决策。

### ③ 人工确认（人工）
- 审核同事在 hergent.cn 打开「建议最终订单」，确认或微调数量后「定稿」。
- 设计原则：**AI 只建议，绝不自动向厂家下单**。定稿动作必须有人点。

### ④ 余额与付款跟踪（半自动）
- **余额**：文员在 hergent.cn 记录/更新「厂家账户余额」（手工填，因为厂家系统无 API）。
- **差额自动算**：定稿订单金额 − 余额 = 需付金额，系统自动算出。
- **群通知（Hermes 企微网关）**：自动在微信群发「本次需付 ¥X，订单含 A/B/C…」，@ 相关负责人。
- **到账跟踪**：付款后文员标记「已到账」，状态流转到「可下单」，全程留痕可追溯。

### ⑤ 厂家系统下单（外部手工，半自动准备）
- hergent.cn **一键生成「厂家系统粘贴模板」**（字段顺序、格式按厂家系统要求排版，确定性输出，不是 AI 即兴）。
- 文员复制进厂家系统完成下单——这一步无 API，只能人做，但准备工作降到最低。

### ⑥ 舟谱入账（外部手工，半自动准备）
- Hermes **一键生成舟谱导入 CSV**（你现在已在用，但要做成确定性：固定字段、固定格式，每次一致可复现，不再「AI 随口给」）。
- 文员导入舟谱 + 打印——导入动作仍手工，但模板零差错。

## 四、数据前置条件（hergent.cn 要算账，必须先有这些数据）

| 数据 | 来源 | 频率 | 说明 |
|---|---|---|---|
| 厂家总指标 / 单品指标 | 你录入（合同） | 每合同期一次 | 你的 IP，收益的最高壁垒 |
| 返利条款 | 你录入（合同） | 每合同期一次 | 阶梯/搭赠规则 |
| 实际销量 / 库存 | 舟谱导出喂入 或 hergent 轻量记录 | 每次报单 | 决定达成率与效期 |
| 预报订单 | 表单 / 企微 | 每次报单 | 一线填报 |
| 厂家账户余额 | 文员手工填 | 每次付款前后 | 厂家系统无 API |

> 关键提示：你之前纠结「数据从舟谱来还是 Hergent 来」。在「只做 AI 层」的当下，最务实的路径是 **舟谱导出 CSV → 喂给 hergent.cn 计算**，不必先建录入系统。等验证了价值、有团队了再决定是否把录入也迁过来。

## 五、分阶段落地建议

- **P1（立刻做，价值最高）**：② 智能审核大脑（指标达成 + 返利 + 库存 → 建议订单）+ ⑤⑥ 一键生成两个确定性模板。
  - 前 4 个工具的算数引擎（预报/货损/对账/工资）后端已存在；指标达成与返利是待建引擎，且由你亲自给规则。
- **P2（接着做）**：④ 余额跟踪 + Hermes 企微自动付款通知 + 到账流转。
- **P3（看时机）**：若蒙牛/舟谱未来开放 API，再升级为直连；否则长期维持「模板 + 人工粘贴」模式，这完全不影响产品可用性与收费。

## 六、一句话价值与风险提示

**价值**：把「每单靠人经验拍脑袋 + 大量复制粘贴」变成「AI 算好建议、人确认、一键出模板」，你最值钱的返利判断被固化成可复用引擎，且全程在企微群里主动推送，老板不用学新系统。

**风险（务必知晓）**：
1. ⑤⑥ 永远依赖人工粘贴/导入，别承诺「全自动下单」「自动同步舟谱」——会踩合规与稳定性雷。
2. 返利/指标规则必须由你亲自把关录入，AI 只执行你给的规则，不因「聪明」擅自改数。
3. 数据准确的前提是舟谱导出/录入准确；garbage in garbage out，AI 救不了脏数据。

---
*关联：此前已隐藏 Web 版「进销存」菜单（保留底层引擎）；本方案复用 hergent-erp 已有的 forecast_engine / check_expiry / 对账 等确定性引擎，不新建 ERP。*
