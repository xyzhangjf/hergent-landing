# v121 智能报单表 — 部署验证报告（2026-09-08）

## 部署范围
v121 第一批（配置面板 + 未来 6 期预览闭环，不含调度执行 P3）。
- 后端 `52d7806`（hergent-erp，分支 upgrade/v84-international，未 push）
- 前端 `9c2aa5c`（hergent-cn-v2，main）

## 关键风险与处置
生产现场与本地工作区并非「干净 HEAD」关系。经 md5 全量比对：
- **生产当前跑的就是未提交工作区版**（如 `forecast.py` 生产 md5 = 工作区 md5，完全一致）。那 3789 行未提交改动 + 多个未跟踪新文件（ai_channels / chanjet_connector / datasource_store / recipe_sync / routers/admin_backup / ai_fallback / ai_observability 等）**早已在线上运行**——`server.py` 第 762–785 行 `include_router` 它们，生产 health=200 即证。
- v121 提交的 4 个文件（`arrival_schedule.py` / `erp_db.py` / `rebate_rules.py` / `forecast_submissions.py`）**是唯一从未部署的部分**。
- 若只部署 HEAD，会把线上正在用的 `forecast.py`(+614 行) 回退 → **功能倒退/报错**。

**结论**：部署整个工作区（= `deploy.sh` 默认行为），既保留线上代码、又把 v121 的 4 个文件更新上线，零回退、零新增风险。差异比对证明工作区与生产内容差异**仅此 4 文件**。

## 执行结果
| 项 | 结果 |
|---|---|
| 后端 rsync | ✅ 4 个 v121 文件落地（md5 与生产=HEAD 完全一致） |
| 后端重启 | ✅ `systemctl restart hergent-erp`，服务 `active`，日志正常 200 |
| 前端 dist 同步 | ✅ rsync + chown；`index.html` HTTP 200；新资源 `Rebate-D8QEgI8N.js` 已上线 |
| 预览端点 `GET /api/rebate-rules/auto-period-preview` | ✅ boss 角色 HTTP 200（demo 租户返回正确空告警信封） |
| RBAC | ✅ supervisor(mptestsp) 403（按设计：`/api/rebate-rules`→sales 模块，supervisor 无权限）；boss 200 |

## 部署脚本注意点（已暴露，待修）
`deploy.sh` 的 `rsync --delete` 因生产独有目录（`.hermes` / `hermes-engine/` / `backups/2026*` / `incident_20260812`）非空而报 `cannot delete non-empty directory`，但**文件传输已完成、重启已执行**（health 在 sleep 5 后正常）。这些目录非 server 源码，不影响运行。后续可给 `deploy.sh` 加 `--exclude` 或在 rsync 加 `--force` 消除噪音（不改行为）。

## 已知遗留（非 v121 引入）
生产日志持续告警 `no such table: webhook_events` / `no such table: cron_tasks` —— 来自其它未提交功能的待跑迁移，已被 try/except 兜住，服务正常，不阻塞。待相关功能上线时一并补齐迁移。

## 下一批（v121 后续，仍待用户拍板）
- P3 调度层（`forecast_period_jobs` 台账 + monitor，写生产 `forecast_periods`）
- P4b 管理员重开 `POST /periods/{pid}/reopen`（仅 admin/boss）
- P4c `forecast_period_current()` 改用 `open_at/close_at` 时刻判定
- P6 `forecast_periods` +10 列
- P7 触达（通知 / 品牌标签 / 小程序顶部「本期需报：X、Y」）
