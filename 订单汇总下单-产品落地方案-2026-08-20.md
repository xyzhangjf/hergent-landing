# 订单汇总 / 下单流程 — 产品落地方案（Hergent）

> 基于真实文件 `最新26年8月下单表8.18.xlsx`（69 个工作表）+ 你描述的 6 步流程 + 现有代码底座，全盘梳理后给出。
> 目标：**用一张 Hergent「预报订单工作台」替代「企微智能表 → 文员导出 Excel → 粘贴下单表 → 人工审核 → 查余额/通知付款 → Hermes 生成舟谱模板 → 手动导入」这条长手工链**。

---

## 0. 一句话结论

你这条链路的"断头路"不在报单（小程序已能报），而在 **中间三道人工墙**：① 文员手抄 Excel 汇总；② 审核靠经验拍脑袋（指标/库存/返利全在脑子里）；③ 余额/付款/舟谱靠手工粘。Hergent 已经把"报单落库 + 周期 + 客户主数据 + 副驾推送"这些地基打好了，**真正要补的是把这三道墙做成系统能力**，而不是从零造一个预报模块。

---

## 1. 现状还原（你的真实表 + 流程）

### 1.1 Excel 真相：不是一张表，是 69 本"双日报单台账"
- 共 **69 个工作表**，每表 = 一个报单周期（`M月D报单 - M月D到货`，间隔约 4 天），从 4 月到 8 月一路排开。表名就是业务主键。
- **行 = 商品 × 下单主体**（如 `红桶（恒滋下单）` vs `红桶（福宝下单）`，同一 SKU 按恒滋/福宝拆成两行）→ 约 160 行/表。
- **列 = 客户（动态维度）**：业务员/导购/门店/渠道混排，19–21 列，且名单随周期变（秦小芳→刘小顶、电商→枣阳+成丽、新增汴河/美联保康）。
- **单元格 = 订货数量**（稀疏，每行通常 2–4 个客户有值）。
- **计算列**：分销价格（公式里藏了 `÷0.9` 返点系数）、合计、件数、最终下单、下单金额。
- **严重缺失字段**：❌ 返利、❌ 库存、❌ 厂家余额、❌ 付款金额、❌ 结构化下单日/到货日（都塞在 `AG 订单排期` 的自由文本里）。
- **数据质量痛点**：永辉代码混填、商品编码不统一、无冻结窗格、手工点状配色、D 列"简称"把品类/下单主体/到货节奏/下单规则揉成一团非结构化文本。

### 1.2 你的 6 步流程（每步痛点）
| 步 | 动作 | 痛点 |
|---|---|---|
| ① | 业务员/促销/导购/分销 在企微智能表填预报 | 企微表与下单表是两套，数据不互通 |
| ② | 文员导出 Excel → 粘贴进下单表 → 发同事审核 | **手抄/重录入，双倍劳动、易错** |
| ③ | 同事审核：综合①指标达成（总指标+单品指标，关联返利）②库存 → 定最终订单 | **全靠经验拍脑袋，无系统辅助，新人接不住** |
| ④ | 文员查厂家余额 → 订单金额−余额=差额 → 微信群通知付款 | **余额手工查、差额手工算、付款手工催** |
| ⑤ | 付款到账后，文员把订单粘贴进厂家系统完成下单 | **又一道手抄** |
| ⑥ | 到货前 1 天，Hermes 生成舟谱导入模板 → 手动导入舟谱 → 通知文员打印 | 半自动，但仍是人工粘贴导入 |

**节奏**：每 2 天到货、提前 4 天下单；报单主要依赖员工经验。

---

## 2. 已有哪些底座（严禁重复造）

| 能力 | 现状（代码实证） | 可复用点 |
|---|---|---|
| 小程序报单落库 | ✅ `routers/forecast_submissions.py`：员工提交→`forecast_submission_create` 落库，含 AI 留痕 | 直接替代企微智能表 |
| 汇总总表 | ✅ `forecast_submission_summary`：按 SKU×单位聚合 + `sources`（含 store_name/role/user_id/qty） | **前端透视 `sources` 即可还原"商品×客户"交叉表，不改模型** |
| 周期实体 | ✅ `forecast_periods` 表（name/order_start/order_end/arrival_date/status） | 周期已是一等实体，直接套用 |
| 客户主数据 | ✅ `contacts`（type=customer/supplier/both） | 客户列可映射为"报单单元" |
| 智能导入器 | ✅ `routers/import_router.py`：preview/execute/进度/AI 列映射/冲突检测/事务回滚 | 加 `forecast_cross` 类别即可导入你的 Excel |
| 定时推送 | ✅ `scheduler.py`：经营卡简报 + 企微/飞书通道（无 webhook 自动 no-op） | 催单直接套范式 |
| AI 副驾卡 | ✅ M4：loss/payroll/rebate 三卡 + 双栏 + 全屏 + 进度条 + 溯源 | forecast 卡直接套同款 |
| AI 建议量 | ⚠️ `forecast_submission_summary` 已接 `forecast_engine.forecast_product`（仅件/瓶/袋/个单位） | 审核台可复用，但需扩展到"周期级" |
| 审批+库存对比 | ⚠️ `forecast_submission_pending` 已含"库存对比 + AI 建议 + 置信度" | 是**单条提交**级，缺**周期级 consolidated 审核** |
| 舟谱模板 | ✅ Hermes agent 已能生成舟谱导入模板 | 接成按钮即可 |

**结论**：80% 地基已存在。本方案只补"交叉视图 + 周期级审核大脑 + 余额/付款闭环 + 结构化日期 + 返利关联"这几块真缺口。

---

## 3. 目标产品架构

```
┌─────────────────────────────────────────────────────────────────────┐
│  小程序（业务员/促销/导购/分销）  一键报单（替代企微智能表）          │
└───────────────────────────────┬─────────────────────────────────────┘
                                 │ 自动落库
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│  Hergent 预报订单工作台（Web，老板/文员）                            │
│  ├─ 周期切换（复用 forecast_periods：8月14报单-8月18到货）            │
│  ├─ 二维交叉表（商品行 × 报单单元列，行/列小计+总额）  ← P0-1        │
│  ├─ Excel 导入引导（导旧表/补录）                    ← P0-2          │
│  └─ AI 副驾实时读（"今天还差谁没报"直接出卡）        ← P0-4          │
└───────┬───────────────────────────┬─────────────────────────────────┘
        │ 到点自动                  │ 提交截止后
        ▼                           ▼
┌───────────────┐         ┌───────────────────────────────────────────┐
│ 定时催单       │         │ AI 审核台（周期级 consolidated）           │
│（scheduler）   │         │ 指标达成 + 库存 + 返利 → 建议最终订单量    │
│ 推未报者       │         │ 复用 forecast_engine + pending 库存对比     │
└───────────────┘         └───────────────────┬───────────────────────┘
                                               │ 老板确认最终订单
                                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│  财务闭环                                                          │
│  ├─ 厂家余额录入 + 差额自动算（订单金额−余额）  ← P1-2               │
│  ├─ 付款通知（微信群，复用推送通道）                                 │
│  └─ 到账确认 → 一键生成舟谱导入模板（Hermes）  ← P1-3               │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
                    厂家系统下单 / 舟谱导入（蒙牛无 API，保留粘贴模板降级）
```

**副驾贯穿全程**：每个周期自动推"经营卡简报 + 还差谁没报 + 审核建议 + 到货提醒"。

---

## 4. 落地计划（P0 → P2，按杠杆/风险排）

### P0 — 最小可用闭环（替代企微+Excel 手工，最高杠杆、最低风险）
| 编号 | 动作 | 复用/新增 | 工作量 | 价值 |
|---|---|---|---|---|
| **P0-1** | **二维交叉表视图**：Forecast.vue 新增"交叉表"视图，由 `summary.sources` 透视出 商品×报单单元 矩阵，带行/列小计+总额+底部钉条 | 纯前端，复用现有 summary 接口 | 小 | 高：直接"还原你原表的样子"，零培训 |
| **P0-2** | **Excel 导入引导**：`import_router` 加 `forecast_cross` 类别，识别"商品行×客户列"自动映射进 `forecast_submission_create` | 扩展现有导入器，不重写 | 中 | 高：导旧表/补录，平滑迁移 |
| **P0-3** | **定时催单**：`scheduler` 加 `_push_forecast_reminder()`，按"应报名单 vs 已报"算"还差 X 人"推企微/小程序 | 套经营卡简报范式 | 中 | 高：不催也跑，老板省心 |
| **P0-4** | **副驾实时读**：`GET /api/ai/forecast-card` + `FORECAST_RE`，老板问"今天还差谁/订了多少"直接出卡 | 套 M4 同款 | 小 | 中：副驾"活"起来 |

### P1 — 审核大脑 + 财务闭环（产品差异化核心）
| 编号 | 动作 | 复用/新增 | 工作量 | 价值 |
|---|---|---|---|---|
| **P1-1** | **AI 审核台（周期级）**：把 `pending` 的单条库存对比+AI 建议，升级为"整周期 consolidated"——综合 厂家总指标/单品指标 + 实时库存 + 历史销量，给每个 SKU 建议最终订单量，老板点确认即定稿 | 扩展 forecast_engine + 新聚合 | 大 | **极高**：替代"经验拍脑袋"，这是竞品没有的 |
| **P1-2** | **余额 & 付款跟踪**：厂家余额字段（手工录入或对接）+ 差额自动算 + 付款通知（复用推送） | 新表+复用推送 | 中 | 高：消灭 ④⑤ 手工 |
| **P1-3** | **一键生成舟谱导入模板**：把 Hermes 模板生成接成按钮，审核定稿后直接出 | 接现有 Hermes | 小 | 高：消灭 ⑥ 手工粘贴 |
| **P1-4** | **结构化日期/下单主体**：把 Excel `AG` 自由文本→结构化 下单日/到货日 + 下单主体（恒滋/福宝）拆字段；交叉表列支持"按报单单元/按业务员"切换 | 模型小扩展 | 中 | 中：为 P2 分析打底 |

### P2 — 打磨 / 产品化（卖给同行）
| 编号 | 动作 | 价值 |
|---|---|---|
| **P2-1** | 返利关联：指标达成 → 返利档测算，接现有 rebate 卡 | 高：审核直接看"冲这档返利该订多少" |
| **P2-2** | 客户分级底色 + hover 金额/返利档 + 同比副指标 | 中：业务员优先级清晰 |
| **P2-3** | 历史周期归档对比（你那 69 张表的价值释放） | 中：复盘/趋势 |
| **P2-4** | 多租户/同行 SaaS 化（每客户隔离实例） | 战略：变现 |

---

## 5. 必须先定的 3 个建模决策（影响 P0 怎么写）

1. **交叉表的"列"= 谁？**
   - 你的 Excel 列是 业务员/导购/门店/渠道 混排。Hergent 现有 `store`（来自 contacts 客户主数据）和 `user`（员工）两个维度。
   - **建议**：列 = 可配的"报单单元"，默认映射 `store`（客户/门店），也可切"按业务员"。这样不同经销商习惯都能兼容。

2. **"下单主体"（恒滋/福宝）怎么存？**
   - 这是蒙牛给你的两个子账户，不是商品属性。建议拆成独立维度（商品行可挂"下单主体"标签），而非揉在 D 列简称里。

3. **返利 / 库存 / 余额 数据从哪来？**
   - 蒙牛/舟谱无开放 API（你已确认）。→ 走**主数据手工录入 + Excel 导入**降级路径，Hergent 负责聚合与辅助决策，不承诺自动直连。

---

## 6. 我的建议：先打 P0 四件套

按你的节奏（P0→P1→P2 自动连续、不中途确认），**第一步做 P0-1 + P0-2 + P0-3**：
- P0-1 直接给你"看起来跟原表一样"的交叉视图（满足你上一条说的"还原原表样子"），且数据可被 AI 读；
- P0-2 让你把现有 69 张 Excel 平滑导进来，不丢历史；
- P0-3 让催单自动跑，替代右栏手写提醒。
- 这三者纯前端为主 + 复用现有导入器/调度，**零新数据模型风险**，立刻替代企微+Excel 手工链。

P1-1（AI 审核台）是真正的"护城河"，但依赖 P0 跑通 + 指标/库存主数据就位，放第二阶段。

---

## 7. 风险与验证
- ⚠️ **本沙箱无浏览器**，任何前端改动都需你在 `hergent.cn` 真机自测（开副驾→问"还差谁没报"→看卡；切周期→看交叉表）。
- ⚠️ **蒙牛/舟谱无 API**：P1-2/P1-3 的"余额/舟谱"仍是半自动（生成模板+手工粘贴），Hergent 不承诺自动直连——这是战略已定的降级路径，不是缺陷。
- ✅ 后端 `py_compile` + 前端 `npm run build` + 生产 rsync/restart/HEALTH 验证链路已跑通多次。

---

## 8. 下一步
回 **「要」** → 我先把 **P0-1 交叉表视图** 出 UI mock + 数据结构（不立刻上码，先对齐你想要的视觉），对齐后端到端做 P0 四件套；或回 **「先 P0-1」/「先 P0-2」/「全列排期」** 指定起点。
