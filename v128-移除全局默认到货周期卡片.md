# v128 · 移除「全局默认到货周期」卡片

> 用户拍板：**A（直接删）**

## 一、为什么删

| 消费点 | 位置 | 改这个值是否受影响 |
|---|---|---|
| **后端计算** | — | ❌ 完全不读。`get_tenant_param("default_arrival_cadence_days")` 只在 `server.py:2477` 的 GET /api/params 出现一次；`arrival_schedule.effective_interval` 对 cadence<1 直接返回 999，注释里的「0=用全局默认」**从未实现** |
| **新建/复制规则** | `useRebateTargetForm.js:245` | ⚠️ 只写进 v121 后的回退字段 `arrival_cadence_days`；真正驱动报单节奏的 `order_cadence_days` 在 L248 **硬编码 2** |
| **Forecast 冲刺看板** | `Forecast.vue:5379` / `5488` | ✅ 唯一真正生效处（表头概览 + 规则 cadence=0 时逐行兜底） |

三条删除理由：
1. **不在客户门面** —— 运维兜底参数，客户天天看的是目标达成。
2. **承诺已失效** —— 卡片写「规则留空时沿用此默认值」，但全局改成 3，新规则还是 2。
3. **口径过期** —— 卡片写「到货周期」，v121 后统一叫「报单周期」。

## 二、改动清单（`Rebate.vue` 单文件）

| 位置 | 改动 |
|---|---|
| 模板 | 删卡片 div（13 行），改留注释说明移除原因 |
| 脚本 | 删 `defaultCadence` / `cdnSaving` / `loadDefaultCadence` / `saveDefaultCadence`（22 行） |
| `onMounted` | 去掉 `loadDefaultCadence()` |
| 传参 | 去掉 `:default-cadence="defaultCadence"`（子组件 prop 保留，默认 2 兜底） |
| 样式 | 删 `.cadence-default-card` 系列（6 行） |

**保留不动**：`GET/PUT /api/params`、`Forecast.vue` 的 `rebateGlobalCadence` 兜底。
→ 参数值冻结在当前 `"2"`，**行为零变化**。

## 三、验证

| 项 | 结果 |
|---|---|
| 后端 health | 200 |
| `/api/params` | 仍返回 `{"default_arrival_cadence_days":"2"}` |
| dist 复核 | 「全局默认到货周期」「cadence-default-card」字面量已消失；Forecast chunk 仍含 `default_arrival_cadence_days` |
| 真机 · 目标与返利 | 无卡片；5 行规则、两个创建按钮均在 |
| 真机 · 新建品牌目标 | 「报单周期(天)=2」「提前天数=4」默认值正常，其余字段齐全 |
| 真机 · 编辑 #10 | 标题「编辑品牌目标」，12 格回填 60/50/60…，报单周期 2 |

**commit**：`a6d2bca`（hergent-landing main，已推送）。dist 已备份至 `/root/hergent-backup/hergent-cn-v2-bak-*`。

## 四、遗留

`useRebateTargetForm.js:248` 的 `order_cadence_days: 2` 仍是硬编码 —— 若日后要支持"某租户默认 3 天报单"，需在此处接默认值。本次未改（不影响现状）。
