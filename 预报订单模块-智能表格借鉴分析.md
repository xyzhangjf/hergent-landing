# 企业微信智能表格 × 预报订单模块：功能对比与借鉴建议清单

> 调研日期：2026-08-24 ｜ 对象：企业微信「智能表格」（多维表）v4.1.x–v5.0.x ｜ 对照：hergent-cn-v2 预报订单模块（Forecast.vue）
> 用途：借鉴智能表格成熟的低代码数据管理范式，指导预报订单模块下一步体验与能力升级。

---

## 一、调研结论：企业微信智能表格核心能力速览

| 能力域 | 关键能力 | 说明 |
|---|---|---|
| 字段类型 | 文本 / 单选 / 多选 / 数字(数字·百分数·货币·进度) / 日期 / 图片 / 文件 / 人员 / 群聊 / 关联 / 链接 / 电话 / 邮箱 / 地理位置 / 复选框 / 条码 / 公式 / 查找引用 / 自动编号 / 操作记录(创建人·最后编辑人·创建时间·最后编辑时间) / AI 字段 | 按列强类型约束，多人录入格式一致，杜绝格式漂移 |
| 多视图 | 表格 / 看板 / 甘特 / 画册 / 日历 / 表单 | 同一份数据多维度展示，任一视图编辑实时同步；可设视图权限（限定成员增改视图） |
| 仪表盘 | 柱状 / 折线 / 饼 / 数字卡 / 词云 | 数据随表格自动刷新；拖拽布局 |
| 自动化流程 | 无代码触发（到达记录时间 / 关键数据变化 / 新增记录）→ 动作（发消息 / 改记录 / 发起群聊@成员） | 含运行日志、模板库，无需写代码 |
| 权限管控 | 工作表 / 记录行 / 字段列 / 单元格 四级 | 全员权限 + 指定成员额外权限；字段「可首次填入」(提交后锁死)；按公式/查找引用结果动态限定可见范围；「筛选与我相关」 |
| 数据关联 | 关联字段（关系数据库式，单向/双向）；查找引用跨表自动查找；公式跨工作表计算（FILTER 实现 SUMIF/VLOOKUP） | 单表上限 10 万行 |
| 收集表单 | 字段一键生成表单，手机填报，提交互不可见 | 适合一线外勤录入 |
| 开放接口 | 自建应用读写智能表格，打通内部系统 | wecom-cli smartsheet 系列命令（records/sheets/fields/views/charts 增删改查） |
| AI 字段 | 订单物流 / 财务会计 / 信息提取 / 图像处理等预置 AI 处理 | 把 AI 结论回填为字段 |

---

## 二、预报订单模块现状（hergent-cn-v2 / Forecast.vue）

- **双视图**：只读交叉表（报单汇总）+ 编辑网格（商品手动录入：名称/条码/规格/单位/标准售价/进价/安全库存/保质期）。
- **列设置**：隐藏 / 删除 / 拖拽排序 / 自动保存 / 冻结「商品名称」列（已落地，是字段 schema 雏形）。
- **交互**：序号列 + 列设置齿轮；合计行常驻底栏；全屏按钮；顶部/交叉/编辑三处统一工具栏（刚完成标准化）。
- **业务流**：客户维度 → 预报 → 下单 → 舟谱模板 → AI 审核台 → 余额付款 → 定时催单。
- **状态**：草稿 / 待审核 / 已推送等，但**可视化弱**（多为文本/标记，无看板/甘特/仪表盘）。
- **缺口**：强类型字段约束、表单收集、自动提醒、行列级权限、跨表关联、状态可视化均尚未系统化。

---

## 三、功能对比矩阵

| 智能表格能力 | 预报模块现状 | 借鉴价值 | 落地难度 |
|---|---|---|---|
| 强类型字段 | 网格录入无类型约束，靠命名约定 | 高（杜绝「合计列被抓成客户列」类错乱） | 低 |
| 多视图（看板/甘特/表单） | 仅表格双视图 | 高（状态跟踪、排期、移动录入） | 中 |
| 仪表盘 | 仅副驾每日简报文本 | 中（预报专属可视化看板） | 中 |
| 自动化流程 | 仅「定时催单」雏形 | 高（催单/通知/派生列自动化） | 中 |
| 行/列/单元格权限 | 仅角色页面级 | 高（进价/返利敏感字段隔离） | 中 |
| 关联 / 查找引用 | 预报与商品/客户主档分离 | 高（改主档联动预报单价） | 高 |
| 收集表单 | 仅 Excel 粘贴 + 网格 | 中（一线手机填报） | 中 |
| 开放接口 | 无 | 中（外溢到企微生态） | 高 |
| AI 字段 | 无 | 中（AI 结论回填字段） | 高 |

---

## 四、五个维度借鉴建议清单

### 维度 1 ｜ 提升订单数据录入与管理的灵活性

- **强类型字段约束**：商品名(文本) / 条码(条码·可接手机扫码) / 规格 / 单位(单选) / 标准售价·进价(货币) / 安全库存(数字) / 保质期(日期或天数) / 客户(人员或关联)。录入即校验，从根上消除后端 `import_router` 的 `_CROSS_SKIP_KEYWORDS` 抓错列痛点。
- **表单 + 网格双模**：把「编辑网格」升级为「表单填报 + 网格复核」——一线销售/促销用手机表单逐行填（仿智能表格表单，提交锁死不可改），文员在网格批量复核。复用现有 `cell-input` 网格，新增 form 模式。
- **字段 schema 配置化**：现有列设置（隐藏/删除/拖拽/冻结）下沉为「字段定义表」，支持新增自定义字段（如批次/温区），与后端 `products`/`grid` 打通。
- **自动编号**：预报期次 / 订单号按规则自动生成（现有「新建期次」加编号规则）。
- **条码 / 图片字段**：扫码录入商品（现有条码列接手机扫码）；到货照片上传（货损佐证）。

### 维度 2 ｜ 实现订单状态的可视化跟踪

- **看板视图**：按状态（草稿 / 待审核 / 已推送 / 已下单 / 已到货）分组，拖拽改状态——对应智能表格看板。在预报模块新增 Kanban toggle（复用 Toolbar 的 `chip` 视图切换）。
- **甘特 / 日历视图**：按「预报日期 → 到货日期（提前 4 天 / 每 2 天到货）」排期，甘特展示品类到货节奏，逾期标红——对应智能表格甘特。
- **状态强类型 + 配色**：状态用单选（选项）而非自由文本，option 配色，对应「单选 + 填色」。
- **预报专属仪表盘**：达成率 / 各客户下单占比 / 品类金额排名 / 延误预警（柱状 / 饼 / 数字卡），在副驾每日简报外独立成板。
- **行条件高亮**：临期未推送、超安全库存预警行自动高亮——对应智能表格「条件填色」。

### 维度 3 ｜ 通过自动化规则减少人工操作

- **定时催单规则化**：把现有「定时催单」标准化为规则引擎——触发（到达预报截止前 N 天 / 状态≠已推送）→ 动作（发企微/飞书提醒责任人）。对应「到达记录时间 / 数据变化 → 发消息」。
- **状态流转自动通知**：草稿 → 待审核 → 已推送 流转时自动 @ 对应角色（销售/督导/经销商）进群。对应「新增记录 / 数据变化 → 发起群聊@成员」。
- **自动派生列**：据「标准售价 − 进价」自动算毛利（公式字段）；据安全库存与预报量算建议补货（查找引用 / 公式）。对应智能表格公式 / 查找引用。
- **运行日志**：自动化执行留痕、异常可查——对应智能表格运行日志。

### 维度 4 ｜ 优化多角色协作与权限控制

现有角色：销售 / 促销 / 督导 / 经销商（小程序多租户）。借鉴智能表格四级权限：

- **工作表级**：经销商仅看本客户预报；督导可审全部。
- **记录行级**：成员只可编辑「负责客户」的行（「仅可编辑包含自己的记录」）——按创建人 / 负责人员设行权限。
- **字段列级**：进价 / 毛利 / 返利等敏感字段仅老板 / 财务可见；客户联系方式对促销隐藏（「字段权限 可查看 / 无权限」）。
- **锁定提交**：已推送 / 已下单的行锁死不可改（「可首次填入 / 提交后不可改」）。
- **视图权限**：限制仅指定成员可增改视图（防误改分组 / 筛选）。
- **实现路径**：现有租户 + 角色体系已具备，只需在 `DataSourceAdapter` 层加 row / column filter，**无需再造 ERP**。

### 维度 5 ｜ 利用数据关联能力打通预报订单与其他业务环节

- **关联字段**：预报订单 ↔ 商品主档（products）↔ 客户主档 ↔ 专属价 ↔ 返利规则 ↔ 货损记录。改商品主档自动联动预报单价（查找引用）。对应智能表格「关联 / 双向关联」。
- **跨表公式**：按商品编号查库存（查找引用）、按客户汇总预报额（FILTER 跨表）、按品类算返利（公式）。现有 `forecast_engine` 承接，前端新增「关联视图」读取。
- **API 打通（外溢到企微生态）**：通过 `wecom-cli smartsheet` / 企微机器人，把预报数据同步到企业微信智能表格——不登录 Hergent 的督导 / 老板也能在企微里看板 + 仪表盘 + 收自动提醒。这是把副驾能力「轻量外溢」的捷径，无需经销商学新系统。（注：当前 `wecom` 连接器为 disconnected，需先连接。）
- **与 AI 副驾融合**：AI 审核台 / 每日简报已是基于数据的自动洞察，可进一步用智能表格「AI 字段（订单物流 / 财务会计）」思路，把 AI 生成结论回填为字段（如「AI 建议下单量」）。

---

## 五、优先级与落地路线

| 优先级 | 项目 | 说明 |
|---|---|---|
| **P0（低难高价值，近期）** | 状态单选强类型 + 行条件高亮预警；定时催单规则化；字段列级权限（进价/返利）；条码 / 自动编号 | 改动小、体验与安全感提升明显 |
| **P1（中难）** | 看板 / 甘特视图；预报专属仪表盘；表单收集模式；关联字段（预报↔商品↔客户）；公式派生列（毛利 / 补货建议） | 需要前端多视图 + 后端关联查询支撑 |
| **P2（高难 / 生态）** | 企微智能表格双向同步（wecom-cli smartsheet）；AI 字段回填；完整自动化规则引擎 + 运行日志 | 涉及连接器与跨系统一致性 |

---

## 六、风险提示

1. **不造 ERP**：关联 / 权限 / 自动化应在 AI 副驾层（配方化）实现，交易一致性仍走连接器 / Excel，不新增 order CRUD（与既有战略一致）。
2. **多租户隔离**：权限 / 关联查询必须带 tenant 上下文，防串户（呼应后端租户库补丁铁律）。
3. **避免退化**：智能表格只是「借鉴思路」，Hergent 已有更强领域算法（货损 / 返利 / 预报配方），不要为对齐通用表而弱化行业能力。
4. **连接器前置**：P2 的企微同步依赖 `wecom` 连接器连通，建议先行评估授权与数据边界。

---

> 附：当前环境 `wecom`（企业微信）连接器状态为 disconnected；如需落地 P2 的企微智能表格同步，需先在企业微信侧完成应用授权并连接连接器。
