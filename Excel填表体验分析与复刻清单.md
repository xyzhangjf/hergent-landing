# Excel 填表体验分析 × Hergent 预报表体可复刻清单

> 目标：拆解 Excel 中成熟的「填表体验」，逐项说明其实现方式与用户价值，并映射到我们的预报订单表体（`Forecast.vue` 编辑网格）。
> 边界红线：预报表体是**业务矩阵**（行=商品、列=客户/单位），不是通用电子表格。应复刻**降低录入摩擦与错误**的交互范式，但不造公式引擎、不做任意合并单元格——那是 Excel 的地盘，不是我们的产品定位（与「不造 ERP / 只做 AI 经营副驾」战略一致）。

---

## 0. 我们已经有的（避免重复造轮子）

在动手前先盘点，下面这些 Excel 范式**已经落地**，本轮无需重复：

| Excel 范式 | 我们现状 | 来源 |
|---|---|---|
| 单击选中 / Shift+点击区域 / 鼠标拖拽框选 | `selectCell` + 拖拽 `onCellDown/Over/Up` + `selRange` | 前期已实现 |
| 方向键 / Enter(下) / Tab(右) 导航 | `onGridKey` | 前期已实现 |
| 复制选区→粘回 Excel（TSV 一致） | `copyRegion()` | 前期已实现 |
| 从 Excel 粘贴区域（含超界自动加行/截断） | `pasteRegion()` | 上一轮已交付 |
| 撤销 / 重做（多入口） | 工具栏按钮 + `Ctrl+Z/Y` + 右键菜单 | 上一轮已交付 |
| 右键菜单：插/删行列、清空单元格、粘贴、撤销 | `openCtx` / `ctxPaste` 等 | 上一轮已交付 |
| 填充柄拖拽 + `Ctrl+D` 向下填充 | 填充柄 + `onGridKey` | 前期已实现 |
| 首列冻结（商品名称）+ 合计行吸底 | `fixed` 列 + sticky 合计 | 前期已实现 |
| 行级预警徽标（低于安全库存/短保/缺资料） | `rowWarn` / `gapSet` | 前期已实现 |
| 列级权限、列设置（隐藏/删除/拖拽排序） | `canSeeCol` / 列菜单 | 前期已实现 |
| 草稿自动缓存（刷新可恢复） | `saveDraftNow` / watch | 前期已实现 |

> 结论：我们的表体已经是一个「Excel 式业务网格」，下面只聚焦**尚未复刻、且值得做**的项。

---

## 1. 单元格编辑体验

### 1.1 双击进入编辑（F2 / 双击定位光标）
- **Excel 实现**：单击=选中整格，双击/F2=光标进入单元格内编辑并可定位到点击位置；选中态与编辑态视觉分明。
- **用户价值**：避免「一碰就改」的误触；长文本可精确定位光标。
- **映射到我们**：当前单元格本就是 `<input>`，聚焦即编辑。可补一层「单击选中、双击进入编辑并定位光标」的区分（P1，体验向 Excel 看齐，优先级中）。

### 1.2 Tab / Enter 切换（带选择扩展）
- **Excel 实现**：`Tab` 右移、`Enter` 下移、`Shift+Tab/Enter` 反向；选区粘贴后自动激活下一格。
- **用户价值**：纯键盘高速录表，手不离开键盘。
- **映射到我们**：`onGridKey` 已覆盖 ✅。建议补「粘贴后自动聚焦下一格」（P1，小增强）。

### 1.3 公式输入（= 前缀 / 公式栏 / 区域引用 / 函数自动补全）
- **Excel 实现**：公式栏 + `=` 触发，支持函数、拖拽引用、F4 绝对引用、名称管理器。
- **用户价值**：任意派生计算，灵活到极致。
- **映射到我们**：**不复制公式引擎**（红线）。我们的「金额=分销价×件数」等是**按业务规则自动计算列**，比让用户手填公式更稳。→ 保持「计算列自动派生 + 只读展示」，这本身就是比 Excel 更好的业务体验。

---

## 2. 数据校验与提示

### 2.1 输入限制（数据验证：整数/小数/日期/序列/文本长度/自定义）
- **Excel 实现**：「数据 → 数据验证」对话框，设条件（如 0–9999 整数、日期、介于）、输入提示（input message）、出错警告（停止/警告/信息三档）。
- **用户价值**：从源头拦住脏数据，订单/库存录错代价极高。
- **映射到我们**：当前只有 `cellInvalid()` 拦负数。→ **P0 高价值**：按列类型扩展校验规则（数量=非负整数、价格=非负小数、日期格式合法），违规格实时红框 + 悬停提示「应为≥0 的数字」。这是业务表最该补的护城河。

### 2.2 错误提示（红框 + 错误图标 + 批注）
- **Excel 实现**：违规单元格红边框 + 角标错误圆点，悬停出信息；可加批注说明。
- **用户价值**：错在哪、为什么错，一眼定位。
- **映射到我们**：现有 `cellInvalid` 仅红字。→ P0 与 2.1 同批做：单元格错误徽标 + tooltip，风格对齐现有行预警。

### 2.3 下拉选择（序列验证 / 下拉框）
- **Excel 实现**：list 验证，来源为区域或手动枚举；支持输入联想。
- **用户价值**：枚举型字段（单位、状态）零拼错，录入飞快。
- **映射到我们**：`单位`/`规格` 等现在是自由文本。→ **P1**：枚举列在编辑态渲染 `<select>`（单位=件/箱/提…），直接消除「件/箱/简写」不一致导致的对账灾难。

---

## 3. 快捷操作

### 3.1 复制粘贴（多格式剪贴板 / 选择性粘贴）
- **Excel 实现**：剪贴板含 BIFF/HTML/CSV 多格式；选择性粘贴（值/公式/格式/转置）。
- **用户价值**：跨应用无损搬运，格式可控。
- **映射到我们**：我们走 TSV（数据无损、够用），不追求富格式。✅ 已达标。可选 P2：加「转置粘贴」。

### 3.2 拖拽填充（填充柄：复制 vs 序列）
- **Excel 实现**：单元格右下角小方块，拖拽填充；**智能识别序列**（1,2,3 / 周一→周日 / 日期 +1 / 线性），右键拖拽出菜单（复制/填充序列/仅格式/快速填充）。
- **用户价值**：规律性数据秒填，序列自动推断省去手算。
- **映射到我们**：填充柄 + `Ctrl+D` 仅做**复制填充**。→ **P1**：加序列识别（数字等差、日期/星期递推），右键拖拽出「复制/填充序列」菜单。中价值，体验加分项。

### 3.3 自动填充序列 / 快速填充（Flash Fill, `Ctrl+E`）
- **Excel 实现**：从示例学习模式（拆分姓名、重排格式），`Ctrl+E` 一键套用。
- **用户价值**：无公式的「模式识别」填充。
- **映射到我们**：预报网格场景少。→ **P2 / 交由 AI 副驾**：这类「从示例推断」更适合放在 Hermes 副驾里做（如「按客户名推算厂家编码」），而非塞进表体。

---

## 4. 格式与排版

### 4.1 条件格式（单元格规则 / 色阶 / 数据条 / 图标集）
- **Excel 实现**：「开始 → 条件格式」，规则管理器，支持色阶、数据条、图标集、公式规则。
- **用户价值**：海量数据一眼看趋势与异常（红绿高亮、进度条）。
- **映射到我们**：已有**行级**预警徽标（低库存/短保/缺资料）。→ **P1 高价值**：下沉到**单元格级**条件格式，且由「配方」驱动（非任意）：
  - 订量 > 安全库存×2 → 橙红（超订预警）
  - 订量 = 0 但有历史 → 灰（可能漏订）
  - 数量格内嵌数据条（占安全库存比例）
  这比 Excel 任意规则更贴合「AI 经营副驾」定位。

### 4.2 冻结窗格（首行 / 首列 / 拆分）
- **Excel 实现**：「视图 → 冻结窗格」，冻结首行、首列或两者。
- **用户价值**：长表滚动时锚定表头/标识列。
- **映射到我们**：首列（商品名称）冻结 + 合计行吸底 ✅。表头行滚动吸顶若未做可补（P0 小项）。

### 4.3 合并单元格
- **Excel 实现**：合并后居中 / 跨列合并 / 取消合并。
- **用户价值**：标题排版美观。
- **映射到我们**：**不复制**（红线）。我们的表体是规范化矩阵，合并会破坏数据模型与复制/粘贴/权限逻辑。业务范围不需要标题合并。

---

## 5. 多选与批量操作

### 5.1 框选（拖拽矩形 / Ctrl 点选不连续 / Shift 范围 / 点表头选整行整列）
- **Excel 实现**：拖拽框选矩形；`Ctrl+点击` 加选不连续；`Shift+点击` 范围；点行号/列标选整行/列。
- **用户价值**：灵活圈定操作范围。
- **映射到我们**：拖拽 + `Shift+点击` 已做 ✅。**P1 补**：`Ctrl+点击` 切换不连续单元格；点列标选整列（便于批量清/填某客户）。

### 5.2 多单元格编辑（选一片，输一个值，全部写入）
- **Excel 实现**：选中区域 → 输入 → `Ctrl+Enter` 写入所有选中格。
- **用户价值**：批量改值零重复操作。
- **映射到我们**：当前仅单格编辑 + 粘贴。→ **P1 高价值**：选中区域输入 + `Ctrl+Enter` 批量写入（按列类型校验/转换）。例：「把所有选中行的单位改为'箱'」秒完成。

### 5.3 批量清除（清除内容 / 格式 / 全部；Delete 键）
- **Excel 实现**：`Delete` 清内容；「清除」菜单分内容/格式/批注/超链接。
- **用户价值**：一键清一片，不误删结构。
- **映射到我们**：右键「清空此单元格」仅单格。→ **P1**：扩展为「清空选区」（Delete 键清选中区域），与 5.2 配套。

---

## 6. 优先级汇总（结合业务价值与难度）

| 项 | 维度 | 优先级 | 说明 |
|---|---|---|---|
| 列类型校验 + 错误徽标/提示 | 2.1/2.2 | **P0** | 拦脏数据，业务护城河，难度低 |
| 表头吸顶（若未做） | 4.2 | **P0** | 小增强 |
| 枚举列下拉（单位/规格） | 2.3 | **P1** | 消除拼错，中难度 |
| 单元格级条件格式（配方驱动） | 4.1 | **P1** | 异常一眼可见，高价值 |
| 填充柄序列识别 + 右键拖拽菜单 | 3.2 | **P1** | 体验加分，中难度 |
| Ctrl+点击不连续选 + 点列标选整列 | 5.1 | **P1** | 圈选更灵活 |
| 选区 `Ctrl+Enter` 批量写入 | 5.2 | **P1** | 批量改值，高价值 |
| 选区 Delete 批量清除 | 5.3 | **P1** | 与 5.2 配套 |
| 双击进入编辑并定位光标 | 1.1 | **P1** | 向 Excel 看齐 |
| 粘贴后自动聚焦下一格 | 1.2 | **P1** | 小增强 |
| 转置粘贴 | 3.1 | **P2** | 锦上添花 |
| Flash Fill / 模式推断 | 3.3 | **P2** | 交给 AI 副驾更合适 |
| 公式引擎 | 1.3 | **不复制** | 红线 |
| 合并单元格 | 4.3 | **不复制** | 破坏数据模型 |

---

## 7. 落地建议（下一步怎么走）

1. **立刻做 P0**：列类型校验 + 单元格错误提示 + 表头吸顶。难度低、价值高、直接减少「订错量/价格录错」类事故。
2. **P1 分批做**：下拉枚举 → 单元格条件格式 → 批量写入/清除 → 框选增强 → 填充序列。每一项都可独立交付、独立真机验证。
3. **P2 / 外溢**：Flash Fill 类「从示例推断」交给 Hermes 副驾；公式类需求用「计算列自动派生」替代。
4. **始终守住两条线**：① 不造通用电子表格（无公式引擎、无任意合并）；② 条件格式/填充规则尽量「配方化」，让不同客户可差异化配置，而非写死。

> 这份清单可直接作为预报表体后续迭代的 backlog。需要我从 **P0（列校验 + 错误提示 + 表头吸顶）** 开始直接编码吗？
