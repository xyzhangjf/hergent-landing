# H4：残留 500 端点修复 — 完成报告

**负责人**：Senior Developer (高级开发工程师)
**日期**：2026-07-24
**状态**：✅ 已实现 + 已部署生产 + 已 git 提交 + 实弹验证通过

---

## 1. 问题根因

P0-2.1 把 `prepay-refunds` / `bpm/definitions` / `salary-send/history` 等端点 RBAC 放行后，老板访问从 `403` → `500`。

根因不是安全层，而是**纯数据层缺口**：
- `erp_db.py` 门面从未实现对应查询函数（`prepay_refund_*` / `bpm_definition_*` / `bpm_instance_*` / `salary_send_log*` / `notification_channels_*`）；
- `/api/bpm` 属租户必需路由，但 `tenant_*.db` 缺 `bpm_*` / `salary_send_logs` / `notification_channels` 表。

任意请求 → `AttributeError` / `no such table` → 500。

---

## 2. 改动清单（4 文件，commit `ed7b1bd`）

| 文件 | 改动 |
|------|------|
| `server/db/queries/bpm.py` | **新增**。`bpm_definition_create/list/get/update/delete` + `bpm_instance_list/get`。全部走 `get_db()` 租户上下文；`bpm_definition_update` 用 `_BPM_DEF_COLUMNS` 白名单防注入。 |
| `server/db/queries/salary.py` | **新增**。`salary_send_log` / `salary_send_log_list` / `notification_channels_get` / `notification_channels_save`。 |
| `server/db/queries/finance.py` | 补 `prepay_refund_create`(`no=_gen_no("PR")`, `status='pending'`) + `prepay_refund_list`（完整 WHERE 过滤，签名严格对齐 `routers/finance.py`）。 |
| `server/erp_db.py` | re-export 上述函数；新增 `salary_detail_get`（委托 `get_salary_details`）；新增 `_ensure_tenant_module_tables()`：遍历 `DB_DIR/tenant_*.db` 幂等 `CREATE TABLE IF NOT EXISTS` 补齐模块表；master 经 `_safe_migrate('v93_prepay_refunds')` 建 `prepay_refunds`。在 `init_db()` 之后、`init_gl()` 之前调用。 |

---

## 3. 架构收益

- 延续 **facade + `db/queries/` 分层**，新模块与既有 `prepayment_list` / `get_salary_details` 同风（`with get_db() as db:` + `[dict(r)...]`）。
- 新租户自动获得模块表（`_ensure_tenant_db` 复制 master schema）；老租户启动幂等回填，零重复建表。
- **严格未动** `server.py` / RBAC / `core.py` / 安全层 — P0-1 / P0-2 上线态不变。

---

## 4. 验证结果

| 验证项 | 结果 |
|--------|------|
| 本地 smoke（`PYTHONPATH=server ERP_SECRET=test`） | 14 函数齐全；tenant backfill `missing=[]`；master+tenant 两路全通 |
| pytest（仓库根） | 321 passed / 28 skipped / 1 pre-existing failed（与本次无关：`test_invalid_tenant_id_rejected` 401≠400） |
| 生产实弹 47.113.224.140:8700（Bearer token） | prepay-refunds GET **200** · bpm/definitions GET **200** / POST **200**(id=1) · salary-send/history **200** · salary-send/channels **200** |
| 生产无 token（P0-1 行为） | 上述端点 **401**（认证网关注入，符合预期，非 500） |
| 部署前备份 | `/tmp/hergent-backup-20260724080320.tar.gz`（356MB，先 `systemctl stop` 再 tar 避免活服写入中断） |

---

## 5. 铁律遵守

备份 → 精确 scp（禁通配符）→ `chown hergent:hergent` → `find -name '*.pyc' -delete` → `systemctl restart hergent-erp` → 冒烟用临时 Bearer token（测完 DELETE）。

---

## 6. 仍待办（沿用 MEMORY.md）

1. 用户拍板：guide 是否需 `data` / accountant 是否需 buying·stock / `user` 角色去留 / 真实仓管=`库管`。
2. 部署 H1b 统一错误信封（仍在 `core.py` 工作树未部署）。
3. P0-3 租户隔离默认拒绝（测试环境先行）。
4. `gl_dimensions` 功能缺口（`db.gl_dimension_*` 系列未实现，解析 500 已修但功能仍 500，独立待修）。
5. 生产库 `sale_order_items` 0 行（数据问题，非代码）。
6. GitHub `--force` 推送 `upgrade/v84-international`。
