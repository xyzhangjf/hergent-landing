# v122 收尾批次 · 交付说明（提交 / 数据还原 / 文件级拆分 / 后端下沉）

> 完成时间：2026-09-08 23:xx
> 范围：① 两仓提交推送 ② demo 数据还原 ③ 目标表单文件级拆分 ④ 维度归口下沉后端 + 存量回填

---

## 一、交付总表

| # | 任务 | 结果 | 产出 |
|---|---|---|---|
| 1 | 两仓 commit + push | ✅ | 后端 `bc5842d` → `feb0f13`；前端 `ab50be1` → `d1bc2d2` |
| 2 | demo 数据还原 | ✅ | tenant_10 #1「蒙牛8月总目标」100 万 → **65 元**，target_year 回 NULL |
| 3 | 文件级拆分 | ✅ | Rebate.vue 3782 → 2899 行，新增 7 个组件文件 |
| 4 | 维度归口下沉后端 | ✅ | `_normalize_by_dimension()`，4 场景验证通过 |
| 5 | 商品名反查回填 | ✅ | scope_key 非数字时按 name 查 products |
| 6 | `target_year` 存量回填 | ✅ | tenant_1 4 条、tenant_10 1 条；生效期为空的 1 条留空（不臆测） |
| 7 | P3 前置：6 期预览核对 | ✅ 待你确认 | `v122-P3-报单节奏预览核对.md` |

---

## 二、改动清单

### 后端（hergent-erp · `upgrade/v84-international`）

| commit | 内容 |
|---|---|
| `bc5842d` | 全量入库生产实际运行版本（66 文件 / +11248 行），含 v116–v122 积压 |
| `feb0f13` | `_normalize_by_dimension()` + 商品名反查 + 在 `validate_rule` 尾部调用 |

`_normalize_by_dimension` 规则：

- **品牌 + 年度（有 12 月分解）** → 强制 `target_type=amount`、`trigger_mode=on_target`、`trigger_threshold=1`、`rebate_basis=rate`、清 `tiers_json`；顶层 `rebate_rate` 取最早非零月兜底；`period_type` 兜底 `year`
- **品牌 + 单期** → 保留阶梯 / 固定金额（UI 已开放触发方式）
- **商品** → `monthly_*` 一律清空；报单节奏**仅在无真实配置时**归零（防静默抹掉既有排程）

### 前端（laozhangai-product · `main`）

| commit | 内容 |
|---|---|
| `ab50be1` | v121/v121b/v121c/v122 前端改动与积压文档入库 |
| `d1bc2d2` | 目标表单文件级拆分 |

新增 `src/components/rebate/`：

| 文件 | 职责 |
|---|---|
| `useRebateTargetForm.js` | 纯逻辑层：日期工具、12 月分解组装/回填/均分、阶梯解析、默认值与复制工厂 |
| `TargetFormModal.vue` | 容器：持有状态与请求，按 `dimension` 分流品牌/商品页 |
| `BrandTargetForm.vue` | 品牌页：年度/单期双模式 + 节奏区 |
| `ProductTargetForm.vue` | 商品页：周期口径 + 单值 + 返利（无节奏区） |
| `MonthlySplitBlock.vue` | 12 个月分解表 |
| `ArrivalRhythmBlock.vue` | 到货与报单节奏（均单 + 6 期预览 + 反推迁移弹窗） |
| `RebateValueBlock.vue` | 返利形式 / 返利值 / 阶梯档位 |

父页退化为宿主：只传 `mode(create/edit/dup)` + 依赖 props，收 `saved` / `conflict` 事件。
所有依赖经 props 传入，**不复用父页作用域变量**，可独立灰度与回退。

---

## 三、真机回归（Chrome + demo boss，tenant_10）9 项全绿

| # | 场景 | 结果 |
|---|---|---|
| 1 | 新建品牌目标默认态 | 标题「创建品牌目标」、12 行、年度 2026、无周期口径/目标度量 ✅ |
| 2 | 全年 1200 万 | 12 × 100 万、合计 1200 ✅ |
| 3 | 只改 3 月 = 150 | 其他月仍 100、合计 1250 ✅ |
| 4 | 报单日 9/9 + N=2 | 到货 9/11（周五）✅ |
| 5 | 切单期 | 12 月表消失、本期目标金额 + 触发方式出现 ✅ |
| 6 | 商品页 | 无节奏区、无月度分解、候选 12、默认 amount ✅ |
| 7 | 编辑存量月规则 | 自动回填单期、金额 0.0065 万 ✅ |
| 8 | 编辑年度规则 | 回填年度、8月110/9月90·8%、合计 200 万 ✅ |
| 9 | 新建落库 + 复制 | period_type=year、12 月各 100 万、返利率 5%；副本名 + 生效期清空 ✅ |

控制台 **0 error / 0 warn**；测试数据已物理删除，存量数据零改写。

---

## 四、本轮踩坑与修正

1. **GitHub https 凭据失效**（PAT 过期）→ 改走 **SSH:443**（`ssh://git@ssh.github.com:443/...`），两仓均秒推。建议后续都用这条。
2. **切片脚本插入被 `continue` 吞掉** —— 插入点与删除区间重叠时，先 `continue` 会跳过插入。必须把插入分支写在 `if i in kill: continue` **之前**。
3. **括号锚点误判** —— 校验删除区间结尾时只比了 `}` 前缀，`parseRuleTiers` 的 `} catch (e) { return [] }` 也以 `}` 开头，导致多删/漏删一个括号。**校验要用完整行内容**。

---

## 五、遗留

1. **P3 调度执行未开工**（等你在 `v122-P3-报单节奏预览核对.md` 确认节奏，尤其「周末是否报单」）
2. **feature flag 未做** —— 旧弹窗代码已随拆分删除，做 flag 需要把 290 行旧代码留成分支，与拆分目的冲突。改用 **git 回退**（`git revert d1bc2d2` + 重建部署，约 1 分钟），等价且更干净。**如果你要求页面级开关，我可以补。**
3. demo 租户品牌档案为空 → 预览里「本期应报」只显示「本品牌」，真实租户会列出品牌名 + 提前天数。
