# Hergent × 畅捷通 技术可行性探路报告（2026-08-19）

> 目标：验证"Hermes AI 副驾 + 畅捷通/好生意/T+ 数据源"是否技术可行，并给出用户注册后可照做的探路脚本。
> 边界说明：**注册开放平台账号、企业实名认证、客户授权**这三步必须由用户本人完成（需手机号+企业资质+客户侧授权动作），AI 无法代过。本报告完成的是"无账号情况下的接入机制研究 + 可行性评估 + 可执行清单"。

---

## 一、结论先行

**技术可行，且门槛比预期低。** 原因：

1. **畅捷通官方已把 SaaS OpenAPI 封装成标准化 MCP 服务**（CJTMSP-MCP），第三方 AI agent 可直接接入读数据，无需自己写对接层。
2. **Hermes 原生支持 MCP**——Hergent 的 AI 大脑（Hermes agent）本就支持 MCP 连接器，接入 = 在 config 里加一段 MCP server 配置。
3. **社区已有成熟开源实现**可作自托管/多租户参考，不必从零造轮子。

⚠️ **关键认知**：连接是标准化的、谁都能做，所以"能连上畅捷通"本身**不是护城河**。**真正的价值在你连上之后，用低温奶配方化算法（货损/返利/预报/工资 skill）做决策建议**——这是通用 SaaS 不会为单一细分行业定制的。

---

## 二、接入机制（三条路，按推荐度排序）

### 路线 A（最省事）：用畅捷通官方 MCP 服务 CJTMSP-MCP
- **MCP 市场**：`https://mcphub-admin.static.chanjet.com/marketplace`
- 流程：登录市场 → 搜 `CJTMSP-MCP` → 点"生成 MCP 配置" → 勾选产品（如"畅捷通云"）→ 客户 OAuth 授权 → 复制 JSON 配置 → 导入到 MCP 客户端。
- 适合：快速验证"Hermes 能否读畅捷通数据"。

### 路线 B（可控/多租户）：自托管社区开源 MCP server
- **cjt2mcp**（glama.ai）：畅捷通 OpenAPI → MCP(streamable-http) 转换，单 Docker 部署，**多自建应用托管模式**——每个客户对应一个畅捷通自建应用，凭据 AES-GCM 加密入库，再给客户交付独立 MCP Key。这恰好是 Hergent"每客户隔离实例"思路的现成参考实现。
- **Chanjet MCP Server**（lobehub）：通用调用 `/tplus/api`、`HYC/ZPlus`、`YDZ/Finance`、`HKJ/Accounting` 接口，自动注入 appKey/appSecret/openToken，支持 OAuth 授权码换 token、多账号 token 自动刷新。`.env` 配 `CHANJET_APP_KEY/SECRET` 即可。
- 适合：要私有部署、掌握数据、做大客户隔离的正式产品。

### 路线 C（最重）：直接用 OpenAPI 自研
- 不推荐。MCP 已封装好，自研等于重复造轮子。

---

## 三、认证模型（必须懂的链路）

```
开发者注册开放平台（企业认证）
   └─ 创建应用 → 拿到 AppKey / AppSecret
        └─ 客户在畅捷通侧"开通授权"（企业级绑定）
             └─ 客户"用户授权"（OAuth 授权码 code）
                  └─ 用 code 换 openToken（默认 6 天有效，可刷新）
                       └─ 调用业务 API（300+/600+ 个，覆盖进销存/财务）
```

- **沙箱**：开放平台提供沙箱 appKey/appSecret，可先免真实客户联调。
- **接口权限**：调用前需在控制台为应用申请所需接口的调用权限，否则报"无权限"。
- **T+ 版本**：T+ 17.0 起强制走开放平台对接（旧版可直连 DB，新版不行）。
- **消息订阅**：支持事件推送（如存货删除、单据新增），基于 RabbitMQ，可做"实时同步"而非仅轮询。

---

## 四、多租户托管参考（来自 cjt2mcp 的设计）

| 维度 | cjt2mcp 做法 | 对 Hergent 的启示 |
|------|-------------|------------------|
| 租户隔离 | 每客户 = 一个畅捷通自建应用，独立 appKey/appSecret | 与你"每客户隔离实例"思路一致 |
| 凭据安全 | AES-GCM 加密入库，MCP Key 交付客户端 | Hergent 多租户库已有隔离基础，可复用 |
| 部署 | 单 Docker，轻量多客户 | 与你"先托管隔离实例再重投入"节奏吻合 |
| token 刷新 | openToken 6 天过期，需刷新机制 | Hermes MCP connector 应内置刷新 |

---

## 五、风险与门槛（诚实列出）

1. **企业认证门槛**：注册开放平台需你的企业资质（你作为蒙牛经销商有营业执照/个体户即可）。这是唯一卡你本人的硬门槛。
2. **客户授权动作**：每个要接入的经销商需在自家畅捷通里授权你的应用（企业管理员首次开通）。——但这其实是**获客闭环的自然一步**：客户愿意授权 = 强付费意愿信号。
3. **token 生命周期**：openToken 默认 6 天，需刷新机制（社区 server 已处理）。
4. **上架 vs 私有**：若只做私有部署/直接授权，不必上架应用市场，规避分润与平台绑定；若要借渠道，再评估上架 ISV（可能 20-30% 抽成）。
5. **命门不绑死**：无论走哪条路，Hergent 必须保留"导入 Excel/CSV、无 ERP 也能跑"的降级路径（现有粘贴模板路线），保持独立获客能力。

---

## 六、用户可执行探路清单（注册后照做，预计 1-3 天）

> 以下步骤中，标注【你】的需本人操作，标注【AI 可协助】的可由我后续代写脚本/配置。

1. **【你】** 注册 `open.chanjet.com` → 企业认证（用你的经销商资质）。
2. **【你】** 创建应用：面向多客户选"商店应用"，单客户验证选"自建应用" → 拿到 `AppKey` / `AppSecret`。
3. **【你】** 进 MCP 市场 `mcphub-admin.static.chanjet.com/marketplace` → 生成 `CJTMSP-MCP` 配置 JSON（含产品授权 + 用户授权）。
4. **【AI 可协助】** 在 Hermes 侧加一段 MCP server 配置（指向步骤 3 的 JSON / 或自托管的 cjt2mcp），使 Hergent 副驾能调用畅捷通工具。
5. **【你】** 用你自己的畅捷通账号（或一个信任的测试客户）完成授权，使 Hermes 能读其真实数据。
6. **【AI 可协助】** 实测读取三类核心数据能否喂给副驾：**库存 / 应收 / 销售单**。
7. **【AI 可协助】** 跑通一个低温奶场景："问 Hermes：本周哪些货临期、货损多少、下趟车预报怎么定"，验证配方化算法在真实数据上成立。
8. **【你+AI】** 评估：私有部署直连（路线 B）vs 上架 ISV 借渠道（路线 A + 应用市场）。

---

## 七、战略含义（回到产品定位）

- **启发 1 被证实不是空想，而是已有官方基建**：对畅捷通/T+ 系客户，Hergent 走"生态集成"而非"舟谱式对抗"完全成立。
- **护城河更清晰**：连接标准化 → 谁都能连 → 价值在"连上后的行业决策"。Hergent 的配方化 skill（货损/返利/预报/工资）是唯一不可被复制的资产。
- **获客杠杆浮现**：畅捷通百万级付费客户 + 应用市场渠道，是 Hergent 作为"低温奶 ISV 行业应用"上架借力的现成通路（但需权衡抽成与绑定）。
- **第二客群成立**：除"自建 Hergent 的同行"外，"已用畅捷通/T+ 的经销商"可列为第二客群，用"你的数据 + 我们的行业大脑"轻量切入。

---

## 八、下一步建议

- 短期（本周）：你完成步骤 1-3 注册与授权，我完成步骤 4-7 的 Hermes MCP 接入与场景实测脚本。
- 中期：根据实测结果，决定是否投入路线 B 的自托管多租户，还是直接上架 ISV。
- 长期：护城河加固 = 把货损/返利/预报配方化 skill 工程化，形成"只会越用越准"的行业资产。
