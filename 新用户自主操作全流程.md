# 新用户自主操作全流程（从零到开通员工小程序权限）

> 适用对象：刚接触 Hergent 的经销商老板 / 管理员
> 目标：不依赖技术人员，按步骤自助完成「注册 → 建公司 → 加员工 → 开通小程序权限」
> 涉及页面：登录页、工作台、数据补录、设置

---

## 流程总览（按实际操作顺序递进）

```
① 账号注册  ──（注册时填写的公司名即自动建成你的专属组织）──▶ ② 公司就绪
       │
       ▼
③ 添加员工档案（数据补录 → 员工档案）
       │
       ▼
④ 为员工开通小程序权限 = 两步：开账号（手机号+密码） + 分配门店
```

---

## 环节一：自主完成账号注册

**操作入口**
- 打开 Hergent 网页端 → 登录页 → 顶部「免费注册」标签（默认是「登录」，点一下切换）。

**前置条件**
- 有一部能收短信的手机号（用于登录账号，也是找回密码的凭证）。
- 想好的「公司名」（会直接变成你的组织名称，见环节二）。

**关键操作动作**
1. 点「免费注册」。
2. 填写三项：
   - **公司名**（如：张记乳品）—— 至少 2 个字，将作为你的专属组织名称。
   - **手机号** —— 你的登录账号基础。
   - **密码** —— 至少 8 位。
3. 点「注册并开始使用」。
4. （可选）弹出「上传第一份数据」引导：从舟谱/系统导出 Excel 拖入，AI 自动识别入库；不想现在传可点「稍后再说」。

**完成后系统预期状态**
- 账号注册成功，并**自动以 boss（公司管理员）身份登录**，直接进入工作台。
- 系统已为你**自动创建专属经营空间（组织/租户）**，并把你设为该空间唯一管理员。
- 顶部/侧边栏出现你公司的功能菜单（预报、数据补录、设置等）。

---

## 环节二：确认 / 完善你的公司（组织）

> ⚠️ **重要事实**：在环节一注册时填写的「公司名」，**系统已经自动帮你建好了对应的公司（组织/租户）**。所以这一步通常无需"新建"，而是确认与完善。

**操作入口**
- 左侧侧边栏「设置」→ 公司信息（或「组织设置」）区域。

**前置条件**
- 已完成环节一注册并处于登录状态（你是 boss）。

**关键操作动作**
1. 进入「设置」查看公司名称、专属空间是否已生成。
2. 如需修改公司显示名、联系人、联系电话等资料，在对应输入框更新并保存。
3. 确认你本人在「成员/管理员」列表里、且角色为 boss。

**完成后系统预期状态**
- 公司信息完善、可见。
- 你拥有该组织的全部管理权限（建员工、开小程序账号、分配门店、查看经营分析）。
- 后续环节三、四都在这个组织空间内进行。

> 注：普通注册用户暂无"再建第二个独立公司"的自助入口；一个注册账号对应一个专属组织。如需多组织，请联系平台方。

---

## 环节三：添加 / 创建员工账号（员工档案）

**操作入口**
- 左侧侧边栏「数据补录」→ 顶部 Tab「员工档案」。
  （若侧边栏没有「数据补录」，可进「设置」→ 点「去补录数据」按钮直达。）

**前置条件**
- 已登录且为 boss。
- 公司（环节二）已就绪。

**关键操作动作**
1. 在「员工档案」页点「新增员工」。
2. 填**员工姓名**（必填），可选填岗位。
3. 点保存 → 该员工出现在「在职员工」列表中。
4. （可选）逐条点员工行的「补录」填写底薪/社保/银行账号，供工资与货损计算使用。
5. （可选）若员工较多，把 `姓名 | 工号 | 岗位 | 底薪 | 社保基数 | 银行账号` 整理成 Excel，交由 AI 批量导入。

**完成后系统预期状态**
- 员工档案创建成功，显示在列表中。
- 此时员工**仅有内部档案，尚无小程序登录权限**（列表「小程序账号」列显示"开账号"）。
- 下一步环节四才真正赋予小程序使用权。

---

## 环节四：为员工分配小程序使用权限

> 小程序指「预报小程序」（员工手机端填报预报用）。授权 = **两步**：先开账号，再分配门店。两步都在「员工档案」列表完成。

**操作入口**
- 「数据补录」→「员工档案」→ 找到目标员工所在行 → 行内两个按钮：「开账号」「门店」。

**前置条件**
- 该员工已在环节三建好档案（列表中可见）。
- 你为 boss。
- （门店分配前）公司已录入客户/门店（ERP 客户档案里有 type=客户 的记录；若还没有，先去 ERP 录入客户）。

### 动作 A：开通小程序账号
1. 点该员工行的「开账号」。
2. 弹窗填写：
   - **手机号 / 账号**（如 13800000001）—— 员工登录小程序用的账号。
   - **初始密码**（至少 4 位）—— 员工首次登录用，可后续自行修改。
3. 点「开通」。

**完成后状态**：列表「小程序账号」列变为「已开通 13xxxxxxxxx」；员工已可用该手机号+密码登录预报小程序。

### 动作 B：分配门店（权限范围）
1. 点该员工行的「门店」。
2. 在弹窗中勾选该员工**允许看到并填报**的客户/门店（可多选）。
3. 点「保存分配」。

**完成后状态**：员工登录小程序后，**只能看到并填报被勾选的这些门店**，其余门店对其不可见。可在同弹窗再次调整。

---

## 完整闭环后的预期效果

| 角色 | 能做什么 |
|------|----------|
| **你（boss）** | 登录网页端管理公司、建员工、开账号、分配门店、看全部经营分析 |
| **员工** | 用手机号+密码登录预报小程序，仅填报你授权的门店，提交预报 |

---

## 常见问题

**Q1：注册时忘了传数据，后面还能传吗？**
能。登录后进「设置 → 去补录数据」或各模块内的导入入口，随时可上传 Excel。

**Q2：员工离职了怎么办？**
在「员工档案」列表对该员工操作：可停其小程序账号 / 取消门店授权，档案保留不影响历史。

**Q3：一个员工能管多家门店吗？**
能。动作 B「门店」可勾选多家，保存后即生效。

**Q4：员工登录小程序提示无权限 / 看不到门店？**
检查两件事：①该员工是否已「开账号」（动作 A）；②是否已「分配门店」且勾选了对应门店（动作 B）。两步都完成才可用。

**Q5：公司名注册填错了能改吗？**
能。进「设置 → 公司信息」修改显示名；如涉及登录子域名等底层标识，必要时联系平台方协助。
