# 招投标雷达「长时间未显示新增」排查结论

> 排查时间：2026-09-11 21:15（GMT+8）｜排查人：Senior Developer
> 结论先行：**既不是系统故障，也不是上游真实无数据——雷达系统运转正常，每日都在正常抓取并入库新招投标信息（最新数据 2026-09-10）。用户「看不到新增」是 UI 语义缺陷导致的观感问题，非数据/系统问题。**

---

## 一、排查链路与证据

| # | 检查项 | 方法 | 结果 |
|---|--------|------|------|
| 1 | 调度是否运行 | `crontab -l` + DB mtime | ✅ `10 7 * * * cd /opt/hergent-erp && python3 bid_radar_collect.py`；DB 文件 mtime = **2026-09-11 07:11**（今日已跑） |
| 2 | 抓取是否成功 | 采集库统计 + 日志 | ✅ 430 条原始记录（去重后 282 条配送类商机），覆盖 **全部 3 个数据源**；日志无报错 |
| 3 | 数据是否新鲜 | `SELECT max(date)` | ✅ 最新发布日 **2026-09-10**（昨天）；区间 2026-08-04 ~ 2026-09-10 |
| 4 | 入库节奏 | 按日发布日计数 | ✅ 近日常量稳定：09-07 30、09-08 29、09-09 34、09-10 27（仅 09-05/09-06 周末仅 2 条，属正常波动） |
| 5 | 路径一致性 | 比对 collector / router / env | ✅ 无 `BID_RADAR_DB` 环境变量覆盖；扁平部署下 router 解析到 `/opt/hergent-erp/bid_radar.db`（即采集库同文件），路径一致 |
| 6 | 后端存活 | `ps` + health | ✅ `server.py` 运行中（pid 2126280），`/api/health` 返回 ok，:8700 监听 |
| 7 | **线上接口实测** | demo-login → `/api/bid-radar` | ✅ `success:true, total:282`；来源分布 全国公共资源 168 / 军队采购网 113 / 中国政府采购网 74；最新条目 2026-09-10 |
| 8 | 是否有缓存层 | nginx `proxy_cache` | ✅ `/api/` 仅 `proxy_pass`，**无缓存**，浏览器每次拿到的都是实时数据 |
| 9 | 前端是否陈旧 | 部署 chunk 校验 | ✅ `BidRadar-iAW_1Ljr.js`（今日 21:17 构建）含当前 KPI 文案，与源码一致，非陈旧构建 |

**结论：采集 → 入库 → 接口 → 前端 全链路健康。系统每日正常接收并展示新招投标信息。**

---

## 二、真正的根因：UI 语义缺陷（非故障）

雷达每天 07:10 抓取，但抓取的是「截至今早 07:10 之前发布」的公告。由此产生两个**结构性**观感问题：

1. **「今日新增」KPI 恒为 0**
   前端 `todayCount` 用浏览器当天日期（`TODAY`）去查 `date_from=TODAY`。但当天（如 09-11）的公告几乎都在工作时段才发布，07:10 那次抓取抓不到，要等**明天**的抓取才进库；而明天你再看时「今天」已是 09-12，09-11 的数据又变成了「昨天」。
   → 结果：**你每次打开页面，「今日新增」几乎都是 0**，看起来像「没更新」。

2. **每条的「新」角标几乎永不出现**
   `isNew(it)` = `it.date === TODAY`。同上原因，白天打开时没有任何一条的发布日等于「今天」，红色「新」角标**永远不会亮**，进一步强化了「没新东西」的错觉。

3. **周末低谷被误读为「停更」**
   09-05（周六）、09-06（周日）各仅 2 条，若用户习惯周末查看，列表几乎不动，易被误判为「卡住」。

> 简言之：数据在每天 07:10 之后就已经更新到「昨天」，但界面把所有「按当天日期」的指标都做成 0/空，于是「有更新」被展示成了「没更新」。

---

## 三、用户自助核验（30 秒确认）

打开招投标雷达页面，看两处即可验证系统其实在正常更新：

- **累计商机** KPI 应显示 **282**（去重后配送类商机）；
- 列表**置顶**的发布日应为 **2026-09-10**（昨天），来源含「全国公共资源交易平台 / 军队采购网 / 中国政府采购网」三种。

若以上成立，则系统完全正常，只是「今日新增」这一项因上述语义问题恒为 0。

---

## 四、建议修复（非必须，按需推进）

| 项 | 现状 | 建议改法 |
|----|------|----------|
| 「今日新增」KPI | 按 `TODAY` 查，恒为 0 | 改为「近 3 日新增」（`date_from = 今天-2`）或「昨日新增」，贴合 07:10 抓取节奏 |
| 行内「新」角标 | `date === TODAY` 永不命中 | 改为「发布于近 24h」（`date >= 今天-1`）或「自上次打开后新增」 |
| 数据时效性提示 | 仅有 `fetched_at`（接口调用时刻） | 增加「数据更新于：每日 07:10 自动抓取 / 最新公告 2026-09-10」，明确时效 |

以上均为前端 `BidRadar.vue` 的小改动，不动后端与采集逻辑。

---

## 五、最终判定

- ❌ **不是系统故障**：调度、抓取、入库、接口、前端构建全部正常，线上接口实测返回最新数据。
- ❌ **不是上游真实无数据**：3 个数据源每日持续有新公告入库（最新 2026-09-10，日均 20~47 条）。
- ✅ **是 UI 展示语义问题**：「按当天日期」的新增指标与角标在白天恒为空，造成「长时间无新增」的观感；属前端体验缺陷，非数据/系统缺陷。
